# 2026-07-31

## 한 줄
> **마감 시점 동결을 구현했고, 그 과정에서 "회귀 테스트가 실제로는 아무것도 검증하지 않고 있었다"는 것을 발견한 날.**

## 이런 일이 있었다
- `03:30` Gemini 독립 예측(Phase A) + 기계 채점(Phase B) 신설
- `03:44` 예측 첫 저장 1건(`平塚 2경주`) — 🔴 **이후 하루 종일 0건이 된다**
- `04:42` 경기장 별칭 2줄 추가(`平塚`·`防府`) → 표준키 커버 12/13 → **13/13**
- `05:00` JMA 아메다스 바람 수집 배선. 57곳 전부 20km 이내(중앙 4.3km)
- `05:16` 마감 시점 단일 동결 구현. 무방비 7필드 보호 + 3단 폴백
- `07:16` 🔴 **회귀 테스트 4개가 fixture만 읽어 영원히 초록이 안 된다는 것을 발견**
- `07:26` `closed` 행이 "붙인 뒤 지워진다"는 것을 특정 → 교체를 병합으로 수정
- `07:39` 과거 492건 소급 복구 실행(백업 492개 선행)
- `07:59` 🔴 **`gemini_forecast.py` 모듈 로드 실패로 예측이 하루 종일 꺼져 있었음을 발견**
- `08:03` 날짜별 작업 기록 체계 신설

## 이래서 이렇게 했다

### ① 마감 시점 단일 동결
- **문제**: 나고야 9R 이 `readonly=True` 인데도 19:41 에 `keyHorses [12,1,2]→[13,4,2]`,
  `strongSignals 3→0` 으로 바뀌었다. `displayedCombos` 만 불변이었다.
- **원인**: readonly 보호 블록이 **5개 필드만** 지켰다. `keyHorses`·`summary`·`strong_signals`·
  `horses`·`elimination`·`compression_pattern`·`third_place_hunt` 는 **목록에 아예 없었다.**
  `displayedCombos` 만 `corePicks` 안이라 우연히 살아남은 것이었다.
- **조치**: `_FREEZE_FIELDS` 신설(기존 5개는 무변경·추가만). `readonly` 를 거는 그 순간
  `an`(마감 후 재계산값)이 아니라 **확정본에서 복원**(3단 폴백). 복원 실패 시 **잠그지 않는다**.
- **결과**: 529개 중 492개(93.0%) 유실 확인, 복원 시뮬레이션 **492/492(100%)** 성공.

### ② `closed` 행이 만들어진 그 저장에서 폐기되던 것
- **문제**: 3단 폴백 ①(`closed_row`)이 44.4% 에 머물렀다. 절반 이상이 ③으로 떨어졌다.
- **원인**: `rec_history = list(prev_hist)` 는 **복사본**이라 '🔒 마감 확정' 행을 append 해도
  `doc` 은 안 바뀐다. 그런데 readonly 블록이 `log["recommendation_history"] = _doc.get(...)` 로
  **디스크 원본을 다시 씌워** 그 행이 곧바로 사라졌다.
  미부착 294건 중 **293건(99.7%)** 이 부착 조건을 전부 충족한 상태였다 — 조건이 아니라 덮어쓰기.
- **조치**: **교체 → 병합.** 디스크 원본을 기준으로 두고(기존 보호 유지) 원본에 없는
  `closed` 행만 얹는다. 중복은 `(time, closed)` 키로 차단.
- **결과**: 실사례 5건에서 원본보존·closed추가·재저장중복0 **동시 충족**. 기존 보호도 무손상
  (이력을 비우든 1행만 남기든 원본 20행 유지).

### ③ 예측이 1건에서 멈춘 것
- **문제**: 129경주 예정인데 예측 저장이 1건.
- **원인**: `gemini_forecast.py` 가 **문법 오류로 import 실패**(`unterminated string literal`).
  서버는 `except` 로 삼켜 `_gforecast = None` 인 채 돌았다. 로그 **1줄**뿐이라 안 보였다.
  🔴 근본 원인은 **`SYNTAX_TARGETS` 에 `gemini_forecast.py` 가 없었던 것** — 목록 누락이 사각지대.
- **조치**: 목록에 추가 + 로드 실패를 배너로 크게 출력 + 부팅 배너에 예측 상태 상시 노출.
- **결과**: 모듈 복구 확인. 다만 **개최 전이라 실데이터 검증은 미완**.

### ④ 아메다스 바람
- **문제**: 경륜은 맞바람이면 선행이 불리한데 바람을 전혀 몰랐다.
- **원인**: `WEATHER_VENUES` 에 경륜장이 0곳이고 API 키도 없어 전부 더미였다.
- **조치**: JMA 아메다스(키 불필요) 배선. 표준키 → 최근접 풍속 관측지점 매핑 자동 생성.
- **결과**: **57/57곳 20km 이내**(중앙 4.3km·최대 13.7km). 실호출 12/12 성공.
  프롬프트 자동 포함 확인(값 있으면 「제공되지 않는 정보」에서 빠짐).

## 틀렸던 것

| 추정 | 실제 | 정정 |
|---|---|---|
| 동결하면 회귀 테스트 4개가 초록이 된다 (**두 번 단언**) | fixture 만 읽어 **구조적으로 영원히 초록 불가** | 원칙 12 신설. `run_freeze_behavior.py` 를 별도 신설해 실제 함수 호출 |
| 유실 규모 235개 | **492개** | 앞 계산은 `closed` 행 보유분만 셌다. 분모가 좁았다 |
| ② 모리오카는 수집 부족이라 동결해도 빈다 | 마감 시점에 `[7,9,8]` 이 **있었다** | "데이터 부족" summary 자체가 새벽 02:11 재분석의 산물. 원인과 결과가 뒤집혀 보였다 |
| `closed` 미부착은 마감 후 저장이 없어서 | 붙인 뒤 **덮어쓰기로 지워진다**(99.7%가 조건 충족) | 조건이 아니라 경로 문제 |
| 미부착은 경륜이 많을 것 | 경륜 57.5% · 경마 52.7% — **둘 다 절반** | 종목 무관. 구조적 결함 |
| ②폴백 보강은 ③을 절반 줄인다 | **96.9% 커버 가능** | 다만 정확도가 아니라 출처 명시성 이득(③이 중앙 T-1.4분으로 더 가깝다) |
| 예측 1건은 `GEMINI_FORECAST_ENABLED` 가 꺼져서 | **모듈 로드 실패** | `.env` 는 `=1` 이고 정상 로드됐다 |
| 부산 3경주는 수집이 멈춰서 | 수집은 정상(스냅샷 9개) · **분석이 안 돌았다** | 마감 5분 전부터 배당을 받고도 추천 미갱신 |
| T-10분 초과 5건 | **4건** | 앞은 마감시각 확보 249건 기준, 뒤는 복구 대상 492건 기준 |
| Phase C(복기)는 이미 있을 수도 | **미구현** | 이번에 신설 |

## 결정
- **동결 범위**: 기존 5개 + 신규 7개. 허용 목록(`result`·`hit`·`review`·`profit`)은 **무변경**
- **복원 실패 시 잠그지 않는다** — 틀린 값을 굳히는 것보다 안 잠긴 채로 표시하는 게 낫다
- **`pre_close_row` 257건은 top-level 을 덮어쓰지 않는다** — 마감 시점 값이 아니므로 `frozen` 에만
- **`run_freeze_regression.py` 는 `EXPECTED_FAIL` 영구 유지** — 외부 앵커로서 보존, 초록은 불가
- **코드 리뷰 Gemini 는 재개하지 않는다**
- **부산형은 미해결로 남긴다** — 한국경마 readonly 표본이 1건이라 0.2%는 믿을 수 없다
- **보류 유지**: ⓐ·ⓑ·ⓒ·`key_horses` A안·`_apply_pace_analysis`·Phase 4·서버 바인딩·
  조합 확대·축 표기·네거티브 필터·`timeline_snapshot` keyHorses·부산형

## 숫자
<!-- AUTO:BEGIN -->
<!-- 이 구간은 tools/build_daily_log.py 가 생성한다. 손으로 고치지 말 것. -->

### 체크리스트
- **0/22** — 0/22 충족 · 미충족 3 · 미측정/미구현 19

### 그날 실적  ⚠ 분모 = 그날 분석 로그 0건
_경주 없음(또는 개최 전)_

### 주요 지표
| 지표 | 값 |
|---|---|
| 동결 시도 | 0건 |
| 아메다스 관측 | 0건 |
| 예측 저장 | 1건 |
| ├ 채점 완료 | 0 |
| └ 복기 완료 | 0 |

### 그날 커밋 (13건)
- `a749e874` 07:59 — fix(forecast): 침묵 실패 가시화 + Phase C(복기) 신설
- `7bfa4f97` 07:47 — fix(freeze): 유실 지표 분류 분리 + 체크리스트 D6(동결 성공률) 추가
- `d872f677` 07:26 — fix(freeze): closed 행을 교체가 아닌 병합으로 — 기존 보호 유지
- `8ca4fbcf` 07:16 — docs: 원칙 12(회귀 테스트는 실제 함수를 호출해야 한다) + 동결 실측 기록
- `a3843394` 05:16 — fix(freeze): 마감 시점 단일 동결 — 무방비 7필드 보호 + 3단 폴백 복원
- `ff258ec7` 05:00 — feat(weather): JMA 아메다스 바람 수집·배선 (57곳 전부 20km 이내)
- `029f0192` 04:42 — fix: 경기장 별칭 2줄 추가 — 平塚(히라츠카)·防府(호후)
- `d768414f` 04:35 — docs: 표준키 실측(12/13 커버) + 아메다스 거리 검증(전 경기장 10km 이내)
- `30fedd78` 04:06 — docs: 정정 2건(총평 활용·주로 분모) + 날씨 소스 조사(JMA 아메다스 권고) + 조교평가 종결
- `03b5d891` 03:57 — feat: 예측 프롬프트 확장 — 재료 자동 포함 + '제공되지 않는 정보' 섹션
- `bb032a1c` 03:46 — feat: Gemini 예측 Phase A/B app.py 배선 + 판정선 고정 + 체크리스트 21항목
- `fd7d0f3c` 03:41 — feat: 경륜 개최별성적·선수속성 저장 배선 (② 파싱되는데 탈락 유형)
- `80b82b33` 03:30 — feat: Gemini 독립 예측(Phase A) + 기계 채점(Phase B) · flow_table 수치 정정

_생성 2026-07-31 08:03:33_
<!-- AUTO:END -->

# 출력 — 화면 API·카카오

> 🤖 **자동 생성** — `python tools/build_codemap.py` · 2026-08-12 08:36:46
> ⚠ 손으로 고치지 마세요. 다음 생성 때 덮어써집니다.
> ⚠ **AST 한계**: `getattr()` 동적 호출 · `importlib` 경유 · 문자열 디스패치는 잡지 못합니다
>   (`gemini_reviewer`·`health_check` 가 실제로 `importlib` 경유입니다).

총 **246개** 함수

### `access_policy()`
- **위치**: `app.py:194` (194~201)
- **설명**: 공개 프론트(bmed-public)가 어떤 데이터가 공개/프리미엄인지 알 수 있게 등급표 노출. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `index()`
- **위치**: `app.py:672` (672~673)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 2)**: `_class_grade_rank`(26253) · `_weather_dummy`(26888)

### `health()`
- **위치**: `app.py:677` (677~684)
- **설명**: 공개 서버 상태 확인용 — status/version 은 외부 모니터링(bmed-public)용 표준 필드. *(주석(본문))*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `api_usage()`
- **위치**: `app.py:688` (688~710)
- **설명**: API 토큰 사용량/예상 비용 일별 집계 조회. ?days=N 으로 최근 N일만(기본 전체). *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —  ⚠ 위험목록 함수 → 전수 나열

### `extract_jockey()`
- **위치**: `app.py:726` (726~728)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `extract_race()`
- **위치**: `app.py:762` (762~764)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `extract_training()`
- **위치**: `app.py:781` (781~784)
- **설명**: 출주표 하단 '조교훈련 및 종합분석' 표 → 마번별 레이팅/조교사/평가기호 *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `extract_results()`
- **위치**: `app.py:788` (788~799)
- **설명**: 경주 결과(착순)표 이미지 → 경주별 착순 *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `result_ocr()`
- **위치**: `app.py:838` (838~876)
- **설명**: 경주결과 화면 캡쳐 이미지 → 결과표(여러 경주) 판독 → 분석경주 자동매칭·등록·학습. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `detect()`
- **위치**: `app.py:901` (901~904)
- **설명**: 여러 페이지 썸네일을 순서대로 받아 race/jockey/other 분류 *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `_korea_deterministic_overlay(race, jstats)`
- **위치**: `app.py:924` (924~940)
- **설명**: pastRaces 가 추출된 경우에만 결정론 점수·각질·변화감지·2착패턴(삼복승필수)을 산출. *(docstring)*
- **분류 근거**: 도메인 2차 (kakao|notify|send|render|ht
- **호출됨(Caller · 1)**: `_do_analyze`(991)

### `analyze()`
- **위치**: `app.py:1068` (1068~1070)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `analyze_japan()`
- **위치**: `app.py:1074` (1074~1097)
- **설명**: 일본경마: [전적표]+[배당판] 두 이미지를 한 번에 병합 분석 *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `analyze_odds()`
- **위치**: `app.py:1101` (1101~1127)
- **설명**: 배당판 이미지 → 마번별 배당 + 이상배당(급락/급등) 감지 *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `analyze_odds_triple()`
- **위치**: `app.py:1169` (1169~1192)
- **설명**: 복승/쌍복/삼복승 배당판 3종을 한 번에 판독하고 베팅종류 간 불일치(이상)를 감지. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `odds_snapshot()`
- **위치**: `app.py:1387` (1387~1396)
- **설명**: 배당 한 라운드 기록: {raceKey, odds:{마번:배당}} → {snaps, series} *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `results_auto()`
- **위치**: `app.py:1400` (1400~1464)
- **설명**: 확장 프로그램: keiba 결과페이지에서 자동 추출한 착순 저장. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `results_get()`
- **위치**: `app.py:1468` (1468~1472)
- **설명**: 저장된 착순 조회: {raceKey} → {results, top3} (없으면 빈 값) *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `triple_ingest()`
- **위치**: `app.py:2473` (2473~2496)
- **설명**: 확장 [전체 자동 수집]: {raceKey, quinella[], exacta[], trio[]} 저장 → {ok, counts} *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `triple_reset()`
- **위치**: `app.py:3268` (3268~3288)
- **설명**: [🔄 새 경주 시작] 활성 3종 배당(triple_store)을 비워 새 경주로 전환. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `starters_reset()`
- **위치**: `app.py:3292` (3292~3298)
- **설명**: [잔존마 방어·3번] 경주 전환 시 이전 경주 전적(starters) 정리. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `race_pin()`
- **위치**: `app.py:3377` (3377~3392)
- **설명**: [경주 고정] GET → 현재 고정 경주 조회. POST {raceKey} → 고정 / POST {clear:true} → 해제. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `triple_latest()`
- **위치**: `app.py:3396` (3396~3466)
- **설명**: 최근(또는 지정 raceKey) 3종 배당 조회 → {raceKey, quinella, exacta, trio, sport}. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `anomaly_feed()`
- **위치**: `app.py:3540` (3540~3555)
- **설명**: [이상감지 누적] 경주별 스냅샷에서 감지된 이상을 시간순·중복제거로 누적 반환. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `learning_near_miss()`
- **위치**: `app.py:3559` (3559~3571)
- **설명**: [2·3번] 4착 near-miss 케이스 + 4착 빈번(2회+) 말 목록(다음 경주 삼복승 보험픽 우선 고려). *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `signal_types_stats()`
- **위치**: `app.py:3575` (3575~3581)
- **설명**: [강한 신호 8유형 5번] 유형별 발생/적중/적중률(입상 기준) — 적중률 높은 유형 가중치 상향 근거. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `compression_stats()`
- **위치**: `app.py:3585` (3585~3590)
- **설명**: [저배당 압축 패턴 4번] 압축 패턴(4배↓2두+/5배↓3두+) 발생 시 실제 복승 적중률. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `third_place_stats()`
- **위치**: `app.py:3594` (3594~3600)
- **설명**: [배당 3착 자동 발굴 5번] 신호 유형별 고배당 3착 후보의 입상·정확3착 적중률. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `pattern_confidence()`
- **위치**: `app.py:3604` (3604~3606)
- **설명**: [복기 학습 재설계 2·4번] 신호 유형별 발생/적중/적중률 + 통계 신뢰도(표본 50회 게이팅). 공식 수정 없음. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `thresholds_optimize()`
- **위치**: `app.py:3610` (3610~3612)
- **설명**: [기준치 도출 2·3·5번] 누적 데이터로 신호 기준치별 적중률·최적 기준치 추천(자동 적용 안 함). *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `thresholds_config()`
- **위치**: `app.py:3616` (3616~3619)
- **설명**: [기준치 도출 4번] 현재 적용된 기준치 + 변경 이력. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `thresholds_apply()`
- **위치**: `app.py:3623` (3623~3643)
- **설명**: [기준치 도출 4번] 사람이 승인한 기준치를 config에 반영(이전값 자동 백업). body{key,value} 또는 body{current:{...}}. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `high_odds_companion_stats()`
- **위치**: `app.py:3647` (3647~3651)
- **설명**: [고배당 동반 패턴 4번] 고배당(7배+) 1착 경주 중 3착 내 다른 고배당 포함 비율. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `after_close_cases()`
- **위치**: `app.py:3655` (3655~3660)
- **설명**: [4번] 마감(T-0) 후 감지되어 베팅 반영 불가했던 신호 케이스 목록 → 수집 간격 단축 필요성 근거. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `dansung_cases()`
- **위치**: `app.py:3664` (3664~3670)
- **설명**: [단통 학습] 복승 최저 ≤1.5배 쏠림 경주 케이스 + 복병(특별) 적중률 통계. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `snapshot_save()`
- **위치**: `app.py:3994` (3994~4039)
- **설명**: [배당판 스냅샷 저장] 확장이 캡처한 base64 PNG(배당판+오버레이+패널·워터마크 포함)를 data/snapshots/<파일명>.png 로 저장. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `snapshot_list()`
- **위치**: `app.py:4043` (4043~4068)
- **설명**: [스냅샷 갤러리] 저장 스냅샷 목록(메타 + 실제결과 비교). 최신순. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `snapshot_get(filename)`
- **위치**: `app.py:4072` (4072~4077)
- **설명**: [스냅샷 이미지] 저장된 PNG 반환(경로조작 방어). *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `snapshot_compare_api()`
- **위치**: `app.py:4081` (4081~4115)
- **설명**: [3단계 비교분석] raceKey 지정 시 그 경주 비교(?raceKey=...), 없으면 저장된 전 비교 목록(최신순). *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `snapshot_health_api()`
- **위치**: `app.py:4119` (4119~4169)
- **설명**: 🔴 [2026-08-01 · 승인 D안] 배당판 스냅샷 유입 진단 — **완전 읽기 전용**. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `snapshot_timing_api()`
- **위치**: `app.py:4173` (4173~4181)
- **설명**: [타이밍 학습 통계] T-10 추천 vs T-2 추천 누적 적중률 — 어느 시점 추천이 더 정확한지. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `after_close_stats_api()`
- **위치**: `app.py:4185` (4185~4187)
- **설명**: [3번] 마감 후 대급락(50%+) → 실제 입상률 통계(패턴 신뢰도 측정). *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `learning_signal_lessons()`
- **위치**: `app.py:4191` (4191~4201)
- **설명**: 초과급락 신호말이 입상했으나 노이즈 판정으로 추천 누락된 사례(집중신호 오판 학습). *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `current_race()`
- **위치**: `app.py:4205` (4205~4295)
- **설명**: 확장이 마지막으로 수집한 '현재 경주' 반환 → {raceKey, updatedAt, counts}. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `_deadline_phase_label(mb, after_close)`
- **위치**: `app.py:5726` (5726~5734)
- **설명**: 이상감지 신호의 유효 시점 라벨. 마감 후=회색 표시용. *(docstring)*
- **분류 근거**: 도메인 2차 (kakao|notify|send|render|ht
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `extract_japan()`
- **위치**: `app.py:14344` (14344~14395)
- **설명**: [출마표2] 전적 + (선택)배당을 함께 받아 저장하고 통합 분석 반환. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `korea_form()`
- **위치**: `app.py:14399` (14399~14436)
- **설명**: [한국경마] PDF Vision 전적을 STARTERS_STORE에 저장(마명·기수 한글 그대로, 전적점수 포함). *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `triple_match()`
- **위치**: `app.py:14481` (14481~14524)
- **설명**: [한국경마 자동연결] 경마장·경주번호로 수집된 배당 raceKey를 찾아 통합분석 반환. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `_pub_horse_cards(an, role, nos)`
- **위치**: `app.py:14997` (14997~15032)
- **설명**: 말별 신호 카드 — 등급·역할·태그·부연설명. *(docstring)*
- **분류 근거**: 도메인 2차 (kakao|notify|send|render|ht
- **호출됨(Caller · 1)**: `public_matrix`(15085)

### `public_races()`
- **위치**: `app.py:15056` (15056~15081)
- **설명**: [공개용] 현재 배당이 수집된(활성) 경주 목록 — 배당판 경주 선택용. 최신 갱신순. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `public_matrix(race_key)`
- **위치**: `app.py:15085` (15085~15128)
- **설명**: [공개용] 배당판 매트릭스 + 3단계 카드 UI 데이터. 읽기 전용(재계산·저장 없음). *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `triple_analyze()`
- **위치**: `app.py:15132` (15132~15335)
- **설명**: 규칙기반 즉시 분석(Claude 미사용): 급락·순위변동·쌍승역전·유력마·삼복승추천. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `signal_timeline_api()`
- **위치**: `app.py:18767` (18767~18775)
- **설명**: [3·4·5번] 이상감지 말 변경 이력 + 신호 안정화 + 최종 유효 신호 시점(경주별). *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `_race_card_timeline(rk)`
- **위치**: `app.py:19280` (19280~19425)
- **설명**: 카드용 신호 이력 1건 조립(읽기 전용). *(docstring)*
- **분류 근거**: 도메인 2차 (kakao|notify|send|render|ht
- **호출됨(Caller · 3)**: `race_card_timeline_api`(19429) · `_race_full`(19729) · `_rec_trail_for_detail`(35762)  ⚠ 위험목록 함수 → 전수 나열

### `race_card_timeline_api()`
- **위치**: `app.py:19429` (19429~19445)
- **설명**: [경주 카드 신호 이력] 완전 읽기 전용. ?raceKey=... (&view=member 면 회원용 축약) *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `race_full_api()`
- **위치**: `app.py:19883` (19883~19906)
- **설명**: [배당판 전체] 완전 읽기 전용. ?track=히라츠카&race=6[&date=2026-08-11] *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `next_api()`
- **위치**: `app.py:20003` (20003~20089)
- **설명**: [명령어 창구] POST 로 쌓고 GET 으로 읽는다. 🔴 로컬 전용. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `report_api()`
- **위치**: `app.py:20093` (20093~20118)
- **설명**: [보고 조회] 완전 읽기 전용. ?n=1 (기본 1 · 0 이면 전체) *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `race_odds_api()`
- **위치**: `app.py:20122` (20122~20172)
- **설명**: [경주 지정 배당] 완전 읽기 전용. ?track=구마모토&race=3[&date=...] *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `race_report_list()`
- **위치**: `app.py:20176` (20176~20191)
- **설명**: [신규 1번] 저장된 경주 재현 리포트 목록 → [{slug,race,date,hit,hit_type,odds,win_tags}]. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `race_report_get()`
- **위치**: `app.py:20195` (20195~20212)
- **설명**: [신규 1·3번] 단일 리포트 전체(추천근거·타임라인·신뢰도 분해). query: slug 또는 raceKey. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `highlights_list()`
- **위치**: `app.py:20216` (20216~20223)
- **설명**: [신규 2번] 고배당 명예의 전당(highlight_wins.json) — 최신순. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —  ⚠ 위험목록 함수 → 전수 나열

### `shadow_trifecta_ep()`
- **위치**: `app.py:20227` (20227~20309)
- **설명**: [섀도우 삼복승 성적 (2026-07-29)] "걸었다면 어땠나"를 실전 데이터로 누적 집계. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `gemini_findings_ep()`
- **위치**: `app.py:20313` (20313~20386)
- **설명**: [Gemini 지적 집계 (2026-07-29)] logs/gemini_review/*.json 의 logic_findings 를 함수별로 집계. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `alerts_list()`
- **위치**: `app.py:20390` (20390~20406)
- **설명**: [신규 경고시스템] 경고 발생 경주 목록(최신순) → [{slug,race,date,count,result,anyHit}]. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `alerts_get()`
- **위치**: `app.py:20410` (20410~20425)
- **설명**: [신규 경고시스템] 단일 경주 경고 전체(odds_snapshot·경고말·판정). query: slug 또는 raceKey. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `odds_archive_list()`
- **위치**: `app.py:20429` (20429~20464)
- **설명**: [영구보존·3번] 저장된 모든 경주 배당 아카이브 목록(복기용) — .json + 압축(.json.gz) 모두 포함. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `odds_archive_get()`
- **위치**: `app.py:20468` (20468~20480)
- **설명**: [영구보존·3번] 단일 경주 배당 아카이브 전체 스냅샷(복기 타임라인). archive_snapshots 우선. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `odds_archive_compress()`
- **위치**: `app.py:20484` (20484~20492)
- **설명**: [영구보존·4번] 7일+ 경주 배당을 .gz 압축 보관(수동 트리거·데이터 삭제 없음). *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `history_list()`
- **위치**: `app.py:20496` (20496~20516)
- **설명**: 저장된 경주 히스토리 목록 → [{file,date,race,raceKey,snaps,hasResult}]. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `history_get()`
- **위치**: `app.py:20520` (20520~20531)
- **설명**: {raceKey|file} → 히스토리 전체(타임라인·스냅샷별 이상감지). *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `recommend_manual()`
- **위치**: `app.py:20668` (20668~20719)
- **설명**: [수동 추천 저장] 라이브 배당 수집이 없어(스냅샷 0) 추천이 자동 생성되지 않는 경주에도 *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `_success_report_text(rk, top3, hit_combos, timelines)`
- **위치**: `app.py:22038` (22038~22066)
- **설명**: [복기 UI 통합] 적중 경주 '왜 맞았는지' 텍스트 리포트(정답말 신호 근거 포함). *(docstring)*
- **분류 근거**: 도메인 2차 (kakao|notify|send|render|ht
- **호출됨(Caller · 1)**: `_failure_report`(21980)

### `_failure_report_text(rk, top3, recommended, was_hit, fail, timelines)`
- **위치**: `app.py:22069` (22069~22112)
- **설명**: [2·3번] 복기 리포트를 사람이 읽는 텍스트로. *(docstring)*
- **분류 근거**: 도메인 2차 (kakao|notify|send|render|ht
- **호출됨(Caller · 1)**: `_failure_report`(21980)

### `failure_report()`
- **위치**: `app.py:22529` (22529~22538)
- **설명**: [2·3번] 미적중 경주 복기 리포트(정답말 역추적 포함). ?raceKey= 또는 body{raceKey}. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `failure_stats()`
- **위치**: `app.py:22542` (22542~22544)
- **설명**: [5·6번] 복기 학습 대시보드(유형 분포·놓친 패턴 TOP·개선 규칙 전/후 적중률). *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `failure_rules()`
- **위치**: `app.py:22548` (22548~22551)
- **설명**: [7번] 실패에서 자동 학습된 규칙 목록. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `dark_horse_stats()`
- **위치**: `app.py:22556` (22556~22561)
- **설명**: 복병 신호별·배당구간별·종목별·순위별 적중률 통계(dark_horse_stats.json). 없으면 재계산. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `dark_horse_log()`
- **위치**: `app.py:22565` (22565~22586)
- **설명**: 복병 로그 목록. ?date=YYYYMMDD 로 날짜 필터(미지정=전체 최신순). *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `dark_horse_missed()`
- **위치**: `app.py:22590` (22590~22607)
- **설명**: 미감지(15배+ 고배당 입상인데 복병 미편성) 경주만 반환 — 사후분석 리스트. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `dark_horse_review()`
- **위치**: `app.py:22611` (22611~22628)
- **설명**: [역산·수동] 지정 경주(raceKey)의 결과로 복병 로그 재분석. body:{raceKey, top3?}. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `timeline_stats()`
- **위치**: `app.py:22633` (22633~22639)
- **설명**: T-7/T-5 확정 적중률·변경 시 적중률·변경마 우승률(timeline_stats.json). *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `timeline_snapshot_get(race_key)`
- **위치**: `app.py:22643` (22643~22648)
- **설명**: 경주별 타임 스냅샷(T-10~T+3) + 결과·분석 반환. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `_day_card_extra(rk)`
- **위치**: `app.py:22768` (22768~22841)
- **설명**: 날짜별 카드에 실을 보강 정보 — 판정 명단·적중 조합·확정배당·등급. 실패 시 {}(기존 동작 유지). *(docstring)*
- **분류 근거**: 도메인 2차 (kakao|notify|send|render|ht
- **호출됨(Caller · 1)**: `day_races`(22845)  ⚠ 위험목록 함수 → 전수 나열

### `day_races()`
- **위치**: `app.py:22845` (22845~23011)
- **설명**: [전날 경주 목록] ?date=YYYYMMDD → 그 날짜 경주 카드(결과·예상vs결과·적중·복병적중·T-5 스크린샷) + KPI 요약. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `kakao_queue()`
- **위치**: `app.py:23016` (23016~23034)
- **설명**: 카카오 알림 큐(발송 대기/이력) 최신순. ?limit=N. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `high_odds_review()`
- **위치**: `app.py:23069` (23069~23112)
- **설명**: [고배당 심층분석] 복승30배+/삼복승100배+ 경주 복기. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `hall_of_fame()`
- **위치**: `app.py:23116` (23116~23123)
- **설명**: [5번] 고배당 적중 명예의 전당(복승 30배+/삼복승 100배+) — 최신순 + 스토리·타임라인. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —  ⚠ 위험목록 함수 → 전수 나열

### `races_list()`
- **위치**: `app.py:23127` (23127~23157)
- **설명**: [2번] 결과 입력용 경주 목록(시간순). ?date=YYYY-MM-DD &pending=1(미입력만). *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `competitor_record()`
- **위치**: `app.py:23357` (23357~23370)
- **설명**: [경쟁분석] 저쪽 추천 저장·비교. body:{raceKey, picks:[[4,10],[7,10]], odds?:[..], note?}. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `competitor_list()`
- **위치**: `app.py:23374` (23374~23394)
- **설명**: [경쟁분석] 저장된 경주 목록(?date=YYYY-MM-DD 필터, 기본 전체). *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `competitor_get()`
- **위치**: `app.py:23398` (23398~23410)
- **설명**: [경쟁분석] 한 경주 상세(?raceKey=). *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `competitor_stats()`
- **위치**: `app.py:23414` (23414~23420)
- **설명**: [경쟁분석] 누적 비교 통계(적중률·축정확도·고배당·우리가 놓친 축 패턴). *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `history_record_result()`
- **위치**: `app.py:23424` (23424~23450)
- **설명**: 경주 결과 입력 → 히스토리에 결과 기록 + 자동학습 + 완전 저장(race_results). *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `review_save()`
- **위치**: `app.py:23526` (23526~23578)
- **설명**: [복기 저장·자유 코멘트] body: {raceKey, signals?, result:{1st,2nd,3rd}?, comment?, importance?(1~3), *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `review_list()`
- **위치**: `app.py:23605` (23605~23626)
- **설명**: [복기 저장] 저장된 복기 목록(최신순). *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `review_stats()`
- **위치**: `app.py:23630` (23630~23666)
- **설명**: [6번 통계] 내 코멘트 모아보기 — 최고중요 경주·키워드 통계·near_miss 패턴. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `review_get()`
- **위치**: `app.py:23670` (23670~23682)
- **설명**: [복기 저장] 복기 1건 전체 조회. ?race_id= 또는 ?raceKey= *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `race_results_list()`
- **위치**: `app.py:23686` (23686~23704)
- **설명**: [1번] 완전 저장된 경주 결과 목록(최신순) → 요약 리스트. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `race_results_get()`
- **위치**: `app.py:23708` (23708~23723)
- **설명**: [1번] 경주 결과 완전 파일 1건 조회. {race_id | raceKey}. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `race_results_missing()`
- **위치**: `app.py:23727` (23727~23729)
- **설명**: [3번] 당일 분석했으나 결과 미입력 경주 추적(누락 방지). *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `ai_training_status()`
- **위치**: `app.py:23733` (23733~23735)
- **설명**: [3번] AI 학습 데이터 현황 대시보드(수집/완전/품질/목표 진행률/예상 완료). *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `ai_training_list()`
- **위치**: `app.py:23739` (23739~23756)
- **설명**: [1번] AI 학습 데이터 목록(최신순·품질점수 포함). *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `ai_training_get()`
- **위치**: `app.py:23760` (23760~23775)
- **설명**: [1번] AI 학습 데이터 1건 조회. {race_id | raceKey}. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `daily_summary()`
- **위치**: `app.py:23779` (23779~23801)
- **설명**: [6번] 일별 자동 요약 조회/갱신. GET ?date=YYYY-MM-DD(없으면 오늘) · POST=강제 재생성. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `daily_learning()`
- **위치**: `app.py:23805` (23805~23835)
- **설명**: [학습일지] 일별 학습 일지 조회/생성. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `results_bulk()`
- **위치**: `app.py:24421` (24421~24440)
- **설명**: [일괄 결과 등록] 결과 페이지 전체를 한 번에 파싱→분석경주 자동매칭→적중판정·학습→요약. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `results_adjust()`
- **위치**: `app.py:24468` (24468~24501)
- **설명**: [보완#1] 일괄 등록 후 경주별 투자금/실수령 배당금 조정 → 저장된 학습 레코드 in-place 갱신. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `learning_stats()`
- **위치**: `app.py:24505` (24505~24508)
- **설명**: 누적 학습 통계 대시보드. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `learning_upset()`
- **위치**: `app.py:24512` (24512~24530)
- **설명**: [2번] 부진마 역전 학습 조회 → {patterns, condition_stats, summary}. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `stats_today()`
- **위치**: `app.py:24610` (24610~24660)
- **설명**: [오늘 결과 통계 대시보드] 오늘(또는 ?date=YYYY-MM-DD) 등록 경주 집계 → *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `learning_mid_high_odds()`
- **위치**: `app.py:24664` (24664~24672)
- **설명**: [5번] 중고배당 유력마(💎) 누적 적중률 조회 — 통계 탭 카드용. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `learning_signal_stats()`
- **위치**: `app.py:24676` (24676~24692)
- **설명**: [4번] 신호별 적중률(유력마 기반) 조회 → 통계 탭 '신호별 적중률' 카드용. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `learning_reversal_stats()`
- **위치**: `app.py:24696` (24696~24705)
- **설명**: [4번] 역배열 강도별 적중률 조회 + (POST) pattern_learning.json 재집계 갱신. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `patterns_discovered()`
- **위치**: `app.py:24709` (24709~24717)
- **설명**: [전체데이터·패턴발견] 적중 경주 공통점 자동 발견 결과 + 데이터 충분도. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `auto_status()`
- **위치**: `app.py:24727` (24727~24734)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `results_auto_status()`
- **위치**: `app.py:24747` (24747~24785)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `analysis_log_list()`
- **위치**: `app.py:24790` (24790~24819)
- **설명**: 저장된 분석 로그 목록(날짜별 정렬) → [{file,race_id,date,race,analyzed_at,snaps,hasResult}]. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `analysis_log_get()`
- **위치**: `app.py:24823` (24823~24834)
- **설명**: {file|raceKey} → 분석 로그 전체. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `analysis_log_memo()`
- **위치**: `app.py:24874` (24874~24896)
- **설명**: 복기 메모 저장 + 복기 표식: {file|raceKey, review} → review 저장 + reviewed=True 표식 *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `review_notes_list()`
- **위치**: `app.py:24900` (24900~24904)
- **설명**: 복기 메모 학습 코퍼스 목록(최신순) — 검색·복기 기억용. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `_keirin_style_label(ch)`
- **위치**: `app.py:25026` (25026~25030)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 도메인 2차 (kakao|notify|send|render|ht
- **호출됨(Caller · 1)**: `_keirin_parse_card`(25066)

### `keirin_card()`
- **위치**: `app.py:25445` (25445~25497)
- **설명**: 경륜 출마표(선수 전적) 수집·분석: {joCode,kaisaiBi,raceNo} 또는 {url} 또는 {html} *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `keirin_odds()`
- **위치**: `app.py:25675` (25675~25721)
- **설명**: [경륜 배당 직접조회] oddspark 복승(2車複)·쌍승(2車単) 배당을 서버가 직접 fetch·파싱해 *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `keirin_current()`
- **위치**: `app.py:25754` (25754~25791)
- **설명**: [경륜 현재경주 자동감지] 지금 발매중(다음 발주)인 경륜 경주를 반환 → 프론트 자동추적용. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `keiba_odds()`
- **위치**: `app.py:26024` (26024~26136)
- **설명**: [경마 서버 직접 수집] oddspark 지방경마 복승(馬連=6)·쌍승(馬単=5)을 서버가 직접 fetch·파싱해 *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `keiba_current()`
- **위치**: `app.py:26168` (26168~26198)
- **설명**: [경주 자동추종] 지정 경마장의 '현재 발매중 경주번호'를 oddspark에서 감지해 반환. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `keiba_schedule_ep()`
- **위치**: `app.py:26202` (26202~26208)
- **설명**: 그날 oddspark 지방경마 개최 경마장·코드 목록(디버그·프론트 확인용). *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `keiba_starters()`
- **위치**: `app.py:26619` (26619~26678)
- **설명**: [지방경마 출주표 전적] oddspark 出走表 + 전5경주(HorseDetail)를 서버가 직접 fetch·파싱해 *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `keiba_form()`
- **위치**: `app.py:26782` (26782~26791)
- **설명**: [방법 B·강제수집 엔드포인트] POST/GET {raceKey, raceDy?} → 서버가 raceKey 기반으로 oddspark 전적 즉시 수집. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `weather_current()`
- **위치**: `app.py:26996` (26996~27002)
- **설명**: 전체 경주장 현재 날씨·주로 상태. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `weather_venue(venue)`
- **위치**: `app.py:27006` (27006~27012)
- **설명**: 특정 경주장 날씨. ?force=1 로 캐시 무시 강제 갱신. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `kra_result()`
- **위치**: `app.py:27246` (27246~27263)
- **설명**: [KRA 결과 자동수집] rcTime 정렬로 착순 산출 → race_results 저장·learning 반영. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `kra_results_backfill()`
- **위치**: `app.py:27475` (27475~27484)
- **설명**: [KRA 결과 백필] 당일(또는 ?date=YYYY-MM-DD) 분석됐으나 결과 없는 한국 경주를 KRA로 자동 채움. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `_keiba_fetch_page(baba, ymd, rno, timeout)`
- **위치**: `app.py:27572` (27572~27590)
- **설명**: RaceMarkTable 페이지 HTML(디코드 완료) 또는 None. 확정분만 캐시. *(docstring)*
- **분류 근거**: 이름 규칙 (kakao|_page$|_api$|notify|send_)
- **호출됨(Caller · 2)**: `_keiba_result_top3`(27593) · `_keiba_result_payouts`(27602)

### `review_sweep_ep()`
- **위치**: `app.py:27776` (27776~27780)
- **설명**: [복기 분류] ?date=YYYY-MM-DD — 하루치 실패 유형 분류(계획서 v4 [1]) → review_stats.json. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `review_replay_ep()`
- **위치**: `app.py:27784` (27784~27789)
- **설명**: [리플레이] ?date=YYYY-MM-DD — 정책별 가상 성적(baseline/경마 신호게이트/저배당 삼복승). *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `audit_cleanup_strays()`
- **위치**: `app.py:27793` (27793~27816)
- **설명**: [정합 스윕 후속 (2026-07-22)] 날짜 미지정 재학습이 '오늘' 파일로 잘못 저장한 타일자 경주 잔재를 *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `audit_results_ep()`
- **위치**: `app.py:27820` (27820~27828)
- **설명**: [정합 스윕] ?date=YYYY-MM-DD (기본 오늘) &limit=N (기본 60·최대 500) — 공식 결과·환급 대조 정정(멱등). *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `race_log_page()`
- **위치**: `app.py:28137` (28137~28251)
- **설명**: [분석 변화 로그 뷰어 (2026-07-19)] 경주별 추천·유력마 변화 이력을 사용자가 직접 보는 페이지. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `daily_cards_page()`
- **위치**: `app.py:28255` (28255~28452)
- **설명**: [날짜별 경주 카드 아카이브 (2026-07-22 권대표 요청)] 전체 경주 카드를 날짜별로 복원해서 보는 페이지. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `review_report_page()`
- **위치**: `app.py:28456` (28456~28571)
- **설명**: [복기 인포그래픽 (2026-07-23 권대표 요청)] 매일 밤 22:05 자동 복기(분류+리플레이+급락 밴드)를 *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `debug_race_data()`
- **위치**: `app.py:28575` (28575~28634)
- **설명**: [원격 진단 (2026-07-23 배당 오염 조사)] 지정 경주의 원본 기록을 읽기 전용으로 요약 — 어떤 소스(src: *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `keirin_today_stats()`
- **위치**: `app.py:28638` (28638~28692)
- **설명**: [오늘 성적 요약 (2026-07-19)] 오늘 경륜 결과 파일 집계 → {races, hits, hitRate, avgOdds}. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `_sport_label(doc, rk)`
- **위치**: `app.py:28714` (28714~28723)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 도메인 2차 (kakao|notify|send|render|ht
- **호출됨(Caller · 1)**: `_scoreboard_daily`(28704)

### `scoreboard_daily_api()`
- **위치**: `app.py:28865` (28865~28867)
- **설명**: 전 종목 일일 성적 JSON — ?date=YYYY-MM-DD (기본 오늘). *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `scoreboard_page()`
- **위치**: `app.py:28871` (28871~28979)
- **설명**: [적중왕 타임락 성적표] 읽기 전용 화면 — 회수율 헤드라인·머니시뮬·종목별·경주별 전체 공개·주간 곡선. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `keirin_results_backfill()`
- **위치**: `app.py:28983` (28983~29002)
- **설명**: [경륜 결과 백필] GET=대상 미리보기 · POST=실제 수집·학습(즉시 실행). ?date=YYYY-MM-DD 지정 가능. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `jp_results_backfill()`
- **위치**: `app.py:29063` (29063~29079)
- **설명**: [일본 NAR 결과 백필] 당일(또는 ?date=YYYY-MM-DD) 분석됐으나 결과 없는 일본 NAR 경주를 *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `kra_test()`
- **위치**: `app.py:29083` (29083~29098)
- **설명**: [KRA 진단] 원시 응답·파싱 결과 확인용. 키 활성화 후 실제 필드 검증에 사용. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `kra_section()`
- **위치**: `app.py:29102` (29102~29115)
- **설명**: [KRA 구간기록·전개 진단] API37_1 구간지수 + API6_1 코너순위 + 전개 시뮬레이션. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `kra_jockey_change()`
- **위치**: `app.py:29119` (29119~29125)
- **설명**: [KRA 기수변경 진단] API10_1 해당 개최일 기수변경 목록. params: {meet, rc_date}. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `kra_ai_results()`
- **위치**: `app.py:29129` (29129~29136)
- **설명**: [KRA AI학습용 결과 조회] API155 개최일 전체 경주 결과(착순·배당·체중증감·거리). params: {meet, rc_date}. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `kra_ai_results_ingest()`
- **위치**: `app.py:29140` (29140~29165)
- **설명**: [KRA AI결과 학습 반영·5단계] API155 과거 결과를 결과학습 경로(_apply_result_learning)에 반영. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `kra_collect()`
- **위치**: `app.py:29169` (29169~29179)
- **설명**: [KRA 1회 수집] 지정 경주 배당을 즉시 수집·파이프라인 주입. params: {raceKey?, meet, rc_date, rc_no}. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `kra_watch()`
- **위치**: `app.py:29183` (29183~29206)
- **설명**: [KRA 30초 자동수집 등록] params: {raceKey?, meet, rc_date, rc_no}. 등록 후 데몬이 30초마다 수집. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `kra_unwatch()`
- **위치**: `app.py:29210` (29210~29216)
- **설명**: [KRA 자동수집 해제] params: {raceKey}. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `kra_status()`
- **위치**: `app.py:29220` (29220~29241)
- **설명**: [KRA 상태] 키 설정 여부·자동수집 대상·마지막 수집 시각/건수. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `kra_apis_test()`
- **위치**: `app.py:29845` (29845~29857)
- **설명**: [KRA 승인 API 일괄 테스트] 4종 엔드포인트 활성 여부·샘플 반환. ?full=1 이면 첫 item 포함. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `kra_race_detail()`
- **위치**: `app.py:29861` (29861~29889)
- **설명**: [KRA 경주별상세성적표] 착순·기수·마명·레이팅·기록 조회. params: {meet, rc_date, rc_no}. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `pace_stats_api()`
- **위치**: `app.py:29893` (29893~29900)
- **설명**: [각질 편성 학습] 페이스별 각질 입상률(빠른→추입·느린→선행) + 시나리오 A/B 적중률. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `form_ext_stats_api()`
- **위치**: `app.py:29904` (29904~29921)
- **설명**: [전적확장 통계] 기수-말 궁합·조교사 전략·마번 유불리 요약. params: {jockey?, horse?, trainer?, meet?, no?}. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `kra_jockey()`
- **위치**: `app.py:29925` (29925~29943)
- **설명**: [KRA 현직기수정보] 기수 복승률(1+2착)·삼착률·통산 성적. params: {meet, name?(필터)}. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `kra_ingest_detail()`
- **위치**: `app.py:30001` (30001~30031)
- **설명**: [KRA 단승 주입·역배열 활성화] 경주별상세성적표의 단승 배당을 파이프라인 주입 → *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `alerts_premium()`
- **위치**: `app.py:30163` (30163~30196)
- **설명**: [프리미엄 알림] 현재 활성 경주 중 등급 조건 충족 경주의 알림 목록(폴링/푸시연동용). *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `_bmed_before_card(rk, form)`
- **위치**: `app.py:30216` (30216~30237)
- **설명**: [B·사전분석] 배당 전 전적/기수 기반 주목마 TOP + 사전예상복승(전적 이중수렴). *(docstring)*
- **분류 근거**: 도메인 2차 (kakao|notify|send|render|ht
- **호출됨(Caller · 1)**: `bmed_view`(30283)

### `_bmed_expert_cards(an)`
- **위치**: `app.py:30252` (30252~30279)
- **설명**: [E·전문가 해석] 각 말 상세: 전적등급·각질·기수·현재배당·흐름. *(docstring)*
- **분류 근거**: 도메인 2차 (kakao|notify|send|render|ht
- **호출됨(Caller · 1)**: `bmed_view`(30283)

### `bmed_view(key)`
- **위치**: `app.py:30283` (30283~30338)
- **설명**: [BMED 통합] 한 경주의 B(사전)·M(신호)·E(해석)·D(결론)을 한 번에. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `jra_starters()`
- **위치**: `app.py:31023` (31023~31067)
- **설명**: [중앙경마(JRA) 전적] netkeiba 馬柱(shutuba_past)를 서버가 직접 fetch·파싱해 각질(netkeiba 표기)· *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `analysis_log_backfill()`
- **위치**: `app.py:31071` (31071~31090)
- **설명**: [4번] 지금까지 수집/분석된 경주들의 로그를 즉시 생성(없으면 생성, 있으면 최신화). *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `analysis_log_backup()`
- **위치**: `app.py:31094` (31094~31098)
- **설명**: [5번] data/analysis_log/ 를 GitHub에 커밋(+push). 하루 경주 종료 후 호출. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `data_backup()`
- **위치**: `app.py:31102` (31102~31113)
- **설명**: [데이터 보호] 학습 코퍼스(결과·분석로그·AI학습·패턴 등) 즉시 GitHub 백업. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `data_status()`
- **위치**: `app.py:31117` (31117~31136)
- **설명**: [데이터 보호] 추적 코퍼스 경로별 파일 수 + git 추적/미추적 요약(보호 현황 표시). *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `kra_key()`
- **위치**: `app.py:31146` (31146~31155)
- **설명**: data.go.kr 서비스키 저장/조회. 저장 위치는 tools/fetch_kra.py 와 공유(data/kra_key.txt). *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `kra_horse()`
- **위치**: `app.py:31159` (31159~31182)
- **설명**: 마명으로 KRA 과거기록 자동매칭. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `odds_undo_route()`
- **위치**: `app.py:31243` (31243~31246)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `odds_clear_route()`
- **위치**: `app.py:31250` (31250~31253)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `odds_race_route()`
- **위치**: `app.py:31257` (31257~31260)
- **설명**: 저장된 시계열 조회: {raceKey} → {snaps, series} *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `odds_compute_route()`
- **위치**: `app.py:31264` (31264~31270)
- **설명**: {raceKey, horses:[{no,name,score}]} → 신호 점수 + 보정 추천(bets) *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `score_form()`
- **위치**: `app.py:32266` (32266~32274)
- **설명**: {horses:[{no,name,recentPlacings:[..]}]} → 마필별 기본 점수만(3-1). *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `score()`
- **위치**: `app.py:32278` (32278~32286)
- **설명**: {race:{distance,course,grade}, horses:[{...전적/기수/마체중/이상감지...}], jockeyStats?} *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `analyze_combined()`
- **위치**: `app.py:32431` (32431~32447)
- **설명**: {raceKey, race, horses(전적), oddsSnapshots(배당), budget} → 통합 점수+등급+이상감지+베팅. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `_render_region_b64(page, frac, target_w)`
- **위치**: `app.py:32544` (32544~32553)
- **설명**: PDF 페이지의 frac(x0,y0,x1,y1 비율) 영역을 target_w 픽셀 폭 PNG(base64)로 렌더. *(docstring)*
- **분류 근거**: 도메인 2차 (kakao|notify|send|render|ht
- **호출됨(Caller · 4)**: `_render_thumb`(32556) · `_render_band`(32557) · `_render_train`(32558) · `_render_full`(32559)

### `_render_thumb(page)`
- **위치**: `app.py:32556` (32556~32556)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 도메인 2차 (kakao|notify|send|render|ht
- **호출됨(Caller · 1)**: `_korea_run_job`(32957)

### `_render_train(page)`
- **위치**: `app.py:32558` (32558~32558)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 도메인 2차 (kakao|notify|send|render|ht
- **호출됨(Caller · 1)**: `_korea_extract_race`(32627)

### `_render_full(page)`
- **위치**: `app.py:32559` (32559~32559)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 도메인 2차 (kakao|notify|send|render|ht
- **호출됨(Caller · 1)**: `_korea_run_job`(32957)

### `_korea_race_label(venue, race_no, distance)`
- **위치**: `app.py:32562` (32562~32566)
- **설명**: static/js/app.js 의 raceLabel 과 동일 — History 제목 매칭을 위해 형식 유지. *(docstring)*
- **분류 근거**: 도메인 2차 (kakao|notify|send|render|ht
- **호출됨(Caller · 1)**: `_korea_run_job`(32957)

### `korea_start()`
- **위치**: `app.py:33217` (33217~33292)
- **설명**: PDF 업로드 → 새 세션 시작(기존 세션/진행중 작업은 덮어씀 = '새 PDF 업로드' 초기화). *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `korea_status()`
- **위치**: `app.py:33296` (33296~33300)
- **설명**: 진행상황만(경량) — 폴링용. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `korea_session()`
- **위치**: `app.py:33304` (33304~33306)
- **설명**: 전체 세션(경주·리포트 포함) — 페이지 로드 시 복원용. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `korea_reset()`
- **위치**: `app.py:33310` (33310~33320)
- **설명**: '새 PDF 업로드' 초기화 — 진행중 작업 취소 + 저장 세션/PDF 삭제. (새로고침으로는 초기화 안 됨) *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `korea_reextract()`
- **위치**: `app.py:33324` (33324~33356)
- **설명**: 경주 요약 페이지 ±1 보정 후 재추출·재분석 (감지가 1p 어긋난 경우). *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `korea_prerace_list()`
- **위치**: `app.py:33360` (33360~33362)
- **설명**: [사전분석] 저장된 경주 목록(index) — 경주 선택 UI/즉시 로드용. 리포트 본문은 제외(경량). *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `korea_prerace_get(key)`
- **위치**: `app.py:33366` (33366~33371)
- **설명**: [사전분석] 저장된 경주 1건 전체(출전마·리포트 포함) 즉시 로드. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `korea_history_save()`
- **위치**: `app.py:33387` (33387~33441)
- **설명**: 통합분석 스냅샷 저장/갱신. 같은 경주 파일은 덮어쓰되 결과(result)는 보존. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —  ⚠ 위험목록 함수 → 전수 나열

### `korea_history_list()`
- **위치**: `app.py:33445` (33445~33461)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `korea_history_get()`
- **위치**: `app.py:33465` (33465~33470)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `korea_history_result()`
- **위치**: `app.py:33474` (33474~33485)
- **설명**: 실제 착순 입력 → 해당 히스토리 파일에 result 병합. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `korea_backup()`
- **위치**: `app.py:33489` (33489~33492)
- **설명**: 완료 후 GitHub 백업 (세션 + 경주 히스토리). best-effort. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `_card_conf_value(conf)`
- **위치**: `app.py:34973` (34973~34986)
- **설명**: [확신도 표시 수정] 카드에 표시할 확신도 스칼라 추출. *(docstring)*
- **분류 근거**: 도메인 2차 (kakao|notify|send|render|ht
- **호출됨(Caller · 5)**: `_triple_analyze`(10724) · `_multi_card`(35366) · `_kakao_rich_message`(36119) · `_kakao_notify_race`(36318) · `_timeline_extract`(36450)

### `ingest_rejects()`
- **위치**: `app.py:35220` (35220~35252)
- **설명**: [수집 진단 2026-07-28] 수집 거부 기록 조회 — '수집이 왜 안 됐는지' 사후 추적용. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `results_history()`
- **위치**: `app.py:35256` (35256~35299)
- **설명**: [신규] 분석 로그 전체 기반 결과기록 목록. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `_multi_card(key, rec)`
- **위치**: `app.py:35366` (35366~35474)
- **설명**: [3번] 카드 요약: analyze(기존 _triple_analyze 재사용·읽기전용) → 유력마TOP3·핵심신호·확신도·마감남은초·색상. *(docstring)*
- **분류 근거**: 도메인 2차 (kakao|notify|send|render|ht
- **호출됨(Caller · 1)**: `multi_dashboard`(35542)

### `multi_keirin_schedule()`
- **위치**: `app.py:35478` (35478~35508)
- **설명**: [확장 경유 경륜 스케줄] 확장(로그인 세션)이 oddspark 경륜 KaisaiRaceList를 fetch·파싱해 전송 → *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `multi_schedule()`
- **위치**: `app.py:35512` (35512~35519)
- **설명**: [1번] 오늘 개최 스케줄 조회(GET) / 강제 재수집(POST). *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `multi_collect_now()`
- **위치**: `app.py:35523` (35523~35538)
- **설명**: [2번] 지금 발주 임박(T-10분 이내) 경주 배당 즉시 수집(수동 트리거). 실패 격리. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `multi_dashboard()`
- **위치**: `app.py:35542` (35542~35626)
- **설명**: [3번] 다중 경주 대시보드 카드 — 배당 수집된 경주(분석 카드) + 아직 수집 전인 예정 경주(카운트다운) 병합. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `multi_race_detail(key)`
- **위치**: `app.py:35842` (35842~35874)
- **설명**: [4번] 카드 클릭 → 해당 경주 전체 분석(기존 _triple_analyze 재사용). 결과입력은 기존 엔드포인트 사용. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `_kakao_rest_key()`
- **위치**: `app.py:35906` (35906~35913)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (kakao|_page$|_api$|notify|send_)
- **호출됨(Caller · 4)**: `_kakao_access_token`(35949) · `kakao_auth`(36046) · `kakao_oauth`(36059) · `kakao_status`(36078)

### `_kakao_token_load()`
- **위치**: `app.py:35916` (35916~35920)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (kakao|_page$|_api$|notify|send_)
- **호출됨(Caller · 3)**: `_kakao_token_request`(35931) · `_kakao_access_token`(35949) · `kakao_status`(36078)

### `_kakao_token_save(tok)`
- **위치**: `app.py:35923` (35923~35928)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (kakao|_page$|_api$|notify|send_)
- **호출됨(Caller · 1)**: `_kakao_token_request`(35931)

### `_kakao_token_request(params)`
- **위치**: `app.py:35931` (35931~35946)
- **설명**: kauth 토큰 엔드포인트 공용 호출(발급/갱신). client_secret 미사용(비활성 유지). *(docstring)*
- **분류 근거**: 이름 규칙 (kakao|_page$|_api$|notify|send_)
- **호출됨(Caller · 2)**: `_kakao_access_token`(35949) · `kakao_oauth`(36059)

### `_kakao_access_token()`
- **위치**: `app.py:35949` (35949~35963)
- **설명**: 유효 액세스 토큰 반환(만료 임박 시 리프레시). 미연동이면 None. *(docstring)*
- **분류 근거**: 이름 규칙 (kakao|_page$|_api$|notify|send_)
- **호출됨(Caller · 1)**: `_kakao_send_to_me`(36026)

### `_kakao_anchor_log(rk, phase, text, result, an)`
- **위치**: `app.py:35982` (35982~36023)
- **설명**: 발송 직후 1건 적재. **완전 방어적** — 여기서 실패해도 발송 흐름에 영향 없음. *(docstring)*
- **분류 근거**: 이름 규칙 (kakao|_page$|_api$|notify|send_)
- **호출됨(Caller · 3)**: `kakao_send_race`(36218) · `_kakao_notify_race`(36318) · `_timeline_snap_tick`(36575)

### `_kakao_send_to_me(text, url)`
- **위치**: `app.py:36026` (36026~36042)
- **설명**: 카카오톡 '나에게 보내기'(기본 텍스트 템플릿). 반환 {ok, error?}. *(docstring)*
- **분류 근거**: 이름 규칙 (kakao|_page$|_api$|notify|send_)
- **호출됨(Caller · 8)**: `kakao_oauth`(36059) · `kakao_test`(36086) · `kakao_send_race`(36218) · `_kakao_notify_race`(36318) · `_timeline_snap_tick`(36575) · `_health_kakao_send`(37403) … [+2]

### `kakao_auth()`
- **위치**: `app.py:36046` (36046~36055)
- **설명**: 카카오 1회 동의 시작 — 사무실 브라우저에서 열면 카카오 로그인·동의 화면으로 이동. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `kakao_oauth()`
- **위치**: `app.py:36059` (36059~36074)
- **설명**: 카카오 동의 콜백 — 코드→토큰 교환·저장 후 테스트 메시지 1건 발송. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `kakao_status()`
- **위치**: `app.py:36078` (36078~36082)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `kakao_test()`
- **위치**: `app.py:36086` (36086~36088)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `_kakao_sent_save()`
- **위치**: `app.py:36109` (36109~36116)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (kakao|_page$|_api$|notify|send_)
- **호출됨(Caller · 2)**: `_kakao_notify_race`(36318) · `_timeline_snap_tick`(36575)

### `_kakao_rich_message(rk, phase, an)`
- **위치**: `app.py:36119` (36119~36214)
- **설명**: [카톡 추천 상세 (2026-07-21 권대표 피드백)] '홍보 문구만 보인다' → 해당 경주의 실제 추천 내역을 *(docstring)*
- **분류 근거**: 이름 규칙 (kakao|_page$|_api$|notify|send_)
- **호출됨(Caller · 2)**: `kakao_send_race`(36218) · `_kakao_notify_race`(36318)

### `kakao_send_race()`
- **위치**: `app.py:36218` (36218~36239)
- **설명**: [수동/시연] 지정 경주의 현재 추천 내역을 즉시 카톡 발송. ?key=경주키 (예: 모리오카 7경주). *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `_kakao_build_message(rk, phase, an, snap)`
- **위치**: `app.py:36242` (36242~36274)
- **설명**: 카카오 알림 텍스트 구성. T-7=1차 확정 / T-5=최종 확정('지금 사세요!'). 복승·복병·배당판 링크 포함. *(docstring)*
- **분류 근거**: 이름 규칙 (kakao|_page$|_api$|notify|send_)
- **호출됨(Caller · 1)**: `_kakao_notify_race`(36318)

### `_kakao_send_blocked_by_src(rk)`
- **위치**: `app.py:36277` (36277~36315)
- **설명**: 🔴 [발송 직전 소스 대조 · 2026-08-03 승인] 배당이 오염됐으면 그 경주는 보내지 않는다. *(docstring)*
- **분류 근거**: 이름 규칙 (kakao|_page$|_api$|notify|send_)
- **호출됨(Caller · 1)**: `_kakao_notify_race`(36318)

### `_kakao_notify_race(rk, phase, an, snap)`
- **위치**: `app.py:36318` (36318~36443)
- **설명**: 알림 발송(또는 큐 적재). KAKAO_NOTIFY_URL 있으면 웹훅 POST, 없으면 kakao_queue.jsonl 적재. *(docstring)*
- **분류 근거**: 이름 규칙 (kakao|_page$|_api$|notify|send_)
- **호출됨(Caller · 1)**: `_timeline_snap_tick`(36575)

### `_korea_kakao_targets(now)`
- **위치**: `app.py:36527` (36527~36553)
- **설명**: [한국] korea_session.json 의 postTime 으로 (postEpoch, venue, raceNo, rk) 목록을 만든다. *(docstring)*
- **분류 근거**: 이름 규칙 (kakao|_page$|_api$|notify|send_)
- **호출됨(Caller · 1)**: `_auto_pred_tick`(36865)

### `auto_prediction_status()`
- **위치**: `app.py:36976` (36976~36978)
- **설명**: [자동 예상 상태] 현재 예상 중 경주 + 다음 자동 분석 경주(분·오버레이 상단 표시용). *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `auto_prediction_toggle()`
- **위치**: `app.py:36982` (36982~36987)
- **설명**: [자동 예상 ON/OFF] body {enabled:true/false} 없으면 토글. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `auto_prediction_list()`
- **위치**: `app.py:36991` (36991~37008)
- **설명**: [자동 예상 목록] 저장된 최종 예상 + 결과 대조. 최신순. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `auto_prediction_get(filename)`
- **위치**: `app.py:37012` (37012~37021)
- **설명**: [자동 예상 1건] 파일명으로 조회(경로조작 방어). *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `health_checklist_api()`
- **위치**: `app.py:37209` (37209~37240)
- **설명**: [완료 조건 체크리스트 2026-07-30] 17항목 자동 판정 — **완전 읽기 전용**. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `_health_kakao_text(rep)`
- **위치**: `app.py:37313` (37313~37380)
- **설명**: 카카오 본문 — **최대한 짧게**. summary 1줄 + 미충족 목록만(충족 항목은 넣지 않는다). *(docstring)*
- **분류 근거**: 이름 규칙 (kakao|_page$|_api$|notify|send_)
- **호출됨(Caller · 2)**: `_health_kakao_send`(37403) · `health_send_kakao_api`(37623)

### `_health_kakao_log(reason, text, result, err)`
- **위치**: `app.py:37383` (37383~37400)
- **설명**: 발송 이력 — **성공·실패 모두** `data/kakao_sent/<날짜>.json` 에 누적. *(docstring)*
- **분류 근거**: 이름 규칙 (kakao|_page$|_api$|notify|send_)
- **호출됨(Caller · 2)**: `_health_kakao_send`(37403) · `_midcheck_run`(38373)

### `_health_kakao_send(reason)`
- **위치**: `app.py:37403` (37403~37423)
- **설명**: 체크리스트를 카카오로 1회 발송. 반환 {ok, sent, summary, error?}. *(docstring)*
- **분류 근거**: 이름 규칙 (kakao|_page$|_api$|notify|send_)
- **호출됨(Caller · 3)**: `health_send_kakao_api`(37623) · `_start_health_kakao_scheduler`(37641) · `_loop`(37684)

### `picks_diff_page()`
- **위치**: `app.py:37521` (37521~37608)
- **설명**: [추천 변조 대조 화면] `/picks-diff[?date=YYYY-MM-DD][&all=1]` *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `picks_diff_api()`
- **위치**: `app.py:37612` (37612~37619)
- **설명**: [추천 변조 대조] `?date=YYYY-MM-DD&all=1` — A(마감 확정) ↔ B(마감 후 재분석) 차이. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `health_send_kakao_api()`
- **위치**: `app.py:37623` (37623~37638)
- **설명**: [체크리스트 카카오 푸시] 수동 발송. GET=미리보기(발송 안 함) · POST=실제 발송. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `_start_health_kakao_scheduler()`
- **위치**: `app.py:37641` (37641~37699)
- **설명**: 매일 `HEALTH_KAKAO_HOUR`(기본 **21**)시에 체크리스트 1회 발송(멱등·하루 1회). *(docstring)*
- **분류 근거**: 이름 규칙 (kakao|_page$|_api$|notify|send_)
- **호출됨(Caller · 1)**: `_boot_background`(38998)  ⚠ 위험목록 함수 → 전수 나열

### `_midcheck_kakao()`
- **위치**: `app.py:37971` (37971~37987)
- **설명**: 오늘 카카오 발송 (건수, 마지막 시각). 실패면 (None, None). *(docstring)*
- **분류 근거**: 이름 규칙 (kakao|_page$|_api$|notify|send_)
- **호출됨(Caller · 1)**: `_midcheck_collect`(38172)

### `_midcheck_text(slot, f, prev_stamp)`
- **위치**: `app.py:38242` (38242~38370)
- **설명**: 카카오 본문 — 짧게. ⚠ 발송 코어는 건드리지 않는다(조립만). *(docstring)*
- **분류 근거**: 도메인 2차 (kakao|notify|send|render|ht
- **호출됨(Caller · 1)**: `_midcheck_run`(38373)  ⚠ 위험목록 함수 → 전수 나열

### `api_midcheck()`
- **위치**: `app.py:38462` (38462~38471)
- **설명**: GET = 미리보기(발송 안 함) · POST = 즉시 1회 발송. ⚠ 읽기 전용 점검이다. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `jra_preload_api()`
- **위치**: `app.py:38784` (38784~38797)
- **설명**: [중앙 전적 선수집] GET=현황(읽기 전용) · POST=즉시 1회(수동). 자동 프로브 없음. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `nar_preload_api()`
- **위치**: `app.py:38801` (38801~38819)
- **설명**: [NAR 전적 선수집] GET=현황(읽기 전용) · POST=즉시 1회 실행(수동). *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `collect_cycles_api()`
- **위치**: `app.py:38823` (38823~38849)
- **설명**: [수집 사이클 관측] ?limit=N — 최근 사이클 + 경주별 '창 진입 횟수'. *(docstring)*
- **분류 근거**: 데코레이터 @app.route
- **호출됨(Caller · 0)**: —

### `gemini_kakao_enabled()`
- **위치**: `gemini_reviewer.py:39` (39~42)
- **설명**: Gemini 검수 결과의 카카오 발송 허용 여부(기본 꺼짐). *(docstring)*
- **분류 근거**: 이름 규칙 (kakao|_page$|_api$|notify|send_)
- **호출됨(Caller · 2)**: `review_async`(440) · `_run`(449)

### `_send_kakao(rk, result)`
- **위치**: `gemini_reviewer.py:395` (395~438)
- **설명**: [지적 본문 전달 (2026-07-29)] 응답에는 {func,line,code,problem,evidence,fix} 가 이미 다 담겨 *(주석(본문))*
- **분류 근거**: 이름 규칙 (kakao|_page$|_api$|notify|send_)
- **호출됨(Caller · 2)**: `review_async`(440) · `_run`(449)

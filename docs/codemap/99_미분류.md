# 미분류 — 분류 규칙에 걸리지 않은 함수

> 🤖 **자동 생성** — `python tools/build_codemap.py` · 2026-07-30 22:47:30
> ⚠ 손으로 고치지 마세요. 다음 생성 때 덮어써집니다.
> ⚠ **AST 한계**: `getattr()` 동적 호출 · `importlib` 경유 · 문자열 디스패치는 잡지 못합니다
>   (`gemini_reviewer`·`health_check` 가 실제로 `importlib` 경유입니다).

총 **641개** 함수

### `load_env()`
- **위치**: `app.py:59` (59~69)
- **설명**: ---------- .env 로더 (dotenv 의존성 없이) ---------- *(주석(def 위))*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 0)**: —

### `_cors_origin_allowed(origin)`
- **위치**: `app.py:97` (97~103)
- **설명**: 요청 Origin 이 허용 목록/패턴에 해당하는지. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_cors_preflight`(118) · `_cors_headers`(131)

### `_cors_preflight()`
- **위치**: `app.py:118` (118~128)
- **설명**: OPTIONS preflight 은 허용 오리진일 때 200 으로 즉시 응답(수동 폴백 경로) *(주석(본문))*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 0)**: —

### `_cors_headers(resp)`
- **위치**: `app.py:131` (131~138)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 0)**: —

### `_access_tier(path)`
- **위치**: `app.py:163` (163~167)
- **설명**: 엔드포인트 접근 등급 반환. 미분류는 공개(현행 유지). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_premium_gate`(176)

### `_request_is_authed(req)`
- **위치**: `app.py:170` (170~172)
- **설명**: 향후 실제 인증 연동 지점(현재는 토큰 유무만 확인 — 미강제 상태라 미사용). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_premium_gate`(176)

### `client(api_key)`
- **위치**: `app.py:197` (197~201)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `call_claude`(624)

### `_api_should_retry(e)`
- **위치**: `app.py:595` (595~604)
- **설명**: 이 예외가 재시도 가치가 있는 일시적 오류인지. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_messages_create_retry`(607)

### `_messages_create_retry(cli, **kwargs)`
- **위치**: `app.py:607` (607~621)
- **설명**: messages.create 를 지수 백오프로 재시도(2·4·8·16초 + 지터). 마지막 실패는 그대로 raise. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `call_claude`(624)

### `call_claude(content, schema, max_tokens, api_key)`
- **위치**: `app.py:624` (624~647)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 10)**: `_do_extract_jockey`(706) · `_do_extract_race`(724) · `_do_extract_training`(760) · `extract_results`(781) · `result_ocr`(831) · `_do_detect`(872) … [+4]

### `image_block(img)`
- **위치**: `app.py:650` (650~658)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 9)**: `_do_extract_jockey`(706) · `_do_extract_race`(724) · `_do_extract_training`(760) · `extract_results`(781) · `result_ocr`(831) · `_do_detect`(872) … [+3]

### `_do_extract_jockey(img, api_key)`
- **위치**: `app.py:706` (706~715)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `extract_jockey`(719) · `_korea_run_job`(28595)

### `_do_extract_race(img, api_key)`
- **위치**: `app.py:724` (724~751)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `extract_race`(755) · `_korea_extract_race`(28442)

### `_do_extract_training(img, api_key)`
- **위치**: `app.py:760` (760~770)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `extract_training`(774) · `_korea_extract_race`(28442)

### `_img_from_dataurl(img)`
- **위치**: `app.py:821` (821~827)
- **설명**: dataURL 문자열('data:image/png;base64,...') 또는 {media_type,data} → image_block 입력형. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `result_ocr`(831)

### `_do_detect(imgs, api_key)`
- **위치**: `app.py:872` (872~890)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `detect`(894) · `_korea_run_job`(28595) · `_detect_batch`(28625)

### `_norm_korea_horse(h, jstats)`
- **위치**: `app.py:901` (901~914)
- **설명**: 추출된 한국 출전마(horseNum/horseName…) → 스코어링 입력(no/name/pastRaces…) 정규화. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_korea_deterministic_overlay`(917) · `_det_horse_note`(936)

### `_korea_deterministic_overlay(race, jstats)`
- **위치**: `app.py:917` (917~933)
- **설명**: pastRaces 가 추출된 경우에만 결정론 점수·각질·변화감지·2착패턴(삼복승필수)을 산출. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_do_analyze`(984)

### `_det_horse_note(h, race, leaders)`
- **위치**: `app.py:936` (936~957)
- **설명**: 한 마리의 pastRaces 로부터 프롬프트에 넣을 결정론 힌트 텍스트(없으면 ''). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_do_analyze`(984)

### `_force_trifecta_from_must(report, overlay)`
- **위치**: `app.py:960` (960~981)
- **설명**: 2착패턴(삼복승필수) 말을 삼복승 조합에 결정론적으로 강제 편성. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_do_analyze`(984)

### `_odds_get_race(db, race_key)`
- **위치**: `app.py:1212` (1212~1213)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `odds_add_snapshot`(1242) · `odds_compute`(1294) · `odds_race_route`(27076)

### `_clamp(x, lo, hi)`
- **위치**: `app.py:1233` (1233~1234)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `odds_compute`(1294)

### `_valid_series(arr)`
- **위치**: `app.py:1237` (1237~1239)
- **설명**: null 제거한 유효 배당만 [(index, odds), ...] *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `odds_compute`(1294)

### `odds_add_snapshot(race_key, odds_by_horse, t)`
- **위치**: `app.py:1242` (1242~1270)
- **설명**: 한 라운드 스냅샷 기록: {마번: 배당} 을 하나의 타임스탬프로 누적. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `odds_snapshot`(1380) · `combined_analyze`(28221)

### `odds_undo(race_key)`
- **위치**: `app.py:1273` (1273~1285)
- **설명**: 마지막 스냅샷 1라운드 되돌리기 *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `odds_undo_route`(27062)

### `odds_clear(race_key)`
- **위치**: `app.py:1288` (1288~1291)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `odds_clear_route`(27069) · `combined_analyze`(28221)

### `odds_compute(race_key, bmed_horses)`
- **위치**: `app.py:1294` (1294~1358)
- **설명**: bmed_horses: [{no, name, score}] → 말별 드롭/괴리/통합 신호 + 태그 *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `odds_compute_route`(27083) · `_anomaly_by_no`(28111)

### `odds_suggest_bets(computed)`
- **위치**: `app.py:1361` (1361~1376)
- **설명**: signalScore 상위 말로 복승(2)·삼복승(3) 보정 추천(국소 계산) *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `odds_compute_route`(27083)

### `_sport_match(rec_sport, want)`
- **위치**: `app.py:1485` (1485~1491)
- **설명**: [종목 분리] 레코드 sport 와 요청 sport 매칭. 레거시(None/'')는 horse 로 간주 *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `triple_latest`(2598) · `current_race`(3249)

### `_horse_has_form(h)`
- **위치**: `app.py:1511` (1511~1515)
- **설명**: 이 마필 항목이 실제 전적(착순/전적점수)을 담고 있는가. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 4)**: `_rec_form_count`(1518) · `_sanitize_starters`(1523) · `extract_japan`(12721) · `korea_form`(12766)

### `_rec_form_count(rec)`
- **위치**: `app.py:1518` (1518~1520)
- **설명**: 레코드 안에서 전적이 채워진 마필 수. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `extract_japan`(12721)

### `_sanitize_starters(horses)`
- **위치**: `app.py:1523` (1523~1539)
- **설명**: 출마표2/DebaTable 파서 오탐(오즈표·전체목록 스크레이핑으로 수백 행이 딸려오는 경우) 방어. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `extract_japan`(12721) · `korea_form`(12766)

### `_form_from_starters(rk, drops, sport, valid_nos)`
- **위치**: `app.py:1542` (1542~1628)
- **설명**: 저장된 전적으로 마필 점수·등급 계산. 배당 급락마는 이상감지 상향 반영. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(9492)

### `_hno(h)`
- **위치**: `app.py:1566` (1566~1570)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_form_from_starters`(1542) · `_learn_upset`(16313)

### `_drop_placeholder_combos(combos, label)`
- **위치**: `app.py:1634` (1634~1654)
- **설명**: [placeholder 배당 필터 (2026-07-19)] 발매 전 껍데기 배당(예 1054.4·9999.9)이 유력마·복병· *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(9492)

### `_is_opening_settle(po, pct)`
- **위치**: `app.py:1679` (1679~1682)
- **설명**: opening/placeholder 배당(직전 100배+)이 실배당으로 정착하며 생기는 기계적 급락(≤-80%). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_triple_analyze`(9492) · `_history_append`(13676)

### `_baseline_reset_needed(prev_q, cur_q, thresh, min_ratio, min_combos)`
- **위치**: `app.py:1685` (1685~1700)
- **설명**: [경주전환·초반미수집 방어] 직전 대비 공통 복승 조합의 다수(60%+)가: *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_do_triple_ingest`(2166) · `_triple_analyze`(9492)

### `_as_qmap(x)`
- **위치**: `app.py:1703` (1703~1717)
- **설명**: 복승 배당을 {정렬튜플: 최저배당} 맵으로 정규화 — 리스트([{combo,odds}])와 *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 7)**: `_next_race_surge`(1720) · `_board_flip_suspect`(1733) · `_combo_count_dip`(1767) · `_column_shift_suspect`(1784) · `_horse_rep_series`(6812) · `_combo_odds_trend`(12491) … [+1]

### `_next_race_surge(prev_q, cur_q, thresh, min_ratio, min_combos)`
- **위치**: `app.py:1720` (1720~1730)
- **설명**: [2번 다음경주 혼입 방어] 직전 대비 공통 복승 조합의 다수(60%+)가 200%+ 급등하면 *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_history_append`(13676)

### `_combo_count_dip(prev_q, cur_q, ratio, min_prev)`
- **위치**: `app.py:1767` (1767~1781)
- **설명**: [3번 조합 수 딥] 직전 대비 복승 조합 수가 급감했는지. 실측 112건/23,230틱(0.48%). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_history_append`(13676)

### `_close(a, b)`
- **위치**: `app.py:1801` (1801~1802)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_column_shift_suspect`(1784)

### `_sport_max_no(sport)`
- **위치**: `app.py:1841` (1841~1843)
- **설명**: 종목별 유효 최대 마번 반환(모르는 종목은 경마 기준 18로 관대 처리). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(9492)

### `_oddspark_covered_active(rk, within)`
- **위치**: `app.py:1846` (1846~1864)
- **설명**: [자동전송 꼬임 방어] 이 raceKey를 서버가 oddspark로 방금(within초 내) 직접 수집 중인가? *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `triple_analyze`(13461)

### `_canon_rk(rk)`
- **위치**: `app.py:1874` (1874~1894)
- **설명**: [① raceKey 통일] 오늘 날짜 접두("2026-07-18 오비히로 2경주")를 무접두("오비히로 2경주")로 통일. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 11)**: `triple_ingest`(1943) · `_do_triple_ingest`(2166) · `extract_japan`(12721) · `triple_analyze`(13461) · `_validate_learning_data`(18408) · `_ml_row_build`(18437) … [+5]

### `_src_is_oddspark(src)`
- **위치**: `app.py:1897` (1897~1899)
- **설명**: 소스 문자열이 oddspark(백업 소스)인지. 사설(ks1)·keiba·kra_api·화면수집 등은 False(=배당판 권위). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_do_triple_ingest`(2166)

### `_combo_value_multiset(combos)`
- **위치**: `app.py:1902` (1902~1911)
- **설명**: 조합 목록 → 배당'값' 멀티셋(반올림). 말번호 무관하게 '같은 경주인지' 비교용. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_oddspark_mapping_suspect`(1928)

### `_combo_map(combos)`
- **위치**: `app.py:1914` (1914~1925)
- **설명**: 조합 목록 → {frozenset(말번호쌍): 배당값}. 말번호 매핑 비교용. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_oddspark_mapping_suspect`(1928)

### `_combo_horse_count(combos)`
- **위치**: `app.py:1980` (1980~1991)
- **설명**: 조합 목록에 등장하는 서로 다른 마번 수(N). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_arity_guard`(2007)

### `_arity_expect(kind, n)`
- **위치**: `app.py:1994` (1994~2004)
- **설명**: N두 기준 기대 조합 수. 복승 nC2 · 쌍승 nP2 · 삼복승 nC3. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_arity_guard`(2007)

### `_exacta_mirrors_quinella(q, x, source)`
- **위치**: `app.py:2035` (2035~2080)
- **설명**: [오염방지 3-b·핵심] 쌍승이 사실상 복승 배당의 복사본인지 판정. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_do_triple_ingest`(2166)

### `_combo_max_no(*sources)`
- **위치**: `app.py:2104` (2104~2126)
- **설명**: [종목 증거판정용] 배당 조합(복승·쌍승·삼복승)에 등장하는 최대 마번을 추출. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_do_triple_ingest`(2166)

### `_race_pin_expire_stale()`
- **위치**: `app.py:2510` (2510~2524)
- **설명**: [날짜 초기화] 고정 경주의 저장 날짜가 오늘이 아니면(어제 이전) 자동 해제 → None 반환. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_race_pin_load`(2527)

### `_race_pin_clear()`
- **위치**: `app.py:2540` (2540~2544)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `_race_pin_expire_stale`(2510) · `race_pin`(2579) · `_startup_date_reset`(32237)

### `_is_central_key(_k)`
- **위치**: `app.py:2631` (2631~2635)
- **설명**: [중앙경마 탭 실시간 분석 (2026-07-19)] JRA는 oddspark 미지원(서버 수집 불가) — 확장이 배당판에서 *(주석(def 위))*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `triple_latest`(2598)

### `_anomaly_pretty(raw)`
- **위치**: `app.py:2671` (2671~2688)
- **설명**: 스냅샷 이상감지 원문 → {text, severity} 로 정규화(사람이 읽는 누적 피드용). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_anomaly_events_from_doc`(2704) · `history_list`(17457)

### `_anomaly_combo(raw)`
- **위치**: `app.py:2691` (2691~2701)
- **설명**: 스냅샷 이상감지 원문 → 조합 문자열(예 '2+3'·'4'·'5→10'). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_anomaly_events_from_doc`(2704)

### `_anomaly_events_from_doc(doc)`
- **위치**: `app.py:2704` (2704~2722)
- **설명**: [이상감지 누적·공용] 스냅샷 doc → 시간순·중복제거 이상감지 이벤트 리스트. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `_concentration_counts`(2725) · `anomaly_feed`(2742) · `_build_analysis_log`(14451)

### `_concentration_counts(doc)`
- **위치**: `app.py:2725` (2725~2738)
- **설명**: [복병_집중급락] 경주 이상감지 이벤트에서 말별 등장 횟수(급락/역전 조합에 낀 횟수). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(9492)

### `_safe_snapshot_name(fn)`
- **위치**: `app.py:2878` (2878~2886)
- **설명**: [경로조작 방어] 파일명 정규화 — basename·허용문자(한글·영숫자·_-.)·.png 강제·길이 제한. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `snapshot_save`(3101) · `snapshot_get`(3178)

### `_snapshot_meta_path(fn)`
- **위치**: `app.py:2889` (2889~2890)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `_snapshot_metas_for_race`(2968) · `snapshot_save`(3101) · `snapshot_list`(3149)

### `_snapshot_result_for(rk)`
- **위치**: `app.py:2893` (2893~2907)
- **설명**: [스냅샷↔결과 비교] raceKey 로 저장된 실제 착순(top3) 조회(유연 매칭). 없으면 None. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 4)**: `_snapshot_timing_learn`(3041) · `snapshot_list`(3149) · `dark_horse_review`(19512) · `_auto_pred_compare`(31376)

### `_snapshot_min_quinella(quinellas)`
- **위치**: `app.py:2914` (2914~2929)
- **설명**: [비교분석] 추천 복승 조합 중 최저 배당 + 그 조합 반환. 없으면 (None, None). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `_snapshot_build_compare`(2990) · `snapshot_save`(3101) · `_pack`(2998)

### `_snapshot_main_combos(quinellas, cap)`
- **위치**: `app.py:2932` (2932~2942)
- **설명**: [비교분석] 추천 변경 여부 판정용 — 상위 복승 조합을 정규화 문자열 집합으로. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_snapshot_build_compare`(2990)

### `_snapshot_signal_first(rk)`
- **위치**: `app.py:2945` (2945~2965)
- **설명**: [비교분석] odds_history 에서 이 경주의 이상감지 신호(signal_horse)가 처음 잡힌 시점을 'T-N분'으로. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_snapshot_build_compare`(2990)

### `_snapshot_metas_for_race(rk)`
- **위치**: `app.py:2968` (2968~2987)
- **설명**: [비교분석] 이 raceKey 로 저장된 스냅샷 메타를 단계(trigger)별로 수집 → {'T-10':meta, 'T-2':meta, 'close':meta}. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_snapshot_build_compare`(2990) · `_apply_result_learning`(17723)

### `_snapshot_build_compare(rk)`
- **위치**: `app.py:2990` (2990~3038)
- **설명**: [비교분석 자동 저장] 3단계(T-10/T-2/마감후) 스냅샷이 모이면 배당·추천 변화량을 산출·저장. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `snapshot_save`(3101) · `snapshot_compare_api`(3187) · `_apply_result_learning`(17723)

### `_pack(meta)`
- **위치**: `app.py:2998` (2998~3006)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_snapshot_build_compare`(2990)

### `_hit(meta)`
- **위치**: `app.py:3051` (3051~3058)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `_snapshot_timing_learn`(3041) · `_pace_log_record`(8916) · `_timeline_snap_apply_result`(31266)

### `_un(combo)`
- **위치**: `app.py:3344` (3344~3346)
- **설명**: 순서무관 정렬 튜플 키. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_odds_map_un`(3349)

### `_odds_map_un(arr)`
- **위치**: `app.py:3349` (3349~3359)
- **설명**: [{combo,odds}] → {정렬튜플: 최저배당}. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 7)**: `_baseline_reset_needed`(1685) · `_as_qmap`(1703) · `_advanced_anomaly`(5336) · `_triple_analyze`(9492) · `_history_append`(13676) · `_horse_series`(5440) … [+1]

### `_win_map_clean(w)`
- **위치**: `app.py:3362` (3362~3379)
- **설명**: 단승 배당 입력({마번:배당} dict 또는 [{no,win}] list) → {마번(str): float} 정규화. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `triple_ingest`(1943) · `_history_append`(13676)

### `_win_map_int(w)`
- **위치**: `app.py:3382` (3382~3390)
- **설명**: {마번(str):배당} → {마번(int):배당}. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_triple_analyze`(9492) · `_history_append`(13676)

### `_odds_map_dir(arr)`
- **위치**: `app.py:3393` (3393~3404)
- **설명**: [{combo,odds}] → {(선,후): 배당} (순서있음, 2마리만). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_exacta_map_any`(4866) · `_triple_analyze`(9492)

### `_form_adj(total)`
- **위치**: `app.py:3424` (3424~3436)
- **설명**: 전적 총점 → 제거 가중치. 전적 없으면 0(중립). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_elimination`(3672)

### `_jockey_place_rate(name)`
- **위치**: `app.py:3444` (3444~3463)
- **설명**: 기수 복승권율(%) — static/data/jockeys.json(KRA 실데이터, mtime 캐시). 없으면 None. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `_elimination`(3672) · `_compare_recommend`(6366) · `_build_ai_training`(15160)

### `_dist_bucket(d)`
- **위치**: `app.py:3491` (3491~3500)
- **설명**: 거리 → 버킷 라벨(경주장·거리별 복승률 키). 짧은/중간/긴 거리 3구간 + 정확거리 병행. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_jp_jockeys_accumulate`(3503) · `_jp_jockey_rate_ctx`(3567)

### `_jp_jockeys_accumulate(details)`
- **위치**: `app.py:3503` (3503~3533)
- **설명**: oddspark 전적(details={lineageNb: past[]})의 (기수·착순)을 기수별 누적(중복 시그니처 방지·최근 30경주 유지). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `keiba_starters`(23095) · `_keiba_autocollect_form`(23159) · `_nar_autocollect_form`(29553)

### `_place_rate_of(placings, min_n)`
- **위치**: `app.py:3536` (3536~3541)
- **설명**: 착순 리스트 → 복승권율(%)=1~3착 비율. 표본 min_n 미만이면 None(불충분). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_jp_jockey_place_rate`(3544) · `_jp_jockey_rate_ctx`(3567)

### `_jp_jockey_place_rate(name)`
- **위치**: `app.py:3544` (3544~3549)
- **설명**: 일본 기수 복승권율(%) = 최근 30경주 중 1~3착 비율. 표본 5경주 미만이면 None(불충분·중립 처리). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_jockey_place_rate`(3444)

### `_jockey_rate_bonus(ctx)`
- **위치**: `app.py:3552` (3552~3564)
- **설명**: [기수 복승률 가감점] 복승률 30%+ → 유력 가점 +10 · 10% 이하 → 제거 감점 -10 · 그 외 0. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_keiba_build_form`(22994) · `_jra_build_form`(26786)

### `_jp_jockey_rate_ctx(name, cur_dist, venue)`
- **위치**: `app.py:3567` (3567~3581)
- **설명**: [강화] 기수 복승률을 거리·경주장 맥락으로 반환. {overall, dist, venue} (%·표본 부족은 None). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_keiba_build_form`(22994) · `_jra_build_form`(26786)

### `_placings_list(placings)`
- **위치**: `app.py:3584` (3584~3585)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `_avg_placing`(3588) · `_prob_ev`(3593) · `_build_ai_training`(15160)

### `_avg_placing(placings)`
- **위치**: `app.py:3588` (3588~3590)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_elimination`(3672)

### `_prob_ev(o, placings)`
- **위치**: `app.py:3593` (3593~3609)
- **설명**: [배당 우선 전환] 시장확률=1/배당×0.75 · 전적확률=최근입상수/경주수 · 통합=시장0.7+전적0.3 · 기대값=통합×배당-1. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_elimination`(3672)

### `_elimination(curQ, curD, exa, drops, form, trio_map, curWin)`
- **위치**: `app.py:3672` (3672~3880)
- **설명**: 배당+전적 복합 제거 판정. 반환: {horses:[...], counts, autoBets}. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(9492)

### `_time_based_drop_signals(rk)`
- **위치**: `app.py:3900` (3900~3948)
- **설명**: 경주 히스토리(발주전분 기록)로 시간대별 마감 임박 급락 신호 생성. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(9492)  ⚠ 위험목록 함수 → 전수 나열

### `_ref(minutes)`
- **위치**: `app.py:3916` (3916~3919)
- **설명**: minutes분 이상 전 스냅샷 중 minutes에 가장 가까운 것(없으면 None) *(주석(본문))*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_time_based_drop_signals`(3900)

### `_pattern_timing_bucket(mb)`
- **위치**: `app.py:3988` (3988~3995)
- **설명**: 급락 발생 시점을 T-N분 버킷으로. (발주전 분) *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_drop_timing_from_timeline`(16391) · `_apply_result_learning`(17723)

### `_extract_patterns(drops, reversals, signals, curQ, bet_rec, advanced)`
- **위치**: `app.py:3998` (3998~4020)
- **설명**: [1·5번] 분석 결과에서 이상감지 패턴 태그 추출. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(9492)

### `_scale_alloc(bet_rec, label, target)`
- **위치**: `app.py:4035` (4035~4046)
- **설명**: [4번] 특정 베팅(label)의 alloc 을 target% 로 두고 나머지를 비례 재분배(합 100 유지). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_adjust_bet_weights`(4049)

### `_adjust_bet_weights(bet_rec, conf)`
- **위치**: `app.py:4049` (4049~4060)
- **설명**: [4번] 패턴 신뢰도로 복승/삼복승 비중 자동 조정. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(9492)

### `_mass_drop_detect(drops, curQ, minutes_before)`
- **위치**: `app.py:4066` (4066~4101)
- **설명**: 전체 복승 조합의 35%+ 또는 30개+ 가 동시에 30%+ 급락하면 '대규모 급락'. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(9492)

### `_excess_drop_analysis(drops, curQ)`
- **위치**: `app.py:4140` (4140~4187)
- **설명**: [1번] 초과 급락률 — 시장 전체 평균 급락 대비 각 말의 집중도. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_triple_analyze`(9492) · `_history_append`(13676)

### `_signal_situation(drops, mass_drop, excess)`
- **위치**: `app.py:4197` (4197~4213)
- **설명**: [3번] 상황별 가중치 자동 조정. 반환 {name, formW, signalW, signalSource, note}. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(9492)

### `_integrated_adaptive(form, curQ, curD, excess, situation)`
- **위치**: `app.py:4216` (4216~4251)
- **설명**: [3번] 상황별 가중치로 통합 등급 재산출(기존 _integrated_grades 40/60은 그대로 두고 별도 추가). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(9492)

### `_near_miss_frequent(min_count)`
- **위치**: `app.py:4379` (4379~4389)
- **설명**: 4착 빈번(min_count회+) 말 이름 집합 → 다음 경주 삼복승 보험픽 우선 고려. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(9492)

### `_pattern_status(fired, rate)`
- **위치**: `app.py:4455` (4455~4464)
- **설명**: 표본수·적중률 → 신뢰도 상태. 반환 (status, icon, note). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_pattern_confidence_view`(4467)

### `_signal_type_hitrates()`
- **위치**: `app.py:4503` (4503~4523)
- **설명**: 유형별 발생/적중/적중률 집계 → 가중치 상향 근거. 판정불가(None)는 분모 제외. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `signal_types_stats`(2777) · `_pattern_confidence_view`(4467) · `_triple_analyze`(9492)

### `_compression_hitrate()`
- **위치**: `app.py:4568` (4568~4586)
- **설명**: 압축 패턴 복승 적중률(강력/중간·급락결합 별) 집계. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `compression_stats`(2787) · `_pattern_confidence_view`(4467)

### `agg(rows)`
- **위치**: `app.py:4572` (4572~4578)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_compression_hitrate`(4568) · `shadow_trifecta_ep`(17188)

### `_high_odds_companion_hitrate()`
- **위치**: `app.py:4633` (4633~4642)
- **설명**: 고배당 1착 경주 중 3착 내 다른 고배당 포함 비율. 반환 {races,hits,rate,reliable}. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `high_odds_companion_stats`(2849) · `_pattern_confidence_view`(4467) · `_triple_analyze`(9492)

### `_tp_reason_bucket(reason)`
- **위치**: `app.py:4661` (4661~4672)
- **설명**: 근거 문자열 → 신호 유형 버킷(급락/역배열/전적/연속하락/이상감지). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_learn_third_place`(4675)

### `_third_place_hitrate()`
- **위치**: `app.py:4702` (4702~4722)
- **설명**: 신호 유형별 3착 후보의 입상(1~3착)·정확3착 적중률 집계. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `third_place_stats`(2796)

### `_deadline_phase_label(mb, after_close)`
- **위치**: `app.py:4758` (4758~4766)
- **설명**: 이상감지 신호의 유효 시점 라벨. 마감 후=회색 표시용. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(9492)

### `_signal_combo_bets(signal_horses, curQ, bet_rec, cap)`
- **위치**: `app.py:4769` (4769~4801)
- **설명**: [신호 조합] 이상감지 신호가 있는 말들의 모든 복승 조합을 추천에 추가(고배당 포함). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(9492)

### `_combo_signal_quality(combo, excess)`
- **위치**: `app.py:4804` (4804~4820)
- **설명**: [4번] 추천 조합의 신호 품질(상/중/하) + 근거. 조합 내 최대 초과급락 말 기준. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(9492)

### `_reversal_level(ratio)`
- **위치**: `app.py:4824` (4824~4836)
- **설명**: [강화] 역전비율(<1)→등급. 배당 차이(=1-ratio) 기준으로 오탐 제거 + 4단계 강도. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_win_exacta_reversal`(4933) · `_mk`(4946)

### `_inversion_tier(diff_pct)`
- **위치**: `app.py:4839` (4839~4850)
- **설명**: [역배열 강도] 배당 차이 %(양수) → 등급. 10% 미만 신호없음(None). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_rank_inversion_detail`(5057)

### `_reversal_role(diff_pct, confirmed)`
- **위치**: `app.py:4853` (4853~4863)
- **설명**: [역배열 강도별 축 역할·사용자 요청] 배당 차이%(양수) + 지속성(2회+ 확정) → 역할. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_reversal_axis_roles`(4897)

### `_exacta_map_any(x)`
- **위치**: `app.py:4866` (4866~4879)
- **설명**: exacta(리스트 [{combo,odds}] 또는 딕셔너리 {'1>2':배당·'1-2'·'1→2'}) → {(선,후):배당} 방향 맵. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_reversal_persistence`(4882)

### `_reversal_axis_roles(wx_reversals, hist, fav_rank, curWin, curQ)`
- **위치**: `app.py:4897` (4897~4930)
- **설명**: [역배열 강도별 처리] challenger별 역할(축/보조/보험) + 지속성(확정/후보) 산출. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(9492)

### `_rep(h)`
- **위치**: `app.py:4904` (4904~4911)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 7)**: `_reversal_axis_roles`(4897) · `_flow_scores`(6835) · `_mid_high_odds_favorites`(6951) · `_core_picks`(7070) · `_confidence_picks`(7117) · `_dense_no_signal_box`(7240) … [+1]

### `_win_exacta_reversal(fav_rank, curD, max_rank)`
- **위치**: `app.py:4933` (4933~4985)
- **설명**: [1번] 쌍승 역전 감지 공식. 단승 유력마 A vs 다른 말 B 방향 비교. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_reversal_persistence`(4882) · `_triple_analyze`(9492)

### `_mk(a, b, ai, bi, multi)`
- **위치**: `app.py:4946` (4946~4965)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 8)**: `_win_exacta_reversal`(4933) · `check_snapshot_coverage`(68) · `check_save_failures_fixed`(152) · `check_save_failures_unfixed`(167) · `check_schema_contract`(180) · `check_schema_drift`(236) … [+2]

### `_quinella_mismatch(fav_rank, curQ)`
- **위치**: `app.py:4988` (4988~5012)
- **설명**: [2번] 복승 불일치 감지 공식. 단승 1+2위 예상 조합 vs 실제 최저복승. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(9492)

### `_best_per_horse(m)`
- **위치**: `app.py:5062` (5062~5071)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_rank_inversion_detail`(5057)

### `_best_first_place(m)`
- **위치**: `app.py:5073` (5073~5084)
- **설명**: 쌍승(방향성) 전용: 각 말이 **1착**일 때의 최저 배당(=시장이 그 말을 승자로 미는 정도). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_rank_inversion_detail`(5057)

### `_inverse_arrangement(fav_rank, has_win, curWin, curQ, wx_reversals, quin_mismatch, excess, form)`
- **위치**: `app.py:5141` (5141~5288)
- **설명**: [역배열 감지] 진짜 역배열 = 시장 인기 순위(배당 낮은 순) ≠ 쌍승 배당 순위가 역전될 때만. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(9492)

### `_add_inv(h)`
- **위치**: `app.py:5151` (5151~5153)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_inverse_arrangement`(5141)

### `_advanced_anomaly(hist, curQ, drops)`
- **위치**: `app.py:5336` (5336~5504)
- **설명**: [2번 고도화] 급락 속도·연속하락/단발반등·페이크베팅·복승 환급률(역수합) 감지. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(9492)

### `_adj(combo, pts)`
- **위치**: `app.py:5345` (5345~5347)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_advanced_anomaly`(5336)

### `_horse_series(no)`
- **위치**: `app.py:5440` (5440~5456)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_advanced_anomaly`(5336)

### `_smart_money_combos(hist, curQ, min_rise, min_drop)`
- **위치**: `app.py:5507` (5507~5549)
- **설명**: [유형8] 상승 후 급락(스마트머니) — 조합 배당이 초반에 상승(인기 이탈)했다가 *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_strong_signals`(5592)

### `_drop_time_weight(mb)`
- **위치**: `app.py:5561` (5561~5577)
- **설명**: [작업2·급락 시간대 가중치] 발주 전 분(mb) → 급락 신뢰도 배수. 학습 데이터 근거: *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_drop_time_tag`(5580) · `_strong_signals`(5592)

### `_drop_time_tag(mb)`
- **위치**: `app.py:5580` (5580~5589)
- **설명**: [작업2] 시간대 강조 라벨. T-1~3분 급락은 '⚡⚡ 최강 신호'로 강조 표시. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_strong_signals`(5592)

### `_strong_signals(advanced, inverse, drops, cur_mb, hist, curQ, after_close)`
- **위치**: `app.py:5592` (5592~5696)
- **설명**: 8유형 강신호 산출(무삭제 — 표시/학습용 파생 뷰). 기존 advanced·inverse·drops·history만 소비. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(9492)

### `add(t, horses, detail, level)`
- **위치**: `app.py:5599` (5599~5602)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_strong_signals`(5592)

### `_horse_repr_odds(h, curWin, curQ)`
- **위치**: `app.py:5700` (5700~5712)
- **설명**: 말 대표 배당 = 단승 우선, 없으면 그 말 포함 최저 복승. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 6)**: `_compression_pattern`(5715) · `_third_place_hunt`(5779) · `_repr_odds_from_rec`(6046) · `_high_odds_companion`(6068) · `consider`(5816) · `repr_odds`(6077)

### `_compression_pattern(key_horses, curWin, curQ, strong_signals, sport)`
- **위치**: `app.py:5715` (5715~5769)
- **설명**: [1·2번] 유력마 TOP3 중 저배당 말이 몰린 축 패턴 감지. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(9492)

### `_third_place_hunt(compression_pattern, strong_signals, anomaly_horse, wx_reversals, form, curWin, curQ, drops, valid_nos)`
- **위치**: `app.py:5779` (5779~5855)
- **설명**: [배당 3착 자동 발굴] 압축 축 2두 + 고배당 3착 후보 → 삼복승 보험 편성. 추정배당은 호출부에서 채움. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(9492)

### `horses_of(*types)`
- **위치**: `app.py:5792` (5792~5798)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_third_place_hunt`(5779)

### `consider(no, priority, reason)`
- **위치**: `app.py:5816` (5816~5832)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_third_place_hunt`(5779)

### `_reversal_backing_bets(inverse, key_horses, curQ, after_close)`
- **위치**: `app.py:5864` (5864~5894)
- **설명**: [히로시마 2R 학습] 역배열이 지목한 '실질 유력마'(invLead: 인기 낮은데 조합배당 최저 = 고배당인데 *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(9492)

### `_forced_trifecta_insurance(cur_mb, after_close, drops, wx_reversals, inverse, strong_signals, key_horses, drop_thresh)`
- **위치**: `app.py:5897` (5897~5950)
- **설명**: T-2분 이내 급락(강급락 pct<=drop_thresh)과 역배열을 동시에 보인 말을 찾아 *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(9492)

### `_closing_drop_insurance(cur_mb, drops, key_horses, valid_nos, mass_drop, drop_thresh)`
- **위치**: `app.py:5953` (5953~5999)
- **설명**: [마감 순간 급락 삼복승 보험] T-0(마감) 전후 급락 말 → 축(유력마 상위2) + 급락말 삼복승 보험 자동 편성. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(9492)

### `_is_pass_race(an)`
- **위치**: `app.py:6003` (6003~6018)
- **설명**: 패스 권고 경주 판정: ①추천 게이트(신호 대기·패스형) 또는 ②전적없음+강신호없음. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_apply_result_learning`(17723)

### `_recommend_flex(strong_signals, signal_confidence, after_close)`
- **위치**: `app.py:6022` (6022~6042)
- **설명**: 신호 강도별 추천 말 수 가이드(강제 아님·안내). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(9492)

### `_repr_odds_from_rec(rec)`
- **위치**: `app.py:6046` (6046~6065)
- **설명**: rec(단승/복승)에서 말별 대표배당 맵 + curWin/curQ 재구성(학습·분석 공용). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_learn_high_odds_companion`(4606)

### `_high_odds_companion(drops, wx_reversals, inverse, curWin, curQ, key_horses, learned, after_close, high_thresh)`
- **위치**: `app.py:6068` (6068~6148)
- **설명**: [참고 추천] 고배당(7배+) 신호말 감지 시 그 말과 복승 배당 낮게 묶인 다른 고배당 말 탐색. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(9492)

### `repr_odds(h)`
- **위치**: `app.py:6077` (6077~6078)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_high_odds_companion`(6068)

### `_capital_preservation(combos)`
- **위치**: `app.py:6152` (6152~6180)
- **설명**: [2번] 원금 보전 자동 계산(예산 무관 '비율' 산출 → 프론트가 예산 곱함). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_bmed_strategy`(6286)

### `_expected_return(plan, curQ)`
- **위치**: `app.py:6183` (6183~6194)
- **설명**: [3번] 기대 환수율 = Σ(적중확률_i × 회수비율_i). 확률=시장 복승 역수(1/o) 정규화(전체 조합). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_bmed_insurance`(6197) · `_bmed_strategy`(6286)

### `_bmed_insurance(key_horses, curQ, signal_confidence, inverse, sport)`
- **위치**: `app.py:6197` (6197~6283)
- **설명**: [보험용 추천] BMED 보험형 매트릭스 — 1착 유력마 축 + 2·3·4위 상대 3조합. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_bmed_strategy`(6286)

### `_median(vals)`
- **위치**: `app.py:6264` (6264~6268)
- **설명**: [보완#2 BUG A] 중간값 = 정확한 중앙값. 조합 2개(짝수)일 때 sorted[n//2]는 최댓값과 같아져 *(주석(def 위))*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_bmed_insurance`(6197)

### `_co(pairs)`
- **위치**: `app.py:6300` (6300~6307)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_bmed_strategy`(6286)

### `_pairs(hs)`
- **위치**: `app.py:6309` (6309~6311)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_bmed_strategy`(6286)

### `_compare_recommend(form, key_horses, excess, drops, bet_rec)`
- **위치**: `app.py:6366` (6366~6394)
- **설명**: [1번] 이상감지 기반 / 전적 기반 / 최종 추천 조합을 각각 산출(복승 2두·삼복승 3두). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(9492)

### `pt(order)`
- **위치**: `app.py:6370` (6370~6373)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_compare_recommend`(6366)

### `_recommend_basis(top_horses, form, elimination, drops, wx_reversals, advanced, signal_confidence, basis_weights)`
- **위치**: `app.py:6397` (6397~6450)
- **설명**: [추천 근거 상세 카드] 상위 추천마(최대 3두)별 근거: 전적·배당·기수·종합확신도. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(9492)

### `_g(m, no)`
- **위치**: `app.py:6409` (6409~6410)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_recommend_basis`(6397) · `_simulate_race_flow_jp`(27427)

### `_bet_judgment(conf_engine, excess, advanced, wx_reversals, form, mass_drop, after_close, signal_ready)`
- **위치**: `app.py:6534` (6534~6621)
- **설명**: [개편·실전] 경주 유형 자동 판정 + 배팅 배분 비율. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(9492)

### `_stage_guide(cur_mb, after_close, judgment, inverse, advanced)`
- **위치**: `app.py:6624` (6624~6649)
- **설명**: [3번] 발주 전 시점(T-3/T-2/T-1/T-30초)별 단계 추천 + 최종 등급. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(9492)

### `_elimination_strong(elimination, form, drops)`
- **위치**: `app.py:6652` (6652~6685)
- **설명**: [5번] 과감한 제거마 목록(기존 _elimination 판정 유지, 강화 파생만). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(9492)

### `_chaotic_race(curQ, curWin, key_horses, anomaly_horse, drops)`
- **위치**: `app.py:6688` (6688~6806)
- **설명**: [혼전 경주] 상위 배당이 근접해 이변 가능성이 큰 경주를 감지하고 고배당 포함 삼복승 전략을 편성. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(9492)

### `_q(a, b)`
- **위치**: `app.py:6746` (6746~6747)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 6)**: `_chaotic_race`(6688) · `_confidence_picks`(7117) · `_triple_analyze`(9492) · `_trio_est`(6749) · `_add`(7174) · `_trio_est`(10530)

### `_trio_est(cc)`
- **위치**: `app.py:6749` (6749~6762)
- **설명**: 삼복승 실배당 미수집 → 구성 복승 3쌍 기하평균×2 ÷보정계수 추정. (odds, is_estimate). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 6)**: `_chaotic_race`(6688) · `_dense_no_signal_box`(7240) · `_triple_analyze`(9492) · `_mk_trio`(6764) · `_tri_odds`(11048) · `_edtri_odds`(11076)

### `_mk_trio(label, cc, alloc, high_return)`
- **위치**: `app.py:6764` (6764~6773)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_chaotic_race`(6688)

### `_horse_rep_series(hist, no, n)`
- **위치**: `app.py:6812` (6812~6832)
- **설명**: 최근 n스냅샷의 말별 대표배당(단승 우선·없으면 최저 복승) 시퀀스. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_flow_scores`(6835)

### `_rep(no)`
- **위치**: `app.py:6860` (6860~6867)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 7)**: `_reversal_axis_roles`(4897) · `_flow_scores`(6835) · `_mid_high_odds_favorites`(6951) · `_core_picks`(7070) · `_confidence_picks`(7117) · `_dense_no_signal_box`(7240) … [+1]

### `_flow_removal(flow, key_horses, keep_low_odds)`
- **위치**: `app.py:6915` (6915~6931)
- **설명**: [1번] 흐름 기반 제거 대상: 죽은인기/3연속상승/페이크/역배열반대. 유력마 축(상위2)은 제외(안전). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(9492)

### `_high_odds_candidates(flow, removed_nos, key_horses, min_odds)`
- **위치**: `app.py:6934` (6934~6948)
- **설명**: [2번] 제거 후 남은 말 중 배당 10배+ 인데 하락/급락(흐름 좋음) → 고배당 후보 자동 편입. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(9492)

### `_mid_high_odds_favorites(valid_nos, curWin, curQ, drops, dark_horses, reversal_roles, form, min_odds)`
- **위치**: `app.py:6951` (6951~7000)
- **설명**: [중고배당 유력마·1번] 복승 10배+ 인데 강한 신호 1개+ 보유 → 💎 중고배당 유력마. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(9492)

### `_rep(no)`
- **위치**: `app.py:6955` (6955~6962)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 7)**: `_reversal_axis_roles`(4897) · `_flow_scores`(6835) · `_mid_high_odds_favorites`(6951) · `_core_picks`(7070) · `_confidence_picks`(7117) · `_dense_no_signal_box`(7240) … [+1]

### `_cross_reversal(curQ, curD, fav_rank, valid_nos, max_rank)`
- **위치**: `app.py:7003` (7003~7067)
- **설명**: [복승 크로스 역배열·전체 말 쌍 비교] 각 말 X에 대해 인기 상위쌍(A인기>B, 상위 max_rank위) 조합에서 *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(9492)

### `_w(ra, rb)`
- **위치**: `app.py:7014` (7014~7019)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_cross_reversal`(7003)

### `_qo(a, b)`
- **위치**: `app.py:7021` (7021~7022)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 6)**: `_cross_reversal`(7003) · `_scenario_plan`(7308) · `_final_picks`(7357) · `_triple_analyze`(9492) · `_qi`(8244) · `_tri_est`(8261)

### `_xo(a, b)`
- **위치**: `app.py:7024` (7024~7025)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_cross_reversal`(7003)

### `_rep(no)`
- **위치**: `app.py:7074` (7074~7081)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 7)**: `_reversal_axis_roles`(4897) · `_flow_scores`(6835) · `_mid_high_odds_favorites`(6951) · `_core_picks`(7070) · `_confidence_picks`(7117) · `_dense_no_signal_box`(7240) … [+1]

### `_conf(no)`
- **위치**: `app.py:7129` (7129~7130)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_confidence_picks`(7117)

### `_rep(no)`
- **위치**: `app.py:7132` (7132~7139)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 7)**: `_reversal_axis_roles`(4897) · `_flow_scores`(6835) · `_mid_high_odds_favorites`(6951) · `_core_picks`(7070) · `_confidence_picks`(7117) · `_dense_no_signal_box`(7240) … [+1]

### `_high(no)`
- **위치**: `app.py:7141` (7141~7143)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_confidence_picks`(7117) · `_add`(7174)

### `_q(a, b)`
- **위치**: `app.py:7145` (7145~7148)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 6)**: `_chaotic_race`(6688) · `_confidence_picks`(7117) · `_triple_analyze`(9492) · `_trio_est`(6749) · `_add`(7174) · `_trio_est`(10530)

### `_add(a, b, reason)`
- **위치**: `app.py:7174` (7174~7186)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_confidence_picks`(7117) · `_rec_combos_from_analysis_log`(17556)

### `_dense_no_signal_box(key_horses, valid_nos, curWin, curQ, trio_map, drops, wx_reversals, dark_horses, strong_signals, dense_odds, strong_drop)`
- **위치**: `app.py:7240` (7240~7286)
- **설명**: [무신호 저배당밀집] 급락(≤-30%)·쌍승역전·복병·강신호가 전부 0건이고 상위 배당이 밀집(대표배당 15배 이내)이면, *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(9492)

### `_rep(no)`
- **위치**: `app.py:7253` (7253~7260)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 7)**: `_reversal_axis_roles`(4897) · `_flow_scores`(6835) · `_mid_high_odds_favorites`(6951) · `_core_picks`(7070) · `_confidence_picks`(7117) · `_dense_no_signal_box`(7240) … [+1]

### `_quinella_target(n_horses, chaotic)`
- **위치**: `app.py:7289` (7289~7305)
- **설명**: [근거 기반 추천·두수별 복승 개수] 출전 두수별 복승 최대 개수 + 혼전 시 +2(상한 8). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(9492)

### `_scenario_plan(cp, curQ, pace_analysis, sig_meta, valid_nos, axis_plan)`
- **위치**: `app.py:7308` (7308~7354)
- **설명**: [시나리오 조합 자동 생성] 전적·각질·편성·배당 신호 종합 → 2가지 시나리오. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_final_picks`(7357)

### `_sc(n)`
- **위치**: `app.py:7319` (7319~7320)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_scenario_plan`(7308)

### `_qo(a, b)`
- **위치**: `app.py:7322` (7322~7325)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 6)**: `_cross_reversal`(7003) · `_scenario_plan`(7308) · `_final_picks`(7357) · `_triple_analyze`(9492) · `_qi`(8244) · `_tri_est`(8261)

### `_vpair(cc)`
- **위치**: `app.py:7371` (7371~7373)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_final_picks`(7357)

### `_vtri(cc)`
- **위치**: `app.py:7375` (7375~7377)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_final_picks`(7357)

### `_has_sig(combo)`
- **위치**: `app.py:7379` (7379~7380)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_final_picks`(7357) · `_combo_has_signal`(7501)

### `_mh(no)`
- **위치**: `app.py:7477` (7477~7478)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 7)**: `_final_picks`(7357) · `_combo_basis`(7480) · `_combo_score`(7498) · `_combo_has_signal`(7501) · `_combo_strong`(7507) · `_combo_has_signal_relaxed`(7523) … [+1]

### `_combo_basis(combo)`
- **위치**: `app.py:7480` (7480~7496)
- **설명**: 조합 근거 문장 리스트 — 급락%·스마트머니·역배열·복병·집중급락(말별). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_final_picks`(7357) · `_force_pair`(7766)

### `_combo_has_signal(combo)`
- **위치**: `app.py:7501` (7501~7505)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_final_picks`(7357)

### `_combo_strong(combo)`
- **위치**: `app.py:7507` (7507~7521)
- **설명**: BMED 특별 강신호: signalScore 70+ OR 급락15%+ OR (스마트머니+역배열 동시). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_final_picks`(7357)

### `_combo_has_signal_relaxed(combo)`
- **위치**: `app.py:7523` (7523~7535)
- **설명**: [메인 보완·완화] 급락 10%+ OR signalScore 50+ OR 기존 신호(drop/rev/smart/anomaly). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_final_picks`(7357)

### `_force_pair(_pa, _pb, _reason, _summary)`
- **위치**: `app.py:7766` (7766~7788)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_final_picks`(7357)

### `_prio_c(_q)`
- **위치**: `app.py:7927` (7927~7933)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_final_picks`(7357)

### `_qo(a, b)`
- **위치**: `app.py:8241` (8241~8242)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 6)**: `_cross_reversal`(7003) · `_scenario_plan`(7308) · `_final_picks`(7357) · `_triple_analyze`(9492) · `_qi`(8244) · `_tri_est`(8261)

### `_qi(a, b, label, rank)`
- **위치**: `app.py:8244` (8244~8247)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_final_picks`(7357)

### `_tri_est(a, b, c)`
- **위치**: `app.py:8261` (8261~8271)
- **설명**: 삼복승 축1+축2+연결 — 실배당 미수집이라 구성 복승 3쌍 기하평균×2 추정(표시용) *(주석(def 위))*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_final_picks`(7357)

### `_qod(a, b)`
- **위치**: `app.py:8295` (8295~8296)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_final_picks`(7357) · `_qitem`(8341)

### `_qitem(a, b, label)`
- **위치**: `app.py:8341` (8341~8345)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_final_picks`(7357)

### `_ev_band_key(o)`
- **위치**: `app.py:8517` (8517~8521)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_ev_band_p`(8527) · `_ev_bands_update`(8548)

### `_ev_band_p(o)`
- **위치**: `app.py:8527` (8527~8545)
- **설명**: 배당대별 적중률 추정 — 학습 표본 50+ 밴드는 실측치(자동 교체), 아니면 보수 가정치. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_apply_profit_strategy`(8583)

### `_ev_bands_update(combo_odds_list, top2)`
- **위치**: `app.py:8548` (8548~8566)
- **설명**: [기대값 학습] 판정된 라이브 복승 추천의 (배당대, 적중) 누적 → 표본 50+부터 가정치 자동 교체. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_apply_result_learning`(17723)

### `_profit_tier_of(sport, category, min_q)`
- **위치**: `app.py:8569` (8569~8580)
- **설명**: 경주 3분류: low(삼복승 집중)/mid/high — 종목별 경계(경륜류 1.8/5 · 일본경마 2/8 · 한국경마 3/10). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_apply_profit_strategy`(8583)

### `_demote(_q, _why)`
- **위치**: `app.py:8608` (8608~8612)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_apply_profit_strategy`(8583)

### `_conn_pair_odds(_c)`
- **위치**: `app.py:8771` (8771~8778)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 0)**: —

### `_hit(cs)`
- **위치**: `app.py:8933` (8933~8934)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `_snapshot_timing_learn`(3041) · `_pace_log_record`(8916) · `_timeline_snap_apply_result`(31266)

### `_dark_sport_of(rk, an)`
- **위치**: `app.py:8996` (8996~9013)
- **설명**: raceKey/분석에서 종목 라벨(keirin/keiba/korea/boat/bike). category 우선. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_dark_log_record`(9142)

### `_dark_odds_band(odds)`
- **위치**: `app.py:9016` (9016~9026)
- **설명**: 복병 배당 구간 라벨(15~30/30~50/50~100/100+). 15배 미만은 None(복병 아님). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_dark_stats_recompute`(9265)

### `_norm_sig_key(s)`
- **위치**: `app.py:9029` (9029~9041)
- **설명**: [신호 라벨 정규화] 이중표기('크로스역배열 크로스역배열')·이모지·인접중복을 단일 라벨로 축약 → 통계 키 통일. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_dark_signal_of`(9044) · `_dark_stats_recompute`(9265)

### `_dark_signal_of(no, an)`
- **위치**: `app.py:9044` (9044~9076)
- **설명**: [복병 신호 우선순위] 말 no의 복병 신호 판정 → (priority, label). priority 낮을수록 강함(1>2>3). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_dark_rank_horses`(9079) · `_analyze_missed_dark_horse`(9232)

### `_dark_combo_section(key_horses, dark_horses, curQ)`
- **위치**: `app.py:9103` (9103~9134)
- **설명**: [복병 복승 자동편성] 유력마 1위 + 복병 1·2순위 → 복승 2조합 + 삼복승 1조합(별도 섹션). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(9492)

### `_od(a, b)`
- **위치**: `app.py:9124` (9124~9125)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_dark_combo_section`(9103)

### `_dark_log_path(rk)`
- **위치**: `app.py:9137` (9137~9139)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_dark_log_record`(9142) · `_dark_log_apply_result`(9175)

### `_dark_log_apply_result(rk, top3, an, quinella_odds)`
- **위치**: `app.py:9175` (9175~9229)
- **설명**: [결과 시점 분석] 복병 rank1~3 입상 판정 + 미감지 사후분석 → dark_horse_log 갱신 + 통계 재계산. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_apply_result_learning`(17723) · `dark_horse_review`(19512)

### `_place(no)`
- **위치**: `app.py:9195` (9195~9199)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_dark_log_apply_result`(9175)

### `_rate(d)`
- **위치**: `app.py:9341` (9341~9343)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 4)**: `_dark_stats_recompute`(9265) · `_threshold_candidates`(15448) · `_recompute_learning_stats`(16943) · `_form_ext_build`(27752)

### `_dark_case_similar(dh)`
- **위치**: `app.py:9368` (9368~9381)
- **설명**: 현재 복병(dh)과 유사한 과거 복병 고배당 '적중' 사례 수(등급 이상·배당대 근접). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(9492)

### `_rep_o_dark(h)`
- **위치**: `app.py:9928` (9928~9935)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(9492)

### `_mkt_repr(h)`
- **위치**: `app.py:10018` (10018~10023)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(9492)

### `_rep_o(h)`
- **위치**: `app.py:10100` (10100~10107)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(9492)

### `_rt_rep(h)`
- **위치**: `app.py:10127` (10127~10134)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(9492)

### `_dark_repr2(h)`
- **위치**: `app.py:10220` (10220~10227)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(9492)

### `_q(a, b)`
- **위치**: `app.py:10312` (10312~10313)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 6)**: `_chaotic_race`(6688) · `_confidence_picks`(7117) · `_triple_analyze`(9492) · `_trio_est`(6749) · `_add`(7174) · `_trio_est`(10530)

### `_addbet(kind, label, combo, alloc, odds)`
- **위치**: `app.py:10317` (10317~10326)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(9492)

### `_trio_est(cc)`
- **위치**: `app.py:10530` (10530~10542)
- **설명**: [3번] 삼복승 실배당 미수집 시: 구성 복승 3쌍의 기하평균×2 로 추정(라벨=추정) *(주석(def 위))*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 6)**: `_chaotic_race`(6688) · `_dense_no_signal_box`(7240) · `_triple_analyze`(9492) · `_mk_trio`(6764) · `_tri_odds`(11048) · `_edtri_odds`(11076)

### `_push_sig(h)`
- **위치**: `app.py:10584` (10584~10586)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(9492)

### `_min_odds(arr)`
- **위치**: `app.py:10667` (10667~10669)
- **설명**: 시계열 차트: 복승·쌍승·삼복승 각 "최저(최인기) 배당"의 라운드별 변화 (3줄) *(주석(def 위))*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(9492)

### `_drop_reason(pct)`
- **위치**: `app.py:10711` (10711~10718)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(9492)

### `_tri_odds(cc)`
- **위치**: `app.py:11048` (11048~11058)
- **설명**: [보완②] 삼복승 예상배당 표기 — 실배당(trio_map) 우선, 없으면 _trio_est 추정. *(주석(def 위))*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(9492)

### `_edtri_odds(cc)`
- **위치**: `app.py:11076` (11076~11083)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(9492)

### `_qo(_x, _y)`
- **위치**: `app.py:11239` (11239~11240)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 6)**: `_cross_reversal`(7003) · `_scenario_plan`(7308) · `_final_picks`(7357) · `_triple_analyze`(9492) · `_qi`(8244) · `_tri_est`(8261)

### `_hscore(_no)`
- **위치**: `app.py:11272` (11272~11274)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(9492)

### `_sm(_no)`
- **위치**: `app.py:11276` (11276~11278)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(9492)

### `_cycle_t2_strong_horses(an)`
- **위치**: `app.py:12204` (12204~12223)
- **설명**: [t2_strong_cycle 예외 편입 판정] 마감 2분(T-2) 이후 '급락 30%+' 또는 '집중급락' 신호에 걸린 말 집합. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_apply_rec_hysteresis`(12226)

### `_apply_rec_hysteresis(rk, an)`
- **위치**: `app.py:12226` (12226~12326)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(9492)

### `_apply_tri_hysteresis(rk, an)`
- **위치**: `app.py:12336` (12336~12399)
- **설명**: ══════════ [② 삼복승 메인 히스테리시스 (2026-07-28)] 복승과 동일 규칙을 삼복승에 대칭 적용 ══════════ *(주석(def 위))*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(9492)

### `_bet_market(rk, sport)`
- **위치**: `app.py:12444` (12444~12451)
- **설명**: raceKey·종목 → 베팅 규칙 시장 키(korea/cycle/japan). 한국이 최우선(경마장명이 확실한 판별자). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_apply_bet_rules`(12559)

### `_combo_odds_trend(rk, combos)`
- **위치**: `app.py:12491` (12491~12544)
- **설명**: {조합: {'first','last','low','surge','rebound'}} — triple_store history(최근 12틱) 기반. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_apply_bet_rules`(12559)

### `_apply_bet_rules(rk, an)`
- **위치**: `app.py:12559` (12559~12717)
- **설명**: 시장별 베팅 규칙 적용. 실패해도 원본 무변경(호출부에서 예외 흡수). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(9492)

### `_rk_venue_num(k)`
- **위치**: `app.py:12782` (12782~12791)
- **설명**: raceKey/경주명에서 (경마장, 경주번호) 추출. 예:'2026-07-03 서울 5R' → ('서울',5). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `triple_match`(12810)

### `_is_japan_key(k)`
- **위치**: `app.py:12804` (12804~12806)
- **설명**: raceKey에 일본 경마장명이 들어있으면 True(한국 경주 매칭에서 제외용). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `triple_match`(12810)

### `_alert_meta(rk)`
- **위치**: `app.py:12866` (12866~12871)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `_record_alert`(12874) · `_match_alerts_to_result`(12915) · `alerts_get`(17371)

### `_match_alerts_to_result(rk, top3, an)`
- **위치**: `app.py:12915` (12915~12943)
- **설명**: [2번] 결과 입력 시 경고 내역과 자동 매칭 — 경고말 입상 여부·경고 무시(추천 누락) 판정. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_apply_result_learning`(17723)

### `_pub_deadline_left(mb)`
- **위치**: `app.py:12957` (12957~12964)
- **설명**: 발주 남은 분(float) → '8분 10초' 표기. 마감 후면 '마감'. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `public_matrix`(13414)

### `_pub_roles(an)`
- **위치**: `app.py:12983` (12983~13012)
- **설명**: 말별 역할(fav/cut/weakcut/dark) — 오버레이 matrixRoles 와 동일 규칙. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `public_matrix`(13414)

### `_pub_matrix(an, role)`
- **위치**: `app.py:13015` (13015~13085)
- **설명**: 복승 매트릭스 + 조합별 신호색. 키는 배당판 기준 '작은번호-큰번호'. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `public_matrix`(13414)

### `_pub_flow(an, no)`
- **위치**: `app.py:13088` (13088~13091)
- **설명**: 말별 배당흐름(flowScores) 조회. 키가 int/str 두 형태로 올 수 있어 둘 다 대응. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_pub_horse_why`(13100) · `_pub_horse_cards`(13326)

### `_pub_conf(an, no)`
- **위치**: `app.py:13094` (13094~13097)
- **설명**: 말별 확신도(confidence.horses) 조회. 키 int/str 양쪽 대응. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_pub_horse_why`(13100)

### `_pub_horse_why(no, an, role)`
- **위치**: `app.py:13100` (13100~13155)
- **설명**: 이 말이 왜 유력/복병/제거인지 — 쉬운 말 한 줄들(기존 신호 재사용·재계산 없음). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_pub_recommendation`(13158) · `_pub_horse_cards`(13326)

### `_pub_recommendation(an, role)`
- **위치**: `app.py:13158` (13158~13291)
- **설명**: 카드 2·3용 추천 + '왜 이 조합인지' 부연설명. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `public_matrix`(13414)

### `_qtxt(q)`
- **위치**: `app.py:13258` (13258~13260)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_pub_recommendation`(13158)

### `_atxt(q)`
- **위치**: `app.py:13274` (13274~13276)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_pub_recommendation`(13158)

### `_pub_pace(an)`
- **위치**: `app.py:13294` (13294~13323)
- **설명**: [7] 각질 편성 분석 공개용 — 편성 집계·페이스·유불리·주목마·시나리오. 없으면 None(패널 숨김). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `public_matrix`(13414)

### `_fmt(q)`
- **위치**: `app.py:13312` (13312~13314)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 5)**: `_pub_pace`(13294) · `kra_status`(25511) · `_hist_row_from_doc`(30059) · `_timeline_snap_tick`(31140) · `drop_stats`(331)

### `_pub_horse_cards(an, role, nos)`
- **위치**: `app.py:13326` (13326~13361)
- **설명**: 말별 신호 카드 — 등급·역할·태그·부연설명. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `public_matrix`(13414)

### `_pub_result(rk)`
- **위치**: `app.py:13364` (13364~13381)
- **설명**: 경주 결과(있으면) — 예상 vs 결과 비교 카드용. 없으면 None. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `public_matrix`(13414)

### `_hist_path(rk)`
- **위치**: `app.py:13561` (13561~13568)
- **설명**: raceKey → (파일경로, date, race). 예: '2026-07-02 나고야 4경주' / '나고야 4경주'. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 26)**: `_ingest_reject_log`(2132) · `anomaly_feed`(2742) · `_snapshot_signal_first`(2945) · `_time_based_drop_signals`(3900) · `_triple_analyze`(9492) · `_pub_result`(13364) … [+20]

### `_archive_compress_old(days)`
- **위치**: `app.py:13587` (13587~13625)
- **설명**: [영구보존·4번] 7일+ 지난 odds_history .json 을 .json.gz 로 압축(데이터 삭제 없이 메모리·용량 관리). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `odds_archive_compress`(17445) · `_boot_background`(32285)

### `_combo_dict(arr)`
- **위치**: `app.py:13628` (13628~13638)
- **설명**: [{combo,odds}] → {'1+2': 45.2, ...}. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_history_append`(13676)

### `_exa_fav_dirs(exa_dict)`
- **위치**: `app.py:13644` (13644~13673)
- **설명**: 쌍승(방향성) 배당 dict('a+b'→odds) → {무순쌍: (유력방향튜플, 유력배당)}. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_history_append`(13676)

### `_pick(label)`
- **위치**: `app.py:13933` (13933~13935)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_history_save_analysis`(13914) · `_dl_reliability`(15821)

### `_corrupt_note(path, reason, tag, dst)`
- **위치**: `app.py:14030` (14030~14056)
- **설명**: 격리 사실을 `data/collect_gaps/<날짜>.json` 에 남긴다(조용히 넘어가지 않게). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_corrupt_quarantine`(14076) · `_json_load_guard`(14098)  ⚠ 위험목록 함수 → 전수 나열

### `_corrupt_quarantine(path, err, tag)`
- **위치**: `app.py:14076` (14076~14095)
- **설명**: 손상 원본을 `.corrupt.<ts>` **사본**으로 격리(원본은 그대로 둔다 — 이동하면 그것도 소실이다). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_json_load_guard`(14098)

### `_analysis_log_path(rk)`
- **위치**: `app.py:14142` (14142~14149)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 23)**: `_triple_analyze`(9492) · `triple_analyze`(13461) · `_canonical_log_key`(14152) · `_build_analysis_log`(14451) · `_learn_aux_combos`(17498) · `_rec_combos_from_analysis_log`(17556) … [+17]

### `_canonical_log_key(rk, live)`
- **위치**: `app.py:14152` (14152~14231)
- **설명**: [중복 key 근본 방지] 같은 경주가 다른 표기(한글 '모리오카 1경주' / 한자 '2026-07-07 盛岡 1R')로 *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 18)**: `_pace_log_record`(8916) · `_triple_analyze`(9492) · `_build_analysis_log`(14451) · `_learn_aux_combos`(17498) · `_rec_combos_from_analysis_log`(17556) · `recommend_manual`(17627) … [+12]

### `_doc_has_recs(d)`
- **위치**: `app.py:14162` (14162~14165)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_canonical_log_key`(14152)

### `_det_combo_sets(items)`
- **위치**: `app.py:14250` (14250~14261)
- **설명**: [{combo:[...]}...] → {frozenset(마번)} 집합. 잘못된 항목은 조용히 건너뛴다. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_deterministic_review`(14264)

### `_raw_profile_snapshot(rk)`
- **위치**: `app.py:14410` (14410~14448)
- **설명**: [소실 방지 (2026-07-29)] 경주 종료 시 영구 소실되는 **원본 입력**을 분석 로그에 복사해 남긴다. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_build_analysis_log`(14451)

### `_combo_reason(nos)`
- **위치**: `app.py:14548` (14548~14550)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_build_analysis_log`(14451)

### `_rec_sig(fr, kh)`
- **위치**: `app.py:14576` (14576~14587)
- **설명**: [복승 ①②③ 이력 (2026-07-19)] 메인만 비교하던 sig에 finalQuinellas 전체 구성 포함 — *(주석(본문))*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_build_analysis_log`(14451)

### `_cp_has_recs(cp)`
- **위치**: `app.py:14638` (14638~14641)
- **설명**: [추천 보존·근본수정] 경기 종료 후 재저장(결과 입력 등) 시 an 이 비어 corePicks/final_recommendation 이 *(주석(def 위))*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_build_analysis_log`(14451)

### `_fr_has_recs(fr)`
- **위치**: `app.py:14643` (14643~14644)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_build_analysis_log`(14451)

### `_strip_race_dist(race)`
- **위치**: `app.py:14838` (14838~14841)
- **설명**: [중복통합] raceKey/경주명에서 끝의 거리접미(예: ' 1200M', '900M')만 제거 → 한 경주 = 한 표기. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `_canon_rk`(1874) · `_analysis_log_path`(14142) · `_race_result_id`(14844)

### `_race_result_id(rk)`
- **위치**: `app.py:14844` (14844~14864)
- **설명**: raceKey → race_id(파일명). 예: '2026-07-05 사가 8경주' → '2026-07-05_사가_8경주'. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 11)**: `_race_result_path`(14867) · `_build_race_result`(14939) · `_missing_results`(15099) · `_ai_training_path`(15148) · `_build_ai_training`(15160) · `_apply_result_learning`(17723) … [+5]

### `_race_result_path(rk)`
- **위치**: `app.py:14867` (14867~14884)
- **설명**: [중복통합·호환 탐색] canonical 파일이 없고, 같은 경주의 '거리접미 변형' 파일이 이미 있으면 그걸 재사용 *(주석(본문))*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `_save_race_result`(15079) · `_competitor_result_for_race`(20031) · `_race_result_summary`(30223)

### `_validate_race_result(data)`
- **위치**: `app.py:14887` (14887~14911)
- **설명**: [4번] 데이터 품질 검증 — 마번 1~16, 배당 1.0~9999, 날짜 형식, 필수값. 오류 리스트 반환(빈=정상). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_save_race_result`(15079)

### `_extract_track_round(rk)`
- **위치**: `app.py:14914` (14914~14916)
- **설명**: raceKey → (track 경마장명, round 경주번호). _area_num 재사용(부경=부산 정규화 포함). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_build_race_result`(14939) · `_build_ai_training`(15160)

### `_rk_sport(rk, an)`
- **위치**: `app.py:14919` (14919~14936)
- **설명**: [오류7] raceKey/분석에서 종목 자동 분류(horse/cycle/boat/bike/korea). 결과·학습 태깅 통일용. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `_build_race_result`(14939) · `_apply_result_learning`(17723) · `_ml_row_build`(18437)

### `_build_race_result(rk, an, record, result, top4, inputs)`
- **위치**: `app.py:14939` (14939~15076)
- **설명**: [1번] 경주별 완전 저장 구조 조립(AI 학습용). an/record/odds_history/입력값 종합. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_save_race_result`(15079)

### `_missing_results(date)`
- **위치**: `app.py:15099` (15099~15137)
- **설명**: [3번] 당일 분석했으나 결과 미입력 경주 추적. analysis_log 스캔 → 결과 없는 raceKey 목록. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 8)**: `race_results_missing`(20537) · `_kra_result_backfill_once`(23820) · `kra_results_backfill`(23900) · `_keirin_result_backfill_once`(24371) · `keirin_results_backfill`(25291) · `_jp_result_backfill_once`(25313) … [+2]

### `_ai_training_path(rk)`
- **위치**: `app.py:15148` (15148~15151)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_save_ai_training`(15363)

### `_odds_range_label(o)`
- **위치**: `app.py:15154` (15154~15157)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_build_ai_training`(15160)

### `_build_ai_training(rk, an, record, result, top4, inputs)`
- **위치**: `app.py:15160` (15160~15300)
- **설명**: [1번] AI 학습용 완전 데이터 구조 조립 — 말별 피처 + 배당 피처 + 예측 + 결과 + 라벨. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_save_ai_training`(15363)

### `_placed(h)`
- **위치**: `app.py:15260` (15260~15264)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_build_ai_training`(15160)

### `_ai_timeline(rk)`
- **위치**: `app.py:15303` (15303~15309)
- **설명**: odds_history 스냅샷 → 학습용 배당 타임라인. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_build_ai_training`(15160)  ⚠ 위험목록 함수 → 전수 나열

### `_ai_rows_for_thresholds()`
- **위치**: `app.py:15413` (15413~15445)
- **설명**: ai_training 파일에서 기준치 계산에 필요한 필드만 추출(결과 입력된 경주만). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_threshold_candidates`(15448)

### `_threshold_candidates()`
- **위치**: `app.py:15448` (15448~15496)
- **설명**: [2·3·5번] 누적 데이터로 신호 기준치별 적중률 계산 → 최적 기준치 추천 + 현재 대비 개선폭. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `thresholds_optimize`(2812)

### `_rate(fired_placed)`
- **위치**: `app.py:15455` (15455~15458)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 4)**: `_dark_stats_recompute`(9265) · `_threshold_candidates`(15448) · `_recompute_learning_stats`(16943) · `_form_ext_build`(27752)

### `_best(cands, cur_val, key_name)`
- **위치**: `app.py:15475` (15475~15489)
- **설명**: 표본 THRESHOLD_SAMPLE_MIN+ 인 후보 중 적중률 최고. 현재값 적중률과 비교. *(주석(본문))*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_threshold_candidates`(15448)

### `_ai_data_status()`
- **위치**: `app.py:15499` (15499~15542)
- **설명**: [3·7번] AI 학습 데이터 현황 — 수집/고품질/평균품질·목표 진행률·마일스톤(100/500) 예상 일정. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `ai_training_status`(20543)

### `_eta_days(target)`
- **위치**: `app.py:15524` (15524~15526)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_ai_data_status`(15499)

### `_build_daily_summary(date)`
- **위치**: `app.py:15549` (15549~15588)
- **설명**: [6번] 당일 경주 요약 저장 — 총경주·평균품질·이상감지·적중·적중률·AI준비 경주·누적. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `_daily_learning_generate`(15846) · `_apply_result_learning`(17723) · `daily_summary`(20589)

### `_loop()`
- **위치**: `app.py:15693` (15693~15699)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 0)**: —  ⚠ 위험목록 함수 → 전수 나열

### `_dl_time_zone(saved_at)`
- **위치**: `app.py:15716` (15716~15722)
- **설명**: 'YYYY-MM-DD HH:MM:SS' → 오전(≤11)/오후(12~16)/저녁(17~)/미상. 마감 시간대 근사. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_daily_bigdata`(15751)

### `_dl_hc_bucket(n)`
- **위치**: `app.py:15725` (15725~15731)
- **설명**: 출전 두수 → 버킷. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_daily_bigdata`(15751)

### `_dl_bucket_add(agg, key, hit)`
- **위치**: `app.py:15734` (15734~15739)
- **설명**: 집계 헬퍼: agg[key] = {n, hits}. hit 가 None(미판정)이면 n만 증가. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_daily_bigdata`(15751)

### `_dl_finalize(agg)`
- **위치**: `app.py:15742` (15742~15748)
- **설명**: {key:{n,hits}} → {key:{n,hits,hit_rate}} (내림차순 n). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_daily_bigdata`(15751)

### `_daily_bigdata(date)`
- **위치**: `app.py:15751` (15751~15781)
- **설명**: [2번] 오늘 경주를 ai_training/ 에서 스캔해 빅데이터 집계. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_daily_learning_generate`(15846)

### `_daily_profit(date)`
- **위치**: `app.py:15784` (15784~15801)
- **설명**: 오늘 결과 입력된 경주의 손익 합(race_results/ investment.profit). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_daily_learning_generate`(15846)

### `_daily_tags(date)`
- **위치**: `app.py:15804` (15804~15818)
- **설명**: 오늘 analysis_log/ 파일들의 pattern_tags 를 모아 유니크 태그 목록(데이터 기반). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_daily_learning_generate`(15846)

### `_dl_reliability()`
- **위치**: `app.py:15821` (15821~15838)
- **설명**: [3번] 누적 패턴 신뢰도 — learning.json 통계에서 파생(마감급락·쌍승역전·전적유력마·추천종합). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_daily_learning_generate`(15846)

### `_pick(st, label)`
- **위치**: `app.py:15829` (15829~15831)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_history_save_analysis`(13914) · `_dl_reliability`(15821)

### `_upset_bump(stats, key, hit)`
- **위치**: `app.py:15996` (15996~16000)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_learn_upset`(16313)

### `_keyhorse_hit(an, top3)`
- **위치**: `app.py:16085` (16085~16095)
- **설명**: [1·2번·유력마 기반 적중] 기존 정확 적중(복승 1+2·삼복승 1+2+3)과 별개 병행 지표. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_learn_signal_stats`(16170) · `_apply_result_learning`(17723)

### `_active_signal_horses(an)`
- **위치**: `app.py:16098` (16098~16140)
- **설명**: [3번] 이 경주에서 각 신호 유형이 선정한 유력마/복병 마번 → {신호명: [마번]}(빈 신호는 제외). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_learn_signal_stats`(16170)

### `_signal_reliability()`
- **위치**: `app.py:16267` (16267~16274)
- **설명**: [5번] 신호별 신뢰도 요약(50경주+ 표본이면 신뢰) → {신호명: {rate_quinella, rate_trifecta, count, reliable}}. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_signal_reliability_for`(16277)

### `_signal_reliability_for(strong_signals, dark_horses, inverse, compression_pattern, cross_reversal)`
- **위치**: `app.py:16277` (16277~16310)
- **설명**: [5번] 현재 분석에서 활성인 신호별 과거 적중률(50경주+ 신뢰) → 프론트 강조 표시용. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(9492)

### `_hno(x)`
- **위치**: `app.py:16321` (16321~16332)
- **설명**: [타입 가드] 말번호를 int/str/dict 어느 형태든 안전 추출. keyHorses·anomalyHorse가 *(주석(본문))*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_form_from_starters`(1542) · `_learn_upset`(16313)

### `_drop_timing_from_timeline(timeline)`
- **위치**: `app.py:16391` (16391~16406)
- **설명**: 배당 타임라인(30초 스냅샷)에서 복승 배당의 최대 하락과 그 시점(T-N분 버킷). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_race_features`(16409)

### `_race_features(log)`
- **위치**: `app.py:16409` (16409~16438)
- **설명**: 분석 로그 1건 → 패턴 발견용 특징. 결과 미입력이면 None(학습 대상 제외). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_discover_patterns`(16441)

### `_discover_patterns()`
- **위치**: `app.py:16441` (16441~16527)
- **설명**: data/analysis_log/ 전체를 스캔해 적중 경주의 공통점을 자동 발견 → discovered_patterns.json. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_apply_result_learning`(17723) · `patterns_discovered`(21464)

### `add_bool(key, label)`
- **위치**: `app.py:16471` (16471~16483)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_discover_patterns`(16441)

### `_safe_num(v)`
- **위치**: `app.py:16547` (16547~16551)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 17)**: `result_ocr`(831) · `_apply_profit_strategy`(8583) · `_apply_bet_rules`(12559) · `_build_race_result`(14939) · `_build_ai_training`(15160) · `shadow_trifecta_ep`(17188) … [+11]

### `_combo_timeline(doc, combo)`
- **위치**: `app.py:16572` (16572~16602)
- **설명**: 스냅샷들에서 특정 복승 조합(combo=[a,b])의 배당 변화 타임라인 추출. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_build_race_report`(16733)

### `_signal_timeline_from_doc(doc)`
- **위치**: `app.py:16605` (16605~16665)
- **설명**: [3·4·5번] 스냅샷의 signal_horse 시퀀스에서 이상감지 말 변경 이력 + 신호 안정화(2연속 확정) *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_signal_timeline`(16668) · `_build_race_report`(16733)

### `_signal_timeline(rk)`
- **위치**: `app.py:16668` (16668~16675)
- **설명**: 경주 히스토리 파일을 읽어 _signal_timeline_from_doc 도출(없으면 None). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_triple_analyze`(9492) · `signal_timeline_api`(16679)

### `_signal_win_tags(an, top3)`
- **위치**: `app.py:16690` (16690~16724)
- **설명**: [4번] 어떤 신호가 실제 입상마를 맞혔는지 태깅. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_build_race_report`(16733) · `_apply_result_learning`(17723)

### `_report_odds_band(odds)`
- **위치**: `app.py:16727` (16727~16730)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_build_race_report`(16733)

### `_build_race_report(rk, an, record, result, doc)`
- **위치**: `app.py:16733` (16733~16911)
- **설명**: [1번] 경주별 완전 재현 리포트를 조립해 data/race_report/에 저장하고 dict 반환. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_apply_result_learning`(17723)

### `_rep_combo_for(no)`
- **위치**: `app.py:16754` (16754~16767)
- **설명**: 말 no 를 포함하는 대표 복승 조합(급락 가장 큰 것 → 없으면 추천 조합). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_build_race_report`(16733)

### `_highlight_story(an, top3, doc)`
- **위치**: `app.py:16914` (16914~16934)
- **설명**: [명예의전당] 고배당 적중 '왜 맞았는지' 스토리 + 정답말 배당 타임라인 스냅샷. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_apply_result_learning`(17723)

### `_rate(records, sel, cond)`
- **위치**: `app.py:16937` (16937~16940)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 4)**: `_dark_stats_recompute`(9265) · `_threshold_candidates`(15448) · `_recompute_learning_stats`(16943) · `_form_ext_build`(27752)

### `rev_hit(r)`
- **위치**: `app.py:16945` (16945~16947)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 0)**: —

### `_is_high_hit(r)`
- **위치**: `app.py:17050` (17050~17053)
- **설명**: [신규 4번] 신호조합 학습: 어떤 신호(조합)가 실제 입상마를 맞혔고, 그 중 고배당 적중 비율. *(주석(def 위))*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_recompute_learning_stats`(16943)

### `agg(sel)`
- **위치**: `app.py:17248` (17248~17257)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_compression_hitrate`(4568) · `shadow_trifecta_ep`(17188)

### `_add(kind, combo, need)`
- **위치**: `app.py:17595` (17595~17600)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_confidence_picks`(7117) · `_rec_combos_from_analysis_log`(17556)

### `_s(v)`
- **위치**: `app.py:17694` (17694~17695)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_conf_picks_from_log`(17681)

### `_winning_quinella_odds(rk, top2)`
- **위치**: `app.py:17701` (17701~17717)
- **설명**: [수익 자동 계산·추정] 실배당 미입력 시 승자 복승 조합(top2)의 마지막 시장배당을 analysis_log *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `_build_race_result`(14939) · `_apply_result_learning`(17723) · `_ml_row_build`(18437)

### `in3(combo)`
- **위치**: `app.py:17784` (17784~17785)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_apply_result_learning`(17723)

### `_cmp_hit(block)`
- **위치**: `app.py:17968` (17968~17972)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_apply_result_learning`(17723)

### `_odds_val(x)`
- **위치**: `app.py:18015` (18015~18016)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_apply_result_learning`(17723)

### `snap_near(minb)`
- **위치**: `app.py:18106` (18106~18114)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_apply_result_learning`(17723)

### `_ml_row_build(rk, an, result, top3, payouts, was_hit)`
- **위치**: `app.py:18437` (18437~18482)
- **설명**: [ML 학습셋] 경주 1건 → {features, label}. 있는 값만 채우고 없으면 None(관대). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_ml_append_row`(18485)

### `_ml_append_row(rk, an, result, top3, payouts, was_hit)`
- **위치**: `app.py:18485` (18485~18490)
- **설명**: [ML 학습셋 누적] ml_training_data.jsonl 에 경주 1건 append(1줄 1경주). 실패 격리. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_apply_result_learning`(17723)

### `_is_tainted_saved(saved_at)`
- **위치**: `app.py:18500` (18500~18503)
- **설명**: 결과 저장 시각(saved_at 'YYYY-MM-DD HH:MM:SS')이 오염 구간 안인지. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_build_signal_performance`(18506) · `_scoreboard_daily`(25042)

### `_build_signal_performance()`
- **위치**: `app.py:18506` (18506~18559)
- **설명**: [signal_performance] 신호별 성과(총·적중·적중률·평균배당·ROI) 재집계 → signal_performance.json. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_kra_backfill_loop`(23857)

### `_horse_repr_timeline(doc, no)`
- **위치**: `app.py:18600` (18600~18636)
- **설명**: [3번] 특정 말의 대표 배당 타임라인을 스냅샷에서 역추적. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `_highlight_story`(16914) · `_classify_failure`(18697) · `_failure_report`(18881)

### `_timeline_rebound(tl)`
- **위치**: `app.py:18639` (18639~18649)
- **설명**: 타임라인이 '급락 후 반등'(페이크 의심)인지 판정. 최저점까지 10%+ 하락 후 최저 대비 10%+ 반등. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_classify_failure`(18697)

### `_timeline_signal_label(pct)`
- **위치**: `app.py:18652` (18652~18664)
- **설명**: 직전대비 급락% → 사람이 읽는 신호 강도 라벨(역추적 리포트용). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_failure_report`(18881) · `_rr_horse_timeline`(19104)

### `_classify_failure(rk, an, top3, doc)`
- **위치**: `app.py:18697` (18697~18814)
- **설명**: [1번] 미적중 경주의 실패 유형 자동 분류 + [2·3번] 정답말 역추적 근거 생성. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_failure_record`(18817) · `_failure_report`(18881)

### `_failure_autorule(d, mpat, ftype, rk, stats)`
- **위치**: `app.py:18850` (18850~18878)
- **설명**: [7번] 놓친 패턴 N건+ 반복 시 규칙 자동 생성. [5번] 생성 시점 추천 적중률을 before로 스냅샷. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_failure_record`(18817)

### `_failure_report(rk)`
- **위치**: `app.py:18881` (18881~18936)
- **설명**: [2·3번] 미적중 경주 복기 리포트 생성(온디맨드). 히스토리 결과+스냅샷에서 재구성. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `failure_report`(19430)

### `_in3(combo)`
- **위치**: `app.py:18901` (18901~18902)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_failure_report`(18881)

### `_success_report_text(rk, top3, hit_combos, timelines)`
- **위치**: `app.py:18939` (18939~18967)
- **설명**: [복기 UI 통합] 적중 경주 '왜 맞았는지' 텍스트 리포트(정답말 신호 근거 포함). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_failure_report`(18881)

### `_failure_report_text(rk, top3, recommended, was_hit, fail, timelines)`
- **위치**: `app.py:18970` (18970~19013)
- **설명**: [2·3번] 복기 리포트를 사람이 읽는 텍스트로. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_failure_report`(18881)

### `_rr_load_by_key(rk)`
- **위치**: `app.py:19079` (19079~19096)
- **설명**: raceKey(또는 race_id)로 race_results/ 파일 1건 로드(가장 최근). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_high_odds_review_one`(19273)

### `_rr_top3(rr)`
- **위치**: `app.py:19099` (19099~19101)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_high_odds_prefilter`(19261)

### `_rr_horse_timeline(rr, h)`
- **위치**: `app.py:19104` (19104~19124)
- **설명**: 정답말 h 의 대표 배당 타임라인 — 각 스냅샷에서 h 가 낀 최저 복승 조합(자금유입 대리). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_high_odds_review_one`(19273)

### `_rr_winning_odds(rr, top3)`
- **위치**: `app.py:19127` (19127~19148)
- **설명**: 정답 복승(top2) 배당 — odds_timeline 최신값 → odds_at_start → investment 폴백. 삼복승은 대개 미수집. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_high_odds_prefilter`(19261)

### `_rr_recommend(rr)`
- **위치**: `app.py:19151` (19151~19164)
- **설명**: 우리 추천 조합(prediction.recommend_main/sub) → (마번 집합, 표시 문자열 리스트). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_rr_classify`(19167) · `_high_odds_review_one`(19273)

### `_rr_classify(rr, top3, timelines)`
- **위치**: `app.py:19167` (19167~19218)
- **설명**: [4번] race_results 기반 미적중 유형 분류(_classify_failure 5유형 로직 이식). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_high_odds_review_one`(19273)

### `_high_odds_simulate(fail)`
- **위치**: `app.py:19221` (19221~19241)
- **설명**: [5번] '개선된 로직 적용 시' 반사실 시뮬레이션. 실패 유형·초점말 급락폭 기반. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_high_odds_review_one`(19273)

### `_high_odds_next_rules(fail)`
- **위치**: `app.py:19244` (19244~19258)
- **설명**: [6번] 미적중 유형별 내일 적용 규칙(고배당 특화). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_high_odds_review_one`(19273)

### `_high_odds_prefilter(rr)`
- **위치**: `app.py:19261` (19261~19270)
- **설명**: 전체 분석 전 저비용 판정: race_results 의 결과 top3 + 정답 복승 배당으로 고배당 여부만 확인. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_high_odds_review_one`(19273) · `_high_odds_scan`(19365)

### `_high_odds_scan(date, save, limit)`
- **위치**: `app.py:19365` (19365~19389)
- **설명**: [1번] 결과 입력된 경주(race_results/)를 스캔해 고배당(복승30+/삼복승100+)만 심층 분석·저장. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `high_odds_review`(19879)

### `_snapshot_index(ttl)`
- **위치**: `app.py:19560` (19560~19587)
- **설명**: {raceKey: {trigger: 파일명, '_any': 파일명}} — 실패 시 빈 dict(호출부가 기존 스캔으로 폴백). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_day_snapshot_for`(19590)  ⚠ 위험목록 함수 → 전수 나열

### `_day_snapshot_for(rk, trigger)`
- **위치**: `app.py:19590` (19590~19617)
- **설명**: 해당 경주의 지정 trigger 스냅샷 파일명 반환(없으면 T-10 등 폴백). 이미지 URL 조립용. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `day_races`(19700)

### `_day_card_extra(rk)`
- **위치**: `app.py:19628` (19628~19696)
- **설명**: 날짜별 카드에 실을 보강 정보 — 판정 명단·적중 조합·확정배당·등급. 실패 시 {}(기존 동작 유지). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `day_races`(19700)  ⚠ 위험목록 함수 → 전수 나열

### `fmt(lst)`
- **위치**: `app.py:19655` (19655~19668)
- **설명**: 조합 리스트 → ['1+11', ...]. 형식이 제각각인 구데이터 방어(문자열·중첩·None 혼재). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_day_card_extra`(19628)

### `_competitor_result_for_race(rk)`
- **위치**: `app.py:20031` (20031~20049)
- **설명**: 경주 결과(1·2·3착) → analysis_log(canonical) 우선, 없으면 race_results. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_competitor_record`(20090)

### `_quinella_hits(combos, result)`
- **위치**: `app.py:20052` (20052~20060)
- **설명**: 복승 조합 중 실제 1·2착(순서무관)과 일치하는 게 있으면 True. 결과 없으면 None. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_competitor_compare`(20063)

### `_competitor_compare(comp_pat, our, result)`
- **위치**: `app.py:20063` (20063~20081)
- **설명**: 저쪽 vs 우리 비교: 적중·축 일치·연결마 차이. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_competitor_record`(20090)

### `_competitor_path(rk, date)`
- **위치**: `app.py:20084` (20084~20087)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_competitor_record`(20090) · `competitor_get`(20208)

### `_comment_tags(comment)`
- **위치**: `app.py:20287` (20287~20294)
- **설명**: 자유 코멘트 → 참고용 키워드 태그 리스트(강제 분류 아님·중복 제거). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `review_save`(20336)

### `__init__(self)`
- **위치**: `app.py:20651` (20651~20653)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `__init__`(20651)

### `handle_starttag(self, tag, attrs)`
- **위치**: `app.py:20654` (20654~20658)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 0)**: —

### `handle_data(self, data)`
- **위치**: `app.py:20659` (20659~20661)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 0)**: —

### `handle_endtag(self, tag)`
- **위치**: `app.py:20662` (20662~20668)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 0)**: —

### `_split(ln)`
- **위치**: `app.py:20682` (20682~20685)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_parse_result_tsv`(20671)

### `ns(s)`
- **위치**: `app.py:20687` (20687~20688)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_parse_result_tsv`(20671) · `_parse_result_rows`(20747)

### `idx(pat, default)`
- **위치**: `app.py:20700` (20700~20704)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_parse_result_tsv`(20671) · `_parse_result_rows`(20747)

### `_fw(s)`
- **위치**: `app.py:20716` (20716~20717)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `_parse_result_tsv`(20671) · `num`(20719) · `fnum`(20725)

### `num(cells, k)`
- **위치**: `app.py:20719` (20719~20723)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_parse_result_tsv`(20671)

### `fnum(cells, k)`
- **위치**: `app.py:20725` (20725~20726)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_parse_result_tsv`(20671)

### `_fw2ascii(s)`
- **위치**: `app.py:20762` (20762~20764)
- **설명**: 전각숫자(０-９)·전각콜론 → 반각(중앙 JRA 결과표 대응) *(주석(본문))*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 4)**: `_parse_result_rows`(20747) · `ns`(20766) · `cell`(20796) · `firstnum`(20799)

### `ns(s)`
- **위치**: `app.py:20766` (20766~20767)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_parse_result_tsv`(20671) · `_parse_result_rows`(20747)

### `idx(pat)`
- **위치**: `app.py:20784` (20784~20788)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_parse_result_tsv`(20671) · `_parse_result_rows`(20747)

### `cell(r, k)`
- **위치**: `app.py:20796` (20796~20797)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_parse_result_rows`(20747)

### `firstnum(s)`
- **위치**: `app.py:20799` (20799~20801)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_parse_result_rows`(20747)

### `_area_num(s)`
- **위치**: `app.py:20945` (20945~20960)
- **설명**: 문자열에서 (경마장 토큰[정규화], 경주번호) 추출. 결과행·raceKey 공용. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 45)**: `results_auto`(1393) · `_canon_rk`(1874) · `_do_triple_ingest`(2166) · `triple_latest`(2598) · `current_race`(3249) · `_dark_case_record`(9384) … [+39]

### `_resolve_race_key(rk, candidates)`
- **위치**: `app.py:20983` (20983~21012)
- **설명**: 입력 raceKey를 실제 분석된 key로 유연 매칭(형식 불일치 방어). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 11)**: `_snapshot_result_for`(2893) · `public_matrix`(13414) · `failure_report`(19430) · `high_odds_review`(19879) · `competitor_record`(20167) · `competitor_get`(20208) … [+5]

### `_today_first(ks)`
- **위치**: `app.py:20999` (20999~21000)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_resolve_race_key`(20983)

### `_rec_from_history(rk)`
- **위치**: `app.py:21015` (21015~21042)
- **설명**: triple_store에 활성 데이터가 없을 때 odds_history 스냅샷에서 rec 재구성(결과학습용). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `_apply_result_learning`(17723) · `_failure_report`(18881) · `dark_horse_review`(19512)  ⚠ 위험목록 함수 → 전수 나열

### `_arr(d)`
- **위치**: `app.py:21027` (21027~21034)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_rec_from_history`(21015)

### `_match_row_to_key(row, analyzed_keys)`
- **위치**: `app.py:21045` (21045~21051)
- **설명**: 결과행(area,round) → 분석된 raceKey 유연 매칭(한국·일본 모든 경기). 실패 시 None. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 0)**: —

### `_match_row_to_all_keys(row, analyzed_keys)`
- **위치**: `app.py:21054` (21054~21071)
- **설명**: 결과행 → (경마장+경주번호) 일치하는 **모든** 분석 key. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_register_result_rows`(21122)

### `_log_has_recommendation(rk)`
- **위치**: `app.py:21074` (21074~21081)
- **설명**: 해당 raceKey 로그가 추천/유력마 데이터를 가졌는지(적중판정 기준 primary 선택용). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_register_result_rows`(21122)

### `_register_result_rows(rows, stake, analyzed_keys)`
- **위치**: `app.py:21122` (21122~21172)
- **설명**: 결과행 리스트 → 분석경주 자동매칭 → 적중판정·학습 → 요약. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `result_ocr`(831) · `results_bulk`(21176)

### `_recompute_pnl(rec, stake, payout)`
- **위치**: `app.py:21198` (21198~21219)
- **설명**: [보완#1] 조정용 손익 재계산 — 단건 record-result 와 동일 규칙. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `results_adjust`(21223)

### `_band(diff)`
- **위치**: `app.py:21310` (21310~21317)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_reversal_strength_stats`(21307) · `drop_stats`(331)

### `_qhit(r)`
- **위치**: `app.py:21372` (21372~21373)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `stats_today`(21365)

### `_thit(r)`
- **위치**: `app.py:21375` (21375~21376)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `stats_today`(21365)

### `_keirin_jo_from_venue(venue)`
- **위치**: `app.py:21701` (21701~21706)
- **설명**: raceKey 경륜장명 → joCode(오다와라36·히로시마62·마에바시01·기후04 등). 없으면 None. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 4)**: `_do_triple_ingest`(2166) · `_triple_analyze`(9492) · `keiba_odds`(22601) · `_keirin_jo_today`(24260)

### `_kstrip(s)`
- **위치**: `app.py:21709` (21709~21712)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 5)**: `_keirin_parse_card`(21755) · `_keirin_table_rows`(21982) · `_keiba_schedule`(22545) · `_keirin_result_parse`(24273) · `_keirin_venue_races`(29255)

### `_keirin_style_label(ch)`
- **위치**: `app.py:21715` (21715~21719)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_keirin_parse_card`(21755)

### `_keirin_style_bonus(r)`
- **위치**: `app.py:21735` (21735~21752)
- **설명**: 결정수 보정: 차입(差)·마크(マ) 우세→추입형 +5 / 도주(逃) 우세→선행형 +3 / 젖히기(捲)→+4. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_keirin_analyze`(21837)

### `_oddspark_grid_combos(html, max_no)`
- **위치**: `app.py:21928` (21928~21964)
- **설명**: oddspark 배당 그리드 HTML → {tuple(sorted(combo)): odds}. 복승(2두)·삼복승(3두) 공용. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_keiba_parse_quinella_grid`(21967) · `_keiba_parse_trio_grid`(21974)

### `_keirin_table_rows(html)`
- **위치**: `app.py:21982` (21982~21991)
- **설명**: oddspark 배당 HTML → 표 행 리스트(각 행=셀 텍스트 리스트). 태그 제거·공백 정리. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 6)**: `_keirin_parse_quinella`(22015) · `_keirin_parse_exacta`(22038) · `_keiba_matrix_rows`(22411) · `_keiba_parse_win`(22425) · `_keiba_horse_count`(22464) · `_keirin_result_parse`(24273)

### `_keirin_odds_pairs(cells)`
- **위치**: `app.py:21994` (21994~22012)
- **설명**: ['4','4.5','2','12.7','4', ...] → 선두 정수(축 차번호)와 (배당,순위) 쌍 목록. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_keirin_parse_quinella`(22015) · `_keirin_parse_exacta`(22038)

### `_keirin_autocollect_form(rk, jo, ymd, race)`
- **위치**: `app.py:22134` (22134~22200)
- **설명**: [경륜 출마표 전적 자동 수집] 배당 수집 시 동시에 oddspark 출마표에서 전적(競走得点·착순·결정수·각질) 수집·저장. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 4)**: `_do_triple_ingest`(2166) · `keirin_odds`(22252) · `keiba_odds`(22601) · `_multi_collect_one`(29676)

### `_odds_target_mismatch(rk, race_no, jo)`
- **위치**: `app.py:22203` (22203~22225)
- **설명**: [검역 게이트 (2026-07-23 다마노 11R 오염 실사고)] 저장 키(raceKey) vs 수집 대상(raceNo·경기장) 교차 검증. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `keirin_odds`(22252) · `keiba_odds`(22601)

### `_sched_post_epoch(rk)`
- **위치**: `app.py:22228` (22228~22248)
- **설명**: [검역 보강 (2026-07-23)] 다중경주 스케줄에서 raceKey의 발주시각(postEpoch·초) 조회. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `keirin_odds`(22252) · `keiba_odds`(22601)

### `_keirin_venue_current(jo, ymd)`
- **위치**: `app.py:22301` (22301~22327)
- **설명**: [경륜 현재경주·단일 경륜장] joCode 한 곳만 조회(가벼움) → 현재 발매중(다음 발주) 경주. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `keirin_current`(22331)

### `_keiba_odds_live(q, x)`
- **위치**: `app.py:22393` (22393~22401)
- **설명**: 파싱된 복승·쌍승이 '발매 중 실배당'인지 판정(마감 후·발매 전 가짜값 차단). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `keiba_odds`(22601) · `_multi_collect_one`(29676)

### `_keiba_matrix_rows(html)`
- **위치**: `app.py:22411` (22411~22422)
- **설명**: 배당 매트릭스 행만 추출(선두=차번호·소수배당 포함·마명(가나/한자) 없는 행). 마방리스트 제외. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_keiba_parse_quinella`(22474) · `_keiba_parse_exacta`(22518)

### `_keiba_horse_count(html)`
- **위치**: `app.py:22464` (22464~22471)
- **설명**: 출전 두수 추정 = 배당표에 등장하는 최대 차번호. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `keiba_odds`(22601)

### `_keiba_schedule(ymd, force)`
- **위치**: `app.py:22545` (22545~22577)
- **설명**: 그날(ymd=YYYYMMDD) oddspark 지방경마 개최 경마장 → {한자경마장명: (opTrackCd, sponsorCd)}. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 7)**: `_keiba_resolve_track`(22580) · `keiba_odds`(22601) · `keiba_current`(22745) · `keiba_schedule_ep`(22779) · `keiba_starters`(23095) · `_keiba_form_collect_by_rk`(23206) … [+1]

### `_keiba_resolve_track(venue, ymd)`
- **위치**: `app.py:22580` (22580~22597)
- **설명**: raceKey 경마장명(한/일/영) → 그날 oddspark 개최 코드 (opTrackCd, sponsorCd) 또는 None. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 4)**: `keiba_odds`(22601) · `keiba_current`(22745) · `keiba_starters`(23095) · `_keiba_form_collect_by_rk`(23206)

### `_keiba_current_raceno(op_track, sponsor, ymd, ttl)`
- **위치**: `app.py:22719` (22719~22741)
- **설명**: [경주 자동추종] oddspark RaceList.do(raceNb 미지정) 응답 <title>이 '현재 발매중 경주'로 자동 포커스됨 *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `keiba_current`(22745)

### `_keiba_cells(tr)`
- **위치**: `app.py:22791` (22791~22794)
- **설명**: ══════════ [지방경마 출주표 전적] oddspark RaceList.do(出走表) + HorseDetail(전5경주) ══════════ *(주석(def 위))*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_keiba_parse_shutsuba`(22805) · `_keiba_parse_horse_past`(22869)

### `_keiba_dist_of(s)`
- **위치**: `app.py:22797` (22797~22802)
- **설명**: 'ダ1230'·'芝1400'·'1230m' → (surface, dist_int). 못 읽으면 (surface, None). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `_keiba_parse_horse_past`(22869) · `_jra_past_cell`(26688) · `_jra_parse_shutuba_past`(26726)

### `_keiba_corner_style(past)`
- **위치**: `app.py:22897` (22897~22924)
- **설명**: 전5경주의 통과순위(通過)로 각질 추정. 코너 초반 순위가 두수 대비 앞이면 선행형, 뒤면 추격형. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_keiba_build_form`(22994) · `_jra_style`(26774)

### `_corner_move_bonus(past)`
- **위치**: `app.py:22927` (22927~22950)
- **설명**: [코너 위치 변화·신규] oddspark 통과순위(corner=3C·4C)의 '첫 코너 → 마지막 코너(4C)' 상대위치 변화로 *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_keiba_build_form`(22994) · `_jra_build_form`(26786)

### `_keiba_last3f_note(past)`
- **위치**: `app.py:22953` (22953~22960)
- **설명**: 전5경주 상3F(막판 스피드) 평균. 빠를수록 추격형 강점. 반환 (avg, note). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_keiba_build_form`(22994) · `_jra_build_form`(26786)

### `_keiba_build_form(shutsuba, details)`
- **위치**: `app.py:22994` (22994~23051)
- **설명**: 출주표 + 말별 전적 → 통합 전적 점수. base_form_score(착순 가중평균) + 각질/거리변화/상3F 보정. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `keiba_starters`(23095) · `_keiba_autocollect_form`(23159) · `_nar_autocollect_form`(29553)

### `_apply_back_power(horses)`
- **위치**: `app.py:23054` (23054~23068)
- **설명**: [3번] 경주 내 상3F 상대 뒷힘 보정(상위30% +15·하위 -5) + '⚡ 거리 미경험이나 뒷힘 강점' 플래그. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_keiba_build_form`(22994) · `_jra_build_form`(26786)

### `_one(item)`
- **위치**: `app.py:23077` (23077~23086)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 0)**: —

### `_keiba_autocollect_form(rk, op_track, sponsor, ymd, race_nb)`
- **위치**: `app.py:23159` (23159~23198)
- **설명**: [지방경마(NAR) 전적 자동수집] 서버 배당 수집(_multi_collect_one) 시 oddspark 出走表+전5경주 전적을 함께 수집·저장(경주당 1회). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_keiba_form_collect_by_rk`(23206) · `_multi_collect_one`(29676)

### `_jp_form_backfill_once()`
- **위치**: `app.py:23246` (23246~23280)
- **설명**: [일본경마 전적 백필·board 무관] 활성/분석된 일본지방경마 경주 중 oddspark 전적이 없는 것을 찾아 *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_kra_backfill_loop`(23857)

### `_kra_api_key()`
- **위치**: `app.py:23299` (23299~23300)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 6)**: `_kra_fetch`(23502) · `_kra_backfill_loop`(23857) · `kra_test`(25374) · `kra_status`(25511) · `_kra_api_get`(25670) · `kra_apis_test`(26136)

### `_weather_api_key()`
- **위치**: `app.py:23316` (23316~23317)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `_weather_fetch_raw`(23355) · `weather_current`(23448) · `_weather_auto_start`(23482)

### `_track_condition(weather_main, rain_mm)`
- **위치**: `app.py:23326` (23326~23337)
- **설명**: [주로 상태 판단] 강수량(mm/1h)·날씨로 주로 등급. 반환 (label, level 0~3). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_weather_get`(23380)

### `_weather_dummy(venue)`
- **위치**: `app.py:23340` (23340~23352)
- **설명**: API 키 없을 때 더미 날씨(테스트용) — 경마장 순번으로 결정적 변형(양호/다습/불량/매우불량 골고루). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_weather_fetch_raw`(23355)

### `_weather_get(venue, force)`
- **위치**: `app.py:23380` (23380~23398)
- **설명**: 경주장 날씨+주로 상태 반환(캐시). {venue, weather_main, desc, temp, rain_mm, wind, condition, level, strongWind, source, at}. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 4)**: `_weather_for_race_key`(23401) · `weather_current`(23448) · `weather_venue`(23458) · `_weather_auto_loop`(23471)

### `_weather_for_race_key(rk)`
- **위치**: `app.py:23401` (23401~23407)
- **설명**: raceKey 에서 경마장 추출 → 날씨. 미매칭 시 None. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(9492)

### `_weather_adjust_form(form, weather)`
- **위치**: `app.py:23410` (23410~23444)
- **설명**: [날씨 분석 반영] form(말별 전적 리스트)에 날씨 보정 적용 → 보정 근거 붙인 리스트 반환(원본 무변경·복사). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(9492)

### `_weather_auto_loop()`
- **위치**: `app.py:23471` (23471~23479)
- **설명**: 발주 60분 이내 경주가 있으면 그 경마장은 10분 주기, 아니면 30분 주기(캐시 TTL 이 흡수). *(주석(본문))*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 0)**: —  ⚠ 위험목록 함수 → 전수 나열

### `_weather_auto_start()`
- **위치**: `app.py:23482` (23482~23489)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_boot_background`(32285)

### `_kra_meet_code(venue)`
- **위치**: `app.py:23492` (23492~23499)
- **설명**: 경마장명(서울/제주/부산경남/부경) → KRA meet 코드('1'/'2'/'3'). 숫자면 그대로. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 13)**: `results_auto`(1393) · `kra_result`(23698) · `_kra_result_backfill_once`(23820) · `kra_test`(25374) · `kra_section`(25393) · `kra_jockey_change`(25410) … [+7]

### `_kra_g(item, *names)`
- **위치**: `app.py:23541` (23541~23548)
- **설명**: item 에서 여러 후보 필드명 중 첫 유효값(대소문자·변형 방어). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_kra_parse_items`(23565) · `_kra_track_info`(25971)

### `_kra_num(v)`
- **위치**: `app.py:23551` (23551~23555)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 7)**: `_kra_parse_items`(23565) · `_kra_jockey_changes`(25874) · `_track_condition_by_water`(25959) · `_kra_track_info`(25971) · `_kra_ai_results`(25999) · `kra_race_detail`(26152) … [+1]

### `_kra_int(v)`
- **위치**: `app.py:23558` (23558~23562)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 13)**: `_triple_analyze`(9492) · `_kra_parse_items`(23565) · `_kra_extract_result`(23650) · `kra_ai_results_ingest`(25431) · `_kra_jockey_winrate`(25705) · `_kra_section_records`(25756) … [+7]

### `_kra_rctime_sec(t)`
- **위치**: `app.py:23636` (23636~23647)
- **설명**: KRA rcTime('1:05.0' 또는 '65.0') → 초(float). 파싱 불가·미완주는 None. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_kra_extract_result`(23650) · `_kra_race_placings`(27726)

### `_kra_extract_result(meet, rc_date, rc_no)`
- **위치**: `app.py:23650` (23650~23673)
- **설명**: [KRA 결과 자동 추출] 경주별상세성적표(racedetailresult)의 rcTime(주행기록) 오름차순 정렬로 착순 산출. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `results_auto`(1393) · `_kra_collect_result_one`(23676) · `kra_result`(23698)

### `_kra_auto_loop()`
- **위치**: `app.py:23724` (23724~23796)
- **설명**: 30초 간격으로 watch 등록된 KRA 경주 배당 자동수집(데몬). 수집 결과(시각·건수·성공)를 watch 레코드에 기록. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 0)**: —  ⚠ 위험목록 함수 → 전수 나열

### `_kra_auto_start()`
- **위치**: `app.py:23799` (23799~23806)
- **설명**: KRA 30초 자동수집 데몬 1회 기동(멱등). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `kra_watch`(25474)

### `_kra_result_backfill_once(date, verbose)`
- **위치**: `app.py:23820` (23820~23854)
- **설명**: [KRA 결과 백필·1회] 해당일(기본 오늘) 분석됐으나 결과 없는 '한국' 경주를 KRA rcTime 착순으로 *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_kra_backfill_loop`(23857) · `kra_results_backfill`(23900)

### `_kra_backfill_loop()`
- **위치**: `app.py:23857` (23857~23886)
- **설명**: 20분 주기 결과 백필(데몬). KRA 키가 있을 때만 동작. 경주 종료 후 rcTime 게시되면 자동 확보 → *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 0)**: —  ⚠ 위험목록 함수 → 전수 나열

### `_kra_backfill_start()`
- **위치**: `app.py:23889` (23889~23896)
- **설명**: KRA 결과 백필 데몬 1회 기동(멱등). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_boot_background`(32285)

### `_jp_baba_code_from_rk(rk)`
- **위치**: `app.py:23932` (23932~23938)
- **설명**: raceKey 문자열에서 NAR 경마장 별칭을 스캔해 k_babaCode 반환(정규화 무관·방어). 미지원=None. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 4)**: `_official_payouts_for_rk`(24047) · `_official_result_audit`(24097) · `_jp_result_backfill_once`(25313) · `jp_results_backfill`(25354)

### `_z2h_digits(s)`
- **위치**: `app.py:23941` (23941~23943)
- **설명**: 전각 숫자(０-９)·전각 공백 → 반각(결과표 전각 대응). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_keiba_parse_result`(23958)

### `_keiba_decode(raw_bytes)`
- **위치**: `app.py:23946` (23946~23955)
- **설명**: keiba.go.jp 응답 디코드 — 着順/馬番 헤더가 보이는 인코딩 채택(EUC-JP/Shift_JIS/UTF-8). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_keiba_fetch_page`(23997)

### `_cells(r)`
- **위치**: `app.py:23966` (23966~23968)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_keiba_parse_result`(23958)

### `_keiba_result_top3(baba, ymd, rno, timeout)`
- **위치**: `app.py:24018` (24018~24024)
- **설명**: keiba.go.jp RaceMarkTable 조회 → 상위3 마번 또는 None. 완전 방어(예외=None). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `_official_payouts_for_rk`(24047) · `_official_result_audit`(24097) · `_jp_result_backfill_once`(25313)

### `_keiba_result_payouts(baba, ymd, rno, timeout)`
- **위치**: `app.py:24027` (24027~24044)
- **설명**: [NAR 공식 확정환급 (2026-07-22)] keiba.go.jp RaceMarkTable → {'quinella': 馬連複배당, 'trifecta': 三連複배당}. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_official_payouts_for_rk`(24047) · `_official_result_audit`(24097)

### `_official_payouts_for_rk(rk, top3, timeout)`
- **위치**: `app.py:24047` (24047~24094)
- **설명**: [확정배당 자동 충전 (2026-07-29 A-3)] raceKey → 공식 확정환급 {'quinella','trifecta'} 또는 None. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_apply_result_learning`(17723)

### `_keirin_jo_today(venue)`
- **위치**: `app.py:24260` (24260~24270)
- **설명**: 경륜장명 → joCode. 오늘 스케줄(확장이 보낸 실코드) 우선, 정적 맵(_keirin_jo_from_venue) 폴백. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 4)**: `_official_payouts_for_rk`(24047) · `_official_result_audit`(24097) · `_keirin_result_backfill_once`(24371) · `keirin_results_backfill`(25291)

### `_keirin_result_top3(jo, ymd, rno, expect_venue)`
- **위치**: `app.py:24352` (24352~24368)
- **설명**: 경륜 결과 1경주 조회 → {ok, top3, quinellaOdds, venue} 또는 {error}. 경륜장명 불일치 시 학습 차단. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `_official_payouts_for_rk`(24047) · `_official_result_audit`(24097) · `_keirin_result_backfill_once`(24371)

### `_keirin_result_backfill_once(date, verbose)`
- **위치**: `app.py:24371` (24371~24476)
- **설명**: [경륜 결과 백필·1회] 당일 분석됐으나 결과 없는 '경륜' 경주를 RaceKekka 착순으로 자동 수집→학습. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_kra_backfill_loop`(23857) · `keirin_results_backfill`(25291)

### `_esc(x)`
- **위치**: `app.py:24489` (24489~24490)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 4)**: `race_log_page`(24480) · `daily_cards_page`(24598) · `review_report_page`(24799) · `scoreboard_page`(25179)

### `_esc(x)`
- **위치**: `app.py:24607` (24607~24608)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 4)**: `race_log_page`(24480) · `daily_cards_page`(24598) · `review_report_page`(24799) · `scoreboard_page`(25179)

### `_esc(x)`
- **위치**: `app.py:24805` (24805~24806)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 4)**: `race_log_page`(24480) · `daily_cards_page`(24598) · `review_report_page`(24799) · `scoreboard_page`(25179)

### `_q_low(qv, k)`
- **위치**: `app.py:24931` (24931~24940)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `debug_race_data`(24918)

### `_sport_label(doc, rk)`
- **위치**: `app.py:25052` (25052~25061)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_scoreboard_daily`(25042)

### `_esc(x)`
- **위치**: `app.py:25184` (25184~25185)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 4)**: `race_log_page`(24480) · `daily_cards_page`(24598) · `review_report_page`(24799) · `scoreboard_page`(25179)

### `_jp_result_backfill_once(date, verbose)`
- **위치**: `app.py:25313` (25313~25350)
- **설명**: [일본 NAR 결과 백필·1회] 당일 분석됐으나 결과 없는 '일본 NAR' 경주를 keiba RaceMarkTable 착순으로 *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_kra_backfill_loop`(23857) · `jp_results_backfill`(25354)

### `_fmt(ts)`
- **위치**: `app.py:25519` (25519~25520)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 5)**: `_pub_pace`(13294) · `kra_status`(25511) · `_hist_row_from_doc`(30059) · `_timeline_snap_tick`(31140) · `drop_stats`(331)

### `_kra_track_item_has_water(it)`
- **위치**: `app.py:25613` (25613~25625)
- **설명**: 응답 item 에 함수율(또는 경주로상태) 필드가 실제로 담겼는지. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_kra_track_op_resolve`(25628)

### `_kra_track_op_resolve(meet, rc_date, rc_no)`
- **위치**: `app.py:25628` (25628~25666)
- **설명**: [핵심] 유효한 API189 op 경로를 반환. 우선순위: *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_kra_track_info`(25971)

### `_kra_api_get(path, params, num_rows)`
- **위치**: `app.py:25670` (25670~25702)
- **설명**: B551015 계열 GET → (items[list], err[str|None]). 현재 활성 KRA 키로 조회. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 13)**: `_kra_extract_result`(23650) · `_kra_track_op_resolve`(25628) · `_kra_section_records`(25756) · `_kra_pass_ranks`(25848) · `_kra_jockey_changes`(25874) · `_kra_jk_winrate_by_no`(25905) … [+7]

### `_kra_jockey_winrate(it)`
- **위치**: `app.py:25705` (25705~25713)
- **설명**: 현직기수 item → (복승률=1+2착%, 삼착률=1+2+3착%). 출주 0이면 (None,None). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `_kra_jk_winrate_by_no`(25905) · `kra_jockey`(26216) · `_kra_jockey_map`(26241)

### `_kra_sec_num(v)`
- **위치**: `app.py:25729` (25729~25735)
- **설명**: 구간기록 문자열/숫자 → float(양수만 유효). 0·'-'·'0'은 결측으로 None. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_kra_section_records`(25756)

### `_kra_pass_positions(seq)`
- **위치**: `app.py:25738` (25738~25753)
- **설명**: [코너 통과순위 파싱] '(2,3,4)-(7,10),(6,5,8,9)=(1,11)' → {마번: 순위그룹}. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_kra_pass_ranks`(25848)

### `_kra_section_records(meet, rc_date, rc_no, force)`
- **위치**: `app.py:25756` (25756~25845)
- **설명**: [API37_1] 현재 출전마별 과거 구간기록 이력 → 말별 S1F/G1F 최고·평균 집계 + 선행력/추입력 지수. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `_triple_analyze`(9492) · `kra_section`(25393) · `_simulate_race_flow_kra`(26034)

### `_kra_jockey_changes(meet, rc_date, force)`
- **위치**: `app.py:25874` (25874~25902)
- **설명**: [API10_1] 해당 개최일 기수변경 목록 → 캐시(1시간). 반환 [{rcNo, chulNo, hrName, hrNo, jkBef*, jkAft*, reason, budam*}]. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 4)**: `_triple_analyze`(9492) · `_kra_auto_loop`(23724) · `kra_jockey_change`(25410) · `_kra_jockey_change_bonus`(25930)

### `_kra_jk_winrate_by_no(jk_no)`
- **위치**: `app.py:25905` (25905~25924)
- **설명**: 기수번호(jkNo) → 복승률(%). 현직기수 캐시(_form_ext_stats 아님, KRA API) 조회. 없으면 None. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_kra_jockey_change_bonus`(25930)

### `_kra_jockey_change_bonus(meet, rc_date, rc_no, chul_no)`
- **위치**: `app.py:25930` (25930~25956)
- **설명**: [API10_1 기수변경 보정] 경주 전 기수 교체 감지 → 승부의지/전력 보정. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(9492)

### `_track_condition_by_water(water)`
- **위치**: `app.py:25959` (25959~25968)
- **설명**: [API189 함수율 기준 주로상태] 요구사항 임계: 0~3% 건조(양호)·4~8% 다습·9%+ 불량. (label, level 0~3). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_kra_track_info`(25971)

### `_kra_track_info(meet, rc_date, rc_no, force)`
- **위치**: `app.py:25971` (25971~25996)
- **설명**: [API189 경주로·함수율] op 경로를 자동탐색/캐시(.env KRA_TRACK_OP 또는 후보 자동발견)해 실측 조회. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 0)**: —

### `_kra_ai_results(meet, rc_date, force)`
- **위치**: `app.py:25999` (25999~26031)
- **설명**: [API155] AI학습용 경주결과 — 개최일 전체 경주 착순·배당·체중증감·거리. 경주번호별 그룹. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `kra_ai_results`(25420) · `kra_ai_results_ingest`(25431)

### `_simulate_race_flow_kra(meet, rc_date, rc_no, valid_nos, win_odds)`
- **위치**: `app.py:26034` (26034~26104)
- **설명**: [6단계 경주 전개 시뮬레이션] API6_1 코너순위 + API37_1 구간지수 통합. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_triple_analyze`(9492) · `kra_section`(25393)

### `_kra_coords_for(rk)`
- **위치**: `app.py:26107` (26107~26132)
- **설명**: raceKey → (meet, rc_date, rc_no) 추정. 우선 _KRA_WATCH(라이브), 없으면 rk 파싱. 실패 시 None. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(9492)

### `_kra_jockey_map(force)`
- **위치**: `app.py:26241` (26241~26274)
- **설명**: 현직기수정보(전 경마장 1·2·3) → {기수명: {quinellaRate(1+2착), placeRate(1+2+3착), winRate(1착), rides, meet}}. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_jockey_rate`(26277)

### `_jockey_rate(name)`
- **위치**: `app.py:26277` (26277~26288)
- **설명**: 기수명 → 복승률 dict(정확 매칭 우선, 부분 매칭 폴백). 한국 기수만 매칭(일본 등은 None). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_bmed_before_card`(26507) · `_bmed_expert_cards`(26543)

### `_premium_alert(rk, an)`
- **위치**: `app.py:26365` (26365~26429)
- **설명**: [프리미엄 알림] 활성 경주 분석(an) → 등급별 알림 dict 또는 None(신호/타이밍 미충족). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `alerts_premium`(26454) · `bmed_view`(26574)

### `_premium_alert_format(a)`
- **위치**: `app.py:26432` (26432~26450)
- **설명**: 알림 dict → 카카오톡/푸시용 텍스트 포맷([4번] 명세). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `alerts_premium`(26454)

### `_race_post_epoch(rk)`
- **위치**: `app.py:26493` (26493~26504)
- **설명**: 스케줄(today_schedule)에서 raceKey 의 발주 epoch 찾기(없으면 None). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `bmed_view`(26574)

### `_bmed_before_card(rk, form)`
- **위치**: `app.py:26507` (26507~26528)
- **설명**: [B·사전분석] 배당 전 전적/기수 기반 주목마 TOP + 사전예상복승(전적 이중수렴). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `bmed_view`(26574)

### `_bmed_no_map(lst)`
- **위치**: `app.py:26531` (26531~26540)
- **설명**: [BMED] 리스트[{no,...}] → {int(no): item}. dict/비정형 입력도 안전 무시. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_bmed_expert_cards`(26543)

### `_bmed_expert_cards(an)`
- **위치**: `app.py:26543` (26543~26570)
- **설명**: [E·전문가 해석] 각 말 상세: 전적등급·각질·기수·현재배당·흐름. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `bmed_view`(26574)

### `_jra_venue_code(venue)`
- **위치**: `app.py:26645` (26645~26657)
- **설명**: 경마장명(한/일/영) → netkeiba 場코드('05' 등) 또는 None. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_jra_resolve_raceid`(26676)

### `_jra_race_list(ymd, force)`
- **위치**: `app.py:26660` (26660~26673)
- **설명**: 그날(ymd=YYYYMMDD) netkeiba 개최 race_id 집합. race_list_sub(서버렌더 fragment)에서 추출. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_jra_resolve_raceid`(26676) · `jra_starters`(26842)

### `_jra_resolve_raceid(venue, ymd, race_no)`
- **위치**: `app.py:26676` (26676~26685)
- **설명**: 경마장+개최일+경주번호 → netkeiba race_id(12자리) 또는 None. race_id에 場코드·경주번호가 내장. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `jra_starters`(26842)

### `_jra_past_cell(cell)`
- **위치**: `app.py:26688` (26688~26723)
- **설명**: 馬柱 Past 셀(과거 1경주) → {date,venue,placing,distance,surface,trackCond,fieldSize, *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_jra_parse_shutuba_past`(26726)

### `g(cls)`
- **위치**: `app.py:26692` (26692~26694)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_jra_past_cell`(26688) · `_build_full_prompt`(246)

### `cellraw(cls)`
- **위치**: `app.py:26752` (26752~26754)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_jra_parse_shutuba_past`(26726)

### `_jra_style(h)`
- **위치**: `app.py:26774` (26774~26783)
- **설명**: 각질 판정: netkeiba 표기(逃/先=선행+3 · 差/追=추격+5 · 自=평지) 우선, 없으면 통과순위 폴백. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_jra_build_form`(26786)

### `_jra_build_form(shutuba)`
- **위치**: `app.py:26786` (26786~26838)
- **설명**: 馬柱 파싱 결과 → 통합 전적 점수. base_form_score(착순 가중평균) + 각질/거리변화/부담/상3F 보정. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `jra_starters`(26842)

### `_kra_load_history()`
- **위치**: `app.py:27007` (27007~27020)
- **설명**: kra_history.json 로드(파일 mtime 기준 캐시 — 30초 폴링마다 재파싱 방지). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 5)**: `_do_analyze`(984) · `_form_from_starters`(1542) · `_kra_recent_placings`(27023) · `kra_horse_summary`(27041) · `_form_ext_build`(27752)  ⚠ 위험목록 함수 → 전수 나열

### `_kra_recent_placings(name, hist)`
- **위치**: `app.py:27023` (27023~27038)
- **설명**: 마명 → KRA 실제 최근 착순 배열(최대 5, 최신순). 없으면 []. [전적→부진마 학습 연결] *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_form_from_starters`(1542)

### `kra_horse_summary(name, hist)`
- **위치**: `app.py:27041` (27041~27058)
- **설명**: 마명 → KRA 실제 과거기록 요약 문자열(분석 프롬프트 주입용). 기록 없으면 '' *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_do_analyze`(984)

### `place_points(p)`
- **위치**: `app.py:27101` (27101~27109)
- **설명**: 착순(int) → 점수. 1=100, 2=75, 3=50, 4=25, 5착 이하=0. 무효(기권/미출주 등)는 None. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `base_form_score`(27112)

### `_to_int(v)`
- **위치**: `app.py:27126` (27126~27131)
- **설명**: '1200', '1200M', '1,200m' → 1200. 숫자 없으면 None. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 14)**: `_det_horse_note`(936) · `_do_analyze`(984) · `_dist_bucket`(3491) · `_triple_analyze`(9492) · `_keiba_parse_horse_past`(22869) · `course_aptitude_bonus`(27142) … [+8]

### `_placings_of(horse)`
- **위치**: `app.py:27134` (27134~27138)
- **설명**: horse 에서 최근 착순 배열을 얻는다. recentPlacings 우선, 없으면 pastRaces[].placing. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `jockey_bonus`(27182) · `score_horses_raw`(28008) · `score_form`(28085)

### `course_aptitude_bonus(past_races, race)`
- **위치**: `app.py:27142` (27142~27166)
- **설명**: 현재 거리 ±100m 경험 +10 / 현재 코스(내·외) 경험 +10 / 현재 조건(급) 경험 +5. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `score_horses_raw`(28008)

### `top20_threshold(rates)`
- **위치**: `app.py:27170` (27170~27179)
- **설명**: 복승률 리스트의 상위 20% 경계값(80번째 백분위). 유효값 없으면 None. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `score_horses_raw`(28008)

### `jockey_bonus(horse, threshold)`
- **위치**: `app.py:27182` (27182~27197)
- **설명**: 기수 3개월 복승률 상위 20% +15 / 기수-마필 직전 경주 적중(같은 기수·3착 이내) +10. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `score_horses_raw`(28008)

### `_running_style(horse)`
- **위치**: `app.py:27201` (27201~27221)
- **설명**: 직전 3경주의 초반 포지션(선행/중단/후방)으로 각질 추정. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `_det_horse_note`(936) · `_gait_of`(27230) · `score_horses_raw`(28008)

### `_gait_of(horse)`
- **위치**: `app.py:27230` (27230~27257)
- **설명**: [1] 각질 4분류 정규화 → 선행/선입/추입/자유. 명시 gait/styleType > runningStyle > pastRaces position. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `_apply_pace_analysis`(27275) · `_simulate_race_flow_jp`(27427) · `_g`(27441)

### `_pace_scenario(pace, counts, form, nH)`
- **위치**: `app.py:27260` (27260~27272)
- **설명**: [6] 경주 전개 시나리오 텍스트(리스트). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_apply_pace_analysis`(27275)

### `_apply_pace_analysis(form, horse_count)`
- **위치**: `app.py:27275` (27275~27329)
- **설명**: [2~7] form 각질 편성 분석 → 페이스 예측 + 각질별 유불리 보정(paceBonus 가산·무삭제). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(9492)

### `_simulate_race_flow_keirin(horses, line, tendency, valid_nos, win_odds)`
- **위치**: `app.py:27336` (27336~27424)
- **설명**: [경륜 특화①②·전개 시뮬레이션 (2026-07-19)] oddspark 출마표 전적(styleType·득점·결정수비율)과 *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(9492)

### `_gscore(g)`
- **위치**: `app.py:27377` (27377~27378)
- **설명**: 그룹 정렬: 선두 선수 총점 높은 라인부터 *(주석(def 위))*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 0)**: —

### `_simulate_race_flow_jp(form, valid_nos, win_odds)`
- **위치**: `app.py:27427` (27427~27487)
- **설명**: [일본경마 전개 시뮬레이션] 각질·상3F로 선행경합→페이스→유리한 말→고배당 복병 예측. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(9492)

### `_g(h)`
- **위치**: `app.py:27441` (27441~27443)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_recommend_basis`(6397) · `_simulate_race_flow_jp`(27427)

### `distance_change_bonus(horse, race, style)`
- **위치**: `app.py:27491` (27491~27504)
- **설명**: 이번 거리 vs 직전 경주 거리. 단축=스피드 유리 +5, 연장=추격형 +3·선행형 -3·미상 적성체크. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 4)**: `_det_horse_note`(936) · `_keiba_build_form`(22994) · `_jra_build_form`(26786) · `score_horses_raw`(28008)

### `_leader_jockeys(jstats, top_frac)`
- **위치**: `app.py:27508` (27508~27517)
- **설명**: jockeyStats(이름→통계)에서 복승권율(placeRate) 상위 top_frac 기수명 set. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `_korea_deterministic_overlay`(917) · `_do_analyze`(984) · `score`(28097)

### `jockey_change_bonus(horse, leader_names, rate_fn)`
- **위치**: `app.py:27520` (27520~27548)
- **설명**: [1] 기수 교체 감지. 직전 기수 vs 이번 기수. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 4)**: `_det_horse_note`(936) · `_keiba_build_form`(22994) · `_jra_build_form`(26786) · `score_horses_raw`(28008)

### `weight_change_bonus(horse)`
- **위치**: `app.py:27552` (27552~27561)
- **설명**: 이번 부담중량 vs 직전. 증가 -5, 감소 +5, 동일/미상 0. (부담중량=핸디캡, 마체중과 별개) *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 4)**: `_det_horse_note`(936) · `_keiba_build_form`(22994) · `_jra_build_form`(26786) · `score_horses_raw`(28008)

### `body_weight_change_bonus(cur_bw, past)`
- **위치**: `app.py:27572` (27572~27591)
- **설명**: [마체중 변화·신규] 이번 마체중(cur_bw) vs 직전 경주 마체중. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_keiba_build_form`(22994) · `_jra_build_form`(26786)

### `grade_bonus(cur_grade, past_grades)`
- **위치**: `app.py:27646` (27646~27665)
- **설명**: [2번] 등급 경험(최상위 하나) + 하락(상위→하위·강자 +15)/상승(하위→상위·적응중 -5). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `_keiba_build_form`(22994) · `_jra_build_form`(26786) · `score_horses_raw`(28008)

### `dist_exp_bonus(past_races, cur_dist)`
- **위치**: `app.py:27669` (27669~27680)
- **설명**: [3번] 당거리(정확히 같은 거리) 출전 경험. 있음 +10, 없음 0. 반환 (bonus, detail, experienced). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `_keiba_build_form`(22994) · `_jra_build_form`(26786) · `score_horses_raw`(28008)

### `dist_aptitude_bonus(past_races, cur_dist, tol)`
- **위치**: `app.py:27683` (27683~27700)
- **설명**: [거리 적성·신규] 이번 경주 거리(±tol m 유사거리) 전적의 복승권율(1~3착 비율)로 적성 등급. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_keiba_build_form`(22994) · `_jra_build_form`(26786)

### `bad_track_rate(past_races)`
- **위치**: `app.py:27703` (27703~27716)
- **설명**: [불량주로 성적·신규] pastRaces 의 condition(주로: 무거움/불량) 경주 복승권율(1·2착 %) 계산. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_form_from_starters`(1542) · `score_horses_raw`(28008)

### `_kra_race_placings(race)`
- **위치**: `app.py:27726` (27726~27738)
- **설명**: KRA 경주 1건 → rcTime 오름차순 착순 맵 {마번:착순}. rcTime 없는 말 제외. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_form_ext_build`(27752)

### `_date_diff_days(d1, d2)`
- **위치**: `app.py:27741` (27741~27749)
- **설명**: '20260620' 형식 두 날짜의 일수 차(d2-d1). 파싱 실패 시 None. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_form_ext_build`(27752)

### `_form_ext_build()`
- **위치**: `app.py:27752` (27752~27839)
- **설명**: KRA 이력 스캔 → {synergy, trainer, postPos} 통계. 표본 하한 적용(궁합2·조교사5·마번5). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_form_ext_stats`(27847)

### `_rate(pls)`
- **위치**: `app.py:27803` (27803~27805)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 4)**: `_dark_stats_recompute`(9265) · `_threshold_candidates`(15448) · `_recompute_learning_stats`(16943) · `_form_ext_build`(27752)

### `_infer_gait_from_history(horse_name)`
- **위치**: `app.py:27842` (27842~27844)
- **설명**: [한국 각질 추론] KRA 이력 stOrd(초반위치) 패턴 → 각질+신뢰도. 반환 {gait, conf, n} 또는 None. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_gait_of`(27230)

### `jockey_horse_synergy_bonus(jockey, horse_name)`
- **위치**: `app.py:27864` (27864~27879)
- **설명**: [2] 기수-말 궁합 — KRA 이력 조합 복승권율. A(40%+)+15 / B(20~40%)+5 / C(20%↓)-5 / 첫기승 표시. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `form_ext_stats_api`(26195) · `score_horses_raw`(28008)

### `post_position_ctx(meet, no)`
- **위치**: `app.py:27901` (27901~27910)
- **설명**: [4] 마번 유불리 — 경주장별 마번 복승률 vs 경주장 평균 편차. 반환 {rate, base, delta, n} 또는 None. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `form_ext_stats_api`(26195) · `score_horses_raw`(28008)

### `back_power_bonus(my_last3f, all_last3f)`
- **위치**: `app.py:27914` (27914~27928)
- **설명**: [3번] 상승3F(막판 뒷힘). 경주 내 상대: 상위30%(빠름) +15, 하위30%(느림) -5, 보통 0. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_apply_back_power`(23054)

### `_high_odds_verdict(win_odds, has_signal)`
- **위치**: `app.py:27932` (27932~27945)
- **설명**: [1·5번] 단승 배당 기준 고배당 제거. 999=무조건 / 100배+=확실 / 50~99=권장 / 30~49=신호없으면 제거. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_elimination`(3672)

### `special_flags(horse, race)`
- **위치**: `app.py:27949` (27949~27971)
- **설명**: 동일거리 2착 2회↑ → 삼복승필수 / 마체중 ±10kg↑ 경고 / 출전간격 3주↑ 주의 / 연속출전 피로도. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `score_horses_raw`(28008)

### `_upgrade(grade)`
- **위치**: `app.py:27978` (27978~27980)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `classify_grades`(27983)

### `score_horses_raw(race, horses, jockey_threshold, leader_names)`
- **위치**: `app.py:28008` (28008~28070)
- **설명**: 3-1~3-4: 마필별 baseScore/courseBonus/jockeyBonus/distanceBonus/jockeyChangeBonus/ *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `compute_horse_scores`(28073) · `combined_analyze`(28221)

### `_anomaly_by_no(race_key, horses_for_edge)`
- **위치**: `app.py:28111` (28111~28120)
- **설명**: odds_compute(전적 총점을 실력 확률로) 결과를 마번→이상감지 dict 로 정리. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `combined_analyze`(28221)

### `_pl_topk_prob(strengths, combo)`
- **위치**: `app.py:28171` (28171~28186)
- **설명**: Plackett-Luce: combo의 k마리가 정확히 상위 k착을 차지할 확률(순서 무관). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `build_bets`(28189)

### `build_bets(picks, scored, budget)`
- **위치**: `app.py:28189` (28189~28218)
- **설명**: 위치 기반 A/B/C/INS 픽으로 5개 베팅 슬롯 구성 → 비중·금액·손익분기·기대값. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `combined_analyze`(28221)

### `_pdf_md5(path)`
- **위치**: `app.py:28285` (28285~28294)
- **설명**: PDF 파일의 MD5 해시(재업로드 동일성 판정용). 실패 시 None. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `korea_start`(28781)

### `_pdf_cache_path(md5)`
- **위치**: `app.py:28297` (28297~28298)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_pdf_cache_load`(28301) · `_pdf_cache_save`(28315)

### `_korea_default_session()`
- **위치**: `app.py:28334` (28334~28338)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 4)**: `_korea_run_job`(28595) · `korea_start`(28781) · `korea_status`(28817) · `korea_session`(28825)

### `_render_region_b64(page, frac, target_w)`
- **위치**: `app.py:28359` (28359~28368)
- **설명**: PDF 페이지의 frac(x0,y0,x1,y1 비율) 영역을 target_w 픽셀 폭 PNG(base64)로 렌더. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 4)**: `_render_thumb`(28371) · `_render_band`(28372) · `_render_train`(28373) · `_render_full`(28374)

### `_render_thumb(page)`
- **위치**: `app.py:28371` (28371~28371)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_korea_run_job`(28595)

### `_render_band(page)`
- **위치**: `app.py:28372` (28372~28372)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_korea_extract_race`(28442)

### `_render_train(page)`
- **위치**: `app.py:28373` (28373~28373)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_korea_extract_race`(28442)

### `_render_full(page)`
- **위치**: `app.py:28374` (28374~28374)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_korea_run_job`(28595)

### `_korea_race_label(venue, race_no, distance)`
- **위치**: `app.py:28377` (28377~28381)
- **설명**: static/js/app.js 의 raceLabel 과 동일 — History 제목 매칭을 위해 형식 유지. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_korea_run_job`(28595)

### `_load_base_jockeys()`
- **위치**: `app.py:28384` (28384~28394)
- **설명**: 정적 기수 DB(static/data/jockeys.json)를 이름 키 dict 로 로드. PDF 추출값을 위에 덮어씀. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_korea_run_job`(28595)

### `_merge_jockey(stats, j)`
- **위치**: `app.py:28397` (28397~28409)
- **설명**: PDF '기수 기승현황표' 한 행을 stats 에 병합 (app.js mergeJockey 와 동일 계산). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_korea_run_job`(28595)

### `_extract_korea_post_time(text)`
- **위치**: `app.py:28415` (28415~28439)
- **설명**: 한국 PDF 요약 페이지 텍스트에서 발주시각 'HH:MM'(24h·2자리 정규화) 추출. 실패 시 None. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_korea_extract_race`(28442)

### `_korea_extract_race(doc, race, api_key)`
- **위치**: `app.py:28442` (28442~28466)
- **설명**: 요약 페이지 1장에서 메인표+조교표를 추출·병합해 출전마 리스트 반환 (app.js extractRaceFull). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `_korea_run_job`(28595) · `korea_reextract`(28845) · `_analyze_one`(28704)

### `_prerace_key(date, venue, race_no)`
- **위치**: `app.py:28493` (28493~28496)
- **설명**: 파일 안전 키: '2026-07-05_부산_3'. 경마장 미상은 '기타'. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_prerace_save_race`(28499)

### `_prerace_index_path()`
- **위치**: `app.py:28529` (28529~28530)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_prerace_index_update`(28533) · `_prerace_index_load`(28545)

### `_prerace_clear()`
- **위치**: `app.py:28583` (28583~28592)
- **설명**: data/prerace/ 전체 삭제('새 PDF 업로드' 초기화 시). 폴더 없으면 무시. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `korea_reset`(28831)

### `_korea_run_job(gen, api_key)`
- **위치**: `app.py:28595` (28595~28771)
- **설명**: 백그라운드 분석 본체. 세션(disk)을 기준으로 진행하며, 이미 끝난 단계는 건너뛰어 '재개'를 지원. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 0)**: —

### `cancelled()`
- **위치**: `app.py:28597` (28597~28598)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `_korea_run_job`(28595) · `_analyze_one`(28704) · `_detect_batch`(28625)

### `_detect_batch(item)`
- **위치**: `app.py:28625` (28625~28633)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 0)**: —

### `_korea_start_job(api_key)`
- **위치**: `app.py:28774` (28774~28777)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `korea_start`(28781) · `_korea_maybe_resume`(29016)

### `_korea_hist_file(date, venue, race_no)`
- **위치**: `app.py:28901` (28901~28904)
- **설명**: 날짜 하이픈 보존: 2026-07-03_서울_5R.json *(주석(본문))*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `korea_history_save`(28908)

### `_korea_maybe_resume()`
- **위치**: `app.py:29016` (29016~29023)
- **설명**: 서버 재시작 시 진행중이던 분석 자동 재개 (data/korea_session.json 기준). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_boot_background`(32285)

### `on_error(e)`
- **위치**: `app.py:29027` (29027~29039)
- **설명**: HTTP 예외(404/405 등)는 원래 상태코드를 보존 — 405가 500으로 둔갑하던 버그 수정 *(주석(본문))*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 0)**: —

### `_korea_schedule_post_epoch(rk, post_epoch)`
- **위치**: `app.py:29141` (29141~29168)
- **설명**: [수정3] 한국경마 경주의 발주 epoch(초)를 today_schedule 에 반영 → 타임 스냅샷(T-7/T-5) 작동. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_do_triple_ingest`(2166) · `kra_watch`(25474)

### `_post_time_epoch(hhmm, ymd)`
- **위치**: `app.py:29171` (29171~29181)
- **설명**: 'HH:MM' + ymd(YYYYMMDD) → 발주 epoch(서버 로컬). 실패 시 None. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 6)**: `_keirin_venue_current`(22301) · `kra_watch`(25474) · `_multi_race_list`(29207) · `_keirin_schedule`(29272) · `_nar_race_list`(29588) · `multi_keirin_schedule`(30399)

### `_multi_post_time(op, sp, ymd, rno)`
- **위치**: `app.py:29187` (29187~29204)
- **설명**: [1번] 특정 경주 RaceList.do?raceNb=N 페이지에서 発走時間(HH:MM) 추출. 당일 캐시·실패 시 None(격리). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 0)**: —

### `_multi_race_list(op, sp, ymd)`
- **위치**: `app.py:29207` (29207~29229)
- **설명**: [1번] oddspark RaceList.do → [{raceNo, postTime, postEpoch}]. 경주번호는 목록 페이지에서, 発走時間은 경주별 페이지에서 병렬 취득. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_multi_schedule_fetch`(29620)

### `_keirin_post_time(jo, ymd, rno)`
- **위치**: `app.py:29235` (29235~29252)
- **설명**: [경륜 서버수집] 경주별 RaceList.do 페이지에서 発走時間(HH:MM) 추출. 당일 캐시·실패 None(격리). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_keirin_venue_current`(22301)

### `_keirin_venue_races(jo, ymd)`
- **위치**: `app.py:29255` (29255~29269)
- **설명**: [경륜 서버수집] joCode의 경륜장명 + 경주번호 목록. raceNo=1 공개 출주표 title에서 경륜장명, *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_keirin_venue_current`(22301) · `_keirin_schedule`(29272)

### `_keirin_schedule(ymd, force)`
- **위치**: `app.py:29272` (29272~29308)
- **설명**: [경륜 서버 직접 수집] oddspark 경륜 홈(로그인 불필요·공개)에서 오늘 개최 경륜장(joCode) 추출 → *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `keirin_current`(22331) · `_multi_schedule_fetch`(29620)

### `_kra_schedule(ymd, force)`
- **위치**: `app.py:29319` (29319~29351)
- **설명**: [한국경마 서버 감지] 오늘 개최 KRA 경주 → [{venue, meet, sport:'korea', races:[{raceNo, horses, postTime:None}]}]. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_multi_schedule_fetch`(29620)

### `_nar_date_param(ymd)`
- **위치**: `app.py:29393` (29393~29394)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `_nar_odds_url`(29397) · `_nar_autocollect_form`(29553) · `_nar_race_list`(29588)

### `_nar_autocollect_form(rk, baba, ymd, rno)`
- **위치**: `app.py:29553` (29553~29585)
- **설명**: [南関東 전적 자동수집] keiba.go.jp DebaTable → starters_store. `_keiba_autocollect_form` 의 NAR판. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_multi_collect_one`(29676)

### `_nar_race_list(baba, ymd)`
- **위치**: `app.py:29588` (29588~29600)
- **설명**: keiba.go.jp RaceList(당일 메뉴) → [{raceNo, postTime, postEpoch}]. 미개최면 []. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_nar_schedule`(29603)

### `_nar_schedule(ymd)`
- **위치**: `app.py:29603` (29603~29617)
- **설명**: 南関東 4장 중 오늘 개최분만 트랙 레코드로 반환(미개최=제외). oddspark 커버 경마장과 중복 없음. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_multi_schedule_fetch`(29620)

### `_multi_key(venue, race_no)`
- **위치**: `app.py:29667` (29667~29669)
- **설명**: [岐阜·기후 중복 통합] 저장키의 경마장명을 표준(한글)으로 정규화 → 岐阜/기후가 한 키로 병합(중복 저장 방지). *(주석(본문))*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 7)**: `_triple_analyze`(9492) · `_multi_collect_one`(29676) · `multi_dashboard`(30463) · `_auto_pred_tick`(31404) · `_collect_cycle_begin`(31654) · `_collect_cycle_end`(31687) … [+1]

### `_multi_sport_of(rec)`
- **위치**: `app.py:29876` (29876~29891)
- **설명**: [통합·4번] rec의 sport/category → 대시보드 종목 그룹(horse|central|cycle|boat|korea). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_multi_card`(30287) · `multi_dashboard`(30463)

### `_card_conf_value(conf)`
- **위치**: `app.py:29894` (29894~29907)
- **설명**: [확신도 표시 수정] 카드에 표시할 확신도 스칼라 추출. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 5)**: `_triple_analyze`(9492) · `_multi_card`(30287) · `_kakao_rich_message`(30882) · `_kakao_notify_race`(30996) · `_timeline_extract`(31064)

### `_live_exact_hit(key, top3, horse_count)`
- **위치**: `app.py:29910` (29910~29996)
- **설명**: [적중 표시 정직화 (2026-07-19)] '라이브에서 실제 화면에 표시된 추천'(analysis_log의 corePicks· *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 5)**: `_build_race_result`(14939) · `_keirin_result_backfill_once`(24371) · `keirin_today_stats`(24981) · `_scoreboard_daily`(25042) · `_race_result_summary`(30223)

### `_combset(pred)`
- **위치**: `app.py:29950` (29950~29961)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_live_exact_hit`(29910)

### `_valid(c)`
- **위치**: `app.py:29971` (29971~29976)
- **설명**: [로그 오염 방어] 출주 두수 초과 마번 조합(타 경주 혼입 — 경륜 7명에 1+10 등)은 판정 제외 *(주석(본문))*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_live_exact_hit`(29910)

### `_gemini_latest_map(ttl)`
- **위치**: `app.py:30006` (30006~30034)
- **설명**: logs/gemini_review/*.json → {raceKey: {status, issues, summary, time}} 최신본 1건씩. 60초 캐시. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_results_history_rows`(30093) · `_multi_card`(30287)

### `_log_polluted(doc)`
- **위치**: `app.py:30037` (30037~30056)
- **설명**: [마감 후 오염 감지] '🔒 마감 확정'(closed) 행의 복승 명단 vs 현재 표시 명단(displayedCombos)이 *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_hist_row_from_doc`(30059)

### `_hist_row_from_doc(doc, race_id)`
- **위치**: `app.py:30059` (30059~30090)
- **설명**: 분석 로그 1건 → 결과기록 카드 1행. (Gemini 는 캐시가 매번 최신으로 덮으므로 여기선 제외) *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_results_history_rows`(30093)

### `_results_history_rows(ttl)`
- **위치**: `app.py:30093` (30093~30137)
- **설명**: [결과기록 전면개편] data/analysis_log/ → 카드 행 목록(최신순). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `results_history`(30177) · `_multi_card`(30287)

### `_race_result_summary(key)`
- **위치**: `app.py:30223` (30223~30279)
- **설명**: [카드 적중 표시 (2026-07-19)] 결과 파일 → {top3, hit(복승 정확), trioHit(삼복승 정확), subHit, *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `race_log_page`(24480) · `_multi_card`(30287) · `multi_race_detail`(30609)

### `_multi_card(key, rec)`
- **위치**: `app.py:30287` (30287~30395)
- **설명**: [3번] 카드 요약: analyze(기존 _triple_analyze 재사용·읽기전용) → 유력마TOP3·핵심신호·확신도·마감남은초·색상. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `multi_dashboard`(30463)

### `_rec_trail_for_detail(key)`
- **위치**: `app.py:30550` (30550~30605)
- **설명**: [추천 내역 상세 (2026-07-21 권대표 요청)] 카드 상세에 '실제 표시된 추천 + 생성됐지만 노출 제외된 *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `multi_race_detail`(30609)

### `_slim(lst)`
- **위치**: `app.py:30577` (30577~30593)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_rec_trail_for_detail`(30550)

### `_combos(key)`
- **위치**: `app.py:30750` (30750~30758)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_kakao_anchor_log`(30745)

### `_star(n)`
- **위치**: `app.py:30889` (30889~30890)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_kakao_rich_message`(30882)

### `_timeline_snap_path(rk)`
- **위치**: `app.py:31060` (31060~31061)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 4)**: `_triple_analyze`(9492) · `timeline_snapshot_get`(19544) · `_timeline_snap_save`(31092) · `_timeline_snap_apply_result`(31266)

### `_timeline_extract(an)`
- **위치**: `app.py:31064` (31064~31089)
- **설명**: 스냅샷용 요약 추출 — main(복승 메인 조합)·dark_horse(복병 1·2순위)·odds(메인 조합 배당). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_timeline_snap_save`(31092)

### `_timeline_phase_of(left)`
- **위치**: `app.py:31133` (31133~31137)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_timeline_snap_tick`(31140)

### `_timeline_snap_tick(races, now, db)`
- **위치**: `app.py:31140` (31140~31263)
- **설명**: [bg 훅] 각 경주의 잔여시간으로 phase 판정 → 미저장 phase 스냅샷 저장. T-7·T-5는 카카오 알림 트리거. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_auto_pred_tick`(31404)

### `_timeline_snap_apply_result(rk, top3)`
- **위치**: `app.py:31266` (31266~31296)
- **설명**: [결과] 각 스냅샷 적중(T7_hit/T5_hit) + 변경마 우승 판정 → 파일 갱신 + 통계 재계산. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_apply_result_learning`(17723)

### `_hit(main)`
- **위치**: `app.py:31279` (31279~31281)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `_snapshot_timing_learn`(3041) · `_pace_log_record`(8916) · `_timeline_snap_apply_result`(31266)

### `_r(v)`
- **위치**: `app.py:31324` (31324~31325)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_timeline_stats_recompute`(31299) · `signal_place_stats`(248)

### `_auto_pred_fname(rk)`
- **위치**: `app.py:31344` (31344~31347)
- **설명**: raceKey → YYYY_MM_DD_경주장_경주번호.json (경로조작 방어). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_auto_pred_save`(31350) · `_auto_pred_compare`(31376)

### `_auto_pred_compare(rk)`
- **위치**: `app.py:31376` (31376~31401)
- **설명**: [결과 대조] 실제 착순(results_store, 확장/일괄 수집분)이 있으면 예상과 대조해 파일에 기록. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_auto_pred_tick`(31404)

### `_auto_pred_tick(sched, now)`
- **위치**: `app.py:31404` (31404~31499)
- **설명**: [자동 예상·연속 전환] bg 루프에서 매 틱 호출 — ①마감5분전 예상 저장 ②결과 대조(+15분) ③현재/다음 상태 갱신. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_multi_bg_loop`(31554)

### `_multi_bg_loop()`
- **위치**: `app.py:31554` (31554~31651)
- **설명**: [1·2번] 백그라운드: 스케줄 30분 갱신 + 발주 임박 경주 배당 주기 수집(실패 격리·서버 죽지 않음). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 0)**: —  ⚠ 위험목록 함수 → 전수 나열

### `esc(s)`
- **위치**: `app.py:31981` (31981~31983)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `picks_diff_page`(31972) · `chips`(31985)

### `chips(items, cls)`
- **위치**: `app.py:31985` (31985~31988)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `picks_diff_page`(31972)

### `_loop()`
- **위치**: `app.py:32113` (32113~32124)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 0)**: —  ⚠ 위험목록 함수 → 전수 나열

### `_snapshot_shortage_check(sched, now)`
- **위치**: `app.py:32160` (32160~32209)
- **설명**: [수집 공백 경보 (2026-07-29)] 발주 3~15분 지난 경주의 배당 스냅샷 수가 SNAPSHOT_MIN_WARN 미만이면 *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_multi_bg_loop`(31554)  ⚠ 위험목록 함수 → 전수 나열

### `_multi_bg_watchdog()`
- **위치**: `app.py:32212` (32212~32224)
- **설명**: [안정화 (2026-07-19)] bg 수집 루프 워치독 — 하트비트가 180초+ 멈추면(스레드 사망·교착) 루프 재기동. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 0)**: —  ⚠ 위험목록 함수 → 전수 나열

### `_start_multi_race_bg()`
- **위치**: `app.py:32227` (32227~32234)
- **설명**: [6번] 다중 경주 백그라운드 시작(실패해도 서버·단일 모드 무영향). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_boot_background`(32285)

### `_startup_date_reset()`
- **위치**: `app.py:32237` (32237~32279)
- **설명**: [서버 시작 시 날짜 자동 초기화] 어제(오늘 아닌 날짜) 데이터를 자동 정리 → '어제 경주 잔존' 방지. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_multi_bg_loop`(31554) · `_boot_background`(32285)

### `_dstr(t)`
- **위치**: `app.py:32243` (32243~32244)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_startup_date_reset`(32237)

### `_boot_background()`
- **위치**: `app.py:32285` (32285~32315)
- **설명**: [24시간 자동수집·백업·학습] 백그라운드 작업 1회 기동(멱등). 로컬(__main__)·gunicorn(모듈로드) 공용. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 0)**: —

### `_flag(name, default)`
- **위치**: `gemini_reviewer.py:29` (29~31)
- **설명**: 환경변수 우선 · 미설정 시 기본값. '1/true/yes/on' 만 켜짐으로 본다. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `gemini_review_enabled`(34) · `gemini_kakao_enabled`(39)

### `_gemini_api_key()`
- **위치**: `gemini_reviewer.py:45` (45~46)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `review_async`(440) · `_run`(449)

### `_mask(text, key)`
- **위치**: `gemini_reviewer.py:49` (49~52)
- **설명**: 로그/예외 문자열에 API 키가 노출되지 않도록 마스킹. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `review_async`(440) · `_run`(449)

### `_fmt_combo(lst)`
- **위치**: `gemini_reviewer.py:54` (54~63)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_build_prompt`(65)

### `_build_prompt(rk, final_q, final_t, special_q, line_pairs, strong_signals, fav_axis, strong_axis, drops, cur_mb)`
- **위치**: `gemini_reviewer.py:65` (65~89)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `review_async`(440) · `_run`(449)

### `_pair_key(k)`
- **위치**: `gemini_reviewer.py:99` (99~103)
- **설명**: 조합 키를 '1+2' 문자열로 정규화((1,2) 튜플·'1+2'·[1,2] 모두 허용). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_fmt_odds_map`(131)

### `_as_odds_items(m)`
- **위치**: `gemini_reviewer.py:106` (106~128)
- **설명**: [형식 방어 2026-07-28] 배당 자료가 dict 와 list 두 형식으로 들어온다. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_fmt_odds_map`(131) · `_build_full_prompt`(246)

### `_fmt_odds_map(m, limit, asc)`
- **위치**: `gemini_reviewer.py:131` (131~142)
- **설명**: 배당 맵/목록 → '1+2:3.4 1+3:5.6 ...' (배당 오름차순). limit=None 이면 전량. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_fmt_timeline`(155) · `_build_full_prompt`(246)

### `_fmt_json_block(obj, title)`
- **위치**: `gemini_reviewer.py:145` (145~152)
- **설명**: 구조가 제각각인 분석 산출물은 JSON 원문 그대로 전달(정보 손실 0). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_build_full_prompt`(246)

### `_fmt_timeline(tl)`
- **위치**: `gemini_reviewer.py:155` (155~164)
- **설명**: 배당 시계열 전량 — 스냅샷마다 전 조합 배당. 급락 판정의 원재료라 절대 요약하지 않는다. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_build_full_prompt`(246)

### `_logic_source()`
- **위치**: `gemini_reviewer.py:206` (206~243)
- **설명**: app.py 에서 판단 로직 함수들의 실제 소스를 추출(줄번호 포함). 실패해도 분석은 계속된다. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_build_full_prompt`(246)

### `_build_full_prompt(ctx)`
- **위치**: `gemini_reviewer.py:246` (246~359)
- **설명**: [전체자료] 로직 소스 + 배당판 전량 + 시계열 전량 + 전적 + 판단근거 + 최종추천(절단 없음). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `_build_full_prompt`(246) · `review_async`(440) · `_run`(449)

### `_resolve_app_module()`
- **위치**: `gemini_reviewer.py:378` (378~392)
- **설명**: 실행 중인 app 모듈을 반환. `python app.py` 는 __main__, gunicorn 은 app 으로 로드된다. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_send_kakao`(395)

### `review_async(rk, final_q, final_t, special_q, line_pairs, strong_signals, fav_axis, strong_axis, drops, cur_mb, ctx)`
- **위치**: `gemini_reviewer.py:440` (440~525)
- **설명**: ctx 를 주면 [전체자료 모드](배당판·시계열·전적·판단근거 전량)로 분석한다. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 0)**: —

### `_run()`
- **위치**: `gemini_reviewer.py:449` (449~524)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 0)**: —

### `_combo_set(v)`
- **위치**: `review_engine.py:41` (41~49)
- **설명**: '1+4' / [1,4] / '1-4' → frozenset({1,4}) *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 6)**: `_displayed_sets`(52) · `_signal_horses`(64) · `_history_combos`(93) · `_locked_quinella_odds`(111) · `_fix_connector_combos`(409) · `replay_day`(478)

### `_displayed_sets(log_doc)`
- **위치**: `review_engine.py:52` (52~61)
- **설명**: 마감 동결 표시 조합(판정 대상) → (복승 set 목록, 삼복승 set 목록). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `classify_race`(134) · `replay_day`(478)

### `_signal_horses(log_doc)`
- **위치**: `review_engine.py:64` (64~90)
- **설명**: 이 경주에서 시스템이 '알고 있었던' 말 전부(유력마·복병·신호·확신도·고배당후보). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `classify_race`(134)

### `_history_combos(log_doc)`
- **위치**: `review_engine.py:93` (93~108)
- **설명**: 추천 이력에 등장했던 복승·삼복승 조합 전부(시점 무관). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `classify_race`(134)

### `_locked_quinella_odds(log_doc, combo_set)`
- **위치**: `review_engine.py:111` (111~121)
- **설명**: 표시(잠금) 시점의 해당 복승 배당 — rec_history 마지막 quinellas 스냅샷에서. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `classify_race`(134)

### `_is_tainted(doc)`
- **위치**: `review_engine.py:124` (124~131)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `classify_race`(134) · `replay_day`(478)

### `classify_race(doc, log_doc)`
- **위치**: `review_engine.py:134` (134~184)
- **설명**: 한 경주 분류. 반환 dict(type, hit, detail, slippage 등) — 저장은 호출부. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `sweep`(187)

### `_hms_sec(s)`
- **위치**: `review_engine.py:240` (240~245)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 4)**: `signal_place_stats`(248) · `replay_day`(478) · `_mb_of`(559) · `_kh_at`(280)

### `_kh_at(min_mb)`
- **위치**: `review_engine.py:280` (280~293)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `signal_place_stats`(248)

### `_r(d, extra)`
- **위치**: `review_engine.py:320` (320~325)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_timeline_stats_recompute`(31299) · `signal_place_stats`(248)

### `_horse_min(qmap)`
- **위치**: `review_engine.py:337` (337~348)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `drop_stats`(331)

### `_band(dr)`
- **위치**: `review_engine.py:350` (350~351)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_reversal_strength_stats`(21307) · `drop_stats`(331)

### `_fmt(t)`
- **위치**: `review_engine.py:392` (392~395)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 5)**: `_pub_pace`(13294) · `kra_status`(25511) · `_hist_row_from_doc`(30059) · `_timeline_snap_tick`(31140) · `drop_stats`(331)

### `_sport_of(doc, rk, keirin_re)`
- **위치**: `review_engine.py:402` (402~406)
- **설명**: ══════════ [2] 리플레이 — 정책별 가상 성적(전략 검증 1호) ══════════ *(주석(def 위))*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `replay_day`(478)

### `_fix_connector_combos(log_doc, limit)`
- **위치**: `review_engine.py:409` (409~457)
- **설명**: [fix_connectors 계열] favAxis 2두 × 연결마(keyHorses[2:] + confTop1) 복승 조합 집합. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `replay_day`(478)

### `_c_odds(_c)`
- **위치**: `review_engine.py:448` (448~451)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 0)**: —

### `replay_day(date, stake, keirin_re)`
- **위치**: `review_engine.py:478` (478~956)
- **설명**: [2] 하루치를 정책별로 재생 — displayedCombos·공식 확정배당 기준 가상 성적. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 0)**: —

### `_book(p, use_q, use_t, qh, th)`
- **위치**: `review_engine.py:526` (526~540)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `replay_day`(478)

### `_mb_of(_e)`
- **위치**: `review_engine.py:559` (559~566)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `replay_day`(478) · `_freeze_sets`(568)

### `_freeze_sets(min_mb)`
- **위치**: `review_engine.py:568` (568~599)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `replay_day`(478)

### `_qsig(snap)`
- **위치**: `tools/dedupe_odds_snapshots.py:43` (43~59)
- **설명**: 복승 배당 내용의 서명 — dict/list 양쪽 형식을 정규화(app.py `_as_qmap` 과 같은 취지). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_key`(62)

### `_key(snap)`
- **위치**: `tools/dedupe_odds_snapshots.py:62` (62~67)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `dedupe_snapshots`(75) · `main`(99)

### `_n_anom(snap)`
- **위치**: `tools/dedupe_odds_snapshots.py:70` (70~72)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `dedupe_snapshots`(75) · `main`(99)

### `dedupe_snapshots(snaps)`
- **위치**: `tools/dedupe_odds_snapshots.py:75` (75~89)
- **설명**: 반환 (남길 리스트, 제거 수). 원본 순서를 보존한다. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `main`(99)

### `main()`
- **위치**: `tools/dedupe_odds_snapshots.py:99` (99~195)
- **설명**: ⚠ 서버가 라이브로 같은 파일을 쓰고 있다. 최근 갱신 파일을 건드리면 *(주석(본문))*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 0)**: —

### `_mk(id, area, name, denominator, current, target, ok, n, note, reason)`
- **위치**: `tools/health_check.py:45` (45~51)
- **설명**: 항목 1건. `denominator` 는 필수다 — 분모를 못 적으면 그 항목은 측정된 것이 아니다. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 8)**: `_win_exacta_reversal`(4933) · `check_snapshot_coverage`(68) · `check_save_failures_fixed`(152) · `check_save_failures_unfixed`(167) · `check_schema_contract`(180) · `check_schema_drift`(236) … [+2]

### `_distinct_ticks(doc)`
- **위치**: `tools/health_check.py:54` (54~63)
- **설명**: 같은 (초, src) 를 1틱으로 센다 — 2중 기록(2026-07-30 수정분) 이전 데이터 보정. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `check_snapshot_coverage`(68)

### `_today_logs()`
- **위치**: `tools/health_check.py:111` (111~124)
- **설명**: 당일 stdout 로그 파일들(회전본 포함). 회전본은 `server_stdout.log.<YYYYMMDD_HHMMSS>`. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_count_save_failures`(127)

### `_red_field_rates()`
- **위치**: `tools/health_check.py:195` (195~233)
- **설명**: 🔴 필드 보유율을 **당일분 / 누적**으로 각각 산출. 반환 (day, cum, day_rows, cum_rows, note). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `check_schema_drift`(236)

### `build_checklist()`
- **위치**: `tools/health_check.py:348` (348~370)
- **설명**: 반환 dict 의 **최상단에 `summary` 계열을 배치**한다(모바일에서 먼저 보이도록). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `main`(373)

### `main()`
- **위치**: `tools/health_check.py:373` (373~398)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 0)**: —

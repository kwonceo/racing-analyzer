# 미분류 — 분류 규칙에 걸리지 않은 함수

> 🤖 **자동 생성** — `python tools/build_codemap.py` · 2026-08-12 07:29:59
> ⚠ 손으로 고치지 마세요. 다음 생성 때 덮어써집니다.
> ⚠ **AST 한계**: `getattr()` 동적 호출 · `importlib` 경유 · 문자열 디스패치는 잡지 못합니다
>   (`gemini_reviewer`·`health_check` 가 실제로 `importlib` 경유입니다).

총 **734개** 함수

### `load_env()`
- **위치**: `app.py:66` (66~76)
- **설명**: ---------- .env 로더 (dotenv 의존성 없이) ---------- *(주석(def 위))*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 0)**: —

### `_cors_origin_allowed(origin)`
- **위치**: `app.py:104` (104~110)
- **설명**: 요청 Origin 이 허용 목록/패턴에 해당하는지. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_cors_preflight`(125) · `_cors_headers`(138)

### `_cors_preflight()`
- **위치**: `app.py:125` (125~135)
- **설명**: OPTIONS preflight 은 허용 오리진일 때 200 으로 즉시 응답(수동 폴백 경로) *(주석(본문))*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 0)**: —

### `_cors_headers(resp)`
- **위치**: `app.py:138` (138~145)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 0)**: —

### `_access_tier(path)`
- **위치**: `app.py:170` (170~174)
- **설명**: 엔드포인트 접근 등급 반환. 미분류는 공개(현행 유지). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_premium_gate`(183)

### `_request_is_authed(req)`
- **위치**: `app.py:177` (177~179)
- **설명**: 향후 실제 인증 연동 지점(현재는 토큰 유무만 확인 — 미강제 상태라 미사용). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_premium_gate`(183)

### `client(api_key)`
- **위치**: `app.py:204` (204~208)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `call_claude`(631)

### `_api_should_retry(e)`
- **위치**: `app.py:602` (602~611)
- **설명**: 이 예외가 재시도 가치가 있는 일시적 오류인지. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_messages_create_retry`(614)

### `_messages_create_retry(cli, **kwargs)`
- **위치**: `app.py:614` (614~628)
- **설명**: messages.create 를 지수 백오프로 재시도(2·4·8·16초 + 지터). 마지막 실패는 그대로 raise. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `call_claude`(631)

### `call_claude(content, schema, max_tokens, api_key)`
- **위치**: `app.py:631` (631~654)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 10)**: `_do_extract_jockey`(713) · `_do_extract_race`(731) · `_do_extract_training`(767) · `extract_results`(788) · `result_ocr`(838) · `_do_detect`(879) … [+4]

### `image_block(img)`
- **위치**: `app.py:657` (657~665)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 9)**: `_do_extract_jockey`(713) · `_do_extract_race`(731) · `_do_extract_training`(767) · `extract_results`(788) · `result_ocr`(838) · `_do_detect`(879) … [+3]

### `_do_extract_jockey(img, api_key)`
- **위치**: `app.py:713` (713~722)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `extract_jockey`(726) · `_korea_run_job`(32957)

### `_do_extract_race(img, api_key)`
- **위치**: `app.py:731` (731~758)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `extract_race`(762) · `_korea_extract_race`(32627)

### `_do_extract_training(img, api_key)`
- **위치**: `app.py:767` (767~777)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `extract_training`(781) · `_korea_extract_race`(32627)

### `_img_from_dataurl(img)`
- **위치**: `app.py:828` (828~834)
- **설명**: dataURL 문자열('data:image/png;base64,...') 또는 {media_type,data} → image_block 입력형. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `result_ocr`(838)

### `_do_detect(imgs, api_key)`
- **위치**: `app.py:879` (879~897)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `detect`(901) · `_korea_run_job`(32957) · `_detect_batch`(32987)

### `_norm_korea_horse(h, jstats)`
- **위치**: `app.py:908` (908~921)
- **설명**: 추출된 한국 출전마(horseNum/horseName…) → 스코어링 입력(no/name/pastRaces…) 정규화. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_korea_deterministic_overlay`(924) · `_det_horse_note`(943)

### `_korea_deterministic_overlay(race, jstats)`
- **위치**: `app.py:924` (924~940)
- **설명**: pastRaces 가 추출된 경우에만 결정론 점수·각질·변화감지·2착패턴(삼복승필수)을 산출. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_do_analyze`(991)

### `_det_horse_note(h, race, leaders)`
- **위치**: `app.py:943` (943~964)
- **설명**: 한 마리의 pastRaces 로부터 프롬프트에 넣을 결정론 힌트 텍스트(없으면 ''). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_do_analyze`(991)

### `_force_trifecta_from_must(report, overlay)`
- **위치**: `app.py:967` (967~988)
- **설명**: 2착패턴(삼복승필수) 말을 삼복승 조합에 결정론적으로 강제 편성. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_do_analyze`(991)

### `_odds_get_race(db, race_key)`
- **위치**: `app.py:1219` (1219~1220)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `odds_add_snapshot`(1249) · `odds_compute`(1301) · `odds_race_route`(31257)

### `_clamp(x, lo, hi)`
- **위치**: `app.py:1240` (1240~1241)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `odds_compute`(1301)

### `_valid_series(arr)`
- **위치**: `app.py:1244` (1244~1246)
- **설명**: null 제거한 유효 배당만 [(index, odds), ...] *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `odds_compute`(1301)

### `odds_add_snapshot(race_key, odds_by_horse, t)`
- **위치**: `app.py:1249` (1249~1277)
- **설명**: 한 라운드 스냅샷 기록: {마번: 배당} 을 하나의 타임스탬프로 누적. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `odds_snapshot`(1387) · `combined_analyze`(32402)

### `odds_undo(race_key)`
- **위치**: `app.py:1280` (1280~1292)
- **설명**: 마지막 스냅샷 1라운드 되돌리기 *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `odds_undo_route`(31243)

### `odds_clear(race_key)`
- **위치**: `app.py:1295` (1295~1298)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `odds_clear_route`(31250) · `combined_analyze`(32402)

### `odds_compute(race_key, bmed_horses)`
- **위치**: `app.py:1301` (1301~1365)
- **설명**: bmed_horses: [{no, name, score}] → 말별 드롭/괴리/통합 신호 + 태그 *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `odds_compute_route`(31264) · `_anomaly_by_no`(32292)

### `odds_suggest_bets(computed)`
- **위치**: `app.py:1368` (1368~1383)
- **설명**: signalScore 상위 말로 복승(2)·삼복승(3) 보정 추천(국소 계산) *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `odds_compute_route`(31264)

### `_sport_match(rec_sport, want)`
- **위치**: `app.py:1492` (1492~1498)
- **설명**: [종목 분리] 레코드 sport 와 요청 sport 매칭. 레거시(None/'')는 horse 로 간주 *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `triple_latest`(3396) · `current_race`(4205)

### `_horse_has_form(h)`
- **위치**: `app.py:1529` (1529~1533)
- **설명**: 이 마필 항목이 실제 전적(착순/전적점수)을 담고 있는가. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 4)**: `_rec_form_count`(1536) · `_sanitize_starters`(1541) · `extract_japan`(14344) · `korea_form`(14399)

### `_rec_form_count(rec)`
- **위치**: `app.py:1536` (1536~1538)
- **설명**: 레코드 안에서 전적이 채워진 마필 수. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `extract_japan`(14344)

### `_sanitize_starters(horses)`
- **위치**: `app.py:1541` (1541~1557)
- **설명**: 출마표2/DebaTable 파서 오탐(오즈표·전체목록 스크레이핑으로 수백 행이 딸려오는 경우) 방어. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `extract_japan`(14344) · `korea_form`(14399)

### `_form_from_starters(rk, drops, sport, valid_nos)`
- **위치**: `app.py:1560` (1560~1681)
- **설명**: 저장된 전적으로 마필 점수·등급 계산. 배당 급락마는 이상감지 상향 반영. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_hno(h)`
- **위치**: `app.py:1609` (1609~1613)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_form_from_starters`(1560) · `_learn_upset`(18395)

### `_drop_placeholder_combos(combos, label)`
- **위치**: `app.py:1687` (1687~1707)
- **설명**: [placeholder 배당 필터 (2026-07-19)] 발매 전 껍데기 배당(예 1054.4·9999.9)이 유력마·복병· *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_is_opening_settle(po, pct)`
- **위치**: `app.py:1754` (1754~1757)
- **설명**: opening/placeholder 배당(직전 100배+)이 실배당으로 정착하며 생기는 기계적 급락(≤-80%). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_triple_analyze`(10724) · `_history_append`(15465)

### `_baseline_reset_needed(prev_q, cur_q, thresh, min_ratio, min_combos)`
- **위치**: `app.py:1760` (1760~1775)
- **설명**: [경주전환·초반미수집 방어] 직전 대비 공통 복승 조합의 다수(60%+)가: *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_do_triple_ingest`(2768) · `_triple_analyze`(10724)

### `_as_qmap(x)`
- **위치**: `app.py:1778` (1778~1792)
- **설명**: 복승 배당을 {정렬튜플: 최저배당} 맵으로 정규화 — 리스트([{combo,odds}])와 *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 7)**: `_next_race_surge`(1795) · `_board_flip_suspect`(1808) · `_combo_count_dip`(1842) · `_column_shift_suspect`(1867) · `_horse_rep_series`(7780) · `_combo_odds_trend`(14110) … [+1]

### `_next_race_surge(prev_q, cur_q, thresh, min_ratio, min_combos)`
- **위치**: `app.py:1795` (1795~1805)
- **설명**: [2번 다음경주 혼입 방어] 직전 대비 공통 복승 조합의 다수(60%+)가 200%+ 급등하면 *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_history_append`(15465)

### `_combo_count_dip(prev_q, cur_q, ratio, min_prev)`
- **위치**: `app.py:1842` (1842~1864)
- **설명**: [3번 조합 수 딥] 직전 대비 복승 조합 수가 급감했는지. 실측 112건/23,230틱(0.48%). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_history_append`(15465)

### `_close(a, b)`
- **위치**: `app.py:1886` (1886~1887)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_column_shift_suspect`(1867)

### `_sport_max_no(sport)`
- **위치**: `app.py:1929` (1929~1931)
- **설명**: 종목별 유효 최대 마번 반환(모르는 종목은 경마 기준 18로 관대 처리). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_oddspark_covered_active(rk, within)`
- **위치**: `app.py:1934` (1934~1952)
- **설명**: [자동전송 꼬임 방어] 이 raceKey를 서버가 oddspark로 방금(within초 내) 직접 수집 중인가? *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `triple_analyze`(15132)

### `_canon_rk(rk)`
- **위치**: `app.py:1962` (1962~1982)
- **설명**: [① raceKey 통일] 오늘 날짜 접두("2026-07-18 오비히로 2경주")를 무접두("오비히로 2경주")로 통일. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 11)**: `triple_ingest`(2473) · `_do_triple_ingest`(2768) · `extract_japan`(14344) · `triple_analyze`(15132) · `_validate_learning_data`(21503) · `_ml_row_build`(21532) … [+5]

### `_src_is_oddspark(src)`
- **위치**: `app.py:1985` (1985~1987)
- **설명**: 소스 문자열이 oddspark(백업 소스)인지. 사설(ks1)·keiba·kra_api·화면수집 등은 False(=배당판 권위). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_do_triple_ingest`(2768) · `_kakao_send_blocked_by_src`(36277)

### `_combo_value_multiset(combos)`
- **위치**: `app.py:1990` (1990~1999)
- **설명**: 조합 목록 → 배당'값' 멀티셋(반올림). 말번호 무관하게 '같은 경주인지' 비교용. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_oddspark_mapping_suspect`(2450)

### `_combo_map(combos)`
- **위치**: `app.py:2002` (2002~2013)
- **설명**: 조합 목록 → {frozenset(말번호쌍): 배당값}. 말번호 매핑 비교용. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_oddspark_mapping_suspect`(2450)

### `_src_divergence(prev_q, new_q)`
- **위치**: `app.py:2030` (2030~2063)
- **설명**: 두 소스의 공통 조합 배당을 대조 → {n, off(%), med} 또는 None. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_do_triple_ingest`(2768) · `_kakao_send_blocked_by_src`(36277)

### `_oddspark_seen_note(rk, q)`
- **위치**: `app.py:2208` (2208~2225)
- **설명**: oddspark 페이로드의 마번을 기록(1층 명단 확장용). 실패해도 예외를 올리지 않는다. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_do_triple_ingest`(2768)

### `_oddspark_seen_get(rk)`
- **위치**: `app.py:2228` (2228~2235)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_ingest_ghost_verdict`(2238) · `_odds_suspect_verdict`(2313)

### `_nos(m)`
- **위치**: `app.py:2395` (2395~2403)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_curq_shrink_guard`(2363)

### `_combo_horse_count(combos)`
- **위치**: `app.py:2577` (2577~2588)
- **설명**: 조합 목록에 등장하는 서로 다른 마번 수(N). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_arity_guard`(2604)

### `_arity_expect(kind, n)`
- **위치**: `app.py:2591` (2591~2601)
- **설명**: N두 기준 기대 조합 수. 복승 nC2 · 쌍승 nP2 · 삼복승 nC3. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_arity_guard`(2604)

### `_exacta_mirrors_quinella(q, x, source)`
- **위치**: `app.py:2637` (2637~2682)
- **설명**: [오염방지 3-b·핵심] 쌍승이 사실상 복승 배당의 복사본인지 판정. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_do_triple_ingest`(2768)

### `_combo_max_no(*sources)`
- **위치**: `app.py:2706` (2706~2728)
- **설명**: [종목 증거판정용] 배당 조합(복승·쌍승·삼복승)에 등장하는 최대 마번을 추출. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_do_triple_ingest`(2768)

### `_race_pin_expire_stale()`
- **위치**: `app.py:3308` (3308~3322)
- **설명**: [날짜 초기화] 고정 경주의 저장 날짜가 오늘이 아니면(어제 이전) 자동 해제 → None 반환. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_race_pin_load`(3325)

### `_race_pin_clear()`
- **위치**: `app.py:3338` (3338~3342)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `_race_pin_expire_stale`(3308) · `race_pin`(3377) · `_startup_date_reset`(38950)

### `_is_central_key(_k)`
- **위치**: `app.py:3429` (3429~3433)
- **설명**: [중앙경마 탭 실시간 분석 (2026-07-19)] JRA는 oddspark 미지원(서버 수집 불가) — 확장이 배당판에서 *(주석(def 위))*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `triple_latest`(3396)

### `_anomaly_pretty(raw)`
- **위치**: `app.py:3469` (3469~3486)
- **설명**: 스냅샷 이상감지 원문 → {text, severity} 로 정규화(사람이 읽는 누적 피드용). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_anomaly_events_from_doc`(3502) · `history_list`(20496)

### `_anomaly_combo(raw)`
- **위치**: `app.py:3489` (3489~3499)
- **설명**: 스냅샷 이상감지 원문 → 조합 문자열(예 '2+3'·'4'·'5→10'). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_anomaly_events_from_doc`(3502)

### `_anomaly_events_from_doc(doc)`
- **위치**: `app.py:3502` (3502~3520)
- **설명**: [이상감지 누적·공용] 스냅샷 doc → 시간순·중복제거 이상감지 이벤트 리스트. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `_concentration_counts`(3523) · `anomaly_feed`(3540) · `_build_analysis_log`(16347)

### `_concentration_counts(doc)`
- **위치**: `app.py:3523` (3523~3536)
- **설명**: [복병_집중급락] 경주 이상감지 이벤트에서 말별 등장 횟수(급락/역전 조합에 낀 횟수). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_safe_snapshot_name(fn)`
- **위치**: `app.py:3676` (3676~3684)
- **설명**: [경로조작 방어] 파일명 정규화 — basename·허용문자(한글·영숫자·_-.)·.png 강제·길이 제한. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `snapshot_save`(3994) · `snapshot_get`(4072)

### `_snapshot_meta_path(fn)`
- **위치**: `app.py:3687` (3687~3688)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 4)**: `_snapshot_dates_for_race`(3793) · `_snapshot_metas_for_race`(3814) · `snapshot_save`(3994) · `snapshot_list`(4043)

### `_snapshot_result_for(rk)`
- **위치**: `app.py:3691` (3691~3705)
- **설명**: [스냅샷↔결과 비교] raceKey 로 저장된 실제 착순(top3) 조회(유연 매칭). 없으면 None. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 4)**: `_snapshot_timing_learn`(3928) · `snapshot_list`(4043) · `dark_horse_review`(22611) · `_auto_pred_compare`(36837)

### `_snapshot_min_quinella(quinellas)`
- **위치**: `app.py:3739` (3739~3754)
- **설명**: [비교분석] 추천 복승 조합 중 최저 배당 + 그 조합 반환. 없으면 (None, None). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `_snapshot_build_compare`(3868) · `snapshot_save`(3994) · `_pack`(3885)

### `_snapshot_main_combos(quinellas, cap)`
- **위치**: `app.py:3757` (3757~3767)
- **설명**: [비교분석] 추천 변경 여부 판정용 — 상위 복승 조합을 정규화 문자열 집합으로. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_snapshot_build_compare`(3868)

### `_snapshot_signal_first(rk)`
- **위치**: `app.py:3770` (3770~3790)
- **설명**: [비교분석] odds_history 에서 이 경주의 이상감지 신호(signal_horse)가 처음 잡힌 시점을 'T-N분'으로. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_snapshot_build_compare`(3868)

### `_snapshot_dates_for_race(rk)`
- **위치**: `app.py:3793` (3793~3811)
- **설명**: [2026-08-01 신설] 이 raceKey 로 스냅샷이 존재하는 **날짜 목록**(최신순). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_snapshot_metas_for_race`(3814) · `snapshot_compare_api`(4081)

### `_snapshot_metas_for_race(rk, date)`
- **위치**: `app.py:3814` (3814~3850)
- **설명**: [비교분석] 이 raceKey 로 저장된 스냅샷 메타를 단계(trigger)별로 수집 → {'T-10':meta, 'T-2':meta, 'close':meta}. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_snapshot_build_compare`(3868) · `_apply_result_learning`(20801)

### `_snapshot_compare_path(rk, date)`
- **위치**: `app.py:3853` (3853~3865)
- **설명**: 🔴 [2026-08-01] 비교분석 저장 경로 — **파일명에 날짜를 넣는다**(원인 ⓑ). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_snapshot_build_compare`(3868) · `snapshot_compare_api`(4081)

### `_snapshot_build_compare(rk, date)`
- **위치**: `app.py:3868` (3868~3925)
- **설명**: [비교분석 자동 저장] 3단계(T-10/T-2/마감후) 스냅샷이 모이면 배당·추천 변화량을 산출·저장. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `snapshot_save`(3994) · `snapshot_compare_api`(4081) · `_apply_result_learning`(20801)

### `_pack(meta)`
- **위치**: `app.py:3885` (3885~3893)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_snapshot_build_compare`(3868)

### `_hit(meta)`
- **위치**: `app.py:3944` (3944~3951)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `_snapshot_timing_learn`(3928) · `_pace_log_record`(10142) · `_timeline_snap_apply_result`(36727)

### `_un(combo)`
- **위치**: `app.py:4300` (4300~4302)
- **설명**: 순서무관 정렬 튜플 키. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_odds_map_un`(4305)

### `_odds_map_un(arr)`
- **위치**: `app.py:4305` (4305~4315)
- **설명**: [{combo,odds}] → {정렬튜플: 최저배당}. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 8)**: `_baseline_reset_needed`(1760) · `_as_qmap`(1778) · `_curq_shrink_guard`(2363) · `_advanced_anomaly`(6304) · `_triple_analyze`(10724) · `_history_append`(15465) … [+2]

### `_win_map_clean(w)`
- **위치**: `app.py:4318` (4318~4335)
- **설명**: 단승 배당 입력({마번:배당} dict 또는 [{no,win}] list) → {마번(str): float} 정규화. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `triple_ingest`(2473) · `_history_append`(15465)

### `_win_map_int(w)`
- **위치**: `app.py:4338` (4338~4346)
- **설명**: {마번(str):배당} → {마번(int):배당}. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_triple_analyze`(10724) · `_history_append`(15465)

### `_odds_map_dir(arr)`
- **위치**: `app.py:4349` (4349~4360)
- **설명**: [{combo,odds}] → {(선,후): 배당} (순서있음, 2마리만). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_exacta_map_any`(5834) · `_triple_analyze`(10724)

### `_form_adj(total)`
- **위치**: `app.py:4380` (4380~4392)
- **설명**: 전적 총점 → 제거 가중치. 전적 없으면 0(중립). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_elimination`(4628)

### `_jockey_place_rate(name)`
- **위치**: `app.py:4400` (4400~4419)
- **설명**: 기수 복승권율(%) — static/data/jockeys.json(KRA 실데이터, mtime 캐시). 없으면 None. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `_elimination`(4628) · `_compare_recommend`(7334) · `_build_ai_training`(17176)

### `_dist_bucket(d)`
- **위치**: `app.py:4447` (4447~4456)
- **설명**: 거리 → 버킷 라벨(경주장·거리별 복승률 키). 짧은/중간/긴 거리 3구간 + 정확거리 병행. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_jp_jockeys_accumulate`(4459) · `_jp_jockey_rate_ctx`(4523)

### `_jp_jockeys_accumulate(details)`
- **위치**: `app.py:4459` (4459~4489)
- **설명**: oddspark 전적(details={lineageNb: past[]})의 (기수·착순)을 기수별 누적(중복 시그니처 방지·최근 30경주 유지). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `keiba_starters`(26619) · `_keiba_autocollect_form`(26684) · `_nar_autocollect_form`(34079)

### `_place_rate_of(placings, min_n)`
- **위치**: `app.py:4492` (4492~4497)
- **설명**: 착순 리스트 → 복승권율(%)=1~3착 비율. 표본 min_n 미만이면 None(불충분). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_jp_jockey_place_rate`(4500) · `_jp_jockey_rate_ctx`(4523)

### `_jp_jockey_place_rate(name)`
- **위치**: `app.py:4500` (4500~4505)
- **설명**: 일본 기수 복승권율(%) = 최근 30경주 중 1~3착 비율. 표본 5경주 미만이면 None(불충분·중립 처리). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_jockey_place_rate`(4400)

### `_jockey_rate_bonus(ctx)`
- **위치**: `app.py:4508` (4508~4520)
- **설명**: [기수 복승률 가감점] 복승률 30%+ → 유력 가점 +10 · 10% 이하 → 제거 감점 -10 · 그 외 0. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_keiba_build_form`(26514) · `_jra_build_form`(30967)

### `_jp_jockey_rate_ctx(name, cur_dist, venue)`
- **위치**: `app.py:4523` (4523~4537)
- **설명**: [강화] 기수 복승률을 거리·경주장 맥락으로 반환. {overall, dist, venue} (%·표본 부족은 None). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_keiba_build_form`(26514) · `_jra_build_form`(30967)

### `_placings_list(placings)`
- **위치**: `app.py:4540` (4540~4541)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `_avg_placing`(4544) · `_prob_ev`(4549) · `_build_ai_training`(17176)

### `_avg_placing(placings)`
- **위치**: `app.py:4544` (4544~4546)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_elimination`(4628)

### `_prob_ev(o, placings)`
- **위치**: `app.py:4549` (4549~4565)
- **설명**: [배당 우선 전환] 시장확률=1/배당×0.75 · 전적확률=최근입상수/경주수 · 통합=시장0.7+전적0.3 · 기대값=통합×배당-1. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_elimination`(4628)

### `_elimination(curQ, curD, exa, drops, form, trio_map, curWin)`
- **위치**: `app.py:4628` (4628~4836)
- **설명**: 배당+전적 복합 제거 판정. 반환: {horses:[...], counts, autoBets}. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_time_based_drop_signals(rk)`
- **위치**: `app.py:4856` (4856~4904)
- **설명**: 경주 히스토리(발주전분 기록)로 시간대별 마감 임박 급락 신호 생성. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)  ⚠ 위험목록 함수 → 전수 나열

### `_ref(minutes)`
- **위치**: `app.py:4872` (4872~4875)
- **설명**: minutes분 이상 전 스냅샷 중 minutes에 가장 가까운 것(없으면 None) *(주석(본문))*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_time_based_drop_signals`(4856)

### `_pattern_timing_bucket(mb)`
- **위치**: `app.py:4944` (4944~4951)
- **설명**: 급락 발생 시점을 T-N분 버킷으로. (발주전 분) *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_drop_timing_from_timeline`(18475) · `_apply_result_learning`(20801)

### `_extract_patterns(drops, reversals, signals, curQ, bet_rec, advanced)`
- **위치**: `app.py:4954` (4954~4976)
- **설명**: [1·5번] 분석 결과에서 이상감지 패턴 태그 추출. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_scale_alloc(bet_rec, label, target)`
- **위치**: `app.py:4991` (4991~5002)
- **설명**: [4번] 특정 베팅(label)의 alloc 을 target% 로 두고 나머지를 비례 재분배(합 100 유지). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_adjust_bet_weights`(5005)

### `_adjust_bet_weights(bet_rec, conf)`
- **위치**: `app.py:5005` (5005~5016)
- **설명**: [4번] 패턴 신뢰도로 복승/삼복승 비중 자동 조정. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_mass_drop_detect(drops, curQ, minutes_before)`
- **위치**: `app.py:5022` (5022~5057)
- **설명**: 전체 복승 조합의 35%+ 또는 30개+ 가 동시에 30%+ 급락하면 '대규모 급락'. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_excess_drop_analysis(drops, curQ)`
- **위치**: `app.py:5096` (5096~5143)
- **설명**: [1번] 초과 급락률 — 시장 전체 평균 급락 대비 각 말의 집중도. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_triple_analyze`(10724) · `_history_append`(15465)

### `_signal_situation(drops, mass_drop, excess)`
- **위치**: `app.py:5153` (5153~5169)
- **설명**: [3번] 상황별 가중치 자동 조정. 반환 {name, formW, signalW, signalSource, note}. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_integrated_adaptive(form, curQ, curD, excess, situation)`
- **위치**: `app.py:5172` (5172~5207)
- **설명**: [3번] 상황별 가중치로 통합 등급 재산출(기존 _integrated_grades 40/60은 그대로 두고 별도 추가). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_near_miss_frequent(min_count)`
- **위치**: `app.py:5339` (5339~5349)
- **설명**: 4착 빈번(min_count회+) 말 이름 집합 → 다음 경주 삼복승 보험픽 우선 고려. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_pattern_status(fired, rate)`
- **위치**: `app.py:5417` (5417~5426)
- **설명**: 표본수·적중률 → 신뢰도 상태. 반환 (status, icon, note). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_pattern_confidence_view`(5429)

### `_signal_type_hitrates()`
- **위치**: `app.py:5465` (5465~5485)
- **설명**: 유형별 발생/적중/적중률 집계 → 가중치 상향 근거. 판정불가(None)는 분모 제외. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `signal_types_stats`(3575) · `_pattern_confidence_view`(5429) · `_triple_analyze`(10724)

### `_compression_hitrate()`
- **위치**: `app.py:5532` (5532~5550)
- **설명**: 압축 패턴 복승 적중률(강력/중간·급락결합 별) 집계. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `compression_stats`(3585) · `_pattern_confidence_view`(5429)

### `agg(rows)`
- **위치**: `app.py:5536` (5536~5542)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_compression_hitrate`(5532) · `shadow_trifecta_ep`(20227)

### `_high_odds_companion_hitrate()`
- **위치**: `app.py:5599` (5599~5608)
- **설명**: 고배당 1착 경주 중 3착 내 다른 고배당 포함 비율. 반환 {races,hits,rate,reliable}. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `high_odds_companion_stats`(3647) · `_pattern_confidence_view`(5429) · `_triple_analyze`(10724)

### `_tp_reason_bucket(reason)`
- **위치**: `app.py:5627` (5627~5638)
- **설명**: 근거 문자열 → 신호 유형 버킷(급락/역배열/전적/연속하락/이상감지). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_learn_third_place`(5641)

### `_third_place_hitrate()`
- **위치**: `app.py:5670` (5670~5690)
- **설명**: 신호 유형별 3착 후보의 입상(1~3착)·정확3착 적중률 집계. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `third_place_stats`(3594)

### `_deadline_phase_label(mb, after_close)`
- **위치**: `app.py:5726` (5726~5734)
- **설명**: 이상감지 신호의 유효 시점 라벨. 마감 후=회색 표시용. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_signal_combo_bets(signal_horses, curQ, bet_rec, cap)`
- **위치**: `app.py:5737` (5737~5769)
- **설명**: [신호 조합] 이상감지 신호가 있는 말들의 모든 복승 조합을 추천에 추가(고배당 포함). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_combo_signal_quality(combo, excess)`
- **위치**: `app.py:5772` (5772~5788)
- **설명**: [4번] 추천 조합의 신호 품질(상/중/하) + 근거. 조합 내 최대 초과급락 말 기준. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_reversal_level(ratio)`
- **위치**: `app.py:5792` (5792~5804)
- **설명**: [강화] 역전비율(<1)→등급. 배당 차이(=1-ratio) 기준으로 오탐 제거 + 4단계 강도. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_win_exacta_reversal`(5901) · `_mk`(5914)

### `_inversion_tier(diff_pct)`
- **위치**: `app.py:5807` (5807~5818)
- **설명**: [역배열 강도] 배당 차이 %(양수) → 등급. 10% 미만 신호없음(None). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_rank_inversion_detail`(6025)

### `_reversal_role(diff_pct, confirmed)`
- **위치**: `app.py:5821` (5821~5831)
- **설명**: [역배열 강도별 축 역할·사용자 요청] 배당 차이%(양수) + 지속성(2회+ 확정) → 역할. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_reversal_axis_roles`(5865)

### `_exacta_map_any(x)`
- **위치**: `app.py:5834` (5834~5847)
- **설명**: exacta(리스트 [{combo,odds}] 또는 딕셔너리 {'1>2':배당·'1-2'·'1→2'}) → {(선,후):배당} 방향 맵. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_reversal_persistence`(5850)

### `_reversal_axis_roles(wx_reversals, hist, fav_rank, curWin, curQ)`
- **위치**: `app.py:5865` (5865~5898)
- **설명**: [역배열 강도별 처리] challenger별 역할(축/보조/보험) + 지속성(확정/후보) 산출. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_rep(h)`
- **위치**: `app.py:5872` (5872~5879)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 7)**: `_reversal_axis_roles`(5865) · `_flow_scores`(7803) · `_mid_high_odds_favorites`(7919) · `_core_picks`(8038) · `_confidence_picks`(8085) · `_dense_no_signal_box`(8208) … [+1]

### `_win_exacta_reversal(fav_rank, curD, max_rank)`
- **위치**: `app.py:5901` (5901~5953)
- **설명**: [1번] 쌍승 역전 감지 공식. 단승 유력마 A vs 다른 말 B 방향 비교. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_reversal_persistence`(5850) · `_triple_analyze`(10724)

### `_mk(a, b, ai, bi, multi)`
- **위치**: `app.py:5914` (5914~5933)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 21)**: `_win_exacta_reversal`(5901) · `check_snapshot_coverage`(114) · `check_save_failures_fixed`(198) · `check_save_failures_unfixed`(213) · `check_schema_contract`(226) · `check_schema_drift`(282) … [+15]

### `_quinella_mismatch(fav_rank, curQ)`
- **위치**: `app.py:5956` (5956~5980)
- **설명**: [2번] 복승 불일치 감지 공식. 단승 1+2위 예상 조합 vs 실제 최저복승. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_best_per_horse(m)`
- **위치**: `app.py:6030` (6030~6039)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_rank_inversion_detail`(6025)

### `_best_first_place(m)`
- **위치**: `app.py:6041` (6041~6052)
- **설명**: 쌍승(방향성) 전용: 각 말이 **1착**일 때의 최저 배당(=시장이 그 말을 승자로 미는 정도). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_rank_inversion_detail`(6025)

### `_inverse_arrangement(fav_rank, has_win, curWin, curQ, wx_reversals, quin_mismatch, excess, form)`
- **위치**: `app.py:6109` (6109~6256)
- **설명**: [역배열 감지] 진짜 역배열 = 시장 인기 순위(배당 낮은 순) ≠ 쌍승 배당 순위가 역전될 때만. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_add_inv(h)`
- **위치**: `app.py:6119` (6119~6121)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_inverse_arrangement`(6109)

### `_advanced_anomaly(hist, curQ, drops)`
- **위치**: `app.py:6304` (6304~6472)
- **설명**: [2번 고도화] 급락 속도·연속하락/단발반등·페이크베팅·복승 환급률(역수합) 감지. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_adj(combo, pts)`
- **위치**: `app.py:6313` (6313~6315)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_advanced_anomaly`(6304)

### `_horse_series(no)`
- **위치**: `app.py:6408` (6408~6424)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_advanced_anomaly`(6304)

### `_smart_money_combos(hist, curQ, min_rise, min_drop)`
- **위치**: `app.py:6475` (6475~6517)
- **설명**: [유형8] 상승 후 급락(스마트머니) — 조합 배당이 초반에 상승(인기 이탈)했다가 *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_strong_signals`(6560)

### `_drop_time_weight(mb)`
- **위치**: `app.py:6529` (6529~6545)
- **설명**: [작업2·급락 시간대 가중치] 발주 전 분(mb) → 급락 신뢰도 배수. 학습 데이터 근거: *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_drop_time_tag`(6548) · `_strong_signals`(6560)

### `_drop_time_tag(mb)`
- **위치**: `app.py:6548` (6548~6557)
- **설명**: [작업2] 시간대 강조 라벨. T-1~3분 급락은 '⚡⚡ 최강 신호'로 강조 표시. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_strong_signals`(6560)

### `_strong_signals(advanced, inverse, drops, cur_mb, hist, curQ, after_close)`
- **위치**: `app.py:6560` (6560~6664)
- **설명**: 8유형 강신호 산출(무삭제 — 표시/학습용 파생 뷰). 기존 advanced·inverse·drops·history만 소비. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `add(t, horses, detail, level)`
- **위치**: `app.py:6567` (6567~6570)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_strong_signals`(6560)

### `_horse_repr_odds(h, curWin, curQ)`
- **위치**: `app.py:6668` (6668~6680)
- **설명**: 말 대표 배당 = 단승 우선, 없으면 그 말 포함 최저 복승. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 6)**: `_compression_pattern`(6683) · `_third_place_hunt`(6747) · `_repr_odds_from_rec`(7014) · `_high_odds_companion`(7036) · `consider`(6784) · `repr_odds`(7045)

### `_compression_pattern(key_horses, curWin, curQ, strong_signals, sport)`
- **위치**: `app.py:6683` (6683~6737)
- **설명**: [1·2번] 유력마 TOP3 중 저배당 말이 몰린 축 패턴 감지. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_third_place_hunt(compression_pattern, strong_signals, anomaly_horse, wx_reversals, form, curWin, curQ, drops, valid_nos)`
- **위치**: `app.py:6747` (6747~6823)
- **설명**: [배당 3착 자동 발굴] 압축 축 2두 + 고배당 3착 후보 → 삼복승 보험 편성. 추정배당은 호출부에서 채움. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `horses_of(*types)`
- **위치**: `app.py:6760` (6760~6766)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_third_place_hunt`(6747)

### `consider(no, priority, reason)`
- **위치**: `app.py:6784` (6784~6800)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_third_place_hunt`(6747)

### `_reversal_backing_bets(inverse, key_horses, curQ, after_close)`
- **위치**: `app.py:6832` (6832~6862)
- **설명**: [히로시마 2R 학습] 역배열이 지목한 '실질 유력마'(invLead: 인기 낮은데 조합배당 최저 = 고배당인데 *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_forced_trifecta_insurance(cur_mb, after_close, drops, wx_reversals, inverse, strong_signals, key_horses, drop_thresh)`
- **위치**: `app.py:6865` (6865~6918)
- **설명**: T-2분 이내 급락(강급락 pct<=drop_thresh)과 역배열을 동시에 보인 말을 찾아 *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_closing_drop_insurance(cur_mb, drops, key_horses, valid_nos, mass_drop, drop_thresh)`
- **위치**: `app.py:6921` (6921~6967)
- **설명**: [마감 순간 급락 삼복승 보험] T-0(마감) 전후 급락 말 → 축(유력마 상위2) + 급락말 삼복승 보험 자동 편성. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_is_pass_race(an)`
- **위치**: `app.py:6971` (6971~6986)
- **설명**: 패스 권고 경주 판정: ①추천 게이트(신호 대기·패스형) 또는 ②전적없음+강신호없음. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_apply_result_learning`(20801)

### `_recommend_flex(strong_signals, signal_confidence, after_close)`
- **위치**: `app.py:6990` (6990~7010)
- **설명**: 신호 강도별 추천 말 수 가이드(강제 아님·안내). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_repr_odds_from_rec(rec)`
- **위치**: `app.py:7014` (7014~7033)
- **설명**: rec(단승/복승)에서 말별 대표배당 맵 + curWin/curQ 재구성(학습·분석 공용). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_learn_high_odds_companion`(5570)

### `_high_odds_companion(drops, wx_reversals, inverse, curWin, curQ, key_horses, learned, after_close, high_thresh)`
- **위치**: `app.py:7036` (7036~7116)
- **설명**: [참고 추천] 고배당(7배+) 신호말 감지 시 그 말과 복승 배당 낮게 묶인 다른 고배당 말 탐색. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `repr_odds(h)`
- **위치**: `app.py:7045` (7045~7046)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_high_odds_companion`(7036)

### `_capital_preservation(combos)`
- **위치**: `app.py:7120` (7120~7148)
- **설명**: [2번] 원금 보전 자동 계산(예산 무관 '비율' 산출 → 프론트가 예산 곱함). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_bmed_strategy`(7254)

### `_expected_return(plan, curQ)`
- **위치**: `app.py:7151` (7151~7162)
- **설명**: [3번] 기대 환수율 = Σ(적중확률_i × 회수비율_i). 확률=시장 복승 역수(1/o) 정규화(전체 조합). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_bmed_insurance`(7165) · `_bmed_strategy`(7254)

### `_bmed_insurance(key_horses, curQ, signal_confidence, inverse, sport)`
- **위치**: `app.py:7165` (7165~7251)
- **설명**: [보험용 추천] BMED 보험형 매트릭스 — 1착 유력마 축 + 2·3·4위 상대 3조합. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_bmed_strategy`(7254)

### `_median(vals)`
- **위치**: `app.py:7232` (7232~7236)
- **설명**: [보완#2 BUG A] 중간값 = 정확한 중앙값. 조합 2개(짝수)일 때 sorted[n//2]는 최댓값과 같아져 *(주석(def 위))*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_bmed_insurance`(7165)

### `_co(pairs)`
- **위치**: `app.py:7268` (7268~7275)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_bmed_strategy`(7254)

### `_pairs(hs)`
- **위치**: `app.py:7277` (7277~7279)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_bmed_strategy`(7254)

### `_compare_recommend(form, key_horses, excess, drops, bet_rec)`
- **위치**: `app.py:7334` (7334~7362)
- **설명**: [1번] 이상감지 기반 / 전적 기반 / 최종 추천 조합을 각각 산출(복승 2두·삼복승 3두). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `pt(order)`
- **위치**: `app.py:7338` (7338~7341)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_compare_recommend`(7334)

### `_recommend_basis(top_horses, form, elimination, drops, wx_reversals, advanced, signal_confidence, basis_weights)`
- **위치**: `app.py:7365` (7365~7418)
- **설명**: [추천 근거 상세 카드] 상위 추천마(최대 3두)별 근거: 전적·배당·기수·종합확신도. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_g(m, no)`
- **위치**: `app.py:7377` (7377~7378)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_recommend_basis`(7365) · `_simulate_race_flow_jp`(31608)

### `_bet_judgment(conf_engine, excess, advanced, wx_reversals, form, mass_drop, after_close, signal_ready)`
- **위치**: `app.py:7502` (7502~7589)
- **설명**: [개편·실전] 경주 유형 자동 판정 + 배팅 배분 비율. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_stage_guide(cur_mb, after_close, judgment, inverse, advanced)`
- **위치**: `app.py:7592` (7592~7617)
- **설명**: [3번] 발주 전 시점(T-3/T-2/T-1/T-30초)별 단계 추천 + 최종 등급. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_elimination_strong(elimination, form, drops)`
- **위치**: `app.py:7620` (7620~7653)
- **설명**: [5번] 과감한 제거마 목록(기존 _elimination 판정 유지, 강화 파생만). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_chaotic_race(curQ, curWin, key_horses, anomaly_horse, drops)`
- **위치**: `app.py:7656` (7656~7774)
- **설명**: [혼전 경주] 상위 배당이 근접해 이변 가능성이 큰 경주를 감지하고 고배당 포함 삼복승 전략을 편성. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_q(a, b)`
- **위치**: `app.py:7714` (7714~7715)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 6)**: `_chaotic_race`(7656) · `_confidence_picks`(8085) · `_triple_analyze`(10724) · `_trio_est`(7717) · `_add`(8142) · `_trio_est`(11771)

### `_trio_est(cc)`
- **위치**: `app.py:7717` (7717~7730)
- **설명**: 삼복승 실배당 미수집 → 구성 복승 3쌍 기하평균×2 ÷보정계수 추정. (odds, is_estimate). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 6)**: `_chaotic_race`(7656) · `_dense_no_signal_box`(8208) · `_triple_analyze`(10724) · `_mk_trio`(7732) · `_tri_odds`(12289) · `_edtri_odds`(12317)

### `_mk_trio(label, cc, alloc, high_return)`
- **위치**: `app.py:7732` (7732~7741)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_chaotic_race`(7656)

### `_horse_rep_series(hist, no, n)`
- **위치**: `app.py:7780` (7780~7800)
- **설명**: 최근 n스냅샷의 말별 대표배당(단승 우선·없으면 최저 복승) 시퀀스. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_flow_scores`(7803)

### `_rep(no)`
- **위치**: `app.py:7828` (7828~7835)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 7)**: `_reversal_axis_roles`(5865) · `_flow_scores`(7803) · `_mid_high_odds_favorites`(7919) · `_core_picks`(8038) · `_confidence_picks`(8085) · `_dense_no_signal_box`(8208) … [+1]

### `_flow_removal(flow, key_horses, keep_low_odds)`
- **위치**: `app.py:7883` (7883~7899)
- **설명**: [1번] 흐름 기반 제거 대상: 죽은인기/3연속상승/페이크/역배열반대. 유력마 축(상위2)은 제외(안전). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_high_odds_candidates(flow, removed_nos, key_horses, min_odds)`
- **위치**: `app.py:7902` (7902~7916)
- **설명**: [2번] 제거 후 남은 말 중 배당 10배+ 인데 하락/급락(흐름 좋음) → 고배당 후보 자동 편입. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_mid_high_odds_favorites(valid_nos, curWin, curQ, drops, dark_horses, reversal_roles, form, min_odds)`
- **위치**: `app.py:7919` (7919~7968)
- **설명**: [중고배당 유력마·1번] 복승 10배+ 인데 강한 신호 1개+ 보유 → 💎 중고배당 유력마. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_rep(no)`
- **위치**: `app.py:7923` (7923~7930)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 7)**: `_reversal_axis_roles`(5865) · `_flow_scores`(7803) · `_mid_high_odds_favorites`(7919) · `_core_picks`(8038) · `_confidence_picks`(8085) · `_dense_no_signal_box`(8208) … [+1]

### `_cross_reversal(curQ, curD, fav_rank, valid_nos, max_rank)`
- **위치**: `app.py:7971` (7971~8035)
- **설명**: [복승 크로스 역배열·전체 말 쌍 비교] 각 말 X에 대해 인기 상위쌍(A인기>B, 상위 max_rank위) 조합에서 *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_w(ra, rb)`
- **위치**: `app.py:7982` (7982~7987)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_cross_reversal`(7971)

### `_qo(a, b)`
- **위치**: `app.py:7989` (7989~7990)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 6)**: `_cross_reversal`(7971) · `_scenario_plan`(8276) · `_final_picks`(8348) · `_triple_analyze`(10724) · `_qi`(9272) · `_tri_est`(9289)

### `_xo(a, b)`
- **위치**: `app.py:7992` (7992~7993)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_cross_reversal`(7971)

### `_rep(no)`
- **위치**: `app.py:8042` (8042~8049)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 7)**: `_reversal_axis_roles`(5865) · `_flow_scores`(7803) · `_mid_high_odds_favorites`(7919) · `_core_picks`(8038) · `_confidence_picks`(8085) · `_dense_no_signal_box`(8208) … [+1]

### `_conf(no)`
- **위치**: `app.py:8097` (8097~8098)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_confidence_picks`(8085)

### `_rep(no)`
- **위치**: `app.py:8100` (8100~8107)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 7)**: `_reversal_axis_roles`(5865) · `_flow_scores`(7803) · `_mid_high_odds_favorites`(7919) · `_core_picks`(8038) · `_confidence_picks`(8085) · `_dense_no_signal_box`(8208) … [+1]

### `_high(no)`
- **위치**: `app.py:8109` (8109~8111)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_confidence_picks`(8085) · `_add`(8142)

### `_q(a, b)`
- **위치**: `app.py:8113` (8113~8116)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 6)**: `_chaotic_race`(7656) · `_confidence_picks`(8085) · `_triple_analyze`(10724) · `_trio_est`(7717) · `_add`(8142) · `_trio_est`(11771)

### `_add(a, b, reason)`
- **위치**: `app.py:8142` (8142~8154)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_confidence_picks`(8085) · `_rec_combos_from_analysis_log`(20597)

### `_dense_no_signal_box(key_horses, valid_nos, curWin, curQ, trio_map, drops, wx_reversals, dark_horses, strong_signals, dense_odds, strong_drop)`
- **위치**: `app.py:8208` (8208~8254)
- **설명**: [무신호 저배당밀집] 급락(≤-30%)·쌍승역전·복병·강신호가 전부 0건이고 상위 배당이 밀집(대표배당 15배 이내)이면, *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_rep(no)`
- **위치**: `app.py:8221` (8221~8228)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 7)**: `_reversal_axis_roles`(5865) · `_flow_scores`(7803) · `_mid_high_odds_favorites`(7919) · `_core_picks`(8038) · `_confidence_picks`(8085) · `_dense_no_signal_box`(8208) … [+1]

### `_quinella_target(n_horses, chaotic)`
- **위치**: `app.py:8257` (8257~8273)
- **설명**: [근거 기반 추천·두수별 복승 개수] 출전 두수별 복승 최대 개수 + 혼전 시 +2(상한 8). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_scenario_plan(cp, curQ, pace_analysis, sig_meta, valid_nos, axis_plan)`
- **위치**: `app.py:8276` (8276~8322)
- **설명**: [시나리오 조합 자동 생성] 전적·각질·편성·배당 신호 종합 → 2가지 시나리오. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_final_picks`(8348)

### `_sc(n)`
- **위치**: `app.py:8287` (8287~8288)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_scenario_plan`(8276)

### `_qo(a, b)`
- **위치**: `app.py:8290` (8290~8293)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 6)**: `_cross_reversal`(7971) · `_scenario_plan`(8276) · `_final_picks`(8348) · `_triple_analyze`(10724) · `_qi`(9272) · `_tri_est`(9289)

### `_vpair(cc)`
- **위치**: `app.py:8362` (8362~8364)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_final_picks`(8348)

### `_vtri(cc)`
- **위치**: `app.py:8366` (8366~8368)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_final_picks`(8348)

### `_has_sig(combo)`
- **위치**: `app.py:8370` (8370~8371)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_final_picks`(8348) · `_combo_has_signal`(8519)

### `_mh(no)`
- **위치**: `app.py:8495` (8495~8496)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 7)**: `_final_picks`(8348) · `_combo_basis`(8498) · `_combo_score`(8516) · `_combo_has_signal`(8519) · `_combo_strong`(8525) · `_combo_has_signal_relaxed`(8541) … [+1]

### `_combo_basis(combo)`
- **위치**: `app.py:8498` (8498~8514)
- **설명**: 조합 근거 문장 리스트 — 급락%·스마트머니·역배열·복병·집중급락(말별). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_final_picks`(8348) · `_force_pair`(8784)

### `_combo_has_signal(combo)`
- **위치**: `app.py:8519` (8519~8523)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_final_picks`(8348)

### `_combo_strong(combo)`
- **위치**: `app.py:8525` (8525~8539)
- **설명**: BMED 특별 강신호: signalScore 70+ OR 급락15%+ OR (스마트머니+역배열 동시). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_final_picks`(8348)

### `_combo_has_signal_relaxed(combo)`
- **위치**: `app.py:8541` (8541~8553)
- **설명**: [메인 보완·완화] 급락 10%+ OR signalScore 50+ OR 기존 신호(drop/rev/smart/anomaly). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_final_picks`(8348)

### `_force_pair(_pa, _pb, _reason, _summary)`
- **위치**: `app.py:8784` (8784~8806)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_final_picks`(8348)

### `_prio_c(_q)`
- **위치**: `app.py:8945` (8945~8951)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_final_picks`(8348)

### `_qo(a, b)`
- **위치**: `app.py:9269` (9269~9270)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 6)**: `_cross_reversal`(7971) · `_scenario_plan`(8276) · `_final_picks`(8348) · `_triple_analyze`(10724) · `_qi`(9272) · `_tri_est`(9289)

### `_qi(a, b, label, rank)`
- **위치**: `app.py:9272` (9272~9275)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_final_picks`(8348)

### `_tri_est(a, b, c)`
- **위치**: `app.py:9289` (9289~9299)
- **설명**: 삼복승 축1+축2+연결 — 실배당 미수집이라 구성 복승 3쌍 기하평균×2 추정(표시용) *(주석(def 위))*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_final_picks`(8348)

### `_qod(a, b)`
- **위치**: `app.py:9323` (9323~9324)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_final_picks`(8348) · `_qitem`(9369)

### `_qitem(a, b, label)`
- **위치**: `app.py:9369` (9369~9373)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_final_picks`(8348)

### `_ev_band_key(o)`
- **위치**: `app.py:9741` (9741~9745)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_ev_band_p`(9751) · `_ev_bands_update`(9772)

### `_ev_band_p(o)`
- **위치**: `app.py:9751` (9751~9769)
- **설명**: 배당대별 적중률 추정 — 학습 표본 50+ 밴드는 실측치(자동 교체), 아니면 보수 가정치. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_apply_profit_strategy`(9809)

### `_ev_bands_update(combo_odds_list, top2)`
- **위치**: `app.py:9772` (9772~9792)
- **설명**: [기대값 학습] 판정된 라이브 복승 추천의 (배당대, 적중) 누적 → 표본 50+부터 가정치 자동 교체. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_apply_result_learning`(20801)

### `_profit_tier_of(sport, category, min_q)`
- **위치**: `app.py:9795` (9795~9806)
- **설명**: 경주 3분류: low(삼복승 집중)/mid/high — 종목별 경계(경륜류 1.8/5 · 일본경마 2/8 · 한국경마 3/10). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_apply_profit_strategy`(9809)

### `_demote(_q, _why)`
- **위치**: `app.py:9834` (9834~9838)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_apply_profit_strategy`(9809)

### `_conn_pair_odds(_c)`
- **위치**: `app.py:9997` (9997~10004)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 0)**: —

### `_hit(cs)`
- **위치**: `app.py:10161` (10161~10162)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `_snapshot_timing_learn`(3928) · `_pace_log_record`(10142) · `_timeline_snap_apply_result`(36727)

### `_dark_sport_of(rk, an)`
- **위치**: `app.py:10224` (10224~10241)
- **설명**: raceKey/분석에서 종목 라벨(keirin/keiba/korea/boat/bike). category 우선. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_dark_log_record`(10370)

### `_dark_odds_band(odds)`
- **위치**: `app.py:10244` (10244~10254)
- **설명**: 복병 배당 구간 라벨(15~30/30~50/50~100/100+). 15배 미만은 None(복병 아님). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_dark_stats_recompute`(10493)

### `_norm_sig_key(s)`
- **위치**: `app.py:10257` (10257~10269)
- **설명**: [신호 라벨 정규화] 이중표기('크로스역배열 크로스역배열')·이모지·인접중복을 단일 라벨로 축약 → 통계 키 통일. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_dark_signal_of`(10272) · `_dark_stats_recompute`(10493)

### `_dark_signal_of(no, an)`
- **위치**: `app.py:10272` (10272~10304)
- **설명**: [복병 신호 우선순위] 말 no의 복병 신호 판정 → (priority, label). priority 낮을수록 강함(1>2>3). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_dark_rank_horses`(10307) · `_analyze_missed_dark_horse`(10460)

### `_dark_combo_section(key_horses, dark_horses, curQ)`
- **위치**: `app.py:10331` (10331~10362)
- **설명**: [복병 복승 자동편성] 유력마 1위 + 복병 1·2순위 → 복승 2조합 + 삼복승 1조합(별도 섹션). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_od(a, b)`
- **위치**: `app.py:10352` (10352~10353)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_dark_combo_section`(10331)

### `_dark_log_path(rk)`
- **위치**: `app.py:10365` (10365~10367)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_dark_log_record`(10370) · `_dark_log_apply_result`(10403)

### `_dark_log_apply_result(rk, top3, an, quinella_odds)`
- **위치**: `app.py:10403` (10403~10457)
- **설명**: [결과 시점 분석] 복병 rank1~3 입상 판정 + 미감지 사후분석 → dark_horse_log 갱신 + 통계 재계산. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_apply_result_learning`(20801) · `dark_horse_review`(22611)

### `_place(no)`
- **위치**: `app.py:10423` (10423~10427)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_dark_log_apply_result`(10403)

### `_rate(d)`
- **위치**: `app.py:10569` (10569~10571)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 4)**: `_dark_stats_recompute`(10493) · `_threshold_candidates`(17466) · `_recompute_learning_stats`(19031) · `_form_ext_build`(31933)

### `_dark_case_similar(dh)`
- **위치**: `app.py:10596` (10596~10609)
- **설명**: 현재 복병(dh)과 유사한 과거 복병 고배당 '적중' 사례 수(등급 이상·배당대 근접). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_rep_o_dark(h)`
- **위치**: `app.py:11169` (11169~11176)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_mkt_repr(h)`
- **위치**: `app.py:11259` (11259~11264)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_rep_o(h)`
- **위치**: `app.py:11341` (11341~11348)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_rt_rep(h)`
- **위치**: `app.py:11368` (11368~11375)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_dark_repr2(h)`
- **위치**: `app.py:11461` (11461~11468)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_q(a, b)`
- **위치**: `app.py:11553` (11553~11554)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 6)**: `_chaotic_race`(7656) · `_confidence_picks`(8085) · `_triple_analyze`(10724) · `_trio_est`(7717) · `_add`(8142) · `_trio_est`(11771)

### `_addbet(kind, label, combo, alloc, odds)`
- **위치**: `app.py:11558` (11558~11567)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_trio_est(cc)`
- **위치**: `app.py:11771` (11771~11783)
- **설명**: [3번] 삼복승 실배당 미수집 시: 구성 복승 3쌍의 기하평균×2 로 추정(라벨=추정) *(주석(def 위))*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 6)**: `_chaotic_race`(7656) · `_dense_no_signal_box`(8208) · `_triple_analyze`(10724) · `_mk_trio`(7732) · `_tri_odds`(12289) · `_edtri_odds`(12317)

### `_push_sig(h)`
- **위치**: `app.py:11825` (11825~11827)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_min_odds(arr)`
- **위치**: `app.py:11908` (11908~11910)
- **설명**: 시계열 차트: 복승·쌍승·삼복승 각 "최저(최인기) 배당"의 라운드별 변화 (3줄) *(주석(def 위))*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_drop_reason(pct)`
- **위치**: `app.py:11952` (11952~11959)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_tri_odds(cc)`
- **위치**: `app.py:12289` (12289~12299)
- **설명**: [보완②] 삼복승 예상배당 표기 — 실배당(trio_map) 우선, 없으면 _trio_est 추정. *(주석(def 위))*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_edtri_odds(cc)`
- **위치**: `app.py:12317` (12317~12324)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_qo(_x, _y)`
- **위치**: `app.py:12480` (12480~12481)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 6)**: `_cross_reversal`(7971) · `_scenario_plan`(8276) · `_final_picks`(8348) · `_triple_analyze`(10724) · `_qi`(9272) · `_tri_est`(9289)

### `_hscore(_no)`
- **위치**: `app.py:12513` (12513~12515)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_sm(_no)`
- **위치**: `app.py:12517` (12517~12519)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_in_roster(_c)`
- **위치**: `app.py:12710` (12710~12714)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_t5_combos(fq)`
- **위치**: `app.py:13607` (13607~13616)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_apply_t5_freeze`(13661)

### `_t5_mark_tiers(fq, frozen)`
- **위치**: `app.py:13630` (13630~13644)
- **설명**: 복승 목록에 등급 표식을 붙인다. **순서·개수는 바꾸지 않는다**(표시 계층 전용). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_apply_t5_freeze`(13661)

### `_t5_items(fq)`
- **위치**: `app.py:13647` (13647~13658)
- **설명**: 조합 → 원본 항목(배당·근거) 매핑. 복원 시 빈 껍데기가 아니라 원래 값을 되살리기 위함. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_apply_t5_freeze`(13661)

### `_apply_t5_freeze(rk, an)`
- **위치**: `app.py:13661` (13661~13781)
- **설명**: [T-5 확정 후 삭제 금지] mb<=5 첫 시점의 복승 집합을 확정하고 이후 삭제를 관측(또는 복원)한다. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_cycle_t2_strong_horses(an)`
- **위치**: `app.py:13796` (13796~13815)
- **설명**: [t2_strong_cycle 예외 편입 판정] 마감 2분(T-2) 이후 '급락 30%+' 또는 '집중급락' 신호에 걸린 말 집합. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_apply_rec_hysteresis`(13818)

### `_apply_rec_hysteresis(rk, an)`
- **위치**: `app.py:13818` (13818~13918)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_apply_tri_hysteresis(rk, an)`
- **위치**: `app.py:13928` (13928~13991)
- **설명**: ══════════ [② 삼복승 메인 히스테리시스 (2026-07-28)] 복승과 동일 규칙을 삼복승에 대칭 적용 ══════════ *(주석(def 위))*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_bet_market(rk, sport)`
- **위치**: `app.py:14063` (14063~14070)
- **설명**: raceKey·종목 → 베팅 규칙 시장 키(korea/cycle/japan). 한국이 최우선(경마장명이 확실한 판별자). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_apply_bet_rules`(14178)

### `_combo_odds_trend(rk, combos)`
- **위치**: `app.py:14110` (14110~14163)
- **설명**: {조합: {'first','last','low','surge','rebound'}} — triple_store history(최근 12틱) 기반. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_apply_bet_rules`(14178)

### `_apply_bet_rules(rk, an)`
- **위치**: `app.py:14178` (14178~14340)
- **설명**: 시장별 베팅 규칙 적용. 실패해도 원본 무변경(호출부에서 예외 흡수). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_rk_venue_num(k)`
- **위치**: `app.py:14439` (14439~14448)
- **설명**: raceKey/경주명에서 (경마장, 경주번호) 추출. 예:'2026-07-03 서울 5R' → ('서울',5). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `triple_match`(14481)

### `_is_japan_key(k)`
- **위치**: `app.py:14475` (14475~14477)
- **설명**: raceKey에 일본 경마장명이 들어있으면 True(한국 경주 매칭에서 제외용). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `triple_match`(14481)

### `_alert_meta(rk)`
- **위치**: `app.py:14537` (14537~14542)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `_record_alert`(14545) · `_match_alerts_to_result`(14586) · `alerts_get`(20410)

### `_match_alerts_to_result(rk, top3, an)`
- **위치**: `app.py:14586` (14586~14614)
- **설명**: [2번] 결과 입력 시 경고 내역과 자동 매칭 — 경고말 입상 여부·경고 무시(추천 누락) 판정. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_apply_result_learning`(20801)

### `_pub_deadline_left(mb)`
- **위치**: `app.py:14628` (14628~14635)
- **설명**: 발주 남은 분(float) → '8분 10초' 표기. 마감 후면 '마감'. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `public_matrix`(15085)

### `_pub_roles(an)`
- **위치**: `app.py:14654` (14654~14683)
- **설명**: 말별 역할(fav/cut/weakcut/dark) — 오버레이 matrixRoles 와 동일 규칙. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `public_matrix`(15085)

### `_pub_matrix(an, role)`
- **위치**: `app.py:14686` (14686~14756)
- **설명**: 복승 매트릭스 + 조합별 신호색. 키는 배당판 기준 '작은번호-큰번호'. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `public_matrix`(15085)

### `_pub_flow(an, no)`
- **위치**: `app.py:14759` (14759~14762)
- **설명**: 말별 배당흐름(flowScores) 조회. 키가 int/str 두 형태로 올 수 있어 둘 다 대응. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_pub_horse_why`(14771) · `_pub_horse_cards`(14997)

### `_pub_conf(an, no)`
- **위치**: `app.py:14765` (14765~14768)
- **설명**: 말별 확신도(confidence.horses) 조회. 키 int/str 양쪽 대응. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_pub_horse_why`(14771)

### `_pub_horse_why(no, an, role)`
- **위치**: `app.py:14771` (14771~14826)
- **설명**: 이 말이 왜 유력/복병/제거인지 — 쉬운 말 한 줄들(기존 신호 재사용·재계산 없음). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_pub_recommendation`(14829) · `_pub_horse_cards`(14997)

### `_pub_recommendation(an, role)`
- **위치**: `app.py:14829` (14829~14962)
- **설명**: 카드 2·3용 추천 + '왜 이 조합인지' 부연설명. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `public_matrix`(15085)

### `_qtxt(q)`
- **위치**: `app.py:14929` (14929~14931)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_pub_recommendation`(14829)

### `_atxt(q)`
- **위치**: `app.py:14945` (14945~14947)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_pub_recommendation`(14829)

### `_pub_pace(an)`
- **위치**: `app.py:14965` (14965~14994)
- **설명**: [7] 각질 편성 분석 공개용 — 편성 집계·페이스·유불리·주목마·시나리오. 없으면 None(패널 숨김). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `public_matrix`(15085)

### `_fmt(q)`
- **위치**: `app.py:14983` (14983~14985)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 5)**: `_pub_pace`(14965) · `kra_status`(29220) · `_hist_row_from_doc`(35138) · `_timeline_snap_tick`(36575) · `drop_stats`(331)

### `_pub_horse_cards(an, role, nos)`
- **위치**: `app.py:14997` (14997~15032)
- **설명**: 말별 신호 카드 — 등급·역할·태그·부연설명. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `public_matrix`(15085)

### `_pub_result(rk)`
- **위치**: `app.py:15035` (15035~15052)
- **설명**: 경주 결과(있으면) — 예상 vs 결과 비교 카드용. 없으면 None. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `public_matrix`(15085)

### `_hist_path(rk)`
- **위치**: `app.py:15350` (15350~15357)
- **설명**: raceKey → (파일경로, date, race). 예: '2026-07-02 나고야 4경주' / '나고야 4경주'. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 28)**: `_ingest_reject_log`(2734) · `anomaly_feed`(3540) · `_snapshot_signal_first`(3770) · `_time_based_drop_signals`(4856) · `_triple_analyze`(10724) · `_pub_result`(15035) … [+22]

### `_archive_compress_old(days)`
- **위치**: `app.py:15376` (15376~15414)
- **설명**: [영구보존·4번] 7일+ 지난 odds_history .json 을 .json.gz 로 압축(데이터 삭제 없이 메모리·용량 관리). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `odds_archive_compress`(20484) · `_boot_background`(38998)

### `_combo_dict(arr)`
- **위치**: `app.py:15417` (15417~15427)
- **설명**: [{combo,odds}] → {'1+2': 45.2, ...}. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_history_append`(15465)

### `_exa_fav_dirs(exa_dict)`
- **위치**: `app.py:15433` (15433~15462)
- **설명**: 쌍승(방향성) 배당 dict('a+b'→odds) → {무순쌍: (유력방향튜플, 유력배당)}. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_history_append`(15465)

### `_pick(label)`
- **위치**: `app.py:15722` (15722~15724)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_history_save_analysis`(15703) · `_dl_reliability`(17869)

### `_corrupt_note(path, reason, tag, dst)`
- **위치**: `app.py:15819` (15819~15845)
- **설명**: 격리 사실을 `data/collect_gaps/<날짜>.json` 에 남긴다(조용히 넘어가지 않게). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_corrupt_quarantine`(15865) · `_json_load_guard`(15887)  ⚠ 위험목록 함수 → 전수 나열

### `_corrupt_quarantine(path, err, tag)`
- **위치**: `app.py:15865` (15865~15884)
- **설명**: 손상 원본을 `.corrupt.<ts>` **사본**으로 격리(원본은 그대로 둔다 — 이동하면 그것도 소실이다). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_json_load_guard`(15887)

### `_analysis_log_path(rk)`
- **위치**: `app.py:15931` (15931~15938)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 26)**: `_triple_analyze`(10724) · `triple_analyze`(15132) · `_canonical_log_key`(15941) · `_build_analysis_log`(16347) · `_card_odds_path`(19230) · `_race_card_timeline`(19280) … [+20]

### `_canonical_log_key(rk, live)`
- **위치**: `app.py:15941` (15941~16020)
- **설명**: [중복 key 근본 방지] 같은 경주가 다른 표기(한글 '모리오카 1경주' / 한자 '2026-07-07 盛岡 1R')로 *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 22)**: `_pace_log_record`(10142) · `_triple_analyze`(10724) · `triple_analyze`(15132) · `_build_analysis_log`(16347) · `_card_odds_path`(19230) · `_race_card_timeline`(19280) … [+16]

### `_doc_has_recs(d)`
- **위치**: `app.py:15951` (15951~15954)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_canonical_log_key`(15941)

### `_det_combo_sets(items)`
- **위치**: `app.py:16039` (16039~16050)
- **설명**: [{combo:[...]}...] → {frozenset(마번)} 집합. 잘못된 항목은 조용히 건너뛴다. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_deterministic_review`(16053)

### `_raw_profile_snapshot(rk)`
- **위치**: `app.py:16199` (16199~16248)
- **설명**: [소실 방지 (2026-07-29)] 경주 종료 시 영구 소실되는 **원본 입력**을 분석 로그에 복사해 남긴다. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_build_analysis_log`(16347) · `_forecast_after_form`(34603)

### `_race_type_of(rk, sport)`
- **위치**: `app.py:16265` (16265~16272)
- **설명**: [읽기 전용] 반에이면 'banei', 아니면 기존 sport 를 그대로 돌려준다. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_build_analysis_log`(16347)

### `_judge_extra_quinellas(cp, sport, already)`
- **위치**: `app.py:16305` (16305~16344)
- **설명**: [판정 편입] 화면에는 나가는데 판정 밖이던 조합을 명단에 더한다. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_build_analysis_log`(16347)

### `_take(src, tag)`
- **위치**: `app.py:16313` (16313~16326)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_judge_extra_quinellas`(16305)

### `_combo_reason(nos)`
- **위치**: `app.py:16486` (16486~16488)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_build_analysis_log`(16347)

### `_rec_sig(fr, kh)`
- **위치**: `app.py:16514` (16514~16525)
- **설명**: [복승 ①②③ 이력 (2026-07-19)] 메인만 비교하던 sig에 finalQuinellas 전체 구성 포함 — *(주석(본문))*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_build_analysis_log`(16347)

### `_cp_has_recs(cp)`
- **위치**: `app.py:16582` (16582~16585)
- **설명**: [추천 보존·근본수정] 경기 종료 후 재저장(결과 입력 등) 시 an 이 비어 corePicks/final_recommendation 이 *(주석(def 위))*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_build_analysis_log`(16347)

### `_fr_has_recs(fr)`
- **위치**: `app.py:16587` (16587~16588)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_build_analysis_log`(16347)

### `_strip_race_dist(race)`
- **위치**: `app.py:16854` (16854~16857)
- **설명**: [중복통합] raceKey/경주명에서 끝의 거리접미(예: ' 1200M', '900M')만 제거 → 한 경주 = 한 표기. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `_canon_rk`(1962) · `_analysis_log_path`(15931) · `_race_result_id`(16860)

### `_race_result_id(rk)`
- **위치**: `app.py:16860` (16860~16880)
- **설명**: raceKey → race_id(파일명). 예: '2026-07-05 사가 8경주' → '2026-07-05_사가_8경주'. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 11)**: `_race_result_path`(16883) · `_build_race_result`(16955) · `_missing_results`(17115) · `_ai_training_path`(17164) · `_build_ai_training`(17176) · `_apply_result_learning`(20801) … [+5]

### `_race_result_path(rk)`
- **위치**: `app.py:16883` (16883~16900)
- **설명**: [중복통합·호환 탐색] canonical 파일이 없고, 같은 경주의 '거리접미 변형' 파일이 이미 있으면 그걸 재사용 *(주석(본문))*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `_save_race_result`(17095) · `_competitor_result_for_race`(23221) · `_race_result_summary`(35302)

### `_validate_race_result(data)`
- **위치**: `app.py:16903` (16903~16927)
- **설명**: [4번] 데이터 품질 검증 — 마번 1~16, 배당 1.0~9999, 날짜 형식, 필수값. 오류 리스트 반환(빈=정상). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_save_race_result`(17095)

### `_extract_track_round(rk)`
- **위치**: `app.py:16930` (16930~16932)
- **설명**: raceKey → (track 경마장명, round 경주번호). _area_num 재사용(부경=부산 정규화 포함). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_build_race_result`(16955) · `_build_ai_training`(17176)

### `_rk_sport(rk, an)`
- **위치**: `app.py:16935` (16935~16952)
- **설명**: [오류7] raceKey/분석에서 종목 자동 분류(horse/cycle/boat/bike/korea). 결과·학습 태깅 통일용. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `_build_race_result`(16955) · `_apply_result_learning`(20801) · `_ml_row_build`(21532)

### `_build_race_result(rk, an, record, result, top4, inputs)`
- **위치**: `app.py:16955` (16955~17092)
- **설명**: [1번] 경주별 완전 저장 구조 조립(AI 학습용). an/record/odds_history/입력값 종합. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_save_race_result`(17095)

### `_missing_results(date)`
- **위치**: `app.py:17115` (17115~17153)
- **설명**: [3번] 당일 분석했으나 결과 미입력 경주 추적. analysis_log 스캔 → 결과 없는 raceKey 목록. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 8)**: `race_results_missing`(23727) · `_kra_result_backfill_once`(27368) · `kra_results_backfill`(27475) · `_keirin_result_backfill_once`(28021) · `keirin_results_backfill`(28983) · `_jp_result_backfill_once`(29005) … [+2]

### `_ai_training_path(rk)`
- **위치**: `app.py:17164` (17164~17167)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_save_ai_training`(17379)

### `_odds_range_label(o)`
- **위치**: `app.py:17170` (17170~17173)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_build_ai_training`(17176)

### `_build_ai_training(rk, an, record, result, top4, inputs)`
- **위치**: `app.py:17176` (17176~17316)
- **설명**: [1번] AI 학습용 완전 데이터 구조 조립 — 말별 피처 + 배당 피처 + 예측 + 결과 + 라벨. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_save_ai_training`(17379)

### `_placed(h)`
- **위치**: `app.py:17276` (17276~17280)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_build_ai_training`(17176)

### `_ai_timeline(rk)`
- **위치**: `app.py:17319` (17319~17325)
- **설명**: odds_history 스냅샷 → 학습용 배당 타임라인. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_build_ai_training`(17176)  ⚠ 위험목록 함수 → 전수 나열

### `_ai_rows_for_thresholds()`
- **위치**: `app.py:17431` (17431~17463)
- **설명**: ai_training 파일에서 기준치 계산에 필요한 필드만 추출(결과 입력된 경주만). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_threshold_candidates`(17466)

### `_threshold_candidates()`
- **위치**: `app.py:17466` (17466~17514)
- **설명**: [2·3·5번] 누적 데이터로 신호 기준치별 적중률 계산 → 최적 기준치 추천 + 현재 대비 개선폭. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `thresholds_optimize`(3610)

### `_rate(fired_placed)`
- **위치**: `app.py:17473` (17473~17476)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 4)**: `_dark_stats_recompute`(10493) · `_threshold_candidates`(17466) · `_recompute_learning_stats`(19031) · `_form_ext_build`(31933)

### `_best(cands, cur_val, key_name)`
- **위치**: `app.py:17493` (17493~17507)
- **설명**: 표본 THRESHOLD_SAMPLE_MIN+ 인 후보 중 적중률 최고. 현재값 적중률과 비교. *(주석(본문))*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_threshold_candidates`(17466)

### `_ai_data_status()`
- **위치**: `app.py:17517` (17517~17560)
- **설명**: [3·7번] AI 학습 데이터 현황 — 수집/고품질/평균품질·목표 진행률·마일스톤(100/500) 예상 일정. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `ai_training_status`(23733)

### `_eta_days(target)`
- **위치**: `app.py:17542` (17542~17544)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_ai_data_status`(17517)

### `_build_daily_summary(date)`
- **위치**: `app.py:17567` (17567~17606)
- **설명**: [6번] 당일 경주 요약 저장 — 총경주·평균품질·이상감지·적중·적중률·AI준비 경주·누적. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `_daily_learning_generate`(17894) · `_apply_result_learning`(20801) · `daily_summary`(23779)

### `_last_run()`
- **위치**: `app.py:17718` (17718~17722)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_start_periodic_backup`(17704) · `_loop`(17734)

### `_loop()`
- **위치**: `app.py:17734` (17734~17747)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 0)**: —  ⚠ 위험목록 함수 → 전수 나열

### `_dl_time_zone(saved_at)`
- **위치**: `app.py:17764` (17764~17770)
- **설명**: 'YYYY-MM-DD HH:MM:SS' → 오전(≤11)/오후(12~16)/저녁(17~)/미상. 마감 시간대 근사. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_daily_bigdata`(17799)

### `_dl_hc_bucket(n)`
- **위치**: `app.py:17773` (17773~17779)
- **설명**: 출전 두수 → 버킷. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_daily_bigdata`(17799)

### `_dl_bucket_add(agg, key, hit)`
- **위치**: `app.py:17782` (17782~17787)
- **설명**: 집계 헬퍼: agg[key] = {n, hits}. hit 가 None(미판정)이면 n만 증가. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_daily_bigdata`(17799)

### `_dl_finalize(agg)`
- **위치**: `app.py:17790` (17790~17796)
- **설명**: {key:{n,hits}} → {key:{n,hits,hit_rate}} (내림차순 n). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_daily_bigdata`(17799)

### `_daily_bigdata(date)`
- **위치**: `app.py:17799` (17799~17829)
- **설명**: [2번] 오늘 경주를 ai_training/ 에서 스캔해 빅데이터 집계. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_daily_learning_generate`(17894)

### `_daily_profit(date)`
- **위치**: `app.py:17832` (17832~17849)
- **설명**: 오늘 결과 입력된 경주의 손익 합(race_results/ investment.profit). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_daily_learning_generate`(17894)

### `_daily_tags(date)`
- **위치**: `app.py:17852` (17852~17866)
- **설명**: 오늘 analysis_log/ 파일들의 pattern_tags 를 모아 유니크 태그 목록(데이터 기반). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_daily_learning_generate`(17894)

### `_dl_reliability()`
- **위치**: `app.py:17869` (17869~17886)
- **설명**: [3번] 누적 패턴 신뢰도 — learning.json 통계에서 파생(마감급락·쌍승역전·전적유력마·추천종합). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_daily_learning_generate`(17894)

### `_pick(st, label)`
- **위치**: `app.py:17877` (17877~17879)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_history_save_analysis`(15703) · `_dl_reliability`(17869)

### `_dl_done_day()`
- **위치**: `app.py:17975` (17975~17979)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_start_daily_learning_scheduler`(17961) · `_loop`(17991)

### `_upset_bump(stats, key, hit)`
- **위치**: `app.py:18070` (18070~18074)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_learn_upset`(18395)

### `_keyhorse_hit(an, top3)`
- **위치**: `app.py:18163` (18163~18173)
- **설명**: [1·2번·유력마 기반 적중] 기존 정확 적중(복승 1+2·삼복승 1+2+3)과 별개 병행 지표. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_learn_signal_stats`(18248) · `_apply_result_learning`(20801)

### `_active_signal_horses(an)`
- **위치**: `app.py:18176` (18176~18218)
- **설명**: [3번] 이 경주에서 각 신호 유형이 선정한 유력마/복병 마번 → {신호명: [마번]}(빈 신호는 제외). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_learn_signal_stats`(18248)

### `_signal_reliability()`
- **위치**: `app.py:18349` (18349~18356)
- **설명**: [5번] 신호별 신뢰도 요약(50경주+ 표본이면 신뢰) → {신호명: {rate_quinella, rate_trifecta, count, reliable}}. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_signal_reliability_for`(18359)

### `_signal_reliability_for(strong_signals, dark_horses, inverse, compression_pattern, cross_reversal)`
- **위치**: `app.py:18359` (18359~18392)
- **설명**: [5번] 현재 분석에서 활성인 신호별 과거 적중률(50경주+ 신뢰) → 프론트 강조 표시용. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_hno(x)`
- **위치**: `app.py:18405` (18405~18416)
- **설명**: [타입 가드] 말번호를 int/str/dict 어느 형태든 안전 추출. keyHorses·anomalyHorse가 *(주석(본문))*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_form_from_starters`(1560) · `_learn_upset`(18395)

### `_drop_timing_from_timeline(timeline)`
- **위치**: `app.py:18475` (18475~18490)
- **설명**: 배당 타임라인(30초 스냅샷)에서 복승 배당의 최대 하락과 그 시점(T-N분 버킷). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_race_features`(18493)

### `_race_features(log)`
- **위치**: `app.py:18493` (18493~18522)
- **설명**: 분석 로그 1건 → 패턴 발견용 특징. 결과 미입력이면 None(학습 대상 제외). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_discover_patterns`(18525)

### `_discover_patterns()`
- **위치**: `app.py:18525` (18525~18613)
- **설명**: data/analysis_log/ 전체를 스캔해 적중 경주의 공통점을 자동 발견 → discovered_patterns.json. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_apply_result_learning`(20801) · `patterns_discovered`(24709)

### `add_bool(key, label)`
- **위치**: `app.py:18557` (18557~18569)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_discover_patterns`(18525)

### `_safe_num(v)`
- **위치**: `app.py:18633` (18633~18637)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 17)**: `result_ocr`(838) · `_apply_profit_strategy`(9809) · `_apply_bet_rules`(14178) · `_build_race_result`(16955) · `_build_ai_training`(17176) · `shadow_trifecta_ep`(20227) … [+11]

### `_combo_timeline(doc, combo)`
- **위치**: `app.py:18660` (18660~18690)
- **설명**: 스냅샷들에서 특정 복승 조합(combo=[a,b])의 배당 변화 타임라인 추출. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_build_race_report`(18821)

### `_signal_timeline_from_doc(doc)`
- **위치**: `app.py:18693` (18693~18753)
- **설명**: [3·4·5번] 스냅샷의 signal_horse 시퀀스에서 이상감지 말 변경 이력 + 신호 안정화(2연속 확정) *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_signal_timeline`(18756) · `_build_race_report`(18821)

### `_signal_timeline(rk)`
- **위치**: `app.py:18756` (18756~18763)
- **설명**: 경주 히스토리 파일을 읽어 _signal_timeline_from_doc 도출(없으면 None). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_triple_analyze`(10724) · `signal_timeline_api`(18767)

### `_signal_win_tags(an, top3)`
- **위치**: `app.py:18778` (18778~18812)
- **설명**: [4번] 어떤 신호가 실제 입상마를 맞혔는지 태깅. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_build_race_report`(18821) · `_apply_result_learning`(20801)

### `_report_odds_band(odds)`
- **위치**: `app.py:18815` (18815~18818)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_build_race_report`(18821)

### `_build_race_report(rk, an, record, result, doc)`
- **위치**: `app.py:18821` (18821~18999)
- **설명**: [1번] 경주별 완전 재현 리포트를 조립해 data/race_report/에 저장하고 dict 반환. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_apply_result_learning`(20801)

### `_rep_combo_for(no)`
- **위치**: `app.py:18842` (18842~18855)
- **설명**: 말 no 를 포함하는 대표 복승 조합(급락 가장 큰 것 → 없으면 추천 조합). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_build_race_report`(18821)

### `_highlight_story(an, top3, doc)`
- **위치**: `app.py:19002` (19002~19022)
- **설명**: [명예의전당] 고배당 적중 '왜 맞았는지' 스토리 + 정답말 배당 타임라인 스냅샷. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_apply_result_learning`(20801)

### `_rate(records, sel, cond)`
- **위치**: `app.py:19025` (19025~19028)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 4)**: `_dark_stats_recompute`(10493) · `_threshold_candidates`(17466) · `_recompute_learning_stats`(19031) · `_form_ext_build`(31933)

### `rev_hit(r)`
- **위치**: `app.py:19033` (19033~19035)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 0)**: —

### `_is_high_hit(r)`
- **위치**: `app.py:19138` (19138~19141)
- **설명**: [신규 4번] 신호조합 학습: 어떤 신호(조합)가 실제 입상마를 맞혔고, 그 중 고배당 적중 비율. *(주석(def 위))*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_recompute_learning_stats`(19031)

### `_card_odds_path(rk)`
- **위치**: `app.py:19230` (19230~19241)
- **설명**: 🔴 odds_history 파일명은 **날짜 접두**다(`2026_08_10_히라츠카_3경주.json`). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `_card_odds_at`(19244) · `_race_full`(19729) · `_cut_tier_candidates`(35635)

### `_card_odds_at(rk, when_hms, combo)`
- **위치**: `app.py:19244` (19244~19277)
- **설명**: 그 시각에 가장 가까운 스냅샷에서 조합 배당을 찾는다. 🔴 빠진 조합의 배당이 핵심이다. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_race_card_timeline`(19280)

### `_race_card_timeline(rk)`
- **위치**: `app.py:19280` (19280~19425)
- **설명**: 카드용 신호 이력 1건 조립(읽기 전용). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `race_card_timeline_api`(19429) · `_race_full`(19729) · `_rec_trail_for_detail`(35762)  ⚠ 위험목록 함수 → 전수 나열

### `_rf_num(v)`
- **위치**: `app.py:19456` (19456~19465)
- **설명**: 숫자로 바꿀 수 있으면 float, 아니면 None. 🔴 반올림하지 않는다. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_rf_combo_map`(19468) · `_race_full`(19729)

### `_rf_combo_map(src, arity)`
- **위치**: `app.py:19468` (19468~19493)
- **설명**: 복승/쌍승/삼복승 원본을 {"1+2": 배당} 으로 편다. dict·list 두 형태를 다 받는다. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_race_full`(19729)

### `_rf_resolve_rk(track, race, date)`
- **위치**: `app.py:19496` (19496~19535)
- **설명**: 경기장명 + 경주번호(+날짜) → 실제 저장 키. 🔴 별칭·표기 변형은 `_track_norm` 에 맡긴다. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `race_full_api`(19883) · `race_odds_api`(20122)

### `_stake_weight(o)`
- **위치**: `app.py:19577` (19577~19582)
- **설명**: 조합 하나의 배분 가중치. 🔴 여기만 바꾸면 배분안이 통째로 바뀐다. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_stake_plan`(19585)

### `_stake_plan(combos, odds_map, total, guard)`
- **위치**: `app.py:19585` (19585~19674)
- **설명**: [원금 회수율] 조합 목록 → 배분 비율·원금 회수율. 🔴 완전 읽기 전용. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_stake_plan_from_cp`(19677)

### `_stake_plan_from_cp(cp, combos, guard)`
- **위치**: `app.py:19677` (19677~19694)
- **설명**: corePicks 에서 배당을 그러모아 `_stake_plan` 을 만든다(추천 목록 기준). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_race_full`(19729) · `_kakao_rich_message`(36119)

### `_rf_signals(doc)`
- **위치**: `app.py:19697` (19697~19726)
- **설명**: [신호 모음] 🔴 **실제 저장 위치에서 읽는다.** *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_race_full`(19729)

### `_race_full(rk)`
- **위치**: `app.py:19729` (19729~19879)
- **설명**: [배당판 전체] 이미 쌓이는 것만 조립한다. 🔴 읽기 전용 · 새 계산 없음. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `race_full_api`(19883) · `race_odds_api`(20122)  ⚠ 위험목록 함수 → 전수 나열

### `_report_head()`
- **위치**: `app.py:19918` (19918~19927)
- **설명**: 지금 HEAD 커밋 해시. ⚠ 보고를 **쓰는 시점** 값이라 그 보고가 담긴 커밋과 한 칸 어긋난다. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_report_append`(19930)

### `_next_local_only()`
- **위치**: `app.py:19977` (19977~19985)
- **설명**: 🔴 로컬(127.0.0.1 · ::1)에서 온 요청만 허용. 아니면 사유 문자열을 돌려준다. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `next_api`(20003)

### `_next_cors(resp)`
- **위치**: `app.py:19988` (19988~19999)
- **설명**: ⚠ localhost 출처만 CORS 허용 — 외부 도메인은 헤더를 주지 않는다. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `next_api`(20003)

### `agg(sel)`
- **위치**: `app.py:20287` (20287~20296)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_compression_hitrate`(5532) · `shadow_trifecta_ep`(20227)

### `_add(kind, combo, need)`
- **위치**: `app.py:20636` (20636~20641)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_confidence_picks`(8085) · `_rec_combos_from_analysis_log`(20597)

### `_s(v)`
- **위치**: `app.py:20735` (20735~20736)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_conf_picks_from_log`(20722)

### `_winning_quinella_odds(rk, top2)`
- **위치**: `app.py:20742` (20742~20758)
- **설명**: [수익 자동 계산·추정] 실배당 미입력 시 승자 복승 조합(top2)의 마지막 시장배당을 analysis_log *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `_build_race_result`(16955) · `_apply_result_learning`(20801) · `_ml_row_build`(21532)

### `__init__(self, on)`
- **위치**: `app.py:20786` (20786~20788)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `__init__`(23841)

### `__enter__(self)`
- **위치**: `app.py:20790` (20790~20794)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 0)**: —

### `__exit__(self, *a)`
- **위치**: `app.py:20796` (20796~20798)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 0)**: —

### `in3(combo)`
- **위치**: `app.py:20870` (20870~20871)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_apply_result_learning`(20801)

### `_cmp_hit(block)`
- **위치**: `app.py:21057` (21057~21061)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_apply_result_learning`(20801)

### `_odds_val(x)`
- **위치**: `app.py:21104` (21104~21105)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_apply_result_learning`(20801)

### `snap_near(minb)`
- **위치**: `app.py:21201` (21201~21209)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_apply_result_learning`(20801)

### `_ml_row_build(rk, an, result, top3, payouts, was_hit)`
- **위치**: `app.py:21532` (21532~21577)
- **설명**: [ML 학습셋] 경주 1건 → {features, label}. 있는 값만 채우고 없으면 None(관대). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_ml_append_row`(21580)

### `_ml_append_row(rk, an, result, top3, payouts, was_hit)`
- **위치**: `app.py:21580` (21580~21587)
- **설명**: [ML 학습셋 누적] ml_training_data.jsonl 에 경주 1건 append(1줄 1경주). 실패 격리. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_apply_result_learning`(20801)

### `_is_tainted_saved(saved_at)`
- **위치**: `app.py:21597` (21597~21600)
- **설명**: 결과 저장 시각(saved_at 'YYYY-MM-DD HH:MM:SS')이 오염 구간 안인지. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_build_signal_performance`(21603) · `_scoreboard_daily`(28704)

### `_build_signal_performance()`
- **위치**: `app.py:21603` (21603~21656)
- **설명**: [signal_performance] 신호별 성과(총·적중·적중률·평균배당·ROI) 재집계 → signal_performance.json. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_kra_backfill_loop`(27405)

### `_horse_repr_timeline(doc, no)`
- **위치**: `app.py:21697` (21697~21733)
- **설명**: [3번] 특정 말의 대표 배당 타임라인을 스냅샷에서 역추적. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `_highlight_story`(19002) · `_classify_failure`(21794) · `_failure_report`(21980)

### `_timeline_rebound(tl)`
- **위치**: `app.py:21736` (21736~21746)
- **설명**: 타임라인이 '급락 후 반등'(페이크 의심)인지 판정. 최저점까지 10%+ 하락 후 최저 대비 10%+ 반등. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_classify_failure`(21794)

### `_timeline_signal_label(pct)`
- **위치**: `app.py:21749` (21749~21761)
- **설명**: 직전대비 급락% → 사람이 읽는 신호 강도 라벨(역추적 리포트용). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_failure_report`(21980) · `_rr_horse_timeline`(22203)

### `_classify_failure(rk, an, top3, doc)`
- **위치**: `app.py:21794` (21794~21911)
- **설명**: [1번] 미적중 경주의 실패 유형 자동 분류 + [2·3번] 정답말 역추적 근거 생성. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_failure_record`(21914) · `_failure_report`(21980)

### `_failure_autorule(d, mpat, ftype, rk, stats)`
- **위치**: `app.py:21949` (21949~21977)
- **설명**: [7번] 놓친 패턴 N건+ 반복 시 규칙 자동 생성. [5번] 생성 시점 추천 적중률을 before로 스냅샷. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_failure_record`(21914)

### `_failure_report(rk)`
- **위치**: `app.py:21980` (21980~22035)
- **설명**: [2·3번] 미적중 경주 복기 리포트 생성(온디맨드). 히스토리 결과+스냅샷에서 재구성. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `failure_report`(22529)

### `_in3(combo)`
- **위치**: `app.py:22000` (22000~22001)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_failure_report`(21980)

### `_success_report_text(rk, top3, hit_combos, timelines)`
- **위치**: `app.py:22038` (22038~22066)
- **설명**: [복기 UI 통합] 적중 경주 '왜 맞았는지' 텍스트 리포트(정답말 신호 근거 포함). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_failure_report`(21980)

### `_failure_report_text(rk, top3, recommended, was_hit, fail, timelines)`
- **위치**: `app.py:22069` (22069~22112)
- **설명**: [2·3번] 복기 리포트를 사람이 읽는 텍스트로. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_failure_report`(21980)

### `_rr_load_by_key(rk)`
- **위치**: `app.py:22178` (22178~22195)
- **설명**: raceKey(또는 race_id)로 race_results/ 파일 1건 로드(가장 최근). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_high_odds_review_one`(22372)

### `_rr_top3(rr)`
- **위치**: `app.py:22198` (22198~22200)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_high_odds_prefilter`(22360)

### `_rr_horse_timeline(rr, h)`
- **위치**: `app.py:22203` (22203~22223)
- **설명**: 정답말 h 의 대표 배당 타임라인 — 각 스냅샷에서 h 가 낀 최저 복승 조합(자금유입 대리). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_high_odds_review_one`(22372)

### `_rr_winning_odds(rr, top3)`
- **위치**: `app.py:22226` (22226~22247)
- **설명**: 정답 복승(top2) 배당 — odds_timeline 최신값 → odds_at_start → investment 폴백. 삼복승은 대개 미수집. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_high_odds_prefilter`(22360)

### `_rr_recommend(rr)`
- **위치**: `app.py:22250` (22250~22263)
- **설명**: 우리 추천 조합(prediction.recommend_main/sub) → (마번 집합, 표시 문자열 리스트). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_rr_classify`(22266) · `_high_odds_review_one`(22372)

### `_rr_classify(rr, top3, timelines)`
- **위치**: `app.py:22266` (22266~22317)
- **설명**: [4번] race_results 기반 미적중 유형 분류(_classify_failure 5유형 로직 이식). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_high_odds_review_one`(22372)

### `_high_odds_simulate(fail)`
- **위치**: `app.py:22320` (22320~22340)
- **설명**: [5번] '개선된 로직 적용 시' 반사실 시뮬레이션. 실패 유형·초점말 급락폭 기반. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_high_odds_review_one`(22372)

### `_high_odds_next_rules(fail)`
- **위치**: `app.py:22343` (22343~22357)
- **설명**: [6번] 미적중 유형별 내일 적용 규칙(고배당 특화). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_high_odds_review_one`(22372)

### `_high_odds_prefilter(rr)`
- **위치**: `app.py:22360` (22360~22369)
- **설명**: 전체 분석 전 저비용 판정: race_results 의 결과 top3 + 정답 복승 배당으로 고배당 여부만 확인. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_high_odds_review_one`(22372) · `_high_odds_scan`(22464)

### `_high_odds_scan(date, save, limit)`
- **위치**: `app.py:22464` (22464~22488)
- **설명**: [1번] 결과 입력된 경주(race_results/)를 스캔해 고배당(복승30+/삼복승100+)만 심층 분석·저장. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `high_odds_review`(23069)

### `_snap_date_of(fn, meta)`
- **위치**: `app.py:22670` (22670~22676)
- **설명**: 스냅샷 파일의 날짜(YYYY-MM-DD). 파일명 접두 우선, 없으면 meta['at'] 앞 10자. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 6)**: `_snapshot_dates_for_race`(3793) · `_snapshot_metas_for_race`(3814) · `_snapshot_build_compare`(3868) · `snapshot_save`(3994) · `_snapshot_index`(22684) · `_day_snapshot_for`(22720)

### `_snap_key(date, rk)`
- **위치**: `app.py:22679` (22679~22681)
- **설명**: 인덱스 키 — 날짜를 반드시 포함한다. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_snapshot_index`(22684) · `_day_snapshot_for`(22720)

### `_snapshot_index(ttl)`
- **위치**: `app.py:22684` (22684~22717)
- **설명**: {'YYYY-MM-DD|raceKey': {trigger: 파일명, '_sameday': 파일명}} — 실패 시 빈 dict. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_day_snapshot_for`(22720)  ⚠ 위험목록 함수 → 전수 나열

### `_day_snapshot_for(rk, trigger, date)`
- **위치**: `app.py:22720` (22720~22757)
- **설명**: 해당 **날짜**·경주의 지정 trigger 스냅샷 파일명 반환. 이미지 URL 조립용. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `day_races`(22845)

### `_day_card_extra(rk)`
- **위치**: `app.py:22768` (22768~22841)
- **설명**: 날짜별 카드에 실을 보강 정보 — 판정 명단·적중 조합·확정배당·등급. 실패 시 {}(기존 동작 유지). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `day_races`(22845)  ⚠ 위험목록 함수 → 전수 나열

### `fmt(lst)`
- **위치**: `app.py:22795` (22795~22808)
- **설명**: 조합 리스트 → ['1+11', ...]. 형식이 제각각인 구데이터 방어(문자열·중첩·None 혼재). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_day_card_extra`(22768)

### `_competitor_result_for_race(rk)`
- **위치**: `app.py:23221` (23221~23239)
- **설명**: 경주 결과(1·2·3착) → analysis_log(canonical) 우선, 없으면 race_results. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_competitor_record`(23280)

### `_quinella_hits(combos, result)`
- **위치**: `app.py:23242` (23242~23250)
- **설명**: 복승 조합 중 실제 1·2착(순서무관)과 일치하는 게 있으면 True. 결과 없으면 None. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_competitor_compare`(23253)

### `_competitor_compare(comp_pat, our, result)`
- **위치**: `app.py:23253` (23253~23271)
- **설명**: 저쪽 vs 우리 비교: 적중·축 일치·연결마 차이. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_competitor_record`(23280)

### `_competitor_path(rk, date)`
- **위치**: `app.py:23274` (23274~23277)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_competitor_record`(23280) · `competitor_get`(23398)

### `_comment_tags(comment)`
- **위치**: `app.py:23477` (23477~23484)
- **설명**: 자유 코멘트 → 참고용 키워드 태그 리스트(강제 분류 아님·중복 제거). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `review_save`(23526)

### `__init__(self)`
- **위치**: `app.py:23841` (23841~23843)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `__init__`(23841)

### `handle_starttag(self, tag, attrs)`
- **위치**: `app.py:23844` (23844~23848)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 0)**: —

### `handle_data(self, data)`
- **위치**: `app.py:23849` (23849~23851)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 0)**: —

### `handle_endtag(self, tag)`
- **위치**: `app.py:23852` (23852~23858)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 0)**: —

### `_split(ln)`
- **위치**: `app.py:23872` (23872~23875)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_parse_result_tsv`(23861)

### `ns(s)`
- **위치**: `app.py:23877` (23877~23878)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_parse_result_tsv`(23861) · `_parse_result_rows`(23937)

### `idx(pat, default)`
- **위치**: `app.py:23890` (23890~23894)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_parse_result_tsv`(23861) · `_parse_result_rows`(23937)

### `_fw(s)`
- **위치**: `app.py:23906` (23906~23907)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `_parse_result_tsv`(23861) · `num`(23909) · `fnum`(23915)

### `num(cells, k)`
- **위치**: `app.py:23909` (23909~23913)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_parse_result_tsv`(23861)

### `fnum(cells, k)`
- **위치**: `app.py:23915` (23915~23916)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_parse_result_tsv`(23861)

### `_fw2ascii(s)`
- **위치**: `app.py:23952` (23952~23954)
- **설명**: 전각숫자(０-９)·전각콜론 → 반각(중앙 JRA 결과표 대응) *(주석(본문))*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 4)**: `_parse_result_rows`(23937) · `ns`(23956) · `cell`(23986) · `firstnum`(23989)

### `ns(s)`
- **위치**: `app.py:23956` (23956~23957)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_parse_result_tsv`(23861) · `_parse_result_rows`(23937)

### `idx(pat)`
- **위치**: `app.py:23974` (23974~23978)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_parse_result_tsv`(23861) · `_parse_result_rows`(23937)

### `cell(r, k)`
- **위치**: `app.py:23986` (23986~23987)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_parse_result_rows`(23937)

### `firstnum(s)`
- **위치**: `app.py:23989` (23989~23991)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_parse_result_rows`(23937)

### `_area_num(s)`
- **위치**: `app.py:24190` (24190~24205)
- **설명**: 문자열에서 (경마장 토큰[정규화], 경주번호) 추출. 결과행·raceKey 공용. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 46)**: `results_auto`(1400) · `_canon_rk`(1962) · `_do_triple_ingest`(2768) · `triple_latest`(3396) · `current_race`(4205) · `_dark_case_record`(10612) … [+40]

### `_resolve_race_key(rk, candidates)`
- **위치**: `app.py:24228` (24228~24257)
- **설명**: 입력 raceKey를 실제 분석된 key로 유연 매칭(형식 불일치 방어). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 11)**: `_snapshot_result_for`(3691) · `public_matrix`(15085) · `failure_report`(22529) · `high_odds_review`(23069) · `competitor_record`(23357) · `competitor_get`(23398) … [+5]

### `_today_first(ks)`
- **위치**: `app.py:24244` (24244~24245)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_resolve_race_key`(24228)

### `_rec_from_history(rk)`
- **위치**: `app.py:24260` (24260~24287)
- **설명**: triple_store에 활성 데이터가 없을 때 odds_history 스냅샷에서 rec 재구성(결과학습용). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `_apply_result_learning`(20801) · `_failure_report`(21980) · `dark_horse_review`(22611)  ⚠ 위험목록 함수 → 전수 나열

### `_arr(d)`
- **위치**: `app.py:24272` (24272~24279)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_rec_from_history`(24260)

### `_match_row_to_key(row, analyzed_keys)`
- **위치**: `app.py:24290` (24290~24296)
- **설명**: 결과행(area,round) → 분석된 raceKey 유연 매칭(한국·일본 모든 경기). 실패 시 None. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 0)**: —

### `_match_row_to_all_keys(row, analyzed_keys)`
- **위치**: `app.py:24299` (24299~24316)
- **설명**: 결과행 → (경마장+경주번호) 일치하는 **모든** 분석 key. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_register_result_rows`(24367)

### `_log_has_recommendation(rk)`
- **위치**: `app.py:24319` (24319~24326)
- **설명**: 해당 raceKey 로그가 추천/유력마 데이터를 가졌는지(적중판정 기준 primary 선택용). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_register_result_rows`(24367)

### `_register_result_rows(rows, stake, analyzed_keys)`
- **위치**: `app.py:24367` (24367~24417)
- **설명**: 결과행 리스트 → 분석경주 자동매칭 → 적중판정·학습 → 요약. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `result_ocr`(838) · `results_bulk`(24421)

### `_recompute_pnl(rec, stake, payout)`
- **위치**: `app.py:24443` (24443~24464)
- **설명**: [보완#1] 조정용 손익 재계산 — 단건 record-result 와 동일 규칙. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `results_adjust`(24468)

### `_band(diff)`
- **위치**: `app.py:24555` (24555~24562)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_reversal_strength_stats`(24552) · `drop_stats`(331)

### `_qhit(r)`
- **위치**: `app.py:24617` (24617~24618)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `stats_today`(24610)

### `_thit(r)`
- **위치**: `app.py:24620` (24620~24621)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `stats_today`(24610)

### `_keirin_jo_from_venue(venue)`
- **위치**: `app.py:24990` (24990~25017)
- **설명**: raceKey 경륜장명 → joCode(오다와라36·히로시마62·마에바시01·기후04 등). 없으면 None. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 4)**: `_do_triple_ingest`(2768) · `_triple_analyze`(10724) · `keiba_odds`(26024) · `_keirin_jo_today`(27835)

### `_kstrip(s)`
- **위치**: `app.py:25020` (25020~25023)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 5)**: `_keirin_parse_card`(25066) · `_keirin_table_rows`(25294) · `_keiba_schedule`(25968) · `_keirin_result_parse`(27866) · `_keirin_venue_races`(33734)

### `_keirin_style_label(ch)`
- **위치**: `app.py:25026` (25026~25030)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_keirin_parse_card`(25066)

### `_keirin_style_bonus(r)`
- **위치**: `app.py:25046` (25046~25063)
- **설명**: 결정수 보정: 차입(差)·마크(マ) 우세→추입형 +5 / 도주(逃) 우세→선행형 +3 / 젖히기(捲)→+4. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_keirin_analyze`(25149)

### `_oddspark_grid_combos(html, max_no)`
- **위치**: `app.py:25240` (25240~25276)
- **설명**: oddspark 배당 그리드 HTML → {tuple(sorted(combo)): odds}. 복승(2두)·삼복승(3두) 공용. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_keiba_parse_quinella_grid`(25279) · `_keiba_parse_trio_grid`(25286)

### `_keirin_table_rows(html)`
- **위치**: `app.py:25294` (25294~25303)
- **설명**: oddspark 배당 HTML → 표 행 리스트(각 행=셀 텍스트 리스트). 태그 제거·공백 정리. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 6)**: `_keirin_parse_quinella`(25327) · `_keirin_parse_exacta`(25350) · `_keiba_matrix_rows`(25834) · `_keiba_parse_win`(25848) · `_keiba_horse_count`(25887) · `_keirin_result_parse`(27866)

### `_keirin_odds_pairs(cells)`
- **위치**: `app.py:25306` (25306~25324)
- **설명**: ['4','4.5','2','12.7','4', ...] → 선두 정수(축 차번호)와 (배당,순위) 쌍 목록. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_keirin_parse_quinella`(25327) · `_keirin_parse_exacta`(25350)

### `_form_is_today(rec)`
- **위치**: `app.py:25519` (25519~25533)
- **설명**: 저장된 전적이 **오늘 것인가**. 🔴 판정 기준은 저장 시각(`t`)의 날짜다. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_keirin_autocollect_form`(25536) · `_keiba_autocollect_form`(26684)

### `_keirin_autocollect_form(rk, jo, ymd, race)`
- **위치**: `app.py:25536` (25536~25623)
- **설명**: [경륜 출마표 전적 자동 수집] 배당 수집 시 동시에 oddspark 출마표에서 전적(競走得点·착순·결정수·각질) 수집·저장. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 4)**: `_do_triple_ingest`(2768) · `keirin_odds`(25675) · `keiba_odds`(26024) · `_multi_collect_one`(34659)

### `_odds_target_mismatch(rk, race_no, jo)`
- **위치**: `app.py:25626` (25626~25648)
- **설명**: [검역 게이트 (2026-07-23 다마노 11R 오염 실사고)] 저장 키(raceKey) vs 수집 대상(raceNo·경기장) 교차 검증. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `keirin_odds`(25675) · `keiba_odds`(26024)

### `_sched_post_epoch(rk)`
- **위치**: `app.py:25651` (25651~25671)
- **설명**: [검역 보강 (2026-07-23)] 다중경주 스케줄에서 raceKey의 발주시각(postEpoch·초) 조회. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `keirin_odds`(25675) · `keiba_odds`(26024)

### `_keirin_venue_current(jo, ymd)`
- **위치**: `app.py:25724` (25724~25750)
- **설명**: [경륜 현재경주·단일 경륜장] joCode 한 곳만 조회(가벼움) → 현재 발매중(다음 발주) 경주. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `keirin_current`(25754)

### `_keiba_odds_live(q, x)`
- **위치**: `app.py:25816` (25816~25824)
- **설명**: 파싱된 복승·쌍승이 '발매 중 실배당'인지 판정(마감 후·발매 전 가짜값 차단). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `keiba_odds`(26024) · `_multi_collect_one`(34659)

### `_keiba_matrix_rows(html)`
- **위치**: `app.py:25834` (25834~25845)
- **설명**: 배당 매트릭스 행만 추출(선두=차번호·소수배당 포함·마명(가나/한자) 없는 행). 마방리스트 제외. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_keiba_parse_quinella`(25897) · `_keiba_parse_exacta`(25941)

### `_keiba_horse_count(html)`
- **위치**: `app.py:25887` (25887~25894)
- **설명**: 출전 두수 추정 = 배당표에 등장하는 최대 차번호. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `keiba_odds`(26024)

### `_keiba_schedule(ymd, force)`
- **위치**: `app.py:25968` (25968~26000)
- **설명**: 그날(ymd=YYYYMMDD) oddspark 지방경마 개최 경마장 → {한자경마장명: (opTrackCd, sponsorCd)}. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 7)**: `_keiba_resolve_track`(26003) · `keiba_odds`(26024) · `keiba_current`(26168) · `keiba_schedule_ep`(26202) · `keiba_starters`(26619) · `_keiba_form_collect_by_rk`(26754) … [+1]

### `_keiba_resolve_track(venue, ymd)`
- **위치**: `app.py:26003` (26003~26020)
- **설명**: raceKey 경마장명(한/일/영) → 그날 oddspark 개최 코드 (opTrackCd, sponsorCd) 또는 None. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 4)**: `keiba_odds`(26024) · `keiba_current`(26168) · `keiba_starters`(26619) · `_keiba_form_collect_by_rk`(26754)

### `_keiba_current_raceno(op_track, sponsor, ymd, ttl)`
- **위치**: `app.py:26142` (26142~26164)
- **설명**: [경주 자동추종] oddspark RaceList.do(raceNb 미지정) 응답 <title>이 '현재 발매중 경주'로 자동 포커스됨 *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `keiba_current`(26168)

### `_keiba_cells(tr)`
- **위치**: `app.py:26214` (26214~26217)
- **설명**: ══════════ [지방경마 출주표 전적] oddspark RaceList.do(出走表) + HorseDetail(전5경주) ══════════ *(주석(def 위))*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_keiba_parse_shutsuba`(26269) · `_keiba_parse_horse_past`(26370)

### `_keiba_dist_of(s)`
- **위치**: `app.py:26220` (26220~26225)
- **설명**: 'ダ1230'·'芝1400'·'1230m' → (surface, dist_int). 못 읽으면 (surface, None). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `_keiba_parse_horse_past`(26370) · `_jra_past_cell`(30862) · `_jra_parse_shutuba_past`(30906)

### `_keiba_corner_style(past)`
- **위치**: `app.py:26398` (26398~26425)
- **설명**: 전5경주의 통과순위(通過)로 각질 추정. 코너 초반 순위가 두수 대비 앞이면 선행형, 뒤면 추격형. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_keiba_build_form`(26514) · `_jra_style`(30955)

### `_corner_move_bonus(past)`
- **위치**: `app.py:26428` (26428~26451)
- **설명**: [코너 위치 변화·신규] oddspark 통과순위(corner=3C·4C)의 '첫 코너 → 마지막 코너(4C)' 상대위치 변화로 *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_keiba_build_form`(26514) · `_jra_build_form`(30967)

### `_keiba_last3f_note(past)`
- **위치**: `app.py:26454` (26454~26461)
- **설명**: 전5경주 상3F(막판 스피드) 평균. 빠를수록 추격형 강점. 반환 (avg, note). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_keiba_build_form`(26514) · `_jra_build_form`(30967)

### `_keiba_build_form(shutsuba, details)`
- **위치**: `app.py:26514` (26514~26575)
- **설명**: 출주표 + 말별 전적 → 통합 전적 점수. base_form_score(착순 가중평균) + 각질/거리변화/상3F 보정. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `keiba_starters`(26619) · `_keiba_autocollect_form`(26684) · `_nar_autocollect_form`(34079)

### `_apply_back_power(horses)`
- **위치**: `app.py:26578` (26578~26592)
- **설명**: [3번] 경주 내 상3F 상대 뒷힘 보정(상위30% +15·하위 -5) + '⚡ 거리 미경험이나 뒷힘 강점' 플래그. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_keiba_build_form`(26514) · `_jra_build_form`(30967)

### `_one(item)`
- **위치**: `app.py:26601` (26601~26610)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_midcheck_text`(38242)

### `_keiba_autocollect_form(rk, op_track, sponsor, ymd, race_nb)`
- **위치**: `app.py:26684` (26684~26746)
- **설명**: [지방경마(NAR) 전적 자동수집] 서버 배당 수집(_multi_collect_one) 시 oddspark 出走表+전5경주 전적을 함께 수집·저장(경주당 1회). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_keiba_form_collect_by_rk`(26754) · `_multi_collect_one`(34659)

### `_jp_form_backfill_once()`
- **위치**: `app.py:26794` (26794~26828)
- **설명**: [일본경마 전적 백필·board 무관] 활성/분석된 일본지방경마 경주 중 oddspark 전적이 없는 것을 찾아 *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_kra_backfill_loop`(27405)

### `_kra_api_key()`
- **위치**: `app.py:26847` (26847~26848)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 6)**: `_kra_fetch`(27050) · `_kra_backfill_loop`(27405) · `kra_test`(29083) · `kra_status`(29220) · `_kra_api_get`(29379) · `kra_apis_test`(29845)

### `_weather_api_key()`
- **위치**: `app.py:26864` (26864~26865)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `_weather_fetch_raw`(26903) · `weather_current`(26996) · `_weather_auto_start`(27030)

### `_track_condition(weather_main, rain_mm)`
- **위치**: `app.py:26874` (26874~26885)
- **설명**: [주로 상태 판단] 강수량(mm/1h)·날씨로 주로 등급. 반환 (label, level 0~3). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_weather_get`(26928)

### `_weather_dummy(venue)`
- **위치**: `app.py:26888` (26888~26900)
- **설명**: API 키 없을 때 더미 날씨(테스트용) — 경마장 순번으로 결정적 변형(양호/다습/불량/매우불량 골고루). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_weather_fetch_raw`(26903)

### `_weather_get(venue, force)`
- **위치**: `app.py:26928` (26928~26946)
- **설명**: 경주장 날씨+주로 상태 반환(캐시). {venue, weather_main, desc, temp, rain_mm, wind, condition, level, strongWind, source, at}. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 4)**: `_weather_for_race_key`(26949) · `weather_current`(26996) · `weather_venue`(27006) · `_weather_auto_loop`(27019)

### `_weather_for_race_key(rk)`
- **위치**: `app.py:26949` (26949~26955)
- **설명**: raceKey 에서 경마장 추출 → 날씨. 미매칭 시 None. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_weather_adjust_form(form, weather)`
- **위치**: `app.py:26958` (26958~26992)
- **설명**: [날씨 분석 반영] form(말별 전적 리스트)에 날씨 보정 적용 → 보정 근거 붙인 리스트 반환(원본 무변경·복사). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_weather_auto_loop()`
- **위치**: `app.py:27019` (27019~27027)
- **설명**: 발주 60분 이내 경주가 있으면 그 경마장은 10분 주기, 아니면 30분 주기(캐시 TTL 이 흡수). *(주석(본문))*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 0)**: —  ⚠ 위험목록 함수 → 전수 나열

### `_weather_auto_start()`
- **위치**: `app.py:27030` (27030~27037)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_boot_background`(38998)

### `_kra_meet_code(venue)`
- **위치**: `app.py:27040` (27040~27047)
- **설명**: 경마장명(서울/제주/부산경남/부경) → KRA meet 코드('1'/'2'/'3'). 숫자면 그대로. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 13)**: `results_auto`(1400) · `kra_result`(27246) · `_kra_result_backfill_once`(27368) · `kra_test`(29083) · `kra_section`(29102) · `kra_jockey_change`(29119) … [+7]

### `_kra_g(item, *names)`
- **위치**: `app.py:27089` (27089~27096)
- **설명**: item 에서 여러 후보 필드명 중 첫 유효값(대소문자·변형 방어). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_kra_parse_items`(27113) · `_kra_track_info`(29680)

### `_kra_num(v)`
- **위치**: `app.py:27099` (27099~27103)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 7)**: `_kra_parse_items`(27113) · `_kra_jockey_changes`(29583) · `_track_condition_by_water`(29668) · `_kra_track_info`(29680) · `_kra_ai_results`(29708) · `kra_race_detail`(29861) … [+1]

### `_kra_int(v)`
- **위치**: `app.py:27106` (27106~27110)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 13)**: `_triple_analyze`(10724) · `_kra_parse_items`(27113) · `_kra_extract_result`(27198) · `kra_ai_results_ingest`(29140) · `_kra_jockey_winrate`(29414) · `_kra_section_records`(29465) … [+7]

### `_kra_rctime_sec(t)`
- **위치**: `app.py:27184` (27184~27195)
- **설명**: KRA rcTime('1:05.0' 또는 '65.0') → 초(float). 파싱 불가·미완주는 None. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_kra_extract_result`(27198) · `_kra_race_placings`(31907)

### `_kra_extract_result(meet, rc_date, rc_no)`
- **위치**: `app.py:27198` (27198~27221)
- **설명**: [KRA 결과 자동 추출] 경주별상세성적표(racedetailresult)의 rcTime(주행기록) 오름차순 정렬로 착순 산출. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `results_auto`(1400) · `_kra_collect_result_one`(27224) · `kra_result`(27246)

### `_kra_auto_loop()`
- **위치**: `app.py:27272` (27272~27344)
- **설명**: 30초 간격으로 watch 등록된 KRA 경주 배당 자동수집(데몬). 수집 결과(시각·건수·성공)를 watch 레코드에 기록. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 0)**: —  ⚠ 위험목록 함수 → 전수 나열

### `_kra_auto_start()`
- **위치**: `app.py:27347` (27347~27354)
- **설명**: KRA 30초 자동수집 데몬 1회 기동(멱등). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `kra_watch`(29183)

### `_kra_result_backfill_once(date, verbose)`
- **위치**: `app.py:27368` (27368~27402)
- **설명**: [KRA 결과 백필·1회] 해당일(기본 오늘) 분석됐으나 결과 없는 '한국' 경주를 KRA rcTime 착순으로 *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_kra_backfill_loop`(27405) · `kra_results_backfill`(27475)

### `_bf_last()`
- **위치**: `app.py:27415` (27415~27419)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_kra_backfill_loop`(27405)

### `_kra_backfill_start()`
- **위치**: `app.py:27464` (27464~27471)
- **설명**: KRA 결과 백필 데몬 1회 기동(멱등). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_boot_background`(38998)

### `_jp_baba_code_from_rk(rk)`
- **위치**: `app.py:27507` (27507~27513)
- **설명**: raceKey 문자열에서 NAR 경마장 별칭을 스캔해 k_babaCode 반환(정규화 무관·방어). 미지원=None. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 5)**: `_official_payouts_for_rk`(27622) · `_official_result_audit`(27672) · `_jp_result_backfill_once`(29005) · `jp_results_backfill`(29063) · `_nar_form_fallback`(34125)

### `_z2h_digits(s)`
- **위치**: `app.py:27516` (27516~27518)
- **설명**: 전각 숫자(０-９)·전각 공백 → 반각(결과표 전각 대응). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_keiba_parse_result`(27533)

### `_keiba_decode(raw_bytes)`
- **위치**: `app.py:27521` (27521~27530)
- **설명**: keiba.go.jp 응답 디코드 — 着順/馬番 헤더가 보이는 인코딩 채택(EUC-JP/Shift_JIS/UTF-8). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_keiba_fetch_page`(27572)

### `_cells(r)`
- **위치**: `app.py:27541` (27541~27543)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_keiba_parse_result`(27533)

### `_keiba_result_top3(baba, ymd, rno, timeout)`
- **위치**: `app.py:27593` (27593~27599)
- **설명**: keiba.go.jp RaceMarkTable 조회 → 상위3 마번 또는 None. 완전 방어(예외=None). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `_official_payouts_for_rk`(27622) · `_official_result_audit`(27672) · `_jp_result_backfill_once`(29005)

### `_keiba_result_payouts(baba, ymd, rno, timeout)`
- **위치**: `app.py:27602` (27602~27619)
- **설명**: [NAR 공식 확정환급 (2026-07-22)] keiba.go.jp RaceMarkTable → {'quinella': 馬連複배당, 'trifecta': 三連複배당}. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `_official_payouts_for_rk`(27622) · `_official_result_audit`(27672) · `_jp_result_backfill_once`(29005)

### `_official_payouts_for_rk(rk, top3, timeout)`
- **위치**: `app.py:27622` (27622~27669)
- **설명**: [확정배당 자동 충전 (2026-07-29 A-3)] raceKey → 공식 확정환급 {'quinella','trifecta'} 또는 None. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_apply_result_learning`(20801)

### `_keirin_jo_today(venue)`
- **위치**: `app.py:27835` (27835~27863)
- **설명**: 경륜장명 → joCode. 오늘 스케줄(확장이 보낸 실코드) 우선, 정적 맵(_keirin_jo_from_venue) 폴백. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 4)**: `_official_payouts_for_rk`(27622) · `_official_result_audit`(27672) · `_keirin_result_backfill_once`(28021) · `keirin_results_backfill`(28983)

### `_keirin_result_top3(jo, ymd, rno, expect_venue)`
- **위치**: `app.py:28000` (28000~28018)
- **설명**: 경륜 결과 1경주 조회 → {ok, top3, quinellaOdds, venue} 또는 {error}. 경륜장명 불일치 시 학습 차단. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `_official_payouts_for_rk`(27622) · `_official_result_audit`(27672) · `_keirin_result_backfill_once`(28021)

### `_keirin_result_backfill_once(date, verbose)`
- **위치**: `app.py:28021` (28021~28133)
- **설명**: [경륜 결과 백필·1회] 당일 분석됐으나 결과 없는 '경륜' 경주를 RaceKekka 착순으로 자동 수집→학습. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_kra_backfill_loop`(27405) · `keirin_results_backfill`(28983)

### `_esc(x)`
- **위치**: `app.py:28146` (28146~28147)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 4)**: `race_log_page`(28137) · `daily_cards_page`(28255) · `review_report_page`(28456) · `scoreboard_page`(28871)

### `_esc(x)`
- **위치**: `app.py:28264` (28264~28265)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 4)**: `race_log_page`(28137) · `daily_cards_page`(28255) · `review_report_page`(28456) · `scoreboard_page`(28871)

### `_esc(x)`
- **위치**: `app.py:28462` (28462~28463)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 4)**: `race_log_page`(28137) · `daily_cards_page`(28255) · `review_report_page`(28456) · `scoreboard_page`(28871)

### `_q_low(qv, k)`
- **위치**: `app.py:28588` (28588~28597)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `debug_race_data`(28575)

### `_sport_label(doc, rk)`
- **위치**: `app.py:28714` (28714~28723)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_scoreboard_daily`(28704)

### `_esc(x)`
- **위치**: `app.py:28876` (28876~28877)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 4)**: `race_log_page`(28137) · `daily_cards_page`(28255) · `review_report_page`(28456) · `scoreboard_page`(28871)

### `_jp_result_backfill_once(date, verbose)`
- **위치**: `app.py:29005` (29005~29059)
- **설명**: [일본 NAR 결과 백필·1회] 당일 분석됐으나 결과 없는 '일본 NAR' 경주를 keiba RaceMarkTable 착순으로 *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_kra_backfill_loop`(27405) · `jp_results_backfill`(29063)

### `_fmt(ts)`
- **위치**: `app.py:29228` (29228~29229)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 5)**: `_pub_pace`(14965) · `kra_status`(29220) · `_hist_row_from_doc`(35138) · `_timeline_snap_tick`(36575) · `drop_stats`(331)

### `_kra_track_item_has_water(it)`
- **위치**: `app.py:29322` (29322~29334)
- **설명**: 응답 item 에 함수율(또는 경주로상태) 필드가 실제로 담겼는지. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_kra_track_op_resolve`(29337)

### `_kra_track_op_resolve(meet, rc_date, rc_no)`
- **위치**: `app.py:29337` (29337~29375)
- **설명**: [핵심] 유효한 API189 op 경로를 반환. 우선순위: *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_kra_track_info`(29680)

### `_kra_api_get(path, params, num_rows)`
- **위치**: `app.py:29379` (29379~29411)
- **설명**: B551015 계열 GET → (items[list], err[str|None]). 현재 활성 KRA 키로 조회. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 13)**: `_kra_extract_result`(27198) · `_kra_track_op_resolve`(29337) · `_kra_section_records`(29465) · `_kra_pass_ranks`(29557) · `_kra_jockey_changes`(29583) · `_kra_jk_winrate_by_no`(29614) … [+7]

### `_kra_jockey_winrate(it)`
- **위치**: `app.py:29414` (29414~29422)
- **설명**: 현직기수 item → (복승률=1+2착%, 삼착률=1+2+3착%). 출주 0이면 (None,None). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `_kra_jk_winrate_by_no`(29614) · `kra_jockey`(29925) · `_kra_jockey_map`(29950)

### `_kra_sec_num(v)`
- **위치**: `app.py:29438` (29438~29444)
- **설명**: 구간기록 문자열/숫자 → float(양수만 유효). 0·'-'·'0'은 결측으로 None. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_kra_section_records`(29465)

### `_kra_pass_positions(seq)`
- **위치**: `app.py:29447` (29447~29462)
- **설명**: [코너 통과순위 파싱] '(2,3,4)-(7,10),(6,5,8,9)=(1,11)' → {마번: 순위그룹}. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_kra_pass_ranks`(29557)

### `_kra_section_records(meet, rc_date, rc_no, force)`
- **위치**: `app.py:29465` (29465~29554)
- **설명**: [API37_1] 현재 출전마별 과거 구간기록 이력 → 말별 S1F/G1F 최고·평균 집계 + 선행력/추입력 지수. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `_triple_analyze`(10724) · `kra_section`(29102) · `_simulate_race_flow_kra`(29743)

### `_kra_jockey_changes(meet, rc_date, force)`
- **위치**: `app.py:29583` (29583~29611)
- **설명**: [API10_1] 해당 개최일 기수변경 목록 → 캐시(1시간). 반환 [{rcNo, chulNo, hrName, hrNo, jkBef*, jkAft*, reason, budam*}]. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 4)**: `_triple_analyze`(10724) · `_kra_auto_loop`(27272) · `kra_jockey_change`(29119) · `_kra_jockey_change_bonus`(29639)

### `_kra_jk_winrate_by_no(jk_no)`
- **위치**: `app.py:29614` (29614~29633)
- **설명**: 기수번호(jkNo) → 복승률(%). 현직기수 캐시(_form_ext_stats 아님, KRA API) 조회. 없으면 None. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_kra_jockey_change_bonus`(29639)

### `_kra_jockey_change_bonus(meet, rc_date, rc_no, chul_no)`
- **위치**: `app.py:29639` (29639~29665)
- **설명**: [API10_1 기수변경 보정] 경주 전 기수 교체 감지 → 승부의지/전력 보정. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_track_condition_by_water(water)`
- **위치**: `app.py:29668` (29668~29677)
- **설명**: [API189 함수율 기준 주로상태] 요구사항 임계: 0~3% 건조(양호)·4~8% 다습·9%+ 불량. (label, level 0~3). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_kra_track_info`(29680)

### `_kra_track_info(meet, rc_date, rc_no, force)`
- **위치**: `app.py:29680` (29680~29705)
- **설명**: [API189 경주로·함수율] op 경로를 자동탐색/캐시(.env KRA_TRACK_OP 또는 후보 자동발견)해 실측 조회. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 0)**: —

### `_kra_ai_results(meet, rc_date, force)`
- **위치**: `app.py:29708` (29708~29740)
- **설명**: [API155] AI학습용 경주결과 — 개최일 전체 경주 착순·배당·체중증감·거리. 경주번호별 그룹. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `kra_ai_results`(29129) · `kra_ai_results_ingest`(29140)

### `_simulate_race_flow_kra(meet, rc_date, rc_no, valid_nos, win_odds)`
- **위치**: `app.py:29743` (29743~29813)
- **설명**: [6단계 경주 전개 시뮬레이션] API6_1 코너순위 + API37_1 구간지수 통합. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_triple_analyze`(10724) · `kra_section`(29102)

### `_kra_coords_for(rk)`
- **위치**: `app.py:29816` (29816~29841)
- **설명**: raceKey → (meet, rc_date, rc_no) 추정. 우선 _KRA_WATCH(라이브), 없으면 rk 파싱. 실패 시 None. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_kra_jockey_map(force)`
- **위치**: `app.py:29950` (29950~29983)
- **설명**: 현직기수정보(전 경마장 1·2·3) → {기수명: {quinellaRate(1+2착), placeRate(1+2+3착), winRate(1착), rides, meet}}. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_jockey_rate`(29986)

### `_jockey_rate(name)`
- **위치**: `app.py:29986` (29986~29997)
- **설명**: 기수명 → 복승률 dict(정확 매칭 우선, 부분 매칭 폴백). 한국 기수만 매칭(일본 등은 None). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_bmed_before_card`(30216) · `_bmed_expert_cards`(30252)

### `_premium_alert(rk, an)`
- **위치**: `app.py:30074` (30074~30138)
- **설명**: [프리미엄 알림] 활성 경주 분석(an) → 등급별 알림 dict 또는 None(신호/타이밍 미충족). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `alerts_premium`(30163) · `bmed_view`(30283)

### `_premium_alert_format(a)`
- **위치**: `app.py:30141` (30141~30159)
- **설명**: 알림 dict → 카카오톡/푸시용 텍스트 포맷([4번] 명세). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `alerts_premium`(30163)

### `_race_post_epoch(rk)`
- **위치**: `app.py:30202` (30202~30213)
- **설명**: 스케줄(today_schedule)에서 raceKey 의 발주 epoch 찾기(없으면 None). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `bmed_view`(30283)

### `_bmed_before_card(rk, form)`
- **위치**: `app.py:30216` (30216~30237)
- **설명**: [B·사전분석] 배당 전 전적/기수 기반 주목마 TOP + 사전예상복승(전적 이중수렴). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `bmed_view`(30283)

### `_bmed_no_map(lst)`
- **위치**: `app.py:30240` (30240~30249)
- **설명**: [BMED] 리스트[{no,...}] → {int(no): item}. dict/비정형 입력도 안전 무시. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_bmed_expert_cards`(30252)

### `_bmed_expert_cards(an)`
- **위치**: `app.py:30252` (30252~30279)
- **설명**: [E·전문가 해석] 각 말 상세: 전적등급·각질·기수·현재배당·흐름. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `bmed_view`(30283)

### `_jra_num(s)`
- **위치**: `app.py:30390` (30390~30395)
- **설명**: netkeiba 배당 문자열 → float. ⚠ 확정배당에 **천 단위 콤마**가 온다(`"1,197.9"`). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_jra_fetch_odds`(30434)

### `_jra_combo_key(k)`
- **위치**: `app.py:30398` (30398~30405)
- **설명**: 조합키 → 마번 리스트. 복승 `"0102"`→[1,2] · 삼복승 `"010203"`→[1,2,3]. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_jra_fetch_odds`(30434)

### `_jra_post_info(race_id)`
- **위치**: `app.py:30408` (30408~30431)
- **설명**: race_id → (발주epoch, 경기장한글, 경주번호). shutuba 의 `RaceData01` 에 `HH:MM発走` 가 있다. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_jra_collect_once`(30492) · `_jra_result_once`(30693)

### `_jra_recent_private(rk)`
- **위치**: `app.py:30455` (30455~30489)
- **설명**: 🔴 [2026-08-01 · 승인 A로 **뒤집힘**] 중앙경마는 **netkeiba 우선 · 확장은 폴백**이다. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_jra_collect_once`(30492)

### `_jra_bg_loop()`
- **위치**: `app.py:30533` (30533~30556)
- **설명**: 🔴 **별도 스레드**(기존 `_multi_bg_loop` 과 분리). 7/30 에 3트랙 동시 개최로 사이클이 37초까지 *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 0)**: —  ⚠ 위험목록 함수 → 전수 나열

### `_jra_res_cells(tr)`
- **위치**: `app.py:30592` (30592~30594)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_jra_parse_result`(30606)

### `_jra_yen(s)`
- **위치**: `app.py:30597` (30597~30603)
- **설명**: `"2,550円"` → 2550. 🔴 천 단위 콤마 제거가 핵심. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_jra_parse_result`(30606)

### `_jra_result_once()`
- **위치**: `app.py:30693` (30693~30727)
- **설명**: 발주 + N분 지난 중앙경마 경주의 착순·확정배당을 수집. 반환 (저장건수, 미확정건수). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_jra_result_loop`(30730)

### `_jra_result_loop()`
- **위치**: `app.py:30730` (30730~30749)
- **설명**: 🔴 [2026-08-01] **sleep-first 로 만들지 않는다**(권대표 지시). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 0)**: —  ⚠ 위험목록 함수 → 전수 나열

### `_jra_venue_code(venue)`
- **위치**: `app.py:30771` (30771~30786)
- **설명**: 경마장명(한/일/영) → netkeiba 場코드('05' 등) 또는 None. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_jra_resolve_raceid`(30805)

### `_jra_race_list(ymd, force)`
- **위치**: `app.py:30789` (30789~30802)
- **설명**: 그날(ymd=YYYYMMDD) netkeiba 개최 race_id 집합. race_list_sub(서버렌더 fragment)에서 추출. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 5)**: `_jra_collect_once`(30492) · `_jra_result_once`(30693) · `_jra_resolve_raceid`(30805) · `jra_starters`(31023) · `_jra_preload_once`(38639)

### `_jra_resolve_raceid(venue, ymd, race_no)`
- **위치**: `app.py:30805` (30805~30814)
- **설명**: 경마장+개최일+경주번호 → netkeiba race_id(12자리) 또는 None. race_id에 場코드·경주번호가 내장. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `jra_starters`(31023)

### `_jra_past_cell(cell)`
- **위치**: `app.py:30862` (30862~30903)
- **설명**: 馬柱 Past 셀(과거 1경주) → {date,venue,placing,distance,surface,trackCond,fieldSize, *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_jra_parse_shutuba_past`(30906)

### `g(cls)`
- **위치**: `app.py:30866` (30866~30868)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_jra_past_cell`(30862) · `_build_full_prompt`(246)

### `cellraw(cls)`
- **위치**: `app.py:30933` (30933~30935)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_jra_parse_shutuba_past`(30906)

### `_jra_style(h)`
- **위치**: `app.py:30955` (30955~30964)
- **설명**: 각질 판정: netkeiba 표기(逃/先=선행+3 · 差/追=추격+5 · 自=평지) 우선, 없으면 통과순위 폴백. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_jra_build_form`(30967)

### `_jra_build_form(shutuba)`
- **위치**: `app.py:30967` (30967~31019)
- **설명**: 馬柱 파싱 결과 → 통합 전적 점수. base_form_score(착순 가중평균) + 각질/거리변화/부담/상3F 보정. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `jra_starters`(31023) · `_jra_preload_once`(38639)

### `_kra_load_history()`
- **위치**: `app.py:31188` (31188~31201)
- **설명**: kra_history.json 로드(파일 mtime 기준 캐시 — 30초 폴링마다 재파싱 방지). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 5)**: `_do_analyze`(991) · `_form_from_starters`(1560) · `_kra_recent_placings`(31204) · `kra_horse_summary`(31222) · `_form_ext_build`(31933)  ⚠ 위험목록 함수 → 전수 나열

### `_kra_recent_placings(name, hist)`
- **위치**: `app.py:31204` (31204~31219)
- **설명**: 마명 → KRA 실제 최근 착순 배열(최대 5, 최신순). 없으면 []. [전적→부진마 학습 연결] *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_form_from_starters`(1560)

### `kra_horse_summary(name, hist)`
- **위치**: `app.py:31222` (31222~31239)
- **설명**: 마명 → KRA 실제 과거기록 요약 문자열(분석 프롬프트 주입용). 기록 없으면 '' *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_do_analyze`(991)

### `place_points(p)`
- **위치**: `app.py:31282` (31282~31290)
- **설명**: 착순(int) → 점수. 1=100, 2=75, 3=50, 4=25, 5착 이하=0. 무효(기권/미출주 등)는 None. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `base_form_score`(31293)

### `_to_int(v)`
- **위치**: `app.py:31307` (31307~31312)
- **설명**: '1200', '1200M', '1,200m' → 1200. 숫자 없으면 None. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 14)**: `_det_horse_note`(943) · `_do_analyze`(991) · `_dist_bucket`(4447) · `_triple_analyze`(10724) · `_keiba_parse_horse_past`(26370) · `course_aptitude_bonus`(31323) … [+8]

### `_placings_of(horse)`
- **위치**: `app.py:31315` (31315~31319)
- **설명**: horse 에서 최근 착순 배열을 얻는다. recentPlacings 우선, 없으면 pastRaces[].placing. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `jockey_bonus`(31363) · `score_horses_raw`(32189) · `score_form`(32266)

### `course_aptitude_bonus(past_races, race)`
- **위치**: `app.py:31323` (31323~31347)
- **설명**: 현재 거리 ±100m 경험 +10 / 현재 코스(내·외) 경험 +10 / 현재 조건(급) 경험 +5. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `score_horses_raw`(32189)

### `top20_threshold(rates)`
- **위치**: `app.py:31351` (31351~31360)
- **설명**: 복승률 리스트의 상위 20% 경계값(80번째 백분위). 유효값 없으면 None. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `score_horses_raw`(32189)

### `jockey_bonus(horse, threshold)`
- **위치**: `app.py:31363` (31363~31378)
- **설명**: 기수 3개월 복승률 상위 20% +15 / 기수-마필 직전 경주 적중(같은 기수·3착 이내) +10. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `score_horses_raw`(32189)

### `_running_style(horse)`
- **위치**: `app.py:31382` (31382~31402)
- **설명**: 직전 3경주의 초반 포지션(선행/중단/후방)으로 각질 추정. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `_det_horse_note`(943) · `_gait_of`(31411) · `score_horses_raw`(32189)

### `_gait_of(horse)`
- **위치**: `app.py:31411` (31411~31438)
- **설명**: [1] 각질 4분류 정규화 → 선행/선입/추입/자유. 명시 gait/styleType > runningStyle > pastRaces position. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `_apply_pace_analysis`(31456) · `_simulate_race_flow_jp`(31608) · `_g`(31622)

### `_pace_scenario(pace, counts, form, nH)`
- **위치**: `app.py:31441` (31441~31453)
- **설명**: [6] 경주 전개 시나리오 텍스트(리스트). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_apply_pace_analysis`(31456)

### `_apply_pace_analysis(form, horse_count)`
- **위치**: `app.py:31456` (31456~31510)
- **설명**: [2~7] form 각질 편성 분석 → 페이스 예측 + 각질별 유불리 보정(paceBonus 가산·무삭제). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_simulate_race_flow_keirin(horses, line, tendency, valid_nos, win_odds)`
- **위치**: `app.py:31517` (31517~31605)
- **설명**: [경륜 특화①②·전개 시뮬레이션 (2026-07-19)] oddspark 출마표 전적(styleType·득점·결정수비율)과 *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_gscore(g)`
- **위치**: `app.py:31558` (31558~31559)
- **설명**: 그룹 정렬: 선두 선수 총점 높은 라인부터 *(주석(def 위))*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 0)**: —

### `_simulate_race_flow_jp(form, valid_nos, win_odds)`
- **위치**: `app.py:31608` (31608~31668)
- **설명**: [일본경마 전개 시뮬레이션] 각질·상3F로 선행경합→페이스→유리한 말→고배당 복병 예측. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_g(h)`
- **위치**: `app.py:31622` (31622~31624)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_recommend_basis`(7365) · `_simulate_race_flow_jp`(31608)

### `distance_change_bonus(horse, race, style)`
- **위치**: `app.py:31672` (31672~31685)
- **설명**: 이번 거리 vs 직전 경주 거리. 단축=스피드 유리 +5, 연장=추격형 +3·선행형 -3·미상 적성체크. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 4)**: `_det_horse_note`(943) · `_keiba_build_form`(26514) · `_jra_build_form`(30967) · `score_horses_raw`(32189)

### `_leader_jockeys(jstats, top_frac)`
- **위치**: `app.py:31689` (31689~31698)
- **설명**: jockeyStats(이름→통계)에서 복승권율(placeRate) 상위 top_frac 기수명 set. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `_korea_deterministic_overlay`(924) · `_do_analyze`(991) · `score`(32278)

### `jockey_change_bonus(horse, leader_names, rate_fn)`
- **위치**: `app.py:31701` (31701~31729)
- **설명**: [1] 기수 교체 감지. 직전 기수 vs 이번 기수. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 4)**: `_det_horse_note`(943) · `_keiba_build_form`(26514) · `_jra_build_form`(30967) · `score_horses_raw`(32189)

### `weight_change_bonus(horse)`
- **위치**: `app.py:31733` (31733~31742)
- **설명**: 이번 부담중량 vs 직전. 증가 -5, 감소 +5, 동일/미상 0. (부담중량=핸디캡, 마체중과 별개) *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 4)**: `_det_horse_note`(943) · `_keiba_build_form`(26514) · `_jra_build_form`(30967) · `score_horses_raw`(32189)

### `body_weight_change_bonus(cur_bw, past)`
- **위치**: `app.py:31753` (31753~31772)
- **설명**: [마체중 변화·신규] 이번 마체중(cur_bw) vs 직전 경주 마체중. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_keiba_build_form`(26514) · `_jra_build_form`(30967)

### `grade_bonus(cur_grade, past_grades)`
- **위치**: `app.py:31827` (31827~31846)
- **설명**: [2번] 등급 경험(최상위 하나) + 하락(상위→하위·강자 +15)/상승(하위→상위·적응중 -5). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `_keiba_build_form`(26514) · `_jra_build_form`(30967) · `score_horses_raw`(32189)

### `dist_exp_bonus(past_races, cur_dist)`
- **위치**: `app.py:31850` (31850~31861)
- **설명**: [3번] 당거리(정확히 같은 거리) 출전 경험. 있음 +10, 없음 0. 반환 (bonus, detail, experienced). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `_keiba_build_form`(26514) · `_jra_build_form`(30967) · `score_horses_raw`(32189)

### `dist_aptitude_bonus(past_races, cur_dist, tol)`
- **위치**: `app.py:31864` (31864~31881)
- **설명**: [거리 적성·신규] 이번 경주 거리(±tol m 유사거리) 전적의 복승권율(1~3착 비율)로 적성 등급. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_keiba_build_form`(26514) · `_jra_build_form`(30967)

### `bad_track_rate(past_races)`
- **위치**: `app.py:31884` (31884~31897)
- **설명**: [불량주로 성적·신규] pastRaces 의 condition(주로: 무거움/불량) 경주 복승권율(1·2착 %) 계산. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_form_from_starters`(1560) · `score_horses_raw`(32189)

### `_kra_race_placings(race)`
- **위치**: `app.py:31907` (31907~31919)
- **설명**: KRA 경주 1건 → rcTime 오름차순 착순 맵 {마번:착순}. rcTime 없는 말 제외. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_form_ext_build`(31933)

### `_date_diff_days(d1, d2)`
- **위치**: `app.py:31922` (31922~31930)
- **설명**: '20260620' 형식 두 날짜의 일수 차(d2-d1). 파싱 실패 시 None. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_form_ext_build`(31933)

### `_form_ext_build()`
- **위치**: `app.py:31933` (31933~32020)
- **설명**: KRA 이력 스캔 → {synergy, trainer, postPos} 통계. 표본 하한 적용(궁합2·조교사5·마번5). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_form_ext_stats`(32028)

### `_rate(pls)`
- **위치**: `app.py:31984` (31984~31986)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 4)**: `_dark_stats_recompute`(10493) · `_threshold_candidates`(17466) · `_recompute_learning_stats`(19031) · `_form_ext_build`(31933)

### `_infer_gait_from_history(horse_name)`
- **위치**: `app.py:32023` (32023~32025)
- **설명**: [한국 각질 추론] KRA 이력 stOrd(초반위치) 패턴 → 각질+신뢰도. 반환 {gait, conf, n} 또는 None. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_gait_of`(31411)

### `jockey_horse_synergy_bonus(jockey, horse_name)`
- **위치**: `app.py:32045` (32045~32060)
- **설명**: [2] 기수-말 궁합 — KRA 이력 조합 복승권율. A(40%+)+15 / B(20~40%)+5 / C(20%↓)-5 / 첫기승 표시. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `form_ext_stats_api`(29904) · `score_horses_raw`(32189)

### `post_position_ctx(meet, no)`
- **위치**: `app.py:32082` (32082~32091)
- **설명**: [4] 마번 유불리 — 경주장별 마번 복승률 vs 경주장 평균 편차. 반환 {rate, base, delta, n} 또는 None. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `form_ext_stats_api`(29904) · `score_horses_raw`(32189)

### `back_power_bonus(my_last3f, all_last3f)`
- **위치**: `app.py:32095` (32095~32109)
- **설명**: [3번] 상승3F(막판 뒷힘). 경주 내 상대: 상위30%(빠름) +15, 하위30%(느림) -5, 보통 0. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_apply_back_power`(26578)

### `_high_odds_verdict(win_odds, has_signal)`
- **위치**: `app.py:32113` (32113~32126)
- **설명**: [1·5번] 단승 배당 기준 고배당 제거. 999=무조건 / 100배+=확실 / 50~99=권장 / 30~49=신호없으면 제거. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_elimination`(4628)

### `special_flags(horse, race)`
- **위치**: `app.py:32130` (32130~32152)
- **설명**: 동일거리 2착 2회↑ → 삼복승필수 / 마체중 ±10kg↑ 경고 / 출전간격 3주↑ 주의 / 연속출전 피로도. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `score_horses_raw`(32189)

### `_upgrade(grade)`
- **위치**: `app.py:32159` (32159~32161)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `classify_grades`(32164)

### `score_horses_raw(race, horses, jockey_threshold, leader_names)`
- **위치**: `app.py:32189` (32189~32251)
- **설명**: 3-1~3-4: 마필별 baseScore/courseBonus/jockeyBonus/distanceBonus/jockeyChangeBonus/ *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `compute_horse_scores`(32254) · `combined_analyze`(32402)

### `_anomaly_by_no(race_key, horses_for_edge)`
- **위치**: `app.py:32292` (32292~32301)
- **설명**: odds_compute(전적 총점을 실력 확률로) 결과를 마번→이상감지 dict 로 정리. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `combined_analyze`(32402)

### `_pl_topk_prob(strengths, combo)`
- **위치**: `app.py:32352` (32352~32367)
- **설명**: Plackett-Luce: combo의 k마리가 정확히 상위 k착을 차지할 확률(순서 무관). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `build_bets`(32370)

### `build_bets(picks, scored, budget)`
- **위치**: `app.py:32370` (32370~32399)
- **설명**: 위치 기반 A/B/C/INS 픽으로 5개 베팅 슬롯 구성 → 비중·금액·손익분기·기대값. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `combined_analyze`(32402)

### `_pdf_md5(path)`
- **위치**: `app.py:32470` (32470~32479)
- **설명**: PDF 파일의 MD5 해시(재업로드 동일성 판정용). 실패 시 None. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `korea_start`(33217)

### `_pdf_cache_path(md5)`
- **위치**: `app.py:32482` (32482~32483)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_pdf_cache_load`(32486) · `_pdf_cache_save`(32500)

### `_korea_default_session()`
- **위치**: `app.py:32519` (32519~32523)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 4)**: `_korea_run_job`(32957) · `korea_start`(33217) · `korea_status`(33296) · `korea_session`(33304)

### `_render_region_b64(page, frac, target_w)`
- **위치**: `app.py:32544` (32544~32553)
- **설명**: PDF 페이지의 frac(x0,y0,x1,y1 비율) 영역을 target_w 픽셀 폭 PNG(base64)로 렌더. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 4)**: `_render_thumb`(32556) · `_render_band`(32557) · `_render_train`(32558) · `_render_full`(32559)

### `_render_thumb(page)`
- **위치**: `app.py:32556` (32556~32556)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_korea_run_job`(32957)

### `_render_band(page)`
- **위치**: `app.py:32557` (32557~32557)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_korea_extract_race`(32627)

### `_render_train(page)`
- **위치**: `app.py:32558` (32558~32558)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_korea_extract_race`(32627)

### `_render_full(page)`
- **위치**: `app.py:32559` (32559~32559)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_korea_run_job`(32957)

### `_korea_race_label(venue, race_no, distance)`
- **위치**: `app.py:32562` (32562~32566)
- **설명**: static/js/app.js 의 raceLabel 과 동일 — History 제목 매칭을 위해 형식 유지. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_korea_run_job`(32957)

### `_load_base_jockeys()`
- **위치**: `app.py:32569` (32569~32579)
- **설명**: 정적 기수 DB(static/data/jockeys.json)를 이름 키 dict 로 로드. PDF 추출값을 위에 덮어씀. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_korea_run_job`(32957)

### `_merge_jockey(stats, j)`
- **위치**: `app.py:32582` (32582~32594)
- **설명**: PDF '기수 기승현황표' 한 행을 stats 에 병합 (app.js mergeJockey 와 동일 계산). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_korea_run_job`(32957)

### `_extract_korea_post_time(text)`
- **위치**: `app.py:32600` (32600~32624)
- **설명**: 한국 PDF 요약 페이지 텍스트에서 발주시각 'HH:MM'(24h·2자리 정규화) 추출. 실패 시 None. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_korea_extract_race`(32627)

### `_korea_extract_race(doc, race, api_key)`
- **위치**: `app.py:32627` (32627~32651)
- **설명**: 요약 페이지 1장에서 메인표+조교표를 추출·병합해 출전마 리스트 반환 (app.js extractRaceFull). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `_korea_run_job`(32957) · `korea_reextract`(33324) · `_analyze_one`(33098)

### `_prerace_key(date, venue, race_no)`
- **위치**: `app.py:32678` (32678~32681)
- **설명**: 파일 안전 키: '2026-07-05_부산_3'. 경마장 미상은 '기타'. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_prerace_save_race`(32684)

### `_prerace_index_path()`
- **위치**: `app.py:32714` (32714~32715)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_prerace_index_update`(32718) · `_prerace_index_load`(32730)

### `_prerace_clear()`
- **위치**: `app.py:32768` (32768~32777)
- **설명**: data/prerace/ 전체 삭제('새 PDF 업로드' 초기화 시). 폴더 없으면 무시. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `korea_reset`(33310)

### `_korea_renumber_by_post(races)`
- **위치**: `app.py:32780` (32780~32826)
- **설명**: 🔴 [2026-08-07] 한국 경주번호를 **발주시각(fitz)** 으로 교정한다. 제자리 수정. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_korea_run_job`(32957)

### `_korea_run_job(gen, api_key)`
- **위치**: `app.py:32957` (32957~33178)
- **설명**: 백그라운드 분석 본체. 세션(disk)을 기준으로 진행하며, 이미 끝난 단계는 건너뛰어 '재개'를 지원. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 0)**: —

### `cancelled()`
- **위치**: `app.py:32959` (32959~32960)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `_korea_run_job`(32957) · `_analyze_one`(33098) · `_detect_batch`(32987)

### `_detect_batch(item)`
- **위치**: `app.py:32987` (32987~32995)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 0)**: —

### `_korea_start_job(api_key)`
- **위치**: `app.py:33181` (33181~33184)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `korea_start`(33217) · `_korea_maybe_resume`(33495)

### `_korea_pdf_date(txt, fallback)`
- **위치**: `app.py:33194` (33194~33213)
- **설명**: [2026-08-06 승인] PDF **원문에서 경주일**을 읽는다. 못 읽으면 fallback(오늘 아님·None 가능). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_korea_run_job`(32957) · `korea_start`(33217)

### `_korea_hist_file(date, venue, race_no)`
- **위치**: `app.py:33380` (33380~33383)
- **설명**: 날짜 하이픈 보존: 2026-07-03_서울_5R.json *(주석(본문))*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `korea_history_save`(33387)

### `_korea_maybe_resume()`
- **위치**: `app.py:33495` (33495~33502)
- **설명**: 서버 재시작 시 진행중이던 분석 자동 재개 (data/korea_session.json 기준). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_boot_background`(38998)

### `on_error(e)`
- **위치**: `app.py:33506` (33506~33518)
- **설명**: HTTP 예외(404/405 등)는 원래 상태코드를 보존 — 405가 500으로 둔갑하던 버그 수정 *(주석(본문))*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 0)**: —

### `_korea_schedule_post_epoch(rk, post_epoch)`
- **위치**: `app.py:33620` (33620~33647)
- **설명**: [수정3] 한국경마 경주의 발주 epoch(초)를 today_schedule 에 반영 → 타임 스냅샷(T-7/T-5) 작동. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_do_triple_ingest`(2768) · `kra_watch`(29183)

### `_post_time_epoch(hhmm, ymd)`
- **위치**: `app.py:33650` (33650~33660)
- **설명**: 'HH:MM' + ymd(YYYYMMDD) → 발주 epoch(서버 로컬). 실패 시 None. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 7)**: `_keirin_venue_current`(25724) · `kra_watch`(29183) · `_multi_race_list`(33686) · `_keirin_schedule`(33751) · `_nar_race_list`(34191) · `multi_keirin_schedule`(35478) … [+1]

### `_multi_post_time(op, sp, ymd, rno)`
- **위치**: `app.py:33666` (33666~33683)
- **설명**: [1번] 특정 경주 RaceList.do?raceNb=N 페이지에서 発走時間(HH:MM) 추출. 당일 캐시·실패 시 None(격리). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 0)**: —

### `_multi_race_list(op, sp, ymd)`
- **위치**: `app.py:33686` (33686~33708)
- **설명**: [1번] oddspark RaceList.do → [{raceNo, postTime, postEpoch}]. 경주번호는 목록 페이지에서, 発走時間은 경주별 페이지에서 병렬 취득. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_multi_schedule_fetch`(34223)

### `_keirin_post_time(jo, ymd, rno)`
- **위치**: `app.py:33714` (33714~33731)
- **설명**: [경륜 서버수집] 경주별 RaceList.do 페이지에서 発走時間(HH:MM) 추출. 당일 캐시·실패 None(격리). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_keirin_venue_current`(25724)

### `_keirin_venue_races(jo, ymd)`
- **위치**: `app.py:33734` (33734~33748)
- **설명**: [경륜 서버수집] joCode의 경륜장명 + 경주번호 목록. raceNo=1 공개 출주표 title에서 경륜장명, *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_keirin_venue_current`(25724) · `_keirin_schedule`(33751)

### `_keirin_schedule(ymd, force)`
- **위치**: `app.py:33751` (33751~33787)
- **설명**: [경륜 서버 직접 수집] oddspark 경륜 홈(로그인 불필요·공개)에서 오늘 개최 경륜장(joCode) 추출 → *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `keirin_current`(25754) · `_multi_schedule_fetch`(34223)

### `_kra_schedule(ymd, force)`
- **위치**: `app.py:33798` (33798~33830)
- **설명**: [한국경마 서버 감지] 오늘 개최 KRA 경주 → [{venue, meet, sport:'korea', races:[{raceNo, horses, postTime:None}]}]. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_multi_schedule_fetch`(34223)

### `_nar_date_param(ymd)`
- **위치**: `app.py:33880` (33880~33881)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `_nar_odds_url`(33884) · `_nar_autocollect_form`(34079) · `_nar_race_list`(34191)

### `_nar_autocollect_form(rk, baba, ymd, rno)`
- **위치**: `app.py:34079` (34079~34111)
- **설명**: [南関東 전적 자동수집] keiba.go.jp DebaTable → starters_store. `_keiba_autocollect_form` 의 NAR판. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `_nar_form_fallback`(34125) · `_multi_collect_one`(34659) · `_nar_preload_once`(38506)

### `_nar_form_fallback(rk, ymd, rno, primary_ok)`
- **위치**: `app.py:34125` (34125~34188)
- **설명**: oddspark 전적이 실패한 경주만 keiba.go.jp DebaTable 로 한 번 더 시도한다. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_multi_collect_one`(34659)

### `_nar_race_list(baba, ymd)`
- **위치**: `app.py:34191` (34191~34203)
- **설명**: keiba.go.jp RaceList(당일 메뉴) → [{raceNo, postTime, postEpoch}]. 미개최면 []. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_nar_schedule`(34206) · `_nar_preload_once`(38506)

### `_nar_schedule(ymd)`
- **위치**: `app.py:34206` (34206~34220)
- **설명**: 南関東 4장 중 오늘 개최분만 트랙 레코드로 반환(미개최=제외). oddspark 커버 경마장과 중복 없음. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_multi_schedule_fetch`(34223)

### `_multi_key(venue, race_no)`
- **위치**: `app.py:34270` (34270~34272)
- **설명**: [岐阜·기후 중복 통합] 저장키의 경마장명을 표준(한글)으로 정규화 → 岐阜/기후가 한 키로 병합(중복 저장 방지). *(주석(본문))*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 10)**: `_triple_analyze`(10724) · `_multi_collect_one`(34659) · `multi_dashboard`(35542) · `_korea_kakao_targets`(36527) · `_auto_pred_tick`(36865) · `_collect_cycle_begin`(37127) … [+4]

### `_frozen_capture(rk, doc, rec_rows)`
- **위치**: `app.py:34293` (34293~34348)
- **설명**: 마감 시점 확정값을 3단 폴백으로 복원. 반환 (frozen dict, src) — 실패 시 (None, 사유). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_build_analysis_log`(16347)

### `_freeze_note(rk, ok, src_or_reason)`
- **위치**: `app.py:34351` (34351~34365)
- **설명**: 동결 성공/실패 1건 적재. **완전 방어적** — 실패해도 저장 흐름 무영향. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_build_analysis_log`(16347)

### `_amedas_map()`
- **위치**: `app.py:34389` (34389~34402)
- **설명**: 표준키 → 관측지점 매핑표. 파일이 바뀌면 자동 재적재(개최지 추가 대비). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_amedas_wind`(34429)

### `_amedas_wind(venue_or_rk)`
- **위치**: `app.py:34429` (34429~34477)
- **설명**: 경기장(표준키·별칭·한자 무관) → 바람 관측값. 없으면 None. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_wind_for_forecast`(34592)

### `_v(k)`
- **위치**: `app.py:34457` (34457~34459)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_amedas_wind`(34429)

### `_amedas_note(rk, w)`
- **위치**: `app.py:34483` (34483~34508)
- **설명**: 관측값 1건 적재(검증용 건수 집계). **완전 방어적** — 실패해도 흐름 무영향. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_wind_for_forecast`(34592)

### `_note_module_error(name, err)`
- **위치**: `app.py:34519` (34519~34520)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 0)**: —

### `_forecast_boot_state()`
- **위치**: `app.py:34542` (34542~34549)
- **설명**: [부팅 상태 노출 (2026-07-31)] 다른 Gemini 기능은 배너에 상태가 찍히는데 예측만 없었다. *(주석(def 위))*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 0)**: —

### `_boot_flag_lines()`
- **위치**: `app.py:34560` (34560~34575)
- **설명**: `*_ENABLED` 환경 플래그를 **자동 수집**해 배너 줄로 만든다. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 0)**: —

### `_boot_module_lines()`
- **위치**: `app.py:34578` (34578~34589)
- **설명**: 선택 모듈 로드 실패를 **크게** 출력. 체크리스트(D7)도 같은 값을 본다. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 0)**: —

### `_wind_for_forecast(rk)`
- **위치**: `app.py:34592` (34592~34600)
- **설명**: 예측용 바람 1건 조회 + 관측 적재. 실패해도 None 만 돌려주고 예측은 계속된다. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_forecast_after_form`(34603)

### `_forecast_after_form(rk, is_cycle)`
- **위치**: `app.py:34603` (34603~34630)
- **설명**: [Phase A] 전적 확보 직후 1회 예측. **완전 방어적** — 여기서 raise 되지 않는다. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_multi_collect_one`(34659)

### `_multi_sport_of(rec)`
- **위치**: `app.py:34955` (34955~34970)
- **설명**: [통합·4번] rec의 sport/category → 대시보드 종목 그룹(horse|central|cycle|boat|korea). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_multi_card`(35366) · `multi_dashboard`(35542)

### `_card_conf_value(conf)`
- **위치**: `app.py:34973` (34973~34986)
- **설명**: [확신도 표시 수정] 카드에 표시할 확신도 스칼라 추출. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 5)**: `_triple_analyze`(10724) · `_multi_card`(35366) · `_kakao_rich_message`(36119) · `_kakao_notify_race`(36318) · `_timeline_extract`(36450)

### `_live_exact_hit(key, top3, horse_count)`
- **위치**: `app.py:34989` (34989~35075)
- **설명**: [적중 표시 정직화 (2026-07-19)] '라이브에서 실제 화면에 표시된 추천'(analysis_log의 corePicks· *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 5)**: `_build_race_result`(16955) · `_keirin_result_backfill_once`(28021) · `keirin_today_stats`(28638) · `_scoreboard_daily`(28704) · `_race_result_summary`(35302)

### `_combset(pred)`
- **위치**: `app.py:35029` (35029~35040)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_live_exact_hit`(34989)

### `_valid(c)`
- **위치**: `app.py:35050` (35050~35055)
- **설명**: [로그 오염 방어] 출주 두수 초과 마번 조합(타 경주 혼입 — 경륜 7명에 1+10 등)은 판정 제외 *(주석(본문))*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_live_exact_hit`(34989)

### `_gemini_latest_map(ttl)`
- **위치**: `app.py:35085` (35085~35113)
- **설명**: logs/gemini_review/*.json → {raceKey: {status, issues, summary, time}} 최신본 1건씩. 60초 캐시. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_results_history_rows`(35172) · `_multi_card`(35366)

### `_log_polluted(doc)`
- **위치**: `app.py:35116` (35116~35135)
- **설명**: [마감 후 오염 감지] '🔒 마감 확정'(closed) 행의 복승 명단 vs 현재 표시 명단(displayedCombos)이 *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_hist_row_from_doc`(35138)

### `_hist_row_from_doc(doc, race_id)`
- **위치**: `app.py:35138` (35138~35169)
- **설명**: 분석 로그 1건 → 결과기록 카드 1행. (Gemini 는 캐시가 매번 최신으로 덮으므로 여기선 제외) *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_results_history_rows`(35172)

### `_results_history_rows(ttl)`
- **위치**: `app.py:35172` (35172~35216)
- **설명**: [결과기록 전면개편] data/analysis_log/ → 카드 행 목록(최신순). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `results_history`(35256) · `_multi_card`(35366)

### `_race_result_summary(key)`
- **위치**: `app.py:35302` (35302~35358)
- **설명**: [카드 적중 표시 (2026-07-19)] 결과 파일 → {top3, hit(복승 정확), trioHit(삼복승 정확), subHit, *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `race_log_page`(28137) · `_multi_card`(35366) · `multi_race_detail`(35842)

### `_multi_card(key, rec)`
- **위치**: `app.py:35366` (35366~35474)
- **설명**: [3번] 카드 요약: analyze(기존 _triple_analyze 재사용·읽기전용) → 유력마TOP3·핵심신호·확신도·마감남은초·색상. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `multi_dashboard`(35542)

### `_cut_tier_candidates(key, doc, cp)`
- **위치**: `app.py:35635` (35635~35759)
- **설명**: 컷에 걸려 화면에서 사라진 조합에 등급을 붙여 돌려준다(표시 전용·읽기 전용). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `triple_analyze`(15132) · `_race_card_timeline`(19280) · `_rec_trail_for_detail`(35762)  ⚠ 위험목록 함수 → 전수 나열

### `_rk_sum(x)`
- **위치**: `app.py:35745` (35745~35757)
- **설명**: 💎 를 먼저, 그 안에서는 **전적 순위가 좋은 순** → 같으면 배당 높은 순. *(주석(def 위))*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_cut_tier_candidates`(35635)

### `_rec_trail_for_detail(key)`
- **위치**: `app.py:35762` (35762~35838)
- **설명**: [추천 내역 상세 (2026-07-21 권대표 요청)] 카드 상세에 '실제 표시된 추천 + 생성됐지만 노출 제외된 *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `multi_race_detail`(35842)

### `_slim(lst)`
- **위치**: `app.py:35789` (35789~35805)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_rec_trail_for_detail`(35762)

### `_combos(key)`
- **위치**: `app.py:35987` (35987~35995)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_kakao_anchor_log`(35982)

### `_star(n)`
- **위치**: `app.py:36126` (36126~36127)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_kakao_rich_message`(36119)

### `_timeline_snap_path(rk)`
- **위치**: `app.py:36446` (36446~36447)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 5)**: `_triple_analyze`(10724) · `timeline_snapshot_get`(22643) · `_frozen_capture`(34293) · `_timeline_snap_save`(36478) · `_timeline_snap_apply_result`(36727)

### `_timeline_extract(an)`
- **위치**: `app.py:36450` (36450~36475)
- **설명**: 스냅샷용 요약 추출 — main(복승 메인 조합)·dark_horse(복병 1·2순위)·odds(메인 조합 배당). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_timeline_snap_save`(36478)

### `_is_korea_rk(rk)`
- **위치**: `app.py:36556` (36556~36560)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_timeline_snap_tick`(36575)

### `_timeline_phase_of(left)`
- **위치**: `app.py:36568` (36568~36572)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_timeline_snap_tick`(36575)

### `_timeline_snap_tick(races, now, db)`
- **위치**: `app.py:36575` (36575~36724)
- **설명**: [bg 훅] 각 경주의 잔여시간으로 phase 판정 → 미저장 phase 스냅샷 저장. T-7·T-5는 카카오 알림 트리거. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_auto_pred_tick`(36865)

### `_timeline_snap_apply_result(rk, top3)`
- **위치**: `app.py:36727` (36727~36757)
- **설명**: [결과] 각 스냅샷 적중(T7_hit/T5_hit) + 변경마 우승 판정 → 파일 갱신 + 통계 재계산. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_apply_result_learning`(20801)

### `_hit(main)`
- **위치**: `app.py:36740` (36740~36742)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `_snapshot_timing_learn`(3928) · `_pace_log_record`(10142) · `_timeline_snap_apply_result`(36727)

### `_r(v)`
- **위치**: `app.py:36785` (36785~36786)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_timeline_stats_recompute`(36760) · `signal_place_stats`(248)

### `_auto_pred_fname(rk)`
- **위치**: `app.py:36805` (36805~36808)
- **설명**: raceKey → YYYY_MM_DD_경주장_경주번호.json (경로조작 방어). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_auto_pred_save`(36811) · `_auto_pred_compare`(36837)

### `_auto_pred_compare(rk)`
- **위치**: `app.py:36837` (36837~36862)
- **설명**: [결과 대조] 실제 착순(results_store, 확장/일괄 수집분)이 있으면 예상과 대조해 파일에 기록. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_auto_pred_tick`(36865)

### `_auto_pred_tick(sched, now)`
- **위치**: `app.py:36865` (36865~36972)
- **설명**: [자동 예상·연속 전환] bg 루프에서 매 틱 호출 — ①마감5분전 예상 저장 ②결과 대조(+15분) ③현재/다음 상태 갱신. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_multi_bg_loop`(37027)

### `_multi_bg_loop()`
- **위치**: `app.py:37027` (37027~37124)
- **설명**: [1·2번] 백그라운드: 스케줄 30분 갱신 + 발주 임박 경주 배당 주기 수집(실패 격리·서버 죽지 않음). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 0)**: —  ⚠ 위험목록 함수 → 전수 나열

### `esc(s)`
- **위치**: `app.py:37530` (37530~37532)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `picks_diff_page`(37521) · `chips`(37534)

### `chips(items, cls)`
- **위치**: `app.py:37534` (37534~37537)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `picks_diff_page`(37521)

### `_hk_done_day()`
- **위치**: `app.py:37668` (37668~37672)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_start_health_kakao_scheduler`(37641) · `_loop`(37684)

### `_loop()`
- **위치**: `app.py:37684` (37684~37696)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 0)**: —  ⚠ 위험목록 함수 → 전수 나열

### `_loop()`
- **위치**: `app.py:37756` (37756~37770)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 0)**: —  ⚠ 위험목록 함수 → 전수 나열

### `_midcheck_procs()`
- **위치**: `app.py:37835` (37835~37849)
- **설명**: app.py 파이썬 프로세스 수. 정상은 부모+자식 **정확히 2개**(Flask 리로더). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_midcheck_collect`(38172)

### `_midcheck_savefail()`
- **위치**: `app.py:37852` (37852~37881)
- **설명**: 오늘 로그의 저장 실패(WinError) 건수. 실패면 None. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_midcheck_collect`(38172)

### `_midcheck_gaps()`
- **위치**: `app.py:37884` (37884~37903)
- **설명**: `data/collect_gaps/<날짜>.json` 에서 (수집 공백 건수, JSON 손상 건수). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_midcheck_collect`(38172)

### `_mc_q_shape(q)`
- **위치**: `app.py:37906` (37906~37930)
- **설명**: 복승 배당에서 (조합 수, 최대 마번). 실패면 (None, None). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_midcheck_diverge`(37933)

### `_midcheck_diverge()`
- **위치**: `app.py:37933` (37933~37968)
- **설명**: 오늘 두 소스(oddspark ↔ 사설)가 **다른 경주를 보고 있는** 경주 수. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_midcheck_collect`(38172)

### `_midcheck_accum()`
- **위치**: `app.py:38047` (38047~38130)
- **설명**: 오늘 축적 지표(완전 읽기 전용). 실패해도 예외를 올리지 않는다. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_midcheck_collect`(38172)

### `_midcheck_accum_delta(today)`
- **위치**: `app.py:38133` (38133~38169)
- **설명**: 어제 대비 증감. 🔴 읽기 실패를 빈 값으로 삼키지 않는다(원칙 9) — 실패면 None. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_midcheck_collect`(38172)  ⚠ 위험목록 함수 → 전수 나열

### `_midcheck_abnormal(f)`
- **위치**: `app.py:38215` (38215~38239)
- **설명**: 이상 사유 목록(빈 리스트면 정상). 14:00 침묵 해제 판정에 쓴다. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_midcheck_text`(38242) · `_midcheck_run`(38373)

### `_midcheck_text(slot, f, prev_stamp)`
- **위치**: `app.py:38242` (38242~38370)
- **설명**: 카카오 본문 — 짧게. ⚠ 발송 코어는 건드리지 않는다(조립만). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_midcheck_run`(38373)  ⚠ 위험목록 함수 → 전수 나열

### `_gv(key)`
- **위치**: `app.py:38254` (38254~38258)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_midcheck_text`(38242)

### `_one(key, label)`
- **위치**: `app.py:38275` (38275~38279)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_midcheck_text`(38242)

### `_d(key)`
- **위치**: `app.py:38332` (38332~38333)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_midcheck_text`(38242)

### `_midcheck_run(slot, force, dry)`
- **위치**: `app.py:38373` (38373~38416)
- **설명**: 한 슬롯을 1회 실행. 반환 {ok, sent, slot, reason, text}. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `_start_midcheck_scheduler`(38419) · `api_midcheck`(38462) · `_loop`(38426)

### `_start_midcheck_scheduler()`
- **위치**: `app.py:38419` (38419~38458)
- **설명**: 하루 3회 정시 점검. 🔴 sleep-first 아님 — 60초 폴링 + 파일 스탬프. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_boot_background`(38998)  ⚠ 위험목록 함수 → 전수 나열

### `_loop()`
- **위치**: `app.py:38426` (38426~38453)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 0)**: —  ⚠ 위험목록 함수 → 전수 나열

### `_nar_preload_venues()`
- **위치**: `app.py:38492` (38492~38503)
- **설명**: {k_babaCode: 한글 대표 경마장명} — 🔴 **`_JP_BABA_CODE` 에서 파생**한다. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_nar_preload_once`(38506) · `nar_preload_api`(38801)

### `_nar_preload_once(ymd, max_races)`
- **위치**: `app.py:38506` (38506~38574)
- **설명**: 개최일 오전에 그날 NAR 전 경주 전적을 미리 받는다. → 통계 dict *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `_start_nar_preload_scheduler`(38577) · `nar_preload_api`(38801) · `_loop`(38605)

### `_done_day()`
- **위치**: `app.py:38589` (38589~38593)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 4)**: `_start_nar_preload_scheduler`(38577) · `_start_jra_preload_scheduler`(38741) · `_loop`(38605) · `_loop`(38765)

### `_loop()`
- **위치**: `app.py:38605` (38605~38616)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 0)**: —  ⚠ 위험목록 함수 → 전수 나열

### `_jra_preload_once(ymd, max_races)`
- **위치**: `app.py:38639` (38639~38738)
- **설명**: 개최일 오전에 그날 중앙(JRA) 전 경주 전적을 미리 받는다. → 통계 dict *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `_start_jra_preload_scheduler`(38741) · `jra_preload_api`(38784) · `_loop`(38765)

### `_done_day()`
- **위치**: `app.py:38749` (38749~38753)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 4)**: `_start_nar_preload_scheduler`(38577) · `_start_jra_preload_scheduler`(38741) · `_loop`(38605) · `_loop`(38765)

### `_loop()`
- **위치**: `app.py:38765` (38765~38776)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 0)**: —  ⚠ 위험목록 함수 → 전수 나열

### `_snapshot_shortage_check(sched, now)`
- **위치**: `app.py:38852` (38852~38901)
- **설명**: [수집 공백 경보 (2026-07-29)] 발주 3~15분 지난 경주의 배당 스냅샷 수가 SNAPSHOT_MIN_WARN 미만이면 *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_multi_bg_loop`(37027)  ⚠ 위험목록 함수 → 전수 나열

### `_multi_bg_watchdog()`
- **위치**: `app.py:38904` (38904~38916)
- **설명**: [안정화 (2026-07-19)] bg 수집 루프 워치독 — 하트비트가 180초+ 멈추면(스레드 사망·교착) 루프 재기동. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 0)**: —  ⚠ 위험목록 함수 → 전수 나열

### `_start_multi_race_bg()`
- **위치**: `app.py:38919` (38919~38947)
- **설명**: [6번] 다중 경주 백그라운드 시작(실패해도 서버·단일 모드 무영향). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_boot_background`(38998)

### `_startup_date_reset()`
- **위치**: `app.py:38950` (38950~38992)
- **설명**: [서버 시작 시 날짜 자동 초기화] 어제(오늘 아닌 날짜) 데이터를 자동 정리 → '어제 경주 잔존' 방지. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_multi_bg_loop`(37027) · `_boot_background`(38998)

### `_dstr(t)`
- **위치**: `app.py:38956` (38956~38957)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_startup_date_reset`(38950)

### `_boot_background()`
- **위치**: `app.py:38998` (38998~39032)
- **설명**: [24시간 자동수집·백업·학습] 백그라운드 작업 1회 기동(멱등). 로컬(__main__)·gunicorn(모듈로드) 공용. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 0)**: —

### `_flag(name, default)`
- **위치**: `gemini_reviewer.py:29` (29~31)
- **설명**: 환경변수 우선 · 미설정 시 기본값. '1/true/yes/on' 만 켜짐으로 본다. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `gemini_review_enabled`(34) · `gemini_kakao_enabled`(39)

### `_gemini_api_key()`
- **위치**: `gemini_reviewer.py:45` (45~46)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `review_async`(440) · `_run`(449)

### `_mask(text, key)`
- **위치**: `gemini_reviewer.py:49` (49~52)
- **설명**: 로그/예외 문자열에 API 키가 노출되지 않도록 마스킹. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `review_async`(440) · `_run`(449)

### `_fmt_combo(lst)`
- **위치**: `gemini_reviewer.py:54` (54~63)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_build_prompt`(65)

### `_build_prompt(rk, final_q, final_t, special_q, line_pairs, strong_signals, fav_axis, strong_axis, drops, cur_mb)`
- **위치**: `gemini_reviewer.py:65` (65~89)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `review_async`(440) · `_run`(449)

### `_pair_key(k)`
- **위치**: `gemini_reviewer.py:99` (99~103)
- **설명**: 조합 키를 '1+2' 문자열로 정규화((1,2) 튜플·'1+2'·[1,2] 모두 허용). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_fmt_odds_map`(131)

### `_as_odds_items(m)`
- **위치**: `gemini_reviewer.py:106` (106~128)
- **설명**: [형식 방어 2026-07-28] 배당 자료가 dict 와 list 두 형식으로 들어온다. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_fmt_odds_map`(131) · `_build_full_prompt`(246)

### `_fmt_odds_map(m, limit, asc)`
- **위치**: `gemini_reviewer.py:131` (131~142)
- **설명**: 배당 맵/목록 → '1+2:3.4 1+3:5.6 ...' (배당 오름차순). limit=None 이면 전량. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_fmt_timeline`(155) · `_build_full_prompt`(246)

### `_fmt_json_block(obj, title)`
- **위치**: `gemini_reviewer.py:145` (145~152)
- **설명**: 구조가 제각각인 분석 산출물은 JSON 원문 그대로 전달(정보 손실 0). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_build_full_prompt`(246)

### `_fmt_timeline(tl)`
- **위치**: `gemini_reviewer.py:155` (155~164)
- **설명**: 배당 시계열 전량 — 스냅샷마다 전 조합 배당. 급락 판정의 원재료라 절대 요약하지 않는다. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_build_full_prompt`(246)

### `_logic_source()`
- **위치**: `gemini_reviewer.py:206` (206~243)
- **설명**: app.py 에서 판단 로직 함수들의 실제 소스를 추출(줄번호 포함). 실패해도 분석은 계속된다. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_build_full_prompt`(246)

### `_build_full_prompt(ctx)`
- **위치**: `gemini_reviewer.py:246` (246~359)
- **설명**: [전체자료] 로직 소스 + 배당판 전량 + 시계열 전량 + 전적 + 판단근거 + 최종추천(절단 없음). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `_build_full_prompt`(246) · `review_async`(440) · `_run`(449)

### `_resolve_app_module()`
- **위치**: `gemini_reviewer.py:378` (378~392)
- **설명**: 실행 중인 app 모듈을 반환. `python app.py` 는 __main__, gunicorn 은 app 으로 로드된다. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_send_kakao`(395)

### `review_async(rk, final_q, final_t, special_q, line_pairs, strong_signals, fav_axis, strong_axis, drops, cur_mb, ctx)`
- **위치**: `gemini_reviewer.py:440` (440~525)
- **설명**: ctx 를 주면 [전체자료 모드](배당판·시계열·전적·판단근거 전량)로 분석한다. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 0)**: —

### `_run()`
- **위치**: `gemini_reviewer.py:449` (449~524)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 0)**: —

### `_combo_set(v)`
- **위치**: `review_engine.py:41` (41~49)
- **설명**: '1+4' / [1,4] / '1-4' → frozenset({1,4}) *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 6)**: `_displayed_sets`(52) · `_signal_horses`(64) · `_history_combos`(93) · `_locked_quinella_odds`(111) · `_fix_connector_combos`(409) · `replay_day`(478)

### `_displayed_sets(log_doc)`
- **위치**: `review_engine.py:52` (52~61)
- **설명**: 마감 동결 표시 조합(판정 대상) → (복승 set 목록, 삼복승 set 목록). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `classify_race`(134) · `replay_day`(478)

### `_signal_horses(log_doc)`
- **위치**: `review_engine.py:64` (64~90)
- **설명**: 이 경주에서 시스템이 '알고 있었던' 말 전부(유력마·복병·신호·확신도·고배당후보). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `classify_race`(134)

### `_history_combos(log_doc)`
- **위치**: `review_engine.py:93` (93~108)
- **설명**: 추천 이력에 등장했던 복승·삼복승 조합 전부(시점 무관). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `classify_race`(134)

### `_locked_quinella_odds(log_doc, combo_set)`
- **위치**: `review_engine.py:111` (111~121)
- **설명**: 표시(잠금) 시점의 해당 복승 배당 — rec_history 마지막 quinellas 스냅샷에서. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `classify_race`(134)

### `_is_tainted(doc)`
- **위치**: `review_engine.py:124` (124~131)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `classify_race`(134) · `replay_day`(478)

### `classify_race(doc, log_doc)`
- **위치**: `review_engine.py:134` (134~184)
- **설명**: 한 경주 분류. 반환 dict(type, hit, detail, slippage 등) — 저장은 호출부. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `sweep`(187)

### `_hms_sec(s)`
- **위치**: `review_engine.py:240` (240~245)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 4)**: `signal_place_stats`(248) · `replay_day`(478) · `_mb_of`(559) · `_kh_at`(280)

### `_kh_at(min_mb)`
- **위치**: `review_engine.py:280` (280~293)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `signal_place_stats`(248)

### `_r(d, extra)`
- **위치**: `review_engine.py:320` (320~325)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_timeline_stats_recompute`(36760) · `signal_place_stats`(248)

### `_horse_min(qmap)`
- **위치**: `review_engine.py:337` (337~348)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `drop_stats`(331)

### `_band(dr)`
- **위치**: `review_engine.py:350` (350~351)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_reversal_strength_stats`(24552) · `drop_stats`(331)

### `_fmt(t)`
- **위치**: `review_engine.py:392` (392~395)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 5)**: `_pub_pace`(14965) · `kra_status`(29220) · `_hist_row_from_doc`(35138) · `_timeline_snap_tick`(36575) · `drop_stats`(331)

### `_sport_of(doc, rk, keirin_re)`
- **위치**: `review_engine.py:402` (402~406)
- **설명**: ══════════ [2] 리플레이 — 정책별 가상 성적(전략 검증 1호) ══════════ *(주석(def 위))*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `replay_day`(478)

### `_fix_connector_combos(log_doc, limit)`
- **위치**: `review_engine.py:409` (409~457)
- **설명**: [fix_connectors 계열] favAxis 2두 × 연결마(keyHorses[2:] + confTop1) 복승 조합 집합. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `replay_day`(478)

### `_c_odds(_c)`
- **위치**: `review_engine.py:448` (448~451)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 0)**: —

### `replay_day(date, stake, keirin_re)`
- **위치**: `review_engine.py:478` (478~956)
- **설명**: [2] 하루치를 정책별로 재생 — displayedCombos·공식 확정배당 기준 가상 성적. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 0)**: —

### `_book(p, use_q, use_t, qh, th)`
- **위치**: `review_engine.py:526` (526~540)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `replay_day`(478)

### `_mb_of(_e)`
- **위치**: `review_engine.py:559` (559~566)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `replay_day`(478) · `_freeze_sets`(568)

### `_freeze_sets(min_mb)`
- **위치**: `review_engine.py:568` (568~599)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `replay_day`(478)

### `_qsig(snap)`
- **위치**: `tools/dedupe_odds_snapshots.py:43` (43~59)
- **설명**: 복승 배당 내용의 서명 — dict/list 양쪽 형식을 정규화(app.py `_as_qmap` 과 같은 취지). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `_key`(62)

### `_key(snap)`
- **위치**: `tools/dedupe_odds_snapshots.py:62` (62~67)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `dedupe_snapshots`(75) · `main`(99)

### `_n_anom(snap)`
- **위치**: `tools/dedupe_odds_snapshots.py:70` (70~72)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `dedupe_snapshots`(75) · `main`(99)

### `dedupe_snapshots(snaps)`
- **위치**: `tools/dedupe_odds_snapshots.py:75` (75~89)
- **설명**: 반환 (남길 리스트, 제거 수). 원본 순서를 보존한다. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `main`(99)

### `main()`
- **위치**: `tools/dedupe_odds_snapshots.py:99` (99~195)
- **설명**: ⚠ 서버가 라이브로 같은 파일을 쓰고 있다. 최근 갱신 파일을 건드리면 *(주석(본문))*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 0)**: —

### `_mark_pending_today(items)`
- **위치**: `tools/health_check.py:63` (63~88)
- **설명**: 🔴 당일 의존 + **아직 측정 못 함(ok is None)** → `⏳미확정` 으로 재분류. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `build_checklist`(1006)

### `_mk(id, area, name, denominator, current, target, ok, n, note, reason)`
- **위치**: `tools/health_check.py:91` (91~97)
- **설명**: 항목 1건. `denominator` 는 필수다 — 분모를 못 적으면 그 항목은 측정된 것이 아니다. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 21)**: `_win_exacta_reversal`(5901) · `check_snapshot_coverage`(114) · `check_save_failures_fixed`(198) · `check_save_failures_unfixed`(213) · `check_schema_contract`(226) · `check_schema_drift`(282) … [+15]

### `_distinct_ticks(doc)`
- **위치**: `tools/health_check.py:100` (100~109)
- **설명**: 같은 (초, src) 를 1틱으로 센다 — 2중 기록(2026-07-30 수정분) 이전 데이터 보정. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `check_snapshot_coverage`(114)

### `_today_logs()`
- **위치**: `tools/health_check.py:157` (157~170)
- **설명**: 당일 stdout 로그 파일들(회전본 포함). 회전본은 `server_stdout.log.<YYYYMMDD_HHMMSS>`. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `_count_save_failures`(173) · `check_forecast_discard`(411)

### `_red_field_rates()`
- **위치**: `tools/health_check.py:241` (241~279)
- **설명**: 🔴 필드 보유율을 **당일분 / 누적**으로 각각 산출. 반환 (day, cum, day_rows, cum_rows, note). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `check_schema_drift`(282)

### `_terminated(idn, title, denom, basis)`
- **위치**: `tools/health_check.py:405` (405~408)
- **설명**: 종결 항목을 **분모에 남긴 채** ok=None 으로 돌려준다(미측정과 사유로 구분). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 3)**: `check_forecast_discard`(411) · `check_forecast_vs_market`(449) · `check_forecast_highodds`(501)

### `_med(v)`
- **위치**: `tools/health_check.py:545` (545~547)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `check_forecast_highodds`(501)

### `_is_settled()`
- **위치**: `tools/health_check.py:641` (641~663)
- **설명**: 그날 경주가 **다 끝났는가**. 반환 (확정여부, 사유). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 2)**: `check_payout_coverage`(666) · `check_measurable_today`(843)

### `_scan_daemons()`
- **위치**: `tools/health_check.py:768` (768~804)
- **설명**: app.py 에서 `while True` + **sleep-first** 데몬을 찾아 (함수, 주기초, 줄) 로 돌려준다. *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `check_daemon_alive`(807)

### `_posted_races_today(now)`
- **위치**: `tools/health_check.py:939` (939~955)
- **설명**: 오늘 **발주가 지난** 경주 수 — data/today_schedule.json 의 postEpoch 기준(읽기 전용). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `snapshot_ingest_counts`(958)

### `build_checklist()`
- **위치**: `tools/health_check.py:1006` (1006~1065)
- **설명**: 반환 dict 의 **최상단에 `summary` 계열을 배치**한다(모바일에서 먼저 보이도록). *(docstring)*
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 1)**: `main`(1068)

### `main()`
- **위치**: `tools/health_check.py:1068` (1068~1094)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 규칙 미매치
- **호출됨(Caller · 0)**: —

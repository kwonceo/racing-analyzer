# 헬퍼 — 짧은 변환 도구(파이프라인 아님)

> 🤖 **자동 생성** — `python tools/build_codemap.py` · 2026-08-12 08:36:46
> ⚠ 손으로 고치지 마세요. 다음 생성 때 덮어써집니다.
> ⚠ **AST 한계**: `getattr()` 동적 호출 · `importlib` 경유 · 문자열 디스패치는 잡지 못합니다
>   (`gemini_reviewer`·`health_check` 가 실제로 `importlib` 경유입니다).

총 **29개** 함수

### `_is_opening_settle(po, pct)`
- **위치**: `app.py:1754` (1754~1757)
- **설명**: opening/placeholder 배당(직전 100배+)이 실배당으로 정착하며 생기는 기계적 급락(≤-80%). *(docstring)*
- **분류 근거**: 헬퍼 규칙
- **호출됨(Caller · 2)**: `_triple_analyze`(10724) · `_history_append`(15465)

### `_as_qmap(x)`
- **위치**: `app.py:1778` (1778~1792)
- **설명**: 복승 배당을 {정렬튜플: 최저배당} 맵으로 정규화 — 리스트([{combo,odds}])와 *(docstring)*
- **분류 근거**: 헬퍼 규칙
- **호출됨(Caller · 7)**: `_next_race_surge`(1795) · `_board_flip_suspect`(1808) · `_combo_count_dip`(1842) · `_column_shift_suspect`(1867) · `_horse_rep_series`(7780) · `_combo_odds_trend`(14110) … [+1]

### `_is_central_key(_k)`
- **위치**: `app.py:3429` (3429~3433)
- **설명**: [중앙경마 탭 실시간 분석 (2026-07-19)] JRA는 oddspark 미지원(서버 수집 불가) — 확장이 배당판에서 *(주석(def 위))*
- **분류 근거**: 헬퍼 규칙
- **호출됨(Caller · 1)**: `triple_latest`(3396)

### `_safe_snapshot_name(fn)`
- **위치**: `app.py:3676` (3676~3684)
- **설명**: [경로조작 방어] 파일명 정규화 — basename·허용문자(한글·영숫자·_-.)·.png 강제·길이 제한. *(docstring)*
- **분류 근거**: 헬퍼 규칙
- **호출됨(Caller · 2)**: `snapshot_save`(3994) · `snapshot_get`(4072)

### `_is_pass_race(an)`
- **위치**: `app.py:6971` (6971~6986)
- **설명**: 패스 권고 경주 판정: ①추천 게이트(신호 대기·패스형) 또는 ②전적없음+강신호없음. *(docstring)*
- **분류 근거**: 헬퍼 규칙
- **호출됨(Caller · 1)**: `_apply_result_learning`(20801)

### `_has_sig(combo)`
- **위치**: `app.py:8370` (8370~8371)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 헬퍼 규칙
- **호출됨(Caller · 2)**: `_final_picks`(8348) · `_combo_has_signal`(8519)

### `_is_japan_key(k)`
- **위치**: `app.py:14475` (14475~14477)
- **설명**: raceKey에 일본 경마장명이 들어있으면 True(한국 경주 매칭에서 제외용). *(docstring)*
- **분류 근거**: 헬퍼 규칙
- **호출됨(Caller · 1)**: `triple_match`(14481)

### `_fmt(q)`
- **위치**: `app.py:14983` (14983~14985)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 헬퍼 규칙
- **호출됨(Caller · 5)**: `_pub_pace`(14965) · `kra_status`(29220) · `_hist_row_from_doc`(35138) · `_timeline_snap_tick`(36575) · `drop_stats`(331)

### `_pick(label)`
- **위치**: `app.py:15722` (15722~15724)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 헬퍼 규칙
- **호출됨(Caller · 2)**: `_history_save_analysis`(15703) · `_dl_reliability`(17869)

### `_pick(st, label)`
- **위치**: `app.py:17877` (17877~17879)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 헬퍼 규칙
- **호출됨(Caller · 2)**: `_history_save_analysis`(15703) · `_dl_reliability`(17869)

### `_safe_num(v)`
- **위치**: `app.py:18633` (18633~18637)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 헬퍼 규칙
- **호출됨(Caller · 17)**: `result_ocr`(838) · `_apply_profit_strategy`(9809) · `_apply_bet_rules`(14178) · `_build_race_result`(16955) · `_build_ai_training`(17176) · `shadow_trifecta_ep`(20227) … [+11]

### `_is_high_hit(r)`
- **위치**: `app.py:19138` (19138~19141)
- **설명**: [신규 4번] 신호조합 학습: 어떤 신호(조합)가 실제 입상마를 맞혔고, 그 중 고배당 적중 비율. *(주석(def 위))*
- **분류 근거**: 헬퍼 규칙
- **호출됨(Caller · 1)**: `_recompute_learning_stats`(19031)

### `_is_tainted_saved(saved_at)`
- **위치**: `app.py:21597` (21597~21600)
- **설명**: 결과 저장 시각(saved_at 'YYYY-MM-DD HH:MM:SS')이 오염 구간 안인지. *(docstring)*
- **분류 근거**: 헬퍼 규칙
- **호출됨(Caller · 2)**: `_build_signal_performance`(21603) · `_scoreboard_daily`(28704)

### `_esc(x)`
- **위치**: `app.py:28146` (28146~28147)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 헬퍼 규칙
- **호출됨(Caller · 4)**: `race_log_page`(28137) · `daily_cards_page`(28255) · `review_report_page`(28456) · `scoreboard_page`(28871)

### `_esc(x)`
- **위치**: `app.py:28264` (28264~28265)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 헬퍼 규칙
- **호출됨(Caller · 4)**: `race_log_page`(28137) · `daily_cards_page`(28255) · `review_report_page`(28456) · `scoreboard_page`(28871)

### `_esc(x)`
- **위치**: `app.py:28462` (28462~28463)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 헬퍼 규칙
- **호출됨(Caller · 4)**: `race_log_page`(28137) · `daily_cards_page`(28255) · `review_report_page`(28456) · `scoreboard_page`(28871)

### `_esc(x)`
- **위치**: `app.py:28876` (28876~28877)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 헬퍼 규칙
- **호출됨(Caller · 4)**: `race_log_page`(28137) · `daily_cards_page`(28255) · `review_report_page`(28456) · `scoreboard_page`(28871)

### `_fmt(ts)`
- **위치**: `app.py:29228` (29228~29229)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 헬퍼 규칙
- **호출됨(Caller · 5)**: `_pub_pace`(14965) · `kra_status`(29220) · `_hist_row_from_doc`(35138) · `_timeline_snap_tick`(36575) · `drop_stats`(331)

### `_to_int(v)`
- **위치**: `app.py:31307` (31307~31312)
- **설명**: '1200', '1200M', '1,200m' → 1200. 숫자 없으면 None. *(docstring)*
- **분류 근거**: 헬퍼 규칙
- **호출됨(Caller · 14)**: `_det_horse_note`(943) · `_do_analyze`(991) · `_dist_bucket`(4447) · `_triple_analyze`(10724) · `_keiba_parse_horse_past`(26370) · `course_aptitude_bonus`(31323) … [+8]

### `_is_korea_rk(rk)`
- **위치**: `app.py:36556` (36556~36560)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 헬퍼 규칙
- **호출됨(Caller · 1)**: `_timeline_snap_tick`(36575)

### `_fmt_combo(lst)`
- **위치**: `gemini_reviewer.py:54` (54~63)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 헬퍼 규칙
- **호출됨(Caller · 1)**: `_build_prompt`(65)

### `_as_odds_items(m)`
- **위치**: `gemini_reviewer.py:106` (106~128)
- **설명**: [형식 방어 2026-07-28] 배당 자료가 dict 와 list 두 형식으로 들어온다. *(docstring)*
- **분류 근거**: 헬퍼 규칙
- **호출됨(Caller · 2)**: `_fmt_odds_map`(131) · `_build_full_prompt`(246)

### `_fmt_odds_map(m, limit, asc)`
- **위치**: `gemini_reviewer.py:131` (131~142)
- **설명**: 배당 맵/목록 → '1+2:3.4 1+3:5.6 ...' (배당 오름차순). limit=None 이면 전량. *(docstring)*
- **분류 근거**: 헬퍼 규칙
- **호출됨(Caller · 2)**: `_fmt_timeline`(155) · `_build_full_prompt`(246)

### `_fmt_json_block(obj, title)`
- **위치**: `gemini_reviewer.py:145` (145~152)
- **설명**: 구조가 제각각인 분석 산출물은 JSON 원문 그대로 전달(정보 손실 0). *(docstring)*
- **분류 근거**: 헬퍼 규칙
- **호출됨(Caller · 1)**: `_build_full_prompt`(246)

### `_fmt_timeline(tl)`
- **위치**: `gemini_reviewer.py:155` (155~164)
- **설명**: 배당 시계열 전량 — 스냅샷마다 전 조합 배당. 급락 판정의 원재료라 절대 요약하지 않는다. *(docstring)*
- **분류 근거**: 헬퍼 규칙
- **호출됨(Caller · 1)**: `_build_full_prompt`(246)

### `_is_tainted(doc)`
- **위치**: `review_engine.py:124` (124~131)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 헬퍼 규칙
- **호출됨(Caller · 2)**: `classify_race`(134) · `replay_day`(478)

### `_fmt(t)`
- **위치**: `review_engine.py:392` (392~395)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 헬퍼 규칙
- **호출됨(Caller · 5)**: `_pub_pace`(14965) · `kra_status`(29220) · `_hist_row_from_doc`(35138) · `_timeline_snap_tick`(36575) · `drop_stats`(331)

### `_key(snap)`
- **위치**: `tools/dedupe_odds_snapshots.py:62` (62~67)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 헬퍼 규칙
- **호출됨(Caller · 2)**: `dedupe_snapshots`(75) · `main`(99)

### `_is_settled()`
- **위치**: `tools/health_check.py:641` (641~663)
- **설명**: 그날 경주가 **다 끝났는가**. 반환 (확정여부, 사유). *(docstring)*
- **분류 근거**: 헬퍼 규칙
- **호출됨(Caller · 2)**: `check_payout_coverage`(666) · `check_measurable_today`(843)

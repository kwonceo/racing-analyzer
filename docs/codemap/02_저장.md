# 저장 — 저장 함수·tmp 패턴·락

> 🤖 **자동 생성** — `python tools/build_codemap.py` · 2026-08-12 07:29:59
> ⚠ 손으로 고치지 마세요. 다음 생성 때 덮어써집니다.
> ⚠ **AST 한계**: `getattr()` 동적 호출 · `importlib` 경유 · 문자열 디스패치는 잡지 못합니다
>   (`gemini_reviewer`·`health_check` 가 실제로 `importlib` 경유입니다).

총 **86개** 함수

### `_log_api_usage(usage, model, label)`
- **위치**: `app.py:551` (551~590)
- **설명**: 매 Claude 호출의 input/output tokens 를 data/api_usage.json 에 일별 누적하고 *(docstring)*
- **분류 근거**: 본문에서 os.replace 직접 호출
- **호출됨(Caller · 1)**: `call_claude`(631)  ⚠ 위험목록 함수 → 전수 나열

### `_odds_save(db)`
- **위치**: `app.py:1214` (1214~1216)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 3)**: `odds_add_snapshot`(1249) · `odds_undo`(1280) · `odds_clear`(1295)

### `_results_save(db)`
- **위치**: `app.py:1235` (1235~1237)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 1)**: `results_auto`(1400)

### `_triple_save(db)`
- **위치**: `app.py:1487` (1487~1489)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 5)**: `_do_triple_ingest`(2768) · `triple_reset`(3268) · `extract_japan`(14344) · `_multi_collect_one`(34659) · `_startup_date_reset`(38950)

### `_starters_save(db)`
- **위치**: `app.py:1524` (1524~1526)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 11)**: `_do_triple_ingest`(2768) · `_starters_prune`(3239) · `extract_japan`(14344) · `korea_form`(14399) · `keirin_card`(25445) · `_keirin_autocollect_form`(25536) … [+5]

### `_ingest_reject_log(rk, reason, source, extra, throttle)`
- **위치**: `app.py:2734` (2734~2765)
- **설명**: [수집 진단 2026-07-28 · 모리오카 실사고] 수집이 '거부'될 때 사유 한 줄을 영구 아카이브에 남긴다. *(docstring)*
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 2)**: `_do_triple_ingest`(2768) · `_multi_collect_one`(34659)

### `_race_pin_save(rk)`
- **위치**: `app.py:3331` (3331~3335)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 1)**: `race_pin`(3377)

### `_board_hint_save(rk, sport)`
- **위치**: `app.py:3354` (3354~3360)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 1)**: `current_race`(4205)

### `_jp_jockeys_save()`
- **위치**: `app.py:4438` (4438~4444)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 1)**: `_jp_jockeys_accumulate`(4459)

### `_after_close_save(d)`
- **위치**: `app.py:5221` (5221~5223)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 2)**: `_record_after_close_case`(5226) · `_after_close_learn_result`(5254)

### `_record_after_close_case(rk, date, mb_signed, anomalies, surge)`
- **위치**: `app.py:5226` (5226~5251)
- **설명**: [4번] 마감 후 신호로 베팅 반영 불가했던 케이스 저장. 경주별 1건(신호 최다 시점)으로 갱신. *(docstring)*
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 1)**: `_history_append`(15465)

### `_near_miss_save(d)`
- **위치**: `app.py:5318` (5318~5320)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 1)**: `_record_near_miss`(5323)

### `_record_near_miss(rk, date, no, name)`
- **위치**: `app.py:5323` (5323~5336)
- **설명**: [2·3번] 추천 말이 4착(아깝게 미적중)한 케이스 저장. 경주별 1건. *(docstring)*
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 1)**: `_apply_result_learning`(20801)

### `_signal_type_stats_save(d)`
- **위치**: `app.py:5364` (5364~5366)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 1)**: `_learn_strong_signals`(5369)

### `_compression_stats_save(d)`
- **위치**: `app.py:5499` (5499~5501)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 1)**: `_learn_compression`(5504)

### `_high_odds_companion_stats_save(d)`
- **위치**: `app.py:5565` (5565~5567)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 1)**: `_learn_high_odds_companion`(5570)

### `_third_place_stats_save(d)`
- **위치**: `app.py:5622` (5622~5624)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 1)**: `_learn_third_place`(5641)

### `_signal_lesson_save(d)`
- **위치**: `app.py:5704` (5704~5706)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 1)**: `_record_signal_lesson`(5709)

### `_record_signal_lesson(case)`
- **위치**: `app.py:5709` (5709~5723)
- **설명**: 초과급락 신호 → 실제 입상인데 노이즈 판정으로 추천 미반영된 사례 축적(경주별 1건). *(docstring)*
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 1)**: `learning_signal_lessons`(4191)

### `_reversal_persistence(hist, fav_rank, lookback)`
- **위치**: `app.py:5850` (5850~5862)
- **설명**: [역배열 지속성] 최근 스냅샷들에서 각 challenger가 역배열(ratio<0.90)로 몇 회 감지됐는지 집계. *(docstring)*
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 1)**: `_reversal_axis_roles`(5865)

### `_drop_persistence(advanced, no)`
- **위치**: `app.py:7443` (7443~7457)
- **설명**: [2번] 시세급락지속성(0~100). 연속하락 확정=높음, 페이크(반등)=낮음. *(docstring)*
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 1)**: `_confidence_engine`(7460)

### `_dark_cases_save(d)`
- **위치**: `app.py:10123` (10123~10128)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 1)**: `_dark_case_record`(10612)

### `_pace_log_record(rk, date, pace_analysis, top3, form, scenario_plan)`
- **위치**: `app.py:10142` (10142~10170)
- **설명**: [8] 편성 유형(페이스) vs 실제 입상마 각질 기록 + 시나리오 A/B 적중 → pace_analysis/ 저장 + 통계 재계산. *(docstring)*
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 1)**: `_apply_result_learning`(20801)

### `_dark_log_record(rk, an)`
- **위치**: `app.py:10370` (10370~10400)
- **설명**: [예측 시점 기록] 복병 rank1~3을 dark_horse_log 에 저장(결과 전). 마감 전만·경주당 최신 갱신. *(docstring)*
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 2)**: `_dark_log_apply_result`(10403) · `_auto_pred_save`(36811)  ⚠ 위험목록 함수 → 전수 나열

### `_dark_case_record(rk, an, top3)`
- **위치**: `app.py:10612` (10612~10656)
- **설명**: [3번·복병 케이스 학습] 결과 입력 시 복병(darkHorses)의 입상 여부를 케이스로 저장. *(docstring)*
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 1)**: `_apply_result_learning`(20801)

### `_dansung_cases_save(d)`
- **위치**: `app.py:10670` (10670~10675)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 1)**: `_dansung_case_record`(10678)

### `_dansung_case_record(rk, an, top3)`
- **위치**: `app.py:10678` (10678~10721)
- **설명**: [단통 경주 학습·저배당 쏠림] 복승 최저배당 ≤1.5배(시장 과도 쏠림) 경주만 별도 기록 → 복병 적중률 통계. *(docstring)*
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 1)**: `_apply_result_learning`(20801)

### `_t5_log(rec)`
- **위치**: `app.py:13596` (13596~13604)
- **설명**: [T-5 동결 관측] append-only. 덮어쓰기가 없어 구조적으로 잃지 않는다(원칙 9·23). *(docstring)*
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 2)**: `_apply_t5_freeze`(13661) · `_timeline_snap_tick`(36575)

### `_record_alert(rk, an)`
- **위치**: `app.py:14545` (14545~14583)
- **설명**: [1번] 유의미한 배당급변 경고를 data/alerts/<race>.json 에 완전 기록. *(docstring)*
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 1)**: `triple_analyze`(15132)

### `_history_append(rk, quinella, exacta, deadline, win, baseline_reset, source)`
- **위치**: `app.py:15465` (15465~15700)
- **설명**: 경주별 히스토리 파일에 스냅샷 1건 추가. 직전 대비 급락(≤-20%) 이상감지 기록. *(docstring)*
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 3)**: `_do_triple_ingest`(2768) · `extract_japan`(14344) · `_multi_collect_one`(34659)

### `_history_save_analysis(rk, an)`
- **위치**: `app.py:15703` (15703~15746)
- **설명**: [경주별 복기] 분석 시점의 전적점수·제거/후보·이상감지 신호·최종 베팅추천을 *(docstring)*
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 1)**: `triple_analyze`(15132)

### `_json_atomic(path, obj, indent)`
- **위치**: `app.py:15759` (15759~15799)
- **설명**: [원자적 저장 2026-07-28] 임시파일 기록 후 os.replace 로 교체. *(docstring)*
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 50)**: `_gate_hit`(2082) · `_snapshot_timing_learn`(3928) · `_after_close_save`(5221) · `_near_miss_save`(5318) · `_signal_type_stats_save`(5364) · `_compression_stats_save`(5499) · `_high_odds_companion_stats_save`(5565) · `_third_place_stats_save`(5622) · `_signal_lesson_save`(5704) · `_ev_bands_update`(9772) · `_dark_cases_save`(10123) · `_pace_stats_recompute`(10173) · `_dark_stats_recompute`(10493) · `_dansung_cases_save`(10670) · `_record_alert`(14545) · `_match_alerts_to_result`(14586) · `_history_append`(15465) · `_history_save_analysis`(15703) · `_corrupt_note`(15819) · `_dr_record`(16157) · `_threshold_config_save`(17416) · `_learning_save`(18039) · `_upset_save`(18065) · `_discovered_save`(18624) · `_learn_aux_combos`(20537) · `recommend_manual`(20668) · `_apply_result_learning`(20801) · `_failure_save`(21692) · `_competitor_recompute_stats`(23304) · `_mark_result_in_log`(24329) · `_review_note_append`(24848) · `analysis_log_memo`(24874) · `_kra_section_records`(29465) · `_kra_jockey_changes`(29583) · `_jra_result_save`(30636) · `_korea_verify_fitz`(32876) · `korea_history_save`(33387) · `_freeze_note`(34351) · `_amedas_note`(34483) · `_kakao_token_save`(35923) · `_kakao_anchor_log`(35982) · `_timeline_stats_recompute`(36760) · `_auto_pred_compare`(36837) · `_collect_cycle_end`(37160) · `_health_kakao_log`(37383) · `_review_pattern_alert`(37717) · `_midcheck_stamp_save`(37825) · `_midcheck_accum_delta`(38133) · `_snapshot_shortage_check`(38852) · `main`(99)  ⚠ 위험목록 함수 → 전수 나열

### `_dr_record(rk, det, pend)`
- **위치**: `app.py:16157` (16157~16196)
- **설명**: [대조 검증용] 결정론 판정 결과를 `logs/det_review/` 에 남긴다. *(docstring)*
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_build_analysis_log(rk, an)`
- **위치**: `app.py:16347` (16347~16839)
- **설명**: _triple_analyze 결과 + odds_history(타임라인/결과) + 전적을 종합해 리치 로그를 만들고 저장. *(docstring)*
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 2)**: `_analysis_log_save`(16842) · `_auto_pred_tick`(36865)  ⚠ 위험목록 함수 → 전수 나열

### `_analysis_log_save(rk, an)`
- **위치**: `app.py:16842` (16842~16847)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 3)**: `triple_analyze`(15132) · `_apply_result_learning`(20801) · `analysis_log_backfill`(31071)

### `_save_race_result(rk, an, record, result, top4, inputs)`
- **위치**: `app.py:17095` (17095~17112)
- **설명**: [1·4번] 완전 저장 파일 생성 + 검증 + 중복 방지(같은 race_id 덮어쓰기). 반환 {ok,path,errors}. *(docstring)*
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 1)**: `_apply_result_learning`(20801)

### `_save_ai_training(rk, an, record, result, top4, inputs)`
- **위치**: `app.py:17379` (17379~17396)
- **설명**: [1·2번] AI 학습 완전 데이터 저장 + 품질검증(중복=같은 race_id 덮어쓰기). *(docstring)*
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 1)**: `_apply_result_learning`(20801)

### `_threshold_config_save(current, applied_note, prev)`
- **위치**: `app.py:17416` (17416~17428)
- **설명**: current 기준치 저장 + 이전값 history 백업(감사·롤백용). *(docstring)*
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 1)**: `thresholds_apply`(3623)

### `_analysis_log_git_backup(label)`
- **위치**: `app.py:17609` (17609~17622)
- **설명**: data/analysis_log/ 를 커밋(+가능하면 push). 원격/인증 미설정이면 조용히 건너뜀. *(docstring)*
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 1)**: `analysis_log_backup`(31094)

### `_run_data_git_backup(label)`
- **위치**: `app.py:17641` (17641~17670)
- **설명**: [데이터 보호] 추적 데이터 경로만 git add + commit(pathspec) + push. 실패는 조용히 로그. *(docstring)*
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 5)**: `_start_periodic_backup`(17704) · `_start_daily_learning_scheduler`(17961) · `data_backup`(31102) · `_loop`(17734) · `_loop`(17991)

### `_data_git_backup(label, delay)`
- **위치**: `app.py:17673` (17673~17685)
- **설명**: [2번] 결과 입력마다 호출 — delay초 디바운스 후 1회 백업(연속 입력 묶음·응답 지연 없음). *(docstring)*
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 3)**: `_apply_result_learning`(20801) · `competitor_record`(23357) · `data_backup`(31102)

### `_backup_interval_seconds()`
- **위치**: `app.py:17692` (17692~17697)
- **설명**: [데이터 자동백업 완성] 결과 입력이 없어도 주기적으로 안전 백업. *(주석(def 위))*
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 0)**: —

### `_start_periodic_backup(interval)`
- **위치**: `app.py:17704` (17704~17751)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 1)**: `_boot_background`(38998)  ⚠ 위험목록 함수 → 전수 나열

### `_mark_run()`
- **위치**: `app.py:17724` (17724~17732)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 본문에서 os.replace 직접 호출
- **호출됨(Caller · 2)**: `_start_periodic_backup`(17704) · `_loop`(17734)

### `_dl_mark(day)`
- **위치**: `app.py:17981` (17981~17989)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 본문에서 os.replace 직접 호출
- **호출됨(Caller · 2)**: `_start_daily_learning_scheduler`(17961) · `_loop`(17991)

### `_loop()`
- **위치**: `app.py:17991` (17991~18026)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 본문에서 os.replace 직접 호출
- **호출됨(Caller · 0)**: —  ⚠ 위험목록 함수 → 전수 나열

### `_learning_save(d)`
- **위치**: `app.py:18039` (18039~18043)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 2)**: `_apply_result_learning`(20801) · `results_adjust`(24468)

### `_upset_save(d)`
- **위치**: `app.py:18065` (18065~18067)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 4)**: `_learn_mass_drop`(18077) · `_learn_smart_money`(18124) · `_learn_upset`(18395) · `learning_reversal_stats`(24696)

### `_signal_stats_save(d)`
- **위치**: `app.py:18237` (18237~18245)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 1)**: `_learn_signal_stats`(18248)

### `_mid_high_stats_save(d)`
- **위치**: `app.py:18298` (18298~18306)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 1)**: `_learn_mid_high_odds`(18309)

### `_discovered_save(out)`
- **위치**: `app.py:18624` (18624~18626)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 1)**: `_discover_patterns`(18525)

### `_highlight_save(entry)`
- **위치**: `app.py:18640` (18640~18649)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 1)**: `_apply_result_learning`(20801)  ⚠ 위험목록 함수 → 전수 나열

### `_report_append(title, part1, part2, part3, meta)`
- **위치**: `app.py:19930` (19930~19966)
- **설명**: [3단 보고 적재] 맨 위가 최신이 되게 **prepend** 한다. 🔴 덮어쓰지 않는다. *(docstring)*
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 0)**: —

### `_rec_combos_from_analysis_log(rk)`
- **위치**: `app.py:20597` (20597~20664)
- **설명**: [적중 판정 폴백] 결과 입력 시 재분석 betRecommend가 비었을 때(경주 종료·활성 rec 소실) *(docstring)*
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 3)**: `_apply_result_learning`(20801) · `_our_picks_for_race`(23210) · `_live_exact_hit`(34989)  ⚠ 위험목록 함수 → 전수 나열

### `_conf_picks_from_log(rk)`
- **위치**: `app.py:20722` (20722~20739)
- **설명**: [확신도 복승 학습] analysis_log에 보존한 corePicks에서 확신도 복승/삼복승 조합 복원. *(docstring)*
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 1)**: `_apply_result_learning`(20801)

### `_failure_save(d)`
- **위치**: `app.py:21692` (21692~21694)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 1)**: `_failure_record`(21914)

### `_failure_record(rk, an, top3, doc, record, stats)`
- **위치**: `app.py:21914` (21914~21946)
- **설명**: [1·4·7번] 실패 분류 → failure_review.json 누적(유형 카운트·놓친 패턴) + 규칙 자동 생성. *(docstring)*
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 1)**: `_apply_result_learning`(20801)

### `_high_odds_case_save(case)`
- **위치**: `app.py:23037` (23037~23051)
- **설명**: [수동 케이스 학습] 사용자가 짚어준 놓친 케이스(카와사키 11R 등)를 data/high_odds_review/에 저장. *(docstring)*
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 1)**: `high_odds_review`(23069)

### `_competitor_record(rk, comp_picks, comp_odds, comp_note)`
- **위치**: `app.py:23280` (23280~23301)
- **설명**: 저쪽 추천 저장 + 우리 추천/결과 비교 분석 → data/competitor_analysis/ + 통계 갱신. *(docstring)*
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 1)**: `competitor_record`(23357)

### `_mark_result_in_log(rk, result)`
- **위치**: `app.py:24329` (24329~24344)
- **설명**: 중복 로그(추천 없는 쪽)에 결과만 기록해 미입력 목록에서 제거(전체 학습은 primary에서만). *(docstring)*
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 1)**: `_register_result_rows`(24367)

### `_keiba_starter_store_row(h)`
- **위치**: `app.py:26464` (26464~26511)
- **설명**: 일본경마 전적 저장 행(공용) — totalScore·근거(detail)·신규 지표(마체중·거리적성·기수복승률) 포함. *(docstring)*
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 5)**: `keiba_starters`(26619) · `_keiba_autocollect_form`(26684) · `jra_starters`(31023) · `_nar_autocollect_form`(34079) · `_jra_preload_once`(38639)

### `_kra_backfill_loop()`
- **위치**: `app.py:27405` (27405~27461)
- **설명**: 20분 주기 결과 백필(데몬). KRA 키가 있을 때만 동작. 경주 종료 후 rcTime 게시되면 자동 확보 → *(docstring)*
- **분류 근거**: 본문에서 os.replace 직접 호출
- **호출됨(Caller · 0)**: —  ⚠ 위험목록 함수 → 전수 나열

### `_bf_mark()`
- **위치**: `app.py:27421` (27421~27429)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 본문에서 os.replace 직접 호출
- **호출됨(Caller · 1)**: `_kra_backfill_loop`(27405)

### `_kra_track_op_save(op)`
- **위치**: `app.py:29312` (29312~29319)
- **설명**: 확정된 op 경로를 영구 캐시(재시작 후 재탐색 방지). *(docstring)*
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 1)**: `_kra_track_op_resolve`(29337)

### `_jra_result_save(rk, parsed, race_id)`
- **위치**: `app.py:30636` (30636~30690)
- **설명**: 기존 결과 저장 경로에 합류 — `analysis_log` 의 `result` 만 채운다. **다른 필드 무수정.** *(docstring)*
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 1)**: `_jra_result_once`(30693)

### `_form_raw_save(sport, html, tag)`
- **위치**: `app.py:30822` (30822~30856)
- **설명**: [전적 원문 보존 (2026-08-02 승인)] **완전 읽기 전용 · 파싱 경로 무개입**. *(docstring)*
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 4)**: `_keirin_parse_card`(25066) · `_keiba_parse_shutsuba`(26269) · `_jra_parse_shutuba_past`(30906) · `_nar_parse_deba`(34014)

### `_pdf_cache_save(sess)`
- **위치**: `app.py:32500` (32500~32512)
- **설명**: 완료된 세션을 PDF 해시 키로 캐시 저장(다음 동일 PDF 재업로드 시 즉시 반환). *(docstring)*
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 1)**: `_korea_run_job`(32957)

### `_korea_save(sess)`
- **위치**: `app.py:32534` (32534~32541)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 3)**: `_korea_run_job`(32957) · `korea_start`(33217) · `korea_reextract`(33324)

### `_korea_git_backup(label)`
- **위치**: `app.py:32654` (32654~32672)
- **설명**: 완료 후 data/korea_session.json + data/korea_history/ 를 커밋(+가능하면 push). *(docstring)*
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 2)**: `_korea_run_job`(32957) · `korea_backup`(33489)

### `_prerace_save_race(date, race)`
- **위치**: `app.py:32684` (32684~32711)
- **설명**: 완료된 경주 1건을 data/prerace/<key>.json 에 원자적 저장 + index.json 갱신. *(docstring)*
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 3)**: `_korea_run_job`(32957) · `korea_start`(33217) · `korea_reextract`(33324)

### `_prerace_index_update(entry)`
- **위치**: `app.py:32718` (32718~32727)
- **설명**: index.json 의 동일 key 항목을 교체(없으면 추가). savedAt 내림차순 정렬. *(docstring)*
- **분류 근거**: 본문에서 os.replace 직접 호출
- **호출됨(Caller · 1)**: `_prerace_save_race`(32684)

### `_multi_store_load()`
- **위치**: `app.py:33572` (33572~33586)
- **설명**: multi_race_store.json 로드. 손상 시 자동 초기화(6번·기존 기능 영향 없음). *(docstring)*
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 10)**: `_oddspark_covered_active`(1934) · `_triple_analyze`(10724) · `_jp_form_backfill_once`(26794) · `debug_race_data`(28575) · `bmed_view`(30283) · `_multi_collect_one`(34659) · `multi_dashboard`(35542) · `multi_race_detail`(35842) · `kakao_send_race`(36218) · `_startup_date_reset`(38950)  ⚠ 위험목록 함수 → 전수 나열

### `_multi_store_save(db)`
- **위치**: `app.py:33589` (33589~33598)
- **설명**: 원자적 쓰기(tmp→replace)로 저장소 손상 방지. *(docstring)*
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 2)**: `_multi_collect_one`(34659) · `_startup_date_reset`(38950)

### `_schedule_save(sched)`
- **위치**: `app.py:33609` (33609~33617)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 4)**: `_korea_schedule_post_epoch`(33620) · `_multi_schedule_fetch`(34223) · `multi_keirin_schedule`(35478) · `_startup_date_reset`(38950)

### `_timeline_snap_save(rk, phase, an)`
- **위치**: `app.py:36478` (36478~36511)
- **설명**: 지정 phase 스냅샷 저장(1회). T-5는 직전 T-7 대비 변경마 계산. 반환 저장된 snap dict 또는 None. *(docstring)*
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 1)**: `_timeline_snap_tick`(36575)  ⚠ 위험목록 함수 → 전수 나열

### `_auto_pred_save(rk, an)`
- **위치**: `app.py:36811` (36811~36834)
- **설명**: [마감5분전 최종 예상 저장] 분석(corePicks)에서 추천·유력마·단통 요약을 data/auto_prediction/ 에 저장. *(docstring)*
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 1)**: `_auto_pred_tick`(36865)

### `_hk_mark(day)`
- **위치**: `app.py:37674` (37674~37682)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 본문에서 os.replace 직접 호출
- **호출됨(Caller · 2)**: `_start_health_kakao_scheduler`(37641) · `_loop`(37684)

### `_midcheck_stamp_save(d)`
- **위치**: `app.py:37825` (37825~37832)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 3)**: `_midcheck_run`(38373) · `_start_midcheck_scheduler`(38419) · `_loop`(38426)

### `_start_nar_preload_scheduler()`
- **위치**: `app.py:38577` (38577~38620)
- **설명**: 🔴 sleep-first 로 만들지 않는다 — **검사 먼저, 대기 나중**. 하루 1회 판정은 **파일 스탬프**로 한다. *(docstring)*
- **분류 근거**: 본문에서 os.replace 직접 호출
- **호출됨(Caller · 1)**: `_boot_background`(38998)  ⚠ 위험목록 함수 → 전수 나열

### `_mark(day)`
- **위치**: `app.py:38595` (38595~38603)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 본문에서 os.replace 직접 호출
- **호출됨(Caller · 4)**: `_start_nar_preload_scheduler`(38577) · `_start_jra_preload_scheduler`(38741) · `_loop`(38605) · `_loop`(38765)

### `_start_jra_preload_scheduler()`
- **위치**: `app.py:38741` (38741~38780)
- **설명**: 🔴 sleep-first 금지 — 검사 먼저, 대기 나중. 하루 1회 판정은 **파일 스탬프**로 한다. *(docstring)*
- **분류 근거**: 본문에서 os.replace 직접 호출
- **호출됨(Caller · 1)**: `_boot_background`(38998)  ⚠ 위험목록 함수 → 전수 나열

### `_mark(day)`
- **위치**: `app.py:38755` (38755~38763)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 본문에서 os.replace 직접 호출
- **호출됨(Caller · 4)**: `_start_nar_preload_scheduler`(38577) · `_start_jra_preload_scheduler`(38741) · `_loop`(38605) · `_loop`(38765)

### `_save_log(rk, result)`
- **위치**: `gemini_reviewer.py:362` (362~371)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 2)**: `review_async`(440) · `_run`(449)

### `sweep(date)`
- **위치**: `review_engine.py:187` (187~233)
- **설명**: [1] 하루치 일괄 분류 → review_stats.json 병합 저장. 반환 요약. *(docstring)*
- **분류 근거**: 본문에서 os.replace 직접 호출
- **호출됨(Caller · 0)**: —

### `_json_atomic(path, obj)`
- **위치**: `tools/dedupe_odds_snapshots.py:92` (92~96)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 50)**: `_gate_hit`(2082) · `_snapshot_timing_learn`(3928) · `_after_close_save`(5221) · `_near_miss_save`(5318) · `_signal_type_stats_save`(5364) · `_compression_stats_save`(5499) · `_high_odds_companion_stats_save`(5565) · `_third_place_stats_save`(5622) · `_signal_lesson_save`(5704) · `_ev_bands_update`(9772) · `_dark_cases_save`(10123) · `_pace_stats_recompute`(10173) · `_dark_stats_recompute`(10493) · `_dansung_cases_save`(10670) · `_record_alert`(14545) · `_match_alerts_to_result`(14586) · `_history_append`(15465) · `_history_save_analysis`(15703) · `_corrupt_note`(15819) · `_dr_record`(16157) · `_threshold_config_save`(17416) · `_learning_save`(18039) · `_upset_save`(18065) · `_discovered_save`(18624) · `_learn_aux_combos`(20537) · `recommend_manual`(20668) · `_apply_result_learning`(20801) · `_failure_save`(21692) · `_competitor_recompute_stats`(23304) · `_mark_result_in_log`(24329) · `_review_note_append`(24848) · `analysis_log_memo`(24874) · `_kra_section_records`(29465) · `_kra_jockey_changes`(29583) · `_jra_result_save`(30636) · `_korea_verify_fitz`(32876) · `korea_history_save`(33387) · `_freeze_note`(34351) · `_amedas_note`(34483) · `_kakao_token_save`(35923) · `_kakao_anchor_log`(35982) · `_timeline_stats_recompute`(36760) · `_auto_pred_compare`(36837) · `_collect_cycle_end`(37160) · `_health_kakao_log`(37383) · `_review_pattern_alert`(37717) · `_midcheck_stamp_save`(37825) · `_midcheck_accum_delta`(38133) · `_snapshot_shortage_check`(38852) · `main`(99)  ⚠ 위험목록 함수 → 전수 나열

### `_count_save_failures()`
- **위치**: `tools/health_check.py:173` (173~195)
- **설명**: 당일 stdout 로그(회전본 포함)에서 WinError 를 **발생 지점별로** 센다. *(docstring)*
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 2)**: `check_save_failures_fixed`(198) · `check_save_failures_unfixed`(213)

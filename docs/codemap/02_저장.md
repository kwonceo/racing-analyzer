# 저장 — 저장 함수·tmp 패턴·락

> 🤖 **자동 생성** — `python tools/build_codemap.py` · 2026-07-30 22:47:30
> ⚠ 손으로 고치지 마세요. 다음 생성 때 덮어써집니다.
> ⚠ **AST 한계**: `getattr()` 동적 호출 · `importlib` 경유 · 문자열 디스패치는 잡지 못합니다
>   (`gemini_reviewer`·`health_check` 가 실제로 `importlib` 경유입니다).

총 **72개** 함수

### `_log_api_usage(usage, model, label)`
- **위치**: `app.py:544` (544~583)
- **설명**: 매 Claude 호출의 input/output tokens 를 data/api_usage.json 에 일별 누적하고 *(docstring)*
- **분류 근거**: 본문에서 os.replace 직접 호출
- **호출됨(Caller · 1)**: `call_claude`(624)  ⚠ 위험목록 함수 → 전수 나열

### `_odds_save(db)`
- **위치**: `app.py:1207` (1207~1209)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 3)**: `odds_add_snapshot`(1242) · `odds_undo`(1273) · `odds_clear`(1288)

### `_results_save(db)`
- **위치**: `app.py:1228` (1228~1230)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 1)**: `results_auto`(1393)

### `_triple_save(db)`
- **위치**: `app.py:1480` (1480~1482)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 5)**: `_do_triple_ingest`(2166) · `triple_reset`(2470) · `extract_japan`(12721) · `_multi_collect_one`(29676) · `_startup_date_reset`(32237)

### `_starters_save(db)`
- **위치**: `app.py:1506` (1506~1508)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 10)**: `_do_triple_ingest`(2166) · `_starters_prune`(2441) · `extract_japan`(12721) · `korea_form`(12766) · `keirin_card`(22079) · `_keirin_autocollect_form`(22134) … [+4]

### `_ingest_reject_log(rk, reason, source, extra, throttle)`
- **위치**: `app.py:2132` (2132~2163)
- **설명**: [수집 진단 2026-07-28 · 모리오카 실사고] 수집이 '거부'될 때 사유 한 줄을 영구 아카이브에 남긴다. *(docstring)*
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 2)**: `_do_triple_ingest`(2166) · `_multi_collect_one`(29676)

### `_race_pin_save(rk)`
- **위치**: `app.py:2533` (2533~2537)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 1)**: `race_pin`(2579)

### `_board_hint_save(rk, sport)`
- **위치**: `app.py:2556` (2556~2562)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 1)**: `current_race`(3249)

### `_jp_jockeys_save()`
- **위치**: `app.py:3482` (3482~3488)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 1)**: `_jp_jockeys_accumulate`(3503)

### `_after_close_save(d)`
- **위치**: `app.py:4265` (4265~4267)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 2)**: `_record_after_close_case`(4270) · `_after_close_learn_result`(4298)

### `_record_after_close_case(rk, date, mb_signed, anomalies, surge)`
- **위치**: `app.py:4270` (4270~4295)
- **설명**: [4번] 마감 후 신호로 베팅 반영 불가했던 케이스 저장. 경주별 1건(신호 최다 시점)으로 갱신. *(docstring)*
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 1)**: `_history_append`(13676)

### `_near_miss_save(d)`
- **위치**: `app.py:4360` (4360~4362)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 1)**: `_record_near_miss`(4365)

### `_record_near_miss(rk, date, no, name)`
- **위치**: `app.py:4365` (4365~4376)
- **설명**: [2·3번] 추천 말이 4착(아깝게 미적중)한 케이스 저장. 경주별 1건. *(docstring)*
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 1)**: `_apply_result_learning`(17723)

### `_signal_type_stats_save(d)`
- **위치**: `app.py:4404` (4404~4406)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 1)**: `_learn_strong_signals`(4409)

### `_compression_stats_save(d)`
- **위치**: `app.py:4537` (4537~4539)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 1)**: `_learn_compression`(4542)

### `_high_odds_companion_stats_save(d)`
- **위치**: `app.py:4601` (4601~4603)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 1)**: `_learn_high_odds_companion`(4606)

### `_third_place_stats_save(d)`
- **위치**: `app.py:4656` (4656~4658)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 1)**: `_learn_third_place`(4675)

### `_signal_lesson_save(d)`
- **위치**: `app.py:4736` (4736~4738)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 1)**: `_record_signal_lesson`(4741)

### `_record_signal_lesson(case)`
- **위치**: `app.py:4741` (4741~4755)
- **설명**: 초과급락 신호 → 실제 입상인데 노이즈 판정으로 추천 미반영된 사례 축적(경주별 1건). *(docstring)*
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 1)**: `learning_signal_lessons`(3235)

### `_reversal_persistence(hist, fav_rank, lookback)`
- **위치**: `app.py:4882` (4882~4894)
- **설명**: [역배열 지속성] 최근 스냅샷들에서 각 challenger가 역배열(ratio<0.90)로 몇 회 감지됐는지 집계. *(docstring)*
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 1)**: `_reversal_axis_roles`(4897)

### `_drop_persistence(advanced, no)`
- **위치**: `app.py:6475` (6475~6489)
- **설명**: [2번] 시세급락지속성(0~100). 연속하락 확정=높음, 페이크(반등)=낮음. *(docstring)*
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 1)**: `_confidence_engine`(6492)

### `_dark_cases_save(d)`
- **위치**: `app.py:8897` (8897~8902)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 1)**: `_dark_case_record`(9384)

### `_pace_log_record(rk, date, pace_analysis, top3, form, scenario_plan)`
- **위치**: `app.py:8916` (8916~8942)
- **설명**: [8] 편성 유형(페이스) vs 실제 입상마 각질 기록 + 시나리오 A/B 적중 → pace_analysis/ 저장 + 통계 재계산. *(docstring)*
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 1)**: `_apply_result_learning`(17723)

### `_dark_log_record(rk, an)`
- **위치**: `app.py:9142` (9142~9172)
- **설명**: [예측 시점 기록] 복병 rank1~3을 dark_horse_log 에 저장(결과 전). 마감 전만·경주당 최신 갱신. *(docstring)*
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 2)**: `_dark_log_apply_result`(9175) · `_auto_pred_save`(31350)  ⚠ 위험목록 함수 → 전수 나열

### `_dark_case_record(rk, an, top3)`
- **위치**: `app.py:9384` (9384~9426)
- **설명**: [3번·복병 케이스 학습] 결과 입력 시 복병(darkHorses)의 입상 여부를 케이스로 저장. *(docstring)*
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 1)**: `_apply_result_learning`(17723)

### `_dansung_cases_save(d)`
- **위치**: `app.py:9440` (9440~9445)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 1)**: `_dansung_case_record`(9448)

### `_dansung_case_record(rk, an, top3)`
- **위치**: `app.py:9448` (9448~9489)
- **설명**: [단통 경주 학습·저배당 쏠림] 복승 최저배당 ≤1.5배(시장 과도 쏠림) 경주만 별도 기록 → 복병 적중률 통계. *(docstring)*
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 1)**: `_apply_result_learning`(17723)

### `_record_alert(rk, an)`
- **위치**: `app.py:12874` (12874~12912)
- **설명**: [1번] 유의미한 배당급변 경고를 data/alerts/<race>.json 에 완전 기록. *(docstring)*
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 1)**: `triple_analyze`(13461)

### `_history_append(rk, quinella, exacta, deadline, win, baseline_reset, source)`
- **위치**: `app.py:13676` (13676~13911)
- **설명**: 경주별 히스토리 파일에 스냅샷 1건 추가. 직전 대비 급락(≤-20%) 이상감지 기록. *(docstring)*
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 3)**: `_do_triple_ingest`(2166) · `extract_japan`(12721) · `_multi_collect_one`(29676)

### `_history_save_analysis(rk, an)`
- **위치**: `app.py:13914` (13914~13957)
- **설명**: [경주별 복기] 분석 시점의 전적점수·제거/후보·이상감지 신호·최종 베팅추천을 *(docstring)*
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 1)**: `triple_analyze`(13461)

### `_json_atomic(path, obj, indent)`
- **위치**: `app.py:13970` (13970~14010)
- **설명**: [원자적 저장 2026-07-28] 임시파일 기록 후 os.replace 로 교체. *(docstring)*
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 42)**: `_snapshot_timing_learn`(3041) · `_after_close_save`(4265) · `_near_miss_save`(4360) · `_signal_type_stats_save`(4404) · `_compression_stats_save`(4537) · `_high_odds_companion_stats_save`(4601) · `_third_place_stats_save`(4656) · `_signal_lesson_save`(4736) · `_ev_bands_update`(8548) · `_dark_cases_save`(8897) · `_pace_stats_recompute`(8945) · `_dark_stats_recompute`(9265) · `_dansung_cases_save`(9440) · `_record_alert`(12874) · `_match_alerts_to_result`(12915) · `_history_append`(13676) · `_history_save_analysis`(13914) · `_corrupt_note`(14030) · `_dr_record`(14368) · `_threshold_config_save`(15398) · `_learning_save`(15967) · `_upset_save`(15991) · `_discovered_save`(16538) · `_learn_aux_combos`(17498) · `recommend_manual`(17627) · `_apply_result_learning`(17723) · `_failure_save`(18595) · `_competitor_recompute_stats`(20114) · `_mark_result_in_log`(21084) · `_review_note_append`(21603) · `analysis_log_memo`(21629) · `_kra_section_records`(25756) · `_kra_jockey_changes`(25874) · `korea_history_save`(28908) · `_kakao_token_save`(30690) · `_kakao_anchor_log`(30745) · `_timeline_stats_recompute`(31299) · `_auto_pred_compare`(31376) · `_collect_cycle_end`(31687) · `_health_kakao_log`(31834) · `_snapshot_shortage_check`(32160) · `main`(99)  ⚠ 위험목록 함수 → 전수 나열

### `_dr_record(rk, det, pend)`
- **위치**: `app.py:14368` (14368~14407)
- **설명**: [대조 검증용] 결정론 판정 결과를 `logs/det_review/` 에 남긴다. *(docstring)*
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 1)**: `_triple_analyze`(9492)

### `_build_analysis_log(rk, an)`
- **위치**: `app.py:14451` (14451~14823)
- **설명**: _triple_analyze 결과 + odds_history(타임라인/결과) + 전적을 종합해 리치 로그를 만들고 저장. *(docstring)*
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 2)**: `_analysis_log_save`(14826) · `_auto_pred_tick`(31404)  ⚠ 위험목록 함수 → 전수 나열

### `_analysis_log_save(rk, an)`
- **위치**: `app.py:14826` (14826~14831)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 3)**: `triple_analyze`(13461) · `_apply_result_learning`(17723) · `analysis_log_backfill`(26890)

### `_save_race_result(rk, an, record, result, top4, inputs)`
- **위치**: `app.py:15079` (15079~15096)
- **설명**: [1·4번] 완전 저장 파일 생성 + 검증 + 중복 방지(같은 race_id 덮어쓰기). 반환 {ok,path,errors}. *(docstring)*
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 1)**: `_apply_result_learning`(17723)

### `_save_ai_training(rk, an, record, result, top4, inputs)`
- **위치**: `app.py:15363` (15363~15378)
- **설명**: [1·2번] AI 학습 완전 데이터 저장 + 품질검증(중복=같은 race_id 덮어쓰기). *(docstring)*
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 1)**: `_apply_result_learning`(17723)

### `_threshold_config_save(current, applied_note, prev)`
- **위치**: `app.py:15398` (15398~15410)
- **설명**: current 기준치 저장 + 이전값 history 백업(감사·롤백용). *(docstring)*
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 1)**: `thresholds_apply`(2825)

### `_analysis_log_git_backup(label)`
- **위치**: `app.py:15591` (15591~15604)
- **설명**: data/analysis_log/ 를 커밋(+가능하면 push). 원격/인증 미설정이면 조용히 건너뜀. *(docstring)*
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 1)**: `analysis_log_backup`(26913)

### `_run_data_git_backup(label)`
- **위치**: `app.py:15623` (15623~15652)
- **설명**: [데이터 보호] 추적 데이터 경로만 git add + commit(pathspec) + push. 실패는 조용히 로그. *(docstring)*
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 5)**: `_start_periodic_backup`(15686) · `_start_daily_learning_scheduler`(15913) · `data_backup`(26921) · `_loop`(15693) · `_loop`(15920)

### `_data_git_backup(label, delay)`
- **위치**: `app.py:15655` (15655~15667)
- **설명**: [2번] 결과 입력마다 호출 — delay초 디바운스 후 1회 백업(연속 입력 묶음·응답 지연 없음). *(docstring)*
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 3)**: `_apply_result_learning`(17723) · `competitor_record`(20167) · `data_backup`(26921)

### `_backup_interval_seconds()`
- **위치**: `app.py:15674` (15674~15679)
- **설명**: [데이터 자동백업 완성] 결과 입력이 없어도 주기적으로 안전 백업. *(주석(def 위))*
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 0)**: —

### `_start_periodic_backup(interval)`
- **위치**: `app.py:15686` (15686~15703)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 1)**: `_boot_background`(32285)  ⚠ 위험목록 함수 → 전수 나열

### `_loop()`
- **위치**: `app.py:15920` (15920~15954)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 본문에서 os.replace 직접 호출
- **호출됨(Caller · 0)**: —  ⚠ 위험목록 함수 → 전수 나열

### `_learning_save(d)`
- **위치**: `app.py:15967` (15967~15969)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 2)**: `_apply_result_learning`(17723) · `results_adjust`(21223)

### `_upset_save(d)`
- **위치**: `app.py:15991` (15991~15993)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 4)**: `_learn_mass_drop`(16003) · `_learn_smart_money`(16048) · `_learn_upset`(16313) · `learning_reversal_stats`(21451)

### `_signal_stats_save(d)`
- **위치**: `app.py:16159` (16159~16167)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 1)**: `_learn_signal_stats`(16170)

### `_mid_high_stats_save(d)`
- **위치**: `app.py:16218` (16218~16226)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 1)**: `_learn_mid_high_odds`(16229)

### `_discovered_save(out)`
- **위치**: `app.py:16538` (16538~16540)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 1)**: `_discover_patterns`(16441)

### `_highlight_save(entry)`
- **위치**: `app.py:16554` (16554~16561)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 1)**: `_apply_result_learning`(17723)  ⚠ 위험목록 함수 → 전수 나열

### `_rec_combos_from_analysis_log(rk)`
- **위치**: `app.py:17556` (17556~17623)
- **설명**: [적중 판정 폴백] 결과 입력 시 재분석 betRecommend가 비었을 때(경주 종료·활성 rec 소실) *(docstring)*
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 3)**: `_apply_result_learning`(17723) · `_our_picks_for_race`(20020) · `_live_exact_hit`(29910)  ⚠ 위험목록 함수 → 전수 나열

### `_conf_picks_from_log(rk)`
- **위치**: `app.py:17681` (17681~17698)
- **설명**: [확신도 복승 학습] analysis_log에 보존한 corePicks에서 확신도 복승/삼복승 조합 복원. *(docstring)*
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 1)**: `_apply_result_learning`(17723)

### `_failure_save(d)`
- **위치**: `app.py:18595` (18595~18597)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 1)**: `_failure_record`(18817)

### `_failure_record(rk, an, top3, doc, record, stats)`
- **위치**: `app.py:18817` (18817~18847)
- **설명**: [1·4·7번] 실패 분류 → failure_review.json 누적(유형 카운트·놓친 패턴) + 규칙 자동 생성. *(docstring)*
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 1)**: `_apply_result_learning`(17723)

### `_high_odds_case_save(case)`
- **위치**: `app.py:19847` (19847~19861)
- **설명**: [수동 케이스 학습] 사용자가 짚어준 놓친 케이스(카와사키 11R 등)를 data/high_odds_review/에 저장. *(docstring)*
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 1)**: `high_odds_review`(19879)

### `_competitor_record(rk, comp_picks, comp_odds, comp_note)`
- **위치**: `app.py:20090` (20090~20111)
- **설명**: 저쪽 추천 저장 + 우리 추천/결과 비교 분석 → data/competitor_analysis/ + 통계 갱신. *(docstring)*
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 1)**: `competitor_record`(20167)

### `_mark_result_in_log(rk, result)`
- **위치**: `app.py:21084` (21084~21099)
- **설명**: 중복 로그(추천 없는 쪽)에 결과만 기록해 미입력 목록에서 제거(전체 학습은 primary에서만). *(docstring)*
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 1)**: `_register_result_rows`(21122)

### `_keiba_starter_store_row(h)`
- **위치**: `app.py:22963` (22963~22991)
- **설명**: 일본경마 전적 저장 행(공용) — totalScore·근거(detail)·신규 지표(마체중·거리적성·기수복승률) 포함. *(docstring)*
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 4)**: `keiba_starters`(23095) · `_keiba_autocollect_form`(23159) · `jra_starters`(26842) · `_nar_autocollect_form`(29553)

### `_kra_track_op_save(op)`
- **위치**: `app.py:25603` (25603~25610)
- **설명**: 확정된 op 경로를 영구 캐시(재시작 후 재탐색 방지). *(docstring)*
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 1)**: `_kra_track_op_resolve`(25628)

### `_pdf_cache_save(sess)`
- **위치**: `app.py:28315` (28315~28327)
- **설명**: 완료된 세션을 PDF 해시 키로 캐시 저장(다음 동일 PDF 재업로드 시 즉시 반환). *(docstring)*
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 1)**: `_korea_run_job`(28595)

### `_korea_save(sess)`
- **위치**: `app.py:28349` (28349~28356)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 3)**: `_korea_run_job`(28595) · `korea_start`(28781) · `korea_reextract`(28845)

### `_korea_git_backup(label)`
- **위치**: `app.py:28469` (28469~28487)
- **설명**: 완료 후 data/korea_session.json + data/korea_history/ 를 커밋(+가능하면 push). *(docstring)*
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 2)**: `_korea_run_job`(28595) · `korea_backup`(29010)

### `_prerace_save_race(date, race)`
- **위치**: `app.py:28499` (28499~28526)
- **설명**: 완료된 경주 1건을 data/prerace/<key>.json 에 원자적 저장 + index.json 갱신. *(docstring)*
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 3)**: `_korea_run_job`(28595) · `korea_start`(28781) · `korea_reextract`(28845)

### `_prerace_index_update(entry)`
- **위치**: `app.py:28533` (28533~28542)
- **설명**: index.json 의 동일 key 항목을 교체(없으면 추가). savedAt 내림차순 정렬. *(docstring)*
- **분류 근거**: 본문에서 os.replace 직접 호출
- **호출됨(Caller · 1)**: `_prerace_save_race`(28499)

### `_multi_store_load()`
- **위치**: `app.py:29093` (29093~29107)
- **설명**: multi_race_store.json 로드. 손상 시 자동 초기화(6번·기존 기능 영향 없음). *(docstring)*
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 10)**: `_oddspark_covered_active`(1846) · `_triple_analyze`(9492) · `_jp_form_backfill_once`(23246) · `debug_race_data`(24918) · `bmed_view`(26574) · `_multi_collect_one`(29676) · `multi_dashboard`(30463) · `multi_race_detail`(30609) · `kakao_send_race`(30937) · `_startup_date_reset`(32237)  ⚠ 위험목록 함수 → 전수 나열

### `_multi_store_save(db)`
- **위치**: `app.py:29110` (29110~29119)
- **설명**: 원자적 쓰기(tmp→replace)로 저장소 손상 방지. *(docstring)*
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 2)**: `_multi_collect_one`(29676) · `_startup_date_reset`(32237)

### `_schedule_save(sched)`
- **위치**: `app.py:29130` (29130~29138)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 4)**: `_korea_schedule_post_epoch`(29141) · `_multi_schedule_fetch`(29620) · `multi_keirin_schedule`(30399) · `_startup_date_reset`(32237)

### `_timeline_snap_save(rk, phase, an)`
- **위치**: `app.py:31092` (31092~31125)
- **설명**: 지정 phase 스냅샷 저장(1회). T-5는 직전 T-7 대비 변경마 계산. 반환 저장된 snap dict 또는 None. *(docstring)*
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 1)**: `_timeline_snap_tick`(31140)  ⚠ 위험목록 함수 → 전수 나열

### `_auto_pred_save(rk, an)`
- **위치**: `app.py:31350` (31350~31373)
- **설명**: [마감5분전 최종 예상 저장] 분석(corePicks)에서 추천·유력마·단통 요약을 data/auto_prediction/ 에 저장. *(docstring)*
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 1)**: `_auto_pred_tick`(31404)

### `_save_log(rk, result)`
- **위치**: `gemini_reviewer.py:362` (362~371)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 2)**: `review_async`(440) · `_run`(449)

### `sweep(date)`
- **위치**: `review_engine.py:187` (187~233)
- **설명**: [1] 하루치 일괄 분류 → review_stats.json 병합 저장. 반환 요약. *(docstring)*
- **분류 근거**: 본문에서 os.replace 직접 호출
- **호출됨(Caller · 0)**: —

### `_json_atomic(path, obj)`
- **위치**: `tools/dedupe_odds_snapshots.py:92` (92~96)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 42)**: `_snapshot_timing_learn`(3041) · `_after_close_save`(4265) · `_near_miss_save`(4360) · `_signal_type_stats_save`(4404) · `_compression_stats_save`(4537) · `_high_odds_companion_stats_save`(4601) · `_third_place_stats_save`(4656) · `_signal_lesson_save`(4736) · `_ev_bands_update`(8548) · `_dark_cases_save`(8897) · `_pace_stats_recompute`(8945) · `_dark_stats_recompute`(9265) · `_dansung_cases_save`(9440) · `_record_alert`(12874) · `_match_alerts_to_result`(12915) · `_history_append`(13676) · `_history_save_analysis`(13914) · `_corrupt_note`(14030) · `_dr_record`(14368) · `_threshold_config_save`(15398) · `_learning_save`(15967) · `_upset_save`(15991) · `_discovered_save`(16538) · `_learn_aux_combos`(17498) · `recommend_manual`(17627) · `_apply_result_learning`(17723) · `_failure_save`(18595) · `_competitor_recompute_stats`(20114) · `_mark_result_in_log`(21084) · `_review_note_append`(21603) · `analysis_log_memo`(21629) · `_kra_section_records`(25756) · `_kra_jockey_changes`(25874) · `korea_history_save`(28908) · `_kakao_token_save`(30690) · `_kakao_anchor_log`(30745) · `_timeline_stats_recompute`(31299) · `_auto_pred_compare`(31376) · `_collect_cycle_end`(31687) · `_health_kakao_log`(31834) · `_snapshot_shortage_check`(32160) · `main`(99)  ⚠ 위험목록 함수 → 전수 나열

### `_count_save_failures()`
- **위치**: `tools/health_check.py:127` (127~149)
- **설명**: 당일 stdout 로그(회전본 포함)에서 WinError 를 **발생 지점별로** 센다. *(docstring)*
- **분류 근거**: 이름 규칙 (_save$|_save_|_append$|_record$|_record_|_log$|_atomic|_store_|_persist|_backup)
- **호출됨(Caller · 2)**: `check_save_failures_fixed`(152) · `check_save_failures_unfixed`(167)

# 수집 — fetch·파서·수집 진입점

> 🤖 **자동 생성** — `python tools/build_codemap.py` · 2026-08-12 08:36:46
> ⚠ 손으로 고치지 마세요. 다음 생성 때 덮어써집니다.
> ⚠ **AST 한계**: `getattr()` 동적 호출 · `importlib` 경유 · 문자열 디스패치는 잡지 못합니다
>   (`gemini_reviewer`·`health_check` 가 실제로 `importlib` 경유입니다).

총 **108개** 함수

### `_odds_load()`
- **위치**: `app.py:1206` (1206~1211)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 5)**: `odds_add_snapshot`(1249) · `odds_undo`(1280) · `odds_clear`(1295) · `odds_compute`(1301) · `odds_race_route`(31257)  ⚠ 위험목록 함수 → 전수 나열

### `_results_load()`
- **위치**: `app.py:1227` (1227~1232)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 5)**: `results_auto`(1400) · `results_get`(1468) · `_snapshot_result_for`(3691) · `_pub_result`(15035) · `results_auto_status`(24747)  ⚠ 위험목록 함수 → 전수 나열

### `_triple_load()`
- **위치**: `app.py:1479` (1479~1484)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 31)**: `_oddspark_covered_active`(1934) · `_do_triple_ingest`(2768) · `triple_reset`(3268) · `triple_latest`(3396) · `current_race`(4205) · `_triple_analyze`(10724) · `_combo_odds_trend`(14110) · `extract_japan`(14344) · `triple_match`(14481) · `public_races`(15056) · `public_matrix`(15085) · `triple_analyze`(15132) · `_build_analysis_log`(16347) · `_race_full`(19729) · `_apply_result_learning`(20801) · `_failure_report`(21980) · `dark_horse_review`(22611) · `_analyzed_race_keys`(24208) · `_all_analyzed_keys`(24347) · `_jp_form_backfill_once`(26794) · `debug_race_data`(28575) · `kra_ingest_detail`(30001) · `bmed_view`(30283) · `analysis_log_backfill`(31071) · `_multi_collect_one`(34659) · `multi_dashboard`(35542) · `multi_race_detail`(35842) · `kakao_send_race`(36218) · `_kakao_send_blocked_by_src`(36277) · `_auto_pred_tick`(36865) · `_startup_date_reset`(38950)  ⚠ 위험목록 함수 → 전수 나열

### `_starters_load()`
- **위치**: `app.py:1516` (1516~1521)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 23)**: `_form_from_starters`(1560) · `_ingest_ghost_verdict`(2238) · `_do_triple_ingest`(2768) · `_starters_prune`(3239) · `_triple_analyze`(10724) · `extract_japan`(14344) · `korea_form`(14399) · `_raw_profile_snapshot`(16199) · `_race_full`(19729) · `keirin_card`(25445) · `_keirin_autocollect_form`(25536) · `keiba_starters`(26619) · `_keiba_autocollect_form`(26684) · `_keiba_form_collect_by_rk`(26754) · `_jp_form_backfill_once`(26794) · `bmed_view`(30283) · `jra_starters`(31023) · `_nar_autocollect_form`(34079) · `_nar_form_fallback`(34125) · `_forecast_after_form`(34603) · `_grade_forecast_on_result`(34633) · `_nar_preload_once`(38506) · `_jra_preload_once`(38639)  ⚠ 위험목록 함수 → 전수 나열

### `_ingest_ghost_verdict(rk, q, sport)`
- **위치**: `app.py:2238` (2238~2310)
- **설명**: [1층 판정] 수신 배당에 출주 명단 밖 마번이 있으면 사유, 없으면 None. *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 1)**: `_do_triple_ingest`(2768)

### `_ingest_normalize_rk(rk, body)`
- **위치**: `app.py:2524` (2524~2555)
- **설명**: raceKey 의 경기장명만 표준키로 바꾼다. 실패·미매칭이면 **원문 그대로**. *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 1)**: `triple_ingest`(2473)

### `_do_triple_ingest(rk, q, x, tr, win, sport, category, source, deadline, scratched)`
- **위치**: `app.py:2768` (2768~3236)
- **설명**: [코어] 3종 배당 스냅샷 저장 + 히스토리 누적 → 역배열·배당변화·이상감지 파이프라인 공용. *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 7)**: `triple_ingest`(2473) · `keirin_odds`(25675) · `keiba_odds`(26024) · `_kra_collect_one`(27165) · `kra_ingest_detail`(30001) · `_jra_collect_once`(30492) … [+1]

### `_race_pin_load()`
- **위치**: `app.py:3325` (3325~3328)
- **설명**: 고정된 raceKey 반환(없으면 None). 파일 기반 → 서버 재시작에도 유지. *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 3)**: `race_pin`(3377) · `triple_latest`(3396) · `current_race`(4205)

### `_board_hint_load()`
- **위치**: `app.py:3363` (3363~3373)
- **설명**: 신선한(TTL 내) 배당판 힌트 {raceKey, sport, t} 반환. 없거나 오래되면 None. *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 3)**: `triple_latest`(3396) · `current_race`(4205) · `public_races`(15056)

### `_jp_jockeys_load()`
- **위치**: `app.py:4428` (4428~4435)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 3)**: `_jp_jockeys_accumulate`(4459) · `_jp_jockey_place_rate`(4500) · `_jp_jockey_rate_ctx`(4523)  ⚠ 위험목록 함수 → 전수 나열

### `_parse_combo_map(qd)`
- **위치**: `app.py:4839` (4839~4847)
- **설명**: 히스토리 스냅샷의 quinella dict('1+2':45.2) → {(1,2):45.2}. *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 1)**: `_time_based_drop_signals`(4856)

### `_after_close_load()`
- **위치**: `app.py:5214` (5214~5218)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 4)**: `after_close_cases`(3655) · `_record_after_close_case`(5226) · `_after_close_learn_result`(5254) · `_after_close_stats`(5289)  ⚠ 위험목록 함수 → 전수 나열

### `_near_miss_load()`
- **위치**: `app.py:5311` (5311~5315)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 3)**: `learning_near_miss`(3559) · `_record_near_miss`(5323) · `_near_miss_frequent`(5339)  ⚠ 위험목록 함수 → 전수 나열

### `_signal_type_stats_load()`
- **위치**: `app.py:5357` (5357~5361)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 3)**: `signal_types_stats`(3575) · `_learn_strong_signals`(5369) · `_signal_type_hitrates`(5465)  ⚠ 위험목록 함수 → 전수 나열

### `_compression_stats_load()`
- **위치**: `app.py:5492` (5492~5496)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 3)**: `compression_stats`(3585) · `_learn_compression`(5504) · `_compression_hitrate`(5532)  ⚠ 위험목록 함수 → 전수 나열

### `_high_odds_companion_stats_load()`
- **위치**: `app.py:5558` (5558~5562)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 2)**: `_learn_high_odds_companion`(5570) · `_high_odds_companion_hitrate`(5599)  ⚠ 위험목록 함수 → 전수 나열

### `_third_place_stats_load()`
- **위치**: `app.py:5615` (5615~5619)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 3)**: `third_place_stats`(3594) · `_learn_third_place`(5641) · `_third_place_hitrate`(5670)  ⚠ 위험목록 함수 → 전수 나열

### `_signal_lesson_load()`
- **위치**: `app.py:5697` (5697~5701)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 2)**: `learning_signal_lessons`(4191) · `_record_signal_lesson`(5709)  ⚠ 위험목록 함수 → 전수 나열

### `_ev_bands_load()`
- **위치**: `app.py:9734` (9734~9738)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 2)**: `_ev_band_p`(9751) · `_ev_bands_update`(9772)  ⚠ 위험목록 함수 → 전수 나열

### `_ev_band_key(o)`
- **위치**: `app.py:9741` (9741~9745)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 도메인 2차 (fetch|parse|collect|ingest|
- **호출됨(Caller · 2)**: `_ev_band_p`(9751) · `_ev_bands_update`(9772)

### `_dark_cases_load()`
- **위치**: `app.py:10116` (10116~10120)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 2)**: `_dark_case_similar`(10596) · `_dark_case_record`(10612)  ⚠ 위험목록 함수 → 전수 나열

### `_norm_sig_key(s)`
- **위치**: `app.py:10257` (10257~10269)
- **설명**: [신호 라벨 정규화] 이중표기('크로스역배열 크로스역배열')·이모지·인접중복을 단일 라벨로 축약 → 통계 키 통일. *(docstring)*
- **분류 근거**: 도메인 2차 (fetch|parse|collect|ingest|
- **호출됨(Caller · 2)**: `_dark_signal_of`(10272) · `_dark_stats_recompute`(10493)

### `_dark_stats_load()`
- **위치**: `app.py:10486` (10486~10490)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 1)**: `dark_horse_stats`(22556)  ⚠ 위험목록 함수 → 전수 나열

### `_dansung_cases_load()`
- **위치**: `app.py:10662` (10662~10667)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 2)**: `dansung_cases`(3664) · `_dansung_case_record`(10678)  ⚠ 위험목록 함수 → 전수 나열

### `_hist_read_any(json_path)`
- **위치**: `app.py:15360` (15360~15373)
- **설명**: [영구보존·4번] odds_history 파일 읽기 — .json 우선, 없으면 7일+ 압축본(.json.gz) 투명 해제. *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 8)**: `_snapshot_signal_first`(3770) · `triple_analyze`(15132) · `_dr_record`(16157) · `odds_archive_list`(20429) · `odds_archive_get`(20468) · `_multi_collect_one`(34659) … [+2]

### `_canonical_log_key(rk, live)`
- **위치**: `app.py:15941` (15941~16020)
- **설명**: [중복 key 근본 방지] 같은 경주가 다른 표기(한글 '모리오카 1경주' / 한자 '2026-07-07 盛岡 1R')로 *(docstring)*
- **분류 근거**: 도메인 2차 (fetch|parse|collect|ingest|
- **호출됨(Caller · 22)**: `_pace_log_record`(10142) · `_triple_analyze`(10724) · `triple_analyze`(15132) · `_build_analysis_log`(16347) · `_card_odds_path`(19230) · `_race_card_timeline`(19280) … [+16]

### `_threshold_config_load()`
- **위치**: `app.py:17407` (17407~17413)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 4)**: `thresholds_config`(3616) · `thresholds_apply`(3623) · `_threshold_config_save`(17416) · `_threshold_candidates`(17466)  ⚠ 위험목록 함수 → 전수 나열

### `_learning_load()`
- **위치**: `app.py:18032` (18032~18036)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 8)**: `_learned_integrated_weights`(7421) · `_triple_analyze`(10724) · `_dl_reliability`(17869) · `_apply_result_learning`(20801) · `_failure_stats`(22115) · `results_adjust`(24468) · `learning_stats`(24505) · `stats_today`(24610)  ⚠ 위험목록 함수 → 전수 나열

### `_upset_load()`
- **위치**: `app.py:18055` (18055~18062)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 5)**: `_learn_mass_drop`(18077) · `_learn_smart_money`(18124) · `_learn_upset`(18395) · `learning_upset`(24512) · `learning_reversal_stats`(24696)  ⚠ 위험목록 함수 → 전수 나열

### `_signal_stats_load()`
- **위치**: `app.py:18221` (18221~18234)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 4)**: `_learn_signal_stats`(18248) · `_signal_reliability`(18349) · `stats_today`(24610) · `learning_signal_stats`(24676)  ⚠ 위험목록 함수 → 전수 나열

### `_mid_high_stats_load()`
- **위치**: `app.py:18283` (18283~18295)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 2)**: `_learn_mid_high_odds`(18309) · `learning_mid_high_odds`(24664)  ⚠ 위험목록 함수 → 전수 나열

### `_discovered_load()`
- **위치**: `app.py:18616` (18616~18621)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 1)**: `patterns_discovered`(24709)  ⚠ 위험목록 함수 → 전수 나열

### `_parse(s)`
- **위치**: `app.py:20630` (20630~20633)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 1)**: `_rec_combos_from_analysis_log`(20597)

### `_failure_load()`
- **위치**: `app.py:21679` (21679~21689)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 3)**: `_failure_record`(21914) · `_failure_stats`(22115) · `failure_rules`(22548)  ⚠ 위험목록 함수 → 전수 나열

### `_rr_load_by_key(rk)`
- **위치**: `app.py:22178` (22178~22195)
- **설명**: raceKey(또는 race_id)로 race_results/ 파일 1건 로드(가장 최근). *(docstring)*
- **분류 근거**: 도메인 2차 (fetch|parse|collect|ingest|
- **호출됨(Caller · 1)**: `_high_odds_review_one`(22372)

### `_snap_key(date, rk)`
- **위치**: `app.py:22679` (22679~22681)
- **설명**: 인덱스 키 — 날짜를 반드시 포함한다. *(docstring)*
- **분류 근거**: 도메인 2차 (fetch|parse|collect|ingest|
- **호출됨(Caller · 2)**: `_snapshot_index`(22684) · `_day_snapshot_for`(22720)

### `_high_odds_cases_load()`
- **위치**: `app.py:23054` (23054~23065)
- **설명**: 저장된 수동 케이스(manual_case=True) 목록(최신순). *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 1)**: `high_odds_review`(23069)

### `_parse_result_tsv(text)`
- **위치**: `app.py:23861` (23861~23934)
- **설명**: [일괄등록·탭구분 텍스트] 헤더+탭(\t) 구분 결과 붙여넣기 파싱(스프레드시트/엑셀 복사 대응). *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 1)**: `_parse_result_rows`(23937)

### `_parse_result_rows(html)`
- **위치**: `app.py:23937` (23937~24005)
- **설명**: 결과 페이지 HTML → [{area,round,no1,no2,no3,qOdds,tOdds}]. 헤더 라벨로 컬럼 판별. *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 1)**: `results_bulk`(24421)

### `_resolve_race_key(rk, candidates)`
- **위치**: `app.py:24228` (24228~24257)
- **설명**: 입력 raceKey를 실제 분석된 key로 유연 매칭(형식 불일치 방어). *(docstring)*
- **분류 근거**: 도메인 2차 (fetch|parse|collect|ingest|
- **호출됨(Caller · 11)**: `_snapshot_result_for`(3691) · `public_matrix`(15085) · `failure_report`(22529) · `high_odds_review`(23069) · `competitor_record`(23357) · `competitor_get`(23398) … [+5]

### `_match_row_to_key(row, analyzed_keys)`
- **위치**: `app.py:24290` (24290~24296)
- **설명**: 결과행(area,round) → 분석된 raceKey 유연 매칭(한국·일본 모든 경기). 실패 시 None. *(docstring)*
- **분류 근거**: 도메인 2차 (fetch|parse|collect|ingest|
- **호출됨(Caller · 0)**: —

### `_register_result_rows(rows, stake, analyzed_keys)`
- **위치**: `app.py:24367` (24367~24417)
- **설명**: 결과행 리스트 → 분석경주 자동매칭 → 적중판정·학습 → 요약. *(docstring)*
- **분류 근거**: 도메인 2차 (fetch|parse|collect|ingest|
- **호출됨(Caller · 2)**: `result_ocr`(838) · `results_bulk`(24421)

### `_parse_reversal_signal(s)`
- **위치**: `app.py:24533` (24533~24549)
- **설명**: [4번] analysis_log signals_detected 의 역배열 신호 → (challenger, ratio) 추출(두 포맷 지원). *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 1)**: `_reversal_strength_stats`(24552)

### `_keirin_parse_card(html)`
- **위치**: `app.py:25066` (25066~25146)
- **설명**: oddspark 경륜 출마표 HTML → {venue,race_no,dist,post,tendency,riders,line,comment}. *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 2)**: `keirin_card`(25445) · `_keirin_autocollect_form`(25536)

### `_keirin_url(jo, ymd, race)`
- **위치**: `app.py:25190` (25190~25192)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 4)**: `keirin_card`(25445) · `_keirin_autocollect_form`(25536) · `_keirin_post_time`(33714) · `_keirin_venue_races`(33734)

### `_oddspark_parse_trio(html, max_no)`
- **위치**: `app.py:25195` (25195~25218)
- **설명**: [삼복승 서버 수집 (2026-07-19)] oddspark 3連複 배당 페이지 → [{combo:[a,b,c], odds}] (방어적 텍스트 파싱). *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 1)**: `_multi_collect_one`(34659)

### `_keirin_odds_url(jo, ymd, race, bet_type)`
- **위치**: `app.py:25221` (25221~25224)
- **설명**: oddspark 경륜 배당 페이지 URL. betType 5=복승(2車複)·6=쌍승(2車単). *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 3)**: `keirin_odds`(25675) · `keiba_odds`(26024) · `_multi_collect_one`(34659)

### `_keiba_parse_quinella_grid(html)`
- **위치**: `app.py:25279` (25279~25283)
- **설명**: [파서 재작성 v2] 복승(馬連=betType6) — colmap 그리드 파싱(9두+ 블록·꼬리 포함 전체 복원). *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 1)**: `_keiba_parse_quinella`(25897)

### `_keiba_parse_trio_grid(html, max_no)`
- **위치**: `app.py:25286` (25286~25291)
- **설명**: [파서 재작성 v2] 삼복승(3連複=betType9) — NAR 페이지는 'a-b-c 텍스트'가 아니라 짝헤더(1-2 등) *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 1)**: `_multi_collect_one`(34659)

### `_keirin_table_rows(html)`
- **위치**: `app.py:25294` (25294~25303)
- **설명**: oddspark 배당 HTML → 표 행 리스트(각 행=셀 텍스트 리스트). 태그 제거·공백 정리. *(docstring)*
- **분류 근거**: 도메인 2차 (fetch|parse|collect|ingest|
- **호출됨(Caller · 6)**: `_keirin_parse_quinella`(25327) · `_keirin_parse_exacta`(25350) · `_keiba_matrix_rows`(25834) · `_keiba_parse_win`(25848) · `_keiba_horse_count`(25887) · `_keirin_result_parse`(27866)

### `_keirin_parse_quinella(html)`
- **위치**: `app.py:25327` (25327~25347)
- **설명**: 복승(betType=5) 매트릭스 파싱 → [{combo:[a,b], odds}]. 상삼각: 행 축차 k, 쌍 index j → 조합{j+1,k}. *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 3)**: `keirin_odds`(25675) · `keiba_odds`(26024) · `_multi_collect_one`(34659)

### `_keirin_parse_exacta(html)`
- **위치**: `app.py:25350` (25350~25368)
- **설명**: 쌍승(betType=6) 매트릭스 파싱 → [{combo:[1착,2착], odds}]. 방향성: 행 축차=1착, 쌍 index j → 2착=j+1. *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 3)**: `keirin_odds`(25675) · `keiba_odds`(26024) · `_multi_collect_one`(34659)

### `_keirin_fetch(url, mode)`
- **위치**: `app.py:25403` (25403~25440)
- **설명**: 공용 fetch. ⚠ oddspark(경륜)·keiba.go.jp(지방)는 **종전과 완전히 동일**하게 지난다. *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 22)**: `_keirin_autocollect_form`(25536) · `keirin_odds`(25675) · `_keiba_schedule`(25968) · `keiba_odds`(26024) · `_keiba_current_raceno`(26142) · `_keiba_fetch_details`(26595) … [+16]

### `_keirin_autocollect_form(rk, jo, ymd, race)`
- **위치**: `app.py:25536` (25536~25623)
- **설명**: [경륜 출마표 전적 자동 수집] 배당 수집 시 동시에 oddspark 출마표에서 전적(競走得点·착순·결정수·각질) 수집·저장. *(docstring)*
- **분류 근거**: 도메인 2차 (fetch|parse|collect|ingest|
- **호출됨(Caller · 4)**: `_do_triple_ingest`(2768) · `keirin_odds`(25675) · `keiba_odds`(26024) · `_multi_collect_one`(34659)

### `_keiba_odds_url(op_track, sponsor, ymd, race_nb, bet_type)`
- **위치**: `app.py:25809` (25809~25813)
- **설명**: oddspark 지방경마 배당 URL. betType 6=복승(馬連)·5=쌍승(馬単). *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 2)**: `keiba_odds`(26024) · `_multi_collect_one`(34659)

### `_keiba_matrix_rows(html)`
- **위치**: `app.py:25834` (25834~25845)
- **설명**: 배당 매트릭스 행만 추출(선두=차번호·소수배당 포함·마명(가나/한자) 없는 행). 마방리스트 제외. *(docstring)*
- **분류 근거**: 도메인 2차 (fetch|parse|collect|ingest|
- **호출됨(Caller · 2)**: `_keiba_parse_quinella`(25897) · `_keiba_parse_exacta`(25941)

### `_keiba_parse_win(html)`
- **위치**: `app.py:25848` (25848~25884)
- **설명**: 단승(単勝=betType1) 표 → {마번(str): 단승배당(float)}. 실패·미발매 시 {}. *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 2)**: `keiba_odds`(26024) · `_multi_collect_one`(34659)

### `_keiba_parse_quinella(html)`
- **위치**: `app.py:25897` (25897~25938)
- **설명**: 복승(馬連=betType6) 매트릭스 → [{combo:[a,b], odds}]. 상대차 라벨을 따라가는 walking 파싱. *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 2)**: `keiba_odds`(26024) · `_multi_collect_one`(34659)

### `_keiba_parse_exacta(html)`
- **위치**: `app.py:25941` (25941~25965)
- **설명**: 쌍승(馬単=betType5) 그리드 → [{combo:[1着,2着], odds}]. 행=2着(axis)·열 위치=1着. *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 2)**: `keiba_odds`(26024) · `_multi_collect_one`(34659)

### `_keiba_parse_shutsuba(html)`
- **위치**: `app.py:26269` (26269~26367)
- **설명**: oddspark 지방경마 출주표(RaceList.do) → {venue,raceNo,distance,surface,trackCond,horses:[...]}. *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 2)**: `keiba_starters`(26619) · `_keiba_autocollect_form`(26684)

### `_keiba_parse_horse_past(html, max_n)`
- **위치**: `app.py:26370` (26370~26395)
- **설명**: HorseDetail.do → 전적 리스트(최근순 최대 max_n). 各: {date,venue,distance,surface,trackCond, *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 2)**: `_keiba_fetch_details`(26595) · `_one`(26601)

### `_keiba_fetch_details(shutsuba, api_key)`
- **위치**: `app.py:26595` (26595~26615)
- **설명**: 출전마별 HorseDetail 을 ThreadPool(동시 4)로 병렬 fetch·파싱 → {lineageNb: past[]}. *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 2)**: `keiba_starters`(26619) · `_keiba_autocollect_form`(26684)

### `_keiba_autocollect_form(rk, op_track, sponsor, ymd, race_nb)`
- **위치**: `app.py:26684` (26684~26746)
- **설명**: [지방경마(NAR) 전적 자동수집] 서버 배당 수집(_multi_collect_one) 시 oddspark 出走表+전5경주 전적을 함께 수집·저장(경주당 1회). *(docstring)*
- **분류 근거**: 도메인 2차 (fetch|parse|collect|ingest|
- **호출됨(Caller · 2)**: `_keiba_form_collect_by_rk`(26754) · `_multi_collect_one`(34659)

### `_keiba_form_collect_by_rk(rk, ymd, force)`
- **위치**: `app.py:26754` (26754~26778)
- **설명**: [방법 A] raceKey → (경마장, 경주번호) → oddspark 트랙코드(opTrackCd/sponsorCd) 재해석 → 서버가 직접 전적 수집. *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 2)**: `keiba_form`(26782) · `_jp_form_backfill_once`(26794)

### `_kra_api_key()`
- **위치**: `app.py:26847` (26847~26848)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 도메인 2차 (fetch|parse|collect|ingest|
- **호출됨(Caller · 6)**: `_kra_fetch`(27050) · `_kra_backfill_loop`(27405) · `kra_test`(29083) · `kra_status`(29220) · `_kra_api_get`(29379) · `kra_apis_test`(29845)

### `_weather_api_key()`
- **위치**: `app.py:26864` (26864~26865)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 도메인 2차 (fetch|parse|collect|ingest|
- **호출됨(Caller · 3)**: `_weather_fetch_raw`(26903) · `weather_current`(26996) · `_weather_auto_start`(27030)

### `_weather_fetch_raw(venue)`
- **위치**: `app.py:26903` (26903~26925)
- **설명**: OpenWeatherMap current weather → 표준 dict. 키 없거나 실패 시 더미. (data, source). *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 1)**: `_weather_get`(26928)

### `_weather_for_race_key(rk)`
- **위치**: `app.py:26949` (26949~26955)
- **설명**: raceKey 에서 경마장 추출 → 날씨. 미매칭 시 None. *(docstring)*
- **분류 근거**: 도메인 2차 (fetch|parse|collect|ingest|
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_kra_fetch(meet, rc_date, rc_no, num_rows)`
- **위치**: `app.py:27050` (27050~27086)
- **설명**: API160_1/integratedInfo_1 호출 → (items[list], meta[dict], err[str|None]). read-only 조회. *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 3)**: `_kra_collect_one`(27165) · `kra_test`(29083) · `_kra_schedule`(33798)

### `_kra_parse_items(items)`
- **위치**: `app.py:27113` (27113~27162)
- **설명**: KRA API160 items → 파이프라인 포맷 (win[dict], q[list], x[list], tr[list]). *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 3)**: `_kra_collect_one`(27165) · `kra_test`(29083) · `_kra_schedule`(33798)

### `_kra_collect_one(rk, meet, rc_date, rc_no)`
- **위치**: `app.py:27165` (27165~27181)
- **설명**: KRA 1경주 배당 수집 → 파이프라인 주입. (ok, info) 반환. 실패해도 확장 폴백에 무영향. *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 2)**: `_kra_auto_loop`(27272) · `kra_collect`(29169)

### `_kra_collect_result_one(rk, meet, rc_date, rc_no)`
- **위치**: `app.py:27224` (27224~27242)
- **설명**: [KRA 결과 자동수집·저장] rcTime 착순 추출 → 기존 결과 학습 경로(_apply_result_learning) 재사용. *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 3)**: `kra_result`(27246) · `_kra_auto_loop`(27272) · `_kra_result_backfill_once`(27368)

### `_keiba_parse_result(html)`
- **위치**: `app.py:27533` (27533~27561)
- **설명**: RaceMarkTable HTML → 착순 상위3 마번[list] 또는 None. 着順/馬番 컬럼 라벨 기반(방어). *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 1)**: `_keiba_result_top3`(27593)

### `_keirin_result_parse(html)`
- **위치**: `app.py:27866` (27866~27997)
- **설명**: RaceKekka.do HTML → {venue, top3:[차번...], quinellaOdds}. 방어적 파싱(구조 변형 대비). *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 1)**: `_keirin_result_top3`(28000)

### `_fn_parse(fn)`
- **위치**: `app.py:28267` (28267~28275)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 1)**: `daily_cards_page`(28255)

### `_kra_track_op_load()`
- **위치**: `app.py:29301` (29301~29309)
- **설명**: 캐시 파일에서 이미 확정된 op 경로 로드(있으면 탐색 생략). *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 1)**: `_kra_track_op_resolve`(29337)

### `_jra_combo_key(k)`
- **위치**: `app.py:30398` (30398~30405)
- **설명**: 조합키 → 마번 리스트. 복승 `"0102"`→[1,2] · 삼복승 `"010203"`→[1,2,3]. *(docstring)*
- **분류 근거**: 도메인 2차 (fetch|parse|collect|ingest|
- **호출됨(Caller · 1)**: `_jra_fetch_odds`(30434)

### `_jra_fetch_odds(race_id, otype)`
- **위치**: `app.py:30434` (30434~30452)
- **설명**: (status, {조합키: 배당}, official_datetime). 실패 시 (None, {}, None). *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 1)**: `_jra_collect_once`(30492)

### `_jra_collect_once()`
- **위치**: `app.py:30492` (30492~30530)
- **설명**: 마감 창에 든 JRA 경주의 배당을 1회 수집. 반환 (수집건수, 생략건수, 마감임박여부). *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 1)**: `_jra_bg_loop`(30533)

### `_jra_parse_result(html)`
- **위치**: `app.py:30606` (30606~30633)
- **설명**: → {finished, order:[{rank,no,name}], top3, payouts, payouts_raw} *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 1)**: `_jra_result_once`(30693)

### `_jra_parse_shutuba_past(html)`
- **위치**: `app.py:30906` (30906~30952)
- **설명**: netkeiba 馬柱(shutuba_past) → {venue,raceNo,distance,surface,trackCond,horses:[...]}. *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 2)**: `jra_starters`(31023) · `_jra_preload_once`(38639)

### `_parse_body_weight(bw)`
- **위치**: `app.py:31745` (31745~31750)
- **설명**: 마체중 표기('486(-4)'·'486kg'·486) → 정수 kg. 괄호 안 증감은 무시(절대값만). 실패 None. *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 1)**: `body_weight_change_bonus`(31753)

### `_pdf_cache_load(md5)`
- **위치**: `app.py:32486` (32486~32497)
- **설명**: 같은 해시의 '완료된' 세션 캐시를 반환(없으면 None). *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 1)**: `korea_start`(33217)

### `_korea_load()`
- **위치**: `app.py:32526` (32526~32531)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 5)**: `_korea_run_job`(32957) · `korea_status`(33296) · `korea_session`(33304) · `korea_reextract`(33324) · `_korea_maybe_resume`(33495)

### `_prerace_key(date, venue, race_no)`
- **위치**: `app.py:32678` (32678~32681)
- **설명**: 파일 안전 키: '2026-07-05_부산_3'. 경마장 미상은 '기타'. *(docstring)*
- **분류 근거**: 도메인 2차 (fetch|parse|collect|ingest|
- **호출됨(Caller · 1)**: `_prerace_save_race`(32684)

### `_prerace_index_load()`
- **위치**: `app.py:32730` (32730~32754)
- **설명**: index.json 로드. 없으면 디렉터리 스캔으로 복구(디스크가 진실 소스). *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 2)**: `_prerace_index_update`(32718) · `korea_prerace_list`(33360)  ⚠ 위험목록 함수 → 전수 나열

### `_prerace_load(key)`
- **위치**: `app.py:32757` (32757~32765)
- **설명**: key 로 경주 1건 전체(리포트 포함) 로드. 없으면 None. *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 1)**: `korea_prerace_get`(33366)

### `_schedule_load()`
- **위치**: `app.py:33601` (33601~33606)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 11)**: `_sched_post_epoch`(25651) · `_keirin_jo_today`(27835) · `keirin_today_stats`(28638) · `_race_post_epoch`(30202) · `_korea_schedule_post_epoch`(33620) · `multi_keirin_schedule`(35478) · `multi_schedule`(35512) · `multi_collect_now`(35523) · `multi_dashboard`(35542) · `_multi_bg_loop`(37027) · `_startup_date_reset`(38950)  ⚠ 위험목록 함수 → 전수 나열

### `_nar_fetch(url)`
- **위치**: `app.py:33865` (33865~33877)
- **설명**: keiba.go.jp 페이지 fetch + 디코드. ⚠ 이 사이트는 UTF-8 이다 — *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 3)**: `_nar_autocollect_form`(34079) · `_nar_race_list`(34191) · `_multi_collect_one`(34659)

### `_nar_odds_url(baba, ymd, rno, kind)`
- **위치**: `app.py:33884` (33884~33887)
- **설명**: odds_flg=4 = '馬番順' — 축마별 단순 표(파싱 최단·열 추적 불필요). *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 1)**: `_multi_collect_one`(34659)

### `_nar_parse_pair_odds(html, keep_direction, max_no)`
- **위치**: `app.py:33890` (33890~33915)
- **설명**: 馬連複/馬連単(馬番順) → [{'combo':[a,b],'odds':x}]. *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 1)**: `_multi_collect_one`(34659)

### `_nar_parse_win(html, max_no)`
- **위치**: `app.py:33918` (33918~33933)
- **설명**: 単勝・複勝 표 → {마번: 단승배당}. 행 = [枠, 馬番, 馬名, 単勝, 複勝하한 - 상한]. *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 1)**: `_multi_collect_one`(34659)

### `_nar_parse_trio(html, max_no)`
- **위치**: `app.py:33939` (33939~33974)
- **설명**: 三連複(馬番順) → [{'combo':[a,b,c],'odds':x}]. *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 1)**: `_multi_collect_one`(34659)

### `_nar_parse_deba(html, max_no)`
- **위치**: `app.py:34014` (34014~34073)
- **설명**: DebaTable → (shutsuba, details) — `_keiba_build_form` 입력 형태로 정규화. *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 1)**: `_nar_autocollect_form`(34079)

### `_nar_autocollect_form(rk, baba, ymd, rno)`
- **위치**: `app.py:34079` (34079~34111)
- **설명**: [南関東 전적 자동수집] keiba.go.jp DebaTable → starters_store. `_keiba_autocollect_form` 의 NAR판. *(docstring)*
- **분류 근거**: 도메인 2차 (fetch|parse|collect|ingest|
- **호출됨(Caller · 3)**: `_nar_form_fallback`(34125) · `_multi_collect_one`(34659) · `_nar_preload_once`(38506)

### `_multi_schedule_fetch()`
- **위치**: `app.py:34223` (34223~34267)
- **설명**: [1번] 오늘 개최 경주 스케줄(경마장+경주번호+발주시각) → today_schedule.json. 실패해도 예외 전파 안 함. *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 2)**: `multi_schedule`(35512) · `multi_collect_now`(35523)

### `_multi_key(venue, race_no)`
- **위치**: `app.py:34270` (34270~34272)
- **설명**: [岐阜·기후 중복 통합] 저장키의 경마장명을 표준(한글)으로 정규화 → 岐阜/기후가 한 키로 병합(중복 저장 방지). *(주석(본문))*
- **분류 근거**: 도메인 2차 (fetch|parse|collect|ingest|
- **호출됨(Caller · 10)**: `_triple_analyze`(10724) · `_multi_collect_one`(34659) · `multi_dashboard`(35542) · `_korea_kakao_targets`(36527) · `_auto_pred_tick`(36865) · `_collect_cycle_begin`(37127) … [+4]

### `_amedas_fetch()`
- **위치**: `app.py:34405` (34405~34426)
- **설명**: 최신 관측 맵을 받아 캐시. 반환 (data, obsTime) — 실패 시 (None, None). *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 1)**: `_amedas_wind`(34429)

### `_multi_collect_one(track, race, ymd)`
- **위치**: `app.py:34659` (34659~34932)
- **설명**: [2번] 한 경주 배당 수집 → multi_race_store 저장(실패 격리·triple_store 미접근). 반환 raceKey 또는 None. *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 2)**: `multi_collect_now`(35523) · `_multi_bg_loop`(37027)

### `_results_history_rows(ttl)`
- **위치**: `app.py:35172` (35172~35216)
- **설명**: [결과기록 전면개편] data/analysis_log/ → 카드 행 목록(최신순). *(docstring)*
- **분류 근거**: 도메인 2차 (fetch|parse|collect|ingest|
- **호출됨(Caller · 2)**: `results_history`(35256) · `_multi_card`(35366)

### `_collect_cycle_begin(sched, targets, now, cycle_no)`
- **위치**: `app.py:37127` (37127~37157)
- **설명**: [수집 사이클 관측 (2026-07-30)] 이 사이클의 대상 선정 결과를 기록용 dict 로 만든다. *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 1)**: `_multi_bg_loop`(37027)

### `_collect_cycle_end(obs, elapsed, sched, now)`
- **위치**: `app.py:37160` (37160~37205)
- **설명**: [수집 사이클 관측] 소요시간 + '창 밖인데 방금 갱신된 경주' 를 붙여 확정 기록. *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 1)**: `_multi_bg_loop`(37027)

### `_midcheck_stamp_load()`
- **위치**: `app.py:37807` (37807~37822)
- **설명**: 스탬프 읽기. 🔴 **실패를 빈 dict 로 삼키지 않는다**(원칙 9) — 실패면 None. *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 3)**: `_midcheck_run`(38373) · `_start_midcheck_scheduler`(38419) · `_loop`(38426)  ⚠ 위험목록 함수 → 전수 나열

### `_midcheck_collect(prev)`
- **위치**: `app.py:38172` (38172~38212)
- **설명**: 점검 사실 수집(완전 읽기 전용). `prev` 는 직전 점검 스탬프(없으면 {}). *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 1)**: `_midcheck_run`(38373)

### `_gemini_api_key()`
- **위치**: `gemini_reviewer.py:45` (45~46)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 도메인 2차 (fetch|parse|collect|ingest|
- **호출됨(Caller · 2)**: `review_async`(440) · `_run`(449)

### `_pair_key(k)`
- **위치**: `gemini_reviewer.py:99` (99~103)
- **설명**: 조합 키를 '1+2' 문자열로 정규화((1,2) 튜플·'1+2'·[1,2] 모두 허용). *(docstring)*
- **분류 근거**: 도메인 2차 (fetch|parse|collect|ingest|
- **호출됨(Caller · 1)**: `_fmt_odds_map`(131)

### `_load(path)`
- **위치**: `review_engine.py:34` (34~38)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 4)**: `sweep`(187) · `signal_place_stats`(248) · `drop_stats`(331) · `replay_day`(478)

### `snapshot_ingest_counts(day)`
- **위치**: `tools/health_check.py:958` (958~977)
- **설명**: (당일 스냅샷 PNG 수, 마지막 저장 시각, 발주완료 경주 수) — D안 API 와 공유한다. *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 2)**: `snapshot_health_api`(4119) · `check_snapshot_ingest`(980)

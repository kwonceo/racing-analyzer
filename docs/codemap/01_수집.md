# 수집 — fetch·파서·수집 진입점

> 🤖 **자동 생성** — `python tools/build_codemap.py` · 2026-07-30 22:47:30
> ⚠ 손으로 고치지 마세요. 다음 생성 때 덮어써집니다.
> ⚠ **AST 한계**: `getattr()` 동적 호출 · `importlib` 경유 · 문자열 디스패치는 잡지 못합니다
>   (`gemini_reviewer`·`health_check` 가 실제로 `importlib` 경유입니다).

총 **77개** 함수

### `_odds_load()`
- **위치**: `app.py:1199` (1199~1204)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 5)**: `odds_add_snapshot`(1242) · `odds_undo`(1273) · `odds_clear`(1288) · `odds_compute`(1294) · `odds_race_route`(27076)  ⚠ 위험목록 함수 → 전수 나열

### `_results_load()`
- **위치**: `app.py:1220` (1220~1225)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 5)**: `results_auto`(1393) · `results_get`(1461) · `_snapshot_result_for`(2893) · `_pub_result`(13364) · `results_auto_status`(21502)  ⚠ 위험목록 함수 → 전수 나열

### `_triple_load()`
- **위치**: `app.py:1472` (1472~1477)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 28)**: `_oddspark_covered_active`(1846) · `_do_triple_ingest`(2166) · `triple_reset`(2470) · `triple_latest`(2598) · `current_race`(3249) · `_combo_odds_trend`(12491) · `extract_japan`(12721) · `triple_match`(12810) · `public_races`(13385) · `public_matrix`(13414) · `triple_analyze`(13461) · `_build_analysis_log`(14451) · `_apply_result_learning`(17723) · `_failure_report`(18881) · `dark_horse_review`(19512) · `_analyzed_race_keys`(20963) · `_all_analyzed_keys`(21102) · `_jp_form_backfill_once`(23246) · `debug_race_data`(24918) · `kra_ingest_detail`(26292) · `bmed_view`(26574) · `analysis_log_backfill`(26890) · `_multi_collect_one`(29676) · `multi_dashboard`(30463) · `multi_race_detail`(30609) · `kakao_send_race`(30937) · `_auto_pred_tick`(31404) · `_startup_date_reset`(32237)  ⚠ 위험목록 함수 → 전수 나열

### `_starters_load()`
- **위치**: `app.py:1498` (1498~1503)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 16)**: `_form_from_starters`(1542) · `_do_triple_ingest`(2166) · `_starters_prune`(2441) · `_triple_analyze`(9492) · `extract_japan`(12721) · `korea_form`(12766) · `_raw_profile_snapshot`(14410) · `keirin_card`(22079) · `_keirin_autocollect_form`(22134) · `keiba_starters`(23095) · `_keiba_autocollect_form`(23159) · `_keiba_form_collect_by_rk`(23206) · `_jp_form_backfill_once`(23246) · `bmed_view`(26574) · `jra_starters`(26842) · `_nar_autocollect_form`(29553)  ⚠ 위험목록 함수 → 전수 나열

### `_do_triple_ingest(rk, q, x, tr, win, sport, category, source, deadline, scratched)`
- **위치**: `app.py:2166` (2166~2438)
- **설명**: [코어] 3종 배당 스냅샷 저장 + 히스토리 누적 → 역배열·배당변화·이상감지 파이프라인 공용. *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 6)**: `triple_ingest`(1943) · `keirin_odds`(22252) · `keiba_odds`(22601) · `_kra_collect_one`(23617) · `kra_ingest_detail`(26292) · `_multi_collect_one`(29676)

### `_race_pin_load()`
- **위치**: `app.py:2527` (2527~2530)
- **설명**: 고정된 raceKey 반환(없으면 None). 파일 기반 → 서버 재시작에도 유지. *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 3)**: `race_pin`(2579) · `triple_latest`(2598) · `current_race`(3249)

### `_board_hint_load()`
- **위치**: `app.py:2565` (2565~2575)
- **설명**: 신선한(TTL 내) 배당판 힌트 {raceKey, sport, t} 반환. 없거나 오래되면 None. *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 3)**: `triple_latest`(2598) · `current_race`(3249) · `public_races`(13385)

### `_jp_jockeys_load()`
- **위치**: `app.py:3472` (3472~3479)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 3)**: `_jp_jockeys_accumulate`(3503) · `_jp_jockey_place_rate`(3544) · `_jp_jockey_rate_ctx`(3567)  ⚠ 위험목록 함수 → 전수 나열

### `_parse_combo_map(qd)`
- **위치**: `app.py:3883` (3883~3891)
- **설명**: 히스토리 스냅샷의 quinella dict('1+2':45.2) → {(1,2):45.2}. *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 1)**: `_time_based_drop_signals`(3900)

### `_after_close_load()`
- **위치**: `app.py:4258` (4258~4262)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 4)**: `after_close_cases`(2857) · `_record_after_close_case`(4270) · `_after_close_learn_result`(4298) · `_after_close_stats`(4331)  ⚠ 위험목록 함수 → 전수 나열

### `_near_miss_load()`
- **위치**: `app.py:4353` (4353~4357)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 3)**: `learning_near_miss`(2761) · `_record_near_miss`(4365) · `_near_miss_frequent`(4379)  ⚠ 위험목록 함수 → 전수 나열

### `_signal_type_stats_load()`
- **위치**: `app.py:4397` (4397~4401)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 3)**: `signal_types_stats`(2777) · `_learn_strong_signals`(4409) · `_signal_type_hitrates`(4503)  ⚠ 위험목록 함수 → 전수 나열

### `_compression_stats_load()`
- **위치**: `app.py:4530` (4530~4534)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 3)**: `compression_stats`(2787) · `_learn_compression`(4542) · `_compression_hitrate`(4568)  ⚠ 위험목록 함수 → 전수 나열

### `_high_odds_companion_stats_load()`
- **위치**: `app.py:4594` (4594~4598)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 2)**: `_learn_high_odds_companion`(4606) · `_high_odds_companion_hitrate`(4633)  ⚠ 위험목록 함수 → 전수 나열

### `_third_place_stats_load()`
- **위치**: `app.py:4649` (4649~4653)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 3)**: `third_place_stats`(2796) · `_learn_third_place`(4675) · `_third_place_hitrate`(4702)  ⚠ 위험목록 함수 → 전수 나열

### `_signal_lesson_load()`
- **위치**: `app.py:4729` (4729~4733)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 2)**: `learning_signal_lessons`(3235) · `_record_signal_lesson`(4741)  ⚠ 위험목록 함수 → 전수 나열

### `_ev_bands_load()`
- **위치**: `app.py:8510` (8510~8514)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 2)**: `_ev_band_p`(8527) · `_ev_bands_update`(8548)  ⚠ 위험목록 함수 → 전수 나열

### `_dark_cases_load()`
- **위치**: `app.py:8890` (8890~8894)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 2)**: `_dark_case_similar`(9368) · `_dark_case_record`(9384)  ⚠ 위험목록 함수 → 전수 나열

### `_dark_stats_load()`
- **위치**: `app.py:9258` (9258~9262)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 1)**: `dark_horse_stats`(19457)  ⚠ 위험목록 함수 → 전수 나열

### `_dansung_cases_load()`
- **위치**: `app.py:9432` (9432~9437)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 2)**: `dansung_cases`(2866) · `_dansung_case_record`(9448)  ⚠ 위험목록 함수 → 전수 나열

### `_hist_read_any(json_path)`
- **위치**: `app.py:13571` (13571~13584)
- **설명**: [영구보존·4번] odds_history 파일 읽기 — .json 우선, 없으면 7일+ 압축본(.json.gz) 투명 해제. *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 7)**: `_snapshot_signal_first`(2945) · `triple_analyze`(13461) · `_dr_record`(14368) · `odds_archive_list`(17390) · `odds_archive_get`(17429) · `_multi_collect_one`(29676) … [+1]

### `_threshold_config_load()`
- **위치**: `app.py:15389` (15389~15395)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 4)**: `thresholds_config`(2818) · `thresholds_apply`(2825) · `_threshold_config_save`(15398) · `_threshold_candidates`(15448)  ⚠ 위험목록 함수 → 전수 나열

### `_learning_load()`
- **위치**: `app.py:15960` (15960~15964)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 8)**: `_learned_integrated_weights`(6453) · `_triple_analyze`(9492) · `_dl_reliability`(15821) · `_apply_result_learning`(17723) · `_failure_stats`(19016) · `results_adjust`(21223) · `learning_stats`(21260) · `stats_today`(21365)  ⚠ 위험목록 함수 → 전수 나열

### `_upset_load()`
- **위치**: `app.py:15981` (15981~15988)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 5)**: `_learn_mass_drop`(16003) · `_learn_smart_money`(16048) · `_learn_upset`(16313) · `learning_upset`(21267) · `learning_reversal_stats`(21451)  ⚠ 위험목록 함수 → 전수 나열

### `_signal_stats_load()`
- **위치**: `app.py:16143` (16143~16156)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 4)**: `_learn_signal_stats`(16170) · `_signal_reliability`(16267) · `stats_today`(21365) · `learning_signal_stats`(21431)  ⚠ 위험목록 함수 → 전수 나열

### `_mid_high_stats_load()`
- **위치**: `app.py:16203` (16203~16215)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 2)**: `_learn_mid_high_odds`(16229) · `learning_mid_high_odds`(21419)  ⚠ 위험목록 함수 → 전수 나열

### `_discovered_load()`
- **위치**: `app.py:16530` (16530~16535)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 1)**: `patterns_discovered`(21464)  ⚠ 위험목록 함수 → 전수 나열

### `_parse(s)`
- **위치**: `app.py:17589` (17589~17592)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 1)**: `_rec_combos_from_analysis_log`(17556)

### `_failure_load()`
- **위치**: `app.py:18582` (18582~18592)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 3)**: `_failure_record`(18817) · `_failure_stats`(19016) · `failure_rules`(19449)  ⚠ 위험목록 함수 → 전수 나열

### `_high_odds_cases_load()`
- **위치**: `app.py:19864` (19864~19875)
- **설명**: 저장된 수동 케이스(manual_case=True) 목록(최신순). *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 1)**: `high_odds_review`(19879)

### `_parse_result_tsv(text)`
- **위치**: `app.py:20671` (20671~20744)
- **설명**: [일괄등록·탭구분 텍스트] 헤더+탭(\t) 구분 결과 붙여넣기 파싱(스프레드시트/엑셀 복사 대응). *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 1)**: `_parse_result_rows`(20747)

### `_parse_result_rows(html)`
- **위치**: `app.py:20747` (20747~20815)
- **설명**: 결과 페이지 HTML → [{area,round,no1,no2,no3,qOdds,tOdds}]. 헤더 라벨로 컬럼 판별. *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 1)**: `results_bulk`(21176)

### `_parse_reversal_signal(s)`
- **위치**: `app.py:21288` (21288~21304)
- **설명**: [4번] analysis_log signals_detected 의 역배열 신호 → (challenger, ratio) 추출(두 포맷 지원). *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 1)**: `_reversal_strength_stats`(21307)

### `_keirin_parse_card(html)`
- **위치**: `app.py:21755` (21755~21834)
- **설명**: oddspark 경륜 출마표 HTML → {venue,race_no,dist,post,tendency,riders,line,comment}. *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 2)**: `keirin_card`(22079) · `_keirin_autocollect_form`(22134)

### `_keirin_url(jo, ymd, race)`
- **위치**: `app.py:21878` (21878~21880)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 4)**: `keirin_card`(22079) · `_keirin_autocollect_form`(22134) · `_keirin_post_time`(29235) · `_keirin_venue_races`(29255)

### `_oddspark_parse_trio(html, max_no)`
- **위치**: `app.py:21883` (21883~21906)
- **설명**: [삼복승 서버 수집 (2026-07-19)] oddspark 3連複 배당 페이지 → [{combo:[a,b,c], odds}] (방어적 텍스트 파싱). *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 1)**: `_multi_collect_one`(29676)

### `_keirin_odds_url(jo, ymd, race, bet_type)`
- **위치**: `app.py:21909` (21909~21912)
- **설명**: oddspark 경륜 배당 페이지 URL. betType 5=복승(2車複)·6=쌍승(2車単). *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 3)**: `keirin_odds`(22252) · `keiba_odds`(22601) · `_multi_collect_one`(29676)

### `_keiba_parse_quinella_grid(html)`
- **위치**: `app.py:21967` (21967~21971)
- **설명**: [파서 재작성 v2] 복승(馬連=betType6) — colmap 그리드 파싱(9두+ 블록·꼬리 포함 전체 복원). *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 1)**: `_keiba_parse_quinella`(22474)

### `_keiba_parse_trio_grid(html, max_no)`
- **위치**: `app.py:21974` (21974~21979)
- **설명**: [파서 재작성 v2] 삼복승(3連複=betType9) — NAR 페이지는 'a-b-c 텍스트'가 아니라 짝헤더(1-2 등) *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 1)**: `_multi_collect_one`(29676)

### `_keirin_parse_quinella(html)`
- **위치**: `app.py:22015` (22015~22035)
- **설명**: 복승(betType=5) 매트릭스 파싱 → [{combo:[a,b], odds}]. 상삼각: 행 축차 k, 쌍 index j → 조합{j+1,k}. *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 3)**: `keirin_odds`(22252) · `keiba_odds`(22601) · `_multi_collect_one`(29676)

### `_keirin_parse_exacta(html)`
- **위치**: `app.py:22038` (22038~22056)
- **설명**: 쌍승(betType=6) 매트릭스 파싱 → [{combo:[1착,2착], odds}]. 방향성: 행 축차=1착, 쌍 index j → 2착=j+1. *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 3)**: `keirin_odds`(22252) · `keiba_odds`(22601) · `_multi_collect_one`(29676)

### `_keirin_fetch(url)`
- **위치**: `app.py:22072` (22072~22074)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 18)**: `_keirin_autocollect_form`(22134) · `keirin_odds`(22252) · `_keiba_schedule`(22545) · `keiba_odds`(22601) · `_keiba_current_raceno`(22719) · `_keiba_fetch_details`(23071) … [+12]

### `_keiba_odds_url(op_track, sponsor, ymd, race_nb, bet_type)`
- **위치**: `app.py:22386` (22386~22390)
- **설명**: oddspark 지방경마 배당 URL. betType 6=복승(馬連)·5=쌍승(馬単). *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 2)**: `keiba_odds`(22601) · `_multi_collect_one`(29676)

### `_keiba_parse_win(html)`
- **위치**: `app.py:22425` (22425~22461)
- **설명**: 단승(単勝=betType1) 표 → {마번(str): 단승배당(float)}. 실패·미발매 시 {}. *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 2)**: `keiba_odds`(22601) · `_multi_collect_one`(29676)

### `_keiba_parse_quinella(html)`
- **위치**: `app.py:22474` (22474~22515)
- **설명**: 복승(馬連=betType6) 매트릭스 → [{combo:[a,b], odds}]. 상대차 라벨을 따라가는 walking 파싱. *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 2)**: `keiba_odds`(22601) · `_multi_collect_one`(29676)

### `_keiba_parse_exacta(html)`
- **위치**: `app.py:22518` (22518~22542)
- **설명**: 쌍승(馬単=betType5) 그리드 → [{combo:[1着,2着], odds}]. 행=2着(axis)·열 위치=1着. *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 2)**: `keiba_odds`(22601) · `_multi_collect_one`(29676)

### `_keiba_parse_shutsuba(html)`
- **위치**: `app.py:22805` (22805~22866)
- **설명**: oddspark 지방경마 출주표(RaceList.do) → {venue,raceNo,distance,surface,trackCond,horses:[...]}. *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 2)**: `keiba_starters`(23095) · `_keiba_autocollect_form`(23159)

### `_keiba_parse_horse_past(html, max_n)`
- **위치**: `app.py:22869` (22869~22894)
- **설명**: HorseDetail.do → 전적 리스트(최근순 최대 max_n). 各: {date,venue,distance,surface,trackCond, *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 2)**: `_keiba_fetch_details`(23071) · `_one`(23077)

### `_keiba_fetch_details(shutsuba, api_key)`
- **위치**: `app.py:23071` (23071~23091)
- **설명**: 출전마별 HorseDetail 을 ThreadPool(동시 4)로 병렬 fetch·파싱 → {lineageNb: past[]}. *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 2)**: `keiba_starters`(23095) · `_keiba_autocollect_form`(23159)

### `_keiba_form_collect_by_rk(rk, ymd, force)`
- **위치**: `app.py:23206` (23206~23230)
- **설명**: [방법 A] raceKey → (경마장, 경주번호) → oddspark 트랙코드(opTrackCd/sponsorCd) 재해석 → 서버가 직접 전적 수집. *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 2)**: `keiba_form`(23234) · `_jp_form_backfill_once`(23246)

### `_weather_fetch_raw(venue)`
- **위치**: `app.py:23355` (23355~23377)
- **설명**: OpenWeatherMap current weather → 표준 dict. 키 없거나 실패 시 더미. (data, source). *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 1)**: `_weather_get`(23380)

### `_kra_fetch(meet, rc_date, rc_no, num_rows)`
- **위치**: `app.py:23502` (23502~23538)
- **설명**: API160_1/integratedInfo_1 호출 → (items[list], meta[dict], err[str|None]). read-only 조회. *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 3)**: `_kra_collect_one`(23617) · `kra_test`(25374) · `_kra_schedule`(29319)

### `_kra_parse_items(items)`
- **위치**: `app.py:23565` (23565~23614)
- **설명**: KRA API160 items → 파이프라인 포맷 (win[dict], q[list], x[list], tr[list]). *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 3)**: `_kra_collect_one`(23617) · `kra_test`(25374) · `_kra_schedule`(29319)

### `_kra_collect_one(rk, meet, rc_date, rc_no)`
- **위치**: `app.py:23617` (23617~23633)
- **설명**: KRA 1경주 배당 수집 → 파이프라인 주입. (ok, info) 반환. 실패해도 확장 폴백에 무영향. *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 2)**: `_kra_auto_loop`(23724) · `kra_collect`(25460)

### `_kra_collect_result_one(rk, meet, rc_date, rc_no)`
- **위치**: `app.py:23676` (23676~23694)
- **설명**: [KRA 결과 자동수집·저장] rcTime 착순 추출 → 기존 결과 학습 경로(_apply_result_learning) 재사용. *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 3)**: `kra_result`(23698) · `_kra_auto_loop`(23724) · `_kra_result_backfill_once`(23820)

### `_keiba_parse_result(html)`
- **위치**: `app.py:23958` (23958~23986)
- **설명**: RaceMarkTable HTML → 착순 상위3 마번[list] 또는 None. 着順/馬番 컬럼 라벨 기반(방어). *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 1)**: `_keiba_result_top3`(24018)

### `_keirin_result_parse(html)`
- **위치**: `app.py:24273` (24273~24349)
- **설명**: RaceKekka.do HTML → {venue, top3:[차번...], quinellaOdds}. 방어적 파싱(구조 변형 대비). *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 1)**: `_keirin_result_top3`(24352)

### `_fn_parse(fn)`
- **위치**: `app.py:24610` (24610~24618)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 1)**: `daily_cards_page`(24598)

### `_kra_track_op_load()`
- **위치**: `app.py:25592` (25592~25600)
- **설명**: 캐시 파일에서 이미 확정된 op 경로 로드(있으면 탐색 생략). *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 1)**: `_kra_track_op_resolve`(25628)

### `_jra_parse_shutuba_past(html)`
- **위치**: `app.py:26726` (26726~26771)
- **설명**: netkeiba 馬柱(shutuba_past) → {venue,raceNo,distance,surface,trackCond,horses:[...]}. *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 1)**: `jra_starters`(26842)

### `_parse_body_weight(bw)`
- **위치**: `app.py:27564` (27564~27569)
- **설명**: 마체중 표기('486(-4)'·'486kg'·486) → 정수 kg. 괄호 안 증감은 무시(절대값만). 실패 None. *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 1)**: `body_weight_change_bonus`(27572)

### `_pdf_cache_load(md5)`
- **위치**: `app.py:28301` (28301~28312)
- **설명**: 같은 해시의 '완료된' 세션 캐시를 반환(없으면 None). *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 1)**: `korea_start`(28781)

### `_korea_load()`
- **위치**: `app.py:28341` (28341~28346)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 5)**: `_korea_run_job`(28595) · `korea_status`(28817) · `korea_session`(28825) · `korea_reextract`(28845) · `_korea_maybe_resume`(29016)

### `_prerace_index_load()`
- **위치**: `app.py:28545` (28545~28569)
- **설명**: index.json 로드. 없으면 디렉터리 스캔으로 복구(디스크가 진실 소스). *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 2)**: `_prerace_index_update`(28533) · `korea_prerace_list`(28881)  ⚠ 위험목록 함수 → 전수 나열

### `_prerace_load(key)`
- **위치**: `app.py:28572` (28572~28580)
- **설명**: key 로 경주 1건 전체(리포트 포함) 로드. 없으면 None. *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 1)**: `korea_prerace_get`(28887)

### `_schedule_load()`
- **위치**: `app.py:29122` (29122~29127)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 11)**: `_sched_post_epoch`(22228) · `_keirin_jo_today`(24260) · `keirin_today_stats`(24981) · `_race_post_epoch`(26493) · `_korea_schedule_post_epoch`(29141) · `multi_keirin_schedule`(30399) · `multi_schedule`(30433) · `multi_collect_now`(30444) · `multi_dashboard`(30463) · `_multi_bg_loop`(31554) · `_startup_date_reset`(32237)  ⚠ 위험목록 함수 → 전수 나열

### `_nar_fetch(url)`
- **위치**: `app.py:29378` (29378~29390)
- **설명**: keiba.go.jp 페이지 fetch + 디코드. ⚠ 이 사이트는 UTF-8 이다 — *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 3)**: `_nar_autocollect_form`(29553) · `_nar_race_list`(29588) · `_multi_collect_one`(29676)

### `_nar_odds_url(baba, ymd, rno, kind)`
- **위치**: `app.py:29397` (29397~29400)
- **설명**: odds_flg=4 = '馬番順' — 축마별 단순 표(파싱 최단·열 추적 불필요). *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 1)**: `_multi_collect_one`(29676)

### `_nar_parse_pair_odds(html, keep_direction, max_no)`
- **위치**: `app.py:29403` (29403~29428)
- **설명**: 馬連複/馬連単(馬番順) → [{'combo':[a,b],'odds':x}]. *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 1)**: `_multi_collect_one`(29676)

### `_nar_parse_win(html, max_no)`
- **위치**: `app.py:29431` (29431~29446)
- **설명**: 単勝・複勝 표 → {마번: 단승배당}. 행 = [枠, 馬番, 馬名, 単勝, 複勝하한 - 상한]. *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 1)**: `_multi_collect_one`(29676)

### `_nar_parse_trio(html, max_no)`
- **위치**: `app.py:29452` (29452~29487)
- **설명**: 三連複(馬番順) → [{'combo':[a,b,c],'odds':x}]. *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 1)**: `_multi_collect_one`(29676)

### `_nar_parse_deba(html, max_no)`
- **위치**: `app.py:29509` (29509~29547)
- **설명**: DebaTable → (shutsuba, details) — `_keiba_build_form` 입력 형태로 정규화. *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 1)**: `_nar_autocollect_form`(29553)

### `_multi_schedule_fetch()`
- **위치**: `app.py:29620` (29620~29664)
- **설명**: [1번] 오늘 개최 경주 스케줄(경마장+경주번호+발주시각) → today_schedule.json. 실패해도 예외 전파 안 함. *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 2)**: `multi_schedule`(30433) · `multi_collect_now`(30444)

### `_multi_collect_one(track, race, ymd)`
- **위치**: `app.py:29676` (29676~29870)
- **설명**: [2번] 한 경주 배당 수집 → multi_race_store 저장(실패 격리·triple_store 미접근). 반환 raceKey 또는 None. *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 2)**: `multi_collect_now`(30444) · `_multi_bg_loop`(31554)

### `_collect_cycle_begin(sched, targets, now, cycle_no)`
- **위치**: `app.py:31654` (31654~31684)
- **설명**: [수집 사이클 관측 (2026-07-30)] 이 사이클의 대상 선정 결과를 기록용 dict 로 만든다. *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 1)**: `_multi_bg_loop`(31554)

### `_collect_cycle_end(obs, elapsed, sched, now)`
- **위치**: `app.py:31687` (31687~31732)
- **설명**: [수집 사이클 관측] 소요시간 + '창 밖인데 방금 갱신된 경주' 를 붙여 확정 기록. *(docstring)*
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 1)**: `_multi_bg_loop`(31554)

### `_load(path)`
- **위치**: `review_engine.py:34` (34~38)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_fetch|_parse|_collect|_ingest|_url$|_urls$|_scrape|_load$|_read)
- **호출됨(Caller · 4)**: `sweep`(187) · `signal_place_stats`(248) · `drop_stats`(331) · `replay_day`(478)

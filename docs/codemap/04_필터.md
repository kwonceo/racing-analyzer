# 필터 — 게이트·면제·상한

> 🤖 **자동 생성** — `python tools/build_codemap.py` · 2026-08-12 08:36:46
> ⚠ 손으로 고치지 마세요. 다음 생성 때 덮어써집니다.
> ⚠ **AST 한계**: `getattr()` 동적 호출 · `importlib` 경유 · 문자열 디스패치는 잡지 못합니다
>   (`gemini_reviewer`·`health_check` 가 실제로 `importlib` 경유입니다).

총 **27개** 함수

### `_premium_gate()`
- **위치**: `app.py:183` (183~190)
- **설명**: 현재: 전부 오픈(PREMIUM_ENFORCED=False → 즉시 통과). 향후 True 로 승격 시에만 차단. *(주석(본문))*
- **분류 근거**: 이름 규칙 (_filter|_exempt|_gate|_cut$|_suspect|_norm$|_dedupe|_prune|_limit|_cap$)
- **호출됨(Caller · 0)**: —

### `image_block(img)`
- **위치**: `app.py:657` (657~665)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 도메인 2차 (filter|exempt|gate|_cut|sus
- **호출됨(Caller · 9)**: `_do_extract_jockey`(713) · `_do_extract_race`(731) · `_do_extract_training`(767) · `extract_results`(788) · `result_ocr`(838) · `_do_detect`(879) … [+3]

### `_valid_series(arr)`
- **위치**: `app.py:1244` (1244~1246)
- **설명**: null 제거한 유효 배당만 [(index, odds), ...] *(docstring)*
- **분류 근거**: 도메인 2차 (filter|exempt|gate|_cut|sus
- **호출됨(Caller · 1)**: `odds_compute`(1301)

### `_board_flip_suspect(prev_q, cur_q, thresh, min_ratio, min_combos)`
- **위치**: `app.py:1808` (1808~1831)
- **설명**: [혼입 가드 v2 (2026-07-20)] 직전 스냅샷 대비 판 전체 뒤집힘(같은 raceKey 에 다른 경주 배당판 *(docstring)*
- **분류 근거**: 이름 규칙 (_filter|_exempt|_gate|_cut$|_suspect|_norm$|_dedupe|_prune|_limit|_cap$)
- **호출됨(Caller · 1)**: `_do_triple_ingest`(2768)

### `_column_shift_suspect(prev_q, cur_q, min_adj, min_common, tol)`
- **위치**: `app.py:1867` (1867~1906)
- **설명**: [3-B번 열 밀림] 현재 값이 '직전의 인접 조합 값'과 더 잘 맞으면 열 인덱스 어긋남으로 판정. *(docstring)*
- **분류 근거**: 이름 규칙 (_filter|_exempt|_gate|_cut$|_suspect|_norm$|_dedupe|_prune|_limit|_cap$)
- **호출됨(Caller · 1)**: `_history_append`(15465)

### `_triple_prune_stale(db, keep_rk, max_age)`
- **위치**: `app.py:1909` (1909~1921)
- **설명**: [경주전환 잔존 방어] 활성 3종 배당(triple_store)에서 max_age(기본 30분) 넘게 *(docstring)*
- **분류 근거**: 이름 규칙 (_filter|_exempt|_gate|_cut$|_suspect|_norm$|_dedupe|_prune|_limit|_cap$)
- **호출됨(Caller · 1)**: `_do_triple_ingest`(2768)

### `_norm(qd)`
- **위치**: `app.py:2036` (2036~2054)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_filter|_exempt|_gate|_cut$|_suspect|_norm$|_dedupe|_prune|_limit|_cap$)
- **호출됨(Caller · 3)**: `_src_divergence`(2030) · `recommend_manual`(20668) · `_extract_korea_post_time`(32600)

### `_gate_hit(name, rk, reason, reach_only, once_key)`
- **위치**: `app.py:2082` (2082~2193)
- **설명**: [도달·발동 계수기] 🔴 **도달과 발동을 따로 센다.** *(docstring)*
- **분류 근거**: 이름 규칙 (_filter|_exempt|_gate|_cut$|_suspect|_norm$|_dedupe|_prune|_limit|_cap$)
- **호출됨(Caller · 26)**: `_combo_count_dip`(1842) · `_column_shift_suspect`(1867) · `_ingest_ghost_verdict`(2238) · `_odds_suspect_verdict`(2313) · `_curq_shrink_guard`(2363) · `_oddspark_mapping_suspect`(2450) · `_arity_guard`(2604) · `_do_triple_ingest`(2768) · `_final_picks`(8348) · `_triple_analyze`(10724) · `_apply_t5_freeze`(13661) · `triple_analyze`(15132) · `_judge_extra_quinellas`(16305) · `_build_analysis_log`(16347) · `_stake_plan`(19585) · `next_api`(20003) · `_keirin_autocollect_form`(25536) · `_keiba_autocollect_form`(26684) · `_korea_renumber_by_post`(32780) · `_korea_roster_sanity`(32829) · `_korea_verify_fitz`(32876) · `_kakao_rich_message`(36119) · `_timeline_snap_tick`(36575) · `_auto_pred_tick`(36865) · `_start_review_scheduler`(37741) · `_loop`(37756)  ⚠ 위험목록 함수 → 전수 나열

### `_odds_suspect_verdict(rk, an)`
- **위치**: `app.py:2313` (2313~2357)
- **설명**: [3층 판정] 오염이면 사유 문자열, 아니면 None. 🔴 **판정 불가도 통과시키지 않는다.** *(docstring)*
- **분류 근거**: 이름 규칙 (_filter|_exempt|_gate|_cut$|_suspect|_norm$|_dedupe|_prune|_limit|_cap$)
- **호출됨(Caller · 1)**: `triple_analyze`(15132)

### `_oddspark_mapping_suspect(prev_q, new_q, min_pairs)`
- **위치**: `app.py:2450` (2450~2469)
- **설명**: [③ 말번호 밀림 감지] 직전(배당판) 복승 조합 vs 새(oddspark) 복승 조합 비교. *(docstring)*
- **분류 근거**: 이름 규칙 (_filter|_exempt|_gate|_cut$|_suspect|_norm$|_dedupe|_prune|_limit|_cap$)
- **호출됨(Caller · 1)**: `_do_triple_ingest`(2768)

### `_starters_prune(keep_rk)`
- **위치**: `app.py:3239` (3239~3264)
- **설명**: [잔존마 방어·3번] starters_store(전적)에서 이전 경주 데이터 제거. *(docstring)*
- **분류 근거**: 이름 규칙 (_filter|_exempt|_gate|_cut$|_suspect|_norm$|_dedupe|_prune|_limit|_cap$)
- **호출됨(Caller · 3)**: `_do_triple_ingest`(2768) · `triple_reset`(3268) · `starters_reset`(3292)

### `_capital_preservation(combos)`
- **위치**: `app.py:7120` (7120~7148)
- **설명**: [2번] 원금 보전 자동 계산(예산 무관 '비율' 산출 → 프론트가 예산 곱함). *(docstring)*
- **분류 근거**: 도메인 2차 (filter|exempt|gate|_cut|sus
- **호출됨(Caller · 1)**: `_bmed_strategy`(7254)

### `_lowodds_exempt(_qq)`
- **위치**: `app.py:9934` (9934~9942)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_filter|_exempt|_gate|_cut$|_suspect|_norm$|_dedupe|_prune|_limit|_cap$)
- **호출됨(Caller · 1)**: `_apply_profit_strategy`(9809)

### `_corrupt_prune(dirpath, keep)`
- **위치**: `app.py:15848` (15848~15862)
- **설명**: 격리본 무한 증가 방지 — 디렉터리별 최신 keep 개만 남긴다(오래된 것부터 제거). *(docstring)*
- **분류 근거**: 이름 규칙 (_filter|_exempt|_gate|_cut$|_suspect|_norm$|_dedupe|_prune|_limit|_cap$)
- **호출됨(Caller · 1)**: `_corrupt_quarantine`(15865)

### `_validate_race_result(data)`
- **위치**: `app.py:16903` (16903~16927)
- **설명**: [4번] 데이터 품질 검증 — 마번 1~16, 배당 1.0~9999, 날짜 형식, 필수값. 오류 리스트 반환(빈=정상). *(docstring)*
- **분류 근거**: 도메인 2차 (filter|exempt|gate|_cut|sus
- **호출됨(Caller · 1)**: `_save_race_result`(17095)

### `_norm(cc, need)`
- **위치**: `app.py:20678` (20678~20684)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_filter|_exempt|_gate|_cut$|_suspect|_norm$|_dedupe|_prune|_limit|_cap$)
- **호출됨(Caller · 3)**: `_src_divergence`(2030) · `recommend_manual`(20668) · `_extract_korea_post_time`(32600)

### `_combo_norm(picks)`
- **위치**: `app.py:23168` (23168~23179)
- **설명**: [[4,10],[7,10],...] → 정렬 2두+ 조합 리스트(정수·중복제거). *(docstring)*
- **분류 근거**: 이름 규칙 (_filter|_exempt|_gate|_cut$|_suspect|_norm$|_dedupe|_prune|_limit|_cap$)
- **호출됨(Caller · 1)**: `_analyze_competitor_pattern`(23182)

### `_track_norm(a)`
- **위치**: `app.py:24171` (24171~24187)
- **설명**: 경마장명 정규화(부경=부산 · 帯広/obihiro/OBI=오비히로 등 한/일/영 별칭 통일). *(docstring)*
- **분류 근거**: 이름 규칙 (_filter|_exempt|_gate|_cut$|_suspect|_norm$|_dedupe|_prune|_limit|_cap$)
- **호출됨(Caller · 29)**: `_canon_rk`(1962) · `_ingest_normalize_rk`(2524) · `_canonical_log_key`(15941) · `_race_result_id`(16860) · `_rf_resolve_rk`(19496) · `_area_num`(24190) … [+23]

### `_weather_venue_norm(venue)`
- **위치**: `app.py:26868` (26868~26871)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_filter|_exempt|_gate|_cut$|_suspect|_norm$|_dedupe|_prune|_limit|_cap$)
- **호출됨(Caller · 1)**: `_weather_get`(26928)

### `_norm(t)`
- **위치**: `app.py:32607` (32607~32609)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_filter|_exempt|_gate|_cut$|_suspect|_norm$|_dedupe|_prune|_limit|_cap$)
- **호출됨(Caller · 3)**: `_src_divergence`(2030) · `recommend_manual`(20668) · `_extract_korea_post_time`(32600)

### `_frozen_capture(rk, doc, rec_rows)`
- **위치**: `app.py:34293` (34293~34348)
- **설명**: 마감 시점 확정값을 3단 폴백으로 복원. 반환 (frozen dict, src) — 실패 시 (None, 사유). *(docstring)*
- **분류 근거**: 도메인 2차 (filter|exempt|gate|_cut|sus
- **호출됨(Caller · 1)**: `_build_analysis_log`(16347)

### `_valid(c)`
- **위치**: `app.py:35050` (35050~35055)
- **설명**: [로그 오염 방어] 출주 두수 초과 마번 조합(타 경주 혼입 — 경륜 7명에 1+10 등)은 판정 제외 *(주석(본문))*
- **분류 근거**: 도메인 2차 (filter|exempt|gate|_cut|sus
- **호출됨(Caller · 1)**: `_live_exact_hit`(34989)

### `_cut_tier_candidates(key, doc, cp)`
- **위치**: `app.py:35635` (35635~35759)
- **설명**: 컷에 걸려 화면에서 사라진 조합에 등급을 붙여 돌려준다(표시 전용·읽기 전용). *(docstring)*
- **분류 근거**: 도메인 2차 (filter|exempt|gate|_cut|sus
- **호출됨(Caller · 3)**: `triple_analyze`(15132) · `_race_card_timeline`(19280) · `_rec_trail_for_detail`(35762)  ⚠ 위험목록 함수 → 전수 나열

### `_picks_norm(lst)`
- **위치**: `app.py:37433` (37433~37450)
- **설명**: [{combo:[..]}] 또는 ["1+2"] 또는 [[1,2]] → 정렬된 "1+2" 문자열 리스트. *(docstring)*
- **분류 근거**: 이름 규칙 (_filter|_exempt|_gate|_cut$|_suspect|_norm$|_dedupe|_prune|_limit|_cap$)
- **호출됨(Caller · 1)**: `_picks_diff_one`(37453)

### `_midcheck_gate()`
- **위치**: `app.py:37990` (37990~38027)
- **설명**: 게이트 도달·발동. 카운터(참고) + `logs/gate_fire/` append 로그(진실)를 함께 낸다. *(docstring)*
- **분류 근거**: 이름 규칙 (_filter|_exempt|_gate|_cut$|_suspect|_norm$|_dedupe|_prune|_limit|_cap$)
- **호출됨(Caller · 1)**: `_midcheck_collect`(38172)  ⚠ 위험목록 함수 → 전수 나열

### `_new_odds_cap(nh)`
- **위치**: `review_engine.py:460` (460~475)
- **설명**: [fix_odds_cap_new] 두수별 메인 상한 상향안 — 현재 10/15/20/25/30 → 30/40/50/60/70(증분 +10 일정). *(docstring)*
- **분류 근거**: 이름 규칙 (_filter|_exempt|_gate|_cut$|_suspect|_norm$|_dedupe|_prune|_limit|_cap$)
- **호출됨(Caller · 1)**: `replay_day`(478)

### `dedupe_snapshots(snaps)`
- **위치**: `tools/dedupe_odds_snapshots.py:75` (75~89)
- **설명**: 반환 (남길 리스트, 제거 수). 원본 순서를 보존한다. *(docstring)*
- **분류 근거**: 도메인 2차 (filter|exempt|gate|_cut|sus
- **호출됨(Caller · 1)**: `main`(99)

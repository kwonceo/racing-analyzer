# 검증 — 테스트·가드·체크리스트

> 🤖 **자동 생성** — `python tools/build_codemap.py` · 2026-08-12 07:29:59
> ⚠ 손으로 고치지 마세요. 다음 생성 때 덮어써집니다.
> ⚠ **AST 한계**: `getattr()` 동적 호출 · `importlib` 경유 · 문자열 디스패치는 잡지 못합니다
>   (`gemini_reviewer`·`health_check` 가 실제로 `importlib` 경유입니다).

총 **38개** 함수

### `_curq_shrink_guard(rk, cur_map, hist)`
- **위치**: `app.py:2363` (2363~2447)
- **설명**: [축소 오염 교정 · 2026-08-05 승인 C안] 마지막 틱이 **직전보다 좁으면** 넓은 틱을 쓴다. *(docstring)*
- **분류 근거**: 이름 규칙 (^check|^test|_test$|guard|_review|audit|health|verify|sanity)
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_arity_guard(kind, combos, label, source)`
- **위치**: `app.py:2604` (2604~2634)
- **설명**: [오염방지 3·서버측 검증] 조합 수 정합성 검증 → 통과=원본 / 실패=[](그 종류만 저장 거부). *(docstring)*
- **분류 근거**: 이름 규칙 (^check|^test|_test$|guard|_review|audit|health|verify|sanity)
- **호출됨(Caller · 1)**: `_do_triple_ingest`(2768)

### `_bettype_guard(kind, combos, label)`
- **위치**: `app.py:2685` (2685~2703)
- **설명**: [보완2·서버측 대조] 조합이 스스로 밝힌 betType 과 담긴 키가 일치하는지 직접 대조. *(docstring)*
- **분류 근거**: 이름 규칙 (^check|^test|_test$|guard|_review|audit|health|verify|sanity)
- **호출됨(Caller · 1)**: `_do_triple_ingest`(2768)

### `_json_load_guard(path, default, tag)`
- **위치**: `app.py:15887` (15887~15925)
- **설명**: [손상 파일 격리] `json.load` 의 안전판. 반환 `(doc, corrupt)`. *(docstring)*
- **분류 근거**: 이름 규칙 (^check|^test|_test$|guard|_review|audit|health|verify|sanity)
- **호출됨(Caller · 12)**: `_ingest_reject_log`(2734) · `_triple_analyze`(10724) · `_record_alert`(14545) · `_history_append`(15465) · `_history_save_analysis`(15703) · `_apply_result_learning`(20801) … [+6]

### `_deterministic_review(pend)`
- **위치**: `app.py:16053` (16053~16151)
- **설명**: [②B라인 누락 · ④급락 미반영] 결정론 판정. 반환 {issues:[...], checked:[...], skipped:[...]}. *(docstring)*
- **분류 근거**: 이름 규칙 (^check|^test|_test$|guard|_review|audit|health|verify|sanity)
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_pairing_miss_review(an, top3)`
- **위치**: `app.py:21764` (21764~21791)
- **설명**: [히로시마 2R 학습·복기] '유력마는 맞았으나 복승 상대 조합이 엇갈려' 복승을 놓친 아쉬운 near-miss. *(docstring)*
- **분류 근거**: 이름 규칙 (^check|^test|_test$|guard|_review|audit|health|verify|sanity)
- **호출됨(Caller · 1)**: `_apply_result_learning`(20801)

### `_high_odds_review_one(rk, save, rr)`
- **위치**: `app.py:22372` (22372~22461)
- **설명**: [1~6번] 고배당 경주 1건 심층 분석 — race_results 기반(정답말 역추적·5유형·ABC·시뮬). *(docstring)*
- **분류 근거**: 이름 규칙 (^check|^test|_test$|guard|_review|audit|health|verify|sanity)
- **호출됨(Caller · 2)**: `_high_odds_scan`(22464) · `high_odds_review`(23069)

### `_review_extract_signals(sig)`
- **위치**: `app.py:23487` (23487~23522)
- **설명**: 확장 analyzeStatus.data 에서 중요 신호만 압축 요약(급락·역배열·스마트머니·경고·확신도). *(docstring)*
- **분류 근거**: 이름 규칙 (^check|^test|_test$|guard|_review|audit|health|verify|sanity)
- **호출됨(Caller · 1)**: `review_save`(23526)

### `_review_flag_top_importance(rec)`
- **위치**: `app.py:23584` (23584~23601)
- **설명**: ⭐⭐⭐ 최고 중요 경주 → 즉시 규칙 검토 큐(참고용)에 누적(최근 200건). *(docstring)*
- **분류 근거**: 이름 규칙 (^check|^test|_test$|guard|_review|audit|health|verify|sanity)
- **호출됨(Caller · 1)**: `review_save`(23526)  ⚠ 위험목록 함수 → 전수 나열

### `_review_notes_load()`
- **위치**: `app.py:24840` (24840~24845)
- **설명**: 복기 메모 학습 코퍼스(리스트) 로드. *(docstring)*
- **분류 근거**: 이름 규칙 (^check|^test|_test$|guard|_review|audit|health|verify|sanity)
- **호출됨(Caller · 2)**: `_review_note_append`(24848) · `review_notes_list`(24900)  ⚠ 위험목록 함수 → 전수 나열

### `_review_note_append(doc, note, file)`
- **위치**: `app.py:24848` (24848~24870)
- **설명**: 복기 메모를 학습 코퍼스에 축적(경주/종목/적중 태그 포함) → 나중에 패턴 마이닝·검색. *(docstring)*
- **분류 근거**: 이름 규칙 (^check|^test|_test$|guard|_review|audit|health|verify|sanity)
- **호출됨(Caller · 1)**: `analysis_log_memo`(24874)

### `_official_result_audit(date, limit, verbose)`
- **위치**: `app.py:27672` (27672~27763)
- **설명**: [결과·환급 정합 스윕 (2026-07-22 권대표 지시)] 판정된 경주의 착순·확정환급을 공식 소스와 대조 → *(docstring)*
- **분류 근거**: 이름 규칙 (^check|^test|_test$|guard|_review|audit|health|verify|sanity)
- **호출됨(Caller · 1)**: `audit_results_ep`(27820)

### `_korea_roster_sanity(races)`
- **위치**: `app.py:32829` (32829~32873)
- **설명**: 🔴 [2026-08-07] 판독 두수가 fitz 두수보다 **크게 적으면 그 경주 명단을 비운다.** *(docstring)*
- **분류 근거**: 이름 규칙 (^check|^test|_test$|guard|_review|audit|health|verify|sanity)
- **호출됨(Caller · 1)**: `_korea_run_job`(32957)

### `_korea_verify_fitz(races)`
- **위치**: `app.py:32876` (32876~32954)
- **설명**: 🔴 [2026-08-06 승인] fitz 경주목록을 진실로 삼아 Vision(detected) races 를 검증한다. *(docstring)*
- **분류 근거**: 이름 규칙 (^check|^test|_test$|guard|_review|audit|health|verify|sanity)
- **호출됨(Caller · 2)**: `_korea_run_job`(32957) · `korea_start`(33217)

### `_health_checklist_build()`
- **위치**: `app.py:37256` (37256~37263)
- **설명**: 체크리스트 생성(엔드포인트와 동일 경로 재사용). 실패 시 예외를 올린다. *(docstring)*
- **분류 근거**: 이름 규칙 (^check|^test|_test$|guard|_review|audit|health|verify|sanity)
- **호출됨(Caller · 3)**: `_health_kakao_send`(37403) · `health_send_kakao_api`(37623) · `_midcheck_collect`(38172)

### `_health_pending_results()`
- **위치**: `app.py:37266` (37266~37279)
- **설명**: 21:00 시점 '결과 대기' 경주 수. 실패해도 None 을 돌려 메시지 조립을 막지 않는다. *(docstring)*
- **분류 근거**: 이름 규칙 (^check|^test|_test$|guard|_review|audit|health|verify|sanity)
- **호출됨(Caller · 1)**: `_health_kakao_text`(37313)

### `_health_prev_day_measurable()`
- **위치**: `app.py:37282` (37282~37310)
- **설명**: [③안 보조 (2026-08-01)] **전날** 측정 가능 비율(확정치). 없으면 None. *(docstring)*
- **분류 근거**: 이름 규칙 (^check|^test|_test$|guard|_review|audit|health|verify|sanity)
- **호출됨(Caller · 1)**: `_health_kakao_text`(37313)

### `_review_pattern_alert(p2_cum)`
- **위치**: `app.py:37717` (37717~37738)
- **설명**: 🔴 [작업2] 패턴 누적이 30(관찰)·200(채택)에 처음 닿는 순간 1회 알림. 스탬프로 중복 방지. *(docstring)*
- **분류 근거**: 이름 규칙 (^check|^test|_test$|guard|_review|audit|health|verify|sanity)
- **호출됨(Caller · 2)**: `_start_review_scheduler`(37741) · `_loop`(37756)  ⚠ 위험목록 함수 → 전수 나열

### `_start_review_scheduler()`
- **위치**: `app.py:37741` (37741~37773)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (^check|^test|_test$|guard|_review|audit|health|verify|sanity)
- **호출됨(Caller · 1)**: `_boot_background`(38998)  ⚠ 위험목록 함수 → 전수 나열

### `gemini_review_enabled()`
- **위치**: `gemini_reviewer.py:34` (34~36)
- **설명**: Gemini API 호출 허용 여부(기본 꺼짐). *(docstring)*
- **분류 근거**: 이름 규칙 (^check|^test|_test$|guard|_review|audit|health|verify|sanity)
- **호출됨(Caller · 1)**: `review_async`(440)

### `check_snapshot_coverage(today)`
- **위치**: `tools/health_check.py:114` (114~154)
- **설명**: ④-1 스냅샷 3틱+ 경주 비율. *(docstring)*
- **분류 근거**: 이름 규칙 (^check|^test|_test$|guard|_review|audit|health|verify|sanity)
- **호출됨(Caller · 1)**: `build_checklist`(1006)

### `check_save_failures_fixed()`
- **위치**: `tools/health_check.py:198` (198~210)
- **설명**: ④-2a 저장 실패 — **수정 경로**(`_json_atomic` 계열). *(docstring)*
- **분류 근거**: 이름 규칙 (^check|^test|_test$|guard|_review|audit|health|verify|sanity)
- **호출됨(Caller · 1)**: `build_checklist`(1006)

### `check_save_failures_unfixed()`
- **위치**: `tools/health_check.py:213` (213~223)
- **설명**: ④-2b 저장 실패 — **미수정 17곳**(`path + ".tmp"` · PID 조차 없음). ⓐ 작업 대상. *(docstring)*
- **분류 근거**: 이름 규칙 (^check|^test|_test$|guard|_review|audit|health|verify|sanity)
- **호출됨(Caller · 1)**: `build_checklist`(1006)

### `check_schema_contract()`
- **위치**: `tools/health_check.py:226` (226~238)
- **설명**: ④-3 schema contract test 통과. *(docstring)*
- **분류 근거**: 이름 규칙 (^check|^test|_test$|guard|_review|audit|health|verify|sanity)
- **호출됨(Caller · 1)**: `build_checklist`(1006)

### `check_schema_drift()`
- **위치**: `tools/health_check.py:282` (282~312)
- **설명**: ④-4 스키마 드리프트 🔴 필드 보유율 ≥90%. *(docstring)*
- **분류 근거**: 이름 규칙 (^check|^test|_test$|guard|_review|audit|health|verify|sanity)
- **호출됨(Caller · 1)**: `build_checklist`(1006)

### `check_score_decomposition()`
- **위치**: `tools/health_check.py:315` (315~351)
- **설명**: ④-5 점수 분해 `rank`·`baseScore` 배선 — **신설**(권대표 지시 2026-07-30). *(docstring)*
- **분류 근거**: 이름 규칙 (^check|^test|_test$|guard|_review|audit|health|verify|sanity)
- **호출됨(Caller · 1)**: `build_checklist`(1006)

### `check_forecast_discard()`
- **위치**: `tools/health_check.py:411` (411~446)
- **설명**: ⑤-1 Gemini 예측 **폐기율** ≤20% (판정선: 30경주 도달 시 형식 점검). *(docstring)*
- **분류 근거**: 이름 규칙 (^check|^test|_test$|guard|_review|audit|health|verify|sanity)
- **호출됨(Caller · 1)**: `build_checklist`(1006)

### `check_forecast_vs_market()`
- **위치**: `tools/health_check.py:449` (449~498)
- **설명**: ⑤-2 Gemini 평균 `hit_count` ≥ 시장 평균 `market_hit_count` (판정선: **100경주**). *(docstring)*
- **분류 근거**: 이름 규칙 (^check|^test|_test$|guard|_review|audit|health|verify|sanity)
- **호출됨(Caller · 1)**: `build_checklist`(1006)

### `check_forecast_highodds()`
- **위치**: `tools/health_check.py:501` (501~559)
- **설명**: [F3 · 2026-08-01 신설] Gemini 의 **고배당 능력** — 시장과 다른 답을 내고 맞혔는가. *(docstring)*
- **분류 근거**: 이름 규칙 (^check|^test|_test$|guard|_review|audit|health|verify|sanity)
- **호출됨(Caller · 1)**: `build_checklist`(1006)

### `check_freeze_success()`
- **위치**: `tools/health_check.py:562` (562~590)
- **설명**: [D6 · 2026-07-31 신설] 마감 시점 동결이 **신규 경주에서 실제로 작동하는가.** *(docstring)*
- **분류 근거**: 이름 규칙 (^check|^test|_test$|guard|_review|audit|health|verify|sanity)
- **호출됨(Caller · 1)**: `build_checklist`(1006)  ⚠ 위험목록 함수 → 전수 나열

### `check_module_load()`
- **위치**: `tools/health_check.py:593` (593~625)
- **설명**: [D7 · 2026-07-31 신설] **선택 모듈이 조용히 죽어 있지 않은가.** *(docstring)*
- **분류 근거**: 이름 규칙 (^check|^test|_test$|guard|_review|audit|health|verify|sanity)
- **호출됨(Caller · 1)**: `build_checklist`(1006)

### `check_payout_coverage()`
- **위치**: `tools/health_check.py:666` (666~699)
- **설명**: [I1] 확정배당 보유율 — **종목별**. 🔴 경마 0.9% 가 이 항목이 있었으면 첫날 잡혔다. *(docstring)*
- **분류 근거**: 이름 규칙 (^check|^test|_test$|guard|_review|audit|health|verify|sanity)
- **호출됨(Caller · 1)**: `build_checklist`(1006)

### `check_backup_alive()`
- **위치**: `tools/health_check.py:702` (702~715)
- **설명**: [I2] 백업 실제 실행 — 마지막 실행 시각과 경과. 🔴 6시간 주기가 0회인 것을 몇 주 몰랐다. *(docstring)*
- **분류 근거**: 이름 규칙 (^check|^test|_test$|guard|_review|audit|health|verify|sanity)
- **호출됨(Caller · 1)**: `build_checklist`(1006)

### `check_server_log_alive()`
- **위치**: `tools/health_check.py:721` (721~752)
- **설명**: [I7] 서버 콘솔 로그가 **지금도 쌓이고 있나** — 최신 `logs/server*.log` 의 갱신 경과(분). *(docstring)*
- **분류 근거**: 이름 규칙 (^check|^test|_test$|guard|_review|audit|health|verify|sanity)
- **호출됨(Caller · 1)**: `build_checklist`(1006)

### `check_daemon_alive()`
- **위치**: `tools/health_check.py:807` (807~840)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (^check|^test|_test$|guard|_review|audit|health|verify|sanity)
- **호출됨(Caller · 1)**: `build_checklist`(1006)

### `check_measurable_today()`
- **위치**: `tools/health_check.py:843` (843~899)
- **설명**: [I4] 🔴 **오늘분 측정 가능 여부** — 결과·확정배당·마감시각이 **모두** 갖춰진 경주 비율. *(docstring)*
- **분류 근거**: 이름 규칙 (^check|^test|_test$|guard|_review|audit|health|verify|sanity)
- **호출됨(Caller · 1)**: `build_checklist`(1006)

### `check_file_duplicate()`
- **위치**: `tools/health_check.py:902` (902~921)
- **설명**: [I5] 파일 중복 감지 — 같은 경기장·경주번호의 **날짜가 다른 파일 수**. *(docstring)*
- **분류 근거**: 이름 규칙 (^check|^test|_test$|guard|_review|audit|health|verify|sanity)
- **호출됨(Caller · 0)**: —

### `check_snapshot_ingest()`
- **위치**: `tools/health_check.py:980` (980~1003)
- **설명**: 🔴 I6 배당판 스냅샷 유입(당일) — 0장이면 회원 화면 경주기록에 이미지가 안 뜬다. *(docstring)*
- **분류 근거**: 이름 규칙 (^check|^test|_test$|guard|_review|audit|health|verify|sanity)
- **호출됨(Caller · 0)**: —

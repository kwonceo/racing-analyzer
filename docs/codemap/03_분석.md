# 분석 — 점수 계산·추천 산출

> 🤖 **자동 생성** — `python tools/build_codemap.py` · 2026-07-30 22:47:30
> ⚠ 손으로 고치지 마세요. 다음 생성 때 덮어써집니다.
> ⚠ **AST 한계**: `getattr()` 동적 호출 · `importlib` 경유 · 문자열 디스패치는 잡지 못합니다
>   (`gemini_reviewer`·`health_check` 가 실제로 `importlib` 경유입니다).

총 **85개** 함수

### `_do_analyze(race, jstats, api_key)`
- **위치**: `app.py:984` (984~1057)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 4)**: `analyze`(1061) · `_korea_run_job`(28595) · `korea_reextract`(28845) · `_analyze_one`(28704)

### `_snapshot_timing_learn(rk, compare, stage)`
- **위치**: `app.py:3041` (3041~3097)
- **설명**: [타이밍 학습] T-10 추천 vs T-2 추천을 실제 착순과 비교 → 어느 시점 추천이 더 정확한지 누적. *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_snapshot_build_compare`(2990)  ⚠ 위험목록 함수 → 전수 나열

### `_odds_score(o)`
- **위치**: `app.py:3411` (3411~3421)
- **설명**: 대표 배당 → 배당점수(시장이 얼마나 지지하는가). 낮은 배당=강한 지지=고점. *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 3)**: `_elimination`(3672) · `_integrated_grades`(3951) · `_integrated_adaptive`(4216)

### `_confidence(ftotal, o)`
- **위치**: `app.py:3612` (3612~3622)
- **설명**: [1번] 배당신뢰도(-10~+30): 전적우수+배당낮음→+30, 전적불량+배당낮음→-10(이변의심), 배당낮음 단독→0. *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_elimination`(3672)

### `_fav_score(ftotal, o, jk_rate)`
- **위치**: `app.py:3625` (3625~3639)
- **설명**: [1번] 유력마 점수 = 전적40% + 배당신뢰도30% + 기수20% + 조건적성10% (각 0~100). *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_elimination`(3672)

### `_elim_score(o, avg_place, jk_rate, has_drop30, in_top_exa, no_dist_exp)`
- **위치**: `app.py:3642` (3642~3669)
- **설명**: [2번] 제거 점수(기본 100에서 감점 + 이변 보류 가점). 낮을수록 제거 대상. *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_elimination`(3672)

### `_integrated_grades(form, curQ, curD, weights)`
- **위치**: `app.py:3951` (3951~3983)
- **설명**: [통합분석] 전적 + 배당(이상감지) 결합 점수 → A/B/C/D 재부여. 기본 전적 30% + 배당 70%(배당 우선). *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_triple_analyze`(9492)

### `_pattern_confidence(cur_patterns, pstats)`
- **위치**: `app.py:4023` (4023~4032)
- **설명**: [5번] 매칭된 패턴들의 과거 적중률(표본가중 평균)로 신뢰도 산출. (n>=5 패턴만) *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_triple_analyze`(9492)

### `_apply_mass_drop_strategy(bet_rec, mass_drop, drops, curQ)`
- **위치**: `app.py:4104` (4104~4135)
- **설명**: [대규모급락 전략] 삼복승 보험 8%→15% 확대 + 중배당(7~40배) 급락 복승 보험 추가 + *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_triple_analyze`(9492)

### `_concentration_score(excess_val)`
- **위치**: `app.py:4190` (4190~4194)
- **설명**: 신호강도(음수 %p, 초과급락 또는 절대급락 중 강한 쪽)를 0~100 집중신호 점수로. -20%p 이상=100점. *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 2)**: `_integrated_adaptive`(4216) · `_signal_confidence`(5029)

### `_after_close_learn_result(rk, date, result)`
- **위치**: `app.py:4298` (4298~4328)
- **설명**: [3번] 마감 후 대급락말이 실제 입상(1~3착)했는지 판정 → surge_hit 갱신(입상률 학습). *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_apply_result_learning`(17723)

### `_after_close_stats()`
- **위치**: `app.py:4331` (4331~4346)
- **설명**: [3번] 마감 후 대급락 → 실제 입상률 통계(신뢰도 측정). *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 2)**: `after_close_stats_api`(3229) · `_triple_analyze`(9492)

### `_learn_strong_signals(rk, date, an, top3)`
- **위치**: `app.py:4409` (4409~4445)
- **설명**: [5번] 결과 → 강한 신호 8유형별 적중(신호말이 1~3착 입상) 누적. 경주별 멱등 저장. *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_apply_result_learning`(17723)

### `_pattern_confidence_view()`
- **위치**: `app.py:4467` (4467~4500)
- **설명**: [2·4번] 신호 유형별 발생/적중/적중률 + 통계 신뢰도 상태(표본 50회 게이팅) 통합. *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `pattern_confidence`(2806)

### `_learn_compression(rk, date, an, top3)`
- **위치**: `app.py:4542` (4542~4565)
- **설명**: [4번] 저배당 압축 패턴 발생 경주 → 압축 2두가 실제 복승 적중(둘 다 1~2착)했는지 누적. 경주별 멱등. *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_apply_result_learning`(17723)

### `_learn_high_odds_companion(rk, date, top3, rec, high_thresh)`
- **위치**: `app.py:4606` (4606~4630)
- **설명**: [4번] 1착이 고배당(7배+)인 경주만 분모로, 2·3착에 다른 고배당(7배+)이 있었는지 누적. 경주별 멱등. *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_apply_result_learning`(17723)

### `_learn_third_place(rk, date, an, top3)`
- **위치**: `app.py:4675` (4675~4699)
- **설명**: [5번] 3착 발굴 후보가 실제 3착/입상했는지 신호 유형별 누적. 경주별 멱등. *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_apply_result_learning`(17723)

### `_reversal_strength_score(ratio)`
- **위치**: `app.py:5015` (5015~5019)
- **설명**: 역전비율(<1)을 0~100 점수(0.50이하=100, 0.90=0 선형). [강화] 10% 미만 차(ratio>0.90)=0점. *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_signal_confidence`(5029)

### `_mismatch_strength_score(ratio)`
- **위치**: `app.py:5022` (5022~5026)
- **설명**: 불일치점수(>1)를 0~100 점수(2.0이상=100, 1.2=0 선형). *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_signal_confidence`(5029)

### `_signal_confidence(excess, wx_reversals, mismatch)`
- **위치**: `app.py:5029` (5029~5054)
- **설명**: [4번] 종합 신뢰도 점수 = 초과급락 40% + 쌍승역전 35% + 복승불일치 25% (말별 0~100). *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_triple_analyze`(9492)

### `_rank_inversion_detail(curWin, curQ, curD)`
- **위치**: `app.py:5057` (5057~5138)
- **설명**: [역배열 정확화] 인기순위(단승 우선·없으면 복승) vs 쌍승 배당순위(쌍승 우선·없으면 복승)를 *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_triple_analyze`(9492)

### `_rebound_analyze(seq)`
- **위치**: `app.py:5291` (5291~5333)
- **설명**: [배당 반등 패턴] 단일 조합의 배당 시퀀스(시간순) → 급락 후 반등/재급락 분류. *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_advanced_anomaly`(5336)

### `_bmed_strategy(curQ, key_horses, excess, inverse, mass_drop, signal_confidence, after_close, sport)`
- **위치**: `app.py:6286` (6286~6362)
- **설명**: [1·4번] 현재 상황 자동 분석 → BMED 5전략 중 최적 선택 + 근거 + 원금보전 배분 + 기대환수율. *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_triple_analyze`(9492)

### `_learned_integrated_weights()`
- **위치**: `app.py:6453` (6453~6468)
- **설명**: [배당 우선 전환] 50경주+ 누적 시 이상감지/전적 적중률 비교로 통합 가중치 자동 조정. *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 2)**: `_integrated_grades`(3951) · `_triple_analyze`(9492)

### `_confidence_engine(signal_confidence, form, advanced, key_horses)`
- **위치**: `app.py:6492` (6492~6531)
- **설명**: [2번] BMED 확신도 = 이상감지강도(40%) + 전적점수(30%) + 시세급락지속성(30%). *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_triple_analyze`(9492)

### `_flow_scores(hist, valid_nos, strong_signals, advanced, curQ, curWin, inverse)`
- **위치**: `app.py:6835` (6835~6912)
- **설명**: [3번] 말별 배당 흐름 점수: 상승-10·변동없음-5·하락+10·급락+20·스마트머니+30. *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_triple_analyze`(9492)

### `_core_picks(key_horses, dark_horses, reversal_roles, drops, curWin, curQ, valid_nos)`
- **위치**: `app.py:7070` (7070~7114)
- **설명**: [핵심 추천·추천 과다 근본 해결] 엄격 우선순위로 축 2마리만 선정 → 복승 X+Y · 삼복승 X+Y+Z(딱 이것만). *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_triple_analyze`(9492)

### `_confidence_picks(confidence, curWin, curQ, key_horses, single_rank, anomaly_horse, valid_nos)`
- **위치**: `app.py:7117` (7117~7237)
- **설명**: [복승 축 = 시장 유력마 저배당 기준] 확신도 1위가 고배당(30배+)이면 복승 축으로 쓰지 않고 삼복승 보험으로만. *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_triple_analyze`(9492)

### `_final_picks(cp, curQ, valid_nos, smart_quinella, max_q, reversal_quinellas, dark_quinellas, signal_horses, sig_meta, sport)`
- **위치**: `app.py:7357` (7357~8480)
- **설명**: [추천 구조 개편·종목별 저배당+신호=메인 / 고배당+강신호=BMED특별] 파생 추천을 새 구조로 정리(기존 후보수집·파생필드 무삭제). *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_triple_analyze`(9492)

### `_combo_score(combo)`
- **위치**: `app.py:7498` (7498~7499)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_final_picks`(7357)

### `_score(_no)`
- **위치**: `app.py:8192` (8192~8193)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_final_picks`(7357)

### `_apply_profit_strategy(cp, curQ, valid_nos, sig_meta, sport, category, mass_drop, key_horses)`
- **위치**: `app.py:8583` (8583~8884)
- **설명**: [수익성 3분류 후처리] core_picks 의 finalQuinellas/finalTrifectas 를 경주 등급에 따라 재구성. *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_triple_analyze`(9492)

### `_pace_stats_recompute()`
- **위치**: `app.py:8945` (8945~8993)
- **설명**: pace_analysis/ 스캔 → 페이스별 각질 입상률(빠른→추입·느린→선행 등) → pace_stats.json. *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 2)**: `_pace_log_record`(8916) · `pace_stats_api`(26184)

### `_dark_rank_horses(an)`
- **위치**: `app.py:9079` (9079~9100)
- **설명**: 분석에서 복병 후보를 신호 우선순위로 정렬해 rank1~3 산출. 각: {horse, signal, priority, odds, detected_at}. *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_dark_log_record`(9142)

### `_analyze_missed_dark_horse(rk, top3, an, ranks)`
- **위치**: `app.py:9232` (9232~9255)
- **설명**: [미감지 사후분석] 15배+ 고배당 말이 입상했는데 복병 rank1~3에 없으면 '놓친 신호'로 설명 반환. 없으면 None. *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_dark_log_apply_result`(9175)

### `_dark_stats_recompute()`
- **위치**: `app.py:9265` (9265~9365)
- **설명**: dark_horse_log 전체 스캔 → 신호별·배당구간별·패턴별 적중률 집계 → dark_horse_stats.json. *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 2)**: `_dark_log_apply_result`(9175) · `dark_horse_stats`(19457)  ⚠ 위험목록 함수 → 전수 나열

### `_triple_analyze(rk, rec)`
- **위치**: `app.py:9492` (9492~12179)
- **설명**: [placeholder 배당 필터·3 (2026-07-19)] 발매 전 껍데기 배당(1054.4·9999.9 등 동일 고배당 반복)을 *(주석(본문))*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 15)**: `extract_japan`(12721) · `triple_match`(12810) · `public_matrix`(13414) · `triple_analyze`(13461) · `_build_analysis_log`(14451) · `_apply_result_learning`(17723) … [+9]

### `_ranks(m)`
- **위치**: `app.py:9645` (9645~9646)
- **설명**: 2) 순위 변동 (배당 낮은=인기 순위) *(주석(def 위))*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_triple_analyze`(9492)

### `_rank_form(h)`
- **위치**: `app.py:9991` (9991~9993)
- **설명**: [배당 우선 전환] 랭킹은 combinedProb(시장0.7+전적0.3) 우선 + 배당 가감된 formScoreAdj 미세반영. *(주석(def 위))*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_triple_analyze`(9492)

### `_bet_grade(mk, o1, form_missing, min_odds)`
- **위치**: `app.py:12547` (12547~12556)
- **설명**: 추천도 등급 → (등급, 근거). *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_apply_bet_rules`(12559)

### `_pub_grade(an)`
- **위치**: `app.py:12967` (12967~12980)
- **설명**: 공개용 신뢰도 등급 A/B/C — 기존 raceJudgment(확신도)를 그대로 사용(재계산 없음). *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `public_matrix`(13414)

### `_ai_quality_score(data)`
- **위치**: `app.py:15312` (15312~15360)
- **설명**: [5번] 데이터 품질 점수 — 필수 5항목 각 20점(합 100). 등급: *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_build_ai_training`(15160)

### `_daily_learning_generate(date, extra)`
- **위치**: `app.py:15846` (15846~15898)
- **설명**: [1번] 일별 학습 일지 생성/갱신 → data/daily_learning/<date>.json. *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 3)**: `_start_daily_learning_scheduler`(15913) · `daily_learning`(20615) · `_loop`(15920)  ⚠ 위험목록 함수 → 전수 나열

### `_daily_learning_hour()`
- **위치**: `app.py:15906` (15906~15910)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_start_daily_learning_scheduler`(15913)

### `_start_daily_learning_scheduler()`
- **위치**: `app.py:15913` (15913~15957)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_boot_background`(32285)  ⚠ 위험목록 함수 → 전수 나열

### `_learn_mass_drop(rk, an, top3, payouts)`
- **위치**: `app.py:16003` (16003~16045)
- **설명**: [대규모급락 학습] 결과 입력 시 pattern_learning.json 의 patterns 배열에 사례 1건 추가 + 통계 갱신. *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_apply_result_learning`(17723)

### `_learn_smart_money(rk, an, top3, payouts, date_str)`
- **위치**: `app.py:16048` (16048~16077)
- **설명**: [스마트머니 자동 학습] 결과 입력 시 darkHorses 중 스마트머니(상승후급락) 감지 말이 실제 입상(1~3착)했는지 *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_apply_result_learning`(17723)

### `_learn_signal_stats(rk, an, top3, date_str)`
- **위치**: `app.py:16170` (16170~16197)
- **설명**: [3번] 결과 입력 시 신호별 적중(유력마 기반) 자동 태깅·저장 → signal_stats.json. 경주별 멱등(재입력 시 교체). *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_apply_result_learning`(17723)

### `_learn_mid_high_odds(rk, an, top3, payouts, date_str)`
- **위치**: `app.py:16229` (16229~16264)
- **설명**: [5번] 중고배당 유력마(💎) 감지 시 어떤 신호로 감지했나·실제 입상했나·배당·누적 적중률 저장(경주별 멱등). *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_apply_result_learning`(17723)

### `_learn_upset(rk, an, top3, date_str)`
- **위치**: `app.py:16313` (16313~16380)
- **설명**: 부진마(최근5경주 평균착순≥4.0)의 입상 여부 + 동반 조건을 학습. *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_apply_result_learning`(17723)

### `_recompute_learning_stats(records)`
- **위치**: `app.py:16943` (16943~17133)
- **설명**: 이상감지 유형별 실제 적중률 + 추천 적중률 + 전적/제거 예측 적중률. *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 3)**: `_dl_reliability`(15821) · `_apply_result_learning`(17723) · `results_adjust`(21223)

### `_learn_aux_combos(rk, top3)`
- **위치**: `app.py:17498` (17498~17553)
- **설명**: [보조 조합 학습 (2026-07-21 권대표 지시)] 표시 밖 생성 조합(삼복승 3번째 이후 — 판정 제외)의 적중을 *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_apply_result_learning`(17723)  ⚠ 위험목록 함수 → 전수 나열

### `_apply_result_learning(rk, result, top3, final_odds, stake, payout, inputs, force_relearn)`
- **위치**: `app.py:17723` (17723~18400)
- **설명**: 경주 결과 → 히스토리 기록 + 자동학습 레코드/통계 갱신(공용). *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 8)**: `results_auto`(1393) · `history_record_result`(20234) · `review_save`(20336) · `_register_result_rows`(21122) · `_kra_collect_result_one`(23676) · `_official_result_audit`(24097) · `_keirin_result_backfill_once`(24371) · `_jp_result_backfill_once`(25313)  ⚠ 위험목록 함수 → 전수 나열

### `_validate_learning_data(rk, result, top3, sport)`
- **위치**: `app.py:18408` (18408~18434)
- **설명**: [데이터 검증] 학습·저장 전 자동 오류 체크. 반환 (ok, errors[]). *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_apply_result_learning`(17723)

### `_failure_stats()`
- **위치**: `app.py:19016` (19016~19056)
- **설명**: [5·6번] 복기 대시보드 데이터. 유형별 분포 + 놓친 패턴 TOP + 규칙(개선 전/후 적중률). *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `failure_stats`(19443)

### `_high_odds_stats()`
- **위치**: `app.py:19392` (19392~19426)
- **설명**: [7번] 고배당 누적 통계 — 총/적중/미적중 + A/B/C 분포 + 개선 후 예상 추가 적중(B+C catchable). *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `high_odds_review`(19879)

### `_analyze_competitor_pattern(picks, odds)`
- **위치**: `app.py:19992` (19992~20017)
- **설명**: 추천 조합 → 축(최다등장 번호)·연결마·조합수·고배당포함 패턴 분석. odds(조합별 배당) 있으면 고배당 판정. *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 2)**: `_our_picks_for_race`(20020) · `_competitor_record`(20090)

### `_our_picks_for_race(rk)`
- **위치**: `app.py:20020` (20020~20028)
- **설명**: 우리 시스템 복승 추천 + 축 → analysis_log corePicks/최종추천에서 복원(_rec_combos 재사용). *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_competitor_record`(20090)

### `_competitor_recompute_stats()`
- **위치**: `app.py:20114` (20114~20163)
- **설명**: data/competitor_analysis/ 전체 스캔 → 적중률·축정확도·고배당 비교 통계 누적. *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 2)**: `_competitor_record`(20090) · `competitor_stats`(20224)

### `_analyzed_race_keys()`
- **위치**: `app.py:20963` (20963~20980)
- **설명**: 분석/수집된 모든 raceKey 후보 집합(활성 3종 배당 + odds_history 스냅샷 + analysis_log). *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_resolve_race_key`(20983)

### `_all_analyzed_keys()`
- **위치**: `app.py:21102` (21102~21119)
- **설명**: 매칭 후보 raceKey 전체 — triple_store(현재 캐시) + analysis_log(과거 분석·프룬됨 포함). *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_register_result_rows`(21122)

### `_reversal_strength_stats()`
- **위치**: `app.py:21307` (21307~21361)
- **설명**: [4번] 역배열 강도별 실제 적중률 집계 — analysis_log 결과 있는 경주에서 경주당 최강 역전 1건을 *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `learning_reversal_stats`(21451)

### `_keirin_grade(score)`
- **위치**: `app.py:21722` (21722~21732)
- **설명**: 競走得点 → 전적 등급(스펙: 95+ A / 85~94 B / 75~84 C / <75 D). 예: 99.9점=A. *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 2)**: `_keirin_analyze`(21837) · `_keirin_autocollect_form`(22134)

### `_keirin_analyze(card)`
- **위치**: `app.py:21837` (21837~21875)
- **설명**: 파싱된 출마표 → 등급·결정수 보정·경향 반영 랭킹. *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 2)**: `keirin_card`(22079) · `_keirin_autocollect_form`(22134)

### `_scoreboard_daily(date)`
- **위치**: `app.py:25042` (25042~25169)
- **설명**: ══════════════ [적중왕 타임락 성적표 (2026-07-20)] 전 종목 일일 성적 — 회수율(ROI) 헤드라인 ══════════════ *(주석(def 위))*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 2)**: `scoreboard_daily_api`(25173) · `scoreboard_page`(25179)

### `_rank_index(field)`
- **위치**: `app.py:25802` (25802~25818)
- **설명**: [선행력지수 s1f_index] S1F 최고 빠른 순위(상위30% +15·하위30% -5) / [추입력지수 g1f_index] G1F 최고 빠른 순위 *(주석(def 위))*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_kra_section_records`(25756)

### `_kra_pass_ranks(meet, rc_date, rc_no)`
- **위치**: `app.py:25848` (25848~25871)
- **설명**: [API6_1] 코너 통과순위 시퀀스 → 말별 코너 위치 궤적 + S1F→최종(G1F) 순위변화(delta). *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 2)**: `kra_section`(25393) · `_simulate_race_flow_kra`(26034)

### `_strategy_hitrate()`
- **위치**: `app.py:26331` (26331~26355)
- **설명**: data/race_results 스캔 → {strategy: {races, hits, rate}}. 1시간 캐시. 알림 '유사케이스 적중률'용. *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_premium_alert`(26365)

### `base_form_score(placings)`
- **위치**: `app.py:27112` (27112~27123)
- **설명**: placings: 최근 경주부터 [직전, -2, -3, -4, -5] 착순 리스트. *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 5)**: `_form_from_starters`(1542) · `_keiba_build_form`(22994) · `_jra_build_form`(26786) · `score_horses_raw`(28008) · `score_form`(28085)

### `_norm_grade(g)`
- **위치**: `app.py:27595` (27595~27601)
- **설명**: 등급명 정규화: 로마숫자→아라비아, 공백 제거, 대문자. 'GⅢ'/'G III'→'G3'. *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 2)**: `_grade_exp_bonus_of`(27626) · `_grade_rank`(27636)

### `_grade_exp_bonus_of(g)`
- **위치**: `app.py:27626` (27626~27633)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `grade_bonus`(27646)

### `_grade_rank(g)`
- **위치**: `app.py:27636` (27636~27643)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `grade_bonus`(27646)

### `_form_ext_stats()`
- **위치**: `app.py:27847` (27847~27861)
- **설명**: 캐시된 전적 확장 통계 반환(kra_history mtime 변경 시 자동 재빌드). *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 6)**: `form_ext_stats_api`(26195) · `_infer_gait_from_history`(27842) · `jockey_horse_synergy_bonus`(27864) · `trainer_strategy_ctx`(27882) · `post_position_ctx`(27901) · `score_horses_raw`(28008)

### `trainer_strategy_ctx(trainer, gap_days)`
- **위치**: `app.py:27882` (27882~27898)
- **설명**: [3] 조교사 전략 — 조교사 전체 복승률 + (있으면) 출전간격버킷 복승률. 표시용 컨텍스트 반환. *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 2)**: `form_ext_stats_api`(26195) · `score_horses_raw`(28008)

### `classify_grades(scored)`
- **위치**: `app.py:27983` (27983~28005)
- **설명**: totalScore 사분위로 A/B/C/D(상위25/50/75%) 부여 후, 이상감지로 보정. *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 3)**: `_form_from_starters`(1542) · `compute_horse_scores`(28073) · `combined_analyze`(28221)

### `compute_horse_scores(race, horses, jockey_threshold, anomaly_by_no, leader_names)`
- **위치**: `app.py:28073` (28073~28081)
- **설명**: 3-1~3-5 통합. anomaly_by_no(마번→이상감지) 주면 등급 보정에 반영. *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 3)**: `_korea_deterministic_overlay`(917) · `_form_from_starters`(1542) · `score`(28097)

### `resolve_picks(scored)`
- **위치**: `app.py:28123` (28123~28133)
- **설명**: 베팅용 A/B/C/D 픽을 등급→총점 순 상위 4두로 위치 기반 선정(빈 등급에도 성립). *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `combined_analyze`(28221)

### `apply_odds_grade_corrections(scored)`
- **위치**: `app.py:28137` (28137~28159)
- **설명**: Phase3 보정(신호≥75 상향 / 급락50%+ 보험강제) 이후 추가 반영: *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `combined_analyze`(28221)

### `combined_analyze(race_key, race, horses, snaps, budget)`
- **위치**: `app.py:28221` (28221~28246)
- **설명**: 4-1 데이터 통합 + 4-2 등급 보정 + 4-3 베팅 생성. *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `analyze_combined`(28250)

### `_analyze_one(idx)`
- **위치**: `app.py:28704` (28704~28728)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 0)**: —

### `_timeline_stats_recompute()`
- **위치**: `app.py:31299` (31299~31333)
- **설명**: timeline_snapshot 스캔 → T-7/T-5 적중률·변경 시 적중률·변경마 우승률 → timeline_stats.json. *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 2)**: `timeline_stats`(19534) · `_timeline_snap_apply_result`(31266)  ⚠ 위험목록 함수 → 전수 나열

### `_picks_diff_one(base)`
- **위치**: `app.py:31904` (31904~31948)
- **설명**: 경주 1건의 A/B 대조 dict. 데이터가 없으면 None. *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_picks_diff_day`(31951)

### `_picks_diff_day(date)`
- **위치**: `app.py:31951` (31951~31968)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 2)**: `picks_diff_page`(31972) · `picks_diff_api`(32063)

### `signal_place_stats(date)`
- **위치**: `review_engine.py:248` (248~328)
- **설명**: [유력마 1위·급락 복병 입상률 (2026-07-23 권대표)] ①분석 유력마 1위(T-5경 이력 keyHorses[0])의 *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `sweep`(187)

### `drop_stats(date)`
- **위치**: `review_engine.py:331` (331~398)
- **설명**: 급락 시점(전구간/마감구간)×강도(15-30/30-50/50+%) 밴드별 급락마 입상률 집계. *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `sweep`(187)

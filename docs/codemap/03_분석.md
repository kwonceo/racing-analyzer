# 분석 — 점수 계산·추천 산출

> 🤖 **자동 생성** — `python tools/build_codemap.py` · 2026-08-12 08:36:46
> ⚠ 손으로 고치지 마세요. 다음 생성 때 덮어써집니다.
> ⚠ **AST 한계**: `getattr()` 동적 호출 · `importlib` 경유 · 문자열 디스패치는 잡지 못합니다
>   (`gemini_reviewer`·`health_check` 가 실제로 `importlib` 경유입니다).

총 **208개** 함수

### `_do_analyze(race, jstats, api_key)`
- **위치**: `app.py:991` (991~1064)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 4)**: `analyze`(1068) · `_korea_run_job`(32957) · `korea_reextract`(33324) · `_analyze_one`(33098)

### `_odds_get_race(db, race_key)`
- **위치**: `app.py:1219` (1219~1220)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 3)**: `odds_add_snapshot`(1249) · `odds_compute`(1301) · `odds_race_route`(31257)

### `odds_add_snapshot(race_key, odds_by_horse, t)`
- **위치**: `app.py:1249` (1249~1277)
- **설명**: 한 라운드 스냅샷 기록: {마번: 배당} 을 하나의 타임스탬프로 누적. *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 2)**: `odds_snapshot`(1387) · `combined_analyze`(32402)

### `odds_undo(race_key)`
- **위치**: `app.py:1280` (1280~1292)
- **설명**: 마지막 스냅샷 1라운드 되돌리기 *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 1)**: `odds_undo_route`(31243)

### `odds_clear(race_key)`
- **위치**: `app.py:1295` (1295~1298)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 2)**: `odds_clear_route`(31250) · `combined_analyze`(32402)

### `odds_compute(race_key, bmed_horses)`
- **위치**: `app.py:1301` (1301~1365)
- **설명**: bmed_horses: [{no, name, score}] → 말별 드롭/괴리/통합 신호 + 태그 *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 2)**: `odds_compute_route`(31264) · `_anomaly_by_no`(32292)

### `odds_suggest_bets(computed)`
- **위치**: `app.py:1368` (1368~1383)
- **설명**: signalScore 상위 말로 복승(2)·삼복승(3) 보정 추천(국소 계산) *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 1)**: `odds_compute_route`(31264)

### `_drop_placeholder_combos(combos, label)`
- **위치**: `app.py:1687` (1687~1707)
- **설명**: [placeholder 배당 필터 (2026-07-19)] 발매 전 껍데기 배당(예 1054.4·9999.9)이 유력마·복병· *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_combo_count_dip(prev_q, cur_q, ratio, min_prev)`
- **위치**: `app.py:1842` (1842~1864)
- **설명**: [3번 조합 수 딥] 직전 대비 복승 조합 수가 급감했는지. 실측 112건/23,230틱(0.48%). *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 1)**: `_history_append`(15465)

### `_oddspark_covered_active(rk, within)`
- **위치**: `app.py:1934` (1934~1952)
- **설명**: [자동전송 꼬임 방어] 이 raceKey를 서버가 oddspark로 방금(within초 내) 직접 수집 중인가? *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 1)**: `triple_analyze`(15132)

### `_src_is_oddspark(src)`
- **위치**: `app.py:1985` (1985~1987)
- **설명**: 소스 문자열이 oddspark(백업 소스)인지. 사설(ks1)·keiba·kra_api·화면수집 등은 False(=배당판 권위). *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 2)**: `_do_triple_ingest`(2768) · `_kakao_send_blocked_by_src`(36277)

### `_combo_value_multiset(combos)`
- **위치**: `app.py:1990` (1990~1999)
- **설명**: 조합 목록 → 배당'값' 멀티셋(반올림). 말번호 무관하게 '같은 경주인지' 비교용. *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 1)**: `_oddspark_mapping_suspect`(2450)

### `_combo_map(combos)`
- **위치**: `app.py:2002` (2002~2013)
- **설명**: 조합 목록 → {frozenset(말번호쌍): 배당값}. 말번호 매핑 비교용. *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 1)**: `_oddspark_mapping_suspect`(2450)

### `_oddspark_seen_note(rk, q)`
- **위치**: `app.py:2208` (2208~2225)
- **설명**: oddspark 페이로드의 마번을 기록(1층 명단 확장용). 실패해도 예외를 올리지 않는다. *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 1)**: `_do_triple_ingest`(2768)

### `_oddspark_seen_get(rk)`
- **위치**: `app.py:2228` (2228~2235)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 2)**: `_ingest_ghost_verdict`(2238) · `_odds_suspect_verdict`(2313)

### `_combo_horse_count(combos)`
- **위치**: `app.py:2577` (2577~2588)
- **설명**: 조합 목록에 등장하는 서로 다른 마번 수(N). *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 1)**: `_arity_guard`(2604)

### `_combo_max_no(*sources)`
- **위치**: `app.py:2706` (2706~2728)
- **설명**: [종목 증거판정용] 배당 조합(복승·쌍승·삼복승)에 등장하는 최대 마번을 추출. *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 1)**: `_do_triple_ingest`(2768)

### `_anomaly_combo(raw)`
- **위치**: `app.py:3489` (3489~3499)
- **설명**: 스냅샷 이상감지 원문 → 조합 문자열(예 '2+3'·'4'·'5→10'). *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 1)**: `_anomaly_events_from_doc`(3502)

### `_snapshot_main_combos(quinellas, cap)`
- **위치**: `app.py:3757` (3757~3767)
- **설명**: [비교분석] 추천 변경 여부 판정용 — 상위 복승 조합을 정규화 문자열 집합으로. *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 1)**: `_snapshot_build_compare`(3868)

### `_snapshot_signal_first(rk)`
- **위치**: `app.py:3770` (3770~3790)
- **설명**: [비교분석] odds_history 에서 이 경주의 이상감지 신호(signal_horse)가 처음 잡힌 시점을 'T-N분'으로. *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 1)**: `_snapshot_build_compare`(3868)

### `_snapshot_timing_learn(rk, compare, stage)`
- **위치**: `app.py:3928` (3928~3990)
- **설명**: [타이밍 학습] T-10 추천 vs T-2 추천을 실제 착순과 비교 → 어느 시점 추천이 더 정확한지 누적. *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_snapshot_build_compare`(3868)  ⚠ 위험목록 함수 → 전수 나열

### `_odds_map_un(arr)`
- **위치**: `app.py:4305` (4305~4315)
- **설명**: [{combo,odds}] → {정렬튜플: 최저배당}. *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 8)**: `_baseline_reset_needed`(1760) · `_as_qmap`(1778) · `_curq_shrink_guard`(2363) · `_advanced_anomaly`(6304) · `_triple_analyze`(10724) · `_history_append`(15465) … [+2]

### `_odds_score(o)`
- **위치**: `app.py:4367` (4367~4377)
- **설명**: 대표 배당 → 배당점수(시장이 얼마나 지지하는가). 낮은 배당=강한 지지=고점. *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 3)**: `_elimination`(4628) · `_integrated_grades`(4907) · `_integrated_adaptive`(5172)

### `_jockey_rate_bonus(ctx)`
- **위치**: `app.py:4508` (4508~4520)
- **설명**: [기수 복승률 가감점] 복승률 30%+ → 유력 가점 +10 · 10% 이하 → 제거 감점 -10 · 그 외 0. *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 2)**: `_keiba_build_form`(26514) · `_jra_build_form`(30967)

### `_confidence(ftotal, o)`
- **위치**: `app.py:4568` (4568~4578)
- **설명**: [1번] 배당신뢰도(-10~+30): 전적우수+배당낮음→+30, 전적불량+배당낮음→-10(이변의심), 배당낮음 단독→0. *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_elimination`(4628)

### `_fav_score(ftotal, o, jk_rate)`
- **위치**: `app.py:4581` (4581~4595)
- **설명**: [1번] 유력마 점수 = 전적40% + 배당신뢰도30% + 기수20% + 조건적성10% (각 0~100). *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_elimination`(4628)

### `_elim_score(o, avg_place, jk_rate, has_drop30, in_top_exa, no_dist_exp)`
- **위치**: `app.py:4598` (4598~4625)
- **설명**: [2번] 제거 점수(기본 100에서 감점 + 이변 보류 가점). 낮을수록 제거 대상. *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_elimination`(4628)

### `_time_based_drop_signals(rk)`
- **위치**: `app.py:4856` (4856~4904)
- **설명**: 경주 히스토리(발주전분 기록)로 시간대별 마감 임박 급락 신호 생성. *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)  ⚠ 위험목록 함수 → 전수 나열

### `_integrated_grades(form, curQ, curD, weights)`
- **위치**: `app.py:4907` (4907~4939)
- **설명**: [통합분석] 전적 + 배당(이상감지) 결합 점수 → A/B/C/D 재부여. 기본 전적 30% + 배당 70%(배당 우선). *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_pattern_confidence(cur_patterns, pstats)`
- **위치**: `app.py:4979` (4979~4988)
- **설명**: [5번] 매칭된 패턴들의 과거 적중률(표본가중 평균)로 신뢰도 산출. (n>=5 패턴만) *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_apply_mass_drop_strategy(bet_rec, mass_drop, drops, curQ)`
- **위치**: `app.py:5060` (5060~5091)
- **설명**: [대규모급락 전략] 삼복승 보험 8%→15% 확대 + 중배당(7~40배) 급락 복승 보험 추가 + *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_excess_drop_analysis(drops, curQ)`
- **위치**: `app.py:5096` (5096~5143)
- **설명**: [1번] 초과 급락률 — 시장 전체 평균 급락 대비 각 말의 집중도. *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 2)**: `_triple_analyze`(10724) · `_history_append`(15465)

### `_concentration_score(excess_val)`
- **위치**: `app.py:5146` (5146~5150)
- **설명**: 신호강도(음수 %p, 초과급락 또는 절대급락 중 강한 쪽)를 0~100 집중신호 점수로. -20%p 이상=100점. *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 2)**: `_integrated_adaptive`(5172) · `_signal_confidence`(5997)

### `_signal_situation(drops, mass_drop, excess)`
- **위치**: `app.py:5153` (5153~5169)
- **설명**: [3번] 상황별 가중치 자동 조정. 반환 {name, formW, signalW, signalSource, note}. *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_after_close_learn_result(rk, date, result)`
- **위치**: `app.py:5254` (5254~5286)
- **설명**: [3번] 마감 후 대급락말이 실제 입상(1~3착)했는지 판정 → surge_hit 갱신(입상률 학습). *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_apply_result_learning`(20801)

### `_after_close_stats()`
- **위치**: `app.py:5289` (5289~5304)
- **설명**: [3번] 마감 후 대급락 → 실제 입상률 통계(신뢰도 측정). *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 2)**: `after_close_stats_api`(4185) · `_triple_analyze`(10724)

### `_learn_strong_signals(rk, date, an, top3)`
- **위치**: `app.py:5369` (5369~5407)
- **설명**: [5번] 결과 → 강한 신호 8유형별 적중(신호말이 1~3착 입상) 누적. 경주별 멱등 저장. *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_apply_result_learning`(20801)

### `_pattern_status(fired, rate)`
- **위치**: `app.py:5417` (5417~5426)
- **설명**: 표본수·적중률 → 신뢰도 상태. 반환 (status, icon, note). *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 1)**: `_pattern_confidence_view`(5429)

### `_pattern_confidence_view()`
- **위치**: `app.py:5429` (5429~5462)
- **설명**: [2·4번] 신호 유형별 발생/적중/적중률 + 통계 신뢰도 상태(표본 50회 게이팅) 통합. *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `pattern_confidence`(3604)

### `_signal_type_hitrates()`
- **위치**: `app.py:5465` (5465~5485)
- **설명**: 유형별 발생/적중/적중률 집계 → 가중치 상향 근거. 판정불가(None)는 분모 제외. *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 3)**: `signal_types_stats`(3575) · `_pattern_confidence_view`(5429) · `_triple_analyze`(10724)

### `_learn_compression(rk, date, an, top3)`
- **위치**: `app.py:5504` (5504~5529)
- **설명**: [4번] 저배당 압축 패턴 발생 경주 → 압축 2두가 실제 복승 적중(둘 다 1~2착)했는지 누적. 경주별 멱등. *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_apply_result_learning`(20801)

### `_compression_hitrate()`
- **위치**: `app.py:5532` (5532~5550)
- **설명**: 압축 패턴 복승 적중률(강력/중간·급락결합 별) 집계. *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 2)**: `compression_stats`(3585) · `_pattern_confidence_view`(5429)

### `_learn_high_odds_companion(rk, date, top3, rec, high_thresh)`
- **위치**: `app.py:5570` (5570~5596)
- **설명**: [4번] 1착이 고배당(7배+)인 경주만 분모로, 2·3착에 다른 고배당(7배+)이 있었는지 누적. 경주별 멱등. *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_apply_result_learning`(20801)

### `_high_odds_companion_hitrate()`
- **위치**: `app.py:5599` (5599~5608)
- **설명**: 고배당 1착 경주 중 3착 내 다른 고배당 포함 비율. 반환 {races,hits,rate,reliable}. *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 3)**: `high_odds_companion_stats`(3647) · `_pattern_confidence_view`(5429) · `_triple_analyze`(10724)

### `_learn_third_place(rk, date, an, top3)`
- **위치**: `app.py:5641` (5641~5667)
- **설명**: [5번] 3착 발굴 후보가 실제 3착/입상했는지 신호 유형별 누적. 경주별 멱등. *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_apply_result_learning`(20801)

### `_third_place_hitrate()`
- **위치**: `app.py:5670` (5670~5690)
- **설명**: 신호 유형별 3착 후보의 입상(1~3착)·정확3착 적중률 집계. *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 1)**: `third_place_stats`(3594)

### `_signal_combo_bets(signal_horses, curQ, bet_rec, cap)`
- **위치**: `app.py:5737` (5737~5769)
- **설명**: [신호 조합] 이상감지 신호가 있는 말들의 모든 복승 조합을 추천에 추가(고배당 포함). *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_combo_signal_quality(combo, excess)`
- **위치**: `app.py:5772` (5772~5788)
- **설명**: [4번] 추천 조합의 신호 품질(상/중/하) + 근거. 조합 내 최대 초과급락 말 기준. *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_reversal_strength_score(ratio)`
- **위치**: `app.py:5983` (5983~5987)
- **설명**: 역전비율(<1)을 0~100 점수(0.50이하=100, 0.90=0 선형). [강화] 10% 미만 차(ratio>0.90)=0점. *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_signal_confidence`(5997)

### `_mismatch_strength_score(ratio)`
- **위치**: `app.py:5990` (5990~5994)
- **설명**: 불일치점수(>1)를 0~100 점수(2.0이상=100, 1.2=0 선형). *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_signal_confidence`(5997)

### `_signal_confidence(excess, wx_reversals, mismatch)`
- **위치**: `app.py:5997` (5997~6022)
- **설명**: [4번] 종합 신뢰도 점수 = 초과급락 40% + 쌍승역전 35% + 복승불일치 25% (말별 0~100). *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_rank_inversion_detail(curWin, curQ, curD)`
- **위치**: `app.py:6025` (6025~6106)
- **설명**: [역배열 정확화] 인기순위(단승 우선·없으면 복승) vs 쌍승 배당순위(쌍승 우선·없으면 복승)를 *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_rebound_analyze(seq)`
- **위치**: `app.py:6259` (6259~6301)
- **설명**: [배당 반등 패턴] 단일 조합의 배당 시퀀스(시간순) → 급락 후 반등/재급락 분류. *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_advanced_anomaly`(6304)

### `_smart_money_combos(hist, curQ, min_rise, min_drop)`
- **위치**: `app.py:6475` (6475~6517)
- **설명**: [유형8] 상승 후 급락(스마트머니) — 조합 배당이 초반에 상승(인기 이탈)했다가 *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 1)**: `_strong_signals`(6560)

### `_strong_signals(advanced, inverse, drops, cur_mb, hist, curQ, after_close)`
- **위치**: `app.py:6560` (6560~6664)
- **설명**: 8유형 강신호 산출(무삭제 — 표시/학습용 파생 뷰). 기존 advanced·inverse·drops·history만 소비. *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_horse_repr_odds(h, curWin, curQ)`
- **위치**: `app.py:6668` (6668~6680)
- **설명**: 말 대표 배당 = 단승 우선, 없으면 그 말 포함 최저 복승. *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 6)**: `_compression_pattern`(6683) · `_third_place_hunt`(6747) · `_repr_odds_from_rec`(7014) · `_high_odds_companion`(7036) · `consider`(6784) · `repr_odds`(7045)

### `_repr_odds_from_rec(rec)`
- **위치**: `app.py:7014` (7014~7033)
- **설명**: rec(단승/복승)에서 말별 대표배당 맵 + curWin/curQ 재구성(학습·분석 공용). *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 1)**: `_learn_high_odds_companion`(5570)

### `_high_odds_companion(drops, wx_reversals, inverse, curWin, curQ, key_horses, learned, after_close, high_thresh)`
- **위치**: `app.py:7036` (7036~7116)
- **설명**: [참고 추천] 고배당(7배+) 신호말 감지 시 그 말과 복승 배당 낮게 묶인 다른 고배당 말 탐색. *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `repr_odds(h)`
- **위치**: `app.py:7045` (7045~7046)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 1)**: `_high_odds_companion`(7036)

### `_bmed_strategy(curQ, key_horses, excess, inverse, mass_drop, signal_confidence, after_close, sport)`
- **위치**: `app.py:7254` (7254~7330)
- **설명**: [1·4번] 현재 상황 자동 분석 → BMED 5전략 중 최적 선택 + 근거 + 원금보전 배분 + 기대환수율. *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_learned_integrated_weights()`
- **위치**: `app.py:7421` (7421~7436)
- **설명**: [배당 우선 전환] 50경주+ 누적 시 이상감지/전적 적중률 비교로 통합 가중치 자동 조정. *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 2)**: `_integrated_grades`(4907) · `_triple_analyze`(10724)

### `_confidence_engine(signal_confidence, form, advanced, key_horses)`
- **위치**: `app.py:7460` (7460~7499)
- **설명**: [2번] BMED 확신도 = 이상감지강도(40%) + 전적점수(30%) + 시세급락지속성(30%). *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_flow_scores(hist, valid_nos, strong_signals, advanced, curQ, curWin, inverse)`
- **위치**: `app.py:7803` (7803~7880)
- **설명**: [3번] 말별 배당 흐름 점수: 상승-10·변동없음-5·하락+10·급락+20·스마트머니+30. *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_high_odds_candidates(flow, removed_nos, key_horses, min_odds)`
- **위치**: `app.py:7902` (7902~7916)
- **설명**: [2번] 제거 후 남은 말 중 배당 10배+ 인데 하락/급락(흐름 좋음) → 고배당 후보 자동 편입. *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_mid_high_odds_favorites(valid_nos, curWin, curQ, drops, dark_horses, reversal_roles, form, min_odds)`
- **위치**: `app.py:7919` (7919~7968)
- **설명**: [중고배당 유력마·1번] 복승 10배+ 인데 강한 신호 1개+ 보유 → 💎 중고배당 유력마. *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_core_picks(key_horses, dark_horses, reversal_roles, drops, curWin, curQ, valid_nos)`
- **위치**: `app.py:8038` (8038~8082)
- **설명**: [핵심 추천·추천 과다 근본 해결] 엄격 우선순위로 축 2마리만 선정 → 복승 X+Y · 삼복승 X+Y+Z(딱 이것만). *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_confidence_picks(confidence, curWin, curQ, key_horses, single_rank, anomaly_horse, valid_nos)`
- **위치**: `app.py:8085` (8085~8205)
- **설명**: [복승 축 = 시장 유력마 저배당 기준] 확신도 1위가 고배당(30배+)이면 복승 축으로 쓰지 않고 삼복승 보험으로만. *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_dense_no_signal_box(key_horses, valid_nos, curWin, curQ, trio_map, drops, wx_reversals, dark_horses, strong_signals, dense_odds, strong_drop)`
- **위치**: `app.py:8208` (8208~8254)
- **설명**: [무신호 저배당밀집] 급락(≤-30%)·쌍승역전·복병·강신호가 전부 0건이고 상위 배당이 밀집(대표배당 15배 이내)이면, *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_final_picks(cp, curQ, valid_nos, smart_quinella, max_q, reversal_quinellas, dark_quinellas, signal_horses, sig_meta, sport)`
- **위치**: `app.py:8348` (8348~9704)
- **설명**: [추천 구조 개편·종목별 저배당+신호=메인 / 고배당+강신호=BMED특별] 파생 추천을 새 구조로 정리(기존 후보수집·파생필드 무삭제). *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_combo_basis(combo)`
- **위치**: `app.py:8498` (8498~8514)
- **설명**: 조합 근거 문장 리스트 — 급락%·스마트머니·역배열·복병·집중급락(말별). *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 2)**: `_final_picks`(8348) · `_force_pair`(8784)

### `_combo_score(combo)`
- **위치**: `app.py:8516` (8516~8517)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_final_picks`(8348)

### `_combo_has_signal(combo)`
- **위치**: `app.py:8519` (8519~8523)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 1)**: `_final_picks`(8348)

### `_combo_strong(combo)`
- **위치**: `app.py:8525` (8525~8539)
- **설명**: BMED 특별 강신호: signalScore 70+ OR 급락15%+ OR (스마트머니+역배열 동시). *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 1)**: `_final_picks`(8348)

### `_combo_has_signal_relaxed(combo)`
- **위치**: `app.py:8541` (8541~8553)
- **설명**: [메인 보완·완화] 급락 10%+ OR signalScore 50+ OR 기존 신호(drop/rev/smart/anomaly). *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 1)**: `_final_picks`(8348)

### `_score(_no)`
- **위치**: `app.py:9220` (9220~9221)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_final_picks`(8348)

### `_ev_band_p(o)`
- **위치**: `app.py:9751` (9751~9769)
- **설명**: 배당대별 적중률 추정 — 학습 표본 50+ 밴드는 실측치(자동 교체), 아니면 보수 가정치. *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 1)**: `_apply_profit_strategy`(9809)

### `_ev_bands_update(combo_odds_list, top2)`
- **위치**: `app.py:9772` (9772~9792)
- **설명**: [기대값 학습] 판정된 라이브 복승 추천의 (배당대, 적중) 누적 → 표본 50+부터 가정치 자동 교체. *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 1)**: `_apply_result_learning`(20801)

### `_apply_profit_strategy(cp, curQ, valid_nos, sig_meta, sport, category, mass_drop, key_horses)`
- **위치**: `app.py:9809` (9809~10110)
- **설명**: [수익성 3분류 후처리] core_picks 의 finalQuinellas/finalTrifectas 를 경주 등급에 따라 재구성. *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_conn_pair_odds(_c)`
- **위치**: `app.py:9997` (9997~10004)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 0)**: —

### `_pace_stats_recompute()`
- **위치**: `app.py:10173` (10173~10221)
- **설명**: pace_analysis/ 스캔 → 페이스별 각질 입상률(빠른→추입·느린→선행 등) → pace_stats.json. *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 2)**: `_pace_log_record`(10142) · `pace_stats_api`(29893)

### `_dark_odds_band(odds)`
- **위치**: `app.py:10244` (10244~10254)
- **설명**: 복병 배당 구간 라벨(15~30/30~50/50~100/100+). 15배 미만은 None(복병 아님). *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 1)**: `_dark_stats_recompute`(10493)

### `_dark_signal_of(no, an)`
- **위치**: `app.py:10272` (10272~10304)
- **설명**: [복병 신호 우선순위] 말 no의 복병 신호 판정 → (priority, label). priority 낮을수록 강함(1>2>3). *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 2)**: `_dark_rank_horses`(10307) · `_analyze_missed_dark_horse`(10460)

### `_dark_rank_horses(an)`
- **위치**: `app.py:10307` (10307~10328)
- **설명**: 분석에서 복병 후보를 신호 우선순위로 정렬해 rank1~3 산출. 각: {horse, signal, priority, odds, detected_at}. *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_dark_log_record`(10370)

### `_dark_combo_section(key_horses, dark_horses, curQ)`
- **위치**: `app.py:10331` (10331~10362)
- **설명**: [복병 복승 자동편성] 유력마 1위 + 복병 1·2순위 → 복승 2조합 + 삼복승 1조합(별도 섹션). *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_analyze_missed_dark_horse(rk, top3, an, ranks)`
- **위치**: `app.py:10460` (10460~10483)
- **설명**: [미감지 사후분석] 15배+ 고배당 말이 입상했는데 복병 rank1~3에 없으면 '놓친 신호'로 설명 반환. 없으면 None. *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_dark_log_apply_result`(10403)

### `_dark_stats_recompute()`
- **위치**: `app.py:10493` (10493~10593)
- **설명**: dark_horse_log 전체 스캔 → 신호별·배당구간별·패턴별 적중률 집계 → dark_horse_stats.json. *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 2)**: `_dark_log_apply_result`(10403) · `dark_horse_stats`(22556)  ⚠ 위험목록 함수 → 전수 나열

### `_triple_analyze(rk, rec)`
- **위치**: `app.py:10724` (10724~13557)
- **설명**: [placeholder 배당 필터·3 (2026-07-19)] 발매 전 껍데기 배당(1054.4·9999.9 등 동일 고배당 반복)을 *(주석(본문))*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 15)**: `extract_japan`(14344) · `triple_match`(14481) · `public_matrix`(15085) · `triple_analyze`(15132) · `_build_analysis_log`(16347) · `_apply_result_learning`(20801) … [+9]

### `_ranks(m)`
- **위치**: `app.py:10886` (10886~10887)
- **설명**: 2) 순위 변동 (배당 낮은=인기 순위) *(주석(def 위))*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_rank_form(h)`
- **위치**: `app.py:11232` (11232~11234)
- **설명**: [배당 우선 전환] 랭킹은 combinedProb(시장0.7+전적0.3) 우선 + 배당 가감된 formScoreAdj 미세반영. *(주석(def 위))*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_min_odds(arr)`
- **위치**: `app.py:11908` (11908~11910)
- **설명**: 시계열 차트: 복승·쌍승·삼복승 각 "최저(최인기) 배당"의 라운드별 변화 (3줄) *(주석(def 위))*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_tri_odds(cc)`
- **위치**: `app.py:12289` (12289~12299)
- **설명**: [보완②] 삼복승 예상배당 표기 — 실배당(trio_map) 우선, 없으면 _trio_est 추정. *(주석(def 위))*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_edtri_odds(cc)`
- **위치**: `app.py:12317` (12317~12324)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_hscore(_no)`
- **위치**: `app.py:12513` (12513~12515)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_t5_combos(fq)`
- **위치**: `app.py:13607` (13607~13616)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 1)**: `_apply_t5_freeze`(13661)

### `_combo_odds_trend(rk, combos)`
- **위치**: `app.py:14110` (14110~14163)
- **설명**: {조합: {'first','last','low','surge','rebound'}} — triple_store history(최근 12틱) 기반. *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 1)**: `_apply_bet_rules`(14178)

### `_bet_grade(mk, o1, form_missing, min_odds)`
- **위치**: `app.py:14166` (14166~14175)
- **설명**: 추천도 등급 → (등급, 근거). *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_apply_bet_rules`(14178)

### `_pub_grade(an)`
- **위치**: `app.py:14638` (14638~14651)
- **설명**: 공개용 신뢰도 등급 A/B/C — 기존 raceJudgment(확신도)를 그대로 사용(재계산 없음). *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `public_matrix`(15085)

### `_combo_dict(arr)`
- **위치**: `app.py:15417` (15417~15427)
- **설명**: [{combo,odds}] → {'1+2': 45.2, ...}. *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 1)**: `_history_append`(15465)

### `_det_combo_sets(items)`
- **위치**: `app.py:16039` (16039~16050)
- **설명**: [{combo:[...]}...] → {frozenset(마번)} 집합. 잘못된 항목은 조용히 건너뛴다. *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 1)**: `_deterministic_review`(16053)

### `_combo_reason(nos)`
- **위치**: `app.py:16486` (16486~16488)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 1)**: `_build_analysis_log`(16347)

### `_odds_range_label(o)`
- **위치**: `app.py:17170` (17170~17173)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 1)**: `_build_ai_training`(17176)

### `_ai_quality_score(data)`
- **위치**: `app.py:17328` (17328~17376)
- **설명**: [5번] 데이터 품질 점수 — 필수 5항목 각 20점(합 100). 등급: *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_build_ai_training`(17176)

### `_ai_data_status()`
- **위치**: `app.py:17517` (17517~17560)
- **설명**: [3·7번] AI 학습 데이터 현황 — 수집/고품질/평균품질·목표 진행률·마일스톤(100/500) 예상 일정. *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 1)**: `ai_training_status`(23733)

### `_daily_learning_generate(date, extra)`
- **위치**: `app.py:17894` (17894~17946)
- **설명**: [1번] 일별 학습 일지 생성/갱신 → data/daily_learning/<date>.json. *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 3)**: `_start_daily_learning_scheduler`(17961) · `daily_learning`(23805) · `_loop`(17991)  ⚠ 위험목록 함수 → 전수 나열

### `_daily_learning_hour()`
- **위치**: `app.py:17954` (17954~17958)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_start_daily_learning_scheduler`(17961)

### `_start_daily_learning_scheduler()`
- **위치**: `app.py:17961` (17961~18029)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_boot_background`(38998)  ⚠ 위험목록 함수 → 전수 나열

### `_learn_mass_drop(rk, an, top3, payouts)`
- **위치**: `app.py:18077` (18077~18121)
- **설명**: [대규모급락 학습] 결과 입력 시 pattern_learning.json 의 patterns 배열에 사례 1건 추가 + 통계 갱신. *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_apply_result_learning`(20801)

### `_learn_smart_money(rk, an, top3, payouts, date_str)`
- **위치**: `app.py:18124` (18124~18155)
- **설명**: [스마트머니 자동 학습] 결과 입력 시 darkHorses 중 스마트머니(상승후급락) 감지 말이 실제 입상(1~3착)했는지 *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_apply_result_learning`(20801)

### `_active_signal_horses(an)`
- **위치**: `app.py:18176` (18176~18218)
- **설명**: [3번] 이 경주에서 각 신호 유형이 선정한 유력마/복병 마번 → {신호명: [마번]}(빈 신호는 제외). *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 1)**: `_learn_signal_stats`(18248)

### `_learn_signal_stats(rk, an, top3, date_str)`
- **위치**: `app.py:18248` (18248~18277)
- **설명**: [3번] 결과 입력 시 신호별 적중(유력마 기반) 자동 태깅·저장 → signal_stats.json. 경주별 멱등(재입력 시 교체). *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_apply_result_learning`(20801)

### `_learn_mid_high_odds(rk, an, top3, payouts, date_str)`
- **위치**: `app.py:18309` (18309~18346)
- **설명**: [5번] 중고배당 유력마(💎) 감지 시 어떤 신호로 감지했나·실제 입상했나·배당·누적 적중률 저장(경주별 멱등). *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_apply_result_learning`(20801)

### `_signal_reliability()`
- **위치**: `app.py:18349` (18349~18356)
- **설명**: [5번] 신호별 신뢰도 요약(50경주+ 표본이면 신뢰) → {신호명: {rate_quinella, rate_trifecta, count, reliable}}. *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 1)**: `_signal_reliability_for`(18359)

### `_signal_reliability_for(strong_signals, dark_horses, inverse, compression_pattern, cross_reversal)`
- **위치**: `app.py:18359` (18359~18392)
- **설명**: [5번] 현재 분석에서 활성인 신호별 과거 적중률(50경주+ 신뢰) → 프론트 강조 표시용. *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_learn_upset(rk, an, top3, date_str)`
- **위치**: `app.py:18395` (18395~18464)
- **설명**: 부진마(최근5경주 평균착순≥4.0)의 입상 여부 + 동반 조건을 학습. *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_apply_result_learning`(20801)

### `_combo_timeline(doc, combo)`
- **위치**: `app.py:18660` (18660~18690)
- **설명**: 스냅샷들에서 특정 복승 조합(combo=[a,b])의 배당 변화 타임라인 추출. *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 1)**: `_build_race_report`(18821)

### `_signal_timeline_from_doc(doc)`
- **위치**: `app.py:18693` (18693~18753)
- **설명**: [3·4·5번] 스냅샷의 signal_horse 시퀀스에서 이상감지 말 변경 이력 + 신호 안정화(2연속 확정) *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 2)**: `_signal_timeline`(18756) · `_build_race_report`(18821)

### `_signal_timeline(rk)`
- **위치**: `app.py:18756` (18756~18763)
- **설명**: 경주 히스토리 파일을 읽어 _signal_timeline_from_doc 도출(없으면 None). *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 2)**: `_triple_analyze`(10724) · `signal_timeline_api`(18767)

### `_signal_win_tags(an, top3)`
- **위치**: `app.py:18778` (18778~18812)
- **설명**: [4번] 어떤 신호가 실제 입상마를 맞혔는지 태깅. *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 2)**: `_build_race_report`(18821) · `_apply_result_learning`(20801)

### `_report_odds_band(odds)`
- **위치**: `app.py:18815` (18815~18818)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 1)**: `_build_race_report`(18821)

### `_rep_combo_for(no)`
- **위치**: `app.py:18842` (18842~18855)
- **설명**: 말 no 를 포함하는 대표 복승 조합(급락 가장 큰 것 → 없으면 추천 조합). *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 1)**: `_build_race_report`(18821)

### `_recompute_learning_stats(records)`
- **위치**: `app.py:19031` (19031~19221)
- **설명**: 이상감지 유형별 실제 적중률 + 추천 적중률 + 전적/제거 예측 적중률. *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 3)**: `_dl_reliability`(17869) · `_apply_result_learning`(20801) · `results_adjust`(24468)

### `_card_odds_at(rk, when_hms, combo)`
- **위치**: `app.py:19244` (19244~19277)
- **설명**: 그 시각에 가장 가까운 스냅샷에서 조합 배당을 찾는다. 🔴 빠진 조합의 배당이 핵심이다. *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 1)**: `_race_card_timeline`(19280)

### `_rf_combo_map(src, arity)`
- **위치**: `app.py:19468` (19468~19493)
- **설명**: 복승/쌍승/삼복승 원본을 {"1+2": 배당} 으로 편다. dict·list 두 형태를 다 받는다. *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 1)**: `_race_full`(19729)

### `_rf_signals(doc)`
- **위치**: `app.py:19697` (19697~19726)
- **설명**: [신호 모음] 🔴 **실제 저장 위치에서 읽는다.** *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 1)**: `_race_full`(19729)

### `_learn_aux_combos(rk, top3)`
- **위치**: `app.py:20537` (20537~20594)
- **설명**: [보조 조합 학습 (2026-07-21 권대표 지시)] 표시 밖 생성 조합(삼복승 3번째 이후 — 판정 제외)의 적중을 *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_apply_result_learning`(20801)  ⚠ 위험목록 함수 → 전수 나열

### `_winning_quinella_odds(rk, top2)`
- **위치**: `app.py:20742` (20742~20758)
- **설명**: [수익 자동 계산·추정] 실배당 미입력 시 승자 복승 조합(top2)의 마지막 시장배당을 analysis_log *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 3)**: `_build_race_result`(16955) · `_apply_result_learning`(20801) · `_ml_row_build`(21532)

### `_learn_suppressed()`
- **위치**: `app.py:20779` (20779~20780)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 23)**: `_after_close_learn_result`(5254) · `_record_near_miss`(5323) · `_learn_strong_signals`(5369) · `_learn_compression`(5504) · `_learn_high_odds_companion`(5570) · `_learn_third_place`(5641) … [+17]

### `_apply_result_learning(rk, result, top3, final_odds, stake, payout, inputs, force_relearn)`
- **위치**: `app.py:20801` (20801~21495)
- **설명**: 경주 결과 → 히스토리 기록 + 자동학습 레코드/통계 갱신(공용). *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 9)**: `results_auto`(1400) · `history_record_result`(23424) · `review_save`(23526) · `_register_result_rows`(24367) · `_kra_collect_result_one`(27224) · `_official_result_audit`(27672) · `_keirin_result_backfill_once`(28021) · `_jp_result_backfill_once`(29005) · `_jra_result_save`(30636)  ⚠ 위험목록 함수 → 전수 나열

### `_odds_val(x)`
- **위치**: `app.py:21104` (21104~21105)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 1)**: `_apply_result_learning`(20801)

### `_validate_learning_data(rk, result, top3, sport)`
- **위치**: `app.py:21503` (21503~21529)
- **설명**: [데이터 검증] 학습·저장 전 자동 오류 체크. 반환 (ok, errors[]). *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_apply_result_learning`(20801)

### `_build_signal_performance()`
- **위치**: `app.py:21603` (21603~21656)
- **설명**: [signal_performance] 신호별 성과(총·적중·적중률·평균배당·ROI) 재집계 → signal_performance.json. *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 1)**: `_kra_backfill_loop`(27405)

### `_timeline_signal_label(pct)`
- **위치**: `app.py:21749` (21749~21761)
- **설명**: 직전대비 급락% → 사람이 읽는 신호 강도 라벨(역추적 리포트용). *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 2)**: `_failure_report`(21980) · `_rr_horse_timeline`(22203)

### `_failure_stats()`
- **위치**: `app.py:22115` (22115~22155)
- **설명**: [5·6번] 복기 대시보드 데이터. 유형별 분포 + 놓친 패턴 TOP + 규칙(개선 전/후 적중률). *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `failure_stats`(22542)

### `_rr_winning_odds(rr, top3)`
- **위치**: `app.py:22226` (22226~22247)
- **설명**: 정답 복승(top2) 배당 — odds_timeline 최신값 → odds_at_start → investment 폴백. 삼복승은 대개 미수집. *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 1)**: `_high_odds_prefilter`(22360)

### `_high_odds_simulate(fail)`
- **위치**: `app.py:22320` (22320~22340)
- **설명**: [5번] '개선된 로직 적용 시' 반사실 시뮬레이션. 실패 유형·초점말 급락폭 기반. *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 1)**: `_high_odds_review_one`(22372)

### `_high_odds_next_rules(fail)`
- **위치**: `app.py:22343` (22343~22357)
- **설명**: [6번] 미적중 유형별 내일 적용 규칙(고배당 특화). *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 1)**: `_high_odds_review_one`(22372)

### `_high_odds_prefilter(rr)`
- **위치**: `app.py:22360` (22360~22369)
- **설명**: 전체 분석 전 저비용 판정: race_results 의 결과 top3 + 정답 복승 배당으로 고배당 여부만 확인. *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 2)**: `_high_odds_review_one`(22372) · `_high_odds_scan`(22464)

### `_high_odds_scan(date, save, limit)`
- **위치**: `app.py:22464` (22464~22488)
- **설명**: [1번] 결과 입력된 경주(race_results/)를 스캔해 고배당(복승30+/삼복승100+)만 심층 분석·저장. *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 1)**: `high_odds_review`(23069)

### `_high_odds_stats()`
- **위치**: `app.py:22491` (22491~22525)
- **설명**: [7번] 고배당 누적 통계 — 총/적중/미적중 + A/B/C 분포 + 개선 후 예상 추가 적중(B+C catchable). *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `high_odds_review`(23069)

### `_analyze_competitor_pattern(picks, odds)`
- **위치**: `app.py:23182` (23182~23207)
- **설명**: 추천 조합 → 축(최다등장 번호)·연결마·조합수·고배당포함 패턴 분석. odds(조합별 배당) 있으면 고배당 판정. *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 2)**: `_our_picks_for_race`(23210) · `_competitor_record`(23280)

### `_our_picks_for_race(rk)`
- **위치**: `app.py:23210` (23210~23218)
- **설명**: 우리 시스템 복승 추천 + 축 → analysis_log corePicks/최종추천에서 복원(_rec_combos 재사용). *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_competitor_record`(23280)

### `_competitor_recompute_stats()`
- **위치**: `app.py:23304` (23304~23353)
- **설명**: data/competitor_analysis/ 전체 스캔 → 적중률·축정확도·고배당 비교 통계 누적. *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 2)**: `_competitor_record`(23280) · `competitor_stats`(23414)

### `_analyzed_race_keys()`
- **위치**: `app.py:24208` (24208~24225)
- **설명**: 분석/수집된 모든 raceKey 후보 집합(활성 3종 배당 + odds_history 스냅샷 + analysis_log). *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_resolve_race_key`(24228)

### `_all_analyzed_keys()`
- **위치**: `app.py:24347` (24347~24364)
- **설명**: 매칭 후보 raceKey 전체 — triple_store(현재 캐시) + analysis_log(과거 분석·프룬됨 포함). *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_register_result_rows`(24367)

### `_reversal_strength_stats()`
- **위치**: `app.py:24552` (24552~24606)
- **설명**: [4번] 역배열 강도별 실제 적중률 집계 — analysis_log 결과 있는 경주에서 경주당 최강 역전 1건을 *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `learning_reversal_stats`(24696)

### `_band(diff)`
- **위치**: `app.py:24555` (24555~24562)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 2)**: `_reversal_strength_stats`(24552) · `drop_stats`(331)

### `_keirin_grade(score)`
- **위치**: `app.py:25033` (25033~25043)
- **설명**: 競走得点 → 전적 등급(스펙: 95+ A / 85~94 B / 75~84 C / <75 D). 예: 99.9점=A. *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 2)**: `_keirin_analyze`(25149) · `_keirin_autocollect_form`(25536)

### `_keirin_style_bonus(r)`
- **위치**: `app.py:25046` (25046~25063)
- **설명**: 결정수 보정: 차입(差)·마크(マ) 우세→추입형 +5 / 도주(逃) 우세→선행형 +3 / 젖히기(捲)→+4. *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 1)**: `_keirin_analyze`(25149)

### `_keirin_analyze(card)`
- **위치**: `app.py:25149` (25149~25187)
- **설명**: 파싱된 출마표 → 등급·결정수 보정·경향 반영 랭킹. *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 2)**: `keirin_card`(25445) · `_keirin_autocollect_form`(25536)

### `_oddspark_grid_combos(html, max_no)`
- **위치**: `app.py:25240` (25240~25276)
- **설명**: oddspark 배당 그리드 HTML → {tuple(sorted(combo)): odds}. 복승(2두)·삼복승(3두) 공용. *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 2)**: `_keiba_parse_quinella_grid`(25279) · `_keiba_parse_trio_grid`(25286)

### `_keirin_odds_pairs(cells)`
- **위치**: `app.py:25306` (25306~25324)
- **설명**: ['4','4.5','2','12.7','4', ...] → 선두 정수(축 차번호)와 (배당,순위) 쌍 목록. *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 2)**: `_keirin_parse_quinella`(25327) · `_keirin_parse_exacta`(25350)

### `_odds_target_mismatch(rk, race_no, jo)`
- **위치**: `app.py:25626` (25626~25648)
- **설명**: [검역 게이트 (2026-07-23 다마노 11R 오염 실사고)] 저장 키(raceKey) vs 수집 대상(raceNo·경기장) 교차 검증. *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 2)**: `keirin_odds`(25675) · `keiba_odds`(26024)

### `_keiba_odds_live(q, x)`
- **위치**: `app.py:25816` (25816~25824)
- **설명**: 파싱된 복승·쌍승이 '발매 중 실배당'인지 판정(마감 후·발매 전 가짜값 차단). *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 2)**: `keiba_odds`(26024) · `_multi_collect_one`(34659)

### `_class_grade_pick(text)`
- **위치**: `app.py:26237` (26237~26250)
- **설명**: 레이스명에서 클래스 1개를 뽑는다(예 'Ｃ３一'). 없거나 혼합이면 None. *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_keiba_parse_shutsuba`(26269)

### `_class_grade_rank(g)`
- **위치**: `app.py:26253` (26253~26266)
- **설명**: 클래스 문자열 → 서열 점수(작을수록 상위). Ａ>Ｂ>Ｃ>Ｄ · 급내 1>2>3 · 조 一>二>三. *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 0)**: —

### `_corner_move_bonus(past)`
- **위치**: `app.py:26428` (26428~26451)
- **설명**: [코너 위치 변화·신규] oddspark 통과순위(corner=3C·4C)의 '첫 코너 → 마지막 코너(4C)' 상대위치 변화로 *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 2)**: `_keiba_build_form`(26514) · `_jra_build_form`(30967)

### `_scoreboard_daily(date)`
- **위치**: `app.py:28704` (28704~28861)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 2)**: `scoreboard_daily_api`(28865) · `scoreboard_page`(28871)

### `_rank_index(field)`
- **위치**: `app.py:29511` (29511~29527)
- **설명**: [선행력지수 s1f_index] S1F 최고 빠른 순위(상위30% +15·하위30% -5) / [추입력지수 g1f_index] G1F 최고 빠른 순위 *(주석(def 위))*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_kra_section_records`(29465)

### `_kra_pass_ranks(meet, rc_date, rc_no)`
- **위치**: `app.py:29557` (29557~29580)
- **설명**: [API6_1] 코너 통과순위 시퀀스 → 말별 코너 위치 궤적 + S1F→최종(G1F) 순위변화(delta). *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 2)**: `kra_section`(29102) · `_simulate_race_flow_kra`(29743)

### `_kra_jockey_change_bonus(meet, rc_date, rc_no, chul_no)`
- **위치**: `app.py:29639` (29639~29665)
- **설명**: [API10_1 기수변경 보정] 경주 전 기수 교체 감지 → 승부의지/전력 보정. *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_simulate_race_flow_kra(meet, rc_date, rc_no, valid_nos, win_odds)`
- **위치**: `app.py:29743` (29743~29813)
- **설명**: [6단계 경주 전개 시뮬레이션] API6_1 코너순위 + API37_1 구간지수 통합. *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 2)**: `_triple_analyze`(10724) · `kra_section`(29102)

### `_strategy_hitrate()`
- **위치**: `app.py:30040` (30040~30064)
- **설명**: data/race_results 스캔 → {strategy: {races, hits, rate}}. 1시간 캐시. 알림 '유사케이스 적중률'용. *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_premium_alert`(30074)

### `base_form_score(placings)`
- **위치**: `app.py:31293` (31293~31304)
- **설명**: placings: 최근 경주부터 [직전, -2, -3, -4, -5] 착순 리스트. *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 5)**: `_form_from_starters`(1560) · `_keiba_build_form`(26514) · `_jra_build_form`(30967) · `score_horses_raw`(32189) · `score_form`(32266)

### `course_aptitude_bonus(past_races, race)`
- **위치**: `app.py:31323` (31323~31347)
- **설명**: 현재 거리 ±100m 경험 +10 / 현재 코스(내·외) 경험 +10 / 현재 조건(급) 경험 +5. *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 1)**: `score_horses_raw`(32189)

### `jockey_bonus(horse, threshold)`
- **위치**: `app.py:31363` (31363~31378)
- **설명**: 기수 3개월 복승률 상위 20% +15 / 기수-마필 직전 경주 적중(같은 기수·3착 이내) +10. *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 1)**: `score_horses_raw`(32189)

### `_apply_pace_analysis(form, horse_count)`
- **위치**: `app.py:31456` (31456~31510)
- **설명**: [2~7] form 각질 편성 분석 → 페이스 예측 + 각질별 유불리 보정(paceBonus 가산·무삭제). *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_simulate_race_flow_keirin(horses, line, tendency, valid_nos, win_odds)`
- **위치**: `app.py:31517` (31517~31605)
- **설명**: [경륜 특화①②·전개 시뮬레이션 (2026-07-19)] oddspark 출마표 전적(styleType·득점·결정수비율)과 *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `_gscore(g)`
- **위치**: `app.py:31558` (31558~31559)
- **설명**: 그룹 정렬: 선두 선수 총점 높은 라인부터 *(주석(def 위))*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 0)**: —

### `_simulate_race_flow_jp(form, valid_nos, win_odds)`
- **위치**: `app.py:31608` (31608~31668)
- **설명**: [일본경마 전개 시뮬레이션] 각질·상3F로 선행경합→페이스→유리한 말→고배당 복병 예측. *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 1)**: `_triple_analyze`(10724)

### `distance_change_bonus(horse, race, style)`
- **위치**: `app.py:31672` (31672~31685)
- **설명**: 이번 거리 vs 직전 경주 거리. 단축=스피드 유리 +5, 연장=추격형 +3·선행형 -3·미상 적성체크. *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 4)**: `_det_horse_note`(943) · `_keiba_build_form`(26514) · `_jra_build_form`(30967) · `score_horses_raw`(32189)

### `jockey_change_bonus(horse, leader_names, rate_fn)`
- **위치**: `app.py:31701` (31701~31729)
- **설명**: [1] 기수 교체 감지. 직전 기수 vs 이번 기수. *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 4)**: `_det_horse_note`(943) · `_keiba_build_form`(26514) · `_jra_build_form`(30967) · `score_horses_raw`(32189)

### `weight_change_bonus(horse)`
- **위치**: `app.py:31733` (31733~31742)
- **설명**: 이번 부담중량 vs 직전. 증가 -5, 감소 +5, 동일/미상 0. (부담중량=핸디캡, 마체중과 별개) *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 4)**: `_det_horse_note`(943) · `_keiba_build_form`(26514) · `_jra_build_form`(30967) · `score_horses_raw`(32189)

### `body_weight_change_bonus(cur_bw, past)`
- **위치**: `app.py:31753` (31753~31772)
- **설명**: [마체중 변화·신규] 이번 마체중(cur_bw) vs 직전 경주 마체중. *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 2)**: `_keiba_build_form`(26514) · `_jra_build_form`(30967)

### `_norm_grade(g)`
- **위치**: `app.py:31776` (31776~31782)
- **설명**: 등급명 정규화: 로마숫자→아라비아, 공백 제거, 대문자. 'GⅢ'/'G III'→'G3'. *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 2)**: `_grade_exp_bonus_of`(31807) · `_grade_rank`(31817)

### `_grade_exp_bonus_of(g)`
- **위치**: `app.py:31807` (31807~31814)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `grade_bonus`(31827)

### `_grade_rank(g)`
- **위치**: `app.py:31817` (31817~31824)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `grade_bonus`(31827)

### `grade_bonus(cur_grade, past_grades)`
- **위치**: `app.py:31827` (31827~31846)
- **설명**: [2번] 등급 경험(최상위 하나) + 하락(상위→하위·강자 +15)/상승(하위→상위·적응중 -5). *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 3)**: `_keiba_build_form`(26514) · `_jra_build_form`(30967) · `score_horses_raw`(32189)

### `dist_exp_bonus(past_races, cur_dist)`
- **위치**: `app.py:31850` (31850~31861)
- **설명**: [3번] 당거리(정확히 같은 거리) 출전 경험. 있음 +10, 없음 0. 반환 (bonus, detail, experienced). *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 3)**: `_keiba_build_form`(26514) · `_jra_build_form`(30967) · `score_horses_raw`(32189)

### `dist_aptitude_bonus(past_races, cur_dist, tol)`
- **위치**: `app.py:31864` (31864~31881)
- **설명**: [거리 적성·신규] 이번 경주 거리(±tol m 유사거리) 전적의 복승권율(1~3착 비율)로 적성 등급. *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 2)**: `_keiba_build_form`(26514) · `_jra_build_form`(30967)

### `_form_ext_stats()`
- **위치**: `app.py:32028` (32028~32042)
- **설명**: 캐시된 전적 확장 통계 반환(kra_history mtime 변경 시 자동 재빌드). *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 6)**: `form_ext_stats_api`(29904) · `_infer_gait_from_history`(32023) · `jockey_horse_synergy_bonus`(32045) · `trainer_strategy_ctx`(32063) · `post_position_ctx`(32082) · `score_horses_raw`(32189)

### `jockey_horse_synergy_bonus(jockey, horse_name)`
- **위치**: `app.py:32045` (32045~32060)
- **설명**: [2] 기수-말 궁합 — KRA 이력 조합 복승권율. A(40%+)+15 / B(20~40%)+5 / C(20%↓)-5 / 첫기승 표시. *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 2)**: `form_ext_stats_api`(29904) · `score_horses_raw`(32189)

### `trainer_strategy_ctx(trainer, gap_days)`
- **위치**: `app.py:32063` (32063~32079)
- **설명**: [3] 조교사 전략 — 조교사 전체 복승률 + (있으면) 출전간격버킷 복승률. 표시용 컨텍스트 반환. *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 2)**: `form_ext_stats_api`(29904) · `score_horses_raw`(32189)

### `back_power_bonus(my_last3f, all_last3f)`
- **위치**: `app.py:32095` (32095~32109)
- **설명**: [3번] 상승3F(막판 뒷힘). 경주 내 상대: 상위30%(빠름) +15, 하위30%(느림) -5, 보통 0. *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 1)**: `_apply_back_power`(26578)

### `_high_odds_verdict(win_odds, has_signal)`
- **위치**: `app.py:32113` (32113~32126)
- **설명**: [1·5번] 단승 배당 기준 고배당 제거. 999=무조건 / 100배+=확실 / 50~99=권장 / 30~49=신호없으면 제거. *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 1)**: `_elimination`(4628)

### `_upgrade(grade)`
- **위치**: `app.py:32159` (32159~32161)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 1)**: `classify_grades`(32164)

### `classify_grades(scored)`
- **위치**: `app.py:32164` (32164~32186)
- **설명**: totalScore 사분위로 A/B/C/D(상위25/50/75%) 부여 후, 이상감지로 보정. *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 3)**: `_form_from_starters`(1560) · `compute_horse_scores`(32254) · `combined_analyze`(32402)

### `score_horses_raw(race, horses, jockey_threshold, leader_names)`
- **위치**: `app.py:32189` (32189~32251)
- **설명**: 3-1~3-4: 마필별 baseScore/courseBonus/jockeyBonus/distanceBonus/jockeyChangeBonus/ *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 2)**: `compute_horse_scores`(32254) · `combined_analyze`(32402)

### `compute_horse_scores(race, horses, jockey_threshold, anomaly_by_no, leader_names)`
- **위치**: `app.py:32254` (32254~32262)
- **설명**: 3-1~3-5 통합. anomaly_by_no(마번→이상감지) 주면 등급 보정에 반영. *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 3)**: `_korea_deterministic_overlay`(924) · `_form_from_starters`(1560) · `score`(32278)

### `resolve_picks(scored)`
- **위치**: `app.py:32304` (32304~32314)
- **설명**: 베팅용 A/B/C/D 픽을 등급→총점 순 상위 4두로 위치 기반 선정(빈 등급에도 성립). *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `combined_analyze`(32402)

### `apply_odds_grade_corrections(scored)`
- **위치**: `app.py:32318` (32318~32340)
- **설명**: Phase3 보정(신호≥75 상향 / 급락50%+ 보험강제) 이후 추가 반영: *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `combined_analyze`(32402)

### `combined_analyze(race_key, race, horses, snaps, budget)`
- **위치**: `app.py:32402` (32402~32427)
- **설명**: 4-1 데이터 통합 + 4-2 등급 보정 + 4-3 베팅 생성. *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `analyze_combined`(32431)

### `_render_band(page)`
- **위치**: `app.py:32557` (32557~32557)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 1)**: `_korea_extract_race`(32627)

### `_analyze_one(idx)`
- **위치**: `app.py:33098` (33098~33122)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 0)**: —

### `_forecast_boot_state()`
- **위치**: `app.py:34542` (34542~34549)
- **설명**: [부팅 상태 노출 (2026-07-31)] 다른 Gemini 기능은 배너에 상태가 찍히는데 예측만 없었다. *(주석(def 위))*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 0)**: —

### `_grade_forecast_on_result(rk, result_top3, payout_quinella)`
- **위치**: `app.py:34633` (34633~34656)
- **설명**: [Phase B] 결과 확정 시 채점. **완전 방어적**. *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_apply_result_learning`(20801)

### `_combos(key)`
- **위치**: `app.py:35987` (35987~35995)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 1)**: `_kakao_anchor_log`(35982)

### `_timeline_stats_recompute()`
- **위치**: `app.py:36760` (36760~36794)
- **설명**: timeline_snapshot 스캔 → T-7/T-5 적중률·변경 시 적중률·변경마 우승률 → timeline_stats.json. *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 2)**: `timeline_stats`(22633) · `_timeline_snap_apply_result`(36727)  ⚠ 위험목록 함수 → 전수 나열

### `_picks_diff_one(base)`
- **위치**: `app.py:37453` (37453~37497)
- **설명**: 경주 1건의 A/B 대조 dict. 데이터가 없으면 None. *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `_picks_diff_day`(37500)

### `_picks_diff_day(date)`
- **위치**: `app.py:37500` (37500~37517)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 2)**: `picks_diff_page`(37521) · `picks_diff_api`(37612)

### `_combo_set(v)`
- **위치**: `review_engine.py:41` (41~49)
- **설명**: '1+4' / [1,4] / '1-4' → frozenset({1,4}) *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 6)**: `_displayed_sets`(52) · `_signal_horses`(64) · `_history_combos`(93) · `_locked_quinella_odds`(111) · `_fix_connector_combos`(409) · `replay_day`(478)

### `_signal_horses(log_doc)`
- **위치**: `review_engine.py:64` (64~90)
- **설명**: 이 경주에서 시스템이 '알고 있었던' 말 전부(유력마·복병·신호·확신도·고배당후보). *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 1)**: `classify_race`(134)

### `_history_combos(log_doc)`
- **위치**: `review_engine.py:93` (93~108)
- **설명**: 추천 이력에 등장했던 복승·삼복승 조합 전부(시점 무관). *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 1)**: `classify_race`(134)

### `_locked_quinella_odds(log_doc, combo_set)`
- **위치**: `review_engine.py:111` (111~121)
- **설명**: 표시(잠금) 시점의 해당 복승 배당 — rec_history 마지막 quinellas 스냅샷에서. *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 1)**: `classify_race`(134)

### `signal_place_stats(date)`
- **위치**: `review_engine.py:248` (248~328)
- **설명**: [유력마 1위·급락 복병 입상률 (2026-07-23 권대표)] ①분석 유력마 1위(T-5경 이력 keyHorses[0])의 *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `sweep`(187)

### `drop_stats(date)`
- **위치**: `review_engine.py:331` (331~398)
- **설명**: 급락 시점(전구간/마감구간)×강도(15-30/30-50/50+%) 밴드별 급락마 입상률 집계. *(docstring)*
- **분류 근거**: 이름 규칙 (_analyze|_score|_picks|_strategy|_confidence|_grade|_rank|_predict|_learn|_stats)
- **호출됨(Caller · 1)**: `sweep`(187)

### `_band(dr)`
- **위치**: `review_engine.py:350` (350~351)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 2)**: `_reversal_strength_stats`(24552) · `drop_stats`(331)

### `_fix_connector_combos(log_doc, limit)`
- **위치**: `review_engine.py:409` (409~457)
- **설명**: [fix_connectors 계열] favAxis 2두 × 연결마(keyHorses[2:] + confTop1) 복승 조합 집합. *(docstring)*
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 1)**: `replay_day`(478)

### `_c_odds(_c)`
- **위치**: `review_engine.py:448` (448~451)
- **설명**: ⚠ 설명 없음 (docstring 추가 필요)
- **분류 근거**: 도메인 2차 (analy|score|pick|strateg|co
- **호출됨(Caller · 0)**: —

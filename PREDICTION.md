# PREDICTION.md — 2부(예상) · **실행 전 기록**

> 규칙: 조사·수정을 **시작하기 전에** 적는다. 실행 후에 고치지 않는다.
> 끝나면 「실제」열만 채우고, 빗나간 항목은 빗나간 채로 남긴다.

---

## 세션 2026-08-01 (24차) — _rec_valid 원인 · 회원 피해 · 무효 측정 재실행

### 작업 1 — `_rec_valid` 가 9로 줄어든 원인

| # | 예상 | 실제 |
|---|---|---|
| V-① | `_rec_valid` 는 **유령 마번 방어**용으로 만들어진 "현재 배당에 실제로 등장하는 마번" 집합일 것이다. 소스는 `curQ`(복승) 또는 **단승(`win`) 맵**. | |
| V-② | 🔴 **단승 맵 기반이면 그게 범인이다** — 사설 배당판의 복승 탭에서는 단승 값이 일부만 채워진다. 중경 7R 에서 복승은 55조합(11두 완전)인데 두수만 9인 것이 그 증거. | |
| V-③ | 빠진 2두는 **최대 마번(10·11)일 가능성**이 높으나, 단승 기반이면 **인기 낮은 아무 말**일 수도 있다. | |
| V-④ | 🔴 **이쪽이 주범일 것이다.** 파서 수정 후 수집된 중경 7R 이 **마번 11개 전부 들어왔는데도 두수 9** 였다 — 파서는 고쳐졌고 이 필터가 남았다. ⇒ 경마 50.5% 중 **상당 부분이 `_rec_valid`** 로 본다. | |

### 작업 2 — 회원 피해

| # | 예상 | 실제 |
|---|---|---|
| L-① | 두수 부족 51건 중 빠진 말이 3착 안에 든 것은 **10~20건**. | |
| L-② | 🔴 빠진 말이 든 정답 조합의 확정배당은 **전체 평균보다 높을 것**(큰 마번 = 인기 낮음 = 고배당). | |
| L-③ | ⚠ `measure_recovery.py` 는 **"빠진 말" 축으로 못 가른다** — 그 도구는 K 확대 리플레이용이다. ⇒ **가를 수 있는 범위만** 내고, 못 하는 것은 그렇게 보고한다. | |

### 작업 3 — 무효 측정 재실행

| # | 예상 | 실제 |
|---|---|---|
| R-① | 경마 분모 12 → **400~550**. 어제 "판정 불가"였던 K 확대가 이제 **판정 가능**해질 것. | |
| R-② | 🔴 방향은 어제와 같을 것(**늘릴수록 회수율 하락**). 다만 **절대값은 크게 달라질 수 있다** — 어제는 n=12 였다. | |
| R-③ | ⚠ **두수 누락 미해결 상태의 값**임을 반드시 명시한다. 작업 1 해결 후 다시 재야 한다. | |

---

## 세션 2026-08-01 (23차) — 누락 규모 재산정 · 백필 apply · 재로드 검증

### 작업 1 — 누락 규모 재산정

| # | 예상 | 실제 |
|---|---|---|
| M-① | 🔴 **대표 지적이 맞을 것이다.** 상삼각 매트릭스면 **최대 마번의 행은 항상 비어** 있어 매 경주 지워진다. 다만 **전 경주는 아닐 것** — 사이트/표 구조가 전부 삼각은 아니다. **20~50%** 로 본다. | |
| M-② | 🔴 "마번 축소 22건"은 **여러 마리가 사라진 경우만** 센 것이다(첫 스냅샷 대비 마지막). **한 마리만 처음부터 없던 경우는 안 세었다** ⇒ **하한이 맞다.** | |
| M-③ | 최대 마번이 실제 입상한 건수는 **45건보다 클 것**(45건은 `raceHorseCount` 기준이고 이번은 조합 마번 기준). | |
| M-④ | 🔴 **경륜이 더 심할 것**으로 본다 — 7~9명이라 삼각 매트릭스가 작고 마지막 행이 더 자주 빈다. ⚠ 반대로 경마는 두수가 커서 표 구조가 다를 수 있다. | |

### 백필 apply

| # | 예상 | 실제 |
|---|---|---|
| B-① | 587건 채워져 경마 확정배당 **24 → 611건**, 보유율 **3.1% → 약 80%**. | |
| B-② | 미확정 경주(착순 없음)에는 **아무것도 안 쓴다** — 도구가 `result.1st` 없으면 대상에서 뺀다. | |
| B-③ | `measure_recovery.py` 경마 분모 **12 → 400~550**. 무효 처리한 측정을 **다시 낼 수 있게 된다.** | |

### 작업 3 — 회원 피해

| # | 예상 | 실제 |
|---|---|---|
| C-① | 🔴 **큰 마번은 인기가 낮아 고배당**이다. 최대 마번이 든 정답 조합의 배당 중앙은 **전체 정답 조합보다 높을 것**. | |
| C-② | ⇒ **사업 원칙(고배당·중배당 기본)과 정면으로 부딪힌다** — 우리가 고배당을 못 잡은 이유의 **구조적 일부**다. | |

---

## 세션 2026-08-01 (22차) — 확장 파서 A·B·C 수정 · 검증 4항목 · 백필

### 파서 수정

| # | 예상 | 실제 |
|---|---|---|
| P-① | `extractByMatrix` 는 **셀을 훑으며 마번을 수집**할 것이다 — 그래서 **빈 행/열이 통째로 빠진다**. 헤더(첫 행·첫 열)를 **먼저 읽어 축을 고정**하면 A 가 해결된다. | |
| P-② | 🔴 **B(칸에 숫자 둘)가 실재하는지 확인이 먼저**다. 삿포로 9R 화면에서 `19.3` 칸 안에 초록색 `2.4배` 가 함께 보였다. 코드가 **첫 숫자만** 잡거나 **마지막 숫자**를 잡고 있을 것. | |
| P-③ | C(`horseCount`)는 payload 필드 추가라 **서버 무변경으로도 안전**하다(서버가 모르면 무시). | |
| P-④ | 🔴 **검증은 오늘 안에 못 끝날 수 있다** — 확장 재로드는 대표가 해야 하고, 라이브 경주가 필요하다. 오늘 남은 중앙경마(15:xx~18:30)로 **일부는 가능**하나 22경주 축소·45건 입상누락은 **과거 데이터라 재측정이 안 된다**. ⇒ **"수정 후 신규 수집분"으로만 검증 가능**하다고 보고해야 한다. | |

### 검증 4항목

| # | 예상 | 실제 |
|---|---|---|
| V-① | 배당 오차중앙 24.1% → **5% 미만**: netkeiba·oddspark 대조로 **신규 수집분만** 잴 수 있다. | |
| V-② | 입상누락 45건 → 0: **과거분은 못 고친다.** 신규분에서 0이 유지되는지로만 본다. | |
| V-③ | 마번 축소 22건 → 0: 동상. | |
| V-④ | 🔴 **두수 = 헤더 정확 일치(오차 0)**: 이건 **수정 직후 바로 확인 가능**하다(payload `horseCount` vs 조합에서 역산한 두수). | |

### 백필 순서

| # | 예상 | 실제 |
|---|---|---|
| B-① | ⚠ **백필은 파서와 무관하다** — 확정배당은 `keiba.go.jp` 공식 소스에서 오고, 착순도 이미 저장돼 있다. **두수 오류가 백필 값을 오염시키지 않는다.** 다만 지시한 순서는 지킨다. | |
| B-② | 백필 후 경마 확정배당 보유율 **6.4% → 약 80%**(587/765 + 기존 24). | |

---

## 세션 2026-08-01 (21차) — 지방·경륜 oddspark 우선 · 두수 누락 · 재작성 목록

### 승인 적용 — 지방·경륜 oddspark 우선

| # | 예상 | 실제 |
|---|---|---|
| G-① | `_fresh_private` 는 다중경주 경로(app.py:30731 부근)에 있다. **그 판정만 끄면** oddspark 가 항상 기록한다. | |
| G-② | 🔴 **oddspark 가 못 가져오는 종목이 있다** — 한국경마(KRA)·JRA중앙·경정·바이크. 그 구간은 **확장이 유일 소스**라 폴백이 반드시 필요하다. | |
| G-③ | 적용 후 `src` 분포가 **oddspark 로 크게 이동**할 것. 다만 사설 전용 경주는 그대로 private. | |

### 작업 1 — 두수 누락

| # | 예상 | 실제 |
|---|---|---|
| N-① | 🔴 **두수 불일치가 흔할 것이다.** private 수집분의 30~60% 로 본다. | |
| N-② | 🔴 **마감이 다가올수록 줄어드는 패턴이 일반적일 것**(9R 에서 6→5두). 마감 직전이 BMED 핵심 구간인데 거기서 말이 사라진다. | |
| N-③ | `max_q` 영향은 **생각보다 작을 것** — 경마 ≤9두는 전부 `_mainmax=3` 이라 5두든 7두든 같다. 🔴 **진짜 피해는 조합 생성**이다(7두 21조합 → 5두 10조합, 절반이 사라진다). | |
| N-④ | 원인: 배당 매트릭스에서 **값이 비어 있는 행/열**(취소마·미발매)을 통째로 건너뛰는 것으로 본다. 9R 화면에서 7번 행이 `-` 로 차 있었다. | |

### 작업 3 — 재작성 목록

| # | 예상 | 실제 |
|---|---|---|
| I-① | 무효 대상: 고배당 실태(17차) · 연결마 폭(18차) · 급락 임계 곡선(19차) · 두수별 추천 개수(15차) — **전부 마감배당 근사 기반**. | |
| I-② | 유효 유지: `measure_recovery.py` 확정배당 기준 결과(조합 확대 K별) · 백필 `--dry` 587건 · 게이트 검증 · 스키마 손대조. | |

---

## 세션 2026-08-01 (20차) — 삿포로 9R 전수 · 파서 오류 사례 · 파서 전종목 영향

### 작업 1 — 삿포로 9R 전수 (결과 5-7-4)

| # | 예상 | 실제 |
|---|---|---|
| R-① | 5번은 우리 `keyHorses` **상위**일 것이고 **시장 인기는 낮았을 것**이다. 그렇다면 **시장이 낮게 본 말을 우리가 본 것** = 우위 사례. | |
| R-② | 🔴 7번(정답 짝)은 **우리가 못 봤을 가능성이 높다**. 오늘 실측상 "축 맞고 짝 놓침"이 46.1% 였다. | |
| R-③ | 5+7 확정 馬連은 **높을 것**(5번 인기 낮으면). **20~60배** 예상. | |
| R-④ | 우리 조합(2+5 등)의 실제 배당은 화면 표시(2.4)와 **크게 다를 것** — netkeiba 2+5 = 19.7 이었다. | |
| R-⑤ | 🔴 **핵심**: 5+7 이 **배당 낮은순에서는 하위권**(10위 이하)일 것이고, **EV 순으로 올리면 상위로 올라올 것**이다. 그러면 **정렬 변경안의 첫 실증**이 된다. ⚠ 반대로 EV 순에서도 하위면 **정렬로는 못 잡는다**는 뜻이다 — 그것도 중요한 결과다. | |

### 작업 3 — 확장 파서 전종목 영향

| # | 예상 | 실제 |
|---|---|---|
| X-① | 🔴 **같은 파서다**. `content.js` 의 `extractByMatrix('odds_content')` 가 **사이트별이 아니라 사설 배당판 공통**이다 ⇒ 지방·경륜 `private` 수집분도 **같은 오류 가능**. | |
| X-② | 다만 지방·경륜은 **`oddspark_bg` 가 주 소스**라 영향이 제한적일 것. `private` 우선 게이트(`_fresh_private`)가 있으므로 **사설이 켜진 구간만** 오염. | |
| X-③ | 🔴 **오늘까지 잰 모든 회수율이 흔들릴 수 있다.** 다만 `measure_recovery.py` 는 **확정배당 기준**이라 배당값 자체는 안전하고, **"마감배당 근사"를 쓴 측정만** 흔들린다. | |

---

## 세션 2026-08-01 (19차) — 확정배당 백필 · 정렬 안 설계 · 급락 임계 곡선

### 작업 1 — 확정배당 백필

| # | 예상 | 실제 |
|---|---|---|
| B-① | 중앙(JRA)은 netkeiba 로 **14건**(결과 없음)만 대상. 확정배당만 없는 건 **0건**이라 중앙은 이미 깨끗하다. | |
| B-② | 🔴 **가장 큰 불확실성**: 지방 650건을 채울 소스가 **과거분을 주는가**. `_keiba_result_payouts` 는 `keiba.go.jp` 를 쓰는데 **당일 위주일 가능성**이 있다. 안 되면 `nar.netkeiba.com` 으로 가야 하고 그러면 **race_id 매핑**이 필요하다. | |
| B-③ | 경륜은 확정배당 보유율이 높을 것(어제 I1 에서 `cycle 30/30 100%`). 미보유분은 **적을 것**. | |
| B-④ | 백필 후 분모: 지방이 되면 **경마 674건** → 최소 100건 **크게 초과**. 안 되면 **24건 그대로** → 판정 불가 유지. | |

### 작업 2 — 정렬 안 설계

| # | 예상 | 실제 |
|---|---|---|
| S-① | 전부 `_final_picks` **한 곳**(app.py:7827 `final_q = _main_cand[:max_q]`)에서 정렬 키만 바꾸면 된다 — **1~5줄**. 되돌리기도 1줄. | |
| S-② | 배당중앙 변화 방향 예상: ①현행 **낮음** · ②EV **중간**(EV 는 저배당 고확률도 높게 나올 수 있다) · ③저평가 **높음** · ④신호강도 **불확실** · ⑤구간균등 **가장 높고 안정적**. | |
| S-③ | ⚠ ②EV 는 우리 확률 추정이 부정확하면 **저배당으로 다시 수렴**한다. 우리 확률의 신뢰도가 관건. | |

### 작업 3 — 급락 임계 곡선

| # | 예상 | 실제 |
|---|---|---|
| T-① | 현재 임계 **-30%**(경고)·**-50%**(강력). 발동률 96.9~99.7%. | |
| T-② | 원칙 18 적정 구간(5~30%)에 들어가려면 **-50% ~ -70%** 는 되어야 할 것. | |
| T-③ | 🔴 그 지점에서 **성적 차이가 안 날 가능성이 높다**. 어제 "급락 임계는 변별력 없음"이 반복 확인됐다. 차이가 나면 그것이 **진짜 우위의 첫 증거**다. | |

---

## 세션 2026-08-01 (18차) — 연결마 폭 · 경쟁AI 등록 · 우리 고유 우위 검증

### 작업 1 — 연결마 폭

| # | 예상 | 실제 |
|---|---|---|
| L-① | 우리 조합 수는 **8두↓ 2.21 · 9~12두 2.52~2.64 · 13두↑ 3.62~4.20**. 타사 3~4개 기준이면 **8두↓·9~12두 구간에서 우리가 적다**(약 1~1.5개). 13두↑는 오히려 비슷하다. | |
| L-② | 🔴 **"축은 맞고 짝을 놓친" 경주가 많을 것이다**(경륜 92.6% 선례). 경마도 **60~80%** 로 본다. | |
| L-③ | 🔴 **확대하면 적중률은 오르고 회수율은 내린다**(어제 K 확대와 같은 방향). **그런데 적중배당 중앙값도 내려갈 것이다** — `_main_cand` 가 **배당 낮은순**이라 추가되는 조합이 **저배당 쪽**이기 때문이다. ⇒ **사업 목표(고배당)에 오히려 역행**할 수 있다. 이게 이번 측정의 핵심 관전 포인트다. | |
| L-④ | ⇒ 만약 L-③ 이 맞으면 결론은 **"조합을 늘리는 것"이 아니라 "정렬을 바꾸는 것"**(17차 안 ②)이다. | |

### 작업 2 — 경쟁 AI 등록

| # | 예상 | 실제 |
|---|---|---|
| C-① | `register_competitor.py` 는 **없을 것**이다(고아 도구 목록에 없었다). `app.py` 안에 `competitor_list`·`_competitor_recompute_stats` API 는 있다. | |
| C-② | 사진 인식은 **Claude Vision 경로**(한국 PDF 전적에 쓰던 것)를 재사용해야 할 것. 즉 **API 비용**이 든다. | |
| C-③ | 🔴 30~50경주를 쌓으려면 대표가 **매 경주 사진을 올려야** 한다. 하루 100경주 중 30~50이면 **사실상 상시 수작업**이다 → **비현실적**이라고 본다. | |
| C-④ | ⇒ 대안(우리 데이터만으로 "놓친 짝" 분석)이 **더 현실적**이다. 이미 오늘 재고 있다. | |

### 작업 3 — 우리 고유 우위

| # | 예상 | 실제 |
|---|---|---|
| E-① | 🔴 케이스스터디 3건은 **선택 편향**일 것이다. 전체에서 **1% 미만**. | |
| E-② | 급락 신호 유무별 성적 대조 — **n 이 작아 판정 불가**일 가능성이 높다(확정배당 24건뿐). | |
| E-③ | 어제 급락 임계 -30% 에서 WARNING **84.2%** 였다 — **거의 전부에 붙는다** = 변별력이 낮다. ⇒ "신호 있음 vs 없음" 비교 자체가 **분모가 한쪽으로 쏠려** 성립하기 어려울 것. | |

---

## 세션 2026-08-01 (17차) — 경마 확정배당 분모 · 고배당 안 설계 · 고배당 실태

### 작업 1 — 확정배당 분모 채우기

| # | 예상 | 실제 |
|---|---|---|
| N-① | 중앙·지방 **둘 다 백필 가능**(16차에서 확인). 며칠치는 **수 개월~수 년** 남아 있을 것. | |
| N-② | 🔴 `_jp_result_backfill_once` 라는 함수는 **없을 것**이다. 어제 만든 것은 `_jra_result_loop`(중앙 전용)이다. 지방은 `_keirin_result_backfill_once`(경륜)·`_official_result_audit` 계열이 있으나 **지방경마 확정배당을 채우는 경로는 없을 것**. | |
| N-③ | 🔴 **가장 큰 장벽은 race_id 매핑**이다. NAR race_id 는 `202645073101` 형태(`45`=경마장 코드)인데 우리는 **경기장 한글명**만 갖고 있다. 경륜 `joCode` 처럼 **코드 사전이 필요**하다. | |
| N-④ | 백필 후 분모: 우리 경마 765건 중 **매핑 가능한 것만** 채워진다. 낙관 **600~750건**, 비관 **매핑 실패로 100건 미만**. ⚠ 최소 100건 기준은 **넘을 것**으로 본다. | |

### 작업 2 — 고배당 안 설계 (설계만)

| # | 예상 | 실제 |
|---|---|---|
| H-① | 🔴 **②(정렬 변경)가 근본**이다. `final_q = _main_cand[:max_q]` 가 **배당 낮은순**이라 저배당 편중은 **정렬의 직접 결과**다. 상한만 올려도(①) 정렬이 그대로면 **저배당이 먼저 채워져 효과가 없다**. | |
| H-② | ③(고배당 전용 슬롯)은 `bmedSpecial`(★★)이 이미 그 설계다. **경륜에서 적중 0** 이었다 — 경마에서 다른지는 **분모가 채워져야** 안다. | |
| H-③ | ④(두수별 분기)는 `_mainmax` 계단이 이미 있으므로 **같은 자리에 붙이면 된다**. 구조 변경이 가장 작다. | |

### 작업 3 — 고배당 실태 (마감배당 근사)

| # | 예상 | 실제 |
|---|---|---|
| A-① | 🔴 **실제 1·2착 조합 배당 중앙값이 우리 추천 중앙값보다 훨씬 높을 것**이다. 추천 중앙 5~10배 vs 적중 중앙 **20~40배**. | |
| A-② | 두수가 클수록 격차가 벌어질 것(13두↑에서 가장 큼). | |
| A-③ | ⚠ 마감배당 근사는 **27%가 30% 초과 오차**다. **방향만** 본다. | |

---

## 세션 2026-08-01 (16차) — 게이트가 낡은 값을 쓰게 하나 · 확정배당 분모 · O6 재측정안

### 작업 1 — 게이트 vs 신선도

| # | 예상 | 실제 |
|---|---|---|
| F-① | 🔴 **측정 불가일 가능성이 높다.** 확장은 `capturedAt`(전송 시각)만 보내고 **사설 배당판이 화면에 쓴 "HH:MM 現在" 는 안 보낸다.** 저장된 스냅샷에는 **서버 수신 시각(`t`)뿐**일 것. ⇒ "표시 시각 vs 수신 시각" 차이는 **데이터가 없어 못 잰다**고 보고해야 할 것. | |
| F-② | netkeiba 와 private 의 **배당값이 실제로 다를 것**이다. 마감 직전은 변동이 크므로 5분 차이면 **최저배당 조합이 바뀔 수도** 있다. ⚠ 어느 쪽이 실제에 가까운지는 **확정배당(馬連 4400 · 조합 3-11)** 과 대조하면 판정 가능하다. | |
| F-③ | 확장 지연은 **사설 배당판 자체의 갱신 주기**에 달렸다. "13:15 現在"가 T-3.5분이었다면 **약 200~300초 지연**. netkeiba(55초)보다 **3~5배 느리다**. | |
| F-④ | ⇒ 안: `_fresh_private` 에 **"확장 값이 netkeiba 보다 낡으면 양보하지 않는다"** 조건 추가. ⚠ 단 확장은 자기 값의 나이를 안 보내므로 **서버가 추정**해야 한다(수신 간격으로). 근거 부족하면 **안만 내고 적용 안 한다**. | |

### 작업 2 — 확정배당 분모

| # | 예상 | 실제 |
|---|---|---|
| P-① | 🟢 netkeiba `result.html` 은 **과거분이 영구 보존**될 것이다(수 년치). ⇒ 소급 백필 **가능**. | |
| P-② | 경마(sport=horse) 343건 중 확정배당 22건 → 백필하면 **대부분 채워질 것**. 다만 **중앙(JRA)만** 된다 — 지방(NAR)은 race_id 체계가 다르다. | |
| P-③ | 🔴 **지방경마(NAR)는 netkeiba `db.netkeiba.com` 쪽에 있을 것**이나 race_id 체계·URL 이 달라 별도 조사가 필요하다. 오늘은 **가능 여부만** 본다. | |
| P-④ | 우리 경마 343건 중 **대부분이 지방(NAR)** 일 것이다(중앙은 15건뿐이었다). ⇒ 중앙만 백필해도 **분모는 크게 안 는다**. | |

### 작업 4 — O6 재측정 안

| # | 예상 | 실제 |
|---|---|---|
| O-① | 안: netkeiba 를 **게이트와 무관하게 항상 fetch 하되 저장은 관찰 전용 파일**에 한다(판정·추천 미사용). ⇒ 이중 기록 없이 지연 측정 가능. | |
| O-② | 비용: 요청이 2배가 되나 실측 0.08초라 **무시 가능**. | |

---

## 세션 2026-08-01 (15차) — 두수 대비 추천개수 · 중경7R 검증 · 마감직전 지연

### 작업 1 — 두수 대비 추천 개수

| # | 예상 | 실제 |
|---|---|---|
| Q-① | `max_q` 는 **두수와 무관한 고정값**일 것(2~3). 종목별로 갈렸다면 경륜만 따로 있을 가능성. **두수 반영은 없을 것**으로 본다. | |
| Q-② | 경마 두수별 추천 개수는 **두수가 늘어도 거의 안 늘 것**(평평). 16두나 8두나 2~3개. | |
| Q-③ | 🔴 **경마 리플레이는 판정 불가일 것.** 어제까지 경마 확정배당이 **0.9%** 였다. 백필은 오늘 중앙 4건뿐이라 분모가 **한 자릿수**일 것. ⇒ K별 비교는 **표본 부족**으로 못 낸다. | |
| Q-④ | 안: **조합 수의 N%** 보다 **두수 구간별 계단식**(예: ~9두 2 / 10~13두 3 / 14두+ 4)이 현실적. 다만 ③을 못 재면 **근거 없는 안**이 되므로 그 사실을 명시한다. | |

### 작업 2 — 중경 7R 검증

| # | 예상 | 실제 |
|---|---|---|
| C-① | 파서가 착순 `3-11-5` 와 払戻를 정확히 뽑을 것. | |
| C-② | 우리 추천 `6+10` · `6+7` 은 top2(3,11)와 안 맞으므로 **hit=False** 로 기록될 것. | |
| C-③ | ⚠ 다만 `hit` 은 **결과 입력 학습 경로**(`_apply_result_learning`)가 채운다. 백필/자동수집은 `result` 만 채우므로 **`hit` 이 비어 있을 수 있다** — 그러면 그 사실을 보고한다. | |

### 작업 3 — 마감 직전 지연

| # | 예상 | 실제 |
|---|---|---|
| D-① | "13:15 現在"가 마감 14초 전에 보였다면 **약 5분 지연**이다. O6(≤90초)를 **크게 벗어난다**. | |
| D-② | 🔴 원인 후보: ⓐ T-3 단축(25초)이 **안 걸렸다** ⓑ 걸렸으나 netkeiba `official_datetime` 자체가 5분 전이었다 ⓒ 화면이 캐시를 그렸다. | |
| D-③ | 내 판단으로는 **ⓑ가 가장 유력**하다 — netkeiba 갱신은 55초 주기지만 **경주 직전에 오히려 갱신이 멈추는 구간**이 있을 수 있다(발매 마감 처리). 실측으로 확인한다. | |

---

## 세션 2026-08-01 (14차) — 枠連 분리 재검증 · 백필 apply · 성적측정 · ③배선

### 추가 검증 — 枠連 ≠ 馬連 인 경주

| # | 예상 | 실제 |
|---|---|---|
| V-① | 8두 이상 경주에서는 **枠(8틀)과 마번이 대부분 다르다** → 두 조합이 **다르게** 나올 것. | |
| V-② | 파서는 `馬連` 만 `quinella` 에 넣고 `枠連` 은 `bracket` 으로 분리하므로 **금액이 서로 섞이지 않을 것**. | |
| V-③ | 🔴 만약 섞였다면 백필 **중단**한다. | |

### 백필 --apply 후 확인

| # | 예상 | 실제 |
|---|---|---|
| B-① | 저장 **4경주**. 백업 디렉터리 생성됨. | |
| B-② | 🔴 삿포로 7R(미확정)에는 **아무것도 안 쓰임**. | |
| B-③ | I1(확정배당 보유율)은 **오를 것**. 중앙 4건이 전부 `payouts.quinella` 를 갖고 들어오므로 분자·분모가 같이 늘어 **비율은 오히려 개선**될 가능성이 높다(지방은 확정배당 결손이 많았다). | |
| B-④ | 지방·경륜 payouts **무영향**(파일 단위로 중앙만 건드린다). | |

### 작업 1 — 첫 성적 측정

| # | 예상 | 실제 |
|---|---|---|
| R-① | 🔴 **n=4 → 판정 불가.** 숫자만 기록한다. | |
| R-② | `measure_recovery.py` 가 `--sport` 로 갈라주지만 **중앙/지방 구분은 `sport=horse` 로 같다** → `category` 로 못 가를 수 있다. 그러면 **그 사실을 보고**하고 분모를 명시한다. | |

### 작업 2 — ③ app.py 배선

| # | 예상 | 실제 |
|---|---|---|
| W-① | 🔴 **sleep-first 금지**(지시). 기동 직후 **바로 1회 돌고** 그 다음 쉰다 — 재기동이 잦으면 sleep-first 는 그때마다 수집 공백을 만든다. | |
| W-② | 주기 **300초**(5분) → **I3 대상**이라 스탬프 파일이 필요하다. 기존 데몬과 같은 방식으로 남긴다. | |
| W-③ | 기존 결과 백필 데몬(`_keirin_result_backfill_once`·`_official_result_audit`)과 **대상이 겹치지 않는다**(그쪽은 지방·경륜). 충돌 없을 것. | |

---

## 세션 2026-08-01 (13차) — 스키마 손대조 · ③④ 배선·백필 · 첫 성적 측정

### 🔴 백필 전 스키마 매핑 손 대조

| # | 예상 | 실제 |
|---|---|---|
| M-① | 우리 `payouts.quinella` 는 **馬連**(2두·순서무관)과 같을 것. 우리 '복승' 추천이 `"5+7"` 형태이고 馬連 払戻가 `"5 7"` 이므로 **조합이 일치**할 것. | |
| M-② | 🔴 **가장 위험한 착오 후보**: 일본 `複勝`(1~3착 안에 들면 적중·1두)과 우리 '복승'이 **이름만 같고 다른 것**이다. 우리 '복승'은 **2두 조합**이므로 `馬連` 이 맞다. 이걸 `複勝` 에 매핑하면 **회수율이 통째로 틀리는데 값은 들어와 조용히 틀린다.** | |
| M-③ | 우리 '삼복승' = `3連複`(3두 순서무관). `3連単`(순서있음)과 혼동하면 배당이 **4~5배 과대**로 잡힌다(실측 4,180 vs 20,550). | |
| M-④ | 손 대조 결과 **일치할 것**으로 본다. 다만 ②③ 중 하나라도 어긋나면 **백필 중단**한다. | |

### ③④ 배선·백필

| # | 예상 | 실제 |
|---|---|---|
| W-① | 오늘 중앙경마 중 **끝난 경주**만 백필 대상. 우리가 배당을 수집한 것은 12:05 이후 발주분이므로 **삿포로 5R~** 이다. 13시대 기준 **3~6경주**. | |
| W-② | 🔴 미확정 경주에 잘못 저장되지 않는지 — `finished` 플래그로 막는다. 착순 0행이면 **아무것도 안 쓴다**. | |
| W-③ | I1(확정배당 보유율)은 **당일 결과확정 경주** 분모다. 중앙이 들어오면 분모·분자 둘 다 늘어 **비율은 크게 안 변할 수도** 있다. | |
| W-④ | 기존 지방·경륜 payouts 경로 **무영향**(입력만 추가). | |

### 작업 1 — 첫 성적 측정

| # | 예상 | 실제 |
|---|---|---|
| S-① | `tools/measure_recovery.py` 가 **있을 것**(지시에 이름이 나왔다). 없으면 즉석 코드 금지 지시에 걸리므로 **그 사실을 보고**한다. | |
| S-② | 🔴 **n 이 매우 작을 것**(3~6경주) → **판정 불가**로 표시해야 한다. | |
| S-③ | 관찰 모드라 중앙 추천은 회원에게 안 나갔다 — **"추천이 나갔다면" 가정 측정**임을 명시해야 한다. | |

---

## 세션 2026-08-01 (12차) — 착순·확정배당 배선(①② 먼저) · netkeiba 추가소스 · 과거이력

### 작업 1-① 실물 확인

| # | 예상 | 실제 |
|---|---|---|
| 1-① | **로그인 불필요**할 것(오즈 API도 불필요했다). HTTP 200 · 서버렌더 HTML. | |
| 1-② | 들어있을 것: **착순(전체)** · 마번/마명/기수/조교사 · 타임 · 착차 · **통과순위** · **상3F** · **마체중(증감)** · 단승/인기 · **払戻(단승·복승·와이드·마단·삼복승·삼연단)**. | |
| 1-③ | 🔴 **払戻에 천 단위 콤마**가 있을 것(오즈 확정배당에서 이미 확인된 특성). | |
| 1-④ | 결과 미확정 경주는 **빈 테이블** 또는 출마표로 폴백될 것 → 분기 필요. | |

### 작업 1-② 파서

| # | 예상 | 실제 |
|---|---|---|
| 2-① | 착순은 `Result_Num`/`ResultTableWrap` 계열 클래스, 払戻는 `Payout_Detail_Table` 계열일 것. | |
| 2-② | 우리 스키마 매핑: 払戻 **馬連 → `payouts.quinella`** · **三連複 → `payouts.trio`** · 単勝 → win. ⚠ 우리 "복승"은 **馬連**이다(1·2착 순서무관). | |

### 작업 2 — netkeiba 추가 소스 (목록만)

| # | 예상 | 실제 |
|---|---|---|
| 3-① | 발주 **전**에 나오는 것(=예측에 쓸 수 있는 것): 조교(`training`) · 혈통 · 기수/조교사 성적 · **인기 추이(오즈 시계열)** · 馬場상태. | |
| 3-② | 🔴 "시장이 안 보는 것" 1순위는 **조교(追い切り)** 라고 본다 — 배당에 늦게 반영된다. 2순위 **마체중 증감**(당일 발표·직전 반영). | |
| 3-③ | 마체중은 **발주 직전**에만 나온다(당일). 페이스·통과순위는 **결과 후**라 예측 불가·복기용. | |

### 작업 3 — 왜 예전엔 배당이 안 됐나

| # | 예상 | 실제 |
|---|---|---|
| 4-① | 🔴 **`action=update` 누락이 원인이 아닐 것이다.** 아예 **시도한 기록이 없을 것**으로 본다. `app.py:27182` 주석이 *"JRA 공식은 POST세션이라 스크래핑 난해 → netkeiba 馬柱"* 라고만 적혀 있고, **배당은 검토 대상에서 빠졌다**. | |
| 4-② | 교훈: **빈 응답을 "불가능"으로 결론내지 않는다.** 오늘도 `action` 없이 부른 빈 응답을 하마터면 "발주 전엔 못 가져온다"로 닫을 뻔했다. | |

---

## 세션 2026-08-01 (11차) — 마감임박 주기 단축 · 관찰해제 조건 확정 · 복승탭 조사

### 작업 0 — 마감 임박 수집 주기 단축

| # | 예상 | 실제 |
|---|---|---|
| A-① | T-3 이내 20~30초로 줄이면 **요청 수는 2~3배**가 되지만, 그 구간에 드는 경주가 **최대 1~2개**라 사이클 소요는 여전히 **1초 미만**일 것. | |
| A-② | 🔴 **실제 지연 감소폭은 작을 것이다.** netkeiba 자체가 55초 주기라 **하한 ~55초**다. 60초→25초로 줄여도 평균 지연은 **약 85초 → 약 68초** 정도(수집 대기 30초→12.5초 감소분만). | |
| A-③ | 별도 스레드라 지방·경륜 영향 **0**일 것. | |

### 작업 1 — 관찰 해제 조건 (숫자 확정)

| # | 예상 | 실제 |
|---|---|---|
| O-① | 급락 발동률: 중앙이 지방의 **130% 이내**. 넘으면 임계 종목별 분리. | |
| O-② | 커버리지 **90%** 이상. | |
| O-③ | deadline 취득률 **95%** 이상. | |
| O-④ | 최소 관찰 **50경주**(2개 개최일). | |
| O-⑤ | 이중 기록 0건 **2개 개최일 연속**. | |
| O-⑥ | 🔴 마감 직전 지연 **90초 이내**. ⚠ 소스 하한이 55초라 이보다 낮게 잡으면 **영원히 못 넘는 판정선**이 된다. | |

### 작업 2 — 확장 복승 탭

| # | 예상 | 실제 |
|---|---|---|
| E-① | `content.js` 는 사설 배당판(asyukk/ks1)에서 **탭 버튼 라벨 텍스트**로 복승 탭을 찾을 것. 라벨 목록에 중앙 표기가 빠졌을 가능성. | |
| E-② | 🔴 **지방에서는 되고 중앙에서만 안 될 것이다** — 지방 private 유입은 오늘도 정상이다(중경 2R 등). ⇒ 같은 사이트인데 **중앙 페이지 DOM 만 다르다**는 뜻. | |
| E-③ | 🔴 **커버리지 3.4% 의 진짜 원인일 수 있다.** "사람이 안 열어서"가 아니라 **열어도 못 긁어서**. 진짜 중앙 15건의 유입 시점이 **특정 날짜 이후 끊겼다면** 그게 증거다. | |

### 작업 3 — 오늘 집계

| # | 예상 | 실제 |
|---|---|---|
| T-① | 오늘 JRA 36경주 중 netkeiba 수집은 **배선 시각(12:05) 이후 발주분만** — 삿포로 5R부터 마지막 니가타 12R(18:30)까지 **약 24경주**. 커버리지 **3.4% → 60~70%** 로 오를 것. | |
| T-② | deadline 취득률 **100%**(shutuba 방식). | |

---

## 세션 2026-08-01 (10차) — 이중기록 게이트 실동작 시험 · 관찰해제 조건 · 결과 배선안

### 작업 1 — 이중 기록 게이트 실동작

| # | 예상 | 실제 |
|---|---|---|
| G-① | 확장이 중앙 배당판을 열면 `source=사설URL` 로 `triple_store` 에 기록되고, 히스토리 `src` 는 `private` 가 된다. | |
| G-② | 🔴 **게이트는 먹을 것이다.** `_jra_recent_private` 는 `triple_store` 의 `source` 를 보고 `netkeiba`·`oddspark` 가 아니면서 200초 이내면 생략한다. ⇒ `"N경주 생략(확장 우선)"` 이 찍힐 것. | |
| G-③ | 창을 닫으면 확장 기록이 멈추고 **200초 뒤** netkeiba 가 재개될 것. | |
| G-④ | 같은 경주에 두 소스가 **동시에** 저장되는 일은 없을 것(게이트가 앞단에서 막으므로). 다만 **전환 순간 1틱** 겹칠 수는 있다. | |
| ⚠ G-⑤ | 🔴 **위험 예상**: 확장이 보내는 raceKey 와 서버가 만드는 raceKey(`"삿포로 6경주"`)가 **다르면 게이트가 무력**해진다. 확장은 경기장명을 다르게 뽑을 수 있다(예: `札幌`·`삿포로`). 이게 이번 시험의 진짜 관전 포인트다. | |

### 작업 2 — 관찰 모드 해제 조건 (숫자로 확정)

| # | 예상 | 실제 |
|---|---|---|
| O-① | 급락 발동률은 **중앙/지방 비율 1.5배**를 넘으면 임계 조정이 필요하다고 볼 것. | |
| O-② | 커버리지 **90%** 이상(지방 수준). | |
| O-③ | 이중 기록 **0건 · 2개 개최일 연속**. | |
| O-④ | 최소 관찰 경주 **50경주**(하루 36경주이므로 2개 개최일). | |
| O-⑤ | deadline 취득 성공률 **95%** 이상. | |

### 작업 3 — 확정배당·착순 배선안

| # | 예상 | 실제 |
|---|---|---|
| P-① | `race/result.html` 은 경주 종료 후 **수 분 내** 갱신될 것. 착순은 `Result_Num`, 払戻는 `Payout` 계열 테이블. | |
| P-② | 호출 시점은 **마감 + 5/10/20분 3회 재시도**(기존 결과 자동수집과 같은 리듬). | |
| P-③ | 기존 `payouts` 저장 경로(`_build_race_result`·`payouts_raw`)에 합류시킨다. **천 단위 콤마 제거 필수.** | |

---

## 세션 2026-08-01 (9차) — netkeiba 배선 구현(관찰 모드) · 확정배당 · 종료 절차

### 구현 전 확인 4가지

| # | 예상 | 실제 |
|---|---|---|
| ⓐ | 조합키는 **`"0102"` = 1번-2번**(2자리 zero-pad 2개). 삼복승은 **`"010203"`**(2자리 3개)일 것. 값은 `["배당","0.0","인기순"]` 배열. | |
| ⓑ | 발주시각은 `race_list_sub.html` 안에 있다(오늘 파싱해 봤을 때 `HH:MM` 이 붙어 있었다). ⚠ 다만 **race_id 와의 연결이 정규식 근접 매칭**이라 불안정할 수 있다 → 별도 확인 필요. | |
| ⓒ | 두수는 **단승(type=1) 키 개수**로 알 수 있으나 우리는 type=1 을 안 받는다. ⇒ **복승 조합키에서 등장하는 마번의 최대값**으로 유도한다. | |
| ⓓ | 관찰만: `source="netkeiba"` 로 저장하고 **추천 반영은 막는다**. ⚠ `_do_triple_ingest` 는 저장과 분석이 붙어 있어, "저장하되 추천 제외"를 하려면 **분석 결과를 쓰는 쪽**에서 걸러야 할 것이다. | |

### 확정배당(추가 2)

| # | 예상 | 실제 |
|---|---|---|
| R-① | 경주 종료 후 같은 API(`action` 없이 또는 `action=update`)가 `status:"result"` 로 **확정배당**을 준다. 실측으로 이미 봤다(삿포로 1R 10:06). | |
| R-② | 🔴 **착순은 이 API 에 없을 것이다.** 오즈 API 는 배당만 준다. 착순은 별도 경로(`race/result.html` 또는 결과 API)가 필요할 것이다. | |
| R-③ | 착순이 별도라면 **기존 결과 자동수집 경로**(`_official_result_audit`·`results_auto`)에 붙이는 것이 맞다. 이번 세션에는 **확정배당까지만** 하고 착순은 확인만 한다. | |

### 신호 임계(추가 1)

| # | 예상 | 실제 |
|---|---|---|
| S-① | 🔴 **중앙에서 급락 발동률이 높아질 것이다.** 55초 간격은 30초의 1.83배 구간이라 한 틱당 변화폭이 크다. `-30%` 임계를 그대로 쓰면 과다 발동한다. | |
| S-② | ⚠ **지금은 바꾸지 않는다**(지시). 관찰 데이터가 쌓인 뒤 종목별로 비교한다. | |

### 작업 2 — 서버 종료 절차

| # | 예상 | 실제 |
|---|---|---|
| K-① | `tools/kill_safe.py` — `netstat` 으로 **8011 LISTEN PID 를 먼저 구하고**, 종료 대상에서 **무조건 제외**한다. 사람 절차는 잊으므로 스크립트로 만든다. | |
| K-② | 오늘 사망 3회: ① 07:03:20~07:49:31(46분·app.py 중간상태) ② 07:49:52~07:51:14(1분22초·app.py 중간상태) ③ 11:39:48~11:40:28(40초·**내가 Stop-Process**). 데이터 결손은 **전부 0**. | |

---

## 세션 2026-08-01 (8차) — netkeiba 갱신 주기 실측 · 부하 설계 · 배선 설계안

### 작업 1 — 배당 갱신 주기 (배선 가치 판정)

| # | 예상 | 실제 |
|---|---|---|
| 1-① | 🔴 **JRA 실시간 오즈는 마감 임박에 약 1분 간격**, 여유 시각엔 **5분 간격**으로 갱신될 것이다. ⇒ **30초 간격으로 부르면 절반 이상이 중복**(`official_datetime` 동일)이다. | |
| 1-② | ⚠ 그래도 **틱 수 자체는 충분할 것이다**. 마감 10분 전~마감까지 1분 간격이면 **약 10틱** — 지방(30초 간격 20틱)의 절반이지만 BMED 급락 판정(직전 대비 %)에는 **부족하지 않다**고 본다. | |
| 1-③ | 🔴 **여유 시각(T-30 이상)에는 거의 안 변할 것**이다. 그 구간을 30초로 긁는 것은 낭비다 ⇒ **수집 창을 마감 임박으로 좁혀야** 한다. | |
| 1-④ | 판정 예상: **통과**. 다만 "30초 간격"이 아니라 **"60초 간격 + 마감 임박 단축"** 이 맞는 설계일 것이다. | |
| 1-⑤ | 지방(oddspark) 비교: 같은 시간대 지방이 **틱이 더 많을 것**(우리가 30초로 긁으므로). | |

### 작업 2 — 부하

| # | 예상 | 실제 |
|---|---|---|
| 2-① | 🟢 **요청 자체는 매우 가볍다** — 실측 0.07~0.08초. `type=4,7` 만 받으면 경주당 2요청. | |
| 2-② | 🔴 **36경주를 한꺼번에 긁을 이유가 없다.** 마감 10분 창에 드는 경주는 동시에 **3~6개**(3트랙 × 각 1~2경주) → 경주당 2요청 = **6~12요청/사이클 ≈ 1초 미만**. | |
| 2-③ | ⚠ 그래도 **기존 사이클에 얹지 말 것**을 권고할 것이다. 7/30 에 3트랙 동시 개최로 사이클이 **37초까지 포화**된 선례가 있다 — **별도 사이클 분리**가 안전하다. | |
| 2-④ | oddspark 는 실측 **9.71초** 걸렸다(중앙 확인 호출). netkeiba 는 0.08초로 **100배 이상 빠르다** — 부하 걱정은 netkeiba 쪽이 아니다. | |

### 작업 3 — 배선 설계안

| # | 예상 | 실제 |
|---|---|---|
| 3-① | 🔴 **이중 기록 위험이 진짜 위험이다.** 확장이 사설 배당판으로 같은 중앙 경주를 보내면 **두 경로가 같은 raceKey 에 저장** → 7/30 oddspark 2중 기록(4,507건) 재발. **소스 우선순위 게이트가 필수**다. | |
| 3-② | 기존 `_do_triple_ingest(..., source=...)` 에 **입력만 추가**하면 되고 저장·동결·판정 경로는 **손대지 않는다**. | |
| 3-③ | 실패 격리는 경륜 `_keirin_autocollect_form` 패턴(try/except·배당 흐름 무영향)을 그대로 쓴다. | |

---

## 세션 2026-08-01 (7차) — 호후 오분류 파급 · A/B/C 적용 · 중앙경마 서비스 자료

### 작업 1 — 호후 오분류가 종목별 집계를 망쳤나

| # | 예상 | 실제 |
|---|---|---|
| 1-① | `_multi_sport_of` 는 **`sp=="cycle" or cat=="cycle"` 를 가장 먼저** 본다(app.py:30813). ⇒ 호후 건의 `sport` 가 `cycle` 이었다면 **`category` 가 틀려도 경륜으로 집계**된다. 반대로 `sport=horse` 였다면 중앙으로 샌다. **`sport` 값이 관건**이다. | |
| 1-② | 🔴 어제 잰 숫자(리플레이 분모 경륜 495/499·경마 266/284, I1 종목별)는 **`sport` 기준**일 가능성이 높다. `category` 기준이면 영향, `sport` 기준이면 무영향. **영향 있어도 소규모(20~30건)** 로 본다. | |
| 1-③ | 경륜장이 `japan_central` 로 저장된 건 전 기간 **5~30건** 예상(호후 외에 와카야마·기후 등 경륜 전용 지명이 더 있을 수 있다). | |
| 1-④ | 반대 방향(중앙경마가 `japan_local`) 은 **0~수건**. 확장 토글이 '지방'에 있을 때 JRA 를 열면 발생 가능. | |

### 작업 2 — A → B → C

| # | 예상 | 실제 |
|---|---|---|
| 2-A① | `_JRA_TRACKS` 현재 12개 항목에 **한자(東京·中山·阪神·京都·中京·福島·新潟·小倉·札幌·函館)가 하나도 없다.** 한글만 있다. | |
| 2-A② | 보강 후 기존 데이터 검증 — **실제 중앙 건이 전부 걸릴 것**. 07-25·07-26·08-01 의 고쿠라·니가타·삿포로·중경이 대상. | |
| 2-B | 가드는 `_KEIRIN_ONLY_RE` guard 와 같은 자리(ingest 종목 정정 블록)에 넣는다. 정정 시 **`print` 로 로그를 남긴다**. | |
| 2-C① | `--dry` 규모: 07-30·07-31 **25건** + 과거분 → 총 **40~120건** 예상. | |
| 2-C② | 🔴 정정하면 **중앙 탭에서 보이던 지방 경주가 사라진다.** 회원 화면이 바뀐다 — 규모 보고 후 승인받고 `--apply`. | |

### 작업 1-B (추가 지시) — 중앙 배당을 서버가 직접 못 가져오는 이유

| # | 예상 | 실제 |
|---|---|---|
| B-① | 🔴 확장은 **사설 배당판**(`asyukk34.qwqwd25.net` / `ks1.dke-d11diw.site`)에서 긁을 것이다. `manifest.json host_permissions` 에 **`jra.go.jp` 가 없다** — `_isJRACentral` 이 `location.href` 로 `jra.go.jp` 를 보긴 하지만 content script 가 그 도메인에 **주입되지 않는다.** ⇒ 실제 소스는 사설 배당판이다. | |
| B-② | 🔴 서버 직접 호출은 **사설 배당판 세션/쿠키 때문에 막힐 가능성**이 가장 높다고 본다. 다만 7/30 `Accept` 헤더 선례가 있으니 **DNS→TCP→TLS→헤더** 로 분리해 시험한다. 결과는 셋 중 하나: ⓐ 헤더만으로 풀린다 ⓑ 쿠키·세션 필요 ⓒ 도메인 자체가 죽어 있다. | |
| B-③ | 🔴 **oddspark 에는 중앙경마 배당이 없을 것이다.** oddspark 는 지방경마(NAR)·경륜·경정 전용이다. 코드의 `_keiba_schedule` 도 NAR 트랙코드 기반이다. ⇒ "있는데 안 긁는" 게 아니라 **정말 없다**. | |
| B-④ | 다른 소스: **JRA 공식(jra.go.jp)** 은 실시간 오즈를 JS/POST 로 내보내 파싱이 무겁다. **netkeiba** 는 오즈 API 가 있으나 비공식이다. 둘 다 **현재 코드에 배선 0**일 것. | |

### 작업 3 — 중앙경마 서비스 여부 (자료만)

| # | 예상 | 실제 |
|---|---|---|
| 3-① | 🔴 **커버리지가 매우 낮을 것이다.** JRA 는 토·일 개최(하루 3장 × 12경주 ≈ 36경주). 최근 30일 개최일 8~10일 × 36 ≈ **300경주** 중 수집은 **30~60건 = 10~20%** 로 예상. | |
| 3-② | 중앙경마 카카오 발송 이력은 **거의 없거나 0건**일 것. | |
| 3-③ | 서버 자동수집 소스: **oddspark 는 JRA 를 안 담는다**(지방·경륜 전용). 코드에 JRA 자동수집 경로가 **없을 것**. netkeiba·JRA 공식은 현재 미배선. | |
| 3-④ | ⇒ **"사람이 열어두는 운영"은 현실적이지 않다.** 토·일 09:50~16:30 을 계속 포그라운드로 두어야 한다. 결론은 **ⓐ 서버 수집 소스를 새로 배선하거나 ⓑ 중앙경마를 서비스 대상에서 제외**하거나 둘 중 하나일 것. | |

### 작업 4 — 날짜 선택기 기본값

| # | 예상 | 실제 |
|---|---|---|
| 4-① | `app.js:126` 만 고치면 된다. 서버 무변경. "오늘 데이터 있으면 오늘" 판정은 `/api/day/races?date=오늘` 을 한 번 불러 `count>0` 으로 본다. | |
| 4-② | ⚠ 기존 동작(사용자가 고른 날짜 유지)은 건드리지 않는다 — **초기 기본값만** 바꾼다. | |

---

## 세션 2026-08-01 (6차) — 중앙경마 수집 여부 · category 오분류 · 화면 표시

### 작업 1 — 오늘 중앙경마 수집

| # | 예상 | 실제 |
|---|---|---|
| 1-① | 🔴 **오늘 스케줄에 중앙경마가 아예 없을 것이다.** | 🟢 **적중**. 12곳 전부 경륜(10곳)+지방(오비히로·코치). **중앙 0곳.** |
| 1-② | ⇒ **스케줄 소스 자체가 중앙경마를 안 담는다.** | 🟢 적중. |
| 1-③ | 🔴 **중앙경마 수집 경로는 지방과 다르다**(확장 수집) ⇒ 사람이 배당판을 열어야 들어온다. | 🟢 적중. `content.js` 가 `category=japan_central` 로 `/api/odds/triple/ingest` 에 보낸다. 서버 자동수집 경로 없음. |
| 1-④ | 오늘 중앙경마 `analysis_log`·`odds_history` **0건**일 것. | 🔴 **빗나감 — 수집되고 있다.** `삿포로 2경주`(10:27·틱5) · `중경 2경주`(10:07·틱4). 다만 **1경주는 없다.** |
| 1-⑤ | 어제·그제도 중앙경마는 거의 없을 것 ⇒ 계속 그랬을 것. | 🟡 **절반**. 07-30·07-31 중앙 **0건**이나 그건 **비개최일**(목·금)이라 정상. 07-25·07-26(금·토)엔 고쿠라·니가타·삿포로 **14~15건 정상 수집**. |
| 1-⑥ | 2026-08-01 이 JRA 개최일인지 서버가 모를 수 있다. | 🟢 **적중**. 스케줄에 없어 **개최 여부를 서버가 모른다**. 확장이 보내야만 존재를 안다. |

### 작업 2 — category 오분류

| # | 예상 | 실제 |
|---|---|---|
| 2-① | 🔴 **원인 1순위: `category` 상속.** `app.py:2394` 부근 `category = (category or prev.get("category") or {...}.get(sport, "japan_local"))` — **`sport` 는 전환 시 상속을 금지**해 놨는데 **`category` 는 `prev` 에서 상속**된다. 앞 경주가 `japan_central` 이었으면 다음 경주가 지방이어도 그대로 붙는다. | |
| 2-② | 원인 2순위: 확장이 중앙 배당판에서 보낸 값이 그대로 저장(소스 기준 판정). | 🔴 **이게 진짜 원인**. `content.js:1023` `isCentral = (japanType === 'central') OR _isJRACentral(rk)` — **팝업 토글이 경기장명 검사를 OR 로 이겨버린다.** 서버는 `body.get("category")` 를 **검증 없이 신뢰**(`app.py:1968`). |
| 2-③ | 🔴 **경기장명으로 판정하지 않을 것이다.** 판정한다면 소노다가 `japan_central` 이 될 수 없다. ⇒ 실제 중앙 10곳(도쿄·나카야마·한신·교토·주쿄·후쿠시마·니가타·고쿠라·삿포로·하코다테)과 대조하면 **저장된 `japan_central` 대부분이 오분류**일 것. | |
| 2-④ | 중앙경마 탭은 **`category` 로 거를 것**(경기장명이 아니라). 그래서 화면에 카와사키·소노다·나고야·몬베츠 같은 **지방이 중앙 탭에 뜬다**. | |
| 2-⑤ | 수정안: **경기장명 화이트리스트로 `category` 를 확정**(경륜 `_KEIRIN_ONLY_RE` 와 같은 방식). ⚠ 소급 정정은 별도 도구(`fix_keirin_sport_tag.py` 선례). **착수는 승인 후.** | |

### 작업 3 — 화면에 오늘이 없다

| # | 예상 | 실제 |
|---|---|---|
| 3-① | 오늘 **지방·경륜 데이터는 있다**(08:20:05부터 틱 213개 실측). ⇒ "데이터가 아예 없어서"는 아니다. | |
| 3-② | 🔴 화면 목록이 **결과가 확정된 날짜만** 내려오거나, **결과 입력분 기준**이라 진행 중인 오늘이 안 뜰 것으로 본다. | |
| 3-③ | 🟢 **어제 스냅샷 날짜 수정의 영향은 아닐 것이다.** `day_races` 카드는 `race_results` 로 만들고 `snapshot` 은 **부가 필드**다. `_day_snapshot_for` 가 `None` 을 줘도 **카드 자체는 그대로 나온다**(이미지만 빈다). | |
| 3-④ | ⇒ 원인은 **오늘 `race_results` 가 아직 0건**(결과 미확정)일 가능성이 가장 높다. | |

---

## 세션 2026-08-01 (5차) — 훅 설치·전수확인 · 스냅샷 판정 중단 · 서버 사망 절차

### 작업 1 — 커밋 훅 설치 + 검증 + **"호출자 없음" 전수 확인**

| # | 예상 | 실제 |
|---|---|---|
| 1-① | 훅 설치 후 **위반 코드를 넣고 `git commit` 하면 실제로 막힌다**(rc≠0·커밋 미생성). | 🟢 **적중**. glob 위반 주입 → `🔴 커밋 차단` · **HEAD 불변**으로 확인. |
| 1-② | `--no-verify` 는 **훅이 아예 안 불리므로 훅 안에서는 못 잡는다.** ⇒ `post-commit` 으로 남겨야 한다. | 🟢 **적중**. `pre-commit` 이 표식을 남기고 `post-commit` 이 표식 부재로 우회를 판별하도록 구현. 실측: `08284803  BYPASS(--no-verify)` 기록됨. |
| 1-③ | 🔴 **전수 확인에서 같은 유형이 더 나온다.** 예상 2~5곳. | 🔴 **예상보다 훨씬 많다.** 테스트만 **11/19가 고아**다. |
| 1-④ | 가장 유력한 것은 **작업 스케줄러 등록 여부**다. | 🟢 **적중**. `register_daily_backup.bat` 가 등록해야 할 작업이 **스케줄러에 없다**(경마 관련 작업 0건). |
| 🔴 1-⑤ | *(예상 못 함)* | 🔴🔴 **`run_selfcheck.py` 자체가 고아다.** 원칙 17의 핵심 도구인데 **아무도 안 부른다** — 게이트와 **똑같은 유형**이다. |

### 작업 2 — 스냅샷 판정 경로 중단 (끄면 무엇이 같이 멈추나)

| # | 예상 | 실제 |
|---|---|---|
| 2-① | 같이 멈추는 것 목록(예상): ⓐ `_snapshot_build_compare` 의 **compare 파일 생성** ⓑ `_snapshot_timing_learn` 의 `snapshot_timing.json` **누적** ⓒ `/api/snapshot/compare` 의 **즉석 산출**(저장분 읽기는 유지) ⓓ `/api/snapshot/timing` 이 보여줄 **새 값**(기존 파일 읽기는 유지) ⓔ 결과확정 훅(`_apply_result_learning`)의 재집계 ⓕ `snapshot_save` 의 `trigger=="close"` 자동 산출. | |
| 2-② | **멈추지 않는 것**: 스냅샷 **저장 자체**(`/api/snapshot/save`)·이미지 조회(`/api/snapshot/get`)·갤러리 목록(`/api/snapshot/list`)·경주기록 카드 이미지(`_day_snapshot_for`). ⚠ 대표가 "스냅샷 포기"라 했지만 **찍히는 것까지 끄라는 뜻은 아니라고 본다**(끊으면 되돌릴 수 없다 — 미드나이트 때와 같은 논리). **판정 계산만** 끊는다. | |
| 2-③ | 되살리는 방법은 **플래그 한 개**(`SNAPSHOT_JUDGE_ENABLED=False`)로 만든다. 코드를 지우지 않는다. | |
| 2-④ | 끈 뒤 `run_glob_safety` 는 **여전히 rc=0**(호출을 끊어도 함수는 남으므로 KNOWN 2건 그대로). | |

### 작업 3 — 격리만 실행 · A/D안 취소

| # | 예상 | 실제 |
|---|---|---|
| 3-① | 격리 8개만 이동. 정상 106개 이관·129쌍 재생성은 **실행 안 함** → 도구에 `--quarantine-only` 를 **추가**(기존 동작 무삭제). | |
| 3-② | `I6` 를 체크리스트에서 빼면 **완료조건 23은 그대로**(I6 는 무결성이라 분모에 없었다). 무결성 항목 수만 5→4로 줄어든다. | |
| 3-③ | `/api/snapshot/health` 는 남긴다(호출 안 하면 비용 0). 체크리스트 미포함. | |

### 작업 4 — 서버 사망 구간 데이터 결손

| # | 예상 | 실제 |
|---|---|---|
| 4-① | 사망 시각 **07:45 전후 · 07:52 전후** 두 번, 각 **수 분**. | 🔴 **빗나감(1차가 훨씬 길다)**. 1차 마지막 요청 **07:03:20** → 재기동 **07:49:31** = **약 46분**. 2차는 **07:49:52 → 07:51:14 = 약 1분 22초**. |
| 4-② | 🔴 **데이터 결손 0건일 것이다.** 그 시각 오늘 첫 발주가 **08:30** 이라 수집 대상 경주가 **없었다**. | 🟢 **적중**. 오늘 첫 수집 틱이 **08:20:05**(다운타임 이후). 08:20:05~09:09:44 사이 **5분 초과 공백 0건**. |
| 4-③ | 다만 **40시간 연속 가동 기록은 깨졌다**. | 🟢 적중. |
| 4-④ | 절차: 편집 직후 **생존 확인** 필수화 등. | 🟢 적용(아래 절차). |
| ⚠ 4-⑤ | *(예상 못 함)* | ⚠ **로그로 완전히 재구성하지 못했다** — 07:03~07:49 구간에 내가 성공적으로 호출한 API 기록이 어느 로그에도 없다. 서버가 두 벌 떠 있었을 가능성(중복 기동)이 남는다. **미해결로 남긴다.** |

### 작업 1 — 게이트가 실제로 막는가

| # | 예상 | 실제 |
|---|---|---|
| 1-① | 🔴 **게이트는 안 막는다.** `run_precommit` 자체는 `rc=1` 을 내지만 **git pre-commit 훅이 없어서** `git commit` 이 그걸 부르지 않는다. 즉 **수동으로 실행해야만 보이는 경고**다. 3차 세션에서 나는 커밋 **전에** 실행하지 않았다 — 그래서 `47877a0e` 가 그냥 됐다. | |
| 1-② | 차단 목록 `[2] 기존 테스트 🔴 차단` 에 `run_glob_safety` 는 **들어 있다**(3차 세션 출력에서 `❌ tests/run_glob_safety.py` 로 그 그룹에 찍혔다). 문제는 목록이 아니라 **호출자가 없다**는 것. | |
| 1-③ | `.git/hooks` 에 `pre-commit` 파일 **없음**(3차 세션에서 이미 확인). 샘플만 있다. | |
| 1-④ | 제안: **pre-commit 훅 설치**(파일 추가·기존 무삭제). ⚠ 단 훅은 되돌리기 어렵고 `--no-verify` 로 우회 가능하므로, **승인 후** 설치하고 우회 시 경고를 남기게 한다. | 🟢 제안대로. **승인 대기.** |
| 🔴 1-⑤ | *(예상 못 함)* | 🔴 **서버가 두 번 죽었다.** `app.py` 를 편집하는 **도중**(두 번에 나눠 고치는 사이) Flask 리로더가 **중간 상태 파일**을 읽어 `SyntaxError` 로 종료했고, 그 뒤 **스스로 안 올라온다**. 07:45·07:52 두 차례. 복구함. |

### 작업 2 — `_snapshot_metas_for_race` 날짜 분리

| # | 예상 | 실제 |
|---|---|---|
| 2-① | ⓐ `_snapshot_metas_for_race(rk, date=None)` 로 날짜 인자 추가 · 날짜 불일치 파일은 버린다. ⓑ 저장 파일명을 `<YYYY-MM-DD>_<raceKey>.json` 으로 바꾼다. **둘 다 안 고치면 다시 섞인다.** | |
| 2-② | 호출부는 3곳(`_snapshot_build_compare`·결과확정 훅 17946·`/api/snapshot/compare`). 앞 둘은 날짜를 구할 수 있고, **API 는 raceKey 만 받아서** 날짜를 못 구한다 → `?date=` 를 **추가**하되 없으면 **최신 날짜**로 처리(기존 호출 무파손). | |
| 2-③ | 오염 8건 처리: **삭제하지 않고** `data/snapshot_compare/_quarantine/` 로 **이동(격리)**. | |
| 2-④ | **재생성 가능하다.** 원본 스냅샷 PNG+메타 434장이 그대로 있고 파일명에 날짜가 100% 있으므로, 날짜별로 다시 만들 수 있다. 다만 **7/31 이후 스냅샷이 0장**이라 그 이후 경주는 재생성 대상 자체가 없다. | |
| 2-⑤ | 수정 후 `run_glob_safety` **rc=0**(신규 위반 0 · KNOWN 2건 잔존)으로 돌아온다. | |
| 2-⑥ | 재생성하면 compare 파일 수가 114개보다 **늘어난다**(같은 raceKey 의 여러 날짜가 이제 각각 파일을 갖는다). 대략 **130~150개** 예상. | |

### 작업 3 — 스냅샷 A안 + D안 (서버 전용·읽기 전용)

| # | 예상 | 실제 |
|---|---|---|
| 3-① | 🔴 **"연속 N경주 0장"은 오탐이 많을 것이다.** 스냅샷은 오버레이가 떠 있는 경주에만 찍히므로 **평소에도 대부분의 경주가 0장**이다(7/30 실측: 카드 108건 중 스냅샷 26장 = 24%). 연속 0장은 정상 상태에서도 흔하다. | |
| 3-② | ⇒ 판정 단위를 **경주 연속**이 아니라 **"당일 전체"** 로 잡아야 한다: *발주완료 경주가 N건 이상인데 **당일 스냅샷 총 0장**이면 경보.* 이건 7/31·8/1 을 정확히 잡고 7/30 은 안 잡는다. | |
| 3-③ | N 제안: **10경주**. 하루 발주완료가 100~130건이므로 10이면 오전 중에 걸리고, 개최가 극히 적은 날의 오탐은 막는다. | |
| 3-④ | D안 `/api/snapshot/health` 는 읽기 전용이라 위험 0. 사유 후보 3개(오버레이 OFF · timerDeadline 고착 · 확장 미실행)를 나열. | |

### 작업 4 — 오버레이 상태 확인(대표 화면)

| # | 예상 | 실제 |
|---|---|---|
| 4-① | 콘솔 한 줄로 `overlayEnabled`·`timerDeadline`·`deadlineSource`·`autoDeadlineRaceKey` 를 한 번에 볼 수 있다. `chrome.storage.local.get(...)` 형태. ⚠ **배당판 탭에서** 실행해야 한다(확장 컨텍스트). | |
| 4-② | 오버레이가 꺼져 있었다면 **켜는 즉시 재개**된다(코드 변경 0). 켜져 있었다면 `timerDeadline` 이 과거 값으로 굳어 있을 것이다. | |

### 작업 5 — 오버레이 조건 (대표 확인: 확장 실행 중·수집 정상·경주 1시간 50분 전이라 오버레이 미표시)

| # | 예상 | 실제 |
|---|---|---|
| 5-① | 오버레이는 **배당판 페이지에 content script 가 붙고 분석 데이터가 있을 때** 뜬다. 발주 임박과 무관하게 뜰 것으로 보나, 패널 렌더 자체에 조건이 걸려 있을 수 있다. | |
| 5-② | 🔴 **핵심 예상**: `chrome.tabs.captureVisibleTab` 은 **현재 창의 활성 탭**만 찍는다. ⇒ ⓐ 브라우저가 켜져 있어야 하고 ⓑ 배당판 탭이 열려 있어야 하고 ⓒ **그 탭이 활성(포그라운드)이어야** 제대로 찍힌다. **백그라운드 탭은 못 찍는다.** | |
| 5-③ | 🔴🔴 **더 나쁜 예상**: 트리거는 백그라운드 탭의 content script 에서도 돈다. 그러면 `captureVisibleTab` 이 **다른 탭 화면을 찍어서** 그 경주 파일명으로 저장할 수 있다 — 즉 **잘못된 이미지가 맞는 이름으로** 저장되는 경로가 존재할 것이다. | |
| 5-④ | ⇒ **버그가 아니라 설계 한계**다. "사람이 그 화면을 열어두고 보고 있어야 찍힌다." 대책이 다르다(서버가 못 만든다 → 감지·안내가 최선). | |
| 5-⑤ | ③ 7/30 18:25 중단은 **이것으로 설명된다**: 그 시각 이후 대표가 배당판을 포그라운드로 두지 않았을 것. 마지막 파일이 `T-10` 1장뿐이고 T-2·마감후가 없는 것이 그 증거(찍는 도중 탭을 떠남). | |
| 5-⑥ | 🔴 7/30 패턴 예상: **같은 시각대에 한 경기장만** 찍힌다(동시 개최여도). 경기장이 시간대별로 **번갈아** 나타날 것이다 — 대표가 탭을 옮겨 다닌 흔적. | |
| 5-⑦ | ④ 여러 경기장 동시 개최 시 **열어둔(활성) 것만** 찍힌다. 전부는 불가능하다. | |
| 5-⑧ | 오늘 첫 경주 시각: 서버 스케줄에서 뽑을 수 있다. 경륜이 이른 시간(오전 8~9시대)일 것으로 예상. | |

---

## 세션 2026-08-01 (3차) — 스냅샷 0장 · KNOWN 등급분리 · 승인③ 적용

### 작업 1 — 스냅샷이 7/31부터 0장

| # | 예상 | 실제 |
|---|---|---|
| 1-① | 스냅샷은 **Chrome 확장이 화면을 캡처해 서버로 POST**(`/api/snapshot/save` 계열)하는 구조일 것. 서버가 스스로 그리지 않는다. | 🟢 **적중**. `overlay.js captureBoardSnapshot` → `background.js SAVE_BOARD_SNAPSHOT` → `POST /api/snapshot/save`(app.py:3111). 서버는 받아 저장만 한다. |
| 1-② | 트리거는 **마감 임박(T-10/T-2/마감1분전)** 자동 캡처. 파일명 `YYYY_MM_DD_경기장_N경주_트리거.png` + 짝 `.json`. | 🟢 **적중**. `overlay.js:1547~1560` T-10(≤10분·>2분)·T-2(≤2분·>0)·close(0~-35초), raceKey당 단계별 1회. |
| 1-③ | 🔴 원인 1순위: **확장이 안 보내고 있다**(확장 미실행·탭 미접속·자동전송 OFF). | 🟡 **절반**. 확장은 **살아 있었다** — 7/31 `private` 배당 유입 **218건**(7/30 은 98건). 확장 전체가 죽은 것이 아니라 **스냅샷 트리거만** 죽었다. |
| 1-④ | 원인 2순위: 저장부가 **조용히 실패**해 로그가 안 남는다. | 🟢 **적중(형태는 다름)**. 서버 로그에 스냅샷 오류 **0건** — 실패한 게 아니라 **요청 자체가 안 왔다**. 서버는 안 온 요청을 알 방법이 없다. |
| 1-⑤ | **어제 우리 수정의 영향은 없을 것.** | 🟢 **적중**. 우리는 **읽기 경로**만 고쳤고, 중단 시점(7/30 18:25)이 그보다 **이틀 앞선다**. |
| 1-⑥ | 오늘(8/1) 생성분도 **0장**일 것. | 🟢 **적중**. 8/1 0장 · 7/31 0장. |
| 1-⑦ | 7/30→7/31 사이 스냅샷 관련 커밋 **0건**일 것. | 🟢 **적중**. `chrome-extension/` 마지막 커밋은 **7/23**. 서버 스냅샷 경로도 무변경. |
| 🔴 1-⑧ | *(예상 못 함)* | 🔴 **중단 시점이 7/31이 아니라 7/30 18:25:32 이다.** 마지막 파일 `2026_07_30_카와사키_8경주_T-10.png` — T-2·마감후가 없는 **미완 1장**. |
| 🔴 1-⑨ | *(예상 못 함)* | 🔴 트리거가 **확장 자체 `timerDeadline`**(배당판 카운트다운 감지값)에만 의존한다. **서버가 아는 발주시각(`deadline_epoch`, 7/31 130/130 = 100% 보유)을 오버레이는 안 쓴다.** 이 값이 0이거나 옛 경주 값으로 굳으면 3단계가 **전부 조용히** 건너뛴다. |

### 작업 2 — KNOWN 3건 등급 분리

| # | 예상 | 실제 |
|---|---|---|
| 2-① | `_snapshot_metas_for_race` = **표시**(비교분석 화면). 판정 무관. | 🔴 **빗나감(정반대)**. `_snapshot_build_compare` 가 이 메타로 **변화량을 계산해 저장**하고 `snapshot_timing.json`(T-10 vs T-2 적중률 학습)에 누적된다. **판정 계열이다.** |
| 2-② | `_gemini_latest_map` = **표시**. 판정 무관. | 🟢 **적중**. 카드·목록 뱃지 전용. |
| 2-③ | 🔴 `_rr_load_by_key` = **판정 계열**. | 🟡 **절반**. 판정 코퍼스(고배당 리뷰)에 닿긴 하나 **실오염 0건**. |
| 2-④ | 실제 오염 규모는 **작을 것**(0~수건). | 🟢 `_rr_load_by_key` 는 **0건**. 🔴 그러나 `_snapshot_metas_for_race` 는 **8건**. |
| 2-⑤ | `race_results` 는 날짜 포함 키가 많아 위험 한정적일 것. | 🟡 **절반**. 날짜 포함은 **52.9%(994/1,880)** 뿐이고, **349종이 2개 이상 파일에 매칭**된다 — 위험은 크다. 다만 실호출 경로가 막혀 있어 발현이 없었다. |
| 2-⑥ | `_rr_load_by_key` 만 KNOWN 에서 빼고 즉시 차단. 나머지 2건 유지. | 🔴 **빗나감**. 뺀 것은 **`_snapshot_metas_for_race`** 다. `_rr_load_by_key` 는 KNOWN 유지(B급). |
| 2-⑦ | ⚠ "오늘 잰 수치를 다시 봐야 하는가" → **실오염 0건이면 다시 안 봐도 된다**에 걸겠다. | 🟡 **절반**. 회수율·성적·완료조건은 **영향 없음**(다시 볼 필요 없음). 단 `snapshot_timing.json` 의 "T-10 이 더 정확(9.1% vs 7.7%)" 결론은 **오염 케이스 포함·표본 13건**이라 **무효로 본다.** |

### 작업 3 — 승인③ 완료조건 ⏳미확정 적용

| # | 예상 | 실제 |
|---|---|---|
| 3-① | 7개에 표식만 붙이면 되고, **분모 23은 그대로** 유지된다. | 🟢 **적중**. `total: 23` 불변. |
| 3-② | done 수는 거의 안 변할 것. 미측정 18 중 일부가 재분류될 뿐. | 🟢 **적중**. `1/23` 유지 · 미측정 18 → **⏳미확정 5 + 미측정 13**. |
| 3-③ | 🔴 D2a·D2b 는 목표가 '0건'이라 성격이 반대다 — 표식 규칙을 따로 봐야 할 수 있다. | 🟢 **적중이자 자동 해결**. `ok is None` 일 때만 ⏳를 붙이는 규칙이라, 이미 잰 D2a(92)·D2b(546)에는 표식이 **안 붙는다**. 그래서 목록은 7개지만 **현재 ⏳는 5개**다. |
### 작업 1~5 실제 결과 요약 (2026-08-01 4차)

- 1: 게이트는 **안 막는다**. run_glob_safety 는 BLOCKING_SUITES 에 들어 있고(run_precommit.py:76)
  run_precommit 자체는 rc=1 을 낸다. 그러나 `.git/hooks` 에는 **.sample 뿐**이고 core.hooksPath 도 미설정,
  run_precommit 을 부르는 자동 경로가 **0곳**이다(안내 print 2줄뿐). ⇒ 수동 체크리스트다.
- 2: compare 날짜 분리 완료. 게이트 rc=0 복귀. 격리·재생성 도구 신설(--dry 기본).
- 3: A안 I6 + D안 /api/snapshot/health 구현. N=10 제안.
- 5: 스냅샷은 **포그라운드 탭만** 찍힌다(captureVisibleTab). 7/30 실측으로 동시 캡처 9건 확인.

### 6차 실제 결과 — 나머지 항목

- 2-①: 🟡 절반. 상속 코드는 실재하나 `prev` 는 **같은 raceKey** 직전 기록이라 교차 오염원은 아니다. 진짜 원인은 2-②.
- 2-③: 🟢 적중. 07-31 japan_central 13건 = 나고야4·소노다4·카와사키4·**호후1(경륜장!)** → 실제 중앙 **0곳**. 07-30 12건도 전부 지방.
- 2-④: 🟢 적중. `_is_central_key`(app.py:2643)·`_multi_sport_of`(:30821) 둘 다 `category=="japan_central"` **OR** 경기장명 — OR 라서 잘못된 category 하나로 중앙 탭에 뜬다.
- 2-⑤: 🟢 그대로 제출. ⚠ **`_JRA_TRACKS` 에 "중경"이 빠져 있다** — 오늘 데이터가 `중경 2경주`다. 화이트리스트부터 보강해야 한다.
- 3-①: 🟢 적중. 오늘 race_results **10건** · `/api/day/races?date=20260801` **count=10** 정상.
- 3-②: 🔴 빗나감. API 는 오늘을 정상 반환한다. 원인은 **프론트 기본값**이다.
- 3-③: 🟢 적중. 스냅샷 날짜 수정과 **무관**(카드는 race_results 기반·snapshot 은 부가 필드).
- 3-④: 🔴 빗나감. race_results 는 10건 있다. 🔴 진짜 원인 = `app.js:126` **날짜 선택기 기본값이 "어제"로 하드코딩**(`d.setDate(d.getDate() - 1)`), 결과기록(History)은 **로컬 결과 입력분**만 쌓여 오늘 미입력이면 안 뜬다.

### 7차 실제 결과 요약 (2026-08-01)

- 1-①: 🟢 적중. `_multi_sport_of` 는 cycle 을 먼저 거른다. I1·리플레이는 **`sport` 기준** → category 오분류 무영향.
- 1-②: 🟢 적중(소규모). **sport 오분류 4건**(경마 1.4%·경륜 0.8%) — 결론 불변.
- 1-③: 🔴 빗나감(훨씬 많다). japan_central 오분류 **151건**(analysis_log). 단 경륜장은 그중 **6건**.
- 1-④: 🔴 오탐 정정. 「고쿠라 20건」은 **고쿠라경륜**이라 오분류가 아니다.
- 2-A①: 🟢 적중. 한자 표기 **0개**였다 → 10개 신설 + `중경` 추가(23개).
- 2-A②: 🟢 적중. 진짜 중앙 **15건 전부** 화이트리스트 적중.
- 2-B: 🟢 적용(로그 남김).
- 2-C①: 🔴 빗나감(더 많다). `--dry` **178건**(예상 40~120).
- 2-C②: 🟢 적중. 회원 화면이 바뀐다 — 승인 대기.
- 3-①: 🟡 절반. 커버리지 **3.4%**(예상 10~20%보다 더 낮다).
- 3-②: 🟢 적중. 카카오 큐 파일 없음 · 결과 확정 **0건**.
- 3-③: 🔴 **크게 빗나감**. netkeiba 배당 API 가 **로그인 없이 200·0.08초**로 열린다.
  서버는 이미 netkeiba 로 **전적을 가져오고 있었다**(app.py:27181~). 배당만 안 쓰고 있었다.
- 3-④: 🔴 결론 뒤집힘. "사람이 열어두는 운영"이 아니라 **서버 배선이 답**이다.
- B-①: 🟢 적중. 확장은 사설 배당판(asyukk/ks1)에서 긁는다. manifest 에 jra.go.jp 없음.
- B-②: 🔴 빗나감(좋은 쪽). 쿠키·세션·헤더 **전부 불필요**. 기본 헤더로 바로 200.
- B-③: 🟢 적중. oddspark 에 중앙경마 **없다**(지명 0건).
- B-④: 🟢 적중. JRA 공식은 POST 세션이라 무겁다 · netkeiba 가 정답.
- 4-①②: 🟢 적용. ⚠ 예상 못 함 — `toISOString()` 이 UTC 라 09시 이전 하루 밀림 · async 가드 중복 진입.

### 8차 실제 결과 (2026-08-01)

- 1-①: 🟡 절반. 마감 임박은 예상대로 ~1분. 🔴 **여유 시각도 똑같이 55초 간격**이었다("여유 땐 5분"은 틀림).
- 1-②: 🟢 적중. T-10 내 **10~11틱** · 값 변화 동반.
- 1-③: 🔴 **빗나감**. 여유 시각에도 같은 주기로 변한다 — 수집 창을 좁힐 근거가 약하다.
- 1-④: 🟢 적중. **통과** · 60초 간격이 맞다(30초는 절반 중복: 26호출 중 14갱신).
- 1-⑤: 🟢 적중. 지방 T-10 내 중앙값 18틱 vs 중앙 10~11틱 = 약 60%.
- 🔴 예상 못 함: **`action=update` 없이는 발주 전 빈 응답**. 7차의 "가져올 수 있다"는 **확정배당 한정**이었다 — 정정함.
- 2-①②: 🟢 적중. 최대 동시 2경주 · 4요청 · 0.3초.
- 2-③: 🟢 적중(권고 유지). 별도 사이클 분리.
- 2-④: 🟢 적중. oddspark 9.71초 vs netkeiba 0.08초.
- 3-①②③: 🟢 설계안에 반영(_fresh_private 재사용 · 입력만 추가 · try/except 격리).
- ⚠ 운영: probe 정리 중 **서버를 같이 죽였다**(약 40초). 데이터 결손 0.

### 9차 실제 결과 (2026-08-01)

- ⓐ: 🟢 적중. 복승 4자리·삼복승 6자리·값 [배당,"",인기순].
- ⓑ: 🔴 빗나감. race_list_sub 매칭 **0/34 실패** → race/shutuba.html 의 `HH:MM発走` 사용(경주당 1회 캐시).
- ⓒ: 🟢 적중. 조합키 마번 최대값으로 12두 정확 유도.
- ⓓ: 🟢 JRA_OBSERVE_ONLY=True 적용.
- ⚠ 예상 못 함: **확정배당에 천 단위 콤마**(`"1,197.9"`). 안 지우면 float 변환이 통째로 깨진다.
- R-①: 🟢 적중(status=result 로 확정배당). 단 수집 창(-180초)이 짧아 이번엔 미포착.
- R-②: 🟢 적중. **착순은 오즈 API 에 없다**(data 키 = odds·official_datetime).
- R-③: 🟢 적중. race/result.html 에 착순+払戻 둘 다 있다 → 별도 배선.
- S-①②: 임계 미변경(지시대로). 관찰 후 판단.
- K-①②: 🟢 tools/kill_safe.py 신설 · 사망 3회 시각·원인·결손 기록.
- ⚠ 예상 못 함: 히스토리 `src` 가 **oddspark/private 2값**이라 netkeiba 가 private 로 묻혔다 → 3값으로 분리.
- 배선 후 확인: ① 삿포로5R 틱14(netkeiba 8) 복승78·삼복승286 · 배당변화 포착(5+7 6.2→4.3)
  ② 이중기록 0건(단 확장이 중앙 미전송이라 게이트 실동작 미검증) ③ 기존 사이클 영향 없음(JRA 0.3초).

### 12차 실제 결과 (2026-08-01)

- 1-①: 🟢 적중. 로그인 불필요 · HTTP 200 · 0.13~0.39초.
- 1-②: 🟢 적중. 착순 전체 12~14두 · 払戻 8종 · 마체중·통과순위·타임·착차·인기 전부 있음. (上り는 별도 클래스 Race_HaronTime)
- 1-③: 🟢 적중. 천 단위 콤마 확인(1,110円 · 2,550円 · 20,550円).
- 1-④: 🟢 적중. 미확정 = 착순 0행 · 払戻 0건으로 깨끗이 분기.
- 2-①: 🟡 절반. 착순은 HorseList(예상은 Result_Num) · 払戻는 Payout_Detail_Table **2개**로 나뉨.
- 2-②: 🟢 적중(매핑 확정).
- 🔴 예상 못 함: **`3連複`(아라비아)** 이라 한자 `三連複` 으로 찾으면 못 찾는다. 실제로 한 번 놓쳤다.
- 3-①②③: 🟢 전부 접근 가능 확인(조교·마체중·오즈시계열·馬柱 모두 발주 전 · 200 · 0.09~0.22초).
- 4-①: 🟢 **적중**. action 누락이 아니라 **배당은 아예 시도 기록이 없다**(커밋 0b9b97fa 는 전적만).
- 4-②: 교훈 기록 완료.
- ⚠ 11차 게이트 시험 결과(이번에 확인): netkeiba 12:47:12 마지막 → private 12:47:39 시작 →
  다음 사이클부터 "1경주 생략(확장 우선)" · 0.35초→0.01초. **동시 저장 0건. 게이트 실동작 검증 성공.**

### 13차 실제 결과 (2026-08-01)

- M-①: 🟢 **적중**. 삿포로6R 추천 [7,8] · 착순 1착8 2착7 · 판정식 set(combo)==top2(app.py:4745) → 적중.
  馬連 払戻 조합 "7 8" · 570円 ⇒ **조합·판정·금액 전부 일치**.
- M-②: 🟢 적중(배제 확인). 複勝 은 "8 7 4" 110/160/150円 — 1두짜리라 우리 2두 조합과 무관.
- M-③: 🟢 적중. 3連複 1,320 vs 3連単 3,510 — 혼동 시 2.7배 과대.
- 🔴 M-⑤ 예상 못 함: **枠連 이 가장 위험하다.** 이번 경주에서 조합이 "7 8" 로 馬連과 **같아 보이는데 540円 ≠ 570円**.
  枠 번호는 마번이 아니다. 파서는 bracket 으로 분리해 quinella 에 안 넣는다.
- M-④: 🟢 일치 → 백필 진행 가능.
- W-①: 🟡 절반. 대상 후보 5경주 · 실제 쓸 것 **4경주**(삿포로 2·5·6R, 중경 2R). 예상 3~6 안.
- W-②: 🟢 적중. 삿포로 7R 미확정 → **아무것도 안 씀**.
- S-①: 🟢 적중. tools/measure_recovery.py **있음**.
- ⚠ 아직 --apply 안 함(손대조 확인 요청 중).

### 14차 실제 결과 (2026-08-01)

- V-①②: 🟢 적중. 9경주 중 7건은 枠連≠馬連 조합 · 2건은 같아 보이나 **금액이 다르다**(1310/1650 · 570/540).
  전 경주 `payouts.quinella == 馬連 払戻` assert 통과.
- V-③: 해당 없음(중단 불필요).
- B-①: 🟢 적중. 4경주 저장 · 백업 backups/jra_result_20260801_131439 (4파일).
- B-②: 🟢 적중. 삿포로 7R(미확정) 1착=None · payouts={} · 백필이력 없음.
- B-③: 🟡 절반. I1 분모 30→34 · horse 4/4(100%) 신규. **비율은 100%로 불변**(지방이 이미 100%였다).
- B-④: 🟢 적중. 지방·경륜 무영향.
- R-①: 🟢 적중이자 더 나쁨. n=1(삿포로)·n=0(중경) → **판정 불가**.
- R-②: 🔴 빗나감(원인이 다름). `--pattern` 이 **정규식이 아니라 glob** 이라 `|` 가 안 먹었다.
  진짜 원인은 **odds_history T-8~T-0 스냅샷 부재** — 배선(12:05) 이전 경주는 배당 시계열이 없다.
- W-①: 🟢 run-first 로 구현(기동 20초 뒤 첫 실행).
- W-②: 🟢 적중. 300초라 I3 대상 · 🔴 **`_scan_daemons` 는 sleep-first 만 탐색**하므로
  run-first 인 이 데몬은 자동으로 안 잡힌다 → `_DAEMON_STAMPS` 에 명시 등록. I3 3/3 유지 확인.
- W-③: 🟢 적중. 기존 결과 백필 데몬과 대상 안 겹침.

### 15차 실제 결과 (2026-08-01)

- Q-①: 🔴 **빗나감**. `max_q` 는 **이미 두수별 계단**이다(app.py:11583~): 경륜3 · 경마≤9두3 · 10두4 · 11~12두4 · 13~18두6.
- Q-②: 🟡 절반. 평평하지 않다(2.21→4.20 상승). 다만 **조합 수 증가를 못 따라간다**(커버율 7.9%→3.5%).
- Q-③: 🟢 **적중**(판정 불가). 분모 전체 16 → 정제 12. 경마 확정배당 343건 중 **22건(6.4%)**.
  방향은 경륜과 같다(현행 63.8% > +1 53.6% > +2 41.9% > +3 33.5%) — **늘릴수록 나빠진다**.
- Q-④: 안 3개 제시. 🔴 권고는 **C(아무것도 바꾸지 않는다)** — 6.4% 분모로는 검증 불가.
- C-①: 🟢 적중. 착순 3-11-5 · payouts 정확.
- C-②: 🟢 적중. 판정식으로 둘 다 False.
- C-③: 🟢 **적중**. `hit` 은 `None` — 자동수집은 `result` 만 채운다. 성적은 `result` 로 재계산해야 한다(measure_recovery 가 그렇게 함).
- D-①: 🔴 **정정**. 대표가 말한 "중경 7R"은 실제 **삿포로 7R**이다(추천 6+10·6+7 · 11두 · 결과 3-11-5 일치).
- D-②: 🔴 **빗나감(원인이 다름)**. ⓐ 아니다(25초 단축 로그 확인) · ⓑ 아니다 ·
  🔴 진짜 원인은 **게이트가 netkeiba 를 막았고 화면에 확장 값이 그려진 것**이다.
  마지막 netkeiba 틱 13:16:30(T-2.0분) 이후 T-2~T-0 전부 private.
- D-③: 🔴 빗나감. netkeiba 갱신 정지가 아니라 **우리가 안 불렀다**(게이트 생략).
- ⚠ 새 발견: **확장이 켜져 있으면 O6(netkeiba 지연)를 측정할 수 없다.** O6 측정 방법의 결함 — 확장 끈 상태로 재야 한다.

### 16차 실제 결과 (2026-08-01)

- F-①: 🟢 **적중**. 스냅샷에 표시 시각 필드가 없다(t·time·src·quinella…). 확장이 "HH:MM 現在"를 안 보낸다 → 측정 불가.
- F-②: 🔴 **빗나감(전제가 틀렸다)**. netkeiba 마지막 13:16:30 = 39.4 · private 첫 13:17:03 = **39.4로 동일**.
  이후 private 이 40.1 → 40.5 로 계속 갱신. 확정 44.0 기준 private(40.5)이 netkeiba(39.4)보다 **더 가깝다**.
  ⇒ **확장 값이 낡지 않았다. 게이트 판단이 옳았다.**
- F-③: 🔴 빗나감. 실질 지연 없음 — "13:15 現在"는 **화면 표시 문구**일 뿐 데이터는 갱신되고 있었다.
- F-④: **게이트 조건 변경 안 함**(실측이 반대이므로).
- P-①: 🟢 적중. 07-05·07-19·07-25·07-26 전부 결과 페이지 존재.
- P-②: 🟢 적중(중앙만 가능한 것은 맞음).
- P-③: 🟢 **적중**. 지방은 **`nar.netkeiba.com`** 별도 도메인. HTTP 200 확인.
  ⚠ JRA 파서를 그대로 쓰면 **払戻는 정확한데 착순이 0행**(NAR 에 HorseList 클래스가 없다).
  NAR 에는 `枠単` 마권이 추가로 있다.
- P-④: 🟢 **적중이자 핵심**. 경마 765건 중 **지방 746건(97.5%)** · 중앙 19건.
  중앙만 백필해도 분모는 거의 안 는다.
- O-①②: 🟢 안 A 권고(항상 fetch·관찰 전용 저장). 구현 안 함.

### 17차 실제 결과 (2026-08-01)

- N-①: 🟢 적중. 중앙·지방 둘 다 백필 가능(16차 확인).
- N-②: 🔴 **빗나감**. `_jp_result_backfill_once` 는 **이미 있고 20분 주기로 돌고 있다**(app.py:25851 · 호출 24324).
  진짜 문제는 ⓐ **당일만 본다**(_missing_results 기본값=오늘) ⓑ 과거분은 확정배당이 안 채워진 채 남았다.
  ⚠ 07-31 주석에 이미 적혀 있었다 — "파서는 이미 있는데 결과 백필 경로에서만 빠져 있었다".
- N-③: 🔴 **빗나감(불필요)**. race_id 매핑이 장벽이 아니다 — **우리 기존 파서(`_keiba_result_payouts`)를
  과거분에 돌리면 된다**. 새 소스(nar.netkeiba)를 붙일 필요가 없다.
- N-④: 🟢 **적중이자 더 좋다**. 확정배당만 채우면 되는 것이 **650건**(착순 이미 있음).
  분모 24 → **674건**(28배). 최소 100건 기준을 크게 넘는다.
- H-①: 🟢 **적중**. 정렬(`final_q = _main_cand[:max_q]`, 배당 낮은순)이 근본. 상한만 올리면 효과 없다.
- H-②③: 🟢 적중(bmedSpecial 이 이미 ③ 설계 · _mainmax 계단이 ④ 자리).
- A-①: 🟢 **적중**. 추천 중앙 6.1배 vs 적중 중앙 10.1배(1.6배).
- A-②: 🟢 **적중이자 훨씬 큼**. 9~12두 2.1배 격차 · **13두↑ 30배 격차**(11.8 → 360.6). ⚠ n=2.
- 🔴 새 발견: **판정 경주 128건 중 70건(54.7%)에서 적중 조합을 우리가 안 샀다.**

### 18차 실제 결과 (2026-08-01)

- L-①: 🟢 적중. 우리 8두↓2.21 · 9~12두 2.5~2.6 · 13두↑3.6~4.2 vs 타사 3~4 → 8두↓·9~12두에서 적다.
- L-②: 🟢 **적중**. 축 맞고 짝 놓침 = 경마 **46.1%**(59/128) · 경륜 **43.4%**(154/355).
  둘 다 놓친 것은 12%뿐 — 유력마 판정은 나쁘지 않고 **폭이 좁다**.
- L-③: 🟢 **적중(핵심)**. 적중률은 오르나 회수율 하락(경륜 71.3→65.5→62.6 · 경마 63.8→53.6→41.9),
  **배당중앙이 안 오른다**(2.2→2.2→2.5). 배당 낮은순 정렬이라 추가분이 저배당이다.
- L-④: 🟢 **결론 도출**. 답은 폭 확대가 아니라 **정렬 변경**(17차 안 ②).
- ⚠ 내 즉석 계산 폐기: 회수율 경마 7951%·경륜 1188% — 정제 필터가 없어 이상치가 그대로 들어갔다.
  measure_recovery.py 를 쓰라는 지시가 옳았다.
- C-①: 🔴 **빗나감**. `tools/register_competitor.py` **있다** · `--img` 사진 인식 모드도 **이미 있다**.
- C-②: 🟢 적중(Vision 경로).
- C-③: 🟢 적중. 상시 수작업이라 비현실적.
- C-④: 🟢 **적중이자 실증**. 축 맞고 짝 놓침 43~46% 를 **타사 사진 없이** 우리 데이터만으로 냈다.
- E-①: 🟢 **적중**. 케이스 3건 / 483경주 = **0.6%** — 선택 편향.
- E-②: 🟢 적중. 판정 불가.
- E-③: 🟢 **적중이자 더 심함**. 신호가 경마 **96.9%** · 경륜 **99.7%** 에 붙는다 → **대조군이 없다**.

### 19차 실제 결과 (2026-08-01)

- B-①: 🟢 적중. 중앙은 확정배당 미보유 0건 — 이미 깨끗하다.
- B-②: 🔴 **빗나감(좋은 쪽)**. `keiba.go.jp` 가 **과거분을 준다**(07-15·07-31 실측).
  ⚠ `len=7724` 는 소스 없음이 아니라 **그날 그 경기장 미개최**다 — 하마터면 오독할 뻔했다.
  nar.netkeiba·race_id 매핑 **불필요**.
- B-③: 🟢 적중. 경륜은 이미 높다(I1 cycle 30/30).
- B-④: 🟢 **적중**. 채울 수 있는 것 **587경주** · 조합 불일치 **0건**.
  경마 확정배당 24 → **611건**. 최소 100건 크게 초과.
- S-①: 🟢 적중. 전부 app.py:7827 한 곳 · 1~5줄.
- S-②③: 설계 제출. ⑤(구간 균등)가 사업 원칙에 가장 부합해 보이나 **검증 전 단정 안 함**.
- T-①: 🟢 적중(-30% 현행 · 발동률 경륜 61.6% · 경마 84.5%).
- T-②: 🟢 적중. 경륜 **-40~-50%**, 경마 **-70%** 에서 적정 구간(5~30%) 진입.
- T-③: 🟡 **절반**. 경륜은 차이가 **난다**(-50%에서 52.2% vs 42.8%). 🔴 그러나 **배당중앙은 안 오른다**(2.6배).
  🔴 경마는 **역방향** — -60%에서 신호 O 33.3% vs 신호 X 45.6%. ⚠ n=39 라 단정 불가.

### 20차 실제 결과 (2026-08-01)

- R-①: 🟢 **적중**. 5번 = confTop1(확신도 1위·43.9) · score 92.9 · axis[3,5] · 신호(집중급락 -44.3%·스마트머니).
  시장 인기 **6人気** → **시장이 낮게 본 말을 우리가 1위로 봤고 실제 2착**. 우위 사례 맞다.
- R-②: 🟢 적중이자 더 나쁨. 7번을 **아예 몰랐다**(horses=2,3,4,5,6 · raceHorseCount=5, 실제 7두).
  🔴 **1번(3착)도 몰랐다.** 확장이 보낸 마번 [1~6] 13조합 → 마감직전 [1~5] 9조합. netkeiba는 21조합.
- R-③: 🟢 적중. 5+7 확정 馬連 **5,560円 = 55.6배**(예상 20~60배 안).
- R-④: 🟢 적중. 2+5 실제 22.2배 vs 확장 표시 **2.4배**.
- R-⑤: 🔴 **빗나감(더 근본적)**. 배당낮은순 16/21위인 것은 맞으나, **7번이 후보에 없어
  어떤 정렬로도 못 잡는다.** ⇒ 정렬 문제가 아니라 **입력 누락** 문제다.
  ⚠ 이 사례를 정렬 변경안의 실증으로 쓰면 **잘못된 결론**이 된다.
- X-①: 🟢 **적중**. 같은 파서다 — private↔oddspark 75건 대조 결과 **오차 중앙 24.1% · 30% 초과 44.0%**.
  최악 나고야 2R 1533.9%(최대 16674.2%).
- X-②: 🔴 **빗나감(영향이 크다)**. "제한적"이 아니라 44%가 30% 초과다. 전 종목 영향.
- X-③: 🟢 적중. 확정배당 기준(measure_recovery)은 안전 · **마감배당 근사 측정만 흔들린다**.

### 21차 실제 결과 (2026-08-01)

- G-①: 🟢 적중. app.py:31113 한 줄. `_fresh_private = False` 로 뒤집고 구 코드는 주석 보존.
- G-②: 🟢 적중. oddspark 없는 종목(한국 KRA·JRA중앙·경정·바이크)은 확장이 유일 소스 → 폴백 유지됨.
  ⚠ 이번 변경은 "oddspark 가 가져왔는데도 확장 때문에 생략하던 것"만 끈다.
- G-③: ⏳ src 분포 변화는 다음 개최일 확인 필요(오늘 남은 경주로는 표본 부족).
- N-①: 🟡 **절반**. 두수 불일치 자체는 예상보다 적다 — private↔oddspark 83쌍 중 **private 가 적음 10건**.
  🔴 그러나 **결과로 본 피해는 크다**: 943경주 중 **45건(4.8%)** 에서 "우리가 없다고 본 마번"이 실제 입상.
- N-②: 🟢 **적중**. 마감으로 갈수록 줄어드는 경주 **22건**(카와사키 7R 11두→5두 등).
- N-③: 🟢 **적중**. `max_q` 영향은 작다(경마 ≤9두는 전부 _mainmax=3).
  🔴 진짜 피해는 **조합 생성** — 7두 21조합 → 5두 10조합, 절반이 사라진다.
- N-④: 🟡 추정 유지(빈 행/열을 건너뛰는 것으로 보임). 확장 코드 미수정이라 **미확정**.
- I-①②: 🟢 무효/유효 목록 작성 완료. CLAUDE.md 최상단에 "인용 금지"로 명시.

### 22차 실제 결과 (2026-08-01)

- P-①: 🟢 **적중이자 원인 확정**. `parseMatrixTable` 이 셀을 훑는데, **취소마 판정(content.js:421)이
  "행 배당셀 0개 = 취소마"** 였다. 삼각 매트릭스의 마지막 마번 행은 원래 0개다 → 1착 7번이 지워졌다.
  ⇒ 수정: 행에도 없고 **열에도 없어야** 취소마(colHasOdds 추가).
- P-②: 🟢 **적중**. `toNum()` 이 정규식 **첫 매치**만 잡는다. `19.3`/`2.4` 중 2.4 를 넣었다.
  ⇒ `cellOdds()` 신설 — 숫자가 둘 이상이면 **큰 값**. 하나면 종전과 동일(무회귀).
- P-③: 🟢 적중. headerCount/headerNos 는 서버 무변경으로 안전.
  ⚠ `buildPayload` 가 구조분해에서 **버리고 있어** 배선까지 이었다.
- P-④: 🟢 **적중**. 검증은 **확장 재로드 + 신규 수집분**이 필요하다. ②③은 과거분을 못 되돌린다.
- V-①②③: ⏳ **미통과**(재로드 대기). V-④: ⏳ 재로드 직후 확인 가능.
- B-①: 🟢 적중. 백필은 파서와 무관하다(공식 소스·payouts 한 필드). 다만 지시한 순서는 지킨다.
- B-②: ⏳ 백필 미실행.

## 23차 예상 (2026-08-01) — `_rec_valid` 원인 · 회원 피해 · 무효측정 재실행

⚠️ **정직성 표기**: 아래 M-①~④(측정)와 B-①(백필)은 **이 항목을 적기 전에 이미 실행됐다.**
   예측이 아니라 **사후 기록**이다. 예측으로 세지 않는다.
   🔴 **진짜 예측은 아래 `Y-` 항목(작업1 잔여 검증)뿐이다.** 실행 전에 적는다.

### 사후 기록 (예측 아님 · 실행 완료분)
- M-①: 백필 587건 적용. 경마 확정배당 3.7% → 79.5%.
- M-②: `measure_recovery --sport horse` 정제분모 110경주. 현행 **86.7%**(+12.2%p).
- M-③: 두수 부족 51/298(17.1%) · 경마 46/91(50.5%) · 경륜 5/207(2.4%).
- M-④: 중경 7R 배당 55조합 완전 · `raceHorseCount=9` · 빠진 마번 **1·7**.

### 🔮 Y — 작업1 잔여 검증 (실행 **전** 기록)

- **Y-①** `_rec_valid` 축소 지점 — `valid_nos`(배당 기반)는 11두로 정상일 것이고,
  줄어드는 곳은 **`_starters_load()` 겹침 판정(app.py:11521~11543)** 일 것이다.
  ⚠ 근거: 배당 55조합이 완전한데 `horses` 만 9두다.
  🔴 **틀릴 수 있는 지점**: `_sport_max_no` 하드캡일 가능성도 있다. 그러면 11 > cap 이어야 하는데
  경마 캡은 18이라 성립하지 않는다 ⇒ **하드캡은 아닐 것**으로 본다.

- **Y-②** `raw_profile.source == "korea"` 가 원인인가 —
  🟡 **반반으로 본다.** `source` 는 **전적(form)** 출처 표기이고, `horses` 두수를 정하는 것은
  `_rec_valid`(배당) ∩ 출마표다. 한국 PDF 전적이 붙어도 **마번 집합이 겹치지 않으면
  60% 게이트에 걸려 무시**될 수 있다. ⇒ **`source=korea` 는 정황이지 확정 원인이 아닐 수 있다.**
  🔴 CLAUDE.md 에 이미 "두 번째 원인"으로 적었는데, **검증 전이라 단정이 과했다.**
  검증 결과 아니면 그 절을 정정한다.

- **Y-③** `_form_from_starters` 의 `source=="korea"` 차단 가드 —
  경륜·경정·바이크만 막고 경마는 안 막는다고 적었다. **코드로 재확인한다.**
  🟡 맞을 것으로 보나, 어제 `SPECIAL_ODDS_MIN` 들여쓰기 오독 전례가 있어 **들여쓰기까지 본다.**

- **Y-④** 기여도 분리 — **오늘은 못 가른다.** 파서 수정 후 데이터가 1경주뿐이다.
  🔴 **"전적 쪽이 주범"이라고 지금 결론내지 않는다.** 다음 개최일 신규 수집분이 필요하다.

- **Y-⑤** 회원 피해(작업2) — 두수 부족 51건 중 **빠진 말이 실제 입상한 비율은 30~50%** 로 본다.
  ⚠ 근거: 이미 잰 "raceHorseCount 보다 큰 마번이 3착 안에 든 경주 45건" 이 하한이고,
  그건 **최대 마번 초과분만** 센 값이라 1·7번 같은 중간 마번 누락은 안 세어졌다.
  🔴 다만 `measure_recovery.py` 는 **빠진 말 기준 필터가 없다** — 그대로는 못 잰다.
  ⇒ **도구에 없는 기능을 즉석 코드로 만들지 않는다.** 못 재면 "못 잰다"고 보고한다.

### 검증 4항목 — 예상
- V-①②④ **계속 미통과**로 본다. ④는 위 원인을 고치기 전에는 0이 될 수 없다.
- V-③만 통과 유지.

## 24차 예상 (2026-08-01) — JRA↔korea 전적 오염 · 불완전 틱 동결 · 지방 커버리지

### K — 작업1 (JRA에 한국 전적)
- **K-①** 폴백 경로: `_starters_load().get(rk)` 가 **날짜 없는 raceKey** 로 조회하는데,
  한국 PDF 사전분석이 같은 bare 키(`중경 6경주` 같은 숫자경주명)를 먼저 써 놓아 **덮어쓴 것**으로 본다.
  🟡 근거: `starters_store` 키에 날짜가 **0%**(어제 실측). 원칙 16 유형의 세 번째 발현.
  🔴 틀릴 수 있는 지점: `_korea_*` 저장이 raceKey를 `서울 5경주` 형태로만 쓴다면 `중경 6경주` 와 충돌하지 않는다.
  그러면 다른 경로(전적 자동수집이 source를 잘못 태그)일 것이다.
- **K-②** 점수 반영 여부 — 🔴 **반영됐을 것으로 본다(70%)**. `_form_from_starters` 가 **마번(no) 매칭**이라
  10두 한국 전적이 JRA 10두에 그대로 들어맞는다(24h 창 조사에서 이미 확인된 구조).
  ⇒ 사실이면 **중앙 11경주 추천 전부 무효**.
  ⚠ 반대 가능성: `record_score` 가 배당 기반이라 전적 없이도 산출되면 영향이 제한적일 수 있다.
- **K-③** 전 기간 규모 — **100~400건**으로 본다. 오늘 중앙 13 중 11(85%)이면 과거도 비슷할 것이다.
  ⚠ 다만 중앙 자체가 총 15~19건뿐이라 **JRA↔korea 는 수십 건**, **지방↔korea 가 더 클 것**으로 본다.
- **K-④** 가드 — `_form_from_starters` 에 이미 경륜·경정·바이크만 막는 `source=="korea"` 분기가 있다(app.py:1553 부근).
  **경마를 추가하면 1줄**로 막힌다. 🟡 다만 한국경마(KRA)는 정상적으로 korea 전적을 써야 하므로
  **`category`/경기장으로 갈라야 한다** — 단순히 sport=horse 전체를 막으면 한국경마가 죽는다.

### F — 작업2 (불완전 틱 동결)
- **F-①** 저장 시점: `_analysis_log_save` 는 **폴링마다** 저장되고, 마감 후 120분 가드로만 멈춘다.
  ⇒ "T-5 에 동결"이 아니라 **마지막 폴링이 우연히 마지막 상태**가 되는 구조로 본다.
- **F-②** 완전성 검사 — 🟡 **부분적으로 있다.** `_combo_count_dip`(직전 대비 조합 <70%)가 있으나
  그건 **급락 계산만 보류**하고 **저장은 그대로** 한다. ⇒ 동결 완전성 게이트는 **없다**로 본다.
- **F-③** 오늘 해당 경주 수 — **경마 20경주 중 5~10건**으로 본다(④ 불일치 8건과 상당 부분 겹칠 것).
- **F-④** 7번 탈락 — 🔴 **오늘 설명 못 할 가능성이 높다.** 45조합 스냅샷에 7번이 있으므로
  `valid_nos` 단계가 아니라 **그 사이 틱(저장 안 된 중간 상태)** 이 원인일 수 있고, 그건 기록이 없다.

### C — 작업3 (지방 커버리지)
- **C-①** 17경주 미수집은 **오전 시간대에 몰려 있을 것**으로 본다(코치·오비히로 1~6R).
  근거: 경마 로그 20건 중 지방 7건이 전부 14:35 이후다.
- **C-②** 🔴 **오늘만이 아닐 것**으로 본다. 어제까지 경마 확정배당이 3.7%였던 것과 같은 뿌리일 수 있다.
- **C-③** 경륜은 94 중 **50~60건**으로 본다(오늘 51건 확인됨 = 54%).
  ⇒ 경륜도 절반이다. **지방경마만의 문제가 아닐 수 있다.**

### 24차 실제 결과 (2026-08-01)

- **K-①**: 🔴 **빗나감.** 날짜 매칭 문제가 **아니다.** 실제 원인은 `_is_japan_key` 가 쓰는
  `_JP_TRACKS` **30곳이 전부 지방(NAR)** 이고 **JRA 중앙 10곳이 하나도 없다**는 것.
  거기에 표기 불일치까지 겹쳤다 — 목록은 `가와사키`·`고치` 인데 실제 키는 **`카와사키`·`코치`**.
  ⇒ 경기장 **화이트리스트 누락 + 표준키 문제**. (원칙 16 아님)
- **K-②**: 🟢 **적중.** `_form_from_starters` 의 `source=="korea"` 차단은 **경륜·경정·바이크만**(app.py:1564).
  경마는 1590 `prescored` 분기로 **그대로 점수화**된다 → `form` → `_integrated_grades` → `keyHorses`.
  🔴 **판정 오염 확정.**
- **K-③**: 🟡 절반. **31건**(예상 100~400). ⚠ `raw_profile` 배선이 7/30 이라 **3일치만 측정 가능**하다.
  가장 많은 곳은 예상과 달리 **카와사키 17건(지방)** 이었다 — JRA 전용 문제가 아니다.
- **K-④**: 🟢 적중. `_JP_TRACKS` 보강 1곳으로 신규 유입이 막힌다. 한국경마 분리 필요성도 맞았다.
- **F-①**: 🟢 적중. **폴링마다 저장**이고 "T-5 동결"이 아니다. 마감 후 120분 가드뿐.
- **F-②**: 🟢 적중. 완전성 게이트 **없다**. `_combo_count_dip` 은 급락 계산만 보류하고 저장은 그대로.
- **F-③**: 🔴 **빗나감(좋은 쪽).** 오늘 **1건**뿐(중경 7R). 예상 5~10 은 과대였다.
- **F-④**: 🟢 적중(설명 못 함). 다만 새 사실 — **중경 7R 은 틱이 3개뿐**(중앙 평균 20)이라
  애초에 수집 자체가 거의 안 된 경주다.
- **C-①②③**: 🔴 **전부 무효 — 내 분모가 틀렸다.**
  "커버리지 29%"는 **하루 전체 스케줄(24)** 을 분모로 쓴 값이고, **발주완료 기준으로는 5/5 = 100%** 다.
  🔴 **분모 오류 일곱 번째.** 원칙 8-C 를 만든 뒤에도 또 냈다.

## 25차 예상 (2026-08-01) — 가드 A·B·C · 오염 표기 · 표준키/커버리지 설계 · 완전성 표기

### G — 가드 A·B·C (실행 전 기록)
- **G-①** `_JP_TRACKS` 는 **`_is_japan_key` 한 곳에서만** 쓰인다(실측 확인). 확장해도
  다른 경로에 영향이 없을 것이다. 🟢 위험 낮음.
- **G-②** A만 넣으면 **오늘 남은 경주는 이미 막힌다**(코치·카와사키·JRA 전부 목록에 들어가므로).
  🔴 그러나 **B가 최종 방어**다 — 목록에 없는 새 표기(예: `추쿄`)가 나오면 A는 뚫린다.
  실제로 오염 집계에 **`추쿄` 1건**이 따로 잡혔다 = 그 시나리오가 이미 일어났다.
- **G-③** C의 거부 로그는 **오늘 안에 실제로 찍힐 것**으로 본다(한국 PDF 세션이 열려 있으면).
  🟡 반대 가능성: 오늘 한국경마 개최가 없어 `pollKoreaOdds` 자체가 안 돌면 **0건**일 수 있다.
  ⚠ 0건이어도 "가드가 안 걸렸다"가 아니라 "호출이 없었다"이다 — **구분해서 보고한다.**
- **G-④** 한국경마 회귀 위험: 서울·부산·부경·제주 키는 `_KRA_TRACK_RE` 로 갈라내므로
  🟢 **영향 0**으로 본다. ⚠ 다만 `과천`(서울 별칭) 같은 표기가 빠지면 한국경마가 죽는다 — **목록을 코드에서 재사용**한다.

### D — 오염 표기
- **D-①** 표기 대상 **31건** 그대로일 것. ⚠ 7/29 이전은 `raw_profile` 이 없어 **측정 불가**를 명시한다.
- **D-②** 표기 후에도 `measure_recovery.py` 결과는 **안 바뀐다**(분모에서 빼지 않으므로). 🟢

### S — 작업1 표준키 설계
- **S-①** 이미 `_TRACK_GROUPS`(55키·197항목)와 `_track_norm` 이 있다. **새로 만들지 않고 그것을 정규화 모듈로 승격**하는 것이 맞다.
  ⚠ 7/31 실측에서 `_track_norm` 이 오늘 개최 13곳 중 **12곳을 이미 커버**했다.
- **S-②** 안 쓰는 곳이 **5~10곳**일 것으로 본다(`_JP_TRACKS`·`_JRA_TRACKS`·`_KEIRIN_ONLY_RE`·`_JP_BABA_CODE`·`WEATHER_VENUES`).

### C2 — 작업2 커버리지 도구
- **C2-①** 분모는 **`postEpoch <= now`(발주완료)** 로 강제한다. 스케줄 전체를 분모로 못 쓰게 **코드에서 차단**한다.
- **C2-②** 오늘 값 재현: 지방 5/5 · 경륜 51/49 · 중앙(스케줄 없음)은 **분모 없음으로 표기**될 것이다.

### I — 작업3 완전성 표기
- **I-①** `incomplete` 발동률은 **5~15%** 로 본다. ⚠ 원칙 18 — 0%나 100%면 감시 항목으로 부적합하다.
- **I-②** 🔴 **중경 7R은 "동결 문제"가 아니라 "수집 부족"일 것**으로 본다(틱 3개). 두 축을 **함께 기록**해야
  구분된다 → 표기에 `ticks` 도 같이 남긴다.

### 25차 실제 결과 (2026-08-01)

- **G-①**: 🟢 적중. `_JP_TRACKS` 는 `_is_japan_key` 한 곳에서만 쓰인다(참조 2회). 확장 위험 0.
- **G-②**: 🟢 적중. A 로 삿포로·카와사키·코치·중경·**추쿄**·니가타·주쿄·오비히로 전부 차단 확인.
  B 도 함께 넣었다 — 목록에 없는 새 표기가 나와도 막힌다.
- **G-③**: ⏳ **미확정**. C 실호출 테스트는 **4/4 거부** 확인. 다만 실사용 거부 로그는
  한국경마 개최가 없으면 안 찍힌다 — **"가드 미작동"이 아니라 "호출 없음"**으로 구분해 기록했다.
- **G-④**: 🟢 적중. 서울·부경·제주·과천 전부 `KRA=True · japan=False` — **한국경마 회귀 0**.
- **D-①**: 🟡 **31 → 33건**(오늘 13 → 15로 늘었다). 하한 표기 명시함.
- **D-②**: 🟢 적중(분모 미변경).
- **S-①**: 🟢 적중. 새로 만들지 않고 `_TRACK_GROUPS`(55키·197항목) 승격이 맞다.
- **S-②**: 🟢 적중. 미사용 목록 **6벌**(`_JP_TRACKS`·`_JRA_TRACKS`·`_KEIRIN_ONLY_RE`·`_KRA_TRACK_RE`·`_JP_BABA_CODE`·`WEATHER_*`).
- **C2-①②**: 🟢 적중. 지방 7/7=100% · 경륜 54/53=102% · 중앙 **분모 없음(산출 불가)** 로 출력.
  ⚠ 이와키타이라 150%(발주 전 조기수집) — 100% 초과가 이상이 아님을 도구가 그대로 보여준다.
- **I-①②**: ⏳ 발동률은 **다음 경주부터** 측정 가능(방금 배선). `collectTicks` 병기 반영했다.

### 25차 실검증 보강 (라이브 실호출)
- **B**: 🟢 니가타 8경주·주쿄 8경주(starters=korea) → **form 0두**. 종전이면 한국 말 10~11두가 들어갔다.
- **무회귀**: 오비히로 4경주(oddspark) 10두 · 코치 3경주 11두 · 오다와라 12경주(keirin) 9두 — 전부 정상.
- **I-①**: 🟢 표기 즉시 작동. 코치 4경주 `combos 32/45 · ticks 3 · incomplete=True`.
- ⚠ **로그 검증 실패 기록**: `logs/server_stdout.log` 가 07:03 에서 멈춰 있어 가드 로그가 0회로 보였다.
  **가드 미작동이 아니라 로그가 안 잡히는 것**이었다. 🔴 **콘솔 로그로 가드를 검증하지 않는다.**

## 26차 예상 (2026-08-01) — 로그 단절 · 게이트 고장 구분 · 오염 격리 · 표준키 ①②③

### L — 작업1 (서버 로그 07:03 단절)
- **L-①** 07:03 은 오늘 **첫 서버 사망 시각**과 정확히 겹친다. ⇒ **재기동한 프로세스가
  `> logs/server_stdout.log` 리다이렉트 없이 떴다**고 본다(내가 복구할 때 `Start-Process` 를
  리다이렉트 인자 없이 썼을 가능성). 🟡 60%
  🔴 다른 가능성: 파일 핸들은 붙어 있는데 **버퍼가 안 비워진다**(파이썬 stdout 블록 버퍼링).
  30MB 파일이라 4KB 버퍼로는 설명이 안 되지만, 리다이렉트 시 line-buffered 가 아니게 되는 것은 사실이다.
- **L-②** 그 뒤 로그는 **아무 데도 안 남았을 것**으로 본다(콘솔 창에만 출력 → 창이 없으면 소실).
- **L-③** 공백 **약 9시간**(07:03~16:40). 그 사이 `[유령마번 제외]`·`[종목 정정]`·`[JRA수집]` 등
  **모든 진단 출력이 소실**됐다.
- **L-④** 해결안: ⓐ `logging` + `RotatingFileHandler` 를 app.py 안에 넣어 **기동 방식과 무관하게** 남긴다(권고)
  ⓑ 배치파일에서 리다이렉트(현행·재기동에 취약). 🟢 ⓐ 가 맞다.
  ⚠ 다만 ⓐ 는 app.py 수정이라 **승인 대상**일 수 있다 — 감시 항목(무결성)만 먼저 넣는다.
- **L-⑤** 무결성 항목 `I6`(로그 최근 갱신) 발동률: 지금 넣으면 **즉시 빨간불**이다(9시간 공백).
  ⚠ 원칙 18 위반 아님 — **고칠 수 있는 실제 결함**이라 빨간불이 맞다.

### GG — 작업2 (게이트 고장 구분)
- **GG-①** 구분 방법: `run_precommit.py` 의 **rc 를 종류로 나눈다**(테스트 실패=1 · 게이트 자체 오류=2).
  🟡 다만 `UnicodeEncodeError` 는 파이썬이 **rc=1** 로 죽으므로 rc 만으로는 못 가른다.
  ⇒ **훅이 stdout/stderr 를 캡처해 `Traceback` 문자열이 있으면 "게이트 오류"로 판정**하는 쪽이 확실하다.
- **GG-②** 자기검증에 "훅이 죽는 상황" 추가 시 **통과할 것**으로 본다(인코딩 오류 주입 → 게이트 오류로 분류).

### P — 오염 11개 정리
- **P-①** 🔴 **격리보다 `source` 재표기가 낫다**고 본다.
  근거: `starters_store` 는 **키 하나에 한 경주**이고, 격리(다른 파일로 이동)하면 그 경주는
  **전적 없음**이 되는데 그건 이미 가드 B 가 만드는 상태와 같다. 반면 재표기는
  **원본을 보존하면서 "이 전적은 오염됐다"는 사실을 그 자리에 남긴다.**
  ⇒ `source: "korea_contaminated"` + `contaminated_at`. 가드 B 는 `=="korea"` 만 보므로 **자동으로 안 걸린다**.
  ⚠ 🔴 그런데 그러면 **가드 B 가 이 레코드를 막지 않게 된다** — `prescored` 분기로 다시 들어갈 수 있다.
  ⇒ **재표기하려면 가드 B 조건도 함께 넓혀야 한다**(`source` 가 korea 로 **시작**하면 차단).
- **P-②** 안전한 순서: 백업 → 재표기 → **가드 B 조건 확장** → 재검증(form 0두 유지).

### T — 작업3 (표준키 ①②③)
- **T-①** 누락 별칭 추가 시 **`防府/호후` · `平塚/히라츠카` · `추쿄→주쿄` · `카와사키/가와사키`** 4건이 핵심.
- **T-②** diff 대조 결과 **불일치가 나올 것**으로 본다(0건이 아닐 것). 🔴 그게 곧 구멍 목록이다.
  가장 클 것: `_KEIRIN_ONLY_RE`(경륜 전용 지명 vs 이중소속 제외 설계라 의도적 차이가 섞인다).
- **T-③** ⚠ **의도적 차이와 구멍을 갈라야 한다** — diff 를 0으로 만드는 것이 목표가 아닐 수 있다.
  이중소속(고치·나고야·카와사키)은 `_KEIRIN_ONLY_RE` 에서 **일부러 뺀 것**이다.

### W — 작업4 (완전성 발동률)
- **W-①** 지금은 표본이 1~2건이라 **판정 불가**. 하루치 후 재측정.

### 26차 실제 결과 (2026-08-01)

- **L-①**: 🟢 **적중**. 현재 서버는 **14:46 기동**(PID 24548)이고 `server_stdout.log` 로 리다이렉트되지 않았다.
  07:03 사망 때 그 파일을 쥔 프로세스가 죽었고, 이후 재기동은 **다른 파일**로 갔다.
- **L-②**: 🔴 **빗나감(좋은 쪽)**. 로그는 **소실되지 않았다** — `logs/server_b.log`(1.2MB · 16:39 갱신)로 가고 있었다.
  🔴 **가드 B 는 거기서 42회 정상 발동**했다(`[전적 오매칭 차단]`). C 도 4회.
  ⇒ 진짜 문제는 "로그가 안 남는다"가 아니라 **로그 파일이 18개로 흩어져 어느 게 현재 것인지 모른다**는 것.
- **L-③**: 🔴 빗나감. 공백 9시간이 아니라 **0시간**이다(다른 파일에 다 있었다).
- **L-④⑤**: 🟢 무결성 항목 신설 — **`I7`**(`I6` 는 뺀 번호라 재사용 금지 · I5 선례).
  특정 파일명을 고정하지 않고 `logs/server*.log` **최신 것**을 본다(재기동으로 파일이 바뀌어도 따라간다).
  현재 **0.1분 · 🟢 통과**. ⚠ 예상과 달리 즉시 빨간불이 아니었다.
- **GG-①**: 🟡 절반. rc 로는 못 가른다는 판단은 맞았고, **출력에 `Traceback` 유무**로 판별하도록 했다.
- **GG-②**: 🟢 **적중**. 자기검증 `pre-commit(고장구분)` 신설 → **7/7 통과**
  (rc=1 · "게이트 오류"로 분류 · "정당" 오분류 없음).
- **P-①②**: 🟢 **적중**. 재표기를 택했고, **가드 B 를 `startswith("korea")` 로 먼저 넓힌 뒤** 실행했다.
  도구가 그 조건을 app.py 에서 읽어 **미확장이면 실행을 거부**하도록 만들었다.
  재표기 11개 후 재검증 — 니가타 8R **form 0두 유지** · 무회귀(오비히로 10두 · 코치 11두).
- **T-①**: 🔴 **빗나감**. `호후`·`히라츠카`·`카와사키`·`코치` 는 `_TRACK_GROUPS` 에 **이미 있었다**.
  진짜 누락은 **`중경`·`주쿄`(中京)** 2건뿐이다.
- **T-②③**: 🟢 **적중**. diff 는 0 이 아니었고, **의도적 차이와 구멍을 갈라야 했다.**
  `에도가와`(경정장) · `코쿠라`(이중소속)를 사유와 함께 EXPECTED 로 분리 → 남은 실패는 **`_JRA_TRACKS` 에 `추쿄` 없음 1건**.
- **원칙 17 자기검증**: 🟢 통과. `app.py` **사본**에서 `_JRA_TRACKS` 의 `삿포로` 를 빼니 누락 1→2건.
  ⚠ 라이브 `app.py` 를 건드리지 않으려 `TRACK_KEY_APP` 환경변수 오버라이드를 넣었다(서버 3회 사망 이력).
  ⚠ 첫 시도는 **엉뚱한 곳에 주입**해 "실패"가 나왔다 — 주입 대상이 검사 축과 맞는지 먼저 봐야 한다.
- **W-①**: ⏳ 표본 1건(코치 4R `32/45 · 틱 3`). 판정 불가 — 하루치 후.

## 27차 예상 (2026-08-01) — 로그 정리 · 가드 후 관찰 · 최종 집계 · 세션 정리

### R — 작업1 (로그 18개)
- **R-①** 파일명 규칙은 **없을 것**으로 본다. `server_stdout`·`server_a`·`server_b`·`server_jra`~`jra5` 는
  **내가 그때그때 손으로 붙인 이름**이다. ⇒ **무작위(사람 임의)** 가 답일 것이다. 🟡 80%
- **R-②** 재기동이 **자동으로** 파일을 바꾸는 게 아니라, **사람이 새로 띄울 때마다 새 이름**을 준 것이다.
  🔴 즉 자동 리로더 재기동(67회)은 파일을 안 바꾼다 — **수동 기동만 바꾼다.**
- **R-③** 정리안: `logs/_archive/` 로 이동(삭제 금지). ⚠ **현재 서버가 쓰는 파일은 절대 건드리면 안 된다**
  (열린 핸들을 옮기면 로그가 끊긴다). ⇒ **최신 1개는 제외**하고, 나머지도 **오늘 것은 남긴다.**

### V — 작업2 (가드 후 관찰)
- **V-①** 16:30 이후 신규 수집분에 `raw_profile.source == "korea"` 는 **0건**일 것으로 본다.
  🔴 단 **주의**: 기존 11개는 `korea_contaminated` 로 재표기됐으므로 그 경주가 재분석되면
  `raw_profile.source` 가 **`korea_contaminated`** 로 찍힌다. **그것을 "오염 신규"로 세면 안 된다.**
- **V-②** form 두수 — 🔴 **JRA 경주는 0두가 정상**이다(가드가 막았고 대체 전적이 없다).
  "0두가 아니라 실제 두수"가 되려면 **netkeiba 馬柱 전적 배선이 그 경주에 붙어야** 하는데,
  오늘 그건 안 했다. ⇒ **중앙은 당분간 전적 없음이 맞다.** 이걸 결함으로 세면 안 된다.
  ⚠ 지방·경륜은 종전대로 정상 두수일 것이다.

### F2 — 작업3 (최종 집계)
- **F2-①** ⚠ 지금은 16:50 이고 **마지막 발주는 23:30**(미드나이트 경륜)이다.
  ⇒ **"최종" 집계는 지금 낼 수 없다.** 중간 집계로 내고 그 사실을 명시한다.
- **F2-②** 완전성 발동률 — 표본 부족으로 **판정 불가**일 것.
- **F2-③** 관찰 해제 조건 6개 — **O2(커버리지 90%)는 확실히 미달**이다(중앙은 분모조차 없다).
  O4(50경주)는 오늘 19경주라 **미달**. ⇒ **해제 불가**가 결론일 것이다.

### S2 — 작업4 (세션 정리)
- **S2-①** 오늘 발견이 많아 CLAUDE.md 최상단이 길어진다. **다음 세션 우선순위 3줄을 맨 위**에 둔다.

### 27차 실제 결과 (2026-08-01)

- **R-①②**: 🟡 **절반**. 규칙은 **있었다** — 각 파일 생성 시각 = 직전 파일 종료 시각으로 **끊김 없이 이어진다**.
  다만 이름이 시간순이 아니라(`stdout6 → jra → jra4 → jra5 → a → b`) **순서를 알 수 없다**.
  🟢 원인 예측은 적중: **수동 기동 때마다 내가 새 이름을 준 것**이고 리로더 재기동(67회)은 파일을 안 바꾼다.
- **R-③**: 🟢 적중. 아카이브 이동 · **현재 파일과 오늘 것은 제외**(열린 핸들).
- **V-①**: 🟢 **적중**. 가드 이후 **처음 수집된 5경주 신규 오염 0건**.
  🔴 예측대로 함정도 나왔다 — 갱신 시각으로 세면 15건이 오염으로 보인다(재저장분).
  **"첫 수집 시각"으로 걸러야 한다.**
- **V-②**: 🟡 절반. form 두수는 **정상(7·10·7·9·7)** 이었다 — 0두가 아니었다.
  🔴 다만 그건 **지방·경륜뿐**이다. **중앙은 16:00 에 끝나 가드 이후 신규 수집이 없어 미검증**이다.
- **F2-①**: 🟢 적중. 마지막 발주 23:30 이라 **최종 아닌 중간 집계**로 냈다.
- **F2-②**: 🟢 적중이자 더 나쁨. incomplete 2/6(33.3%) 이지만 **둘 다 수집 진행 중**(틱 1~2) —
  지금 지표는 "동결이 불완전"이 아니라 **"아직 다 안 모임"** 을 재고 있다. **발동률로 인용 금지.**
- **F2-③**: 🟢 적중. **해제 불가**. O2 분모 없음 · O4 20/50 미달 · O5 1건.
  🔴 **O1 은 예상과 반대** — 중앙 45.0% ↔ 지방 88.9%(비 50.6%). 중앙이 **덜** 발동한다.
  🟡 O3 정확히 95.0%(경계) · O6 최대 91초(1초 초과).

## 28차 예상 (2026-08-01) — 오비히로 5R "만들었다 지운 조합" · 전수 조사

### O5R — 오비히로 5경주
- **O5R-①** 삼복승 `3-5-10` 은 **중간 틱에 있었다가 마감 확정본에서 빠졌을 것**으로 본다.
  ⚠ 다만 `displayedCombos` 는 **틱별로 남지 않는다** — `analysis_log` 는 **최종 1벌**만 저장한다.
  🔴 **틱별 전수는 `recommendation_history`·`odds_timeline`·`timeline_snapshot` 에서만 복원 가능**하고,
  그것도 남아 있을 때만이다. **못 볼 가능성이 40%** 다. 못 보면 "못 봤다"고 적는다.
- **O5R-②** 취소 원인 1순위는 **EV 필터**(`_mkt2`)로 본다(50%).
  근거: 오늘까지 실측에서 EV 강등이 2.5배+ 후보의 **67.6%** 를 막았다.
  2순위 **max_t 개수 제한**(30%), 3순위 **재계산으로 축이 바뀜**(20%).
  🔴 **배당 상한(50배)일 가능성도 있다** — 3-10-5 가 고배당이면 `SPECIAL_ODDS_MAX=50.0` 에 걸린다.
- **O5R-③** 3번이 늦게 유력마에 들어왔다면, `_final_picks` 가 **`key_horses` 를 인자로 안 받으므로**
  (7/31 확정 사실) **유력마가 바뀌어도 복승 조합이 자동으로 다시 만들어지지 않는다.**
  ⇒ 🔴 **"3+10 이 안 만들어진 것"은 버그가 아니라 구조**로 본다(70%).
- **O5R-④** 동결은 **마감 직전 마지막 폴링** 상태를 얼린다(11차 세션 확인). 정답이 있던 틱이
  그 전에 있었다면 **동결이 정답을 버린 것**이 된다.
- **O5R-⑤** 확정배당 — 오비히로 5R 은 `payouts` 백필 대상이라 **있을 것**으로 본다(80%).

### X — 작업1 전수 조사
- **X-①** 🔴 **`measure_recovery.py` 로는 이 측정을 못 한다.** 그 도구는 **최종 추천**만 읽는다.
  "중간에 있다가 사라진 조합"은 **`recommendation_history` 가 필요**하다.
  ⇒ 지시대로 즉석 코드는 안 만든다. **도구에 없으면 "못 잰다"고 보고**하고,
  필요하면 **도구 확장 설계안**을 낸다(승인 후 구현).
- **X-②** `recommendation_history` 보유율이 낮을 것으로 본다(30~60%). 그게 분모 상한이다.
- **X-③** 규모 예상: 사라진 조합이 정답이었던 경주는 **전체의 3~8%**.
  ⚠ 어제 "축 맞고 짝 놓침 46.1%" 와는 **다른 것**이다 — 이건 **한 번은 잡았다가 버린 것**이다.

### Y — 작업2 (늦게 올라온 말)
- **Y-①** 조합은 **매 분석마다 새로 만든다**(캐시 아님). 다만 `_final_picks` 입력이 `curQ`(배당)이라
  **유력마 변화가 조합 생성에 직접 반영되지 않는다.**
- **Y-②** 🟢 **어제 사세보 2R 과 같은 유형**으로 본다(80%) — `key_horses` 미전달이라는 같은 뿌리.

### 28차 실제 결과 (2026-08-01)

- **O5R-①**: 🟡 절반. `displayedCombos` 는 최종 1벌뿐이라는 예상은 맞았으나,
  🟢 **`recommendation_history` 9틱이 남아 있어 전수 복원이 됐다**(못 볼 확률 40% 예상 → 실제로는 봤다).
  `3+5+10` 은 16:44:20 삼복승 보험으로 등장 → 16:45:04 에 빠짐 → 마감 확정본 **삼복승 0개**.
- **O5R-②**: 🟡 **절반 · 원인이 두 개였다.**
  · 복승 `3+10` → 🟢 **EV 필터**(`ev 0.73` · `refReason "베팅규칙 참고 강등"`) — 1순위 예상 적중.
  · 삼복승 `3+5+10` → 🔴 **예상 밖 · `trioShadow=true`(섀도우 모드)**. 지운 게 아니라 판정 명단에서 통째 제외.
  ⇒ **"만들었다가 지웠다"가 하나의 사건이 아니었다.**
- **O5R-③**: 🔴 **빗나감.** "조합이 안 만들어졌다"가 아니라 **만들어졌고 강등됐다.**
  `_final_picks` 가 `key_horses` 를 안 받는 구조는 사실이나 **이번 건의 직접 원인은 아니다.**
  ⇒ 어제 사세보 2R 을 같은 유형으로 묶은 것도 **재확인 필요**(그때 `quinellaRef` 를 안 봤다).
- **O5R-④**: 🟢 적중이자 정정. 동결(16:49:58)이 버린 게 아니라 **그 전에 이미 강등돼 있었다.**
- **O5R-⑤**: 🔴 **못 잤다.** `result = null`(17:08 미확정). **놓친 회수는 결과 확정 후에 낸다.**
- **X-①**: 🟢 **적중.** `measure_recovery.py` 는 최종 추천만 읽어 이 측정을 못 했다.
  ⇒ 즉석 코드 대신 **도구에 안 2개를 추가**했다(`현행 + 강등분` · `강등분만`).
- **X-③**: 🟡 근접. 예상 3~8% → 실제 **8.9%**(10건/112경주).
  🔴 그리고 **적중배당 중앙 7.3배**(현행 3.4배의 2.1배) — 회수율(한계 45.8%)과 방향이 반대다.
  ⚠ 상위3 제외 22.7% 로 무너진다 — **소수 고배당 의존**.
- **Y-①②**: 🔴 **빗나감**(위 O5R-③과 같은 이유).

## 29차 예상 (2026-08-01) — 코치 4R 복병×유력마 교차 · 전수 측정

### K4 — 코치 4경주 (결과 7-10-8)
- **K4-①** 🔴 **최우선**. 어제 완전성 표기가 `32/45 조합 · 틱 3 · incomplete=True` 였다.
  빠진 13조합에 `7+10` 이 들어 있을 확률 **약 30%**(13/45)로 본다.
  🔴 **입력에 없었으면 로직 논의가 통째로 무의미하다.** 이것부터 확인한다.
  ⚠ 단 `incomplete` 는 **폴링 시점 스냅샷**이라, 마지막 틱에는 45조합이 다 찼을 수 있다.
  ⇒ **최종 틱 기준으로 다시 봐야 한다.**
- **K4-②** 7번이 잡힌 신호 — `dark_quinellas`(복병) 또는 `signal_horses`(급락)로 본다.
  🟡 `bmedSpecial` 일 가능성도 있다(고배당 슬롯).
- **K4-③** 10번 `keyHorses` 순위는 **1~2위**로 본다(대표가 "유력마"라 했으므로).
- **K4-④** 🔴 **교차 경로는 없을 것**으로 본다(70%).
  근거: `_final_picks(cp, curQ, valid_nos, smart_quinella, max_q, reversal_quinellas,
  dark_quinellas, signal_horses, sig_meta, sport)` — **`key_horses` 를 안 받는다**(7/31 확정).
  ⇒ `dark`(복병)와 `keyHorses`(유력마)를 **교차시킬 재료가 애초에 한 함수 안에 없다.**
  ⚠ 다만 오비히로 5R 에서 "만들어졌는데 EV 로 잘렸다"가 나왔으므로,
  🔴 **여기도 `quinellaRef` 를 먼저 봐야 한다.** 안 보고 "미생성"이라 하면 어제와 같은 오판이다.
- **K4-⑤** 확정배당 — 코치는 지방(NAR)이라 `_jp_result_backfill_once` 대상. **있을 확률 70%**.
  ⚠ 오비히로 5R 은 아직 `result=null` 이었다. 코치 4R 도 미확정일 수 있다.

### C — 작업1 전수 측정
- **C-①** 🔴 **`measure_recovery.py` 에 `dark`(복병) 목록이 없다.** `bm`(bmedSpecial)만 있다.
  ⇒ 도구에 **`dk` 필드 + 교차 안**을 추가해야 잴 수 있다(즉석 코드 금지 → 도구 확장).
- **C-②** "복병 1 + 유력마 1" 이 1·2착이었던 경주 = 전체의 **5~12%** 로 본다.
- **C-③** 그 조합의 배당 분포는 **현행(3.4배)보다 높을 것**이다 — 복병은 인기가 낮다.
  🟡 **8~20배 중앙**으로 예상.
- **C-④** 회수율은 **100% 미만**일 것으로 본다. 어제 강등분(45.8%)과 같은 패턴 —
  🔴 **적중배당은 높고 회수율은 낮은** 형태. 상위3 제외에서 무너질 것이다.

### L2 — 작업2 (오비히로 5R 과 묶기)
- **L2-①** 공통 뿌리는 **"목록은 여러 개인데 합치는 곳이 없다"** 로 본다.
  · 오비히로 5R: 유력마 안 3두 전조합 중 하나가 **EV 로** 잘림
  · 코치 4R: 복병과 유력마가 **다른 목록**이라 교차가 안 됨
  ⇒ 🔴 **같은 뿌리가 아닐 수 있다.** 하나는 **필터**, 하나는 **생성**이다. 섞어 부르지 않는다.

### 29차 실제 결과 (2026-08-01)

- **K4-①**: 🔴 **빗나감(좋은 쪽)**. `combosSeen 45 / 45` **완전** · `incomplete` 없음.
  어제 본 `32/45` 는 **수집 진행 중 스냅샷**이었다. ⇒ **입력에 있었다. 로직 문제 확정.**
- **K4-②**: 🟢 **적중이자 더 강하다.** 7번은 `darkHorsePicks` **1순위(forced)** · `confTop1` **확신도 1위**
  · `axis[0]` **축 1순위**. **못 본 게 아니라 최고 등급으로 봤다.**
- **K4-③**: 🟢 적중. 10번은 `finalQuinellas [5,10]`·`confQuinellas [8,10] "시장유력"`.
- **K4-④**: 🟢 **적중**. `7+10` 이 `finalQuinellas`·`quinellaRef`·`bmedSpecial`·`confQuinellas`
  **네 곳 어디에도 없다** ⇒ **미생성**. 🔴 **오비히로 5R(강등)과 원인이 다르다.**
  ⚠ 예측대로 `quinellaRef` 를 먼저 확인해 오판을 피했다.
- **K4-⑤**: 🟢 적중. 확정 **복승 37.7배 · 삼복승 37.6배**.
  🔴 **예상 밖의 발견**: `finalTrifectas` 에 **`[7,8,10]` odds 37.6** 이 있다 — **정답 삼복승을 정확히 만들었다.**
  `shadow:true` 라 판정 명단에서 빠졌다. **오비히로 5R 과 같은 뿌리(이틀 연속).**
- **C-①**: 🟢 적중. 도구에 `dk`(복병) + 교차 안 2개를 추가해야 잴 수 있었다.
- **C-②**: 🔴 **빗나감(훨씬 크다)**. 예상 5~12% → 실제 **24.8%** 가 "복병1+유력마1"이 1·2착이었다.
  그중 **조합이 없던 것은 9건(8.0%)**.
- **C-③**: 🔴 **빗나감**. 교차 조합 **전체**의 적중배당 중앙은 **3.5배**(현행 3.4배와 거의 같다).
  🟢 다만 **놓친 9건**은 중앙 **21.8배 · 20~50배가 5건(56%)** 로 고배당이다.
  ⇒ 🔴 **"교차를 전부 사면 고배당이 는다"는 틀렸다. 구분해서 말해야 한다.**
- **C-④**: 🟢 적중. 한계 회수율 **52.5%** — 판정선 74.5% 미달. **전면 채택 근거 없음.**
- **L2-①**: 🟢 **적중**. 같은 뿌리가 아니었다 — **삼복승 섀도우만 공통**, 복승은 **강등 vs 미생성**으로 다르다.

## 30차 예상 (2026-08-01) — trioShadow · 놓친 9건 선별조건 · EV 강등분

### T3 — 작업1 trioShadow
- **T3-①** `trioShadow` 는 **조건**일 것으로 본다(70%). 상수 하나면 CLAUDE.md 에 남았을 것이다.
  🟡 종목별 분기 가능성 — 경륜은 삼복승 성적이 나빴다는 기록이 있다(*"경륜에서 적중 0"*).
- **T3-②** 🔴 **해제 조건이 정의돼 있지 않을 것**으로 본다(65%).
  근거: CLAUDE.md 에 *"섀도우 삼복승 50경주 도달 시 재평가(`GET /api/shadow/trifecta`)"* 라는
  메모만 있고 **숫자 판정선이 없다.** ⇒ 있으면 "50경주"가 전부일 것이고, 그건 **표본 조건이지 성적 조건이 아니다.**
- **T3-③** 섀도우 삼복승 성적 — 🔴 **회수율은 낮을 것**(50~80%)으로 본다.
  근거: 삼복승은 적중률이 낮고(2~5%) 구좌가 많다. ⚠ 다만 **적중배당 중앙은 매우 높을 것**(30~100배).
  🔴 **종목별로 갈릴 것**: 경마가 경륜보다 나을 것으로 본다(두수가 많아 배당이 크다).
- **T3-④** 해제 시 표시 개수 **경주당 +2~3개**, 구좌 **2배 이상** 증가로 본다.

### N9 — 작업2 놓친 9건
- **N9-①** 공통 조건 후보 1순위는 **`forced: true`**(강제 편입 복병).
  코치 4R 의 7번이 그랬고, `forced` 는 "신호가 강해 억지로 넣었다"는 뜻이라 변별력이 있을 것이다. 🟡 50%
  🔴 그러나 **n=9 다.** 어떤 조건을 뽑아도 **사후 최적화**가 된다.
- **N9-②** 🔴 **74.5% 를 넘는 지점이 나올 것**으로 본다 — **다만 그건 좁혀서 그런 것**이다.
  조건을 좁히면 구좌가 줄어 회수율이 쉽게 오른다. **그 자체는 근거가 아니다.**
  ⇒ **반드시 기간분할(OOS)로 확인**한다. 전반/후반 둘 다 넘어야 의미가 있다.
- **N9-③** 발동률은 **5% 미만**일 것으로 본다(원칙 18 미달 → 감시·채택 부적합).
- **N9-④** 유력마 순위 — **1~2위와의 교차**가 나을 것으로 본다.

### EV — 작업3 EV 강등분
- **EV-①** 강등분 배당 분포는 **10~30배에 몰릴 것**. EV 임계 1.0 은 환급률 75% 시장에서
  **11.4~15배·28.6배+ 만 통과**시키는 구조라(7/29 기록) 그 사이가 전부 강등된다.
- **EV-②** 임계를 **0.7 로 낮추면** 오비히로 5R `3+10`(ev 0.73)이 살아난다.
  🟡 그러나 **구좌가 크게 늘어 회수율은 떨어질 것**이다.
- **EV-③** 🔴 **상위3 제외에서 무너질 것**(어제 강등분만 22.7%). **채택 근거는 안 나올 것**으로 본다.

### 30차 실제 결과 (2026-08-01)

- **T3-①**: 🔴 **빗나감**. 조건이 아니라 **상수**다 — `BET_RULES["trioShadow"] = True`(app.py:12683),
  **종목 무관 전역**. 커밋 `fe19e9bd`(2026-07-29 · 권대표 승인).
- **T3-②**: 🟢 **적중**. 숫자 판정선이 **없다**. "50경주 도달 시 재평가"는 **표본 조건이지 성적 조건이 아니다.**
  ⇒ 이번에 `3제외 ≥ 74.5% AND n ≥ 150조합` 으로 못 박는 설계안을 냈다.
- **T3-③**: 🟢 **적중이자 결정적**. 종목이 갈렸다 —
  🟢 **경륜 3제외 77.0% (> 74.5%)** · 🔴 경마 42.9%(중앙은 1제외에서 31.5% 로 붕괴).
  ⚠ 예상(경마가 경륜보다 나을 것)은 **정반대로 틀렸다.**
  🔴 그리고 **이틀 연속 버린 정답 2건은 둘 다 경마**다 — 감각과 측정이 반대다.
- **T3-④**: 🟡 절반. 경주당 생성 삼복승 경륜 5.9 · 경마 5.4개 → **표시 3배**(예상 +2~3개보다 크다).
- **N9-①**: 🔴 **빗나감**. `forced` 는 미추천 14건 중 **1건(7%)** 뿐이다.
  🔴 **모든 지표에서 미추천 쪽이 오히려 약했다**(복병1순위 43% vs 62% · 확신도1위 36% vs 49%).
  ⇒ **"강한 복병을 놓친다"가 아니라 "약한 복병이라 안 만들어진다"** — 의도대로 동작한 결과.
- **N9-②**: 🔴 **못 냈다.** 공통 조건 자체가 없어 **거를 조건을 만들 수 없었다.**
  ⇒ 사후 최적화를 경계하라는 지시가 **실제로 필요했다** — 억지로 뽑았으면 근거 없는 규칙이 됐다.
- **N9-③**: 🟢 적중. `forced` 발동률 **2.7%** — 원칙 18(5~30%) 미달. 30건 모으려면 **약 1,100경주**.
- **EV-①**: 🔴 **빗나감(중요)**. 10~30배가 아니라 **5~20배 중배당이 79%(381/485)** 다.
  🔴 **~5배 저배당도 63조합 강등**됐고 그 구간 적중률이 **23.8% 로 가장 높다.**
  ⇒ **"EV 가 고배당을 자른다"는 그림은 틀렸다.** 자르는 것은 중배당 덩어리다.
  ⚠ 다만 "강등분 중 **적중한 것**"은 고배당(중앙 7.3배)이 맞다 — **둘을 구분해야 한다.**
- **EV-②③**: 🔴 **측정 실패.** 즉석 계산에 정제 필터를 못 넣어 회수율이 1000~2500% 로 튀었다.
  **그 값은 폐기**하고 `--ev-sweep` 도구 확장 설계로 남겼다(승인 후 구현).
  ⚠ 원칙 15("회수율은 도구로")가 **또 한 번 실증됐다.**

## 31차 예상 (2026-08-01) — 경륜 삼복승 해제 · EV 임계 스윕

### R3 — 경륜 삼복승 해제(A안)
- **R3-①** `BET_RULES["trioShadow"]` 를 **bool → 종목별 dict** 로 바꾼다.
  ⚠ 읽는 곳이 2곳(app.py:12956 `if BET_RULES.get("trioShadow")`, 14963 `an.get("trioShadow")`).
  🔴 **`an["trioShadow"]` 는 그대로 두고 값만 종목별로 정한다** — 하위 경로를 안 건드리는 쪽이 안전하다.
- **R3-②** 경마 무영향 확인 방법: 해제 후 경마 로그의 `displayedCombos.trifectas` 가 **계속 []** 여야 한다.
- **R3-③** 회원 화면 증가 — 경륜 경주당 생성 5.9개 → **상위 2개 제한**이므로 **+2개**로 본다.
  🟡 다만 생성이 2개 미만인 경주가 있어 **평균 +1.7~2.0** 일 것이다.
- **R3-④** 🔴 되돌리기: dict 를 `False` 로. **한 줄.**

### ES — EV 임계 스윕
- **ES-①** 정제 필터(괴리 0.5~2.0배)를 넣으면 지난번 2500% 가 **100~200% 대로 내려올 것**이다.
- **ES-②** 🔴 **"회수율 74.5% 유지 + 적중배당 중앙 최대" 임계가 존재할 확률은 반반**으로 본다.
  근거: 강등분 배당 분포가 **5~20배 중배당 79%** 인데 그 구간 적중률이 4~8% 로 낮다.
  ⇒ 많이 복원할수록 구좌가 늘어 회수율이 떨어지는 구조다.
  🟡 **높은 임계(0.85~0.95)에서 소수만 복원**하면 74.5% 를 지킬 수 있으나 **적중배당은 안 오를 것**이다
  (그 구간이 저배당 쪽이기 때문). ⇒ 🔴 **"둘 다 좋은 지점"은 없을 가능성이 높다(60%).**
- **ES-③** 종목별 — **경륜이 경마보다 나을 것**(경륜 복승 회수율이 원래 높다).
- **ES-④** ⚠ **없으면 "없다"고 보고한다.** 억지로 만들지 않는다.

### K4S — 코치 4R 사례 보존
- **K4S-①** 관찰 목록에만 넣는다. 조건화하지 않는다. 재검토 시점 **약 1,100경주 후**.

## 32차 예상 (2026-08-01) — 복병 3착 진입률 · 복병 포함 삼복승 · 급락 3착 기여도

### D3 — 작업1 복병 3착 진입률
- **D3-①** 무작위 기대는 10두면 30%. 복병이 신호 기반이면 **35~45%** 일 것으로 본다.
  🔴 그러나 지금까지 복승(1·2착) 기준으로는 우위가 거의 없었다(엣지 1.0 근처가 반복).
  ⇒ **3착 기준도 무작위와 큰 차이가 없을 가능성이 40%** 다.
- **D3-②** 신호별로는 **집중급락 · forced** 가 높을 것으로 본다. `anomCount` 가 클수록 높을 것.
  🟡 다만 오늘 실측에서 `anomCount` 중앙이 **0** 이었다 — 대부분 신호가 안 붙어 있다.
  ⇒ **anomCount 구간별 표본이 한쪽에 몰려 판정 불가일 수 있다.**
- **D3-③** 인기 하위인데 3착 = **고배당 삼복승 재료**. 여기가 대표 원칙과 직결된다.
- **D3-④** `darkHorsePicks` 1순위가 2·3순위보다 나을 것으로 본다(70%).

### DT — 작업2 복병 포함 삼복승
- **DT-①** 🔴 **복병 포함 쪽이 더 나쁠 가능성이 55%** 로 본다.
  근거: 복병은 인기가 낮아 3착 안에 들 확률 자체가 낮다. 배당은 크지만 적중이 희박하다.
  🟡 반대로 배당이 커서 **적중배당 중앙은 훨씬 높을 것**이다(20~50배).
- **DT-②** 판정선(3제외 74.5%)을 넘을 확률 **30%**. 넘더라도 **표본이 얇아 판정 불가**일 수 있다.
- **DT-③** 코치 4R `7+8+10` 은 7번이 복병 1순위이므로 **복병 포함 집합에 들어간다**(확실).

### DR — 작업3 급락 3착 기여도
- **DR-①** -30% 임계는 복승 기준으로 발동률 84.2%(경마)라 변별력이 없었다.
  🔴 **3착 기준에서도 발동률이 같으므로 변별력 문제는 그대로**일 것으로 본다(70%).
  ⇒ 달라지는 것은 **분자(적중률)** 뿐이다. -40%·-50% 로 올려야 대조군이 생긴다.

### 31·32차 실제 결과 (2026-08-01)

- **R3-①**: 🟡 절반. dict 로 바꿨으나 **읽는 곳도 함께 고쳐야 했다**(`BET_RULES.get()` 이 dict 를
  truthy 로 읽어 **전 종목 섀도우가 될 뻔했다**). 🔴 **중간 상태로 두면 위험한 편집**이었고 즉시 마무리했다.
- **R3-②③④**: 🟢 적중. 경마는 `an["trioShadow"]` 가 계속 True → `trifectas: []` 유지.
  표시는 읽는 쪽이 이미 `[:2]` 라 자동으로 2개. 되돌리기는 dict → `True` **한 줄**.
- **ES**: ⏸ **못 했다.** 새 지시(복병 3착)가 들어와 `--ev-sweep` 은 다음으로 넘긴다.
- **D3-①**: 🔴 **종목이 갈렸다.** 경마 전체 배수 **1.01**(무작위와 같다) · 경륜 전체 1.03.
  🟢 그러나 **경륜 복병 1순위 54.3% · 배수 1.27 · n=622** — **대표 관찰이 경륜에서는 지지된다.**
  🔴 경마는 1순위도 1.09 · **중앙은 0.88 로 밑돈다.**
- **D3-②**: 🟡 절반. `smartMoney`(경륜 1.18)·`anomCount 3~5`(경륜 1.39)는 우위가 있다.
  🔴 그러나 **`forced` 는 경마 1.00**(예상과 반대) · 경륜은 n=5.
  🟢 예측대로 **`anomCount 0` 이 경륜 2,266 / 경마 643 으로 표본이 한쪽에 몰렸다.**
- **D3-④**: 🟢 **적중**. 1순위가 2·3순위보다 낫다(경륜 1.27 vs 1.05·1.07).
- **DT-①**: 🔴 **빗나감**. 복병 포함이 미포함보다 **확실히 낫다**(경마 3제외 44.1% ↔ 29.4%).
- **DT-②**: 🟢 적중. 판정선 74.5% **미달**(경마 1순위 포함도 53.6%). **부분 해제 근거 없음.**
  🟡 다만 **적중배당 중앙 8.4배** — 회수율만으로 닫지 않고 관찰 목록에 남긴다.
- **DT-③**: 🟢 적중(코치 4R 은 복병 1순위 포함 집합).
- **DR**: ⏸ **못 했다**(작업3 급락 3착 기여도). 컨텍스트 한계로 다음 세션.

## 33차 예상 (2026-08-01) — 경륜 삼복승 해제 실동작 검증 · 급락 3착 기여도

### V3 — 해제 실동작
- **V3-①** 변경 후 처음 분석된 **경륜** 경주의 `displayedCombos.trifectas` 에 **2개**가 들어올 것.
  🟡 다만 `finalTrifectas` 가 2개 미만이면 그만큼만 — **1개인 경주도 있을 수 있다.**
- **V3-②** 같은 시각 **경마** 경주는 계속 **`[]`** 여야 한다. 🔴 하나라도 채워지면 **즉시 되돌린다.**
- **V3-③** `corePicks.trioShadow` 는 경륜에서 **키 자체가 없어야** 한다(설정 안 함) ·
  경마는 **True** 로 남아야 한다.
- **V3-④** ⚠ **판정 명단이 늘어난다** = `hit.trifecta_hit` 이 이제 경륜에서 계산된다.
  🔴 **오늘 이전 경륜 성적과 직접 비교하면 안 된다**(분모가 바뀐다). 비교 시점을 명시해야 한다.

### DR2 — 급락 3착 기여도 (32차 DR 재기술)
- **DR2-①** 급락 신호가 붙은 말의 **3착 이내 진입률**을 급락 폭별로 본다.
  🔴 복승 기준에서 -30% 발동률이 경마 84.5%·경륜 61.6% 로 **변별력이 없었다.**
  3착 기준에서도 **발동률은 같다**(같은 신호이므로). 달라지는 것은 **분자**뿐이다.
- **DR2-②** 🟡 **-40% 이상에서만 배수가 1.15 를 넘을 것**으로 본다.
  근거: 오늘 `anomCount 3~5`(경륜 1.39)가 우위를 보인 것과 같은 방향 — **신호가 강할수록 낫다.**
- **DR2-③** ⚠ 급락 폭은 `anomalies` 문자열에서 파싱해야 한다(구조화 필드가 아니다).
  🔴 **말 단위가 아니라 조합 단위 문자열**(`"급락감지: 1+8 -92%"`)이라 **말로 환원해야** 한다.
  ⇒ 한 조합의 급락을 두 말에 각각 귀속시키면 **중복 계상**이 된다. 그 한계를 명시한다.

### 33차 실제 결과 (2026-08-01)

- **V3-①②③**: 🟢 **전부 적중**. 이와키타이라 9R(cycle) `trifectas=[[1,5,6],[1,5,7]]` · `trioShadow=None` /
  오비히로 9R(horse) `trifectas=[]` · `trioShadow=True`. **경마에 새어나가지 않았다.**
- **V3-④**: 🟢 적중. 판정 명단이 늘어 `trifecta_hit` 이 경륜에서 계산되기 시작한다 —
  **오늘 이전 경륜 성적과 직접 비교 금지**를 CLAUDE.md 에 명시했다.
- **DR2-①**: 🟢 적중. 전체 배수 경마 1.01 · 경륜 1.03 — **3착 기준에서도 변별력이 없다.**
- **DR2-②**: 🔴 **빗나감(중요)**. **-40% 이상에서 좋아지지 않는다.**
  경마 **-50% 이하가 0.84 로 무작위보다 나쁘고**, 가장 좋은 구간은 **-20~-30%(1.40)** 이다.
  경륜은 **-10~-20%(1.17)·-10~0%(1.18)** 이 최고다. ⇒ **"급락이 깊을수록 좋다"는 틀렸다.**
- **DR2-③**: 🟢 적중. 조합 단위라 **말 평균으로 환원**했고 그 한계를 문서에 명시했다.
- 추가 발견: **발동률이 경륜에서 부적정**(-30% 이하 2.0%) — 깊은 임계는 경륜에서 표본이 안 쌓인다(원칙 18).

## 34차 예상 (2026-08-01) — 니가타 11R 1,040배 · 최종집계 · 경마 12.5% 원인

### N11 — 니가타 11경주 (2+11 · 1,040배)
- **N11-①** 11번은 **복병(darkHorsePicks)** 으로 들어왔을 것으로 본다(60%).
  1,040배면 인기 최하위권이라 `keyHorses`(시장·전적 상위)에 들 가능성이 낮다.
  🟡 2번은 시장 상위(유력마)일 것이다.
- **N11-②** 🔴 **EV 필터를 어떻게 통과했나가 핵심이다.**
  1,040배는 경마 배당 상한(두수별 20~35배)을 **훨씬 넘는다.** 메인으로는 못 들어온다.
  ⇒ **`bmedSpecial`(고배당 슬롯)일 가능성 50%**, **면제 토큰(시장최저·유력마1·2위)** 20%,
  **급락 신호 기반 편입** 30%.
  ⚠ 🔴 그런데 `displayedCombos.quinellas` 에 `2+11` 이 **1순위로** 있었다 —
  `bmedSpecial` 은 `displayedCombos` 에 안 들어간다(오늘 확인). ⇒ **메인이었다는 뜻**이고,
  그렇다면 **추천 시점 배당은 낮았고 마감에 폭등했을** 가능성이 크다(60%).
  🔴 **그러면 "고배당을 노려서 잡은 것"이 아니라 "저배당인 줄 알고 샀는데 폭등한 것"이 된다.**
  이 구분이 결정적이다. **추천 시점 배당을 반드시 확인한다.**
- **N11-③** 확정배당 100배+ 적중은 전 기간 **3~10건**으로 본다.
- **N11-④** 🔴 **조건화 금지.** n이 작으면 관찰 목록에만.

### F3 — 최종 집계
- **F3-①** ⚠ 미드나이트 경륜이 **23:30까지** 남아 지금은 **최종이 아니다.** 그 사실을 명시한다.
- **F3-②** 경마 3건 제외하면 **회수 0** — **인용 불가**로 표시한다.
- **F3-③** 경륜 71.5% 가 추세인지 하루인지 — **최근 3~5일과 대조**한다.
  🟡 어제까지 경륜 정제 회수율이 71.6~71.8% 였으므로 **추세일 것**으로 본다(70%).
  ⇒ **하루 부진이 아니라 구조적으로 판정선 아래**일 가능성이 높다.
- **F3-④** 삼복승 해제 후 경륜 — **n이 4~6경주뿐**이라 **판정 불가**일 것이다.

### H12 — 경마 12.5%
- **H12-①** 파서 수정(15:11)·가드(16:30) 전후 비교는 **경마 표본이 24경주**라
  각 구간 10경주 내외 → 🔴 **판정 불가**일 것이다. 방향만 낼 수 있다.
- **H12-②** 🟡 경마 적중률이 낮은 진짜 이유는 **두수(11~16두)** 일 것으로 본다 —
  경륜 7~9명 대비 조합 수가 훨씬 많아 2조합으로는 구조적으로 못 덮는다.
  ⇒ **오늘 수정과 무관한 구조적 차이**일 가능성이 크다. 그것도 함께 확인한다.

### 34차 실제 결과 (2026-08-01)

- **N11-①**: 🟡 절반. 11번은 **`keyHorses` 1위이자 복병**(`darkHorsePicks`)에 둘 다 있었다.
  2번은 `confTop1`·`axis[0]` — 유력마가 맞다.
- **N11-②**: 🟢 **예측한 함정이 정확히 맞았다.** 추천 시점 `2+11` 배당은 **10.4배**였고
  `reason "유력마 1·2위 조합 · 신호 근거로 기대값 면제"` · `ev 0.96` 로 **메인**이었다.
  🔴 **그런데 확정배당도 10.4배다** — 폭등한 게 아니라 **내가 円을 배로 읽었다.**
  ⇒ 🔴🔴 **"1,040배 적중"은 존재하지 않았다. 단위 오류였다.**
- **N11-③**: 🔴 **질문 자체가 무효화됐다.** "확정배당 100배+ 적중"의 상당수가 円 오류다.
  실측: 경마 확정배당 ≥100 인 18건 중 **정제 통과 7건만 진짜**(비율 1.00~1.18).
- **N11-④**: 🟢 **조건화하지 않았다.** 오히려 **사례 자체가 성립하지 않았다** —
  1건으로 로직을 만들지 말라는 지시가 **실제로 사고를 막았다.**
- **F3-①**: 🟢 적중(미드나이트 23:30까지 남아 최종 아님).
- **F3-②③**: 🔴 **전면 재산출.** 단위 정정 후 경마 **44.1%** · 경륜 **69.0%** · 합계 **61.9%**.
  ❌ 이전 보고 "경마 3509% · 합계 1078.7%" **폐기**.
- 🟢 **`measure_recovery.py` 는 오염되지 않았다** — 정제 필터가 円 오류 건을 전부 걸러냈다.
  **원칙 15("회수율은 도구로")가 다시 실증됐다.**
- **H12-①②**: ⏸ 경마 24경주로 전후 분할 시 각 10경주 내외 → **판정 불가**(예측대로). 착수 보류.

## 35차 예상 (2026-08-01) — 円→배 변환 A·B · 단위 전수 · 경륜 추세

### A2 — 승인 A (저장 시 /100)
- **A2-①** 🔴 **이중 적용 위험이 실재한다.** `_jra_result_save` 가 같은 경주를 **재수집**하면
  이미 정정된 값에 또 나눌 수 있다. ⇒ **파서 반환값(円)에 대고 나눠야** 안전하다.
  저장 직전 1회 변환이면 재수집 때도 항상 円→배가 되므로 **이중 적용은 없을 것**으로 본다(80%).
  ⚠ 그래도 **소급 도구(B)와 겹치면** 위험 — B 는 `payouts_raw` 円 판별 + `unit_fixed` 이력으로 막는다.
- **A2-②** 검증은 **다음 중앙경마 결과**가 필요하다. 🔴 **오늘 JRA 는 16:10 에 끝났다** →
  **오늘 안에는 검증 불가**일 가능성이 높다(70%). 그러면 "미검증"으로 명시한다.

### B2 — 승인 B (소급 24건)
- **B2-①** 지방 0건이 나올 것으로 본다(판별이 `payouts_raw` 円 키이므로).
  🔴 **--dry 에서 지방이 1건이라도 나오면 중단**한다.
- **B2-②** 24건 전부 `quinella`·`trifecta`·`win`·`exacta` 등 **여러 필드가 함께** 円이다.
  ⇒ **quinella 만 고치면 안 된다.** payouts 전 필드를 나눠야 한다.

### U — 작업1 단위 전수
- **U-①** `payouts` 저장 경로는 **4~6곳**으로 본다(`_jra_result_save` · `_keiba_result_payouts` 소비처 ·
  `_apply_result_learning` · `results_bulk` · `backfill_payouts.py` · `backfill_jra_result.py`).
- **U-②** 🔴 **`results_bulk`(일괄등록) 경로가 의심스럽다** — 사설 HTML 파싱이라 단위 규약이 없다.
  7/30 에 "사설 삼복승이 공식의 11.4배"였던 건이 **단위가 아니라 마권 오식별**로 결론났었는데,
  🟡 **단위 문제였을 가능성을 다시 봐야 한다**(30%).
- **U-③** 회귀 테스트: `payouts.X ≥ 100 AND payouts_raw 에 円 키` → 실패. **오늘 24건이 잡힐 것**이다.
  ⇒ 🔴 그래서 **B 를 먼저 돌리고 테스트를 넣어야** 초록이 된다. 순서가 중요하다.

### C3 — 작업2 경륜 추세
- **C3-①** 🔴 **추세일 것**으로 본다(75%). 어제 71.6~71.8 · 오늘 69.0 — **3일 이상 74.5 아래**면 확정.
- **C3-②** 날짜별 n 이 20~60경주라 **일별 신뢰구간이 매우 넓을 것**이다(±20%p).
  ⇒ **개별 날짜로 판정하지 않고 누적·추세로 본다.**
- **C3-③** 🔴 추세가 확정되면 **"현행 로직은 환급률을 못 넘는다"** 가 된다. 방향 재검토 사안이다.

### 35차 실제 결과 (2026-08-01)

- **A2-①**: 🟢 적중. **파서 반환값(항상 円)** 을 나누게 해 재수집에도 멱등이다.
  🔴 **예상 밖 수확**: `tools/backfill_jra_result.py` **에도 같은 버그가 있었다** — 함께 고쳤다.
  한쪽만 고쳤으면 그 도구를 돌릴 때 다시 円으로 쌓였다.
- **A2-②**: 🟢 적중. 오늘 JRA 는 **16:10 종료** → **검증 미완**. 다음 개최일로 넘긴다.
- **B2-①**: 🟢 적중. `--dry` category 분포 `{'japan_central': 17}` — **지방 0건**.
  ⚠ `quinella≥100` 인데 円 키 없는 7건은 **category 오분류된 지방**의 진짜 고배당(102~186배) — 제외됨.
- **B2-②**: 🟢 **적중**. `win`·`exacta`·`trio`·`trifecta` 가 **함께** 円이었다. 전 필드를 나눴다.
  ⚠ 대상이 24 → **17건**이다(24는 `q≥100` 휴리스틱, 17이 정확한 円 키 기준).
- **U-①②**: 🟢 대체로 적중. 변환 없는 곳은 **2곳**(app.py + backfill_jra_result)이었다.
  `results_bulk` 는 円 개념이 없어 이번 유형과 다르다(⚠ 7/30 카와사키 건은 원본 미저장이라 재확인 불가).
- **U-③**: 🔴 **테스트를 두 번 만들었다.** `payouts ≥ 100` 기준이 **삼복승 255.9배·3連単 630.6배를 오탐**했다
  (삼복승은 100배가 정상). ⇒ **원본 円과 직접 대조**로 고쳤다. **원칙 8-D 의 새 사례.**
  자기검증(円 주입 → rc=1 + 적발) 🟢 통과.
- **C3-①**: 🔴 **빗나감**. **추세가 아니다.** 07-29 78.9% · 07-30 80.8% 로 **위였던 날이 있다.**
- **C3-②**: 🟢 적중. 누적 95%CI **[57.1, 84.1]** — 폭 27%p. **74.5 포함 → 판정 불가.**
- **C3-③**: ⚠ **확정하지 못했다.** 점추정 70.2% 는 아래지만 **구분 불가**다. 표본이 더 필요하다.

## 36차 예상 (2026-08-01 · 13차 세션) — 최종 집계 · 円 검증 · ev-sweep

⚠ **기록 시각 21:38. 측정 실행 전에 적는다.**

### A — 작업1 최종 집계(미드나이트 포함)
- **A-①** 🔴 **지금은 "최종"이 될 수 없다.** 현재 **21:38** 이고 미드나이트 경륜이 **23:30 까지 남아 있다**
  (코치·케이오카쿠·다케오 실측 수집 중). ⇒ 08-01 을 "어제"로 본다면 **최종 집계 불가**(90%).
  **07-31 은 완결됐으므로 확정치를 낼 수 있다.** 두 날짜를 갈라서 보고할 것으로 본다.
- **A-②** 🔴 **삼복승 3제외 74.5% 는 판정 불가일 것**(80%). 해제 시각이 **18:50** 이라
  해제 후 신규 경륜 삼복승 조합이 **n≥150 에 못 미친다**고 본다(3시간 · 경륜 20~30경주 × 2조합 = 40~60).
  ⇒ 섀도우 기간을 포함한 전체로 재면 **76~77% 유지**될 것이다(어제 실측 76.6~77.0%).
  🔴 **두 분모를 섞어 인용하면 안 된다.**
- **A-③** 円 정정 후 경마 회수율은 **어제 값(85~86%)과 크게 다르지 않을 것**(85%).
  근거: 정제 필터(괴리 0.5~2.0배)가 円 오류 17건을 **이미 전부 걸러내고 있었다**(어제 확인).
  ⇒ 🟡 **바뀌면 오히려 이상하다.** 크게 바뀌면 정정이 과했는지 되짚어야 한다.

### B — 작업2 円 변환 검증
- **B-①** 🔴 **오늘은 검증 불가일 것**(85%). 08-01 JRA 는 **16:10 종료**이고 승인 A 적용은 **20:42** 라
  그 사이 신규 중앙경마 결과가 없다. ⇒ **다음 개최일(08-02 일요일 예상)로 미룬다.**
- **B-②** 🟡 다만 `_jra_result_loop`(run-first·300초)가 **기존 경주를 재수집**했을 수 있다(40%).
  그 경우 **재수집분이 배 단위인지**로 A 를 부분 검증할 수 있다.
  ⚠ 재수집이 있었다면 **이중 적용 여부**(값이 1/100 로 또 줄었는지)를 **반드시 함께 본다.**
- **B-③** 이중 적용은 **일어나지 않을 것**(85%). 파서 반환값(항상 円)에 대고 나누므로 멱등이다.

### C — 작업3 `--ev-sweep`
- **C-①** 🔴 **임계를 올리면 배당중앙은 거의 안 오를 것**(70%). 어제 실측상 **강등분의 79%가 5~20배 중배당**이고
  30배+ 는 6조합뿐이다. ⇒ 임계를 올려 자르는 것은 **중배당 덩어리**이지 고배당이 아니다.
- **C-②** 🔴 **74.5% 를 지키면서 배당중앙이 최대인 임계는 현행(1.0)보다 낮은 0.7~0.9** 일 것(60%).
  근거: 강등분 적중배당 중앙이 **7.3배**(현행 3.4배의 2.1배)라 **되살릴수록 배당중앙은 오른다.**
  ⚠ 다만 **회수율이 74.5 아래로 떨어지는 지점이 먼저 올 가능성**도 있다(40%) — 그러면 **해답 없음**이다.
- **C-③** 두수별 분해는 **경마 표본이 얇아 판정 불가 셀이 많을 것**(85%). 13두↑는 n<10 이 예상된다.
- **C-④** ⚠ 어제 세운 함정: **회수율 최대와 배당중앙 최대는 다른 임계**일 것이다(80%).
  **두 곡선을 같이 그려야** 대표 원칙(고배당·중배당 기본)에 맞는 지점을 고를 수 있다.

### 36차 실제 결과 (2026-08-01 · 21:50)

- **A-①**: 🟢 **적중.** 21:35 기준 미드나이트 3곳이 **수집 중**(케이오카쿠 3R 21:34:55 · mb=2).
  ⇒ 08-01 은 **미확정**으로 표기하고 07-31 과 갈라서 보고했다.
- **A-②**: 🔴 **절반 빗나감.** n 은 충분했다 — 누적 **1,858조합**(≥150 통과).
  그러나 **3제외가 77.0% → 74.7% 로 떨어졌다**(여유 0.2%p). 08-01 하루가 64.3% 로 끌어내렸다.
  🔴 **예상 밖 발견**: **일별 3제외는 구조적으로 낮게 나온다**(날마다 3건씩 빼므로).
  일별은 전부 55~64% 인데 누적은 74.7% 다. ⇒ **"매일 추적해 74.5 미만이면 되돌린다"는 규칙은
  일별로는 달성 불가**다. **판정은 누적으로 해야 한다.**
- **A-③**: 🟢 **적중.** 경마 7월 **87.5%**(어제 86.7%) · 7+8월 **81.6%**. 円 정정에도 크게 안 바뀌었다.
  정제 필터가 이미 걸러내고 있었다는 어제 판단이 재확인됐다.
- **B-①**: 🟢 **적중.** 08-01 중앙경마 19건 전부 **mtime 20:42:12**(소급 정정 시각)에 멈춰 있다.
  **신규 결과 0건** ⇒ 승인 A **검증 미완**. 다음 개최일로 이월.
- **B-②**: 🔴 **빗나감.** 재수집 **0건**이었다(로그·mtime 모두). 부분 검증도 불가.
- **B-③**: 🟡 **미검증.** 이중 적용 여부는 신규 결과가 없어 확인 자체가 불가능했다.
  ⚠ 다만 저장값이 전부 **배 단위**(4.3~55.6배)로 정상이고 100배 부풀림은 사라졌다.
- **C-①**: 🟡 **부분 적중.** 배당중앙이 경마 3.5→3.9배(+0.4) · 경륜 2.2→2.3배(+0.1) 로 **거의 안 올랐다.**
- **C-②**: 🔴 **빗나감.** 최적 임계는 **경마 0.50 · 경륜 0.70** 이었다(예상 0.7~0.9).
  🔴 **더 중요한 것**: **3제외 기준으로는 어떤 임계도 74.5% 를 못 넘는다**(경마 67.8 · 경륜 67.3).
  ⇒ 점추정으로만 통과하고 **극단값 의존이다. 채택 근거가 되지 못한다.**
- **C-③**: 🟢 **적중.** ≤9두 n=20 · 13두+ n=14 로 **판정 불가**. 10~12두(n=49)만 유효.
- **C-④**: 🔴 **빗나감.** 두 최대가 **같은 임계**였다(경마 0.50 · 경륜 0.70 둘 다).
  ⚠ 이유는 배당중앙이 임계에 거의 반응하지 않아 **회수율이 사실상 단독 결정**했기 때문이다.
- 🔴 **도구 결함을 만들었다가 잡았다(원칙 8-D)**: 처음엔 `EV 1.00` 을 기준선으로 삼았는데,
  **ev≥1.0 인데도 다른 사유로 강등된 조합이 51개** 있어 현행과 같지 않았다.
  ⇒ **복원 0 인 현행 행을 따로** 찍도록 고쳤다. 안 고쳤으면 경마를 "68.9→80.8% 개선"으로 **오보**할 뻔했다.

## 37차 예상 (2026-08-01 · 14차 세션) — trifecta 마권 혼재 · 추적 규칙 · 고배당 경로

⚠ **기록 시각 22:05. 측정 실행 전에 적는다.**

### D — 작업1 `payouts.trifecta` 마권 혼재
- **D-①** 경로는 **4개**로 본다. 중앙(netkeiba)=**3連単**(확인됨) · 지방(`_keiba_result_payouts`)=**3連複**(80%) ·
  경륜(oddspark)=**3連複=삼복승**(90%) · 사설(`results_bulk`)=🔴 **불명**(규약 없음).
  ⚠ 7/30 카와사키 "사설 삼복승이 공식의 11.4배" 건이 **3連単 오식별이었을 가능성**을 다시 본다(40%).
- **D-②** 🔴 **판정식은 순서 무관이 맞을 것**(90%). `measure_trio` 가 `sorted(top3)` 와 조합을 비교한다.
  ⇒ **삼복승으로 판정하고 3連単 배당으로 회수를 계산**했다면 **회수가 부풀려진다.** 방향은 확실하다.
- **D-③** 🔴 **오염 규모가 예상보다 작을 것**(60%). `trio`+`trifecta` 를 **둘 다** 가진 건은 **17건**뿐이다.
  ⚠ 🔴 **진짜 위험은 다른 데 있다** — `trifecta` **하나만** 가진 610건 중 **중앙경마가 섞여 있다면**
  그 값이 3連単인지 3連複인지 **판별 근거가 없다**(30%). 이게 최악의 경우다.
- **D-④** 중앙 881.1% 는 **크게 낮아질 것**(70%). 다만 **재측정 가능한 것은 `trio` 보유분뿐**이라
  나머지는 **"재측정 불가"로 남을 가능성**이 있다(50%).
- **D-⑤** 🟢 **3連複은 이미 받고 있을 것**(80%) — `payouts.trio` 가 그것이다.
  ⇒ **파서가 아니라 도구(`measure_trio`)가 잘못된 필드를 읽는 문제**일 가능성이 높다.
  그러면 **저장 경로 수정 없이 도구만 고쳐도** 된다(위험 낮음).

### E — 작업2 추적 규칙
- **E-①** 🟢 **경륜 오염 0 이 재확인될 것**(95%). 경륜은 `trio` 필드가 아예 없고 `trifecta` 만 있으며,
  배당중앙 **5.0배**는 3連単(보통 20~50배)이 아니라 **3連複 수준**이다.
- **E-②** 규칙 정정은 **문서 수정**이라 위험이 없다. ⚠ 다만 **완료선을 낮추는 것이 아님**을 명시해야 한다
  — 74.5% 는 그대로이고 **분모 정의만 누적으로 못 박는 것**이다.

### F — 작업3 고배당 다른 경로
- **F-①** 아직 안 본 경로는 **6~8개**가 나올 것으로 본다. 🔴 **두수별 분기가 가장 유력**(60%).
  근거: 13두+ 에서만 배당중앙 9.9배가 보였고, 대표 원칙의 "경마는 이변이 흔하다"와 방향이 같다.
- **F-②** 🔴 **표본 확보가 병목일 것**(85%). 경마 13두+ 는 127경주 중 **14건(11%)** 이라
  n=30 까지 **약 2주**가 걸린다. ⇒ **중앙경마 수집이 늘어야 13두+ 가 빨리 쌓인다**(중앙은 대부분 12~18두).
- **F-③** ⚠ **정렬 변경(19차 안 ②~⑤)이 아직 한 번도 실측되지 않았다**는 점을 다시 지적하게 될 것이다.
  설계만 있고 리플레이가 없다.

### 37차 실제 결과 (2026-08-01 · 22:40)

- **D-①**: 🟢 **적중.** 경로 4개 · 중앙만 반대(3連複→trio · 3連単→trifecta) ·
  지방·경륜·사설은 전부 **3連複 → trifecta(또는 trio)** 로 삼복승 의미다.
  ⚠ 사설은 `final_odds["trio"]` 를 쓰고 읽는 쪽이 `trifecta or trio` 폴백이라 안전했다.
  🔴 **7/30 카와사키 11.4배 건은 이번에 확인하지 못했다**(원본 HTML 미저장) — 그대로 남긴다.
- **D-②**: 🟢 **적중.** `measure_trio` 는 `sorted(top3)` 비교라 **순서 무관 판정**인데 배당은 3連単이었다.
  🔴 **예상 밖 확장**: `_apply_result_learning:18429` 도 같은 필드를 읽어 **`pnl` 까지 오염**된다.
- **D-③**: 🟢 **적중(규모 작음).** 오염 **17건**. `trifecta` 만 있는 125건은
  **category 오분류된 지방**(오오이·몬베츠)이라 안전했다 — 최악 시나리오는 실현되지 않았다.
- **D-④**: 🟢 적중. 중앙 40.0% → **33.1%** · 경마 전체 49.4% → **46.5%** · 지방 **무변(57.0%)**.
  ⚠ 어제의 881.1% 는 **円 정정 단계에서 이미** 40.0% 로 내려와 있었다(3連単은 그 위에 얹힌 오염).
- **D-⑤**: 🟢 **적중.** 3連複은 이미 `payouts.trio` 로 받고 있었다. **파서 무수정 · 도구 1줄 수정**으로 해결.
- **E-①**: 🟢 **적중.** 경륜 `trio` 보유 **0건** → 정정 전후 동일. 74.7%(당시)는 오염 없음이 확정됐다.
  ⚠ 재측정값이 75.7% 로 오른 것은 **미드나이트 경주가 더 쌓인 것**이지 정정 효과가 아니다.
- **E-②**: 🟢 적중. 문서 수정만. **완료선(74.5%)은 그대로**이고 분모 정의만 누적으로 못 박았다.
- **F-①**: 🟢 적중(**8개**). 🔴 **1순위는 두수별 분기가 아니라 「정렬 변경」으로 바꿨다** —
  설계만 있고 **한 번도 실측되지 않았고**, 저배당 편중의 **직접 원인**이기 때문이다.
- **F-②**: 🔴 **빗나감(좋은 쪽으로).** 13두+ 의 **59%가 중앙경마**인데 중앙 수집이 08-01에 시작됐다.
  ⇒ 개최일 하루 20~36경주가 들어오므로 **2주가 아니라 다음 주말 2일이면 n=30 도달**이 현실적이다.
- **F-③**: 🟢 적중. 정렬 변경(19차 안 ②~⑤)이 **아직 한 번도 리플레이되지 않았다**는 점을 다시 지적했다.

## 38차 예상 (2026-08-01 · 15차 세션) — 학습경로 마권 정정 · 정렬 4종 · "같은 필드 다른 것"

⚠ **기록 시각 22:58. 실행 전에 적는다.**

### G — 승인1 학습 경로(`_apply_result_learning`) 마권 정정
- **G-①** 🔴 **실제 오염 레코드는 17건보다 훨씬 적을 것**(80%). `trio` 보유 17건 중
  **삼복승을 적중한 건만** `t_odds`·`pnl` 이 틀린다. 삼복승 적중률 20% 가정 시 **3~5건**.
  ⚠ 미적중이면 배당을 안 쓰므로 오염되지 않는다.
- **G-②** 🟢 **`signal_stats`·EV밴드는 거의 안 바뀔 것**(80%). EV 밴드(`_ev_bands_update`)는
  **복승(quinella) 기반**이고 삼복승 배당과 무관하다. ⇒ 바뀌면 오히려 다른 경로 오염을 의심해야 한다.
- **G-③** 소급 정정은 **필요하지만 규모가 작을 것**. `--dry` 로 대상 0~5건이면 **도구를 새로 만들 가치가 없고**
  기존 `fix_jra_payout_unit.py` 패턴을 따르는 편이 낫다(50%).

### H — 승인2 정렬 4종 리플레이
- **H-①** 🔴🔴 **가장 큰 위험: `_main_cand` 를 재현할 수 없을 가능성**(60%).
  정렬을 바꾸려면 **정렬 전 후보 목록**이 있어야 하는데, `analysis_log` 에 저장된 것은
  **채택분(`displayedCombos`) + 강등분(`quinellaRef`)** 뿐이다. 이 둘의 합집합이
  `_main_cand` 와 같다는 보장이 없다(EV 필터 **이전** 단계에서 잘린 것은 어디에도 없다).
  ⇒ 🔴 **원칙 3(코드 경로 재현 확인)에 걸린다.** 재현 못 하는 부분은 **결과에 명시**해야 한다.
- **H-②** 🔴 **구좌 수는 안들끼리 같아야 한다**(정렬이므로). 다르면 개수 변경이 섞인 것이다.
  ⚠ 다만 **후보가 채택 개수보다 적은 경주**에서는 네 안이 모두 같아진다(정렬해도 순서가 무의미).
  그런 경주가 많으면 **변별력이 없을 것**이다(50%).
- **H-③** 배당중앙은 **오를 것**(70%) — 저배당순을 버리므로. 회수율은 **떨어질 것**(70%).
  ⇒ 🔴 **판정선(74.5% 유지 + 배당중앙 최대)을 통과하는 안이 하나도 없을 가능성**이 상당하다(50%).
- **H-④** 신호강도 순(③)은 **계산 불가일 수 있다**(40%) — `anomCount`·확신도가 조합 단위로
  저장돼 있는지 확인이 필요하다. 없으면 **말 단위에서 조합으로 환원**해야 하고 그건 정의가 갈린다.

### I — 작업3 "같은 필드 다른 것" 전수
- **I-①** **5~8개**가 나올 것으로 본다. 이미 아는 것: ⓐ배당 단위(円↔배) ⓑ마권(3連単↔3連複)
  ⓒ`raceHorseCount` ↔ 실제 마번 수 ⓓ`grade`(전적등급 ↔ 통합등급 — `gradeAtBonus` 신설 사례)
  ⓔ`category`(japan_central 오분류) ⓕ`src`(netkeiba 를 private 로 묶던 것).
- **I-②** 회귀 테스트로 만들 수 있는 것은 **3~4개**. 🔴 **판별 근거가 데이터에 남아 있어야**
  테스트가 성립한다(円은 `payouts_raw`, 마권은 `trio` 공존 — 둘 다 그래서 가능했다).
  ⇒ **판별 근거가 없는 항목은 테스트 불가로 분류**하고 그 사실 자체를 적어야 한다.

### J — F3 소급 계산 (⚠ 23:10 추가 · 실행 전 기록)
- **J-①** 🔴 **"시장과 다른 답 && 적중"은 30~60건** 나올 것으로 본다(변별 91건의 절반 안팎).
  ⚠ Gemini 우위 26건보다는 **많을 것**이다 — 점수는 져도 다른 말 하나를 맞춘 경우가 포함되므로.
- **J-②** 🟡 **그 적중 건의 배당이 시장보다 높을 것**(65%). Gemini 가 시장과 다른 말을 찍었다는 것은
  **시장이 낮게 본 말**이라는 뜻이므로 구조적으로 배당이 높다. ⚠ 이건 **동어반복에 가깝다** —
  "다른 답"의 정의상 배당이 높을 수밖에 없다. **회수율(④)로 판정해야지 배당 분포만으로 판정하면 안 된다.**
- **J-③** 열위 65건 중에도 고배당 적중이 **있을 것**(70%). F2 가 배당을 안 보므로 구조적으로 가능하다.
- **J-④** 🔴 **가상 회수율은 74.5% 를 못 넘을 것**(70%). 근거: Gemini 평균 적중 1.73 ↔ 시장 1.95 로
  **적중 자체가 시장보다 낮다.** 배당이 높아도 적중률이 낮으면 회수율은 안 나온다.
  ⚠ 다만 **상위1·3 제외 전 점추정에서는 넘을 가능성**이 있다(고배당 몇 건이 끌어올림) — 그러면
  **극단값 의존**이므로 원칙 2대로 제외 수치를 함께 봐야 한다.
- **J-⑤** ⚠ **분모 주의**: 예측 241건 ↔ 채점 212건 ↔ 확정배당 보유는 그보다 적을 것이다.
  measure 계열 조인(정제 필터)까지 통과하면 **150건 내외**로 줄어들 것으로 본다(60%).

### 38차 실제 결과 (2026-08-01 · 23:50)

- **G-①**: 🔴 **빗나감(더 좋은 쪽으로).** 오염 **0건**이다 — `trio` 보유 17경주 중 삼복승 **적중이 0건**
  (경마 삼복승이 섀도우라 판정 대상이 아니었다). ⇒ **소급 정정 도구 불필요.**
- **G-②**: 🟢 **적중.** EV 밴드는 복승 기반이라 무영향. 실측 밴드값 불변.
- **G-③**: 🟢 적중(도구 안 만듦). 코드 1줄 + 회귀 테스트에 학습 경로 추가로 마무리했다.
- **H-①**: 🟢 **적중.** `_main_cand` 는 재현 불가다 — EV·저배당 컷 **이전** 후보가 저장되지 않는다.
  ⇒ **"최종 후보 안에서 순서만 바꾸면"** 으로 범위를 좁혀 측정하고 **한계를 명시**했다.
- **H-②**: 🟢 **적중.** 구좌 **완전 동일**(경마 271 · 경륜 528) — 정렬 비교 성립을 실측으로 확인했다.
- **H-③**: 🟡 **절반 적중.** 배당중앙은 올랐다(경마 3.5→**6.4배**). 회수율도 떨어졌다(82.8→80.1%).
  🔴 **그러나 예상과 달리 74.5% 를 지켰다** — "통과하는 안이 없을 것"(50%)이 **빗나갔다.**
  **경마 ④(배당 높은순)가 두 기준을 동시에 만족한 첫 안이다.**
- **H-④**: 🔴 **빗나감.** `stars` 는 저장돼 있었다. 그런데 **정렬 결과가 ①과 완전히 동일** —
  **`stars` 에 변별력이 없다**(순서를 안 바꾼다). 계산 불가가 아니라 **무의미**했다.
- 🔴 **예상 밖 발견 1**: ④ **「시장 대비 저평가 순」 = ② 「EV 순」** 이다(수학적으로 동일).
  ⇒ 안이 5개가 아니라 4개다. **설계 목록 자체가 중복이었다.**
- 🔴 **예상 밖 발견 2**: **F2 판정선이 코드와 달랐다.** 코드는 전체 채점 n(212)로 세어
  **이미 판정을 내고 있었고**, 판정선은 변별 100경주였다. ⇒ 변별 기준으로 고쳐 **91/100 보류**로 정정.
  **하향이 아니라 더 엄격해진 정정**이다.
- **J-①**: 🟡 **하한 근처.** 50건(예상 30~60). 갈린 경주 143 중 35.0%.
- **J-②**: 🟢 적중(6.8 ↔ 5.4배, 1.26배). ⚠ 예상대로 **동어반복 성격**이라 회수율로 판정했다.
- **J-③**: 🔴 **빗나감.** 열위 65건 중 고배당(10배+) 단독 적중은 **2건뿐**이다. "있을 것(70%)"이 틀렸다.
- **J-④**: 🟢 **적중.** 가상 회수율 **74.0% ↔ 시장 74.5%** — 못 넘었고 **3제외에서 뚜렷이 열세**(53.1↔60.1).
- **J-⑤**: 🟢 적중. 조인 211 → 정제 **202경주**(예상 150 내외보다 많았다).

## 39차 예상 (2026-08-02 · 16차 세션) — 합의↔코드 대조 · 정렬 표본 확대

⚠ **기록 시각 00:05. 실행 전에 적는다.**

### K — 작업1 합의 ↔ 코드 대조
- **K-①** 🔴 **불일치보다 "코드에 아예 없다"가 훨씬 많을 것**(80%). 판정선 대부분은
  **CLAUDE.md 문서에만** 있고 코드에는 없다고 본다. 예: 삼복승 해제 판정선(누적 3제외 ≥74.5% AND n≥150) ·
  관찰 해제 조건 O1~O6 · 정제 필터 경계. ⇒ **"불일치 N건"이 아니라 "미구현 N건"으로 분류**해야 한다.
  ⚠ 이 둘은 위험도가 다르다 — **불일치(코드가 다르게 돈다)가 미구현보다 위험하다.**
  F2 가 정확히 그 유형이었다(코드가 **더 느슨하게** 판정 중이었다).
- **K-②** 승인했는데 미구현은 **3~6건**. 후보: O1~O6 자동 판정 · 삼복승 누적 추적 자동화 ·
  스냅샷 A안(I6) · 표준키 1~2단계 · `--ev-sweep` 후속(임계 미변경이므로 정상).
- **K-③** 🔴 **자동 대조는 부분적으로만 가능할 것**(85%). CLAUDE.md 는 자연어라
  **문서→코드 자동 대조는 못 한다.** 현실적 답은 **상수를 한 파일에 모으고**(`tools/thresholds.py`)
  **테스트가 그 값을 코드·health_check 와 대조**하는 것이다. 문서는 그 파일을 인용하게 한다.
- **K-④** ⚠ 오늘 F2 를 잡은 것은 **대표가 "판정선 재확인"을 지시**했기 때문이다.
  ⇒ 자동화 전까지는 **판정 직전에 판정선을 코드로 다시 읽는 절차**가 유일한 방어선이다.

### L — 작업2 정렬 표본 확대 (컨텍스트 허용 시)
- **L-①** 🔴 **전 기간으로 넓히면 회수율 우위가 줄어들 것**(70%). n=125 는 8월분이 섞인 최근 표본이고,
  7월 초는 확정배당·수집 품질이 낮다. ⇒ **80.1% 가 74.5 아래로 내려갈 가능성**이 있다(45%).
- **L-②** 🔴 **적중 빈도 감소는 균일하지 않을 것**(80%). 배당 높은순은 **저배당 적중을 버리므로**
  0적중인 날이 **현행보다 뚜렷이 많아질 것**이다. ⇒ **연속 미적중 일수가 회원 이탈의 실질 지표**다.
- **L-③** 두수별로는 **13두+ 에서 가장 유리할 것**(70%) — 이변이 흔하고 고배당이 많다.
  ⚠ 다만 n=14~27 이라 **판정 불가**로 끝날 가능성이 높다(80%).
- **L-④** 부분 적용안(보조 슬롯만·13두+만)이 **전면 적용보다 나을 것**(60%) — 적중 빈도 손실을 줄이면서
  배당중앙을 올릴 수 있다. ⚠ 단 **구좌가 늘면 정렬이 아니라 확대**가 되므로 구분해야 한다.

### 39차 실제 결과 (2026-08-02 · 00:25)

- **K-①**: 🟢 **적중.** 판정선 12개 중 **일치 8 · 불일치 2 · 미구현 2**.
  🔴 **불일치 2건은 둘 다 "코드가 더 느슨한" 방향**이었다(F2 · 삼복승 추적 주석) — 가장 위험한 방향이다.
  🟡 미구현(O1~O6 · 삼복승 판정선)은 **오판하지 않으므로 상대적으로 안전**하다.
  ⚠ 내가 `_EV_RESCUE_MAX = 12.0` 을 불일치로 오탐했다 — **정규식이 한 줄 다중대입을 못 잡은 것**이었고
  실제는 `LO,_HI,_MAX = 12.0, 30.0, 1` 로 **문서와 일치**했다(원칙 8-D 재발 · 원자료 확인으로 정정).
- **K-②**: 🟢 적중(**미구현 3건 + 보류 1건**). O1~O6 자동판정 · 삼복승 누적추적 자동화 · 표준키.
  ⚠ `--ev-sweep` 은 **"측정만"이 승인 범위**라 미구현이 아니다(분류를 갈랐다).
- **K-③**: 🟢 **적중.** 문서→코드 자동 대조는 **불가능**(자연어)이라 결론냈고,
  **역방향(코드에 단일 원본을 두고 문서가 인용)** 으로 설계했다.
- **K-④**: 🟢 적중. 자동화 전까지는 **판정 직전 코드 재확인**이 유일한 방어선임을 명시했다.
- **L**: 🔴 **미착수** — 컨텍스트 한계로 작업1까지만 수행했다(지시대로).

## 40차 예상 (2026-08-02 · 17차 세션) — 「같은 필드 다른 것」 전수

⚠ **기록 시각 00:40. 실행 전에 적는다. 이번 세션은 이 작업 하나만 한다.**

### M — 전수 조사
- **M-①** 🔴 **8~15개**가 나올 것으로 본다. 이미 확정된 3개(円↔배 · 3連単↔3連複 · `trifecta` 필드명)
  외에 **새로 5~10개**. 후보: `minutes_before`(None 이 "마감 후"와 "미확정" 두 뜻) ·
  `grade`(전적등급 ↔ 통합등급) · `src`(netkeiba 를 private 로 묶던 것) · `raceHorseCount`(≠ 마번 수) ·
  `odds`(단승/복승/삼복승이 문맥별) · `category`(오분류) · `line`(라인 ↔ 평탄 마번열).
- **M-②** 🔴 **판정·회수에 쓰이는 것 중 새 발견은 2~4개**로 본다. 대부분은 표시·집계 계층일 것이다.
  ⚠ **가장 위험한 후보는 `payouts_raw`** — 소스별로 키 체계가 다르고(円 표기 ↔ 없음)
  그것이 円 판별의 유일한 근거다. **키가 없는 소스는 판별 자체가 불가**하다.
- **M-③** 🔴 **회귀 테스트로 만들 수 있는 것은 절반 이하**(60%). 円·마권이 테스트 가능했던 이유는
  **판별 근거가 데이터에 함께 남아 있었기 때문**이다(`payouts_raw` 円 키 · `trio` 공존).
  ⇒ **근거가 안 남는 필드는 테스트 불가**로 분류하고 **그 사실 자체를 목록에 적어야 한다.**
- **M-④** ⚠ **"의미가 다르다"와 "정의가 다르다"를 갈라야 한다.**
  · 단위 다름(円↔배) → 값만 변환하면 된다
  · 마권 다름(3連単↔3連複) → **다른 것을 재고 있다**(변환 불가)
  · 정의 다름(`grade` 시점) → **같은 이름이 시점마다 다른 값**(스냅샷이 필요하다)
  세 유형은 **고치는 방법이 전혀 다르다.** 목록에 유형을 표기할 것으로 본다.
- **M-⑤** ⚠ 이번 세션 규칙상 **새 발견을 쫓지 않는다.** 목록에만 넣는다.
  🔴 **오늘까지 3회 연속 이 규칙이 깨졌다**(더 급한 발견이 앞섰다).

### 40차 실제 결과 (2026-08-02 · 01:00)

- **M-①**: 🟢 **적중.** **12개** 나왔다(기존 3 + 신규 9). 예상한 후보가 대부분 실재했다.
- **M-②**: 🟢 **적중.** 판정·회수 계층 새 발견은 **3개**(`payouts_raw` 보유 1.6% · 삼복승 `odds` 추정 혼재 · `horses[].odds` 종목 편차).
  🔴 **가장 위험한 후보 예측이 맞았다** — `payouts_raw` 는 **17건(1.6%)에만** 있고, 그것이 円 판별의 유일한 근거다.
  ⇒ **円 회귀 테스트가 전체의 1.6%만 검사한다**는 사실을 이번에 처음 쟀다.
- **M-③**: 🟢 **적중.** 테스트 가능 8 / 어려움 3 / 불가 1 — **판별 근거가 데이터에 남는지**가 갈랐다.
- **M-④**: 🟢 **적중.** 유형을 **A 단위 · B 대상 · C 정의 · D 어휘 · E 보유**로 갈랐고,
  🔴 **조사 중 유형 F(런타임↔저장 스키마 차이)를 추가로 만들었다** — 예상에 없던 유형이다.
- **M-⑤**: 🟢 **지켰다.** 새 발견을 쫓지 않고 목록에만 넣었다(4회 만에 규칙 준수).

#### 🔴 예상 밖 발견 3건
1. **유형 F** — 저장된 `horses[]` 에 `totalScore`·`combinedProb` 가 **경마·경륜 둘 다 0%** 다.
   ⇒ *"경륜에 totalScore 가 없다"(7/31)* 를 **정정한다.** 종목 문제가 아니라 **런타임↔저장 스키마 차이**이고,
   위험 범위가 **경륜 한정이 아니라 사후 분석 전체**다.
2. **각질 어휘가 3체계이고 종목별로도 다르다** — `젖히기형`은 경륜에만, `평지형`은 경마에만 있다.
3. 🔴 **`__pycache__` 가 낡은 값을 돌려주는 실사고** — `PAYBACK 74.5 → 70.0` 은 **길이가 같아**
   .pyc 의 (mtime, size) 검증을 통과했다. **파일은 74.5인데 import 는 70.0** 이었다.
   ⇒ 판정선을 **같은 길이로** 고치면 실전에서도 캐시가 안 갱신될 수 있다. **소스 직접 로드로 고쳤다.**
   ⚠ 자기검증(원칙 17)이 **테스트의 실제 결함을 잡아낸 첫 사례**다.

#### 오탐 2건 (원칙 8-D)
- `raceHorseCount != len(horses)` 100% → **조회 위치 오류**(최상위가 아니라 `corePicks` 안).
- (어제) `_EV_RESCUE_MAX` 불일치 → **정규식이 한 줄 다중대입을 못 잡은 것**.
🔴 **둘 다 "놀라운 수치"였고 둘 다 측정 오류였다.**

## 41차 예상 (2026-08-02 · 18차 세션) — 전적이 순위를 바꾸는가 · 변화 vs 수준

⚠ **기록 시각 01:20. 실행 전에 적는다.**

### N — 작업1 전적의 실질 영향
- **N-③** 🔴 **핵심.** **전적 가중치를 0으로 둬도 조합이 크게 안 바뀔 것**(70%).
  근거는 어제 실측이다 — 상위 3두 포함률: `keyHorses`(60/40) **47.3%** ↔ 배당 낮은순 **51.9%**
  ↔ `ranked` 전체 **55.6%** ↔ `record_score` 단독(전적만) **18.2%**.
  🔴 **전적을 섞은 쪽이 순수 배당보다 오히려 낮다.** ⇒ 전적 40%는 **성능을 깎는 방향**으로 작용 중이다.
  조합 일치율은 **70~85%** 로 본다(60%). ⚠ 80% 를 넘으면 **"60/40 표기가 오해를 만든다"가 성립**한다.
- **N-①** 틱별 `keyHorses` 변화는 **경주당 2~5회**로 본다(60%). 🔴 **바뀔 때 배당만 바뀌었을 것**(85%) —
  전적은 경주 전 확정이고 마감까지 재수집되지 않는다(`_KEIBA_FORM_DONE` 게이트).
  ⚠ 다만 **전적이 늦게 들어오는 경주**(수집 지연)에서는 전적 변화도 있을 수 있다(30%).
- **N-②** 🔴 **"전적 고정 + 배당만 변화" = 실제 변화**일 것(80%). 즉 **순위 변동은 전부 배당 때문**이다.
- **N-④** 시장 0.7 → 0.3 뒤집으면 **악화될 것**(80%). 전적 단독이 18.2% 로 무작위(14.3%)에 가깝기 때문이다.
  ⚠ 단 **적중배당 중앙은 오를 수 있다**(50%) — 시장과 멀어지므로.

### O — 작업2 변화 vs 수준
- **O-①** 🔴 **"변화"를 보는 경로는 이상감지 하나뿐일 것**(80%).
  조합 생성·정렬·확률(`ranked`·`combinedProb`·배당 낮은순)은 **전부 수준**이다.
  ⇒ **BMED 라는 이름과 달리 실제 파이프라인의 대부분이 수준을 따라간다.**
- **O-②** 변화만으로 추천하면 **포함률·회수율 둘 다 떨어질 것**(70%).
  ⚠ **적중배당 중앙은 오를 것**(75%) — 수준을 안 따라가므로 저배당이 안 뽑힌다.
  🔴 어제 정렬④(배당 높은순)가 **회수율 -2.7%p 에 배당중앙 1.8배**였던 것과 같은 구조일 것이다.

### P — 작업3 전개 엔진 재측정
- **P-①** 🔴 **지금은 못 돌린다**(85%). 두수 파서 수정이 **2026-08-01** 이라 그 이후 데이터가
  **1~2일치뿐**이다. `flow_table` 은 07-21~07-29 표본으로 만들어졌다.
- **P-②** 필요 표본: `빠른|7두` 셀 기준 어제 계산으로 **500경주**가 필요했다. 하루 20~30경주면
  🔴 **약 3~4주**다. ⚠ 다만 **"두수 누락이 결과를 뒤집었는지"만 보려면** 훨씬 적어도 된다 —
  **같은 셀에서 엣지 부호가 바뀌는지**만 확인하면 되므로 **100경주(4~5일)** 면 방향은 볼 수 있다(60%).
- **P-③** ⚠ 어제 확인한 것: 두수 누락은 **경마 50.5% · 경륜 2.4%** 였다.
  🔴 **전개 엔진은 경륜 전용**이었으므로 **누락률 2.4% 짜리 데이터로 잰 것**이다.
  ⇒ *"입력이 반쪽이었다"* 는 **경마에는 맞지만 경륜에는 안 맞을 가능성**이 높다(70%).
  **이게 사실이면 전개 엔진 결과를 무효로 볼 근거가 약해진다.**

## 42차 예상 (2026-08-02 · 19차 세션) — 전적표 패턴 분석 · 데이터 가용성

⚠ **기록 시각 02:00. 실행 전에 적는다. 작업1(가용성)이 선행 조건이다.**

### Q — 작업1 데이터 가용성
- **Q-①** 🟢 **착순 시계열은 있을 것**(85%). `record_detail` 이 horses[] 에 **100%** 있고
  `raw_profile.entries[].recentPlacings`·`pastPlacings` 도 있다. **3~5전**으로 본다.
  ⚠ 다만 **순서가 최신→과거인지 과거→최신인지**가 명시돼 있지 않을 수 있다(50%).
  🔴 **그 방향을 잘못 읽으면 우상향과 하락이 정확히 뒤집힌다.** 반드시 실값으로 확인해야 한다.
- **Q-②** 🔴 **④(기대 배반)를 만들 시장 정보는 없을 것**(75%). `pop`(인기순위)은
  **스키마 드리프트 🔴 1순위**로 **미배선(보유 0%)** 이다. 馬柱에 `人気` 가 있을 가능성은 60%.
  🔴 **대안(우리 데이터 자기조인)도 어려울 것**(70%) — 과거 경주의 배당은 `odds_history` 에 있으나
  **말 단위(마명)로 조인할 인덱스가 없다.** raceKey↔마명 매핑을 새로 만들어야 한다.
  ⇒ **④는 수집부터 해야 할 가능성이 높다.**
- **Q-③** 🔴 **승급·강등 정보는 없을 것**(85%). 클래스(条件·급별)를 저장하는 필드를 본 적이 없다.
  ⚠ 경륜은 **급별(S級·A級)** 이 출마표에 있어 상대적으로 쉬울 수 있다(40%).
- **Q-④** 🟢 종목별로 다를 것(90%). 경륜은 각질·라인·급별이 있고 경마는 없다.
  ⇒ **패턴 정의를 종목별로 갈라야 한다.** ①②는 공통, ③④는 종목별.

### R — 작업2 패턴 정의
- **R-①** 발동률: 우상향 **10~20%** · 반복형 **5~15%** · 승급부진 **5% 미만**(데이터 없으면 0).
  ⚠ 🔴 **④가 가장 유망한데 데이터가 가장 없다** — 이게 이번 세션의 핵심 제약일 것이다.
- **R-②** ⚠ **정의를 먼저 못 박는다.** 성적을 보고 정의를 맞추면 사후 최적화다(대표 지시).
  발동률만 먼저 재고, 성적은 그 다음이다(원칙 4).

### S — 작업3 성적
- **S-①** 🔴 **대부분 n<30 으로 판정 불가일 것**(70%). 전적 보유 경주가 제한적이고
  패턴 발동률이 10~20% 면 **경주 300건에서 30~60건**이라 겨우 걸친다.
- **S-②** ⚠ **경마는 `record_score` 부족이 51/125 경주**였다(어제 실측) — 전적 기반 측정의 분모가 얇다.

### 42-B 예상 (2026-08-02 · 02:20) — record_score 가 순서를 뭉개는가

- **T-①** 🔴 **8-6-5-3 과 3-5-6-8 이 같은 점수일 것**(75%). "최근 착순 40점"이라는 표현 자체가
  **집계(평균·합)** 를 시사한다. 순서를 보려면 가중치가 필요한데 그런 설계는 문서에 없다.
  ⚠ 다만 **가중평균(최근에 더 큰 가중)** 일 가능성도 있다(25%) — 그러면 두 값이 **다르다.**
- **T-②** 순서만 다르고 점수가 같은 말은 **전체의 10~25%** 로 본다(60%).
- **T-③** 🔴 **순서를 보는 항목은 없을 것**(70%). 거리적성·기수복승률·체중은 전부 **현재 시점** 값이다.
- **T-④** ⚠ **편향 방향**: `record_score` 가 없어 제외된 51경주는 **전적 수집이 실패한 경주**다.
  전적이 잘 갖춰진 경주만 남으므로 **전적축에 유리한 방향**으로 편향된다.
  ⇒ 🔴 **"전적이 해롭다"는 결론은 더더욱 낼 수 없다** — 편향이 전적에 **유리한데도** 낮게 나온 것이
  사실이지만, n=74 로는 **어느 쪽도 판정 불가**다.

### 42차 실제 결과 (2026-08-02 · 02:50)

- **T-①**: 🔴 **빗나감.** `base_form_score` 는 **가중평균**(`FORM_WEIGHTS=[.40,.30,.20,.05,.05]`)이라
  **순서를 본다.** 8-6-5-3 = **2.6** ↔ 3-5-6-8 = **21.1** 로 다르다.
  🔴 **그러나 더 심각한 것을 찾았다** — `place_points` 가 **5착 이하를 전부 0** 으로 만든다.
  ⇒ **12-9-7-5(우상향) = 9-9-9-9(전패) = 0.0**. **개선 과정이 통째로 사라진다.**
  ⇒ 대표 지적의 **취지는 맞았고 실제 문제는 더 컸다.**
- **T-③**: 🟢 적중. 두수(`fieldSizes`)는 저장돼 있는데 **점수식이 쓰지 않는다** — 7두 5착 = 18두 5착.
- **T-④**: 🟢 적중. 제외된 51경주는 **전적 수집 성공분만 남기므로 전적축에 유리한 편향**이다.
  ⇒ 🔴 **"전적 40%가 17%p 깎는다"를 확정으로 쓰지 않는다**(대표 지시 수용). n=74 는 판정 불가다.
- **Q-①**: 🟢 적중. `recentPlacings`·`pastPlacings` 는 **최신이 맨 앞**(스키마 주석으로 확정).
- **Q-②**: 🟢 **적중.** ④는 **불가**. `pop` 은 **현재 경주** 것이고 보유 3.8%.
- **Q-③**: 🔴 **빗나감(좋은 쪽으로).** 경륜 `starters_store` 에 **`prev1`/`prev2`** 로
  **과거 3개 개최의 경기장·등급(Ｆ１/Ｆ２)·날짜·착순·타임**이 통째로 있다. ③을 만들 수 있다.
  ⚠ 단 **`raw_profile` 에는 빠져 있어 소급 불가** — 저장 배선이 필요하다("받는데 저장에서 탈락" 유형).
- **Q-④**: 🟢 적중(종목별로 다르다 · 경륜이 훨씬 풍부).
- **S-①**: 🟢 적중(대부분 표본 부족).

### 41차 실제 결과 (2026-08-02 · 01:40)

- **N-③**: 🔴 **빗나감.** 조합 겹침이 **경륜 62.2% · 경마 51.5%** 로 예상(70~85%)보다 훨씬 낮다.
  ⇒ **"전적 40%는 이름만"이 아니다.** 전적을 빼면 **조합의 38~48%가 달라진다.**
  🔴 **더 중요한 발견**: **종목별로 방향이 정반대**다 — 경륜은 전적을 섞으면 회수율이 **오르고**(70.7→76.4%),
  경마는 **내려간다**(72.3→54.9%). ⇒ **같은 60/40 을 두 종목에 쓰는 근거가 없다.**
  ⚠ 내가 근거로 삼은 어제 수치(포함률 47.3% < 51.9%)는 **포함률**이었고, **회수율은 반대**였다.
  **원칙 14(포함률 ≠ 회수율)를 내가 스스로 어겼다.**
- **N-①②**: 🟢 **구조로 확정.** 전적은 `_KEIBA_FORM_DONE` 게이트로 경주당 1회만 수집되므로
  **틱별 변동은 전부 배당 때문**이다. ⚠ 틱별 전수 측정은 하지 않았다(목록에 남김).
- **N-④**: ⏸ **미측정.** 시장 0.7→0.3 은 `combinedProb` 재계산이 필요한데 저장값으로 재현이 안 된다.
- **O-①**: 🟢 **적중.** 변화를 보는 경로는 **이상감지 계열뿐**이고 조합 생성·정렬·확률은 **전부 수준**이다.
- **O-②**: ⏸ **미측정.** 조합 생성 자체를 대체해야 해 현재 도구로 근사가 안 된다. **설계가 먼저다.**
- **P-①②**: 🟢 적중(지금 못 돌린다 · 방향만 보려면 100경주 4~5일).
- **P-③**: 🟢 **적중.** 경륜 누락률 **1.9%**(경마 43.4%). **50.5% 는 경마 수치**였고
  전개 엔진은 경륜 전용이므로 **"입력이 반쪽이었다"는 성립하지 않는다.**
  ⇒ **전개 엔진 결과를 무효로 볼 근거가 약하다 — 재측정 우선순위를 낮춘다.**







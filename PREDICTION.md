# 예상 기록 — 역수 배분 비율 회원 발송 최적화 (2026-08-12)

## 1부. 쉬운 설명

지금 회원에게 나가는 카톡에는 마권 번호만 있습니다. 그래서 회원은 조합마다
같은 돈을 겁니다. 그런데 배당이 다른 조합에 같은 돈을 걸면, 낮은 배당이
맞았을 때 원금도 못 건집니다.

배당의 역수에 비례해 걸면 어느 조합이 맞아도 돌려받는 돈이 같아집니다.
1.3배 조합에 많이 걸고 30배 조합에 조금 거는 식입니다.

소급으로 재보니 8월 11일 기준 상위 3건을 뺀 회수율이 42.2%에서 58.1%로
올랐습니다. 그래서 이 비율을 카톡에 실어 보냅니다.

다만 회원에게는 비율만 보냅니다. 회수율 숫자나 권장·경고 문구는 안 보냅니다.
"원금 회수율 190%"는 맞았을 때 값이고 실제 회수율은 66.6%인데, 그대로
내보내면 회원이 매번 90% 번다고 오해하게 됩니다. 나중에 더 큰 불신이 옵니다.

그리고 배당이 오염된 경주에서는 비율을 아예 안 보냅니다. 8월 11일 하루에만
다른 경주 배당이 들어온 사고가 다섯 건이었습니다. 틀린 배당으로 계산한
비율은 틀린 비율입니다.

## 2부. 예상 (실행 전에 적는다)

### 무엇을 바꾸나
- `_stake_plan` 에 정수 보정을 넣는다. 반올림 뒤 남는 오차를 최저배당 조합에
  가감해 합계를 정확히 100%로 만든다.
- 안전 게이트 셋을 넣는다. oddsSource 가 private 면 반환하지 않는다.
  두수와 배당 조합 수가 nC2 와 안 맞으면 반환하지 않는다.
  배당이 null 인 조합은 비율에서 빼고 그 사실을 내부 로그에 남긴다.
- T-5 시점 값을 corePicks 에 고정한다. 이후 재계산하지 않는다.
- 카톡에서 회수율 문구와 권장·경고를 뺀다. 조합과 비율만 남긴다.
- 권장·경고는 API 응답과 내부 로그에만 남긴다.

### 무엇을 안 바꾸나
- 조합 선택 로직(`_final_picks`)과 판정 명단은 한 줄도 안 건드린다.
- 기존 카톡 줄(복승·삼복승·복병·신호·확신도)은 그대로 둔다. 추가만 한다.
- 판정선 74.5 는 낮추지 않는다.

### 예상되는 결과
- 카톡 본문에 "배분 3+5 93% · 2+4 4% · 2+3 3%" 같은 한 줄이 늘어난다.
  합계는 정확히 100%가 된다.
- 회수율·경고 문구는 카톡에서 사라진다. API 응답에는 남는다.
- private 소스 경주에서는 배분 줄 자체가 안 나간다. 8월 11일 기준으로
  모리오카 2경주, 우라와 1경주, 야히코 7경주가 그에 해당한다.
- 정수 보정 때문에 비율 합이 99%나 101%가 되는 일이 없어진다.

### 틀릴 수 있는 지점
- 두수 정보를 못 구하는 경주가 있을 수 있다. 그러면 nC2 대조가 성립하지
  않는다. 그때는 게이트를 통과시킨다. 판정 불가를 차단으로 바꾸지 않는다.
- T-5 고정본이 저장되기 전에 카톡이 나갈 수 있다. 그 경우 카톡은 발송
  시점 배당으로 계산하는데, 그 시점이 곧 T-5 라 결과는 같아야 한다.
  다르게 나오면 배선이 틀린 것이다.
- 계수기가 0 이면 배선이 안 탄 것이다. 다음 개최에서 확인해야 한다.

## 3부. 결과

(앞 항목 실행 완료 — 아래 후속 작업으로 이어짐)

---

# 예상 기록 — 역수 배분을 고배당 가중으로 (2026-08-12)

## 1부. 쉬운 설명

역수 배분은 고배당 조합에 아주 조금 겁니다. 30배짜리에 4%만 갑니다.
맞아도 회수가 121%밖에 안 됩니다.

그래서 15배 넘는 조합에만 비중을 6배 올리고 다시 정규화합니다.
저배당 위주는 그대로 두되 고배당 쪽 몫을 키우는 방식입니다.

최고배당 하나에 몰아주는 안도 재봤는데 기각했습니다. 3건 제외 회수율이
72.0%로 판정선 아래로 떨어졌습니다.

## 2부. 예상 (실행 전에 적는다)

### 무엇을 바꾸나
- `_stake_plan` 의 가중식을 `1/배당` 에서 `(1/배당) x (배당>=15 이면 6)` 으로 바꾼다.
- 스위치로 역수와 가중을 오갈 수 있게 한다.
- 정수 보정과 합계 100% 는 그대로 둔다.
- 계수기에 배수와 경계를 함께 남겨 나중에 4배·8배·10배·20배와 비교한다.

### 무엇을 안 바꾸나
- 조합 선택 로직과 판정 명단은 안 건드린다.
- 카톡은 비율만 나가는 형태를 유지한다. 회수율·경고는 계속 내부용이다.
- 안전 게이트(private 차단·nC2 대조·배당 null 제외)는 그대로 간다.

### 소급 근거
- 균등 87.5% / 3제외 74.8%
- 역수 89.5% / 3제외 77.3%
- 가중 6배 91.2% / 3제외 79.0%  <- 채택
- 몰아주기 70% 88.2% / 3제외 72.0%  <- 기각
- 고배당 15배+ 적중 20건에서 받는 돈: 역수 3.43 -> 가중 6배 10.34

### 예상되는 결과
- 15배 이상 조합의 비중이 눈에 띄게 커진다. 야히코 4R 이면 30.4배 조합이
  4.0% 에서 약 20% 안팎으로 오를 것이다.
- 합계는 정수 보정으로 정확히 100% 를 유지한다.
- 카톡 배분 줄도 같은 비율로 바뀐다.
- 원금 회수율(1 나누기 Sum(1/배당))은 배분과 무관하므로 값이 안 바뀐다.
  바뀌면 배선이 틀린 것이다.

### 틀릴 수 있는 지점
- 15배 이상 조합이 없는 경주에서는 역수와 결과가 같아야 한다. 다르면 틀렸다.
- 가중을 곱한 뒤 정규화하므로 저배당 비중이 줄어든다. 그만큼 저배당 적중 시
  회수가 줄어든다. 그것이 3제외에서 어떻게 나오는지는 며칠 실전으로 봐야 한다.

## 3부. 결과

### 예상이 맞은 것
- 야히코 4경주 30.4배 조합이 4% 에서 18% 로 올랐다. 예상 "20% 안팎" 과 일치.
  41.1배도 13% 다. 적중 시 회수가 121 에서 547.2 로 뛴다.
- 합계는 세 경주 모두 정확히 100%. 정수 보정이 작동한다.
- 원금 회수율은 121% / 120% / 93% 로 역수 때와 똑같다. 배분과 무관하다는
  예상대로다. 바뀌었으면 배선이 틀린 것이었다.
- 배당 없는 조합은 모리오카에서 [[2,4],[4,5]] 로 명시되고 비율에서 빠진다.

### 실물 예
- 카사마츠 5경주: 30.8배 4+8 이 3% 에서 16% 로. 그날 실제로 그게 맞았으니
  회수가 92.9 에서 492.8 이 된다. 5.3배.
- 그 경주는 verdict 가 warn 인데 그 문구는 회원에게 안 간다.
  콘솔에 [내부·비발송] 로만 찍힌다.

### 아직 못 본 것
- 카톡 실제 발송 텍스트. 진행 중 경주가 없어 _kakao_rich_message 가 안 돈다.
  kakao_stake_plan / grade_judge_freeze 계수기 둘 다 0 이다.
- private 소스 차단(stake_plan_block). 어제 경주가 전부 oddspark 라 미발동.
- 다음 개최에서 셋 다 확인할 것.

### 남은 비교 대상
- 배수 4·8 과 경계 10·20 은 상수 두 줄(STAKE_HI_ODDS / STAKE_HI_BOOST)만
  바꾸면 재측정된다. 실전 표본이 쌓이면 같은 스크립트로 다시 잰다.

---

# 예상 기록 — 보고를 파일로 남기고 API로 (2026-08-12)

## 1부. 쉬운 설명

지금은 보고를 손으로 옮겨 붙입니다. 길면 잘리고 밖에 있으면 아예 못 봅니다.
파일에 쌓아두고 주소 하나로 읽게 하면 그 손이 없어집니다.

## 2부. 예상 (실행 전에 적는다)

### 무엇을 만드나
- `docs/REPORT.md` 에 3단 보고를 쌓는다. 맨 위가 최신이다.
- 보고마다 시각과 그때의 커밋 해시를 붙인다.
- 파일이 커지면 `docs/report_archive/` 로 회전시키고 본문은 비운다.
- `GET /api/report?n=1` 로 최근 n건을 원문 그대로 돌려준다.

### 무엇을 안 바꾸나
- 기존 파일·엔드포인트를 지우지 않는다. 추가만 한다.
- 판정·추천·수집 경로에 손대지 않는다. 완전 읽기 전용이다.

### 예상되는 결과
- 이번 작업 보고가 `docs/REPORT.md` 의 첫 항목으로 들어간다.
- `/api/report` 가 그것을 잘리지 않은 원문으로 돌려준다.
- 한글이 깨지지 않는다.

### 틀릴 수 있는 지점
- 구분자를 잘못 고르면 보고 본문 안의 가로줄과 겹쳐 잘못 잘린다.
  본문에서 쓰지 않을 고유 표식을 써야 한다.
- 커밋 해시는 보고를 쓰는 시점의 HEAD 라 그 보고가 담긴 커밋과 한 칸 어긋난다.
  그 사실을 파일에 명시해야 나중에 오해가 없다.

## 3부. 결과

### 예상대로 된 것
- `docs/REPORT.md` 생성(2,964바이트). 이번 작업 보고가 **첫 항목**으로 들어갔다.
- `/api/report?n=1` 이 `ok=True · total=1 · count=1` 로 응답한다.
- 본문 1,441자가 **잘리지 않고 원문 그대로** 나온다. 한글도 정상이다.
- 시각(2026-08-12 06:24:55)과 커밋(69483a5c0)이 함께 붙었다.
- "이 보고가 담긴 커밋은 보통 그 다음 커밋"이라는 경고가 파일에 실렸다.

### 틀릴 수 있다고 적은 두 가지
- 구분자: `<!--REPORT-->` 를 써서 본문 가로줄과 안 겹쳤다. 잘못 잘린 곳 없다.
- 커밋 해시 어긋남: 실제로 어긋난다. 69483a5c0 은 **직전** 커밋이다.
  파일에 그 사실을 적어 두었으므로 오해가 생기지 않는다.

### 덧붙인 것
- `&format=md` 로 마크다운 원문을 그대로 받을 수 있게 했다.
- 400KB 를 넘으면 `docs/report_archive/` 로 회전한다. 지우지 않는다.
- 응답에 `archives` 목록을 실어 과거 파일을 찾을 수 있게 했다.

---

# 예상 기록 — 코드맵 갱신과 보완 (2026-08-12)

## 1부. 쉬운 설명

app.py 가 3만 줄을 넘었습니다. 어디를 고쳐야 할지 찾기가 어렵습니다.
코드맵은 함수마다 어디에 있고 누가 부르고 무엇을 부르는지 적은 지도입니다.

이미 7월 30일에 만들어져 있었습니다. 다만 그 뒤로 코드가 바뀌어 낡았고
빠진 항목이 있습니다.

## 2부. 예상 (실행 전에 적는다)

### 먼저 확인한 것 (지시대로 보고)
- `_triple_analyze` · `_final_picks` -> **03_분석**
- `_history_save_analysis` · `_json_atomic` -> **02_저장**
- 이름 기반 우선 규칙이 이미 반영돼 있어 의도대로 들어간다.

### 무엇을 보완하나
- 위험목록 ③(파서 반환 키 vs 저장행 키 차집합)이 **빠져 있다**. 넣는다.
- 신선도 배너가 없다. `00_INDEX.md` 맨 위에 낡음 표시를 넣는다.
  🔴 차단하지 않는다 — 라이브 중 커밋을 막으면 안 된다.
- 낡은 코드맵을 다시 생성한다.

### 무엇을 안 바꾸나
- `app.py` 는 한 줄도 안 건드린다.
- 기존 섹션 분류 규칙과 수정 지시 넷의 반영분은 그대로 둔다.
- 설명은 계속 docstring·주석에서만 뽑는다. 새로 쓰지 않는다.

### 수치 예상
- 함수 총수: 지난번 1,125개. 그 뒤 배선이 늘었으니 **1,150~1,250개** 로 본다.
  ⚠ 지시서의 1,200~1,500 범위 하단에 가까울 것이다.
- 설명 자동 추출률: 지난번 74.7%. 새 함수에 주석을 많이 달았으니 **73~78%** 유지.
  지시서 기준 60% 이상은 넉넉히 넘는다.
- 빈값 폴백(①): 지난번 57곳. 그 뒤 방어를 더 넣었으니 **55~70곳**.
  ⚠ 지시서의 62곳은 app.py 전체 기준이고 코드맵은 핫 경로만 세므로 더 적을 수 있다.
- os.replace(②): 지난번 20곳. **18~24곳**.
- sleep 데몬(⑤): 지난번 11곳. 지시서 예상 3~6곳과 크게 다르다.
  ⚠ 코드맵은 `while True` 없이 `time.sleep` 만 있어도 세는 것으로 보인다.
  실제 sleep-first 데몬은 CLAUDE.md 실측으로 6곳이다. 그 차이를 확인해야 한다.
- ③(파서 키 차집합): 새로 만드는 것이라 예상값이 없다. **10~40곳** 으로 본다.

### 틀릴 수 있는 지점
- 함수 수가 1,250을 넘으면 그동안 배선이 예상보다 훨씬 많았다는 뜻이다.
- ⑤가 11곳으로 그대로면 세는 기준이 CLAUDE.md 실측(6곳)과 다른 것이다.
  그 경우 기준 차이를 기록만 하고 코드맵 기준을 바꾸지 않는다.
  🔴 「무엇이 어디에」만 담고 「왜 결함인가」는 안 담는 원칙 때문이다.
- ③은 파서와 저장행을 짝지어야 하는데 이름만으로 짝을 못 찾을 수 있다.
  못 찾으면 빈 목록이 되고, 그것도 사실대로 남긴다.

## 3부. 결과

### 수치 대조 (예상 → 실제)

- 함수 총수: 1,150~1,250 예상 → **1,279개**. 🔴 **예상을 넘었다.**
  7/30 이후 열흘 남짓에 154개가 늘었다. app.py 가 32,349 → 39,078줄로 6,729줄 늘었다.
  ⚠ 지시서 범위(1,200~1,500)에는 들어간다. 내 하단 추정이 짧았다.
- 설명 자동 추출률: 73~78% 예상 → **74.8%**. 맞았다. 지시서 기준 60%를 넘는다.
- ①(빈값 폴백): 55~70곳 예상 → **69곳**. 맞았다(지난번 57 → 69).
- ②(os.replace): 18~24곳 예상 → **29곳**. 🔴 **예상을 넘었다.**
  락 없는 지점이 27곳이다. tmp 패턴을 쓰는 저장이 그만큼 늘었다.
- ④(전역 상수): 예상 안 함 → 153개(지난번 75). 스위치를 많이 늘린 결과다.
- ⑤(sleep 데몬): 11 → **21곳**. 예상대로 CLAUDE.md 실측(6곳)과 기준이 다르다.
  코드맵은 `while True` + `time.sleep` 이면 전부 센다. 실측 6곳은 **주기 5분 이상**만이다.
  🔴 기준 차이를 기록만 하고 코드맵 기준은 바꾸지 않는다.
  「무엇이 어디에」만 담고 「왜 결함인가」는 안 담는 원칙 때문이다.
- ③(파서 키 차집합): 10~40곳 예상 → **1쌍**. 🔴 **크게 빗나갔다.**
  이유는 아래에 적는다.

### 왜 달랐나 — ③

처음 돌렸을 때 4쌍 전부 「'str' object has no attribute '_fields'」였다.
`get_keys` 가 **AST 노드**를 받는데 이름 문자열을 넘겼다.
`func_node(name)` 을 거치도록 고치니 1쌍이 나왔다.

1쌍뿐인 이유는 나머지 세 쌍의 차집합이 실제로 비었기 때문이 아니다.
`audit_key_drift.py` 를 직접 돌리면 네 쌍 모두 탈락 후보가 나온다
(11개 · 13개 · 4개 · 13개). 그쪽은 **정규화 규칙이 더 정교**하다.
코드맵의 단순 차집합은 그중 일부만 잡는다.

🔴 즉 ③은 **여전히 전용 도구가 정확하다.** 코드맵에는 요약만 싣고
「수동 확인: `python tools/audit_key_drift.py`」 안내를 남기는 것이 옳았다.
지금 상태로도 색인에 ③이 표기되어 존재 자체는 드러난다.

### 신선도 배너

작동한다. 00_INDEX 맨 위에 이렇게 뜬다.
「직전 생성 이후 바뀐 소스 2개 — app.py (32349 → 39078줄) · tools/health_check.py (403 → 1099줄)」
🔴 차단하지 않는다. 알리기만 한다.

### 수정 지시 넷 확인

- ① 이름 기반 우선: 이미 반영돼 있었다. 네 함수가 의도대로 들어간다.
  `_triple_analyze`(03_분석 10724) · `_final_picks`(03_분석 8348) ·
  `_history_save_analysis`(02_저장 15703) · `_json_atomic`(02_저장 15759)
- ② 위험목록 함수는 Caller 전수 나열: 코드에 「위험목록 함수 → 전수 나열」 표기가 있다.
- ③ 「왜 결함인가」 미포함: 헤더의 사고 사례는 **판정 기준을 알리는 한 줄**이고
  함수별 항목에는 판단이 없다. 유지한다.
- ④ 근거 없는 권고 금지: 권고문이 없다. 유지한다.

### 남은 것 (사실만)

- 99_미분류가 734개(57.4%)다. 지난번 641개에서 늘었다.
  분류 규칙에 안 걸리는 함수가 과반이다.
- `_start_periodic_backup` 이 「영속화 없음」으로 표기되는데,
  실제로는 `data/_periodic_backup_last.txt` 를 쓴다(2026-08-01 배선).
  도구가 그 패턴을 못 읽는다. 🔴 도구의 오탐이다.

---

# 예상 기록 — 미분류 734개 조사 (2026-08-12)

## 1부. 쉬운 설명

코드맵에서 절반 넘는 함수가 「미분류」입니다. 소속을 모르는 지도는 반쪽입니다.

무엇이 거기 들어 있는지부터 봅니다. 규칙을 먼저 만들지 않습니다.

## 2부. 예상 (실행 전에 적는다)

### 무엇을 하나
- 734개를 **이름 앞머리로 묶어** 상위 20개를 낸다.
- 지금 분류 규칙이 무엇을 보는지 코드에서 확인한다.
- 07 섹션이 필요한지는 **설계안만** 낸다. 배선하지 않는다.
- app.py 증가분을 섹션별로 센다. **사실만 낸다. 판단하지 않는다.**

### 무엇을 안 하나
- `app.py` 는 건드리지 않는다.
- 분류 규칙을 이번에 고치지 않는다. 무엇이 있는지 보는 것이 먼저다.

### 예상
- 앞머리 상위는 `_kra_` · `_keiba_` · `_korea_` · `_jra_` · `_kakao_` 같은
  **도메인 접두**가 많을 것으로 본다. 지금 규칙은 동사(`_save`·`_fetch`)를 보는데
  도메인 접두는 동사가 뒤에 오거나 아예 없다.
- 미분류의 상당수는 **헬퍼·유틸**(`_fmt`·`_esc`·`_num`·`_to_`)일 것이다.
  그것은 억지로 여섯에 넣으면 안 된다. 07 이 필요한 근거가 된다.
- 규칙이 좁은 쪽과 성격이 다른 쪽이 **섞여 있을** 것이다.
  둘의 비율이 이번 조사의 핵심 숫자다.
- app.py 증가 6,729줄 중 절반 이상이 **주석**일 것으로 본다.
  이 프로젝트는 사고 경위를 코드 옆에 길게 적는다.

### 틀릴 수 있는 지점
- 앞머리가 너무 잘게 흩어져 상위 20개로 절반도 못 덮을 수 있다.
  그러면 접두 기반 규칙이 답이 아니라는 뜻이다.
- 주석 비율이 낮으면 실제 배선이 그만큼 늘었다는 뜻이고,
  그건 지도가 더 급하다는 근거가 된다.

## 3부. 결과

### 예상이 빗나간 것

- 접두 상위 20개가 **29.7%(218/734)** 밖에 못 덮었다.
  두 토큰으로 묶으면 9.8% 다. 🔴 **접두 기반 규칙은 답이 아니었다.**
  「상위 20개로 절반도 못 덮으면 접두가 답이 아니다」라고 적어둔 그대로다.
- 헬퍼가 상당수일 것으로 봤는데 **40개(5.4%)** 뿐이었다.
  도메인 접두 101개(13.8%), 내부 계산 16개(2.2%), 나머지 583개(79.4%)가 성격 미상이었다.

### 진짜 원인

🔴 **미분류 734개 중 기존 동사 규칙에 매치되는 것이 0개**였다.
적용 순서 문제가 아니라 규칙이 **아예 안 닿는 이름들**이다.
기존 여섯 규칙은 전부 동사(`_save`·`_fetch`·`_analyze`)인데
`_kra_g` `_jra_num` 처럼 도메인 접두 뒤에 짧은 이름이 오면 못 잡는다.

### 배선 결과 (승인분)

- 도메인 2차 규칙: 이름 **어디에든** 동사 조각이 있으면 그 섹션으로. 느슨한 매치.
- 07_헬퍼 신설: 짧은 변환 도구 29개.
- **미분류 734 → 510개.** 224개가 소속을 찾았다.
  03_분석 89 → 208 · 01_수집 86 → 108 · 05_출력 227 → 246 · 02_저장 86 → 103.
- ⚠ 남은 510개를 억지로 넣지 않았다(대표 지시).

### 미분류 우선순위 (대표 지시)

「지도의 목적은 고칠 때 찾는 것」이라는 기준으로 두 축을 넣었다.

- 최근 30일 손댄 횟수 상위 50: `_area_num` 22회 · `_auto_pred_tick` 9 ·
  `_boot_background` 9 · `_keiba_build_form` 8 · `_raw_profile_snapshot` 8
- 호출되는 횟수 상위 50: `_area_num` 46회 · `_mk` 21 · `_kra_meet_code` 13
- 🔴 **두 목록이 겹치는 14개가 최우선**이다.
  `_area_num` · `_canon_rk` · `_race_result_id` · `_kra_meet_code` · `_missing_results` 등.
- 🔴 나머지 460개는 **미분류로 남긴다**고 파일에 명시했다.

### 내가 한 번 틀렸다

`_git_touch_counts` 에서 `collections` 를 썼는데 그 모듈이 전역에 없었다.
예외가 잡혀 조용히 빈 dict 가 되어 **손댐이 전부 0** 으로 나왔다.
지역 import 로 고쳤다. 🔴 폴백이 결함을 안 보이게 한 전형이다(원칙 24).

### app.py 증가 (사실만)

- 7/30 이후 **추가 7,012줄 · 삭제 283줄 · 순증 6,729줄**. 거의 전부 추가다.
- 현재 39,078줄 중 주석 4,198줄(10.7%) · 빈줄 2,992줄(7.7%) · docstring 872블록.
- ⚠ 이것은 **전체 대비 비율**이지 증가분만 가른 값이 아니다.
  증가분의 주석 비율을 내려면 커밋별 diff 를 줄 단위로 분류해야 한다. 별도 작업이다.
- 판단은 하지 않는다.

---

# 예상 기록 — 쌍승역전을 거꾸로 재본다 (2026-08-12)

## 1부. 쉬운 설명

지금까지는 「쌍승역전이 지목한 말이 1착이냐」로만 쟀고 7.6% 였습니다. 나빴습니다.

그런데 이 신호의 본뜻은 「단승 1위가 실제로는 1위가 아니다」입니다.
그러면 지목마를 보는 게 아니라 **기준마가 떨어지는지**를 봐야 합니다.

기후 4경주에서 단승 1위 7번을 기준으로 다섯 건이 떴고 그 7번이 3착이었습니다.
반대로 야히코 4경주는 기준마가 1착이었고 지목 여섯이 전부 틀렸습니다.

## 2부. 예상 (실행 전에 적는다)

### 무엇을 재나
- 신호가 뜬 경주에서 **기준마(단승 1위)가 1착이 아닌 비율**.
- 신호가 안 뜬 경주의 같은 비율과 나란히 놓는다. 그것이 대조군이다.
- 건수별(1건 · 2~3건 · 4건 이상)로 나눈다.
- 지목마의 **3착권** 비율. 복승은 순서가 없으므로 1착만 보면 안 된다.
- 지목마 **둘이 함께** 3착권에 든 비율.

### 예상
- 🔴 **차이가 거의 없을 것으로 본다.** 앞서 잰 지목마 1착 7.6% 대 시장 45.8% 는
  격차가 너무 커서, 방향만 뒤집는다고 값이 생기기 어렵다.
- 기준마 탈락률은 신호 유무와 무관하게 **55~60%** 근처일 것이다.
  시장 단승 1위의 1착률이 40% 안팎이라는 것을 이미 쟀기 때문이다.
- 건수가 많을수록 탈락률이 오를 가능성은 있다. 다만 4건 이상은 표본이 얇을 것이다.
- 3착권으로 넓히면 지목마 적중은 오른다. 당연하다 — 기준이 느슨해졌기 때문이다.
  🔴 **오르는 것 자체는 의미가 없다.** 시장 대비로 봐야 한다.

### 틀릴 수 있는 지점
- 신호가 거의 전 경주에 뜨면 대조군이 안 만들어진다.
  앞서 잰 보유율이 428/2,635 였으니 그럴 위험은 낮다.
- 기준마(favorite)가 저장돼 있으므로 「단승 1위」를 따로 계산하지 않아도 된다.
  다만 그 값이 **신호가 본 시점의 단승 1위**라 마감 배당과 다를 수 있다.
- 표본 게이트(적중 30건)를 반드시 적용한다. 기후 4R 한 건으로 일반화하지 않는다.

## 3부. 결과

### 예상이 맞은 것
- 기준마 탈락률이 55~60% 근처일 것 → **57.8%**. 맞았다.
- 3착권으로 넓히면 오를 것 → 89.5%. 맞았고 **그래서 의미가 없다**는 것도 맞았다.
  지목마가 평균 4.6두라 무작위로 뽑아도 높게 나온다.

### 예상이 빗나간 것 — 방향이 반대다
- 🔴 **대조군이 70.5% 로 더 높았다.** 신호 뜬 쪽 57.8% 보다 **12.7%p 높다.**
  즉 **신호가 뜨면 기준마가 오히려 더 잘 1착한다.**
  「단승 1위가 실제로는 1위가 아니다」라는 신호의 본뜻과 결과가 반대다.
  ⚠ 「차이가 거의 없을 것」이라 적었는데 차이가 있었고, 방향이 반대였다.
- 🔴 **건수가 많을수록 나빠진다.** 1건 81.8% · 2~3건 82.4% · **4건 이상 52.2%**.
  「건수가 많을수록 탈락률이 오를 가능성」이라 적었는데 정반대다.
  기후 4경주는 5건이었고 그 구간이 가장 나쁘다 — **그 한 건이 우연이었다.**

### 남는 것 하나
- 2~3건 구간만 82.4% 로 전체 대조군(69.6%)보다 12.8%p 높다.
  ⚠ 51경주로 얇고, 1건·4건 이상과 방향이 달라 **우연일 수 있다.**
  단독으로 배선할 근거가 아니다.

### 판정
🔴 **쌍승역전은 거꾸로 재도 값이 없다.** 앞서 지목마 1착으로 쟀을 때(7.6% vs 시장 45.8%)와
같은 결론이고, 이번에는 **기준마 축에서도 역방향**임이 확인됐다.

---

# 예상 기록 — 부담중량과 성별 (2026-08-12)

## 1부. 쉬운 설명

암말이 숫말과 같은 무게를 지면 불리하다는 것이 경마의 기본입니다.
그래서 보통 2kg를 깎아줍니다.

그런데 같은 55kg이라도 400kg 말에게는 13.8%이고 550kg 말에게는 10%입니다.
절대 무게보다 **몸무게 대비 비율**이 진짜 축일 수 있습니다.

우리 데이터로 검증합니다.

## 2부. 예상 (실행 전에 적는다)

### 먼저 저장 실태
- 🔴 **성별은 저장되지 않을 것으로 본다.** `sexAge` 가 파서에는 있는데
  저장행에서 탈락한 목록에 들어 있었다(2026-07-30 스키마 드리프트 조사).
- `burdenWeight` · `bodyWeight` 는 8/11 배선분이라 **그 이후만** 있을 것이다.
  보유율은 낮을 것이고, 경륜에는 애초에 없다.
- 감량 표기(▲☆◇★)는 **저장 안 될 것**이다. 파서에서 본 적이 없다.

### 수치 예상
- 부담 대비 비율이 절대 부담중량보다 **결과와 더 잘 맞을 것**으로 본다.
  대표 지시의 논리가 물리적으로 타당하다.
- 성별 차이는 있을 것이나, **비율로 맞추면 상당 부분 사라질 것**으로 본다.
  암말이 대체로 가볍기 때문이다. 그러면 원인은 성별이 아니라 체격이다.
- 감량 2kg 검증은 **표본 부족으로 판정 불가**일 가능성이 높다.
  8/11 이후 데이터만 있고 성별이 없으면 애초에 못 가른다.

### 점수 경로
- 🔴 `totalScore` 조립(app.py 25541 계열)에 `wbonus`(부담중량 **변화**)는 있으나
  **비율은 없다**고 이미 확인했다. 성별도 없을 것이다.

### 틀릴 수 있는 지점
- 성별이 실제로 저장돼 있으면 예상이 틀린 것이고 그건 좋은 소식이다.
- 부담·마체중 보유율이 너무 낮으면 **작업 2·3·4 전부 판정 불가**로 끝난다.
  그 경우 「무엇을 배선해야 재게 되는가」를 답으로 낸다.
- 표본 30건 미만은 전부 판정 불가로 찍는다.

## 3부. 결과

### 저장 실태 (예상 대비)
- 🔴 성별 **0%** · 부담중량 **0%** · 감량 표기 **0%**. 예상보다 나쁘다.
- `bodyWeight` 만 179행(1.6%). 8/11 배선분이 경마에서만 채워진다.
- 파서가 아예 안 뽑는다 — `斤量`·`負担重量`·`減量` 이 app.py 에 **0회**.
- 작업 2·3·4 는 **부담+마체중 둘 다 가진 말이 0두**라 전부 측정 불가.

### 원인 (후속 조사에서 확정)
- 소스가 서로 다른 한쪽만 뽑는다:
  `keiba_nar` bodyWeight 20% / weight 0% · `oddspark` weight 90% / bodyWeight 0%.
- 그리고 `_form_from_starters` 의 키 35개에 **`weight` 와 `pastPlacings` 가 없다.**
  저장소에 90% · 59% 있는데 `form` 까지 안 온다.
- `past`(착차)는 원본이 전 소스 0% — 파서를 고쳐야 한다.

### 점수 경로
- `burdenWeight` · 성별 **없음**. `weight` 23회는 전부 마체중 **변화** 계열이다.
- 2026-08-10 확인과 같다. 부담 대비 비율은 계산식에 없다.

---

# 예상 기록 — 죽은 값 감지 배선 (2026-08-12)

## 1부. 쉬운 설명

확장이 보낸 배당이 몇 분 동안 한 글자도 안 바뀌는 일이 있었습니다.
마감 직전인데 멈춰 있으면 그건 살아 있는 시장이 아니라 죽은 값입니다.

8월 11일에만 다른 경주 배당이 들어온 사고가 세 건이었고 셋 다 값이 고정이었습니다.

## 2부. 예상 (실행 전에 적는다)

### 무엇을 하나
- 수신 시점에 **직전과 조합 집합이 완전히 같은 상태가 몇 분 지속됐는지** 잰다.
- 4분 이상이면 죽은 값으로 보고 **계수기만 찍는다.**
- 🔴 **관찰 모드다. 폐기하지 않는다.** 며칠 세고 나서 판단한다.

### 근거 (이미 측정한 것)
- oddspark: 3분 이상 고정 0.4% · **4분 이상 0건** · 최대 3.8분.
- private: 4분 이상 **3.8%** · 5분 이상 3.1% · 최대 177.7분.
- ⇒ 임계 4분에서 **oddspark 오탐 0건**이다.

### 예상되는 결과
- 계수기가 붙고, 확장 값에서만 간헐적으로 발동한다.
- 오늘은 경륜만 개최라 확장 유입이 적어 **발동 0일 수 있다.**
  그러면 미검증으로 남기고 다음 개최에서 본다.
- oddspark 에서 발동하면 **측정이 틀렸다는 뜻**이다. 그때는 임계를 다시 잰다.

### 틀릴 수 있는 지점
- 조합 집합 비교를 값까지 포함해서 해야 한다. 조합 목록만 같고 값이 바뀌는 경우를
  놓치면 과소 탐지가 된다.
- 마감 전후로 수집이 뜸해지면 「고정」과 「미수집」이 구분되지 않는다.
  틱 간격이 벌어진 것과 값이 안 바뀐 것은 다르다. 틱 수도 함께 남긴다.

## 3부. 결과

- 배선 완료. 처음에는 사고 세 건을 **하나도 못 잡았다** — 두 소스가 한 틱씩 번갈아 들어와
  이력을 거꾸로 훑다가 바로 앞이 다른 소스라 첫 비교에서 끊겼다.
  **같은 소스끼리만 이어 보도록** 고쳤다.
- 모리오카 2R **9틱 7.0분** 고정으로 잡힌다.
- 우라와 1R · 야히코 7R 은 고정이 **2.0분**이라 임계 미달. 확장 틱이 5개뿐이라
  애초에 4분을 채울 수 없었다. 🔴 **오래 고정된 것만 잡는다는 한계**를 그대로 남긴다.
- 🟢 **오탐 0** — 전 기간 oddspark 발동 0건 · private 만 680틱 37경주. 사전 측정과 일치.
- 실전 계수기는 아직 0(오늘 경륜만 개최). 다음 경마 개최일에 확인.

---

# 예상 기록 — form 키 두 줄 + 경주 전환 가짜 급락 (2026-08-12)

## 1부. 쉬운 설명

두 가지입니다.

하나는 저장소에 있는 부담중량과 과거착순이 분석까지 안 오는 것입니다.
키 두 줄을 옮기면 됩니다.

둘은 경주가 바뀔 때 직전 경주 배당과 비교해 **가짜 급락**이 만들어지는 것입니다.
기후 6경주에서 15조합이 15초 만에 60~94% 동시 급락으로 잡혔고 A등급이 됐습니다.

## 2부. 예상 (실행 전에 적는다)

### 승인 1 — 키 두 줄
- `_form_from_starters` 에 `weight` · `pastPlacings` 를 **옮기기만** 한다.
- 🔴 점수 계산은 안 건드린다. 그래서 **유력마와 점수가 안 바뀌어야 한다.**
  바뀌면 그 키가 어딘가에서 읽히고 있다는 뜻이고, 즉시 되돌린다.
- 스위치로 되돌릴 수 있게 한다.

### 승인 2 — 가짜 급락
- 🔴 **기존 방어가 이미 있다**(`_baseline_reset_needed` · `_next_race_surge`).
  그런데 기후 6R 이 뚫렸다면 **왜 안 걸렸는지가 먼저**다. 새 게이트를 만들기 전에 그것을 본다.
- invSum 2.061 은 환급률 206% 라는 뜻이라 **정상 시장에서 나올 수 없다.**
  정상 경주의 invSum 은 1.34 근처(환급률 74.5%)로 이미 쟀다.
- ⇒ invSum 게이트가 가장 좁고 확실할 것으로 본다. 다만 **정상 분포를 먼저 재고**
  임계를 정한다.

### 예상 수치
- 전 조합 동시 급락(80% 이상 조합이 -50% 넘게)은 **전체의 1~3%** 로 본다.
  드물어야 정상이다. 5% 를 넘으면 방어가 통째로 뚫린 것이다.
- invSum 1.6 초과는 **1% 미만**일 것이다. 정상 분포가 1.34 근처로 좁기 때문이다.

### 틀릴 수 있는 지점
- 키 두 줄을 넣었는데 점수가 바뀌면 예상이 틀린 것이고 **되돌린다.**
- 동시 급락이 흔하면(5%+) 그건 방어가 아니라 **급락 판정 자체**를 다시 봐야 한다.

## 3부. 결과

- 승인1: 키 두 줄 추가 완료. **40경주 대조에서 점수·등급·전적 변화 0건.** 순수 전달 확인.
- 승인2: 🔴 **환급률 게이트 기각.** 정상 oddspark 틱 12,129개에서 중앙 1.344 · 최대 1.423 ·
  **1.5 초과 0건**. 게이트로 쓰면 아무것도 안 걸린다. 기후 6R 도 1.31~1.34 로 정상이다.
  대표가 본 2.061 은 저장된 배당으로 **재현되지 않는다.**
- 기후 6R 실물: 같은 초에 **66 → 36 → 21 조합**(12두/9두/7두). 다른 경주 셋이 섞였다.
- 동시 급락 규모: 17,474틱 중 **36개(0.21%)**. 드물다.

---

# 예상 기록 — 상류에서 막는다 · 소스 URL 대조 (2026-08-12)

## 1부. 쉬운 설명

배당 오염이 매일 나옵니다. 지금까지 만든 방어는 전부 **들어온 뒤에** 이상한지 보는 것입니다.
그래서 새로운 유형이 계속 샙니다.

**들어오는 문 앞에서** 막습니다. 배당이 실려 온 주소에 경주 번호가 들어 있으니
그것과 저장하려는 경주가 같은지 대조합니다.

## 2부. 예상 (실행 전에 적는다)

### 작업 4는 이미 답이 나왔다
🔴 **딥 게이트는 걸렸다.** 66→36 은 54.5%, 36→21 은 58.3% 로 둘 다 문턱 아래이고
`combo_count_dip` 이 오늘 5회 발동했다. 마지막 사유가 "조합 36→21 (58%)" 로 그 경주다.
⇒ 「왜 안 걸렸나」라는 전제가 틀렸다. **걸렸는데 화면에만 그 순간이 그려졌다.**

### 작업 1·2 예상
- `oddspark` 와 `keiba.go.jp` 는 URL 에 경주 식별자가 있어 **뽑을 수 있다.**
- 🔴 **사설(ks1·asyukk)은 못 뽑을 것**으로 본다. 실측한 소스 문자열이
  `https://ks1.dke-d11diw.site/user_frame.php?PLACE_TYPE=JC` 였다.
  경기장도 경주번호도 없고 **종목 코드 하나뿐**이다.
- ⇒ 🔴 **오염의 대부분이 확장(private)에서 나오는데 그쪽을 못 막는다.**
  이 방어는 서버 수집분만 지킨다. 그 한계를 먼저 적어야 한다.

### 작업 3 예상
- oddspark 는 서버가 URL 을 직접 만들므로 **불일치가 0에 가까울 것**이다.
  즉 거부율은 낮지만 **막는 것도 거의 없다.**
- 그렇다면 이 게이트의 실익이 작다는 뜻이고, 정직하게 그렇게 보고해야 한다.

### 작업 5 예상
- 기후 7R 의 2번 열이 통째로 뛴 것은 **열 밀림 계열**로 본다.
  `_column_shift_suspect` 가 있는데 오늘 1회만 발동했다.
- 상류 대조로는 **못 잡는다.** 같은 경주 같은 소스인데 값만 어긋난 것이기 때문이다.

### 틀릴 수 있는 지점
- 사설 URL 에 경주 식별자가 있으면 예상이 틀린 것이고, 그건 좋은 소식이다.
- oddspark 에서 불일치가 실제로 나오면 서버 수집에도 문제가 있다는 뜻이다.

## 3부. 결과

- 🔴 **URL 이 아예 저장되지 않는다.** `src` 값이 `oddspark`·`private`·`netkeiba` 세 개뿐이고
  **URL 형태 0종**이다. 상류 대조는 불가능하다.
- 기후 7R 은 열 밀림이 아니라 **다른 경주 배당**이다. `2+4` 는 13.9→311.3 인데
  `1+3` 은 129.7→3.0 으로 **반대 방향**이고, 그 값이 **5분 고정**이다.
  🟢 오늘 배선한 **죽은 값 감지가 잡을 유형**이다.
- 두 소스가 다 있는 806경주 중 최대 마번 불일치 **58건(7.2%)**.
- 작업4 정정: 딥 게이트는 **걸렸다**(54.5% · 58.3%). 전제가 틀렸다.

---

# 예상 기록 — 자체검사 조합에 면제 부여 (2026-08-12)

## 1부. 쉬운 설명

승인받아 넣은 자체검사가 오늘 조합을 383번 넣었는데 **최종 명단에 하나도 안 남습니다.**
뒤에 있는 필터가 전부 잘라냅니다.

작동하는 것처럼 보이는데 실제로는 아무것도 안 하는 상태입니다.

## 2부. 예상 (실행 전에 적는다)

### 무엇을 하나
- 유력마 짝과 **같은 방식**으로 면제 표식을 준다(`_KP_MARK` 방식 그대로).
- 🔴 새 경로를 만들지 않는다. 바로 위 블록이 이미 그렇게 한다.

### 예상되는 결과
- 오늘 강등 6건이 최종에 남게 된다.
- 구좌가 는다. 소급 실측이 **+3%** 였으므로 그 정도로 본다.
- 판정 명단 `extra` 에 `pair` 가 잡히기 시작한다(지금 0건).

### 틀릴 수 있는 지점
- 구좌가 3%보다 훨씬 많이 늘면 **면제 범위가 넓은 것**이다.
  유력마 짝은 경주당 1개인데 자체검사는 최대 2개라 그럴 수 있다.
  그 경우 상한을 1로 줄이는 것을 검토한다.
- 면제를 줬는데도 안 남으면 **다른 곳에서 또 자르는 것**이고, 그때는 그 지점을 찾는다.

## 3부. 결과

🔴 **면제를 줬는데도 안 걸린다.** 그리고 `key_pair_exempt`(8/10 배선분)도 **오늘 0회**다.
- 면제가 안 걸린다 = 그 조합이 최종에 **이미 있다**는 뜻인데, 표식 붙은 조합은 0개다.
  ⇒ `final_q` 가 중간에 **새 리스트로 대체**된다. 조합은 남고 내가 붙인 dict 는 사라진다.
- 최종 명단 최다 사유가 **「T-5 확정 후 삭제 금지(복원)」 15건**이다.
  🔴 EV 필터가 아니라 **T-5 동결이 명단을 통째로 덮어쓴다.**
- 면제를 어디에 두든 **T-5 동결 뒤**여야 하는데 지금은 그 앞이다.
- 순서를 바꾸는 것은 판정 명단 구조 변경이라 **승인 대기로 멈췄다.**

---

# 예상 기록 — 확장 값을 표시·추천·신호에서 배제 (2026-08-12)

## 1부. 쉬운 설명

사흘째 같은 오염이 납니다. 지금까지 만든 것은 전부 **감지**이고 차단이 없습니다.

서버가 같은 시각에 정상 배당을 보내고 있는데 화면은 확장 값을 그립니다.
서버 값이 있으면 그쪽을 쓰도록 바꿉니다.

## 2부. 예상 (실행 전에 적는다)

### 무엇을 하나
- 분석이 읽는 배당(`curQ`)을 만들 때 **oddspark 스냅샷이 있으면 그것을 쓴다.**
- 확장만 있는 경주는 **배당 없음으로 두어 추천을 내지 않는다.**
  🔴 틀린 추천보다 안 내는 것이 낫다(대표 지시).
- ⚠ **저장은 두 소스 다 남긴다.** 사후 대조에 필요하다.
- 스위치와 계수기를 단다.

### 오탐 범위 (이미 잰 것)
- 8월 807경주: 두 소스 다 있음 **257(31.8%)** · 확장만 **74(9.2%)** · 서버만 476(59.0%)
- 두 소스가 다 있는 257건 중 **114건(44.4%)** 에서 확장 값이 마지막 = 화면이 그것을 그린다.
- 마감 3분 이내 틱은 서버가 **68.4%** 라 부작용이 작다.

### 예상되는 결과
- 그 114건에서 화면·추천이 서버 값으로 바뀐다.
- 확장만 있는 74건(9.2%)에서 **추천이 사라진다.** 이것이 이 안의 대가다.
- 계수기가 즉시 붙는다. 오늘 경륜 개최 중이라 발동을 볼 수 있을 것이다.

### 틀릴 수 있는 지점
- 확장만 있는 경주가 9.2%인데, 그 경주들이 **특정 경기장에 몰려 있으면**
  그 경기장 서비스가 통째로 멈춘다. 배선 전에 분포를 확인해야 한다.
- `curQ` 를 바꾸면 급락 계산 기준도 바뀐다. 급락 신호가 줄어들 수 있다.
  그건 의도한 것이다(가짜 급락이 준다). 다만 **정상 급락도 줄면** 과교정이다.

## 3부. 결과

🔴 **예상한 위험이 실제였다 — 배선 전에 잡았다.**
- 서울 20/20 · 부산 15/15 · 제주 13/13 이 **전부 확장만**. 한국 경마는 서버 경로가 없다.
  그대로 배제하면 **한국 서비스가 통째로 멈춘다.**
- 확장만 75경주 중 **70경주에 추천이 나갔다.**
- ⇒ 수정안을 좁혔다: **서버 경로가 있는 종목에서만** 배제한다.

### 종목별 서버 경로 (실측 확정)
- `cycle/cycle` **460경주 전부 서버** — 100%. (내가 「경륜에도 경로가 있나」를 확인 못 했던 부분)
- `horse/japan_local` 267 중 241 — **90.3%**
- `horse/japan_central` 64 중 63 — **98.4%**
- 🔴 `horse/korea` 48 중 **0** — 0%. 서버 경로 자체가 없다.
⇒ 판정 기준은 **`category != korea`** 하나면 된다.

### ✏️ 내가 틀린 것
쿠마모토·마츠야마·도다·도쿠야마를 한국과 묶어 말했는데 **그 넷은 일본 지방경마**다.
종목에는 경로가 있는데 그 경기장만 빠진 것이라 **성격이 다르다** — 수집 결손이다.

### 수집 결손 26건
서버 경로가 있는 종목인데 확장만인 경주. 쿠마모토 6 · 소노다 3 · 카나자와 3 · 마츠야마 3 ·
우라와 2 · 오비히로 2 · 후나바시 2 …
⚠ 날짜가 **8/6~8/9 에 몰려** 있고 8/10 이후는 거의 없다. 그 사이 무언가 고쳐진 것으로 보인다.
⚠ 후나바시 두 건은 확장 틱이 **188·300개**인데 서버가 한 번도 안 붙었다.

---

# 예상 기록 — 서버 우선 배선 (2026-08-12)

## 2부. 예상 (실행 전에 적는다)

### 무엇을 하나
- 분석이 읽는 배당을 만들 때, **한국이 아니고** 최근 이력에 oddspark 틱이 있으면
  **그 값을 쓴다.** 확장 값은 저장만 되고 분석에는 안 들어간다.
- 한국(`category=korea`)은 **손대지 않는다.**
- 확장만 있는 경주도 **그대로 쓴다**(대표 승인분 · 배제하지 않는다).
- 스위치와 계수기를 단다.

### 예상되는 결과
- 두 소스가 다 있는 257건 중 확장이 마지막인 **114건**에서 분석 배당이 바뀐다.
- 계수기가 오늘 바로 붙을 것이다(경륜 개최 중이고 경륜은 서버 100%).
- 한국은 발동 0이어야 한다. 발동하면 조건이 틀린 것이다.

### 틀릴 수 있는 지점
- oddspark 틱이 **너무 오래된 것**이면 낡은 값을 쓰게 된다.
  시간 제한을 둬야 한다. 5분 정도로 본다.
- `_curq_shrink_guard` 와 순서가 겹친다. 그쪽이 먼저 돌면 결과가 달라질 수 있다.

## 3부. 결과

(실행 후 채운다)

## [2026-08-12] 확장 수정 — 긁는 동안 경주가 바뀌면 버린다

### 2부 · 예상 (실행 전)

확인한 것:
- payload 에 `source: location.href` 는 **이미 있다**. 서버가 안 쓰고 있었을 뿐이다.
- POST_TRIPLE 전송부는 3곳(1200 keiba · 2334 사설주력 · 2422 매트릭스).
- raceKey 는 **수집 시작 전 한 번** 뽑고 긁는 동안 그대로 쓴다 → 이것이 오염 구조.

예상:
1. 긁기 직후 raceKey 를 다시 뽑아 대조하면, 바뀐 경우 전송을 버릴 수 있다.
   발동률은 낮을 것으로 본다(경주 전환은 20분에 한 번). 0~2% 예상.
2. 식별자(날짜·경주번호)를 payload 에 실으면 서버가 2차 대조할 수 있다.
3. 서버 대조는 **식별자가 있을 때만** 한다. 옛 확장(재로드 전)은 식별자가 없으므로
   그대로 받아야 한다 — 안 그러면 재로드 전까지 수집이 통째로 멈춘다. 🔴 이것이 가장 큰 위험.
4. 거부는 조용히 하지 않는다. 계수기와 로그에 남긴다.

미리 밝혀둘 한계:
- 🔴 확장을 고쳐도 **대표가 브라우저에서 재로드해야** 반영된다. 그 전에는 옛 코드가 돈다.
- 🔴 오늘 안에 발동을 볼 가능성은 낮다. 발동률이 낮은 것이 정상이다.

### 3부 · 결과 (확장 수정)

배선 완료. 문법·서버 200 확인.

- payload 에 `source: location.href` 는 **이미 있었다**(서버가 안 쓰고 있었을 뿐).
- 진짜 구멍은 raceKey 를 수집 시작 전 한 번만 뽑는 것이었다.
- 전송부 3곳 전부에 재확인 배선.
- 서버 대조 실호출 3종 정상(없음 200 · 일치 200 · 불일치 409).
- 🟢 예상 3(옛 확장 호환)이 맞았다 — rkVerify 없으면 통과 확인.
- 🟢 부수: server_odds_prefer 첫 실발동 (소노다 8경주 14:27:05).
- 🔴 예상대로 확장 재로드 전에는 옛 코드가 돈다.

## [2026-08-12] 소노다 3R 화면 모순 아홉 가지 — 조사 (코드 무변경)

### 2부 · 예상 (실행 전)

② 같은 말이 복병이면서 제거:
   두 목록이 **서로 다른 함수**가 만들고 서로를 안 본다고 본다.
   복병은 배당 급락 계열, 제거는 무변동 계열이라 **입력 시점이 다를** 것으로 본다.
   🔴 "어느 쪽이 맞나"는 시점이 다르면 **둘 다 맞을 수 있다**.

④ 신호 0개인데 집중급락 13~20회:
   "신호"와 "집중급락"이 **다른 것을 세고 있다**고 본다.
   신호는 strong_signals(임계 통과분), 집중급락은 급락 횟수 누적일 것이다.
   ⚠ 확신도 두 값(30.0 / 24.2)은 동결 전후일 가능성.

③ 배당이 목록마다 다름:
   각 목록이 **다른 틱**을 읽는다고 본다. 히라츠카 4R 12틱 문제와 같은 계열.

⑧ 유력마 1위가 추천에 없음:
   `_final_picks` 가 key_horses 를 인자로 받지 않는 구조 결함(8/10 기록)의 재현으로 본다.

미리 밝힐 한계:
- 🔴 화면 캡처가 아니라 **저장값**으로 조사한다. 화면은 마감 후 재계산일 수 있다(원칙 8).
- ⚠ 저장값과 화면이 다르면 그 자체가 별건이다.

### 3부 · 결과 (소노다 3R 조사)

- ② 예상 적중 — 두 목록이 다른 함수 산출물이고 서로 안 본다.
  🔴 다만 예상보다 나쁘다: 「죽은 인기」는 elimination 이 아니라 `_flow_scores`(7949) 산출물이고
  진짜 `elimination.eliminated` 는 **빈 배열**이다. 제거된 말이 0마리인데 화면은 제거라 부른다.
- ④ 예상 적중 — 다른 것을 센다. 🔴 예상보다 심하다: 숫자가 **셋**이다.
  signals 15 ↔ count 4(고유 타입) ↔ raceGrade.signalCount 0.
- ③ 예상 적중 — horses[].odds 가 9마리 전부 None. 화면 배당은 전부 다른 소스.
- ⑧ 🔴 예상 빗나감 — key_horses 구조 결함이 아니었다. 9번은 8조합 중 3개에 **있다**.
  화면에 상위 3개만 보인 것이다. (정답 9+7 이 없는 것은 사실)
- 원칙 8 확인 — 화면과 저장값이 실제로 달랐다. 화면은 여러 시점을 섞어 그린다.

## [2026-08-12] 확장 배당 완전 차단 — 배선

### 2부 · 예상 (실행 전)

🔴 가장 큰 위험을 먼저 적는다:
   "확장만 있는 경주는 추천을 내지 말 것"을 그대로 적용하면
   **한국 경마가 통째로 멎는다**(서버 경로 0% · 8월 48경주 전부 확장만).
   ⇒ 서버 우선 때와 같이 **한국은 제외해야 한다**고 본다.

예상:
1. 확장만 74건 중 상당수가 한국일 것으로 본다. 한국을 빼면 실제 대상은 훨씬 적다.
2. SERVER_ODDS_PREFER 로 「서버가 있으면 서버」는 **이미 절반 됐다**.
   남은 것은 「확장만이면 추천 안 냄」 하나다.
3. 매트릭스와 finalQuinellas 가 다른 값을 읽는 것은
   매트릭스가 triple_store 활성 캐시를, 추천은 분석 시점 curQ 를 읽기 때문으로 본다.

⚠ 함수 통과로 보고하지 않는다. 실경주에서 발동을 봐야 한다.

### 3부 · 결과 (확장 배당 차단)

- 🟢 예상 1 적중 — 확장만 77건 중 **48건(62.3%)이 한국**. 빼면 실제 대상 29건.
  🔴 그대로 막았으면 한국 경마 추천이 통째로 멎었다.
- 🟢 예상 2 적중 — 서버 우선으로 절반은 이미 됐고, 남은 것은 「확장만이면 추천 안 냄」 하나였다.
- 🟢 예상 3 적중 — 매트릭스는 `triple_latest` 가 `rec["quinella"]` 원본을 그대로 반환한다(가드 미통과).
  추천은 `cur_q`(가드 후). 카사마츠 3R 에서 매트릭스만 오염이던 이유가 이것이다.
- ✏️ 내 시험 코드가 한 번 틀렸다 — `_triple_load()` 를 고정하지 않아 주입이 반영 안 됐다(원칙 8-D).
  고친 뒤 4종 전부 정상.
- 🟢 경륜 0건 — 영향 없음. 실데이터 9경주 차단 0(전부 서버 틱 보유).

## [2026-08-12] 매트릭스에 오염 교정 태우기 — 승인분

### 2부 · 예상 (실행 전)

작업1(범위):
- 🔴 바뀌는 경주는 **적을 것**으로 본다. `_curq_shrink_guard` 는 「마지막 틱이 직전보다 좁을 때」만
  발동하고, 실측 발동률이 낮았다. **5% 미만**을 예상한다.
- 바뀌면 확장(private) 비중이 큰 경기장에 몰릴 것으로 본다.
- ⚠ 예상보다 크면(20% 초과) 멈추고 보고한다.

작업3(확인):
- 🔴 **추천 조합은 바뀌지 않아야 한다.** 추천은 이미 교정값을 쓰고 있다.
  바뀌면 내가 잘못 태운 것이므로 즉시 되돌린다.
- 등급·확신도도 안 바뀌어야 한다(같은 이유).

함께 처리할 화면 결함 둘:
- 「제거」 라벨 → 「흐름 약함」. 저장 키는 안 건드리고 **표시 문구만** 바꾼다.
- 신호 개수 → 항목 수와 종류 수가 다르다는 것을 화면에 명시.
⚠ 둘 다 표시 계층이라 판정·성적에 영향이 없어야 한다.

### 3부 · 결과 (매트릭스 교정)

- 🟢 예상 적중 — 바뀌는 경주 **2.3%(866 중 20건)**. 5% 미만 예상 안.
- 🟢 예상 적중 — 실경주 6건 매트릭스·추천 **둘 다 무변경**. 부작용 없음.
- 🟢 주입 시험에서 21조합 → 36조합 실발동. 계수기 발동 1.
- 🔴 내 측정이 **두 번** 틀렸다(원칙 8-D · 8-E):
  ① `_curq_shrink_guard` 의 `return None` = **무변경**인데 빈 값으로 세어 100% 로 나왔다.
  ② 저장 파일은 `{"1+2":5.4}` dict · 캐시는 `[{combo,odds}]` list — `_odds_map_un` 은 list 만 받는다.
  둘 다 **함수 본문과 원자료를 직접 열어** 고쳤다.
- 화면 문구 둘 함께 처리 · 캐시 버전 20260812a 로 올림.

## [2026-08-12] 삼복승 세 번째 자리 — 소급 측정 (배선 금지)

### 2부 · 예상 (실행 전)

- 🔴 대표 관찰이 맞다면 **정답 3착마가 유력마 밖**이므로,
  후보 B(확신도 1위 · 유력마 밖)와 A(급락 1위)가 현행보다 나을 것으로 본다.
- ⚠ 그러나 삼복승은 **적중이 적어 대부분 판정 불가(30건 미만)** 가 나올 것으로 본다.
  경륜 삼복승 확정배당 보유율이 57.7% 로 낮다는 기록이 있다.
- 조합 개수는 늘릴수록 적중률↑ 회수율↓ 이 나올 것이다(이미 여러 번 확인된 방향).
- 🔴 회수율이 판정선 74.5% 를 넘는 안은 **없을 가능성이 높다**.
  삼복승은 구좌가 크고 적중이 드물다.

미리 밝힐 한계:
- 🔴 `measure_recovery` 는 복승 기준이라 삼복승 정책이 없다. 규칙 상수만 import 하고
  삼복승 계산은 새로 짠다 — 그 자체가 재현 위험이다(원칙 3). 재현 못 한 것을 명시한다.
- ⚠ 대표가 든 4경주는 **선택 편향**이다. 전수로 재야 한다.

### 3부 · 결과 (삼복승 세 번째 자리)

- 🟢 예상 적중 — 회수율 **전 후보 판정 불가**. 삼복승 확정배당 53/1296(4.1%).
- 🔴 예상 빗나감 — 유력마 밖에서 뽑는 후보 B 가 **가장 나쁘다**(5.7% ↔ 현행 9.7%).
  대표 관찰(정답 3착마가 유력마 밖 54.5%)은 **맞다**. 그러나
  「정답이 유력마 밖에 많다」와 「유력마 밖에서 뽑으면 맞는다」는 **다른 말**이다.
- 1조합 적중률: 다이아 11.1(발동713) > 급락1위 10.5 > 복병 10.1 > 현행 9.7 > 3착사냥 7.5 > B 5.7
- 3조합에서는 현행 24.2% 최고 — 조합 수에 따라 순서가 뒤집힌다.
- 대안 축(축1+급락상위2) 기각 — 적중 114 → 60 반토막.
- 🔴 내 측정이 두 번 틀렸다: 회수 분모에 배당 없는 경주 포함 / bmedSpecial·third_place_hunt 형식 오인.
- ⇒ **채택 가능한 안 없음.** 확정배당 확보가 선행 조건.

## [2026-08-12] 삼복승 확정배당 채우기

### 2부 · 예상 (실행 전)

- 🔴 복승은 잘 차 있고 삼복승만 낮을 것으로 본다. 그렇다면 **파서가 아니라 저장 누락**이다.
  CLAUDE.md 기록에 「파서는 이미 있었고 다른 데서 호출 중인데 결과 백필 경로에서만 빠졌다」는
  같은 유형 전례가 있다(경마 payouts 0.9% 사건).
- 종목별로는 경륜이 낮을 것으로 본다(기록: 경륜 삼복승 배당 보유 57.7%).
- `payouts_raw` 에 원문이 남아 있으면 **소급 복원이 가능**하다. 그쪽을 먼저 본다.
- ⚠ raw 에도 없으면 외부 소스 재조회가 필요하고, 그건 경주 수만큼 요청이 든다.

미리 밝힐 한계:
- 🔴 소급이 안 되면 며칠이 필요한지 계산해 보고한다(대표 지시).

### 3부 · 결과 (삼복승 확정배당)

- 🔴 예상 빗나감 — 저장 누락도 파서 문제도 아니었다. **종목별 키 이름이 다르다.**
  중앙만 `trio`, 경륜·지방·사설은 `trifecta` 에 3連複. 중앙의 `trifecta` 는 3連単(다른 마권).
- 🔴 그 규칙이 **app.py 21428 주석에 2026-08-01 자로 이미 있었다.** 내가 못 보고 `trio` 만 셌다.
  ⇒ 원칙 8-E 재발 — 「없다」고 하기 전에 코드·원자료를 먼저 본다.
- 🟢 보유율 4.1% → **96.8%**. 백필 불필요. 소급 계산도 즉시 가능.
- 🟢 재측정 결과 **A 급락1위가 대표 기준을 유일하게 통과**:
  회수율 73.1%(현행 62.4%) · 적중률 10.4%(현행 9.6%) · 3제외 62.4%(현행 55.3%).
  ⚠ 절대 판정선 74.5% 는 1.4%p 미달.
- 축 대안 기각 확정 — 71.3% → 50.5%.

## [2026-08-12] 삼복승 후보 A 관찰 모드 + 페이크 감점 측정

### 2부 · 예상 (실행 전)

작업1(배선):
- 후보 A = 복승 본선 1위 두 마리 + 급락 1위 말, 한 조합.
- 🔴 판정 명단(displayedCombos)에 **넣지 않는다.** 회수율에도 안 들어간다.
  `corePicks.trioObserve` 로 따로 남기고 화면에는 「관찰」로 표시한다.
- ⚠ 현행 보험(finalTrifectas)은 **한 줄도 안 건드린다.**

작업3(겹침률):
- 🔴 현행 보험과 **같은 조합만 고르면 관찰할 값이 없다.**
  소급으로 겹침률을 먼저 재야 한다. 30~70% 겹침이면 관찰 가치가 있다.
  90% 넘게 겹치면 배선해도 의미가 없다 — 그때는 그대로 보고한다.

함께 볼 것(페이크 감점):
- 🔴 `_flow_scores` 가 페이크 반등에 -15 를 준다(app.py 8011 부근).
- 대표가 든 2건은 **선택 편향**이다. 전수로 재야 한다.
- 예상: 페이크 감점 대상의 3착권 비율이 전체 평균과 **큰 차이가 없을** 것으로 본다.
  차이가 없으면 감점 근거가 약하다는 뜻이다.
- ⚠ 적중 30건 미만이면 판정 불가로 찍는다.

### 3부 · 결과 (관찰 배선 + 페이크 감점)

- 🟢 예상 적중 — 겹침률을 먼저 재야 했다. 새 조합 **37.4%** 로 관찰 가치 있음.
- 🔴 예상 빗나감 — 페이크 감점은 **차이가 있었다.**
  되오른 말 3착권 **24.1%** ↔ 그 외 **30.4%** (-6.3%p). **감점이 정당하다.**
  대표가 든 2건은 선택 편향이었다.
- ⚠ `flowScores` 가 저장되지 않아 `advanced.horseStreaks.rebounded` 로 대체 측정.
  분모가 181경주(12.3%)로 얇다 — 그대로 밝힌다.
- 배선은 displayedCombos 조립 **뒤**에 붙여 판정에 섞일 수 없게 했다.

## [2026-08-12] 1번 파서가 마지막 마번을 놓친다 — 조사

### 2부 · 예상 (실행 전)

- 2·3번은 **이미 처리했다**(SCOREBOARD_SEAT_COUNT · MATRIX_SHRINK_GUARD + 화면 문구 둘).
  대표가 그 사이 등록한 명령이라 중복이다. 확인만 하고 넘어간다.
- 1번: 오즈파크 그리드 파서(`_oddspark_grid_combos`)의 **열 추적**이 원인일 것으로 본다.
  CLAUDE.md 기록에 「복승 파서 조합 누락 3.2% · 특정 마번 1개의 전체 행이 통째 누락」이 이미 있다.
  🔴 그 기록은 「첫 배당은 전용 행이 없는 최소 차번(implicit)」 복원 실패를 의심하고 있었다.
  ⇒ 이번 관찰(마지막 마번)이 그것과 **같은 건인지 반대편인지**를 갈라야 한다.
- 발생률은 3~7% 대일 것으로 본다.
- ⚠ arity_guard 가 못 잡는다는 대표 지적은 맞다. nC2 비율이 100%로 통과한다.

미리 밝힐 한계:
- 🔴 원본 HTML 이 저장되지 않아 **사후 재현이 불가능**하다. 다음 개최일 라이브가 필요하다.

### 3부 · 결과 (1번 마지막 마번)

- 🔴 예상 빗나감 — 오즈파크 파서 문제가 **아니다**. 소스가 oddspark 4 · private 5로 반반.
- 🔴 예상 빗나감 — 발생률 1.0%(9/872)로 예상(3~7%)보다 낮다.
- 🔴 결정적 — 같은 경주 안에서 **있는 틱과 없는 틱이 섞인다**(몬베츠 8R 14/78).
  파서가 구조적으로 못 읽는다면 전부 없어야 한다. 다른 원인이다.
- 9건 분해: 파서 누락 의심 7 · 취소마로 보임 2.
- 🔴 두수 대조 가드는 **지금 만들 수 없다** — 취소마 2건이 오탐(22%). 원칙 20.
- 2·3번은 이미 배선돼 있었다(중복 명령).

## [2026-08-13] 4번 판정선 175% 소급 측정

### 2부 · 예상 (실행 전)

- 🔴 대표 지적은 산술적으로 맞다. 적중률 57.8%면 손익분기 173%다.
  다만 **적중률 57.8%가 어디서 나온 값인지** 확인이 필요하다 — 전체 적중률로 다시 잰다.
- 임계를 올릴수록 남는 경주가 급격히 줄 것으로 본다.
  「적중 시 회수」는 조합 수가 적을수록 커지므로, 175% 이상은 **1~2조합 경주**에 몰릴 것이다.
- 🔴 그래서 「회수율은 오르는데 경주가 안 남는다」가 될 가능성이 높다.
  상품이 되는지(남는 경주 수)를 반드시 함께 본다 — 대표 지시.
- ⚠ 적중 30건 미만이면 판정 불가로 찍는다.

미리 밝힐 한계:
- 🔴 `_stake_plan` 결과가 저장되지 않으므로 **소급 재계산**이다.
  실제 배분은 15배+ 가중(×6)을 쓰는데, 이 측정의 「적중 시 회수」는 역수 기준이다.
  두 값이 다르다는 점을 명시한다(원칙 3 — 재현 못 한 것).

### 3부 · 결과 (4번 판정선)

- 🔴 대표 지적은 맞다. **더 심하다** — 손익분기가 173%가 아니라 **270%**(적중률 37.1%).
- 🟢 예상 적중 — 임계를 올릴수록 경주가 급감(250%에서 27.3%만 남음).
- 🔴 결정적 — **3제외가 62.6~64.8%로 평평하다.** 250%의 74.9%는 상위 3건이 만든 값이고
  3제외에서는 오히려 **가장 낮다(62.6%)**. 임계 상향은 실질 개선이 아니다.
- ⇒ 임계 상향 근거 약함. 다만 150%를 「권장」이라 부르는 표기는 정정이 필요하다.

## [2026-08-13] 5번 다이아 조합 판정 편입 — 측정

### 2부 · 예상 (실행 전)

- ⚠ 8/11 에 이미 `DIA_JUDGE_ENABLED=True · TOPN=1` 로 배선했다.
  그런데 대표는 "전부 판정 제외나 강등"이라 한다 → **배선이 안 타고 있을 가능성**이 크다.
  8/11 기록에 이미 「💎 reach 38 · fire 0」이 있었다. 그 원인이 중복 제외였는지 확인한다.
- 🔴 예상: 💎 상위1이 **이미 판정 명단에 있는 경우가 많아** 새로 더해지는 것이 적을 것이다.
  그러면 계수기가 0인 것이 설계대로이고, 대표가 본 것은 다른 현상이다.
- 회수율은 8/11 소급값(💎 단독 60.3% · 3제외 48.7%)과 비슷할 것으로 본다.
  ⇒ 더하면 전체 회수율은 내려가고 배당중앙은 오를 것이다.

미리 밝힐 한계:
- 🔴 `bmedSpecial` 은 조합이 여러 개다. 상위 1개만 쓴다(배선과 같은 기준).

### 3부 · 결과 (5번 💎 판정 편입)

- 🟢 예상 적중 — 배선은 정상 작동 중(8/11~13 로그 281건 중 157건에 `extra`).
  계수기가 비어 보인 것은 **날짜 전환 초기화**였다. 고장 아님.
- 🔴 대표가 든 3경주는 원인이 다 달랐다:
  기후 4R — 💎가 정답 맞음. 그러나 **💎 두 번째**라 TOPN=1 설계로 빠짐.
  우라와 10R · 몬베츠 10R — 💎가 **아예 없다**(제외가 아니라 미생성).
- 🟢 예상 적중 — 더하면 회수율 67.7→66.0%, 3제외 64.8→63.1%로 **내려간다**.
  추가분만 56.5% · 3제외 37.6% · 배당중앙 12.9(판정불가·적중19).
- 추가 측정 — 정답이 💎 안에 있는 경주 51/741(6.9%).
  그중 1번째 28(54.9%) · **2번째 18(35.3%)** · 3번째 4.
  ⇒ TOPN 2 로 늘리면 +18건을 더 잡지만 구좌도 그만큼 는다.

## [2026-08-13] 어디서 이기고 있는가 — 조각별 측정

### 2부 · 예상 (실행 전)

- 🔴 **3제외 100%를 넘는 조각은 없을 것**으로 본다.
  전체 3제외가 64.8%이고, 임계를 올려도 62~65%로 평평했다.
  조각을 잘라도 그 평평함이 유지될 가능성이 크다.
- 만약 넘는 조각이 나온다면 **적중 30건 미만일 것**으로 본다(우연).
  그래서 건수를 반드시 함께 본다.
- 유망 후보는 조합 1개 × 저배당대. 다만 그건 저배당이라 배당중앙이 낮다.
- 🔴 **없으면 없다고 분명히 말한다. 억지로 찾지 않는다**(대표 지시).

함께 고칠 것:
- 「매수 권장 150%」 → 「참고」. 손익분기 270% 아래인데 권장이라 하면 오해를 부른다.
  ⚠ 표시 문구만 바꾼다. 계산·판정 무개입.

### 3부 · 결과 (조각별 측정)

- 🟢 예상 적중 — **3제외 100%를 넘는 조각은 0개다.**
- 🟢 예상 적중 — 100% 넘어 보인 둘(2개×6~10배 111.3% · 1개×10배+ 304.4%)은
  3제외에서 64.7%/0%로 무너지고 적중 15건·1건이라 판정 불가였다.
- 최고 조각 경륜 66.4%(843경주) ↔ 전체 64.8% — 차이 1.6%p뿐.
- 🔴 뜻밖 — **압축(축 명확)이 26.8%로 가장 나쁘다**(적중 22, 판정불가).
- 「매수 권장 150%」 → 「참고」로 표기 정정. 문법·HTTP 200 확인.

## [2026-08-13] 우리 강점을 회원 화면에 — 조사 + 문구안 (배선 없음)

### 2부 · 예상 (실행 전)

- 🔴 「없는 것은 만들지 말라」가 핵심이다. **보유율부터 실측**한다.
  보유율이 낮은 항목으로 문구를 만들면 대부분 경주에서 빈 줄이 된다.
- 예상 보유율: 추천 이력 높다 · 급락 원자료 높다 · T-5 복원 낮다 · 오염 감지 매우 낮다.
- 🔴 오염 감지는 발동률이 1~5% 대라 **문구로 쓰기 어려울 것**으로 본다.
  그렇다면 그것은 빼고 보고한다.
- ⚠ 「적중을 암시하는 말 금지」 — 회수율·적중률이 전 조각에서 지고 있으므로(오늘 측정)
  성과를 암시하는 문구는 **사실과 어긋난다**. 과정만 쓴다.

작업3 위치:
- 한 경주 두 줄 상한. 조합 옆은 이미 근거 문구가 있어 복잡하다 → **카드 아래** 제안 예정.

⚠ 배선하지 않는다. 문구안까지만이다.

### 3부 · 결과 (강점 노출 문구안)

- 🟢 예상 적중 — 배당 오염 감지는 **사흘간 0건**이라 못 쓴다. 뺐다.
- 🟢 예상 적중 — 추천 이력·급락 원자료는 보유율이 높다(98.4% · 93.5%).
- 🔴 예상보다 나았던 것 — T-5 복원 75.7% · 마감신호 추가 78.0%.
  「낮을 것」으로 봤는데 넷 중 셋 경주에 있다. 쓸 수 있다.
- 갱신 틱수 99.0%가 가장 안정적 → 1줄 고정용으로 적합.
- 문구안 5개 · 위치안(카드 아래 2줄, 2줄째는 택1·없으면 미출력) 제출.
- ⏸ 배선하지 않았다.

## [2026-08-13] 조합 수 상한 — 대표 지적 측정

### 2부 · 예상 (실행 전)

- 🔴 대표 산수는 맞다. 균등 매수면 필요 배당 = 조합 수다.
- 예상: 적중 경주의 **절반 이상**이 회수 100% 미만일 것으로 본다.
  현행 적중 배당중앙이 2.5배인데 평균 조합 수가 2.3개다 — 아슬아슬하다.
- 조합 수가 많을수록 손해 비율이 급격히 오를 것이다(6조합이면 6배 필요).
- 🔴 상한을 걸면 **적중률은 크게 떨어지고 회수율은 오를 것**으로 본다.
  다만 3제외에서도 오르는지가 판단 기준이다(오늘 임계 측정에서 3제외는 평평했다).
- ⚠ 표시 순서 앞쪽을 남기는 방식이라, 「배당 낮은 순」이 아닐 수 있다.
  잘리는 것이 무엇인지 함께 밝힌다.

### 3부 · 결과 (조합 수 상한)

- 🔴 예상 빗나감 — 손해 적중은 절반이 아니라 **24.5%(126/515)** 였다. 그래도 넷에 하나다.
- 🟢 예상 적중 — 상한을 걸면 적중률↓ 회수율↑.
  🟢 예상보다 나았다: **3제외도 함께 오른다**(64.8 → 67.7%). 오늘 유일한 안이다.
- 🔴 3조합이 가장 나쁘다 — 손해율 64.6%(배당중앙 2.0 ↔ 본전선 3배).
- 손해 적중 **126 → 2건**. 구좌는 40% 감소.
- 고정 상한(1·2·3개)은 전부 대표안보다 못하다 — **배당을 보고 정하는 것이 낫다.**
- ⚠ 손익분기 270%에는 여전히 미달. 지는 폭이 주는 것이다.
- ⏸ 배선하지 않았다.

## [2026-08-13] 조합 수 상한 배선 (승인)

### 2부 · 예상 (실행 전)

작업1(상한):
- displayedCombos 조립부에서 **자른다**. finalQuinellas 는 건드리지 않는다(생성물 보존).
- 🔴 삼복승·💎 관찰은 상한 밖 — 복승만 자른다(대표 지시).
- 🔴 자르는 순서가 중요하다. 저장 순서 그대로 앞에서 남긴다(측정과 같은 방식).
  ⚠ 배당 낮은 순이 아닐 수 있다 — 측정도 그 방식이었으므로 일치한다.
- 상한 판정용 「시장 최저 배당」은 finalQuinellas 의 최저 odds 를 쓴다(측정과 동일).

작업2(화면):
- `payouts.quinella / 구좌수` 를 계산해 카드에 함께 낸다.
- 100% 미만이면 「적중」을 크게 쓰지 않는다.
- 소급 표시 — 저장값으로 계산 가능하므로 과거도 자동으로 같은 기준이 된다.

작업3 검증 예상:
- 🔴 실제가 측정값(구좌 1805 · 적중률 30.5% · 회수율 72.1% · 손해적중 2)과
  **완전히 같지는 않을 것**으로 본다. 측정은 이미 저장된 displayedCombos 를 잘랐고,
  배선은 생성 시점에 자르므로 그 뒤 로직(빈 명단 폴백 등)이 다르게 걸릴 수 있다.
  ⚠ 다르면 그 차이를 설명할 수 있어야 한다. 설명 못 하면 되돌린다.

### 3부 · 결과 (조합 수 상한 배선)

- 🟢 실발동 — 기후 5경주 09:40:30 · 최저 3.5배 → 4조합에서 2조합. 도달 98 · 발동 2.
- 🟢 화면 검산 — 구마모토 11경주(6조합 2.3배 = 38%)가 「맞았지만 손해」로 나온다.
- 🔴 내 필드명 오인 1회 — `prediction.combos`/`result.quinellaOdds` 로 봤으나
  실제는 카드 최상위 `combos`·`quinellaOdds`. 응답을 열어 고쳤다(원칙 8-E).
- ⚠ 8/12 카드 기준 손해율 **53.7%** ↔ 소급(판정명단) 24.5% — **세는 대상이 다르다.**
  카드에는 조합이 더 많이 담긴다. 어느 쪽도 틀린 게 아니다.
- 🟢 예상 적중 — 배선 결과가 소급값과 같은지는 **오늘 경주 종료 후**에야 확인된다.

## [2026-08-13] 상한이 finalQuinellas 에 안 걸린다 — 원인 확인

### 2부 · 예상 (실행 전)

- 🔴 대표 지적이 맞을 것으로 본다. 나는 `displayedCombos` 만 잘랐고
  `finalQuinellas` 는 **일부러 안 건드렸다**(생성물 보존).
  그런데 화면이 `finalQuinellas` 를 읽으면 회원은 잘리기 전 개수를 본다.
- 판정은 `displayedCombos` 로 한다(회수율 측정이 그것을 쓴다).
- 🔴 그러면 **판정과 화면이 다른 필드**를 보는 것이고, 그게 진짜 결함이다.
- 카톡은 어느 것을 쓰는지 확인이 필요하다 — 셋이 다르면 통일이 선행이다.

판단 예상:
- 🔴 「화면이 picks 를 읽게」가 맞다고 본다. `finalQuinellas` 를 자르면
  💎·라인짝 편입과 사후 대조 자료가 사라진다(생성물이라 남겨야 한다).
- ⚠ 다만 화면 수정 범위가 크면 finalQuinellas 를 자르는 쪽이 실무적일 수 있다.
  어느 쪽이든 **셋이 같은 것을 보게** 하는 것이 목표다.

참고선 150:
- 손익분기 270 아래이므로 값도 올려야 논리가 맞다. 다만 올리면 거의 안 걸린다.
  ⚠ 제안만 하고 배선하지 않는다(대표 지시).

### 3부 · 결과 (상한 보충)

- 🔴 예상 적중 — 내 배선이 **로그에만** 걸렸다. 화면·카톡이 읽는 `an["corePicks"]` 에는 안 닿았다.
- 필드 감사: 화면 5곳 + 카톡 2곳이 전부 `finalQuinellas`. 판정만 `displayedCombos`.
  ⇒ **회원이 받는 것과 우리가 재는 것이 다른 필드**였다.
- 🟢 판단 — 회원이 받는 쪽을 자른다. `_apply_combo_cap` 을 `_triple_analyze` 반환 직전으로 옮김(멱등).
- 🟢 실발동 — 야히코 1경주 3.0배 3→2조합 · 소노다 1경주 6.9배 4→3조합.
- 참고선 150: ⏸ 지금 바꾸지 않기를 권한다. 상한이 오늘 들어가 「적중 시 회수」가 구조적으로 오른다.

## [2026-08-13] 상한이 정답을 자른다 — 긴급 재측정

### 2부 · 예상 (실행 전)

- 🔴 아침 측정의 구멍을 인정한다. **남은 조합만** 봤고 잘린 쪽을 안 셌다.
  그래서 「손해적중 126 → 2건」이 좋아 보였지만, 잘려서 **적중 자체가 사라진 것**은 안 셌다.
  ⚠ 적중률이 39.9 → 30.5% 로 떨어진 것이 바로 그것인데, 나는 그것을 「감수」로만 적었다.
- 예상: 잘린 것이 정답인 경주가 **5~10%** 대일 것으로 본다.
  3배 미만 구간에서 가장 클 것이다(상한 1개라 가장 많이 자른다).
- 🔴 「3배 미만 아예 안 사기」가 회수율은 오르지만 경주가 크게 준다.
- 작업3: T-5 복원이 뒤로 밀리는지 reason 분포로 본다.
  ⚠ 표본 5건짜리 Override 가 T-5 확정을 밀어내면 그건 순서 문제다.

미리 밝힐 한계:
- 🔴 저장된 `finalQuinellas` 순서가 곧 우선순위다. 그 순서를 만든 로직은 재현하지 않았다.

### 3부 · 결과 (상한이 자른 쪽)

- 🔴 내 아침 측정에 구멍이 있었다 — **잘린 쪽을 안 셌다.** 대표 지적이 타당하다.
  잘린 것이 정답인 경주 **126/1303(9.7%)**. 적중 525→399 감소분과 정확히 일치한다.
- 🟢 예상 적중 — 3배 미만이 13.6%로 가장 크다.
- 🔴 예상 빗나감 — **T-5 복원 우선은 오히려 나빠진다**(71.5/67.1 → 70.8/66.4).
  T-5 복원 자체 성적이 회수율 50.6% · 3제외 32.3%로 전체 평균보다 훨씬 낮다(판정불가·20건).
- 🟢 Override 지적은 맞다 — 121조합 적중 4(3.3%) · 3제외 5.0%.
  뒤로 미루면 73.9/69.5로 개선.
- 🟢 **가장 좋은 것은 배당 낮은 순 정렬 — 74.9% / 70.5%.** 오늘 잰 모든 순서 중 최고.
- 3배 미만 상한 1개 자체는 옳다(1개 69.2/64.9 > 2개 64.2/60.2 > 전부 64.8/60.1).

## [2026-08-13] 상한 정렬 배선 (승인) — 배당 낮은 순 + Override 뒤로

### 2부 · 예상 (실행 전)

- `_apply_combo_cap` 안에서 **자르기 전에 정렬**한다. 자르는 자리는 이미 하나로 모여 있다.
- 정렬 키: `(Override 여부, 배당)` — Override 는 뒤로, 그 안에서 배당 낮은 순.
  ⚠ 측정에서 「배당 낮은 순」 단독이 74.9/70.5 로 최고였고 「Override 뒤로」는 73.9/69.5 였다.
  둘을 합치면 배당 낮은 순과 거의 같아질 것으로 본다(Override 는 대개 고배당이므로).
  🔴 합친 값을 반드시 측정해 확인한다. 합쳐서 더 나빠지면 배당 낮은 순 단독으로 간다.
- 검산: 카사마츠 1경주에서 2+5(5.6배)가 2+6(4.9배) 다음이 되어야 한다.
  ⚠ 5+7(74.8배)은 Override 이고 고배당이라 어느 규칙으로도 뒤로 간다.

작업3 대조 예상:
- 🔴 배선 결과가 측정값(74.9/70.5)과 **정확히 같지 않을 수 있다.**
  측정은 저장된 finalQuinellas 를 정렬했고, 배선은 생성 시점에 정렬한다.
  ⚠ 다르면 그 차이를 설명하고, 설명 못 하면 되돌린다.

함께 정리(표기):
- 회수 100~120%대는 「본전」으로. 세이부엔 7경주 1.0배가 적중으로 뜬 건 오해를 부른다.

### 3부 · 결과 (상한 정렬 배선)

- 🟢 예상 적중 — Override뒤+배당낮은순이 배당낮은순 단독과 **완전히 같다**(74.9/70.6).
  Override 가 대개 고배당이라 배당 정렬만으로 뒤로 간다. 그래도 두 키를 함께 뒀다.
- 🟢 카사마츠 1경주 검산 통과 — 2+6 다음이 2+5가 되어 **정답 적중**.
  ⚠ 다만 그 경주는 이미 저장된 마감분이라 파일은 안 바뀐다. 배선은 새 분석부터.
- 🟢 소급 대조 일치 — 현행 71.6/67.2 → 새 순서 74.9/70.6.
- 🟢 실발동 — 야히코 4경주 3.0배 3→2조합 · 발동 18회.
- 표기: 회수 100~120%는 「본전」(회색)으로. 세이부엔 7경주 1.0배 건 해소.

## [2026-08-13] 다른 경주 착순이 들어왔다 — 긴급 조사

### 2부 · 예상 (실행 전)

- 🔴 배당 오염과 **같은 계열**일 것으로 본다. 착순도 경주 식별자 없이 매칭될 가능성.
- 규모는 배당 오염(1.0%)보다 **작을 것**으로 본다. 착순은 소스가 적고 백필 게이트가 있다.
  예상 0.2~1% 대.
- 🔴 그러나 규모가 작아도 **영향은 크다** — 적중 판정이 뒤집히면 회수율·학습이 전부 틀어진다.
- 두수 판정은 **합집합**을 써야 한다(원칙 22). 출마표 단독으로 세면 부분수집을 오염으로 오판한다.
- 가드는 「착순 마번 > 명단」이라 **오탐이 구조적으로 없다** — 대표 지적이 맞다.
  다만 명단 근거가 얇은 경주는 판정을 건너뛰어야 한다.

미리 밝힐 한계:
- 🔴 실제 착순이 무엇인지는 외부 소스를 다시 봐야 알 수 있다.
  이 측정은 「틀렸다」까지만 말할 수 있고 「정답이 무엇인가」는 못 낸다.

### 3부 · 결과 (착순 오염)

- 🟢 예상 적중 — 규모는 배당 오염보다 작다. **5건(0.37%)**.
- 🔴 원인은 예상과 달랐다 — 식별자 문제가 아니라 **검증이 절대 범위(1~16)만 봤다.**
  카사마츠의 10·9 는 그 범위 안이라 그냥 통과했다. 검증은 있었는데 두수를 안 봤다.
- 🟢 오탐 분리 성공 — 삿포로 2건은 명단 불연속(부분수집)이라 판정 보류.
- 🟢 가드 시험 3종 통과 — 오염값 거부 · 정답 통과 · 명단없음 보류.
- 회수율 영향 0.16%p 로 작다. ⚠ **다만 개별 경주는 다르다**(카사마츠 4R 은 적중인데 미적중).
- ✏️ 내 실수 1회 — `_analysis_log_path` 가 3값 반환인데 1값으로 받아 첫 시험이 전부 통과로 나왔다.

## [2026-08-13] 소스 URL 대조 가드 — 오탐률 먼저

### 2부 · 예상 (실행 전)

- 🔴 **URL 이 저장돼 있는 경주가 매우 적을 것**으로 본다.
  8/10 CLAUDE.md 기록: 「src 원시 URL 은 전체 16,931틱 중 9건뿐」이었다.
  대부분은 `private`·`oddspark` 같은 **소스명 한 단어**로 저장된다.
  ⇒ 대조할 URL 자체가 없으면 가드가 **구조적으로 못 돈다.**
- 그래서 예상: URL 보유 1% 미만 · 불일치 건수도 한 자릿수.
- 🔴 그렇다면 「지금 붙일 것」이라는 지시에 대해 **못 붙인다고 정직하게 보고**해야 한다.
  대신 확장이 URL 을 싣게 하는 것이 선행 조건이다(어제 rkVerify 와 같은 계열).
- ⚠ 오탐률 20% 기준은 URL 이 있는 경주 안에서만 의미가 있다.

작업3(화면 ↔ API):
- 어제 확인했다 — 매트릭스는 `triple_latest` 원본, 추천은 교정 후 `cur_q`.
  오늘 매트릭스에도 교정을 태웠으므로 지금은 같아야 한다.

### 3부 · 결과 (소스 URL 대조)

- 🟢 예상 적중 — URL 없는 경주 **78.4%**. 대조할 것이 없다.
- 🔴 예상보다 나빴다 — URL 이 있는 185경주 중 **122건(65.9%)이 어긋난다.**
  기준 20% 를 크게 넘어 **배선하지 않았다**(대표 지시대로 보고).
- 🔴 이유가 결정적 — 저장된 URL 1268건이 **전부 DebaTable(출마표 페이지)** 이다.
  배당 페이지가 아니다. 전적은 다른 경주 것을 미리 받으므로 경주번호가 달라도 정상이다.
  ⇒ **대조 전제 자체가 틀렸다.**
- 🔴 카사마츠 6R 진짜 원인 — 두수가 **8두 → 10두로 늘었다.**
  지금 감지는 조합이 **줄어드는 것만** 본다. 증가는 어떤 게이트도 안 잡는다.
- 제안: 두수 **증가** 감지(취소마는 늘지 않는다). ⏸ 배선은 승인 후.

## [2026-08-13] 두수 증가 감지 — 규모·오탐률 (측정만)

### 2부 · 예상 (실행 전)

- 🔴 **초반 증가가 대부분일 것**으로 본다. 배당판이 처음에 부분수집되고 채워진다.
  그렇다면 「증가 = 이상」이라는 단순 규칙은 오탐률이 매우 높다.
- 갈림은 **시점**일 것으로 본다. 마감 직전 증가는 취소마로 설명되지 않는다.
  ⇒ T-2 이내 증가만 잡으면 오탐률 20% 아래로 내려갈 가능성이 있다.
- 예상: 한 번이라도 증가한 경주 50~80% (매우 흔할 것) · 그중 T-2 이내는 소수.
- 🔴 조건을 못 찾으면 **못 찾는다고 분명히 말한다**(대표 지시).

작업2(세 값이 다름):
- 저장 두 값이 다른 것은 위 증가 이벤트와 같은 것이다.
- 화면 값은 저장돼 있지 않아 **사후 확인이 불가능**할 것으로 본다.
  ⚠ 그러면 「어느 값이 맞는가」는 지금 데이터로 답할 수 없다.

### 3부 · 결과 (두수 증가)

- 🔴 예상 빗나감 — 증가한 경주가 **6.6%(56/853)** 로 예상(50~80%)보다 훨씬 적다.
- 🔴 예상 빗나감 — **「증가 = 오염」이 성립하지 않는다.**
  착순 검증에서 늘어난 값이 맞는 경우 3건 확인(제주 4R 10번 2착 · 제주 7R 9번 3착 · 부산 7R 11번 2착).
- ⇒ 마감 5분 이내 11건 중 최소 3건 정상 = **오탐 27%**. 기준 20% 초과.
  🔴 **조건을 못 찾았다. 분명히 밝힌다.**
- 🟢 부수 발견 — 마감 5분 이내 증가 11회가 **전부 확장**. 서버 틱은 0건.
- 카사마츠 6R 의 진짜 문제는 두수가 아니라 **배당값이 앞 경주와 비슷했던 것**이다.

## [2026-08-13] 경기장 이름 자리에 분류명 + 정렬 미적용 — 조사

### 2부 · 예상 (실행 전)

작업1(규모):
- 🔴 확장이 경기장명을 못 읽었을 때 화면의 분류 표기(地方/中央)를 넣은 것으로 본다.
  `extractRaceKey` 가 본문 정규식으로 뽑는데 실패하면 다른 문자열이 잡힌다.
- 건수는 적을 것으로 본다(1% 미만). 다만 같은 경주가 두 이름으로 갈리면 배당이 나뉜다.

작업3(정렬):
- 🔴 오전에 넣은 정렬은 `_apply_combo_cap` **안**이고, 그 함수는 **상한이 걸릴 때만** 돈다.
  `len(fq) <= cap` 이면 그대로 반환한다 — **정렬을 안 한다.**
  ⇒ 조합이 2개이고 상한도 2면 정렬이 안 걸린다. 그것이 원인일 가능성이 크다.
- ⚠ 그렇다면 배선 자체가 절반만 된 것이다. 정직하게 밝힌다.

### 3부 · 결과 (地方 + 정렬)

- 地方 저장 7건(전부 오늘) · 🔴 그중 4건은 카사마츠 이름으로도 **동시 존재**.
  6경주는 값까지 다르다(카사마츠 10두 ↔ 地方 8두).
- 🔴 예상 빗나감 — 정렬은 **돌았다**(comboCap before=3→cap=1 기록).
  안 바뀐 이유는 **1+5 와 4+5 가 둘 다 2.3배 동점**이라서다(안정 정렬).
- ⇒ 규칙의 구멍: **배당이 같을 때 무엇을 앞에 둘지가 없다.**
  4+5 는 그 경주 유일한 급락 신호였다. 제안: 동점이면 신호 있는 쪽 우선.
- 🟢 예상 적중(부분) — 정렬은 **상한이 걸릴 때만** 돈다. 조합 수가 상한 이하면 종전 순서 그대로다.

## [2026-08-13] 정렬 두 가지 보완 (승인)

### 2부 · 예상 (실행 전)

작업1(동점 → 신호 우선):
- 신호 판정은 조합의 `reason`·`basis` 문자열로 한다. 별도 필드가 없다.
- 순서: 급락 > 집중급락 > 역배열 > 복승불일치 > 다이아.
- 🔴 검산: 카사마츠 9경주(地方 9경주)에서 4+5 가 남아야 한다.

작업2(항상 정렬):
- 🔴 이것이 **회원이 보는 첫 조합을 바꾼다.** 지금까지는 상한이 걸릴 때만 바뀌었다.
- 비중(stakePlan)은 조합 목록으로 계산하므로 순서가 바뀌어도 합은 같다.
  다만 「1순위」라는 개념을 쓰는 곳이 있으면 그것이 바뀐다 — 확인이 필요하다.
- ⚠ 소급 대조에서 나빠지면 되돌린다.

예상 수치:
- 상한이 안 걸리는 경주가 다수이므로 **소급 영향이 오늘 정렬 배선보다 클 것**으로 본다.
- 방향은 좋아질 것으로 본다(배당 낮은 순이 74.9/70.6 으로 최고였다).
  🔴 다만 그 측정은 **상한 걸린 경주만** 대상이었다. 전체로 넓히면 다를 수 있다.

地方 7건:
- ⚠ 임의로 지우지 않는다. 판단 기준과 제안만 낸다.

### 3부 · 결과 (정렬 두 가지 보완)

- 🟢 작업1 검산 통과 — 카사마츠 9경주에서 4+5(진성급락·랭크0)가 1+5(근거없음·랭크99)를 앞선다.
- 🟢 작업2 통과 — 상한 이하일 때도 정렬된다(50배/12배 → 12배가 앞).
- 🔴 예상 빗나감 — **성적이 안 바뀐다.** 항상 정렬해도 68.1%/65.0% 로 현행과 동일(적중 499 동일).
  자르지 않으면 집합이 같아 순서만 바뀐다. 회원이 보는 첫 조합은 바뀌지만 성적은 그대로다.
- 동점 있는 경주가 1352건 중 **17건**뿐이라 동점 규칙도 소급 영향이 없다(74.09% 동일).
- 🟢 (참고) 신호를 배당보다 먼저 보는 안은 72.2%로 **떨어진다** — 지금 순서가 맞다.

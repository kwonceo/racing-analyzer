# 예상 기록 — 역수 배분 비율 회원 발송 최적화 (2026-08-12)

## 1부. 쉬운 설명

지금 회원에게 나가는 카톡에는 마권 번호만 있습니다. 그래서 회원은 조합마다
같은 돈을 겁니다. 그런데 배당이 다른 조합에 같은 돈을 걸면, 낮은 배당이
맞았을 때 원금도 못 건집니다.

배당의 역수에 비례해 걸면 어느 조합이 맞아도 돌려받는 돈이 같아집니다.
1.3배 조합에 많이 걸고 30배 조합에 조금 거는 식입니다.

소급으로 재보니 8월 11일 기준 상위 3건을 뺀 회수율이 42.2%에서 58.1%로
올랐습니다. 그래서 이 비율을 카톡에 실어 보냅니다.

다만 회원에게는 비율만 보냅니다. 회수율 숫자나 권장·경고 문구는 안 보냅니다.
"원금 회수율 190%"는 맞았을 때 값이고 실제 회수율은 66.6%인데, 그대로
내보내면 회원이 매번 90% 번다고 오해하게 됩니다. 나중에 더 큰 불신이 옵니다.

그리고 배당이 오염된 경주에서는 비율을 아예 안 보냅니다. 8월 11일 하루에만
다른 경주 배당이 들어온 사고가 다섯 건이었습니다. 틀린 배당으로 계산한
비율은 틀린 비율입니다.

## 2부. 예상 (실행 전에 적는다)

### 무엇을 바꾸나
- `_stake_plan` 에 정수 보정을 넣는다. 반올림 뒤 남는 오차를 최저배당 조합에
  가감해 합계를 정확히 100%로 만든다.
- 안전 게이트 셋을 넣는다. oddsSource 가 private 면 반환하지 않는다.
  두수와 배당 조합 수가 nC2 와 안 맞으면 반환하지 않는다.
  배당이 null 인 조합은 비율에서 빼고 그 사실을 내부 로그에 남긴다.
- T-5 시점 값을 corePicks 에 고정한다. 이후 재계산하지 않는다.
- 카톡에서 회수율 문구와 권장·경고를 뺀다. 조합과 비율만 남긴다.
- 권장·경고는 API 응답과 내부 로그에만 남긴다.

### 무엇을 안 바꾸나
- 조합 선택 로직(`_final_picks`)과 판정 명단은 한 줄도 안 건드린다.
- 기존 카톡 줄(복승·삼복승·복병·신호·확신도)은 그대로 둔다. 추가만 한다.
- 판정선 74.5 는 낮추지 않는다.

### 예상되는 결과
- 카톡 본문에 "배분 3+5 93% · 2+4 4% · 2+3 3%" 같은 한 줄이 늘어난다.
  합계는 정확히 100%가 된다.
- 회수율·경고 문구는 카톡에서 사라진다. API 응답에는 남는다.
- private 소스 경주에서는 배분 줄 자체가 안 나간다. 8월 11일 기준으로
  모리오카 2경주, 우라와 1경주, 야히코 7경주가 그에 해당한다.
- 정수 보정 때문에 비율 합이 99%나 101%가 되는 일이 없어진다.

### 틀릴 수 있는 지점
- 두수 정보를 못 구하는 경주가 있을 수 있다. 그러면 nC2 대조가 성립하지
  않는다. 그때는 게이트를 통과시킨다. 판정 불가를 차단으로 바꾸지 않는다.
- T-5 고정본이 저장되기 전에 카톡이 나갈 수 있다. 그 경우 카톡은 발송
  시점 배당으로 계산하는데, 그 시점이 곧 T-5 라 결과는 같아야 한다.
  다르게 나오면 배선이 틀린 것이다.
- 계수기가 0 이면 배선이 안 탄 것이다. 다음 개최에서 확인해야 한다.

## 3부. 결과

(앞 항목 실행 완료 — 아래 후속 작업으로 이어짐)

---

# 예상 기록 — 역수 배분을 고배당 가중으로 (2026-08-12)

## 1부. 쉬운 설명

역수 배분은 고배당 조합에 아주 조금 겁니다. 30배짜리에 4%만 갑니다.
맞아도 회수가 121%밖에 안 됩니다.

그래서 15배 넘는 조합에만 비중을 6배 올리고 다시 정규화합니다.
저배당 위주는 그대로 두되 고배당 쪽 몫을 키우는 방식입니다.

최고배당 하나에 몰아주는 안도 재봤는데 기각했습니다. 3건 제외 회수율이
72.0%로 판정선 아래로 떨어졌습니다.

## 2부. 예상 (실행 전에 적는다)

### 무엇을 바꾸나
- `_stake_plan` 의 가중식을 `1/배당` 에서 `(1/배당) x (배당>=15 이면 6)` 으로 바꾼다.
- 스위치로 역수와 가중을 오갈 수 있게 한다.
- 정수 보정과 합계 100% 는 그대로 둔다.
- 계수기에 배수와 경계를 함께 남겨 나중에 4배·8배·10배·20배와 비교한다.

### 무엇을 안 바꾸나
- 조합 선택 로직과 판정 명단은 안 건드린다.
- 카톡은 비율만 나가는 형태를 유지한다. 회수율·경고는 계속 내부용이다.
- 안전 게이트(private 차단·nC2 대조·배당 null 제외)는 그대로 간다.

### 소급 근거
- 균등 87.5% / 3제외 74.8%
- 역수 89.5% / 3제외 77.3%
- 가중 6배 91.2% / 3제외 79.0%  <- 채택
- 몰아주기 70% 88.2% / 3제외 72.0%  <- 기각
- 고배당 15배+ 적중 20건에서 받는 돈: 역수 3.43 -> 가중 6배 10.34

### 예상되는 결과
- 15배 이상 조합의 비중이 눈에 띄게 커진다. 야히코 4R 이면 30.4배 조합이
  4.0% 에서 약 20% 안팎으로 오를 것이다.
- 합계는 정수 보정으로 정확히 100% 를 유지한다.
- 카톡 배분 줄도 같은 비율로 바뀐다.
- 원금 회수율(1 나누기 Sum(1/배당))은 배분과 무관하므로 값이 안 바뀐다.
  바뀌면 배선이 틀린 것이다.

### 틀릴 수 있는 지점
- 15배 이상 조합이 없는 경주에서는 역수와 결과가 같아야 한다. 다르면 틀렸다.
- 가중을 곱한 뒤 정규화하므로 저배당 비중이 줄어든다. 그만큼 저배당 적중 시
  회수가 줄어든다. 그것이 3제외에서 어떻게 나오는지는 며칠 실전으로 봐야 한다.

## 3부. 결과

### 예상이 맞은 것
- 야히코 4경주 30.4배 조합이 4% 에서 18% 로 올랐다. 예상 "20% 안팎" 과 일치.
  41.1배도 13% 다. 적중 시 회수가 121 에서 547.2 로 뛴다.
- 합계는 세 경주 모두 정확히 100%. 정수 보정이 작동한다.
- 원금 회수율은 121% / 120% / 93% 로 역수 때와 똑같다. 배분과 무관하다는
  예상대로다. 바뀌었으면 배선이 틀린 것이었다.
- 배당 없는 조합은 모리오카에서 [[2,4],[4,5]] 로 명시되고 비율에서 빠진다.

### 실물 예
- 카사마츠 5경주: 30.8배 4+8 이 3% 에서 16% 로. 그날 실제로 그게 맞았으니
  회수가 92.9 에서 492.8 이 된다. 5.3배.
- 그 경주는 verdict 가 warn 인데 그 문구는 회원에게 안 간다.
  콘솔에 [내부·비발송] 로만 찍힌다.

### 아직 못 본 것
- 카톡 실제 발송 텍스트. 진행 중 경주가 없어 _kakao_rich_message 가 안 돈다.
  kakao_stake_plan / grade_judge_freeze 계수기 둘 다 0 이다.
- private 소스 차단(stake_plan_block). 어제 경주가 전부 oddspark 라 미발동.
- 다음 개최에서 셋 다 확인할 것.

### 남은 비교 대상
- 배수 4·8 과 경계 10·20 은 상수 두 줄(STAKE_HI_ODDS / STAKE_HI_BOOST)만
  바꾸면 재측정된다. 실전 표본이 쌓이면 같은 스크립트로 다시 잰다.

---

# 예상 기록 — 보고를 파일로 남기고 API로 (2026-08-12)

## 1부. 쉬운 설명

지금은 보고를 손으로 옮겨 붙입니다. 길면 잘리고 밖에 있으면 아예 못 봅니다.
파일에 쌓아두고 주소 하나로 읽게 하면 그 손이 없어집니다.

## 2부. 예상 (실행 전에 적는다)

### 무엇을 만드나
- `docs/REPORT.md` 에 3단 보고를 쌓는다. 맨 위가 최신이다.
- 보고마다 시각과 그때의 커밋 해시를 붙인다.
- 파일이 커지면 `docs/report_archive/` 로 회전시키고 본문은 비운다.
- `GET /api/report?n=1` 로 최근 n건을 원문 그대로 돌려준다.

### 무엇을 안 바꾸나
- 기존 파일·엔드포인트를 지우지 않는다. 추가만 한다.
- 판정·추천·수집 경로에 손대지 않는다. 완전 읽기 전용이다.

### 예상되는 결과
- 이번 작업 보고가 `docs/REPORT.md` 의 첫 항목으로 들어간다.
- `/api/report` 가 그것을 잘리지 않은 원문으로 돌려준다.
- 한글이 깨지지 않는다.

### 틀릴 수 있는 지점
- 구분자를 잘못 고르면 보고 본문 안의 가로줄과 겹쳐 잘못 잘린다.
  본문에서 쓰지 않을 고유 표식을 써야 한다.
- 커밋 해시는 보고를 쓰는 시점의 HEAD 라 그 보고가 담긴 커밋과 한 칸 어긋난다.
  그 사실을 파일에 명시해야 나중에 오해가 없다.

## 3부. 결과

### 예상대로 된 것
- `docs/REPORT.md` 생성(2,964바이트). 이번 작업 보고가 **첫 항목**으로 들어갔다.
- `/api/report?n=1` 이 `ok=True · total=1 · count=1` 로 응답한다.
- 본문 1,441자가 **잘리지 않고 원문 그대로** 나온다. 한글도 정상이다.
- 시각(2026-08-12 06:24:55)과 커밋(69483a5c0)이 함께 붙었다.
- "이 보고가 담긴 커밋은 보통 그 다음 커밋"이라는 경고가 파일에 실렸다.

### 틀릴 수 있다고 적은 두 가지
- 구분자: `<!--REPORT-->` 를 써서 본문 가로줄과 안 겹쳤다. 잘못 잘린 곳 없다.
- 커밋 해시 어긋남: 실제로 어긋난다. 69483a5c0 은 **직전** 커밋이다.
  파일에 그 사실을 적어 두었으므로 오해가 생기지 않는다.

### 덧붙인 것
- `&format=md` 로 마크다운 원문을 그대로 받을 수 있게 했다.
- 400KB 를 넘으면 `docs/report_archive/` 로 회전한다. 지우지 않는다.
- 응답에 `archives` 목록을 실어 과거 파일을 찾을 수 있게 했다.

---

# 예상 기록 — 코드맵 갱신과 보완 (2026-08-12)

## 1부. 쉬운 설명

app.py 가 3만 줄을 넘었습니다. 어디를 고쳐야 할지 찾기가 어렵습니다.
코드맵은 함수마다 어디에 있고 누가 부르고 무엇을 부르는지 적은 지도입니다.

이미 7월 30일에 만들어져 있었습니다. 다만 그 뒤로 코드가 바뀌어 낡았고
빠진 항목이 있습니다.

## 2부. 예상 (실행 전에 적는다)

### 먼저 확인한 것 (지시대로 보고)
- `_triple_analyze` · `_final_picks` -> **03_분석**
- `_history_save_analysis` · `_json_atomic` -> **02_저장**
- 이름 기반 우선 규칙이 이미 반영돼 있어 의도대로 들어간다.

### 무엇을 보완하나
- 위험목록 ③(파서 반환 키 vs 저장행 키 차집합)이 **빠져 있다**. 넣는다.
- 신선도 배너가 없다. `00_INDEX.md` 맨 위에 낡음 표시를 넣는다.
  🔴 차단하지 않는다 — 라이브 중 커밋을 막으면 안 된다.
- 낡은 코드맵을 다시 생성한다.

### 무엇을 안 바꾸나
- `app.py` 는 한 줄도 안 건드린다.
- 기존 섹션 분류 규칙과 수정 지시 넷의 반영분은 그대로 둔다.
- 설명은 계속 docstring·주석에서만 뽑는다. 새로 쓰지 않는다.

### 수치 예상
- 함수 총수: 지난번 1,125개. 그 뒤 배선이 늘었으니 **1,150~1,250개** 로 본다.
  ⚠ 지시서의 1,200~1,500 범위 하단에 가까울 것이다.
- 설명 자동 추출률: 지난번 74.7%. 새 함수에 주석을 많이 달았으니 **73~78%** 유지.
  지시서 기준 60% 이상은 넉넉히 넘는다.
- 빈값 폴백(①): 지난번 57곳. 그 뒤 방어를 더 넣었으니 **55~70곳**.
  ⚠ 지시서의 62곳은 app.py 전체 기준이고 코드맵은 핫 경로만 세므로 더 적을 수 있다.
- os.replace(②): 지난번 20곳. **18~24곳**.
- sleep 데몬(⑤): 지난번 11곳. 지시서 예상 3~6곳과 크게 다르다.
  ⚠ 코드맵은 `while True` 없이 `time.sleep` 만 있어도 세는 것으로 보인다.
  실제 sleep-first 데몬은 CLAUDE.md 실측으로 6곳이다. 그 차이를 확인해야 한다.
- ③(파서 키 차집합): 새로 만드는 것이라 예상값이 없다. **10~40곳** 으로 본다.

### 틀릴 수 있는 지점
- 함수 수가 1,250을 넘으면 그동안 배선이 예상보다 훨씬 많았다는 뜻이다.
- ⑤가 11곳으로 그대로면 세는 기준이 CLAUDE.md 실측(6곳)과 다른 것이다.
  그 경우 기준 차이를 기록만 하고 코드맵 기준을 바꾸지 않는다.
  🔴 「무엇이 어디에」만 담고 「왜 결함인가」는 안 담는 원칙 때문이다.
- ③은 파서와 저장행을 짝지어야 하는데 이름만으로 짝을 못 찾을 수 있다.
  못 찾으면 빈 목록이 되고, 그것도 사실대로 남긴다.

## 3부. 결과

### 수치 대조 (예상 → 실제)

- 함수 총수: 1,150~1,250 예상 → **1,279개**. 🔴 **예상을 넘었다.**
  7/30 이후 열흘 남짓에 154개가 늘었다. app.py 가 32,349 → 39,078줄로 6,729줄 늘었다.
  ⚠ 지시서 범위(1,200~1,500)에는 들어간다. 내 하단 추정이 짧았다.
- 설명 자동 추출률: 73~78% 예상 → **74.8%**. 맞았다. 지시서 기준 60%를 넘는다.
- ①(빈값 폴백): 55~70곳 예상 → **69곳**. 맞았다(지난번 57 → 69).
- ②(os.replace): 18~24곳 예상 → **29곳**. 🔴 **예상을 넘었다.**
  락 없는 지점이 27곳이다. tmp 패턴을 쓰는 저장이 그만큼 늘었다.
- ④(전역 상수): 예상 안 함 → 153개(지난번 75). 스위치를 많이 늘린 결과다.
- ⑤(sleep 데몬): 11 → **21곳**. 예상대로 CLAUDE.md 실측(6곳)과 기준이 다르다.
  코드맵은 `while True` + `time.sleep` 이면 전부 센다. 실측 6곳은 **주기 5분 이상**만이다.
  🔴 기준 차이를 기록만 하고 코드맵 기준은 바꾸지 않는다.
  「무엇이 어디에」만 담고 「왜 결함인가」는 안 담는 원칙 때문이다.
- ③(파서 키 차집합): 10~40곳 예상 → **1쌍**. 🔴 **크게 빗나갔다.**
  이유는 아래에 적는다.

### 왜 달랐나 — ③

처음 돌렸을 때 4쌍 전부 「'str' object has no attribute '_fields'」였다.
`get_keys` 가 **AST 노드**를 받는데 이름 문자열을 넘겼다.
`func_node(name)` 을 거치도록 고치니 1쌍이 나왔다.

1쌍뿐인 이유는 나머지 세 쌍의 차집합이 실제로 비었기 때문이 아니다.
`audit_key_drift.py` 를 직접 돌리면 네 쌍 모두 탈락 후보가 나온다
(11개 · 13개 · 4개 · 13개). 그쪽은 **정규화 규칙이 더 정교**하다.
코드맵의 단순 차집합은 그중 일부만 잡는다.

🔴 즉 ③은 **여전히 전용 도구가 정확하다.** 코드맵에는 요약만 싣고
「수동 확인: `python tools/audit_key_drift.py`」 안내를 남기는 것이 옳았다.
지금 상태로도 색인에 ③이 표기되어 존재 자체는 드러난다.

### 신선도 배너

작동한다. 00_INDEX 맨 위에 이렇게 뜬다.
「직전 생성 이후 바뀐 소스 2개 — app.py (32349 → 39078줄) · tools/health_check.py (403 → 1099줄)」
🔴 차단하지 않는다. 알리기만 한다.

### 수정 지시 넷 확인

- ① 이름 기반 우선: 이미 반영돼 있었다. 네 함수가 의도대로 들어간다.
  `_triple_analyze`(03_분석 10724) · `_final_picks`(03_분석 8348) ·
  `_history_save_analysis`(02_저장 15703) · `_json_atomic`(02_저장 15759)
- ② 위험목록 함수는 Caller 전수 나열: 코드에 「위험목록 함수 → 전수 나열」 표기가 있다.
- ③ 「왜 결함인가」 미포함: 헤더의 사고 사례는 **판정 기준을 알리는 한 줄**이고
  함수별 항목에는 판단이 없다. 유지한다.
- ④ 근거 없는 권고 금지: 권고문이 없다. 유지한다.

### 남은 것 (사실만)

- 99_미분류가 734개(57.4%)다. 지난번 641개에서 늘었다.
  분류 규칙에 안 걸리는 함수가 과반이다.
- `_start_periodic_backup` 이 「영속화 없음」으로 표기되는데,
  실제로는 `data/_periodic_backup_last.txt` 를 쓴다(2026-08-01 배선).
  도구가 그 패턴을 못 읽는다. 🔴 도구의 오탐이다.

---

# 예상 기록 — 미분류 734개 조사 (2026-08-12)

## 1부. 쉬운 설명

코드맵에서 절반 넘는 함수가 「미분류」입니다. 소속을 모르는 지도는 반쪽입니다.

무엇이 거기 들어 있는지부터 봅니다. 규칙을 먼저 만들지 않습니다.

## 2부. 예상 (실행 전에 적는다)

### 무엇을 하나
- 734개를 **이름 앞머리로 묶어** 상위 20개를 낸다.
- 지금 분류 규칙이 무엇을 보는지 코드에서 확인한다.
- 07 섹션이 필요한지는 **설계안만** 낸다. 배선하지 않는다.
- app.py 증가분을 섹션별로 센다. **사실만 낸다. 판단하지 않는다.**

### 무엇을 안 하나
- `app.py` 는 건드리지 않는다.
- 분류 규칙을 이번에 고치지 않는다. 무엇이 있는지 보는 것이 먼저다.

### 예상
- 앞머리 상위는 `_kra_` · `_keiba_` · `_korea_` · `_jra_` · `_kakao_` 같은
  **도메인 접두**가 많을 것으로 본다. 지금 규칙은 동사(`_save`·`_fetch`)를 보는데
  도메인 접두는 동사가 뒤에 오거나 아예 없다.
- 미분류의 상당수는 **헬퍼·유틸**(`_fmt`·`_esc`·`_num`·`_to_`)일 것이다.
  그것은 억지로 여섯에 넣으면 안 된다. 07 이 필요한 근거가 된다.
- 규칙이 좁은 쪽과 성격이 다른 쪽이 **섞여 있을** 것이다.
  둘의 비율이 이번 조사의 핵심 숫자다.
- app.py 증가 6,729줄 중 절반 이상이 **주석**일 것으로 본다.
  이 프로젝트는 사고 경위를 코드 옆에 길게 적는다.

### 틀릴 수 있는 지점
- 앞머리가 너무 잘게 흩어져 상위 20개로 절반도 못 덮을 수 있다.
  그러면 접두 기반 규칙이 답이 아니라는 뜻이다.
- 주석 비율이 낮으면 실제 배선이 그만큼 늘었다는 뜻이고,
  그건 지도가 더 급하다는 근거가 된다.

## 3부. 결과

### 예상이 빗나간 것

- 접두 상위 20개가 **29.7%(218/734)** 밖에 못 덮었다.
  두 토큰으로 묶으면 9.8% 다. 🔴 **접두 기반 규칙은 답이 아니었다.**
  「상위 20개로 절반도 못 덮으면 접두가 답이 아니다」라고 적어둔 그대로다.
- 헬퍼가 상당수일 것으로 봤는데 **40개(5.4%)** 뿐이었다.
  도메인 접두 101개(13.8%), 내부 계산 16개(2.2%), 나머지 583개(79.4%)가 성격 미상이었다.

### 진짜 원인

🔴 **미분류 734개 중 기존 동사 규칙에 매치되는 것이 0개**였다.
적용 순서 문제가 아니라 규칙이 **아예 안 닿는 이름들**이다.
기존 여섯 규칙은 전부 동사(`_save`·`_fetch`·`_analyze`)인데
`_kra_g` `_jra_num` 처럼 도메인 접두 뒤에 짧은 이름이 오면 못 잡는다.

### 배선 결과 (승인분)

- 도메인 2차 규칙: 이름 **어디에든** 동사 조각이 있으면 그 섹션으로. 느슨한 매치.
- 07_헬퍼 신설: 짧은 변환 도구 29개.
- **미분류 734 → 510개.** 224개가 소속을 찾았다.
  03_분석 89 → 208 · 01_수집 86 → 108 · 05_출력 227 → 246 · 02_저장 86 → 103.
- ⚠ 남은 510개를 억지로 넣지 않았다(대표 지시).

### 미분류 우선순위 (대표 지시)

「지도의 목적은 고칠 때 찾는 것」이라는 기준으로 두 축을 넣었다.

- 최근 30일 손댄 횟수 상위 50: `_area_num` 22회 · `_auto_pred_tick` 9 ·
  `_boot_background` 9 · `_keiba_build_form` 8 · `_raw_profile_snapshot` 8
- 호출되는 횟수 상위 50: `_area_num` 46회 · `_mk` 21 · `_kra_meet_code` 13
- 🔴 **두 목록이 겹치는 14개가 최우선**이다.
  `_area_num` · `_canon_rk` · `_race_result_id` · `_kra_meet_code` · `_missing_results` 등.
- 🔴 나머지 460개는 **미분류로 남긴다**고 파일에 명시했다.

### 내가 한 번 틀렸다

`_git_touch_counts` 에서 `collections` 를 썼는데 그 모듈이 전역에 없었다.
예외가 잡혀 조용히 빈 dict 가 되어 **손댐이 전부 0** 으로 나왔다.
지역 import 로 고쳤다. 🔴 폴백이 결함을 안 보이게 한 전형이다(원칙 24).

### app.py 증가 (사실만)

- 7/30 이후 **추가 7,012줄 · 삭제 283줄 · 순증 6,729줄**. 거의 전부 추가다.
- 현재 39,078줄 중 주석 4,198줄(10.7%) · 빈줄 2,992줄(7.7%) · docstring 872블록.
- ⚠ 이것은 **전체 대비 비율**이지 증가분만 가른 값이 아니다.
  증가분의 주석 비율을 내려면 커밋별 diff 를 줄 단위로 분류해야 한다. 별도 작업이다.
- 판단은 하지 않는다.

---

# 예상 기록 — 쌍승역전을 거꾸로 재본다 (2026-08-12)

## 1부. 쉬운 설명

지금까지는 「쌍승역전이 지목한 말이 1착이냐」로만 쟀고 7.6% 였습니다. 나빴습니다.

그런데 이 신호의 본뜻은 「단승 1위가 실제로는 1위가 아니다」입니다.
그러면 지목마를 보는 게 아니라 **기준마가 떨어지는지**를 봐야 합니다.

기후 4경주에서 단승 1위 7번을 기준으로 다섯 건이 떴고 그 7번이 3착이었습니다.
반대로 야히코 4경주는 기준마가 1착이었고 지목 여섯이 전부 틀렸습니다.

## 2부. 예상 (실행 전에 적는다)

### 무엇을 재나
- 신호가 뜬 경주에서 **기준마(단승 1위)가 1착이 아닌 비율**.
- 신호가 안 뜬 경주의 같은 비율과 나란히 놓는다. 그것이 대조군이다.
- 건수별(1건 · 2~3건 · 4건 이상)로 나눈다.
- 지목마의 **3착권** 비율. 복승은 순서가 없으므로 1착만 보면 안 된다.
- 지목마 **둘이 함께** 3착권에 든 비율.

### 예상
- 🔴 **차이가 거의 없을 것으로 본다.** 앞서 잰 지목마 1착 7.6% 대 시장 45.8% 는
  격차가 너무 커서, 방향만 뒤집는다고 값이 생기기 어렵다.
- 기준마 탈락률은 신호 유무와 무관하게 **55~60%** 근처일 것이다.
  시장 단승 1위의 1착률이 40% 안팎이라는 것을 이미 쟀기 때문이다.
- 건수가 많을수록 탈락률이 오를 가능성은 있다. 다만 4건 이상은 표본이 얇을 것이다.
- 3착권으로 넓히면 지목마 적중은 오른다. 당연하다 — 기준이 느슨해졌기 때문이다.
  🔴 **오르는 것 자체는 의미가 없다.** 시장 대비로 봐야 한다.

### 틀릴 수 있는 지점
- 신호가 거의 전 경주에 뜨면 대조군이 안 만들어진다.
  앞서 잰 보유율이 428/2,635 였으니 그럴 위험은 낮다.
- 기준마(favorite)가 저장돼 있으므로 「단승 1위」를 따로 계산하지 않아도 된다.
  다만 그 값이 **신호가 본 시점의 단승 1위**라 마감 배당과 다를 수 있다.
- 표본 게이트(적중 30건)를 반드시 적용한다. 기후 4R 한 건으로 일반화하지 않는다.

## 3부. 결과

### 예상이 맞은 것
- 기준마 탈락률이 55~60% 근처일 것 → **57.8%**. 맞았다.
- 3착권으로 넓히면 오를 것 → 89.5%. 맞았고 **그래서 의미가 없다**는 것도 맞았다.
  지목마가 평균 4.6두라 무작위로 뽑아도 높게 나온다.

### 예상이 빗나간 것 — 방향이 반대다
- 🔴 **대조군이 70.5% 로 더 높았다.** 신호 뜬 쪽 57.8% 보다 **12.7%p 높다.**
  즉 **신호가 뜨면 기준마가 오히려 더 잘 1착한다.**
  「단승 1위가 실제로는 1위가 아니다」라는 신호의 본뜻과 결과가 반대다.
  ⚠ 「차이가 거의 없을 것」이라 적었는데 차이가 있었고, 방향이 반대였다.
- 🔴 **건수가 많을수록 나빠진다.** 1건 81.8% · 2~3건 82.4% · **4건 이상 52.2%**.
  「건수가 많을수록 탈락률이 오를 가능성」이라 적었는데 정반대다.
  기후 4경주는 5건이었고 그 구간이 가장 나쁘다 — **그 한 건이 우연이었다.**

### 남는 것 하나
- 2~3건 구간만 82.4% 로 전체 대조군(69.6%)보다 12.8%p 높다.
  ⚠ 51경주로 얇고, 1건·4건 이상과 방향이 달라 **우연일 수 있다.**
  단독으로 배선할 근거가 아니다.

### 판정
🔴 **쌍승역전은 거꾸로 재도 값이 없다.** 앞서 지목마 1착으로 쟀을 때(7.6% vs 시장 45.8%)와
같은 결론이고, 이번에는 **기준마 축에서도 역방향**임이 확인됐다.

---

# 예상 기록 — 부담중량과 성별 (2026-08-12)

## 1부. 쉬운 설명

암말이 숫말과 같은 무게를 지면 불리하다는 것이 경마의 기본입니다.
그래서 보통 2kg를 깎아줍니다.

그런데 같은 55kg이라도 400kg 말에게는 13.8%이고 550kg 말에게는 10%입니다.
절대 무게보다 **몸무게 대비 비율**이 진짜 축일 수 있습니다.

우리 데이터로 검증합니다.

## 2부. 예상 (실행 전에 적는다)

### 먼저 저장 실태
- 🔴 **성별은 저장되지 않을 것으로 본다.** `sexAge` 가 파서에는 있는데
  저장행에서 탈락한 목록에 들어 있었다(2026-07-30 스키마 드리프트 조사).
- `burdenWeight` · `bodyWeight` 는 8/11 배선분이라 **그 이후만** 있을 것이다.
  보유율은 낮을 것이고, 경륜에는 애초에 없다.
- 감량 표기(▲☆◇★)는 **저장 안 될 것**이다. 파서에서 본 적이 없다.

### 수치 예상
- 부담 대비 비율이 절대 부담중량보다 **결과와 더 잘 맞을 것**으로 본다.
  대표 지시의 논리가 물리적으로 타당하다.
- 성별 차이는 있을 것이나, **비율로 맞추면 상당 부분 사라질 것**으로 본다.
  암말이 대체로 가볍기 때문이다. 그러면 원인은 성별이 아니라 체격이다.
- 감량 2kg 검증은 **표본 부족으로 판정 불가**일 가능성이 높다.
  8/11 이후 데이터만 있고 성별이 없으면 애초에 못 가른다.

### 점수 경로
- 🔴 `totalScore` 조립(app.py 25541 계열)에 `wbonus`(부담중량 **변화**)는 있으나
  **비율은 없다**고 이미 확인했다. 성별도 없을 것이다.

### 틀릴 수 있는 지점
- 성별이 실제로 저장돼 있으면 예상이 틀린 것이고 그건 좋은 소식이다.
- 부담·마체중 보유율이 너무 낮으면 **작업 2·3·4 전부 판정 불가**로 끝난다.
  그 경우 「무엇을 배선해야 재게 되는가」를 답으로 낸다.
- 표본 30건 미만은 전부 판정 불가로 찍는다.

## 3부. 결과

### 저장 실태 (예상 대비)
- 🔴 성별 **0%** · 부담중량 **0%** · 감량 표기 **0%**. 예상보다 나쁘다.
- `bodyWeight` 만 179행(1.6%). 8/11 배선분이 경마에서만 채워진다.
- 파서가 아예 안 뽑는다 — `斤量`·`負担重量`·`減量` 이 app.py 에 **0회**.
- 작업 2·3·4 는 **부담+마체중 둘 다 가진 말이 0두**라 전부 측정 불가.

### 원인 (후속 조사에서 확정)
- 소스가 서로 다른 한쪽만 뽑는다:
  `keiba_nar` bodyWeight 20% / weight 0% · `oddspark` weight 90% / bodyWeight 0%.
- 그리고 `_form_from_starters` 의 키 35개에 **`weight` 와 `pastPlacings` 가 없다.**
  저장소에 90% · 59% 있는데 `form` 까지 안 온다.
- `past`(착차)는 원본이 전 소스 0% — 파서를 고쳐야 한다.

### 점수 경로
- `burdenWeight` · 성별 **없음**. `weight` 23회는 전부 마체중 **변화** 계열이다.
- 2026-08-10 확인과 같다. 부담 대비 비율은 계산식에 없다.

---

# 예상 기록 — 죽은 값 감지 배선 (2026-08-12)

## 1부. 쉬운 설명

확장이 보낸 배당이 몇 분 동안 한 글자도 안 바뀌는 일이 있었습니다.
마감 직전인데 멈춰 있으면 그건 살아 있는 시장이 아니라 죽은 값입니다.

8월 11일에만 다른 경주 배당이 들어온 사고가 세 건이었고 셋 다 값이 고정이었습니다.

## 2부. 예상 (실행 전에 적는다)

### 무엇을 하나
- 수신 시점에 **직전과 조합 집합이 완전히 같은 상태가 몇 분 지속됐는지** 잰다.
- 4분 이상이면 죽은 값으로 보고 **계수기만 찍는다.**
- 🔴 **관찰 모드다. 폐기하지 않는다.** 며칠 세고 나서 판단한다.

### 근거 (이미 측정한 것)
- oddspark: 3분 이상 고정 0.4% · **4분 이상 0건** · 최대 3.8분.
- private: 4분 이상 **3.8%** · 5분 이상 3.1% · 최대 177.7분.
- ⇒ 임계 4분에서 **oddspark 오탐 0건**이다.

### 예상되는 결과
- 계수기가 붙고, 확장 값에서만 간헐적으로 발동한다.
- 오늘은 경륜만 개최라 확장 유입이 적어 **발동 0일 수 있다.**
  그러면 미검증으로 남기고 다음 개최에서 본다.
- oddspark 에서 발동하면 **측정이 틀렸다는 뜻**이다. 그때는 임계를 다시 잰다.

### 틀릴 수 있는 지점
- 조합 집합 비교를 값까지 포함해서 해야 한다. 조합 목록만 같고 값이 바뀌는 경우를
  놓치면 과소 탐지가 된다.
- 마감 전후로 수집이 뜸해지면 「고정」과 「미수집」이 구분되지 않는다.
  틱 간격이 벌어진 것과 값이 안 바뀐 것은 다르다. 틱 수도 함께 남긴다.

## 3부. 결과

- 배선 완료. 처음에는 사고 세 건을 **하나도 못 잡았다** — 두 소스가 한 틱씩 번갈아 들어와
  이력을 거꾸로 훑다가 바로 앞이 다른 소스라 첫 비교에서 끊겼다.
  **같은 소스끼리만 이어 보도록** 고쳤다.
- 모리오카 2R **9틱 7.0분** 고정으로 잡힌다.
- 우라와 1R · 야히코 7R 은 고정이 **2.0분**이라 임계 미달. 확장 틱이 5개뿐이라
  애초에 4분을 채울 수 없었다. 🔴 **오래 고정된 것만 잡는다는 한계**를 그대로 남긴다.
- 🟢 **오탐 0** — 전 기간 oddspark 발동 0건 · private 만 680틱 37경주. 사전 측정과 일치.
- 실전 계수기는 아직 0(오늘 경륜만 개최). 다음 경마 개최일에 확인.

---

# 예상 기록 — form 키 두 줄 + 경주 전환 가짜 급락 (2026-08-12)

## 1부. 쉬운 설명

두 가지입니다.

하나는 저장소에 있는 부담중량과 과거착순이 분석까지 안 오는 것입니다.
키 두 줄을 옮기면 됩니다.

둘은 경주가 바뀔 때 직전 경주 배당과 비교해 **가짜 급락**이 만들어지는 것입니다.
기후 6경주에서 15조합이 15초 만에 60~94% 동시 급락으로 잡혔고 A등급이 됐습니다.

## 2부. 예상 (실행 전에 적는다)

### 승인 1 — 키 두 줄
- `_form_from_starters` 에 `weight` · `pastPlacings` 를 **옮기기만** 한다.
- 🔴 점수 계산은 안 건드린다. 그래서 **유력마와 점수가 안 바뀌어야 한다.**
  바뀌면 그 키가 어딘가에서 읽히고 있다는 뜻이고, 즉시 되돌린다.
- 스위치로 되돌릴 수 있게 한다.

### 승인 2 — 가짜 급락
- 🔴 **기존 방어가 이미 있다**(`_baseline_reset_needed` · `_next_race_surge`).
  그런데 기후 6R 이 뚫렸다면 **왜 안 걸렸는지가 먼저**다. 새 게이트를 만들기 전에 그것을 본다.
- invSum 2.061 은 환급률 206% 라는 뜻이라 **정상 시장에서 나올 수 없다.**
  정상 경주의 invSum 은 1.34 근처(환급률 74.5%)로 이미 쟀다.
- ⇒ invSum 게이트가 가장 좁고 확실할 것으로 본다. 다만 **정상 분포를 먼저 재고**
  임계를 정한다.

### 예상 수치
- 전 조합 동시 급락(80% 이상 조합이 -50% 넘게)은 **전체의 1~3%** 로 본다.
  드물어야 정상이다. 5% 를 넘으면 방어가 통째로 뚫린 것이다.
- invSum 1.6 초과는 **1% 미만**일 것이다. 정상 분포가 1.34 근처로 좁기 때문이다.

### 틀릴 수 있는 지점
- 키 두 줄을 넣었는데 점수가 바뀌면 예상이 틀린 것이고 **되돌린다.**
- 동시 급락이 흔하면(5%+) 그건 방어가 아니라 **급락 판정 자체**를 다시 봐야 한다.

## 3부. 결과

- 승인1: 키 두 줄 추가 완료. **40경주 대조에서 점수·등급·전적 변화 0건.** 순수 전달 확인.
- 승인2: 🔴 **환급률 게이트 기각.** 정상 oddspark 틱 12,129개에서 중앙 1.344 · 최대 1.423 ·
  **1.5 초과 0건**. 게이트로 쓰면 아무것도 안 걸린다. 기후 6R 도 1.31~1.34 로 정상이다.
  대표가 본 2.061 은 저장된 배당으로 **재현되지 않는다.**
- 기후 6R 실물: 같은 초에 **66 → 36 → 21 조합**(12두/9두/7두). 다른 경주 셋이 섞였다.
- 동시 급락 규모: 17,474틱 중 **36개(0.21%)**. 드물다.

---

# 예상 기록 — 상류에서 막는다 · 소스 URL 대조 (2026-08-12)

## 1부. 쉬운 설명

배당 오염이 매일 나옵니다. 지금까지 만든 방어는 전부 **들어온 뒤에** 이상한지 보는 것입니다.
그래서 새로운 유형이 계속 샙니다.

**들어오는 문 앞에서** 막습니다. 배당이 실려 온 주소에 경주 번호가 들어 있으니
그것과 저장하려는 경주가 같은지 대조합니다.

## 2부. 예상 (실행 전에 적는다)

### 작업 4는 이미 답이 나왔다
🔴 **딥 게이트는 걸렸다.** 66→36 은 54.5%, 36→21 은 58.3% 로 둘 다 문턱 아래이고
`combo_count_dip` 이 오늘 5회 발동했다. 마지막 사유가 "조합 36→21 (58%)" 로 그 경주다.
⇒ 「왜 안 걸렸나」라는 전제가 틀렸다. **걸렸는데 화면에만 그 순간이 그려졌다.**

### 작업 1·2 예상
- `oddspark` 와 `keiba.go.jp` 는 URL 에 경주 식별자가 있어 **뽑을 수 있다.**
- 🔴 **사설(ks1·asyukk)은 못 뽑을 것**으로 본다. 실측한 소스 문자열이
  `https://ks1.dke-d11diw.site/user_frame.php?PLACE_TYPE=JC` 였다.
  경기장도 경주번호도 없고 **종목 코드 하나뿐**이다.
- ⇒ 🔴 **오염의 대부분이 확장(private)에서 나오는데 그쪽을 못 막는다.**
  이 방어는 서버 수집분만 지킨다. 그 한계를 먼저 적어야 한다.

### 작업 3 예상
- oddspark 는 서버가 URL 을 직접 만들므로 **불일치가 0에 가까울 것**이다.
  즉 거부율은 낮지만 **막는 것도 거의 없다.**
- 그렇다면 이 게이트의 실익이 작다는 뜻이고, 정직하게 그렇게 보고해야 한다.

### 작업 5 예상
- 기후 7R 의 2번 열이 통째로 뛴 것은 **열 밀림 계열**로 본다.
  `_column_shift_suspect` 가 있는데 오늘 1회만 발동했다.
- 상류 대조로는 **못 잡는다.** 같은 경주 같은 소스인데 값만 어긋난 것이기 때문이다.

### 틀릴 수 있는 지점
- 사설 URL 에 경주 식별자가 있으면 예상이 틀린 것이고, 그건 좋은 소식이다.
- oddspark 에서 불일치가 실제로 나오면 서버 수집에도 문제가 있다는 뜻이다.

## 3부. 결과

- 🔴 **URL 이 아예 저장되지 않는다.** `src` 값이 `oddspark`·`private`·`netkeiba` 세 개뿐이고
  **URL 형태 0종**이다. 상류 대조는 불가능하다.
- 기후 7R 은 열 밀림이 아니라 **다른 경주 배당**이다. `2+4` 는 13.9→311.3 인데
  `1+3` 은 129.7→3.0 으로 **반대 방향**이고, 그 값이 **5분 고정**이다.
  🟢 오늘 배선한 **죽은 값 감지가 잡을 유형**이다.
- 두 소스가 다 있는 806경주 중 최대 마번 불일치 **58건(7.2%)**.
- 작업4 정정: 딥 게이트는 **걸렸다**(54.5% · 58.3%). 전제가 틀렸다.

---

# 예상 기록 — 자체검사 조합에 면제 부여 (2026-08-12)

## 1부. 쉬운 설명

승인받아 넣은 자체검사가 오늘 조합을 383번 넣었는데 **최종 명단에 하나도 안 남습니다.**
뒤에 있는 필터가 전부 잘라냅니다.

작동하는 것처럼 보이는데 실제로는 아무것도 안 하는 상태입니다.

## 2부. 예상 (실행 전에 적는다)

### 무엇을 하나
- 유력마 짝과 **같은 방식**으로 면제 표식을 준다(`_KP_MARK` 방식 그대로).
- 🔴 새 경로를 만들지 않는다. 바로 위 블록이 이미 그렇게 한다.

### 예상되는 결과
- 오늘 강등 6건이 최종에 남게 된다.
- 구좌가 는다. 소급 실측이 **+3%** 였으므로 그 정도로 본다.
- 판정 명단 `extra` 에 `pair` 가 잡히기 시작한다(지금 0건).

### 틀릴 수 있는 지점
- 구좌가 3%보다 훨씬 많이 늘면 **면제 범위가 넓은 것**이다.
  유력마 짝은 경주당 1개인데 자체검사는 최대 2개라 그럴 수 있다.
  그 경우 상한을 1로 줄이는 것을 검토한다.
- 면제를 줬는데도 안 남으면 **다른 곳에서 또 자르는 것**이고, 그때는 그 지점을 찾는다.

## 3부. 결과

(실행 후 채운다)

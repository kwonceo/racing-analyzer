# 적중왕 추천 로직 지도 (LOGIC_MAP.md) — 2026-07-23

> Claude Code 세션용 파악 가이드. app.py는 23만 줄+ 이므로 **이 지도의 함수명으로 grep해서** 이동할 것.
> ⚠ 원칙: 파악 먼저 보고 · 기존 기능 삭제 금지(추가만) · 적중판정 기준 변경 금지 · 로직 변경은 리플레이 통과 후
> · `py app.py` 직접 실행 금지(서버 1대 원칙 — 이미 실행 중) · git push는 권대표 승인 후.

## 1. 추천 파이프라인 (데이터 → 화면)
1. **수집**: 확장(사설 복승, source=private) + 서버 oddspark(복승·쌍승·삼복승, source=oddspark_bg)
   → `_do_triple_ingest()` (검역 가드 포함: `_board_flip_suspect`, 발주시각 불일치 격리)
   → `_history_append()` (odds_history 스냅샷·급락 감지 `_excess_drop_analysis`)
2. **분석**: `_triple_analyze(rk, rec)` — 단일 거대 함수. 내부 순서:
   - 유력마 산출(keyHorses) · 신호 감지(strongSignals·역배열 inverse·급락 q_drops)
   - `early_drop_horses`/`closing_drop_horses` (급락마 보존 명단 — odds_history doc에 저장)
   - **corePicks 생성**: `_final_picks()` (복승 ①②③·삼복승 — grep `def _final_picks`)
   - `_apply_profit_strategy()` — 수익성 3분류 후처리(저배당=삼복승 집중·EV 컷·quinellaRef 강등)
   - **삼복승 축보험** 블록 (grep `삼복승 축보험`) — 축2두+💎고배당/확신도1위 삽입
   - 단통 재편성 `dansungPlan` · 고배당 복병 이동 (grep `고배당(25배`) — 복승을 💎섹션으로 추방
   - 히스테리시스 `_apply_rec_hysteresis()` — 표시 안정화(교체 상한 2회)
   - 등급 `raceGrade` 산출 + `_GRADE_LOCK` (T-7/T-5 핀·마감 동결)
3. **기록·판정**: `_build_analysis_log()` — rec_history 누적 + **displayedCombos**(복승 전체+삼복승 상위2
   = 화면 표시 = 적중 판정 명단·마감 동결) + 마감 확정 행(closed) 추가
4. **알림**: `_kakao_rich_message()`/`_kakao_notify_race()` (T-7/T-5) + `_timeline_snap_tick()`
   (🔁 즉시 변경 알림·`_KAKAO_SENT` 파일 영속화)
5. **복기**: `review_engine.py` — sweep(분류·급락밴드 drop_stats·유력마/복병 signal_place_stats) +
   replay_day(정책 6종: baseline/signal_gate/lowodds_trio/t5·t2·t1_freeze)

## 2. 실증된 모순 7건 (2026-07-21~23 실경주 — 수정 후보의 근거)
1. **원 메인 밀림** (도야마 9R 7/22): 삼복승 원 메인 1+2+5(125배 적중)가 축보험/재편성 삽입에 밀려
   3순위(보조·판정제외)로 강등. → "원 메인은 상위 2 보존" 안.
2. **고배당 복병 이동이 정답 추방** (도야마 9R): 복승 1+2(32배 적중)가 "25배↑ 고배당 복병 이동" 규칙으로
   판정 밖 💎섹션 이동. → 임계(25배) 재검토 안.
3. **삼복승 축 몰빵** (오다와라 6R 7/23): 삼복승 4개 전부 복승① 4+5 축 — 복승②(2+4 적중 25.1배)와
   ③(1+7)의 교차 조합 2+4+7 미생성. → "복승② 축 삼복승 1개 보장" 안.
4. **확신도1위+고배당유력마 조합 미편성** (아오모리 3R 7/23): 확신도1위 3번(1착)+💎고배당 유력마
   1번(2착, '삼복승 보험 필수' 표시까지) — 복승 1+3(11.4배 정답) 미생성.
5. **축 고정** (소노다 4R 7/23): 마감 유력마 1·2위 = 12·8인데 복승 조합 전부 4번 축(4+12/11+12/4+8) —
   정답 8+12 미편성. 삼복승 2+8+12도 재료(2번·8번 급락 감지 완료)만 있고 미생성.
6. **신호→편성 3분 지연** (소노다 4R): 8번 자금집중 신호 12:07:04 감지("추천 필수" 마킹)했으나
   편성 반영은 12:09:54(마감 6초 전) — 배당 순위 역전 완료를 기다리는 구조.
7. **마감 직전 교체의 양면** (사세보 1R vs 소노다 2R 7/23): 늦은 변경이 정답을 넣기도(사세보 1R 4+6
   복귀 적중), 정답을 버리기도(소노다 2R 2+3 폐기→미적중). 동결 커브(T-5/T-2/T-1)로 계측 중 —
   7/22 기준 T-2 동결이 밸런스 최고(ROI 264·적중 29 vs 현행 287·24).

## 3. 검증된 신호 강도 (실측 — 편성 개선의 재료)
- **T-5 유력마 1위: 입상률 64%** (사흘 120표본, 기대 41% 대비 +23%p, 1착률 ~25%) — 최강 신호.
- T-5 유력마가 마감 시점 유력마보다 정확(62~65% vs 48~52%) — 마감 재편이 판단을 깎음.
- 전구간 15%+ 꾸준 급락마: 입상률 +10~17%p 우위. 마감 순간 급락·50%+ 극단 급락은 우위 없음(오염 의심).
- 급락 복병(darkT5): 7/21 +9%p vs 7/22 -8%p 혼조 — 검역 가드(7/23 낮 배포) 이후 데이터로 재평가.

## 4. 작업 규약 (Claude Code 세션 필수)
- 수정 전 반드시 해당 블록을 읽고 영향도 보고 → 권대표 승인 → 수정.
- 편성 로직 변경은 **review_engine.replay_day에 정책 변형을 먼저 추가**해 과거 데이터 ROI/적중률 검증 통과
  후에만 본 코드 적용 (선례: 경마 신호 게이트 — 리플레이 탈락으로 보류됨).
- 검증: `py -m py_compile app.py` (서버는 Werkzeug 자동 리로드 — 재시작 불필요).
- 판정(displayedCombos·적중 기준)·CORS·카카오 Secret·Railway·학습 데이터 절대 금지.
- 커밋: 작은 단위, 메시지 한국어. push 전 권대표 확인.
- 유용한 도구: `http://127.0.0.1:8011/api/debug/race-data?key=경주키`(수집 스냅샷 소스별),
  `/race-log?key=`(추천 변화 이력), `/review-report`(일일 복기), `/api/review/replay?date=`(정책 비교),
  `/daily-cards?date=`(날짜별 카드).

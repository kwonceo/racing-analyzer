# CHANGELOG

경마 BMED 분석기 개발 히스토리. [Keep a Changelog](https://keepachangelog.com/ko/) 형식을 따르며, 버전은 `git log`의 주요 마일스톤 커밋에 태그로 고정한다.

> 태그 매핑: `v1.0.0`=`743e509` · `v2.0.0`=`bab0cbf` · `v2.1.0`=`e5f9028` · `v2.2.0`=`ab772d7` · `v2.3.0`·`v2.3.0-stable`(AI 준비 완료 체크포인트, 현재 최신)
> ⚠️ 사용자 초안의 "v2.1.2 현재 최신"은 실제 진행(확장 2.1.4 + 사전분석·공식개선)을 반영해 **v2.2.0**으로 확정함.

---

## v2.3.0 (2026-07-05) — 이상감지 영구 누적 + 마감 전 3단계 알림 · **현재 최신**

### 추가된 기능
- **📊 배당 매트릭스 전종목 — 복승 삼각 매트릭스 + 유력/제거/복병/역배열 색상·급락강조·예산·유사케이스**(`static/js/app.js renderBmedMatrix`·`renderBmedMatrixPanel`·`_matrixRoleMap`·`_MATRIX_COLOR`): 전 종목 분석 화면에 "📊 배당 매트릭스" 토글 패널 추가(사용자 요청). **[1·전종목]** `renderTripleAnalyze`(일본·경륜·중앙·경정·바이크)+`renderKoreaIntegrated`(한국)에 주입 → `a.quinella`(복승 배당)로 삼각 매트릭스. **[2·종목특성]** 경륜은 헤더에 각질(styleType)·競走得点 등급(absGrade/competScore) 표시. **[3·자동색상]** 유력마⭐녹색·복병🟣보라·제거권장△주황·확실제거❌빨강(`_horseRoleMap`+darkHorses 확장), 역배열 노란 강조(inverse.invHorses·outline). **[4·호버근거]** 셀 title 에 조합·배당·역할태그·급락% 표시. **[5·예산]** 네덜란드식 배분(betRecommend alloc·예산 입력 시 금액). **[6·실시간]** 30초 재렌더 자동 반영 + 급락(-20%↓) 셀 빨강 테두리+▼, 추천 조합 금색 테두리, 유력×유력 녹색. **[7·유사케이스]** patternMatch.confidence 적중률·표본. **[8·타임라인]** 기존 renderSignalTimeline 패널 유지. 토글 열림상태 30초 재렌더에도 유지(`window._bmedMatrixOpen`+ontoggle). 검증: 합성(마번4·6조합·유력2/복병1/제거1·역배열1·급락1·유사47%) 로직 정상·`new Function` 구문 OK·CSP 없음(인라인 ontoggle 동작). **프론트 전용**(서버·확장 무변경·ZIP 불필요). 기존 매트릭스(캡처용 renderOddsMatrix)·분석 **무삭제**(신규 매트릭스 패널만 가산).
- **🇰🇷 KRA 한국경마 분석 강화 — 기수 복승률 자동주입 + 단승 인기순위·역배열 활성**(`app.py _kra_jockey_map`·`_jockey_rate`·`_bmed_before_card`·`_bmed_expert_cards` + `/api/kra/ingest-detail`): 활성 KRA API로 한국경마 BMED 강화(사용자 요청). **[2번 기수 복승률 자동주입]** 현직기수정보(전 경마장)→`{기수명: 복승률(1+2착)·삼착률·출주}` 6시간 캐시(`_kra_jockey_map`·실패 시 jockeys.json 폴백)→**BMED E카드·사전분석 주목마에 `jockeyQuinellaRate` 자동 첨부**("기수 김용근 복승률 28%(출주 6708)"). **[3번 사전분석 강화]** 발주 전 B(사전) 주목마에 기수 복승률 표시→전문가 수준 카드. **[1번 단승→인기순위·역배열]** E카드에 단승 배당 오름차순 `favRank`(인기순위) 추가 + `POST /api/kra/ingest-detail`(경주별상세성적표 단승→파이프라인 주입→한국경마도 단승 인기순위·복승불일치=역배열 분석 활성, ⚠확정배당=복기/학습용). 검증: 라이브(마이아 복승률 33%·김용근 28%)·E카드(6번 인기1위 단승1.8 기수 복승률28%)·B카드(마이아 33%)·단승주입 인기순위(6번1.8=1위)·`import ast` OK. **서버 전용**(확장 무변경). 기존 분석·jockeys.json **무삭제**(복승률 주입·favRank·주입 엔드포인트만 가산).
- **🇰🇷 KRA 승인 공공API 연동 — 경주별상세성적표(단승·연승 배당)·현직기수정보(복승률) 활성 확인·서버 연동**(`app.py _kra_api_get`·`_kra_jockey_winrate`·`KRA_APIS` + `/api/kra/apis-test·race-detail·jockey`): 이미 승인된 KRA API 4종 실테스트 → **2종 활성 확인**(키 유효). **✅ 경주별상세성적표**(`racedetailresult/getracedetailresult`): `win`=단승배당·`plc`=연승배당·`rcTime`=기록·`differ`=착차·기수·조교사·마체중 → `/api/kra/race-detail`(단승 오름차순 **인기순위** favRank 도출·사용자 요청 '단승→인기순위'). **✅ 현직기수정보**(`currentjockeyInfo/getcurrentjockeyinfo`): 1/2/3착 통산·출주수 → **복승률(1+2착)·삼착률** 계산 `/api/kra/jockey`(BMED E카드 '기수 복승률' 직결). **❌ 경주성적정보·기수통산성적비교**: 엔드포인트 404/500(미확정) → `apis-test` 가 상태 표시. `/api/kra/apis-test` 로 4종 활성/필드 일괄 확인. 검증: 라이브(서울 20260704 1R 단승 1.8→인기1위·기수 마이아 복승률 33%)·`import ast` OK. ⚠ 확정배당율 API160_1(별도) 은 여전히 500(미활성)이라 이 활성 2종을 먼저 연동. **서버 전용**(확장 무변경). 기존 `tools/fetch_kra.py` 배치·jockeys.json **무삭제**(라이브 조회 엔드포인트만 가산).
- **🧭 BMED 사전분석 시스템 — B(사전)·M(신호)·E(해석)·D(결론) 통합 뷰**(`app.py _bmed_before_card`·`_bmed_expert_cards`·`_race_post_epoch` + `GET /api/bmed/<raceKey>`): 발주 전에 완성된 분석을 한 번에 제공(사용자 요청·보고 바로 베팅). **[B 사전분석]** 배당 전에도 starters(전적)로 주목마 TOP3(전적·기수·각질·등급)+사전예상복승(전적 이중수렴 상위2두). **[M 실시간신호]** 급락(≤-20%)·역배열·스마트머니 요약+전략. **[E 전문가해석]** 각 말 상세카드(전적등급·각질·기수·현재배당·흐름점수). **[D 최종결론]** corePicks 복승/삼복승(≤2)+프리미엄 알림등급. **[phase]** 발주 남은분으로 before(T-10분+)→market(수집중)→decision(T-3분내)→closed 자동 전환(분석 minutesBefore 우선·없으면 스케줄 postEpoch). 기존 `_triple_analyze`·starters·`_premium_alert` 조립(재계산 최소)·비정형 필드 안전 map(`_bmed_no_map`). 검증: 실 starters로 B카드(사전예상복승 3+7·주목마 전적/기수)·합성 배당으로 E카드(등급·각질·배당·흐름)·D(복승·삼복승·🔴즉시)·`import ast` OK. **서버 전용**(확장 무변경). 기존 분석·리포트 **무삭제**(BMED 조립 뷰만 가산).
- **🔔 프리미엄 알림 시스템 — 등급별(🔴즉시/🟠예고/🟡사전) 실시간 알림 + 유사케이스 적중률**(`app.py _premium_alert`·`_premium_alert_format`·`_strategy_hitrate` + `GET /api/alerts/premium`): 361경주 적중 분석(신호성숙 T-10분+ 57%·전략별 보험형48.9%·역배열형46.6% vs 전략없음 2.8%) 근거로 설계·구현. **[등급]** 🔴즉시(T-3분·급락≤-30%|역배열|스마트머니)·🟠예고(T-5분·급락≤-20%|역배열형성)·🟡사전(T-10분·급락≤-15%|정답조합권). **[게이팅]** 전략/신호 없는 경주는 알림 제외(오알림=신뢰하락 방지). **[포맷]** 카카오톡/푸시용 텍스트(마감 남은시간·신호요약·복승/삼복승·예상배당·`유사케이스 N%(전략 M경주)`) — `_strategy_hitrate`(race_results 스캔·1시간캐시, BMED_ 접두어 방어 lookup). `/api/alerts/premium`(활성경주 스캔·등급정렬·`?format=text`·`?grade=` 필터). 검증: 합성 T-2.5분 강신호→🔴즉시 포맷 정확(목업 일치)·T-8분약신호→🟡사전·무신호→게이팅(None)·전략적중률(역배열47%·보험49%) 실집계·`import ast` OK. **서버 전용**(확장 무변경). 기존 알림(경고신호·마감전3단계)·분석 **무삭제**(프리미엄 알림 엔드포인트만 가산).
- **🇰🇷 KRA 공공API(확정배당율 통합 API160_1) 연동 — 단승/복승/쌍승/삼복승 서버 수집(확장 폴백 유지)**(`app.py _kra_fetch`·`_kra_parse_items`·`_kra_collect_one`·`_kra_auto_loop` + `/api/kra/test·collect·watch·unwatch·status`): 한국마사회 data.go.kr `B551015/API160_1` 를 **추가 소스**로 연동(사용자 요청). 단승(WIN)→인기순위·복승(QNL)→기존유지·쌍승(EXA)→역배열 감지·삼복승(TLA)→보험 조합을 `_do_triple_ingest`(sport=horse·category=korea)에 주입 → 한국경마도 역배열·삼복승·급락 자동 활성. **30초 자동수집**(`/api/kra/watch` 등록 경주만·데몬 멱등). 키는 `.env KRA_API_KEY`(gitignore·커밋 금지·`_kra_api_key()`로 로드)·`.env.example` 문서화. 응답 필드명은 관례 후보(chulNo1~3·divRate·ssMark 등) 방어적 흡수 + `/api/kra/test` 로 원시응답·파싱결과 확인(라이브 검증용). **⚠ 기존 Chrome 확장 한국경마 배당 수집은 폴백으로 완전 보존**. 검증: 엔드포인트 5종 정상(키 로드·watch/unwatch·30초 데몬 기동)·`import ast` OK. **⚠ 현재 API가 진짜키·가짜키 모두 HTTP500 반환(키 미활성 또는 서비스오류 추정) → 키 활성화 후 `/api/kra/test` 로 필드 확정 필요**(확정배당율=경주 후 값이라 실시간 예상배당과 차이 가능성도 확인 요). **서버 전용**(확장 무변경). 기존 수집경로 **무삭제**(KRA 소스만 가산).
- **🚴 경륜 스케줄 서버 직접 수집 — 지방경마 + 경륜 동시 자동수집(Chrome 확장 불필요)**(`app.py _keirin_schedule`·`_keirin_venue_races`·`_keirin_post_time`·`_multi_schedule_fetch`): 경륜 스케줄이 확장(로그인 세션) 경유 `POST /api/multi/keirin-schedule` 로만 들어오던 것을 **서버가 oddspark 공개경로에서 직접 수집**하도록 추가(사용자 요청·oddspark 배당/출주표 로그인 불필요 확인). **[개최 감지]** `KaisaiRaceList.do` 는 로그인 벽이라, **경륜 홈**(`oddspark.com/keirin/`·공개)의 `RaceList.do?joCode&kaisaiBi=오늘` 링크에서 개최 joCode 추출. **[경륜장·경주·발주]** 각 joCode 의 공개 출주표(`RaceList.do?...&raceNo=1`) title 에서 경륜장명(예 宇都宮=24)·경주번호(1~12), 경주별 페이지 `発走時間 HH:MM` 로 발주시각(태그 제거 후 매칭·경마와 구조 차이) 병렬취득(6시간 캐시). `_multi_schedule_fetch` 가 지방경마에 이어 **경륜 트랙(sport=cycle)도 병합** → `_multi_collect_one`(기존 joCode 경로)이 배당·전적 서버 자동수집 → 대시보드에 경륜 카드 표시. **⚠ 기존 확장 경유 `/api/multi/keirin-schedule` 완전 보존**(서버 수집과 병존·오버라이드 가능). 검증: 실측 오늘 개최 joCode 8곳(24 宇都宮 등)·발주시각(R3 11:34·야간 21:20) 추출·`import ast` OK. **서버 전용**(확장 무변경). 기존 경마 스케줄·확장 경로 **무삭제**(경륜 서버수집만 가산).
- **📄 경륜·일본경마·다중경주 공개 API 문서 + 내일 테스트 체크리스트**(`docs/PUBLIC_API.md`, `docs/TOMORROW_TEST.md`): bmed-public 연동/내일 경륜·일본경마 테스트 대비. `PUBLIC_API.md` 에 경륜(`/api/keirin/card·starters·odds`)·일본경마(`/api/keiba/odds·current·schedule·starters`)·다중경주(`/api/multi/schedule·collect·dashboard·race`) 요청/응답 형식 표 + bmed-public 프록시 매핑(`/api/public/{keirin,keiba,multi}/*`) 정리. `multi/dashboard` 가 `triple_store` 병합으로 **경륜·일본경마 카드 포함**(경정 제외·`bySport` 구분)임을 명시. `TOMORROW_TEST.md` 는 경륜/일본경마 테스트 순서·확인 화면·오류 대처표·curl 스니펫. 실측: 프록시 경유 `multi/dashboard`·`multi/schedule`·`keiba/schedule` 200 확인. **문서 전용**(코드 무변경). 기존 API **무삭제**.
- **🌐 공개 서비스 연동 준비 — CORS·공개 API 정리·접근등급 구조·응답 민감정보 제거**(`app.py`, `requirements.txt`, `docs/PUBLIC_API.md`): bmed-public(공개 프론트) 연동을 위해 외부 API 호출 기반 정비(사용자 요청). **[1 CORS]** `flask_cors` 도입(미설치 시 수동 `after_request`+OPTIONS preflight 폴백 → 서버 기동 보장) — 허용 오리진: bmed-public Railway(`web-production-d4723.up.railway.app`)·`localhost:8012`/`8011`·**ngrok 와일드카드**(`*.ngrok.io`/`*.ngrok-free.app`/`*.ngrok.app`, 정규식 앵커링)·`*.up.railway.app`. `/api/*` 만 적용·`GET/POST/OPTIONS`. **[2 공개 API]** `/api/multi/schedule`(오늘 경주)·`/api/multi/dashboard`(현황)·`/api/odds/triple/latest`(최신 배당)·`/api/odds/signal-timeline`(신호)·`/api/odds/triple/analyze`(분석)·`/api/learning/stats`(통계) 응답 형식 `docs/PUBLIC_API.md` 정리(전부 200 확인). **[3 접근 등급]** 향후 인증 차단 구조 신설(`PREMIUM_ENFORCED`·`PUBLIC_ENDPOINTS`/`PREMIUM_ENDPOINTS`·`before_request` 게이트·`GET /api/access/policy`) — **현재 전부 오픈**(게이트 즉시 통과), True 승격 시에만 프리미엄(BMED 신호 상세·복승/삼복승 추천) 인증 요구(402). 기존 라우트 코드 무수정(비침습). **[4 health]** `/api/health` 에 `status`/`version`(=`APP_VERSION` 2.3.0)·`cors` 필드 추가(기존 `ok`/`model`/`has_key`/`pdf_ready` 보존). **[5 민감정보]** `/api/health` 에서 `pdf_error`(내부 경로) 제거 + 프로덕션(`FLASK_ENV=production`) 500 에러를 `str(e)` 대신 일반 메시지(`internal_error`)로 응답(로컬 debug 는 상세 유지). 검증: 실 HTTP(8099 임시 인스턴스) localhost:8012→허용 오리진 `Access-Control-Allow-Origin` 반영·evil.com 차단·OPTIONS preflight 200·PREMIUM_ENFORCED=True 시 공개200/프리미엄402/인증200 + `import ast` OK. **서버 전용**(확장 무변경·ZIP 불필요). 기존 엔드포인트·응답 필드 **무삭제**(신규 필드/구조만 가산).
- **🚴 경륜 전적 절대등급(競走得点) 통합 뷰 표시 — 상대 사분위 + 절대 95/85/75 등급 병행**(`app.py _keirin_autocollect_form`·`_form_from_starters`, `static/js/app.js renderFormGrades`): 경륜 통합등급이 사분위 상대등급(경주 내 A/B/C/D)만 표시돼 절대 競走得点 등급이 안 보이던 것 보완(직전 경륜 전적 자동수집 후속). `_keirin_autocollect_form`이 각 선수에 **`competScore`(競走得点)·`absGrade`(95+A/85+B/75+C/<75D)**를 저장 → `_form_from_starters`가 통합 form에 전달(기존 사분위 `grade`는 무삭제 유지) → 전적 표에 **절대등급(競走得点) 컬럼** 추가 표시(경륜은 마번→차번·기수→각질·제목 '경륜 출마표·競走得点'으로 자동 전환). 검증: 오다와라 3R(平石 competScore 86.8→절대 B·상대 A / 高峰 84.3→절대 C·상대 A 등 상대·절대 동시 표시) + 회귀(report69·formula24·flow23·prerace18) 통과. app.py `import ast`·app.js `new Function` OK. **서버+프론트만**(확장 무변경). 기존 사분위 등급·전적 표 **무삭제**(절대등급 컬럼·competScore만 가산).
- **🚴 경륜 출마표 전적 자동 수집 — 배당 수집 시 동시에 oddspark 전적(競走得点·각질·등급) → 통합등급 자동 반영**(`app.py _keirin_autocollect_form`·`_keirin_jo_from_venue`·`_do_triple_ingest`·`keirin_odds`·`_multi_collect_one`·`KEIRIN_JO`): 경륜은 복승+쌍승만 수집·전적 없던 것을(수동 `/api/keirin/starters` 엔드포인트만 존재·starters_store에 경륜 0건) **배당 수집 시 자동으로 전적까지** 수집(사용자 요청). **[자동 수집]** `_keirin_autocollect_form`이 oddspark 출마표(`RaceList.do?joCode&kaisaiBi&raceNo`)를 fetch·파싱(기존 `_keirin_parse_card`·`_keirin_analyze` 재사용) → 선수번호·명·競走得点·최근착순·결정수(逃/捲/差)·각질(선행/추입) → STARTERS_STORE(source=keirin) 저장 → `_form_from_starters`→`_triple_analyze` 통합등급(A/B/C/D) 자동 반영. **[joCode 자동 감지]** raceKey 경륜장명 → joCode 역매핑(오다와라36·히로시마62·**마에바시01·기후04** 추가). **[전 경로 커버]** `_do_triple_ingest`(sport=cycle) 훅으로 **확장 asyukk 수집**도 자동, `keirin_odds`·`_multi_collect_one`(다중경주) 명시 호출. 1회만 fetch(이미 저장 시 생략)·실패해도 배당 흐름 무영향. **[등급 스펙]** 競走得点 95+ A/85~94 B/75~84 C/<75 D(`_keirin_grade`·카드 분석)·통합등급은 기존 사분위 상대등급. 검증: 라이브(오다와라 3R 7명·平石 91.8 등·source=keirin 저장·통합등급 A/B/C/D 반영)·joCode 자동감지·확장경로 훅 + 회귀(report69·formula24·flow23·multi19·reversal29) 통과. app.py `import ast` OK. **서버 전용**(확장 무변경·ZIP 불필요). 기존 수동 엔드포인트·경마 전적·통합등급 **무삭제**(경륜 자동 수집·joCode 역매핑만 가산).

### 수정된 버그
- **🚂 Railway/gunicorn 백그라운드 스케줄러 미기동 수정 — 24시간 자동수집 활성화**(`app.py _boot_background`·`Procfile`, `docs/RAILWAY_DEPLOY.md`): 다중경주 자동수집(`_start_multi_race_bg`)·6시간 백업·학습일지·날짜정리가 `if __name__=="__main__"` 안에만 있어 **gunicorn 배포(Railway)에서 전부 스킵**되던 치명 블로커 수정(24시간 자동수집 목표). 백그라운드 기동을 `_boot_background()`(멱등)로 묶고 **모듈 로드 시점(`SERVER_SOFTWARE=gunicorn`)에도 호출** → gunicorn 에서도 자동수집·백업·학습 정상 기동. **Procfile `--workers 1 --threads 8`**(워커 다중 시 수집·git백업 중복 방지). `docs/RAILWAY_DEPLOY.md` 신규(임시 파일시스템 방어=Railway Volume `/app/data`·환경변수·TZ·점검 체크리스트). 검증: 로컬 import 시 미기동(안전)·gunicorn 조건 매칭·`import ast` OK. 기존 로컬 `__main__` 기동 경로 **무삭제**(WERKZEUG_RUN_MAIN 유지·`_boot_background` 재사용).
- **🐛 `_pattern_confidence` 함수명 충돌 수정 — 패턴매칭 신뢰도가 매 분석 실패하던 버그**(`app.py`): 동명 함수가 2개 정의(2인자 베팅용 신뢰도 산출 + 0인자 신호유형 뷰) → Python 이 뒤 정의(0인자)로 앞을 덮어써, 분석 파이프라인의 `_pattern_confidence(cur_patterns, _pstats)`(2인자) 호출이 매번 `takes 0 positional arguments but 2 were given` 로 실패(`[패턴매칭] 실패` 로그 다발)하던 것 수정. **0인자 뷰 함수를 `_pattern_confidence_view` 로 분리**하고 유일 호출부(`/api/learning/pattern-confidence`) 갱신 → 2인자 베팅용 `_pattern_confidence` 복원. 이로써 패턴 신뢰도 기반 복승/삼복승 비중 자동조정(`_adjust_bet_weights`)이 정상 작동(그간 예외로 통째 스킵됨). 검증: 다중 경주 분석 시 `[패턴매칭] 실패` 0건·두 엔드포인트(pattern-confidence 뷰·triple/analyze patternMatch) 200·`import ast` OK. **서버 전용**(확장 무변경). 기존 두 함수 로직 **무삭제**(개명·분리만).
- **🔮 경주 재현 리포트 "경기 전 추천마"에 입상마 혼입 수정 — 유력마(우리 추천)만 표시**(`app.py _build_race_report`, `static/js/app.js _reportCompareStrip`): 리포트 `why_recommended`가 `top3(실제 입상마)+keyHorses(유력마)`를 합쳐 만들어져 **우리가 추천하지 않은 입상마까지 "경기 전 추천마"에 섞여** 표시(후쿠시마 8R 12·6·9 입상마가 추천마 8두에 포함돼 "3/8두 입상 38%"처럼 혼동)되던 문제 수정(사용자 요청: "우리가 추천한 말들이 유력마 기준으로 보여져야 헷갈리지 않아"). **[백엔드]** 리포트에 실제 추천마 집합 **`recommended_horses`**(우선순위: keyHorses 유력마 → 없으면 betRecommend 복승/삼복승 메인 조합의 말) 신설 + 각 `why` 항목에 **`recommended` 플래그**(유력마 여부) 추가. **[프론트]** `_reportCompareStrip`의 `preNos`(경기 전 추천마·적중률)를 `recommended_horses` 우선 → `recommended` 플래그 → (구 리포트) why 전체 폴백 순으로 산출 → 입상마 혼입 제거, 적중률/총평이 실제 추천 기준으로 정확해짐. **⚠ 기존 `why_recommended`(입상마 신호 분석 포함)·타임라인 전부 보존(무삭제) — 표시 기준만 유력마로 좁힘**. 검증: app.py `import ast`·app.js `new Function` OK. **서버+프론트만**(확장 무변경·ZIP 불필요).
- **🎯 추천 과다 근본 수정 — 복승 ≤2·삼복승 ≤2 (총 4개)로 엄격 압축·나머지 숨김**(확장 v2.1.79, `app.py _final_picks`·`_triple_analyze`, `chrome-extension/overlay.js`, `static/js/app.js renderCorePicks`): 7마리 경주에 추천이 9개까지 나와 "사실상 전부 사라"가 되던 문제 근본 수정(사용자 요청). 여러 파생 추천(확신도 복승·복병 자동삼복승·초기/마감급락 보존·스마트머니 복승·무신호밀집 박스…)이 모두 화면에 쌓여 의미가 없던 것을, **최종 복승 ≤2 · 삼복승 ≤2 (총 4개)** 로 압축. **[선정 우선순위]** 복승: ①시장 최저복승 ②확신도1위+시장유력 ③스마트머니+시장유력(중복제거·상위 2개). 삼복승: ①삼복승 메인(확신도/축) 고정 ②가장 강한 자동 1개 = **추정배당 낮은 순**(적중 확률 높은 순)으로 복병·급락보존·밀집박스·확신도보험 중 최강 1개(상위 2개). `core_picks.finalQuinellas`/`finalTrifectas` 신설. **[표시]** 오버레이·핵심추천 카드를 **"🎯 지금 사세요!" + 복승 2줄 + 삼복승 2줄** 딱 이것만 표시로 재작성(복병/급락보존/밀집박스 개별 라인·삼복승 보험 라인 전부 숨김). 구 캐시 데이터엔 confQuinellas/quinella·삼복승 폴백. **⚠ 기존 파생 필드(confQuinellas·darkTrifectas·earlyDropTrifectas·closingDropTrifectas·denseBoxTrifectas 등)·betRecommend·학습 전부 보존(무삭제) — 표시만 4개로 압축**. 검증: 합성(7두·파생 9개 → 복승 6+7·2+6 / 삼복승 1+6+7·4+6+7 = 목업 정확 일치·총 4개) + report69·flow23 회귀 통과. app.py `import ast`·app.js `new Function`·overlay.js `node --check` OK + ZIP 재빌드(v2.1.79). ⚠ 확장 새로고침 필수.

### 추가된 기능
- **⏱ 조기 수집 강화 + 무신호밀집 박스 학습·배분 연동(보완 4·3·2)**(확장 v2.1.78, `chrome-extension/content.js`·`background.js`, `app.py _triple_analyze`·`_apply_result_learning`·`_recompute_learning_stats`): 직전 무신호밀집 박스 기능의 보완 3건. **[4번·조기수집(근본해결)]** 코치 8R처럼 배당판이 늦게 열려 T-2분에야 첫 수집되던 문제 완화 — ①경륜 전용이던 **배당판 로드/탭클릭/포커스 즉시수집 킥스타트(`_maybeKeirinKickstart`)를 경정(boat)·바이크(bike)까지 일반화**(사설 asyukk 배당판 열리는 즉시 수집 시작) ②`background.js autoTick` **마감 10분전 수집 간격 60초→30초 단축**(흐름 조기 포착) + **T-10분 "조기 배당 감시 시작" 알림** 1회. **[3번·박스 학습 연동]** early_drop/closing_drop과 동일하게 결과 입력 시 `dense_box_fired`(발동)·`dense_box_hit`(박스 삼복승=top3 3두 모두 박스 포함 시 적중)·`dense_box_nos` 기록 → `conf_pick_stats.dense_box`(발동 빈도·적중률) 누적. **[2번·배분 연동]** `denseBoxTrifectas`를 표시만 아니라 **예산 배분(betRecommend)에 편성** — 무신호라 혼전블록 미발동으로 이 시점 bet_rec이 사실상 최종인 점을 이용, 박스 조합을 **전체 예산의 20%로 균등 배분**(box/(기존합+box)=0.2 → 기존합×0.25를 조합수로 나눔·`삼복승 박스(무신호밀집)` 라벨·중복조합은 기존 배분 유지). 검증: content.js·background.js `node --check` OK + report69·flow23 회귀 통과 + dense_box 학습 필드 배선 확인. app.py `import ast`·app.js `new Function` OK + ZIP 재빌드(v2.1.78). ⚠ 확장 새로고침 필수. 기존 수집 루프·간격 단축·킥스타트(경륜)·박스 감지 **무삭제**(경정/바이크 일반화·T-10분 강화·학습/배분 훅만 가산).

### 수정된 버그
- **⚠️ 무신호 저배당밀집 삼복승 박스 확대 — 급락·역배열·복병 0건 + 상위배당 밀집 시 축을 3두→4~5두로 확대**(확장 v2.1.77, `app.py _dense_no_signal_box`·`_triple_analyze`, `chrome-extension/overlay.js`, `static/js/app.js renderCorePicks`): 이상감지 신호가 전무한 저배당 밀집 경주에서 시장 최저 대장주가 배신하고 근소 4위 저배당말이 입상해 3두 컷으로 놓치던 버그 수정(코치 경정 8R 2-1-6·사용자 요청). **근본 원인**: 배당이 T-2분부터 고정(급락·쌍승역전·복병·강신호 0건)이라 순수 배당순위(5·6·2)로만 추천 → **시장 최저 대장주 5번(5+6=5.7 시장1위) 착외 붕괴**, **근소 4위 1번(1+5=9.2배)이 2착**인데 `key_horses=상위3두`라 한 칸 차이로 탈락 → 정답 삼복승 1+2+6 놓침(복승 5+6·삼복승 2+5+6 모두 미적중). **[해결]** `_dense_no_signal_box` 신설 — ①강한 급락(≤-30%)·쌍승역전·복병·강신호가 **전부 0건**이고 ②대표배당(단승 우선·없으면 최저복승) **15배 이내가 4~5두 밀집**일 때, 삼복승 박스를 그 **4~5두 C(n,3) 전 조합**(추정배당 낮은 순 최대 6개·`core_picks.denseBoxTrifectas`)으로 확대. **신호가 하나라도 있으면 비활성**(기존 3두 축·혼전 박스 그대로 유지·무삭제). 오버레이·핵심추천 카드에 "⚠️ 무신호 밀집 경주 → 삼복승 박스 확대(N·M·X·Y번)". **[결과 학습]** 코치 8R(2-1-6) 정식 입력(복승·삼복승 미적중·무신호_저배당밀집_대장주배신). 검증: 합성(코치 8R 재현 → 박스 [5,6,2,1]에 정답 1+2+6 포함·급락 -40% 주입 시 비활성) + 회귀(report69·formula24·flow23) 통과. app.py `import ast`·app.js `new Function`·overlay.js `node --check` OK + ZIP 재빌드(v2.1.77). ⚠ 확장 새로고침 필수. 기존 유력마 3두·혼전 복승 박스(`_chaotic_race`)·확신도/복병/급락 보존 **무삭제**(무신호 시에만 별도 필드로 박스 확대 가산).
- **🔧 복승 축 교정 — 복승불일치 🔴🔴 감지 시 자금이탈 고배당말을 축에서 강등하고 시장 최저복승 조합을 복승 축으로 강제**(`app.py _triple_analyze`, `static/js/app.js renderSignalQuality`): 단승 인기(예: 3·6)로 복승 축이 잡혔으나 시장 실제 최저복승은 다른 조합(3+10)이라 정답을 놓치던 버그 수정(코치 경정 7R 복기·사용자 요청). **근본 원인**: `key_horses = ranked[:3]`가 단승 순위(`single_rank`)를 최우선해 단승 인기말(6번·자금이탈)이 복승 축이 되고, 복승 시장 최강말(10번·3+10=3.3배)이 축에서 배제됨. `_quinella_mismatch`가 이미 🔴🔴 ratio 9.515로 "10번 집중 자금유입"을 정확히 경고(결과 입력 시 `win_tags=복승불일치_적중`·10번 2착 확증)했으나 추천이 미반영. **[해결]** `_triple_analyze`의 최종 유력마 확정 직후 **[복승불일치 축교정]** 가드 추가 — `quin_mismatch` 레벨이 🔴/🔴🔴이면 ①예상 조합에만 있고 실제 최저복승엔 없는 **자금이탈 고배당말을 축(상위2)에서 맨 뒤로 강등** ②**시장 최저복승 조합(actual)을 복승 축으로 강제**(경마·경정·경륜 전 종목 동일). 축이 실제로 바뀔 때만 교정·`axisCorrected` 표식. 신호품질 카드에 "🔧 복승 축 교정: N+M (자금이탈 X번 강등)" 표시. **[결과 학습]** 코치 7R(3-10-2) 정식 입력(복승 3+6 미적중·유형2 페이크베팅·복승불일치_적중 태깅). 검증: 합성(단승 3·6·복승 최저 3+10 🔴🔴 9.515 → 축 3+6→3+10 교정·6번 강등) + 회귀(report69·formula24·flow23·reversal29) 통과. app.py `import ast`·app.js `new Function` OK. **서버+프론트만**(확장 무변경·ZIP 불필요). 기존 유력마·단승 우선·복승불일치 감지 **무삭제**(축교정 재정렬만 가산·단승 인기≠복승 인기 시 복승 시장 우선).
- **⏱ 마감급락 보존 — 마감 임박(T-2~0분) 뒤늦게 잡힌 급락말을 10초 flip-flop 재편에도 삼복승 보험으로 유지**(확장 v2.1.76, `app.py _history_append`·`_triple_analyze`·`_apply_result_learning`·`_recompute_learning_stats`, `chrome-extension/overlay.js`, `static/js/app.js renderCorePicks`): 시스템이 마감 임박에 급락으로 정확히 잡은 정답마가 **직후 다른 말 급락으로 10초 단위 추천 재편에 밀려 탈락**하던 반복 버그 해결(오비히로 9R 3번 2착 케이스: T-1분 3+5 -39%·2+3 -37%+쌍승역전 3→6으로 keyHorses·삼복승 3+5+7 편입 → 10초 뒤 2+5 -28% 급락으로 2번 재부각되며 3번 재탈락). 초기급락 보존(마감 5분+ 전)이 놓치는 **마감 2분 이내 구간**을 담당. **[등록]** `_history_append`가 마감 2분 이내·절대 10%+ 자금집중(`_excess_drop_analysis absStrong`)·비대규모급락일 때 `closing_drop_horses`(raceKey 히스토리 파일에 firstTime/firstMb/firstPct 영구 등록·최대 4두)에 기록 → 이후 재편에도 폐기 안 함. **[보존]** `_triple_analyze`가 복승 축 2두 + 보존말로 **삼복승 보험(`closingDropTrifectas`) 강제 편성**(초기급락·복병 조합과 중복 제거). **[표시]** 오버레이·핵심추천 카드에 `⏱ 마감급락 보존: N번 | 마감임박 감지·재편에도 유지` + `🎯 자동삼복승(마감급락 보존 조합)`. **[학습]** 결과 입력 시 보존말 입상 판정(`closing_drop_fired`/`closing_drop_placed`) → `conf_pick_stats.closing_drop` 입상률 누적. 검증: report69·flow23 회귀 통과 + 오비히로 9R 결과(1-3-5-6) 실입력(복승 5+7 미적중·유형2 페이크베팅 분류 확인). app.py `import ast`·app.js `new Function`·overlay.js `node --check` OK + ZIP 재빌드(v2.1.76). ⚠ 확장 새로고침 필수. 기존 초기급락 보존·복병·마감급락 보험(`_closing_drop_insurance`) **무삭제**(closing_drop 영구 등록·보존·학습만 가산·stateless 보험과 상호 보완).
- **🐎 복병 = 삼복승 자동 편성 + 제거 금지 — 복병 감지말이 동시에 제거 대상이 되는 충돌 해결**(확장 v2.1.75, `app.py _triple_analyze`(복병 제거금지·darkTrifectas), `chrome-extension/overlay.js`, `static/js/app.js renderCorePicks`): 복병(집중급락 10회+·스마트머니)으로 감지된 정답마가 동시에 `_elimination`에서 제거 대상으로 판정돼 최종 추천에서 빠지던 충돌 버그 수정(모리오카 6R 7번 사례·사용자 요청). **[제거 금지]** `_elimination` 결과에서 `dark_horses` 마번이 제거(not keep/override) 대상이면 **override=True로 승격**(`verdict=⭐ 복병(제거 취소)`·`overrideReason`)하고 counts 재계산·`darkSpared` 기록 → **복병 우선, 제거 취소**. **[삼복승 자동 편성]** 마감 전·복병 감지 시 복승 축 2두(`favAxis`) + 복병말로 삼복승 조합을 `core_picks.darkTrifectas`에 자동 편성(초기급락 조합과 중복 제외·실배당 없으면 `_trio_est` 추정), `darkHorsePicks`에 복병 목록 저장. **[표시]** 오버레이·핵심추천 카드에 `🐎 복병 편성(제거 금지): N번` + `🎯 자동삼복승: X (복병 편성)`. 검증: cross_reversal 회귀 포함 report69·formula24·reversal29 통과. app.py `import ast`·app.js `new Function`·overlay.js `node --check` OK + ZIP 재빌드(v2.1.75). ⚠ 확장 새로고침 필수. 기존 복병·제거·삼복승 로직 **무삭제**(복병 override·darkTrifectas만 가산).
- **🔀 복승 + 쌍승 크로스 역배열 감지 → 복승 메인 자동 편성**(확장 v2.1.75, `app.py _cross_reversal`·`_triple_analyze`, `static/js/app.js renderCrossReversal`, `chrome-extension/overlay.js`): 기존 크로스 역배열이 삼복승 보험으로만 반영돼 실질 유력마를 복승 메인에서 놓치던 문제 보완(사용자 요청). **[복승/쌍승 점수 분리]** `_cross_reversal`이 통합 `score`(기존 보존)와 별도로 **복승측 `qScore`(인기1위+X vs 인기2위+X 복승 편차)·쌍승측 `xScore`(인기1위→X vs 인기2위→X 쌍승 편차)**를 각각 산출, **양쪽 동시 역배열(0.5+·`both`)** 판정. **[복승 메인 편성]** 복승 또는 쌍승 qScore/xScore≥0.5 시 인기1위+X·인기2위+X를 **복승 메인에 자동 편성**(라벨 `복승 메인(크로스역배열·양쪽감지 복승X·쌍승X)`), `both`이면 배분 상향(강한 신호). **[표시]** 분석 카드·오버레이에 복승·쌍승 점수 + `🔁 양쪽감지` 배지. 검증: 합성(5번 복승0.71·쌍승0.71·both=True·복승 메인 편성) + report69·formula24·reversal29 회귀 통과. app.py `import ast`·app.js `new Function`·overlay.js `node --check` OK + ZIP 재빌드(v2.1.75). ⚠ 확장 새로고침 필수. 기존 크로스 역배열(삼복승 보험)·역배열 학습 **무삭제**(qScore·xScore·both·복승 메인 편성만 가산).
- **🎯 복승 축 = 시장 유력마 저배당 기준 · 고배당(30배+) 확신도말 복승 금지**(확장 v2.1.72, `app.py _confidence_picks`, `static/js/app.js renderCorePicks`, `chrome-extension/overlay.js`): 확신도 1위 말이 고배당(예: 97배)일 때 이를 복승 축으로 써서 **873배·111.9배 엉뚱한 복승**이 나오던 버그 근본 수정(하코다테 5R 확신도 복승 1+2=111.9배 케이스·사용자 요청). **[복승 축 재정의]** 복승 메인 축 = ①시장 유력마 상위 2두(저배당) ②급락/역배열 저배당 신호말 ③전적/확신도 저배당 말. **[고배당 금지 이중 가드]** 개별 대표배당 30배+ 말 복승 축 제외(`_high`, 단승 우선·중앙 JRA는 최저 복승 대체) + **조합 배당 50배+ 자체 차단**(`COMBO_HIGH`, 단승 미수집 중앙 JRA에서 1+2=111.9 같은 고배당 조합 방지). **[고배당 확신도말 재배치]** 확신도 1위가 고배당이면 복승 축 대신 **삼복승 보험**으로만(고배당 확신도말 + 유력마 2두). 삼복승 메인=시장 유력마 3두. 저배당 확신도 1위(후쿠시마 6번)는 기존대로 복승 포함 유지. 오버레이·핵심추천 카드에 "확신도1위 N번🔺고배당→삼복승보험" 표기. 검증: 사용자예시(유력마 4·12·11·확신도1위 1번 97배 → 복승 4+12·삼복승 4+11+12·보험 1+4+12) + 하코다테(1+2=111.9 차단·3+7·1+7만) + 후쿠시마(6+8·삼복승 5+6+8 정답 유지) + 회귀(report69·formula24·flow23·reversal29) 통과. app.py `import ast`·app.js `new Function`·overlay.js `node --check` OK + ZIP 재빌드(v2.1.72). ⚠ 확장 새로고침 필수. 기존 확신도 복승·삼복승·학습 **무삭제**(복승 축 선정 기준만 저배당 유력마로 교정 + 고배당 가드 가산).

### 추가된 기능
- **📌 초기 급락마 보존 로직 — 마감 5분+ 전 급락 감지말을 후반 재편에도 삼복승 보험 유지**(확장 v2.1.74, `app.py _history_append`·`_triple_analyze`·`_apply_result_learning`·`_recompute_learning_stats`, `chrome-extension/overlay.js`, `static/js/app.js renderCorePicks`): 초기(마감 5분+ 전)에 급락 절대 10%+로 잡힌 정답마가 후반 다른 말 대급락으로 최종 추천에서 탈락하던 반복 버그 해결(카나자와 8R 6번 1착·복승 2+6 34.8배 놓침·사용자 요청). **[등록]** `_history_append`가 마감 5분+ 전·절대 10%+ 자금집중(`_excess_drop_analysis absStrong`)·비대규모급락(q_drops<30)일 때 `early_drop_horses`(raceKey별 히스토리 파일에 firstTime/firstMb/firstPct와 함께 영구 등록·최대 4두)에 기록 → **후반 재편·다른 말 대급락에도 폐기 안 함**. **[보존]** `_triple_analyze`가 `early_drop_horses`를 읽어 복승 축 2두 + 보존말로 **삼복승 보험(`earlyDropTrifectas`) 강제 편성**(축과 다를 때). **[표시]** 오버레이·핵심추천 카드에 `📌 초기급락 보존: N번 (후반 재편에도 유지)` + `🛡 삼복승 보험(초기급락)` 조합·추정배당. **[학습]** 결과 입력 시 보존말 실제 입상 판정(`early_drop_fired`/`early_drop_placed`) → `conf_pick_stats.early_drop` 입상률 누적. 검증: 마감 6분전 6번 급락 등록 → 후반 2번 대급락 재편에도 6번 유지(4두 캡) + 회귀(report69·flow23) 통과. app.py `import ast`·app.js `new Function`·overlay.js `node --check` OK + ZIP 재빌드(v2.1.74). ⚠ 확장 새로고침 필수. 기존 급락·삼복승·확신도 복승 **무삭제**(early_drop 등록·보존·보험·학습만 가산).
- **💾 배당 데이터 영구 보존 시스템 — raceKey별 append-only 아카이브(탭 전환·마감·경주 전환에도 삭제 안 함)**(`app.py _history_append`·`_hist_read_any`·`_archive_compress_old`·`/api/odds/archive/list·get·compress`, `static/index.html`·`static/js/app.js` 배당 히스토리 보관함): 탭 전환·발주지연 시 배당 데이터가 유실(20분+ 갭 시 snapshots 초기화)돼 이전 경주 복기가 불가하던 문제 해결(사용자 요청). **[1·2번 append-only 아카이브]** `_history_append`가 분석용 `snapshots`(오염방어로 리셋/300캡)와 **별개로** `archive_snapshots`에 모든 수집 배당(복승·쌍승·단승·이상감지)을 **절대 삭제·초기화 없이 영구 누적**(경주 전환 20분+ 갭 시에도 세션 경계 마커만 추가·데이터 유지, 5000 안전상한). data/odds_history/YYYYMMDD_경마장_N경주.json 그대로 사용. **[3번 복기 조회]** 일본경마 탭에 `📚 배당 히스토리 보관함` 카드 추가 — `/api/odds/archive/list`(저장된 모든 경주·유효 스냅샷수·결과·압축여부) → 경주 클릭 시 `/api/odds/archive/get`으로 전체 배당 타임라인(시각·마감분·상위6조합·이상감지·세션경계) 표 복기. 구파일(archive 미보유)은 snapshots 폴백. **[4번 메모리 관리]** `_archive_compress_old(7)`이 7일+ 지난 .json을 .json.gz로 **압축 보관(데이터 삭제 아님)** — 서버 시작 시 자동 + `/api/odds/archive/compress` 수동. `_hist_read_any`가 .json/.json.gz 투명 해제. 검증: 20분+ 갭 리셋 후 snapshots=1(오염방어 유지)·archive_snapshots=4(보존+경계마커) + 7일+ 압축·투명읽기 + 라이브 347경주 목록 + 회귀(report69·flow23) 통과. app.py `import ast`·app.js `new Function` OK. **서버+프론트만**(확장 무변경·ZIP 불필요). 기존 snapshots 오염방어·리셋·학습 **무삭제**(archive_snapshots 계층·복기 UI·압축만 가산).
- **📊 확신도 복승 실적중 학습 + 삼복승 예상배당 표기(보완①②)**(확장 v2.1.71, `app.py _confidence_picks`·`_conf_picks_from_log`·`_apply_result_learning`·`_recompute_learning_stats`, `static/js/app.js renderCorePicks`, `chrome-extension/overlay.js`): 직전 확신도 복승 기능 후속 보완 2건. **[보완①·학습 연결]** 확신도 1위 필수 포함 복승/삼복승이 실제 적중했는지 결과 입력마다 판정(`conf_quinella_hit`·`conf_trifecta_hit`·`conf_trifecta_ins_hit`·`conf_top1_placed`·`conf_forced_placed`) → `_recompute_learning_stats.conf_pick_stats`(적중률·확신도 1위 입상률 + **임계값 자동 조정 권고**: 확신도 1위 입상률 <40%·표본10+ 상향 / ≥70% 유지·확대). 재분석 공백 시 저장 로그(`_conf_picks_from_log`, analysis_log에 corePicks 보존) 폴백. **[보완②·삼복승 배당]** 확신도 삼복승 메인/보험에 실배당(trio_map) 우선·없으면 `_trio_est` 추정배당(`confTrifectaOdds`·`confTrifectaInsOdds`) → 오버레이·핵심추천 카드에 "N배(추정)" 표기. **실전 검증(마에바시 3R 1-2-6)**: 최종 추천 6+7 미적중이나 **확신도 복승 [1+2] 정답 적중·삼복승 보험 [1+2+6] 정답 적중**(이상감지 쌍승역전·복승불일치가 2번 지목 적중)으로 두 기능 실효성 확인 + 회귀(report69·formula24·flow23) 통과. app.py `import ast`·app.js `new Function`·overlay.js `node --check` OK + ZIP 재빌드(v2.1.71). ⚠ 확장 새로고침 필수. 기존 학습·통계·추천 **무삭제**(conf_pick_stats·삼복승 추정배당 필드만 가산).
- **🎯 확신도 1위 말 복승 필수 포함 — 확신도 70+ 무조건 편성·1위+2위·1위+시장유력 자동 복승**(확장 v2.1.70, `app.py _confidence_picks`·`_triple_analyze`, `static/js/app.js renderCorePicks`, `chrome-extension/overlay.js`): 확신도(BMED confidence engine) 1위 말이 복승 메인에서 빠져 정답을 놓치던 문제 근본 해결(후쿠시마 4경주 6번 확신도 96.2가 복승 메인 2+8에 없어 6+8 정답 놓침). `_confidence_picks`가 확신도 엔진(`_confidence_engine`) 결과로 **①확신도 1위+2위 ②확신도 1위+시장유력(단승 최저, 확신도1위와 다른 시장 최고인기) ③확신도 70점+ 말 무조건 복승 메인 포함**(빠지면 확신도 1위와 페어링)을 복승 자동 편성 + **삼복승 메인=시장유력+확신도1·2위 / 보험=확신도1·2위+이상감지 말**(TOP5 이상감지 자동 포함). 분석 화면 `🎯 핵심 추천` 카드·오버레이에 복승 다중 라인(근거 표기)+삼복승+삼복승 보험 표시(확신도 1위 N번 헤더). 검증: 후쿠시마 재현(확신도1위=6·복승 6+8·2+6·삼복승 2+6+8·**보험 5+6+8=실제 정답 삼복승** 산출) + 회귀(report69·formula24·flow23) 통과. app.py `import ast`·app.js `new Function`·overlay.js `node --check` OK + ZIP 재빌드(v2.1.70). ⚠ 확장 새로고침 필수. 기존 `_core_picks`(축2두)·betRecommend·매트릭스 **무삭제**(corePicks에 confQuinellas·confTrifecta·forcedHorses 등 별도 필드로만 부가·확신도 1위 필수 편성).
- **🔀 크로스 역배열 적중률 학습 추가 — 신호별 적중률(signal_stats)에 '크로스역배열' 편입**(`app.py _active_signal_horses`·`_signal_reliability_for`·`learning_signal_stats`): 크로스 역배열이 실제 2착 예측에 효과적인지 추적하도록 기존 신호별 학습에 편입(직전 크로스역배열 기능 후속·보완점3). `_active_signal_horses`가 **강한(0.5+) 크로스역배열 말**을 `크로스역배열` 신호로 추가 → 결과 입력마다 `_learn_signal_stats`가 복승/삼복승 적중·그 말 입상 여부를 `signal_stats.json`에 자동 집계(멱등). `📊 신호별 적중률` 카드·오늘 통계 `by_signal`·분석 화면 신뢰도 칩(`_signal_reliability_for`)에 크로스역배열 표시. 검증: 활성신호 편입(13번 0.72 감지·0.4 제외) + 회귀(report69·formula24·flow23) 통과. app.py `import ast` OK. **서버 전용**(프론트 카드 자동 표시·확장 무변경). 기존 신호별 학습·통계 **무삭제**(크로스역배열 유형만 가산).
- **🔀 복승 크로스 역배열 전체 적용 — 인기 상위쌍 편차로 각 말 실질 강세(2착 유력) 점수화**(확장 v2.1.68, `app.py _cross_reversal`·`_triple_analyze`, `static/js/app.js renderCrossReversal`, `chrome-extension/overlay.js`): 특정 조합만 보던 역배열을 **전체 말 쌍 비교**로 확장(사용자 요청). **[1 전체 쌍 비교]** `_cross_reversal`이 각 말 X에 대해 인기 상위(최대 5위) 모든 쌍(A인기>B)에서 **복승(A+X) > 복승(B+X)**(=X가 덜 인기있는 B와 더 싸게 붙음=X 실질 강세) 편차%(1.0 캡)를 가중합산·정규화. **가중치**: 1·2위쌍 ×3 / 1·3·2·3위쌍 ×2 / 기타 ×1. **[2 쌍승 동일]** A→X vs B→X·X→A vs X→B도 보조 가중(×0.5). **[3 점수]** 0.3🟡/0.5🟠/0.7🔴. **[4 표시]** 분석 화면 `🔀 복승 크로스 역배열` 카드(`🔴 13번 점수 0.72 → 6·11번 1착 시 2착 강력`)·오버레이 강조(0.5+). **삼복승 자동 편성**: 인기 상위2두 + 크로스역배열말(0.5+) → 삼복승 보험. 검증: 크로스 점수(13번 인기상위와 비싸게·하위와 싸게 → refs 기여순·강한케이스 0.6🟠) + 회귀(report69·formula24·flow23·multi19·reversal29) 통과. app.py `import ast`·app.js `new Function`·overlay.js `node --check` OK + ZIP 재빌드(v2.1.68). ⚠ 확장 새로고침 필수. 기존 역배열(쌍승역전)·추천·베팅표 **무삭제**(크로스 역배열 점수·삼복승 편성만 가산).
- **🎯 핵심 추천 근본 정리 — 엄격 우선순위 축 2두 → 복승 X+Y·삼복승 X+Y+Z(딱 이것만)**(확장 v2.1.67, `app.py _core_picks`·`_triple_analyze`, `static/js/app.js renderCorePicks`, `chrome-extension/overlay.js`): 유력마·복병·실시간추가·고배당을 다 합치면 전체 마번이 포함돼 의미 없던 추천 과다 문제 근본 해결(사용자 요청). `_core_picks`가 **엄격 우선순위로 축 2두만 선정** — 1순위 역배열+급락 동시 / 2순위 스마트머니 / 3순위 집중급락 최다 / 4순위 유력마 저배당 → 상위 2두=축, 3위=삼복승 3착. **복승 X+Y·삼복승 X+Y+Z만** 산출(복승 배당은 실배당·삼복승은 실배당 없으면 `_trio_est` 추정). 분석 화면 **최상단 `🎯 핵심 추천 (딱 이것만)` 초록 대형 카드**(복승/삼복승 + 축 근거), 오버레이도 최상단 `🎯 복승 X+Y · 🛡 삼복승 X+Y+Z`만 크게. ⚠ **기존 추천(betRecommend 전체·유력마·복병·임팩트카드) 완전 무삭제** — 핵심 추천 카드를 최상단에 추가만(상세는 아래 그대로). 마감 후 미표시. 검증: 우선순위(역배열+급락 5번→축1·스마트머니 7번→축2·집중급락12회 3번→3착·복승5+7·삼복승3+5+7) + 회귀(report69·formula24·flow23·multi19) 통과. app.py `import ast`·app.js `new Function`·overlay.js `node --check` OK + ZIP 재빌드(v2.1.67). ⚠ 확장 새로고침 필수. 기존 추천·베팅표·매트릭스 **무삭제**(핵심 추천 축2두 카드만 가산).

### 수정된 버그
- **🇰🇷 한국경마 자동 감지 강화 — 경마 지역='자동'에서도 복승만 수집(쌍승/삼복승 차단)**(확장 v2.1.73, `chrome-extension/content.js isKoreaMode`·`koreanByBetTabs`·`KRA_TRACK_RE`·`collectTripleByTabs`·`collectTripleKeiba`): 경마 지역을 '자동'으로 두면 한국경마 감지에 실패해 쌍승/삼복승을 계속 수집하던 문제 근본 수정(사용자 요청). **근본 원인**: 자동수집(`_resolveRaceKey`)은 배당판 표시 raceKey를 우선하는데 사설 보드는 경마장명이 누락돼(예 '5경주'), 사용자가 팝업에 '서울 5경주'를 입력해도 `isKoreaByRaceKey`가 못 봄 → `isKoreaMode=false`. **[해결]** `isKoreaMode`가 ①**팝업 입력 raceKey(override)도 함께 검사**(자동수집 raceKey에 경마장명 없어도 사용자 입력 '서울/부산/제주'로 한국 확정 — 근본 버그 해결) ②**배당판 마권종류 탭 감지**(`koreanByBetTabs`: 복승 탭 있고 쌍승/삼복승 탭 전혀 없으면 한국 — 경마장명 없어도 감지) ③KRA_TRACK_RE에 렛츠런/한국마사회/경마공원/KRA 추가 ④종목=한국·raceKey·페이지·탭 어느 하나라도 KRA면 복승만. 두 수집 경로(`collectTripleByTabs`·`collectTripleKeiba`)에 override 전달. 검증: 6개 시나리오(자동+입력서울 true·배당판탭 복승만 true·일본 도쿄/쌍승삼복승탭 false 오탐없음·신호없음 판단보류) 통과. content.js `node --check` OK + ZIP 재빌드(v2.1.73). ⚠ 확장 새로고침 필수. 기존 한국 판정(종목·raceKey·페이지)·일본/중앙/경륜 수집 **무삭제**(입력 raceKey·탭 감지·KRA 별칭만 가산).
- **🇰🇷 한국경마 쌍승/삼복승 탭 클릭 완전 차단 — 없는 탭 왔다갔다 오류 제거**(확장 v2.1.69, `chrome-extension/content.js collectTripleByTabs`·`collectTripleKeiba`·`isKoreaMode`): 한국경마에서 쌍승/삼복승 탭 클릭을 시도하다 없는 탭이라 오류가 나고 탭이 왔다갔다 반복되던 문제 수정(사용자 요청). 원인: 코드상 한국모드(`isKoreaMode`=종목=한국 OR raceKey/페이지 KRA 감지) 가드는 이미 두 수집 경로(`collectTripleByTabs` 탭클릭·`collectTripleKeiba` fetch)에 완비돼 복승만 수집·쌍승(1634)/전적(1673)/삼복승(1703) 탭 클릭을 스킵하고 있었으나, **직전 확장 ZIP이 이 가드를 담아 재빌드되지 않아 구코드가 브라우저에서 계속 실행**된 것. 로그 문구를 사용자 요청대로 **`[한국경마] 복승만 수집 (쌍승/삼복승 미지원)`**로 통일(메인·쌍승 스킵·삼복승 스킵·keiba 경로 4곳)하고 **manifest 2.1.68→2.1.69 bump + ZIP 재빌드**로 최신 가드 코드를 배포. 검증: content.js `node --check`·manifest JSON·ZIP 내 manifest version 2.1.69 확인. **확장 전용**(서버·프론트 무변경). ⚠ **브라우저에서 확장 새로고침 필수**(안 하면 구코드가 계속 쌍승/삼복승 탭을 클릭). 기존 한국 판정·복승 수집·일본/중앙/경륜 경로 **무삭제**(로그 문구 통일 + ZIP 재배포만).
- **🏇 중앙경마(JRA) oddspark 서버 수집 자동 비활성화 — Failed to fetch 방지**(`static/js/app.js jpIsCentralName`·`fetchKeibaOdds`·`keibaOddsAutoPoll`·`keibaDetectCurrentRace`·`_renderKeibaStatus`): 중앙경마 탭/경주에서도 oddspark 서버 수집을 시도해 `Failed to fetch` 오류가 나던 문제 수정(사용자 요청). oddspark는 지방경마(NAR)만 제공(JRA는 로그인/POST 필요). **[자동 OFF]** `jpIsCentralName`(JRA 경마장명: 도쿄·나카야마·한신·쿄토·삿포로·하코다테·후쿠시마·니가타·주쿄·고쿠라 한/일 표기)으로 현재 경주가 중앙경마면 `fetchKeibaOdds`·자동폴링(`keibaOddsAutoPoll`)·경주추종(`keibaDetectCurrentRace`)·토글 즉시수집 모두 **스킵**(기존 한국경주 가드와 동일 패턴). **[토글 비활성화+안내]** `_renderKeibaStatus`가 중앙경마 시 토글 disabled + `🏇 중앙경마(JRA) · oddspark 미지원 — Chrome 확장으로만 수집` 안내, **지방경마로 돌아오면 토글 자동 재활성화**(localStorage 설정 복원). 검증: 중앙경마 감지(도쿄·한신·中山→OFF / 오비히로·사가→활성 / 서울→한국가드) + app.js `new Function` OK. **프론트 전용**(서버·확장 무변경). 기존 지방경마 oddspark 수집·한국 가드 **무삭제**(중앙경마 가드만 가산).

### 수정된 버그
- **🔄 경주 전환 시 이전 경주 급락 데이터 잔존 수정 — 시간 간격(20분+) 기반 자동 기준값 재설정**(`app.py _do_triple_ingest`·`_history_append`): 새 경주 시작 시(같은 raceKey 재사용·발주 지연) 이전 경주 급락/이상감지가 남아 엉뚱한 급락 신호가 다수 뜨던 버그 수정(사용자 요청). 기존 `_baseline_reset_needed`(95%+ 동시급락)·`_established`(4+스냅샷 후 휴리스틱 초기화 차단)로는 **시간이 크게 벌어진 재수집**을 못 잡던 문제. **[1·2 시간간격 리셋]** `_do_triple_ingest`가 직전 수집 후 **20분+ 경과 시 stale_gap** → 확립 여부 무관하게 강제 기준값 재설정(이전 급락/배당 완전 제거·이 수집=새 기준값). `_history_append`도 히스토리 파일 마지막 스냅샷이 20분+ 전이면 **이전 경주 스냅샷·이상감지(anomaly_history) 완전 제거** 후 새 기준값(급락 계산 생략). 첫 수집=기준값(급락 계산 안 함)은 기존 워밍업 로직 유지·2번째부터 비교. **[3 새 경주 버튼]** 기존 `triple_reset`(활성 배당+전적 초기화)·`hardResetRaceState`(프론트 패널/타임라인 초기화) 유지. **[4 검증]** 이전 경주 급락 데이터(4스냅샷·established) 상태에서 20분+ 후 새 수집 → **baselineReset=True·history 1건·급락 0 초기화** 확인. 오다와라 1R(07:34→08:24 발주지연 50분 갭 후 마감 대량급락) 같은 케이스 방어. 회귀(report69·formula24·flow23·multi19) 통과. app.py `import ast` OK. **서버 전용**. 기존 baseline·경주전환·워밍업 로직 **무삭제**(시간간격 리셋만 가산).

### 추가된 기능
- **🏇 전체 경주 탭 중앙경마(JRA) 그룹 추가 — 지방경마와 구분 표시**(`app.py _multi_sport_of`·`_JRA_TRACKS`, `static/js/app.js` 종목 그룹/토글/라벨): 🏁 전체 경주 탭에 중앙경마(JRA)가 안 보이던 문제 수정(사용자 요청). 원인 파악: ①`today_schedule.json`은 지방경마(NAR)만(oddspark·keiba.go.jp에 **JRA 없음**) ②JRA 배당은 로그인/POST 필요라 서버 자동수집 불가 ③기존 `_multi_sport_of`가 JRA를 `horse`(지방)로 묶어 구분 안 됨. **[해결]** `_multi_sport_of`가 **category=japan_central 또는 JRA 경마장명(도쿄·나카야마·한신·쿄토·삿포로 등)** → `central` 분류(지방경마와 별도 그룹). 프론트에 `🏇 중앙경마` 그룹·토글·라벨 추가(기본 ON·경정만 OFF). JRA는 **확장이 수집한 중앙경마 데이터**(triple_store category=japan_central·경륜/한국과 동일 읽기 전용 경로)로 대시보드에 표시 → `🏇 중앙경마 (N)` 그룹. 검증: 분류(japan_central·도쿄→central·오비히로→horse·서울→korea) + 대시보드(bySport central 그룹·도쿄 11R central 태그) 통과. app.py `import ast`·app.js `new Function` OK. **서버+프론트만**(확장 무변경). 기존 종목 그룹·지방경마·병합 **무삭제**(중앙경마 그룹만 가산). ⚠ JRA는 확장이 중앙경마 배당판을 수집한 경주만 표시(서버 자동 스케줄 없음·oddspark/keiba.go.jp에 JRA 미제공).
- **🎯 추천 화면 핵심 임팩트 정리 — 복승 3 + 삼복승 2(총 5개) 카드 + 별점 + 나머지 접기**(`static/js/app.js renderBetRecommend`·`_renderImpactCard`·`_betStars`·`_impactPick`): 추천 13개가 한 표에 나와 혼란스럽던 것을 핵심만 큰 카드로 정리(사용자 요청). **[임팩트 카드]** `🎯 지금 사세요!` 카드에 **복승 최대 3개(배분 높은 순) + 삼복승 최대 2개** = 총 5개만 크게 표시(조합·예상배당·별점). **[별점]** `★★★ 신호 강함(상) · ★★ 중간 · ★ 참고용` — signalQuality(상/중/하) 우선, 없으면 라벨/배분 폴백(메인·30%+→★★★·보조·12%+→★★). 삼복승은 보험/메인 태그. **[나머지 접기]** 카드 밖 조합·역배열·받치기 등은 `＋ 추가 참고 조합 N개 · 배분/금액 상세 보기` 접기(`<details>`)로 이동 — **기존 전체 표(배분·금액·신호품질·범례)는 그대로 접혀서 보존**(무삭제). 마감/대기 배너(recommendClosed/Gated)·확정/강제 배너는 기존 유지. 검증: 임팩트 선택·별점(4+7 1.5배 ★★★·6+7 16.2배 ★★·삼복승 메인/보험·나머지 2개 접기) 재현 + app.js `new Function` OK. **프론트 전용**(서버·확장 무변경). 기존 추천 표·배분·금액·신호품질 **완전 무삭제**(임팩트 카드 상단 요약 + 상세 접기만 가산).
- **🚴 경륜 배당판 자동 수집 — 탭 클릭·포커스 즉시 트리거(SPA 전환 대응·oddspark 로그인 불필요)**(확장 v2.1.66, `chrome-extension/content.js _maybeKeirinKickstart`): 배당판(asyukk34) 안에서 **경륜 탭을 클릭(페이지 리로드 없는 SPA 전환)** 할 때 최대 10초 지연되던 것을 즉시 수집으로 강화(사용자 요청·직전 v2.1.65 확장). `_maybeKeirinKickstart(reason)`가 **①배당판 로드 ②경륜 탭 클릭(document click 700ms 디바운스) ③탭 포커스 복귀(visibilitychange)** 세 경로에서 발동 — 종목=경륜(resolveSport) + autoSend ON일 때만 `collectTriple('auto')` 즉시 실행. 4초 쿨다운·재진입 가드로 과호출 방지, 경마·경정·autoSend OFF는 기존 동작 유지. oddspark 로그인 없이 asyukk 사설 배당판에서 작동(content script 실행 경로). content.js `node --check` OK + ZIP 재빌드(v2.1.66). ⚠ 확장 새로고침 필수. 기존 자동수집·킥스타트·수동 버튼 **무삭제**(클릭·포커스 즉시 트리거만 가산).
- **🚴 경륜 배당판 자동 수집 트리거 — 배당판 열면 자동전송 ON일 때 즉시 수집(수동 버튼 불필요)**(확장 v2.1.65, `chrome-extension/content.js _keirinAutoKickstart`): 경륜 배당판을 열 때마다 `[전체 자동 수집]`을 수동으로 눌러야 하던 불편 해소(사용자 요청). **[1 자동 트리거]** 배당판 로드 완료 시 `_keirinAutoKickstart`가 **종목=경륜 감지(resolveSport) + autoSend ON**이면 `collectTriple('auto')`로 **즉시 1회 킥스타트**(백그라운드 첫 틱 대기 없이 바로 수집) — 경마·경정 등은 기존 동작 유지(경륜만 즉시). **[2 자동전송 기억]** autoSend는 기존대로 `chrome.storage`에 저장돼 다음에 열어도 ON 유지(변경 없음·확인). **[3 페이지 전환]** 경륜 탭/보드 전환은 기존 `watchSportChange`(종목→경륜 감지 시 재수집)·`watchRaceChange`(raceKey 변경 재수집)가 자동 처리 → 별도 버튼 불필요. 실패해도 무시(기존 수집 흐름 무영향·재진입 가드). content.js `node --check` OK + ZIP 재빌드(v2.1.65). ⚠ 확장 새로고침 필수. 기존 자동수집 루프·백그라운드·수동 버튼 **무삭제**(경륜 킥스타트만 가산).
- **💰 네덜란드식 계산기 매트릭스판 — 축/주력/복병/제거 상태 + 삼복승 매트릭스 + 자금분배 엔진(샘플 통합·참고용)**(`static/js/app.js renderDutchCalc`·`_dutchEngine`·`_dutchInitStates`·`_dutchTripleOdds`): 사용자 제공 샘플(네덜란드.txt) 로직을 통합해 기존 더칭 패널을 **매트릭스판으로 고도화**(무삭제·같은 패널 강화). **[1 마번 상태]** 마번 버튼 클릭으로 **일반→축(AXIS·빨강)→주력(MAIN·초록)→복병(DARK·노랑)→제거(DROP·회색)** 순환(축은 1마리 제한). **[2 삼복승 매트릭스]** 축 고정 시 **우상단 삼각형 매트릭스** 동적 생성(r>c·축행렬·제거마 셀 차단, MAIN·DARK 색상 힌트, 삼복승 배당 표시). **[3 자금분배 엔진]** 복병(DARK) 삼복승 보험 + 쌍승 원금보험(축 포함 저배당)=예산÷배당(본전 보전) → 오버플로우 경고(보험합≥예산) → 잔액을 핵심 메인 삼복승(축+주력2)에 배분. 포트폴리오별 추천금액·적중 환수·순손익 출력. **[연동]** 삼복승 배당(betRecommend expOdds·trio)·쌍승(exacta) 실시간 자동 연결 + **기존 추천 자동 반영**(복승 메인 첫째→축·나머지 유력마→주력·복병/스마트머니/중고배당→복병·제거마→제거), 사용자 수동 조정 가능. 일본경마 등 분석 뷰 하단 `💰 네덜란드식 계산기 (참고용)` 섹션·30초 자동 재계산. 검증: 엔진(복병삼복+쌍승 본전보전·핵심메인 잔액·투입합계=예산 100,000) 샘플 재현 + app.js `new Function` OK. **프론트 전용**(서버·확장 무변경). 기존 추천·베팅표·매트릭스 **무삭제**(더칭 패널을 매트릭스판으로 강화). ⚠ 삼복승 실배당 미수집 시 '-'(추정배당만).
- **🚴 확장 경유 경륜 스케줄 자동 수집 — 로그인 세션으로 oddspark 경륜 KaisaiRaceList fetch(하루 1회 08시)**(확장 v2.1.64, `chrome-extension/background.js fetchKeirinSchedule`·`scheduleKeirinDaily`, `manifest.json` host_permissions, `app.py /api/multi/keirin-schedule`·`_multi_collect_one`): oddspark 경륜은 로그인 벽이라 서버 직접 스케줄 수집 불가 → **로그인 세션 보유한 확장이 대신 fetch·파싱 후 서버로 POST**(기존 `FETCH_RESULT_HTML` 동일 패턴). **[확장]** `fetchKeirinSchedule`이 `KaisaiRaceList.do`(credentials:include)에서 개최 경륜장 joCode 추출 → 각 `RaceList.do`에서 경주번호+발주시각(発走時刻) 파싱(경륜장명은 title/joCode 매핑) → `POST /api/multi/keirin-schedule` 전송. `chrome.alarms` **매일 08:00**(24시간 주기)+설치/시작 시 예약. manifest host_permissions에 `oddspark.com` 추가. **[서버]** `/api/multi/keirin-schedule`가 받은 경륜 스케줄을 `today_schedule.json`에 **sport=cycle 트랙으로 병합**(기존 경마 트랙 보존·멱등). `_multi_collect_one`이 종목 라우팅 — joCode=경륜(betType 5복승·6쌍승·배당은 공개라 서버 수집 가능) / opTrackCd=경마. → 경륜도 발주 10분전 자동 수집·전체경주 대시보드 표시. 검증: 경륜 스케줄 병합(경마 트랙 보존·postEpoch 산출·멱등) + 회귀(multi19·report69·formula24) 통과. app.py `import ast`·background.js `node --check` OK + ZIP 재빌드(v2.1.64). ⚠ 확장 새로고침 + oddspark 로그인 필요. 기존 결과 fetch 릴레이·경마 스케줄·수집 **무삭제**(경륜 스케줄 확장 fetch·서버 병합·cycle 수집 라우팅만 가산).
- **💰 네덜란드식(더칭) 계산기 별도 패널 — 참고용(기존 추천 무영향)**(`static/js/app.js renderDutchCalc`·`_dutchHtml`·`_dutchRefresh`·`_ensureDutchDelegation`): 기존 신호 기반 추천은 그대로 두고 **더칭 계산기를 별도 패널로 추가**(사용자 4부). **[1 레이아웃]** 추천(`renderBetRecommend`) 바로 아래 `💰 네덜란드식 계산기 (참고용)` 보라 테두리 패널(기존 추천과 별개). **[2 UI]** 조합 클릭으로 **🟢 메인 / 🟡 보험 / ⬜ 해제** 순환 토글 + 예산 입력 → 자동 금액 계산. 표시: 메인 `X+Y → N원 (적중 시 N원)`(더칭=어느 메인이 맞아도 동일 수익) · 보험 `A+B → N원 (본전 보전 예산원)`. **⚠️ 보험금 합 ≥ 예산 시 `배당 낮아 본전 불가` 경고**. **[3 실시간 재계산]** 30초 분석 갱신마다 최신 배당으로 자동 재계산(선택·예산 상태 module 보존) + 토글/예산 입력 즉시 재계산. **[4 자동 연동]** 기존 추천 조합 자동 입력 — 복승 메인/보조 → 🟢 메인 · 삼복승/보험 → 🟡 보험, 사용자 수동 조정 가능. 계산: 보험=예산/배당(적중 시 예산 본전), 메인=(예산−보험합) 더칭 배분(1/배당 비례·동일 수익). 검증: 더칭 로직(보험 20배→본전보전·메인 동일수익·보험 1.5배×2→본전불가 경고) 수동 검증 + app.js `new Function` OK. **프론트 전용**(서버·확장 무변경). 기존 추천·베팅표·매트릭스 **완전 무삭제**(더칭 계산기 패널만 가산).
- **🏁 전체 경주 탭 종목 통합 — 일본경마+경륜+한국경마 + 종목 토글(경정 제외)**(`app.py multi_dashboard`·`multi_race_detail`·`_multi_sport_of`·`_multi_card`, `static/js/app.js renderMultiDashboard`·`_renderMultiToggles`, `static/index.html`): 전체 경주 대시보드에 경륜·한국경마를 추가(사용자 5부·경정 제외). **[1·경륜]** ⚠ oddspark 경륜 `KaisaiRaceList.do`는 **로그인 필요**(응답=로그인 페이지)라 서버 자동 스케줄 수집 불가 → **Chrome 확장이 이미 수집한 경륜 경주를 `triple_store`에서 읽어(읽기 전용) 목록에 포함**(로그인 벽 우회·`triple_store` 쓰기 없음). **[3·한국경마]** 확장이 수집한 한국경마(`category=korea`)도 동일하게 읽기 전용 포함. **[4·통합 대시보드]** 카드에 종목 라벨(🇯🇵일본경마/🚴경륜/🇰🇷한국경마) + **종목별 그룹핑** + 마감 임박순 정렬 + **⚡ 이상감지 배지**(급락/역배열/스마트머니) + 💎 중고배당 배지 + T-3분 빨강 테두리. `multi_dashboard`가 `multi_race_store`(서버수집 일본경마) + `triple_store`(확장수집 경륜/한국, 읽기전용·중복제거·30분+ 마감 제외) 병합, `bySport` 카운트 반환. `multi_race_detail`이 triple_store 폴백. **[5·종목 토글]** ☑일본경마 ☑경륜 ☑한국경마 체크박스(경정 제외·localStorage 유지) → 원하는 종목만 표시. 검증: 통합 대시보드(일본경마·경륜·한국 3종 병합·bySport·종목태그) + 회귀(multi19·report69·formula24·flow23) 통과. app.py `import ast`·app.js `new Function` OK. **서버+프론트만**(확장 무변경·ZIP 불필요). 기존 다중경주·triple_store(읽기만)·단일분석 **무영향·무삭제**(경륜/한국 병합·종목 토글만 가산). ⚠ 경륜 스케줄 자동수집은 로그인 벽으로 미지원(확장 수집분만 표시)·경정 제외.
- **💎 중고배당 유력마 감지 + 알림·추천·학습 시스템**(확장 v2.1.63, `app.py _mid_high_odds_favorites`·`_learn_mid_high_odds`·`_triple_analyze`·`_multi_card`·`/api/learning/mid-high-odds`, `static/js/app.js renderMidHighFavorites`·`renderMidHighStats`, `chrome-extension/overlay.js`): 복승 10배+ 인데 강한 신호 있는 말을 별도 강조(사용자 5부). **[1 감지]** `_mid_high_odds_favorites`가 **복승 10배+ & 신호 1개+**(급락 -30%+·역배열 10%+·스마트머니·집중급락 10회+·전적 A/B등급) → 💎 중고배당 유력마 분류(마감 후 미표시). **[2 즉시 알림]** 분석 화면 상단 `💎 고배당 유력마 감지! 7번(28배) 스마트머니 → 삼복승 보험 필수` 강조 배너 + **전용 알림음(기존 경고음과 다른 상승 아르페지오)** + 화면 깜빡임(경주+마번 1회) + 오버레이 최상단 💎 박스. **[3 대시보드 연동]** 전체 경주 카드에 `💎 고배당` 배지 + 보라 테두리 강조(`_multi_card.midHigh`). **[4 추천 자동 편성]** 저배당 축2두 + 💎 중고배당 말 → **삼복승 대박 보험** 자동 편성(`삼복승 보험(💎중고배당 N배·신호)`). **[5 학습]** `_learn_mid_high_odds`가 감지 신호·실제 입상·배당·누적 적중률을 `mid_high_odds_stats.json`에 자동 저장(신호별 입상률·경주별 멱등) + `GET /api/learning/mid-high-odds` + 통계 탭 `💎 중고배당 유력마 적중률` 카드. 검증: 감지(7번 26배 4신호·저배당 A등급 제외)·삼복승 보험 편성 + 회귀(multi19·report69·formula24·flow23) 통과. app.py `import ast`·app.js `new Function`·overlay.js `node --check` OK + ZIP 재빌드(v2.1.63). ⚠ 확장 새로고침 필수. 기존 고배당후보(흐름)·복병·베팅·학습 **무삭제**(중고배당 감지·알림·보험·학습만 가산).
- **🌅 서버 시작 시 어제 경주 데이터 자동 초기화(고정·다중수집·단일캐시·스케줄) + 자정 롤오버**(`app.py _startup_date_reset`·`_race_pin_save`·`_multi_bg_loop`): 서버를 재시작해도 어제 고정 경주·수집 데이터가 남아있던 문제 수정(사용자 요청). **[고정 자동 해제]** `race_pin.json`에 고정 날짜 기록 → 서버 시작 시 **오늘 아닌 날짜의 고정은 자동 해제**(오늘 고정은 유지). **[다중 수집 초기화]** `multi_race_store`에서 오늘 아닌 경주 제거(oddspark 다중 수집 날짜 초기화·새 날짜로 재시작). **[스케줄 초기화]** `today_schedule.json`이 어제 날짜면 비움 → 백그라운드가 오늘 스케줄로 재수집. **[단일 활성캐시]** `triple_store`에서 어제 경주 제거(오늘만 유지). **[자정 롤오버]** 백그라운드 루프가 날짜 변경을 감지하면 자동으로 어제 데이터 정리+스케줄 재수집. ⚠ **학습 데이터(odds_history·learning·pattern·signal_stats 등)는 절대 미접근** — 활성 캐시/고정만 정리(기존 기능 무영향). 검증: 어제 고정→해제·오늘 고정→유지·어제 경주 제거·오늘 경주 유지·스케줄 초기화 단위 + 회귀(multi19·report69·formula24) 통과. app.py `import ast` OK. **서버 전용**. 기존 고정·저장소·prune 로직 **무삭제**(날짜 초기화만 가산).

### 수정된 버그
- **🏁 다중 경주 발주시각 파싱 실패 수정(発走時間) + 예정 경주 카운트다운 카드**(`app.py _multi_post_time`·`_multi_race_list`·`multi_dashboard`, `static/js/app.js _multiCardHtml`): 다중 경주 스케줄은 수집되나(4개 경마장·45경주) **발주시각이 거의 다 None**이라 자동 수집 트리거가 작동 못 하던 문제 수정. 원인: ①oddspark 실제 라벨이 `発走時間`(時間)인데 정규식은 `発走時刻/締切`만 매칭 ②목록 페이지엔 발주시각이 없고 **경주별 페이지에만** `発走時間 HH:MM` 존재. **[해결]** `_multi_post_time`이 경주별 `RaceList.do?raceNb=N`에서 `発走時間` 추출(당일 캐시로 재fetch 방지)·`_multi_race_list`가 ThreadPool로 병렬 취득 → **라이브 검증 오비히로 12/12 발주시각 파싱 성공**(1R 14:30·2R 15:05…). **[추가]** 대시보드에 **배당 수집 전 예정 경주도 카운트다운 카드**(점선 테두리·`⏳ 수집 대기`)로 표시 → 발주 10분전 이전에도 오늘 전체 경주가 F5로 즉시 보임. 상태줄 `오늘 N경주 (수집·분석 N · 예정 N)`. 검증: `run_multi.py` 19케이스(발주시각 경주별 파싱·캐시 포함) + 회귀(report69·formula24·flow23) 통과. app.py `import ast`·app.js `new Function` OK. 기존 다중경주 로직·triple_store 무영향 **무삭제**(발주시각 파싱 수정·예정 카드만 가산).

### 추가된 기능
- **📅 오늘 경주 결과 통계 대시보드 — 요약·경마장별·신호별·타임라인**(`app.py /api/stats/today`·`_apply_result_learning`(record 날짜·경마장·유력마적중 필드), `static/js/app.js renderTodayStats`): 통계 탭 상단에 **오늘 결과 한눈 요약** 신설(사용자 요청·5부). **[1 API]** `GET /api/stats/today`(?date= 지정 가능) → 총·복승/삼복승 적중·패스·손익·`by_track`(경마장별)·`by_signal`(신호별·signal_stats 오늘분 재사용)·`timeline`(시간순). 학습 레코드에 `date`·`venue`·`keyhorse_quinella_hit`/`keyhorse_trifecta_hit` 추가(정확 적중 우선·없으면 유력마 기반 병행). **[2 요약 카드]** 통계 탭 최상단 큰 박스 — 등록 N경주·복승 적중 N건(N%)·삼복승 N건(N%)·손익 ±N원. **[3 경마장별]** 오비히로 N/N(N%)·사가 N/N(N%). **[4 신호별]** 스마트머니·역배열·급락 등 N/N(N%). **[5 타임라인]** `20:45 오비히로 1경주 ✅ 복승 4.2배` 시간순. 검증: 엔드포인트(오늘 필터·경마장별·신호별·타임라인 정렬) + 회귀(multi18·report69·formula24) 통과. app.py `import ast`·app.js `new Function` OK. **서버+프론트만**(확장 무변경). 기존 통계 카드·학습 레코드 **무삭제**(날짜·경마장·유력마적중 필드 + 오늘 대시보드만 가산). 신호별 적중 학습(직전 커밋)과 연동.
- **🎯 적중 판정 기준 개선(유력마 기반·기존 정확적중과 병행) + 신호별 적중 학습·통계·강조**(`app.py _keyhorse_hit`·`_active_signal_horses`·`_learn_signal_stats`·`_signal_reliability`·`_apply_result_learning`·`/api/learning/signal-stats`·`_triple_analyze`, `static/js/app.js renderSignalStats`·`renderSignalReliability`, `data/signal_stats.json`): 정확 조합 적중 외에 **분석 품질(유력마 선정)** 을 평가하는 새 지표 신설(사용자 5부). ⚠ **기존 정확 적중(복승 1+2·삼복승 1+2+3·손익)은 불변** — 새 지표는 병행. **[1 복승 적중]** `_keyhorse_hit`: 유력마 상위3 중 **2마리+ 입상(1~3착)** → 복승 적중. **[2 삼복승 적중]** 유력마 2+ 입상 AND 유력마·복병 합쳐 top3 3자리 커버(**유력마2+복병1** = 스마트머니 복병 3착 시 적중). **[3 신호별 저장]** `_active_signal_horses`가 이 경주 활성 신호(스마트머니·집중급락·역배열·급락·전적이중수렴·저배당압축)별 선정 마번 추출 → `_learn_signal_stats`가 결과 입력마다 신호별 복승/삼복승 적중을 `signal_stats.json`에 자동 태깅(경주별 멱등·재입력 교체). **[4 통계 탭]** `📊 신호별 적중률` 카드 — 신호별 복승/삼복승 적중률·표본·신뢰(50경주+). **[5 자동 반영·강조]** `_triple_analyze`가 활성 신호의 과거 적중률을 `signalReliability`로 반환 → 분석 화면에 `스마트머니 적중률 72% · 신뢰도 높음` 강조 칩(50경주+ 신뢰·CLAUDE.md 유의성 게이팅 준수, 자동 가중치 변경은 수동 검토). 검증: 복승 2/3·삼복승 유력마2+복병1·신호학습 멱등성 단위 + 회귀(multi18·report69·formula24·flow23) 통과. app.py `import ast`·app.js `new Function` OK. **서버+프론트만**(확장 무변경). 기존 정확 적중·손익·near-miss·기존 학습 **완전 무삭제**(유력마 기반 지표·신호별 통계만 병행 가산).
- **🏁 다중 경주 동시 배당판 — 오늘 개최 전체 경주 실시간 대시보드(별도 저장소·기존 단일분석 무영향)**(`app.py` 다중경주 모듈·`/api/multi/schedule·collect·dashboard·race`, `static/index.html` 🏁 전체 경주 탭, `static/js/app.js` 다중경주 대시보드, `tests/run_multi.py`): 여러 경주를 동시에 모니터링하는 대시보드를 **별도 추가 기능**으로 신설(사용자 요청·6부). ⚠ **핵심 원칙: `triple_store.json` 절대 미접근** — `multi_race_store.json`·`today_schedule.json` 전용 저장소(단위 테스트로 triple_store 불변 검증). **[1 스케줄]** `_multi_schedule_fetch`가 oddspark 개최 목록(`_keiba_schedule` 재사용)+RaceList 발주시각 파싱 → `today_schedule.json`(경마장+경주번호+발주시각), 백그라운드 30분 갱신. **[2 동시 수집]** 발주 10분전부터 각 경주 배당을 oddspark에서 수집(기존 `_keiba_parse_*` 재사용) → `multi_race_store.json`(원자적 쓰기). **[3 대시보드 탭]** `🏁 전체 경주` 탭 — 경마장+경주번호·마감 남은시간·유력마 TOP3·핵심신호(급락/역배열/스마트머니)·확신도 카드, **T-3분 빨강·T-10분 주황·그외 기본 테두리**, 마감 임박 순 정렬·30초 폴링. **[4 클릭 상세]** 카드 클릭 → 기존 `_triple_analyze`(읽기전용 재사용)로 복승 매트릭스·핵심신호·추천조합 표시(`sportAnalysisHTML` 재사용)+결과입력 이동. **[5 마감 알림]** T-3분 경주 감지 시 소리+상단 빨강 배너 `⚡ 사가 9경주 마감 3분전!`(경주당 1회). **[6 안전장치]** fetch 실패 경주만 건너뜀·스케줄 실패 시 단일 모드 유지·저장소 손상 시 자동 초기화·백그라운드 예외는 서버 안 죽임. 검증: `run_multi.py` 18케이스(저장소 격리·손상복구·발주시각·RaceList 파싱·카드·**triple_store 불변**·스케줄 실패 격리) + 기존 회귀 163건(report69·formula24·flow23·reversal29·prerace18) 전건 통과. app.py `import ast`·app.js `new Function` OK. **서버+프론트만**(확장 무변경·ZIP 불필요). 기존 단일 경주 분석·triple_store·모든 파이프라인 **완전 무영향**(별도 저장소·읽기전용 분석 재사용).
- **📋 일괄 결과 탭구분 파싱 강화 — 하이브리드(헤더라벨+고정인덱스) 매핑 + AI학습 NameError 수정 + 붙여넣기 안내**(`app.py _parse_result_tsv`·`_build_ai_training`, `static/index.html`): 탭구분 붙여넣기가 파싱 안 된다는 리포트 대응(사용자 요청). 서버 파서 자체는 정상 동작 확인(오비히로·사가 매칭·등록 성공)했으나, **헤더 라벨 변형에도 견디도록 하이브리드 매핑 추가** — 헤더 라벨 우선, 실패 시 **사용자 지정 고정 인덱스**(0구분/1경주지역/2라운드/3~5착/6단승/7복승/8쌍승/11삼복승)로 폴백. 경마장명 한글 매핑(帯広→오비히로·佐賀→사가·園田→소노다·川崎→카와사키·高知→코치)은 기존 `_track_norm`이 정상 처리 확인. **[부수 버그 수정]** 결과 등록마다 `[AI학습저장] 조립 실패: name 'ABS_STRONG' is not defined`가 뜨던 NameError 수정(`_build_ai_training`이 지역 상수 ABS_STRONG 참조 → 리터럴 10.0). **[프론트 안내]** 일괄 등록 붙여넣기 박스에 "엑셀/스프레드시트 표 그대로 복사(탭 구분)" 안내 + placeholder 예시 추가. 검증: 사용자 실제 형식(연승/복연승 슬래시값 포함)·헤더 라벨 변형(3복승→index11 폴백)·경마장 5종 매핑 + report69·formula24·flow23 통과. app.py `import ast` OK. **서버+프론트만**(확장 무변경). 기존 HTML/URL/OCR 파싱 **무삭제**(하이브리드 인덱스 폴백·NameError 수정·안내만 가산). ⚠ 서버 재시작 필요(기존 실행 중이면 최신 파서 미반영 가능).
- **💰 스마트머니 적중 자동 학습 훅 — 결과 입력 시 자동 집계(수동 저장 → 자동화)**(`app.py _learn_smart_money`·`_apply_result_learning`·`learning_upset`): 스마트머니(상승후급락) 복병의 실제 입상 통계를 수동 저장에서 **자동 집계로 전환**(직전 보완점3). `_apply_result_learning`이 결과 입력마다 `an.darkHorses` 중 스마트머니 감지 말이 **실제 1~3착 입상했는지 자동 판정** → `pattern_learning.json smart_money_stats`(count·hit·rate·cases[최근100]) 갱신. 수동 저장(오비히로 11R)과 **동일 저장소를 자동 누적** → 표본이 쌓이면 신뢰도 산출(50+ 유의 게이팅). `GET /api/learning/upset` 응답에 `smartMoneyStats` 추가(통계 탭 노출용). 검증: 적중(7번→입상 count+hit)·미적중(9번→count만)·스마트머니 없음(무시) 격리 단위 + report69·formula24 회귀 통과. app.py `import ast` OK. **서버 전용**(확장 무변경). 기존 부진마·대규모급락·near-miss 학습 훅 **무삭제**(스마트머니 자동 집계만 가산).
- **📋 일괄 결과 등록 — 헤더 포함 탭구분 텍스트(엑셀/스프레드시트 복사) 파싱 지원**(`app.py _parse_result_tsv`·`_parse_result_rows`): 기존 HTML 표만 파싱하던 일괄 등록에 **탭(\t) 구분 텍스트 붙여넣기**를 추가(사용자 요청). `_parse_result_tsv`가 헤더(`구분/경주지역/라운드/1착/2착/3착/단승/복승/쌍승/연승/복연승/삼복승/삼쌍승`)를 감지해 **라벨로 컬럼 인덱스 매핑**(순서·컬럼수 달라도 대응·정확매칭으로 복승↔복연승·쌍승↔삼쌍승 오매칭 방지) → `{area,round,no1~3,sOdds,qOdds,xOdds,tOdds}` 추출(단승·쌍승 추가). 탭 없으면 2칸+ 공백 분리 폴백·전각숫자 대응·중복 헤더행 스킵. `_parse_result_rows`가 HTML 표 태그 없으면 **자동으로 TSV 파서로 폴백**(기존 HTML 경로 무손상). 경주지역+라운드로 분석 로그 자동 매칭→적중판정→학습(`_match_row_to_key`·`_register_result_rows` 기존 파이프라인 재사용). 검증: 사용자 예시(오비히로 1R·사가 9R) 파싱 정확(복승 4.4·쌍승 13.7·삼복승 32 컬럼 정확 매핑)·area+round 매칭·report69 회귀 통과. app.py `import ast` OK. **서버 전용**(확장 무변경·ZIP 불필요). 기존 HTML 표 파싱·URL 로드·이미지 OCR 경로 **무삭제**(TSV 폴백만 가산).
- **🎯 raceKey 경주번호 오검출 수정 — 활성 경주 탭/헤더 우선 읽기(佐賀 9R을 4R로 오검출 방지)**(확장 v2.1.62, `chrome-extension/content.js _activeRaceNo`·`extractRaceKey`): 사가 9경주를 보고 있는데 `佐賀 4R`로 잘못 감지되던 문제 수정(사용자 요청). 원인: URL `k_raceNo`가 stale하거나 본문 첫 `第N競走`(경주 목록 첫 항목)를 잡아 **실제 표시 경주(9R)를 4R로 오검출**. **[해결]** `_activeRaceNo()`가 화면에 실제 표시 중인 경주번호를 우선 읽음 — ①활성 경주 탭·링크(current/active/selected/on·aria-current/selected 등 다수 클래스 후보) ②경주 드롭다운(select)의 선택 option ③두드러진 경주 헤더(h1~h3·raceTitle 계열, `第N競走`/`NR`/`N레이스` 포함). 이 값을 **URL k_raceNo·본문 스캔보다 최우선** 채택(활성 탭 못 읽으면 기존 URL→본문 순 폴백 유지·무삭제). 활성 탭≠URL이면 콘솔에 `[raceKey 오검출 방지] 활성 경주 탭=9R ≠ URL k_raceNo=4R → 활성 탭 채택` 로그. 경주번호 파싱(`_n`)은 1~12만 허용(13R·0R 거부). 검증: `_n` 파싱 11케이스(第9競走·9R·9レース·9경주·범위밖 거부) 통과 + content.js `node --check` OK + ZIP 재빌드(v2.1.62). ⚠ 확장 새로고침 필수. 기존 URL 파라미터·본문 헤더·한국 네비 추출 **무삭제**(활성 탭 최우선만 가산). ⚠ keiba.go.jp 실제 활성탭 클래스가 후보에 없으면 F12 진단 필요(사용자 안내).
- **🔒 경주 자동 전환 근본 수정 — 서버측 raceKey 잠금(재시작에도 유지)**(`app.py _race_pin_load/save/clear`·`/api/race/pin`·`current_race`·`triple_latest`, `static/js/app.js toggleRacePin`·`initRaceRefresh`·`newRaceStart`): 프론트 게이트(76bff0e·383c5ac)로도 **사가→오비히로 전환이 서버 재시작 후 반복**되던 문제 근본 해결(사용자 3회 리포트). 근본 원인: oddspark 자동수집이 다른 경주(오비히로)를 저장하면 `current_race`·`triple_latest`가 **max-t(최신)로 그 경주를 반환** → 프론트가 아무리 막아도 서버가 계속 다른 경주를 최신으로 알림. **[서버측 잠금]** 새 `POST /api/race/pin {raceKey}`로 경주 고정 → **파일(`data/race_pin.json`)에 저장**(서버 재시작에도 유지). 고정 중이면 `current_race`·`triple_latest`가 max-t/번호진행 폴백을 **건너뛰고 항상 고정 경주 반환** → oddspark가 오비히로를 최신 저장해도 분석기는 사가 유지. `POST {clear:true}`로 해제. **[프론트 연동]** 📌 고정 버튼이 서버 잠금까지 호출(`toggleRacePin`), 페이지 로드 시 서버 고정 상태 복원(`initRaceRefresh`·재시작에도 프론트 동기화), `새 경주 시작`은 고정 자동 해제. 검증: 사가 고정 후 오비히로 재수집해도 current_race·triple/latest 모두 사가 유지 · 해제 시 오비히로 복귀 end-to-end 통과 + app.py `import ast`·app.js `new Function` OK. **서버+프론트**(확장 무변경). 기존 max-t 폴백·번호진행·stale 방어·프론트 게이트 **무삭제**(고정 시 우선 잠금만 가산). 프론트 게이트(_targetRaceKey)와 이중 방어.
- **🎯 축 선정 공식 강화 — 역배열 강도별 역할(축/보조/보험) + 지속성 확정 + 저배당 보조 유지 + 강도별 적중률 통계**(`app.py _reversal_role`·`_reversal_persistence`·`_reversal_axis_roles`·`_flow_removal`·`_reversal_strength_stats`·`/api/learning/reversal-stats`, `data/pattern_learning.json reversal_stats`): 역배열을 일괄 축 승격하던 것을 강도·지속성별로 세분화(사용자 요청 4부). **[1 강도별 역할]** `_reversal_axis_roles`가 challenger별 배당차%로 역할 부여 — **35%+ → 축(복승 메인 승격)** / **20~35% → 복승 보조** / **10~20% → 삼복승 보험** / **10% 미만 무시**. 기존 pre_reversal(ratio<0.80 일괄 축)을 강도별로 확장(무삭제). **[2 지속성 확정]** `_reversal_persistence`가 최근 스냅샷 exacta에서 역전 재계산 → **마감 전 2회+ 연속 감지 = 확정 축**, **1회만 = 후보(축→보조 강등)**. **[3 저배당 보조 유지]** `_flow_removal`이 흐름 없어도 **대표배당 3배 이하 저배당 말은 제거하지 않고 보조 유지**(시장 최상위권은 흐름 무관 실전력 → 완전 제거 금지). **[4 강도별 적중률 통계]** `_reversal_strength_stats`가 analysis_log 결과 있는 경주에서 경주당 최강 역전 1건을 강도 버킷으로 입상률·1착률 집계 → `pattern_learning.json reversal_stats` 갱신 + `GET/POST /api/learning/reversal-stats`. **집계 결과(28건): 축(35%+) 입상률 23.8%·1착률 9.5% / 보험(10~20%) 입상률 16.7%** → 역배열 강도가 높다고 적중률이 높지 않음(과신 경계·표본 50+ 유의 게이팅). 검증: `_reversal_role`(강도 5케이스)·`_reversal_axis_roles`(2회→확정축·1회→후보강등)·`_flow_removal`(3배↓ 유지) 단위 + 회귀(formula24·flow23·reversal29·report69·prerace18) 통과. app.py `import ast` OK. **서버 전용**(확장 무변경·ZIP 불필요). 기존 pre_reversal·복승받치기·흐름제거·역배열 감지 **무삭제**(강도별 역할·지속성·저배당 유지·통계만 가산).
- **🚨 경주 자동 전환 긴급 수정(근본 원인) — 30초 배당 폴링이 고정 무시하던 문제 차단**(`static/js/app.js _targetRaceKey`·`pollJapanOdds`): 앞선 고정 수정 후에도 **사가 5경주가 계속 오비히로 8경주로 전환**되던 진짜 원인 발견·수정. 12초 `refreshCurrentRace`만 고쳤으나, **별도의 30초 `pollJapanOdds`가 전역 최신 경주(`/api/odds/triple/latest` max-t)를 무조건 렌더**하며 고정·경마장 규칙을 무시하고 있었음. **[해결]** 두 자동 경로가 공유하는 `_targetRaceKey(latestRk)` 게이트 신설 — ①최초 로드=최신 따름 ②고정 중=현재 경주 유지 ③다른 경마장=전환 안 함 ④같은 경마장 다음 경주만 전환. `pollJapanOdds`가 이 게이트 통과 경주만 분석·렌더 → 고정/다른장이면 상단 바에 `📌 고정 중 · 다른 경주 감지(오비히로 8경주)` 힌트만 표시하고 **화면은 현재(사가 5경주) 유지**. 검증: `_targetRaceKey` 4케이스(최초로드·다른장유지·같은장전환·고정유지) 단위 통과 + app.js `new Function` OK. **프론트만**(서버·확장 무변경). 기존 폴링·렌더·onJapanOddsUpdate **무삭제**(전환 게이트만 가산).
- **🐎 오버레이 복병 정리 — 최대 3두 우선순위 표시 + 스마트머니 복승 보조 + 시장유력마 중복 제거**(확장 v2.1.61, `app.py _triple_analyze`(smart_quinella), `chrome-extension/overlay.js updatePanel`): 오버레이 복병이 6두까지 나열돼 산만하던 것을 핵심만 정리(사용자 요청). **[1 복병 최대 3두·우선순위]** 복병 후보를 점수화해 상위 3두만 표시 — ①**스마트머니+집중급락 동시**(pr3) ②**집중급락 횟수 많은 순**(pr2·anomCount 내림차순) ③**역배열 감지 말**(pr1). 3두 미만이면 급락 이상감지말로 보충. **[2 스마트머니 복승 보조]** `_triple_analyze`가 스마트머니(상승후급락=세력 진입) 복병을 **유력마 축과 복승 보조로 자동 편성**(`복승 보조(스마트머니 복병)` 8%·마감 전·최대 2두, `smartQuinella` 반환) → 웹 베팅표·오버레이 모두 반영. 오버레이에 `💰 복승 추가: 2+10 (스마트머니 · N배)` 강조 박스. **[3 시장 유력마 중복 제거]** 하단 `📊 시장 유력(전적 미수집)` 표시에서 **이미 유력마(keyHorses)·복병에 있는 말 제외**(중복 표시 방지). 검증: 복승 보조 편성 로직 + 회귀(formula24·flow23·report69) 통과. app.py `import ast`·overlay.js `node --check` OK + ZIP 재빌드(v2.1.61). ⚠ 확장 새로고침 필수. 기존 복병(집중급락/역배열/급락)·시장유력마·결론박스 **무삭제**(3두 제한·우선순위·스마트머니 복승·중복제거만 가산).
- **📌 경주 자동 전환 버그 수정 — 현재 경주 고정 버튼 + 다른 경마장 자동 전환 차단(사가↔오비히로 혼재 방지)**(`static/index.html rrPinBtn`, `static/js/app.js refreshCurrentRace`·`toggleRacePin`·`_raceVenue`): 사가 5경주를 보고 있는데 oddspark 자동 수집이 오비히로 8경주(반에이)를 수집하면 `/api/current_race`(max-t)가 그쪽을 최신으로 반환해 분석기가 **엉뚱한 경마장 경주로 자동 전환**되던 문제 수정(사용자 요청). **[1 수동/고정 시 자동 전환 비활성]** 12초 자동 폴링(`refreshCurrentRace(false)`)이 경주 변경을 감지해도, **고정 상태거나 다른 경마장이면 화면 전환 안 함**(라벨엔 감지된 경주 힌트만 표시). 수동 새로고침(`manual`)·다음 경주는 명시적 행동이라 전환 유지. **[2 종목/경마장 혼재 방지]** `_raceVenue(raceKey)`(경주번호 앞 경마장명 추출)로 **직전 보던 경마장 ≠ 새 경주 경마장이면 자동 전환 차단** → `🔀 다른 경마장 경주 감지(오비히로 8경주) — 전환하려면 새로고침` 안내. **같은 경마장 다음 경주(사가 5R→6R)는 기존대로 자동 전환**(정상 진행 유지). **[3 현재 경주 고정 버튼]** 상단 바에 `📌 현재 경주 고정` 버튼 추가 — 클릭 시 자동 전환 완전 중단(localStorage 유지, 새로고침에도 지속), 고정 중엔 라벨에 `📌` 표시 + 다른 경주 감지 시 힌트만. 다시 클릭(`📌 고정 해제`)하면 자동 전환 재개. 검증: `_raceVenue` 파싱(사가·오비히로·帯広·R표기)·경마장 비교(사가5≠오비히로8·사가5=사가6) 단위 통과 + app.js `new Function` OK. **서버+프론트만**(확장 무변경·ZIP 불필요). 기존 자동 전환(같은장 다음경주)·새 경주 시작·수동 새로고침 **무삭제**(고정·다른장 차단만 가산).
- **🎯 확장 종목 자동감지 강화 — URL·탭텍스트 기반 종목 재감지 + 전환 시 재수집(sport 태그 항상 정확 전송)**(확장 v2.1.60, `chrome-extension/content.js resolveSport`·`detectSport`·`watchSportChange`·`_resolveRaceKey`): 배당판 사이트 전환 시(경마↔경륜↔경정) 팝업에 남은 이전 종목 태그가 그대로 전송돼 서버 종목 전환 초기화가 늦던 문제 보완(사용자 요청). **[종목 결정 강화]** 새 `resolveSport(팝업선택, raceKey)`가 우선순위로 종목 확정 — ①raceKey에 일본 경마장명 → **무조건 경마**(팝업이 경륜/경정이어도 정정) ②URL/탭 제목에 명시 종목어(`競輪`/경륜·`競艇`/경정·오토레이스) → **그 종목으로 정정**(팝업 stale 방지) ③강한 신호 없으면 팝업 수동선택 존중 ④그것도 없으면 배당판 본문 텍스트 보조감지. 기존 `effSport = 팝업 or detectSport`(팝업이 무조건 우선)를 대체해 **강한 페이지 신호가 stale 팝업을 정정**. **[URL·종목 변경 감지]** `watchSportChange`(10초 워치 루프에 추가)가 **URL 변경 또는 종목 변경**을 감지하면 직전 전송 종목(`_lastSentSport`)과 비교 → 다르면 `collectTriple('sport-change')`로 **즉시 재수집** → 새 sport 태그가 서버에 전달되어 서버측 종목 전환 초기화(배당·전적·히스토리)가 즉시 작동. `_resolveRaceKey`의 board-first 목록에 `sport-change` 추가(전환 시 현재 배당판 경주 우선). **[탭텍스트 감지 확장]** `detectSport` 본문 키워드에 `ボートレース`/보트레이스·`けいりん` 추가(경륜/경정 배당판 표기 다양성 대응). 검증: resolveSport 우선순위 6케이스(경마장raceKey→경마 정정·URL競輪→경륜·제목競艇→경정·신호없음 팝업존중·본문보조·무신호 경마) 단위 통과 + content.js `node --check` OK + ZIP 재빌드(v2.1.60). ⚠ 확장 새로고침 필수. 기존 detectSport·수동선택·경주감지(watchRaceChange)·한국모드 판정 **무삭제**(강한신호 정정·URL/종목 워치·재수집만 가산). 서버측 종목 전환 초기화(v2.1.59 직전 커밋)와 상호 보완.
- **🔄 종목 전환(경마→경륜) 잔존 데이터 완전 초기화 + 마번 범위 검증**(`app.py _do_triple_ingest`·`_sport_max_no`·`_triple_analyze`(valid_nos·유력마 최종게이트)): 경륜(7명) 경주인데 이전 지방경마(11두) 데이터가 남아 **7명 경주에 11번이 추천**되던 잔존 버그 수정(사용자 요청). **[1 종목 전환 완전 초기화]** `_do_triple_ingest`가 같은 raceKey에서 **종목(sport)이 바뀌면**(horse→cycle 등) 확립 여부와 무관하게 **강제 초기화** — 히스토리(이전 종목 배당) 비움 + 이전 종목 전적(starters) 제거(한국 PDF 사전분석분은 아침 일괄이라 보존) + 새 종목을 우선 적용(기존엔 `sport = sport or prev.sport`로 이전 종목이 상속돼 전환이 묻히던 문제 해결). 반환 `sportChanged` 플래그. **[2 마번 범위 검증]** `_sport_max_no`(경륜 9·경정 6·오토 8·경마 18)로 **종목별 최대 마번 초과분을 유효출전마(valid_nos)에서 제외** → 배당에 11번이 섞여 있어도 경륜이면 무시. 이상감지말(anomaly_horse)·유력마(key_horses)·실시간추가(realtime_added)·역배열조기(pre_reversal)·급락승격(surge_promote)에 **최종 게이트** 추가 — 추천 조립 직전 범위밖 마번 완전 제외("수집된 마번 외 추천 완전 제외"). **[3 새 경주 버튼]** 기존 `[🆕 새 경주 시작]`(`newRaceStart`→`/api/odds/triple/reset` 전체 초기화 + `hardResetRaceState` 프론트 상태 초기화 + 확장 재수집)이 이미 모든 활성 데이터 강제 초기화 → 유지. **[부수 버그 수정]** `_horse_rep_series`(흐름 점수)가 hist quinella를 `_parse_combo_map`(dict 전용)으로 파싱해 triple_store history(list 형식)에서 크래시하던 문제 → **list·dict 양형식 지원 `_as_qmap`으로 교체**(직전 커밋의 dict-only 오수정 정정). 검증: 경마(11두)→경륜(7명) 전환 시 history·전적 초기화·sport=cycle 확인 + 경륜 배당에 10·11번 섞여도 validHorses [1,2,3,7]·추천 미포함 확인 + 흐름점수 정상 산출(양형식) + 회귀(formula24·flow23·reversal29·report69·prerace18) 통과. app.py `import ast` OK. **서버 전용**(확장 무변경·ZIP 불필요). 기존 유력마·전적·잔존마 필터·baseline 재설정 **무삭제**(종목 전환 감지·마번 범위 게이트만 가산).
- **🌊 배당 흐름 기반 제거 시스템 강화 — "들어올 말 찾기 → 안 들어올 말 제거"(핵심 철학 전환)**(확장 v2.1.59, `app.py _horse_rep_series`·`_flow_scores`·`_flow_removal`·`_high_odds_candidates`·`_triple_analyze`, `static/js/app.js renderFlowSection`·`renderTopHorses`·`_marketEffOdds`, `chrome-extension/background.js autoTick`): 배당 흐름(시간에 따른 변화)을 점수화해 **흐름 없는 말은 저배당이어도 제거, 흐름 좋은 고배당은 삼복승 보험으로 편입**하는 관점 전환(사용자 요청 5부). **[1 제거 기준 강화]** `_flow_scores`가 말별 최근 6스냅샷 대표배당(단승 우선·없으면 최저 복승) 흐름을 판정 → **죽은인기(T-5~T-1분 무변동)·3회 연속 상승(자금 이탈)·페이크(급락 후 반등, `horseStreaks.rebounded`)·역배열 반대(쌍승역전 detected 시 밀려나는 원인기마 `banner.reversal.favorite`)** 를 `removeReasons`로 태깅, `_flow_removal`이 유력마 축(상위2) 제외하고 제거 대상 선정(⚠ 실제 승자 오제거 방지 위해 challenger·축은 절대 제거 안 함). **[2 고배당 후보 발굴]** `_high_odds_candidates`가 제거 후 남은 말 중 **대표배당 10배+ 인데 하락/급락/스마트머니(흐름 좋음)** → 고배당 후보 자동 편입 + `_triple_analyze`가 축2두+후보 **삼복승 보험(💎)** 자동 편성(마감 전만·상위2두). **[3 배당 흐름 점수]** 상승 -10·변동없음 -5·하락 +10·급락 +20·스마트머니 +30(강한 것 우선) + 페이크 -15. **[4 추천 표시 흐름 순]** `_marketEffOdds`에 흐름 계수 반영(스마트머니×0.6·급락×0.75·하락×0.9·상승×1.4·무변동×1.2) → TOP5·유력마 정렬이 **저배당 순 → 흐름 점수 순**(기존 시장배당·이상감지 할인 유지·곱셈만 가산). TOP5 각 행에 `흐름 +N점 {추세}` 칩 + 카드 하단 `💎 고배당 추천(흐름 기반)`·`🔴 제거(흐름 없음)` 섹션(`renderFlowSection`). **[5 수집 간격 최적화]** `background.js autoTick`에 **마감 10분전 1분 → 7.5분전 30초** 단계 추가(기존 5분 15초·3분 10초·2분 5초·30초 3초 유지, `Math.min`으로 설정이 더 빠르면 유지) → 배당 흐름 변화 최대 포착. 검증: 시뮬 히스토리로 죽은인기(무변동)·3연속상승 제거·스마트머니 고배당(18배) 후보 편입·흐름 점수 정확 산출 확인 + 회귀(formula24·flow23·report69) 통과. app.py `import ast`·app.js `new Function`·background.js `node --check` OK + ZIP 재빌드(v2.1.59). ⚠ 확장 새로고침 필수. 기존 유력마/제거/복병/베팅 로직 **무삭제**(흐름 점수·흐름 제거·고배당 후보·수집단계만 파생 가산).
- **🎯 삼복승 구성 3두의 복승 3쌍 자동 편성 (하코다테 11R 복기 반영)**(`app.py _triple_analyze` 베팅 조립): 하코다테 11경주(경륜) 복기 — 삼복승 메인 1+3+7인데 복승 추천엔 1+7·3+7만 있고 **1+3 쌍이 누락**됐던 문제(기존 복승 메인=상위2유력마[h1,h2]·보조=[h1,h3]만 편성). 수정: 삼복승 3두의 **복승 3쌍(h1+h2·h1+h3·h2+h3)을 모두** 추천에 포함하고, **배당 낮은 순으로 정렬해 최저 복승을 메인·나머지를 보조**(alloc 43/20/12)로 배정. 하코다테 11R 재현: 1+3+7 → 복승 메인 3+7(9.1)·보조 1+7(9.5)·보조 1+3(12.0) 전부 편성. `_addbet` dedup으로 중복 자동 제거·기존 삼복승/보험/받치기 로직 무영향. 유력마 2두면 기존대로 복승 메인 1쌍. 검증: 3쌍 정렬 단위 + 회귀 185건+flow23 통과. app.py `import ast` OK. 서버 전용(확장 불필요). 기존 베팅 조립 **무삭제**(3쌍 편성·배당순 메인만 개선).
- **🛡️ 마감 순간 급락 삼복승 보험 + 경륜 발주시각 조기 감지 + 이상감지 히스토리 최신순(기후 9R 복기 반영)**(확장 v2.1.58, `app.py _closing_drop_insurance`·`_triple_analyze`, `chrome-extension/content.js`, `static/js/app.js _anomalyRows`): 기후 9경주(경륜) 복기 — 복승 1+7은 적중했으나 삼복승 3착에서 신호 약한 5번(20배)이 들어와 놓친 near-miss + 모든 급락이 마감(T-0)에 몰려 반영 못 한 케이스. **[1 결과 입력]** 기후 9R(1착1·2착7·3착5·복승 4.4) 저장 → 복승 적중·삼복승 미적중 학습 반영. **[2 마감 순간 급락 삼복승 보험]** `_closing_drop_insurance`가 마감(cur_mb≤1) 순간 30%+ 급락 말(역배열 없어도)을 축(유력마 상위2)+급락말 삼복승 보험 자동 편성(`_addbet` 4%). 기후 9R 재현: 1+7 축 + 5·6번 → **1+5+7·1+6+7 보험**. ⚠ **대규모 급락(하코다테 10R 163건 동시급락=시장 재설정 노이즈)은 이중 가드**(massDrop 플래그 + 급락말이 출전 60%+면 marketWide) 제외. **[3 경륜 발주시각 조기 감지]** `content.js` POST_TIME/카운트다운 정규식에 일본 공영경기 라벨(投票締切·発走時刻·残り·あと·締切まで, 초 없는 "N分"도) 추가 → 경륜 배당판 발주시각 조기 감지·마감 훨씬 전부터 수집. **[이상감지 히스토리 최신순]** `_anomalyRows`가 서버 시간순(오래된→최신)을 복사본 reverse로 **최신 감지가 맨 위** 표시(현재 피드·히스토리 공통). 검증: 기후 9R active·하코 10R marketWide skip 단위 + 회귀 185건+flow23 통과. app.py `import ast`·app.js `new Function`·content.js `node --check` OK + ZIP 재빌드(v2.1.58). ⚠ 확장 새로고침 필수. 기존 forced_trifecta·발주감지·피드 렌더 **무삭제**(마감급락 보험·라벨·최신순만 가산).
- **🐎 복병_집중급락 패턴 학습·즉시 적용 (집중급락 10회+ 복병 자동 편입 + 스마트머니 신뢰도)**(확장 v2.1.57, `app.py _concentration_counts`·`_triple_analyze`(darkHorses), `static/js/app.js renderDarkHorses`, `chrome-extension/overlay.js updatePanel`, `data/pattern_learning.json bokbyeong_patterns`): 마에바시 8경주(경륜) 복기 — 8번이 낀 조합 다수(2+8·8+9·7+8·6+8·4+8·3+8·5+8)가 동시 급락해 이상감지 **15회 최다**(자금 집중)였고 스마트머니(상승후급락)까지 겹쳤으나, 대표배당이 시장 중하위권(12~16배 7위)이라 메인 유력마엔 미편입됐던 케이스 학습. **[패턴 저장]** `pattern_learning.json`에 새 키 `bokbyeong_patterns`로 케이스 저장(기존 `patterns` 89건·`condition_stats` 무손상). **[즉시 적용]** `_concentration_counts`가 경주 이상감지 이벤트에서 말별 등장 횟수 집계 → `_triple_analyze`가 **집중급락 10회+ 말은 배당 순위 무관 복병 자동 편입**(`darkHorses.forced`) + **스마트머니(strong_signals Type 8) 감지 말은 신뢰도 '높음'** 표시. 웹 복병 섹션 `renderDarkHorses`(🔥 집중급락 N회·💰 스마트머니·신뢰 높음, 유력마 제외·상위 4두)·오버레이 복병에 편입. 검증: 마에바시 8R 실데이터로 8번 15회→복병 자동편입 확인 + 회귀 185건+flow23 통과. app.py `import ast`·app.js `new Function`·overlay.js `node --check` OK + ZIP 재빌드(v2.1.57). ⚠ 확장 새로고침 필수. 기존 복병(역배열/급락) 산출 **무삭제**(집중급락·스마트머니 복병만 가산).
- **📡 수집 조기 중단 방어 (발주 전 미수집 감지 + 자동 재수집)**(확장 v2.1.56, `app.py _triple_analyze`, `static/js/app.js renderTripleAnalyze`, `chrome-extension/overlay.js updatePanel`·`background.js autoTick`): 고쿠라 8경주(중앙경마)가 T-8분에 수집이 끊겨 JRA 마감구간(T-3/T-2분)을 통째로 놓친 케이스 방어(사용자 요청). **[서버 감지]** `_triple_analyze`가 **발주시각 감지됨(cur_mb>0) + 마지막 수집 후 2분+ 경과 + 마감 전**이면 `collectionStalled`(+`secsSinceCollect`) 반환. **[웹·오버레이 경고+재수집]** 분석기·오버레이에 `⚠️ 수집 중단 감지 · 자동 재수집 시도 중...` 배너 + 확장에 `FORCE_COLLECT` 릴레이(30초 1회 throttle, 웹=`nudgeExtensionCollect` postMessage / 오버레이=`chrome.runtime.sendMessage`). **[백그라운드 self-heal]** `background.js autoTick`이 발주 전(left>0)인데 마지막 수집 성공 후 2분+ 경과면 `_nextDueAt=0`으로 **즉시 재수집**(백그라운드 전용 모드에서도 자동 복구) + autoStatus `stalled` 플래그. overlay.js·background.js `node --check` OK + ZIP 재빌드(v2.1.56). ⚠ 확장 새로고침 필수. 기존 수집 루프·FORCE_COLLECT 핸들러 **무삭제**(중단 감지·자동 재수집만 가산).
- **🛠️ 발주시각 오검출 방어 + 급락 후 추천 재산출 지속 + 중앙경마(JRA) 배당판 조기 마감 특성(하코다테 8R 복기 반영)**(확장 v2.1.55, `app.py _history_append`·`_analysis_log_save`·`_triple_analyze`, `static/js/app.js`, `chrome-extension/overlay.js`): 하코다테 8경주 복기 결과 — 발주시각이 **T-1분→T-7분으로 역행 오검출**되어 초반이 '마감/T-1분'으로 오판됐고(realtime_added가 오검출 after_close로 무력화), 8번(8+9 -43% 급락·8↔3 역배열)이 있었으나 **추천 로그가 13:38 이후 멈춘** 문제 3방향 수정. **[1번 발주시각 오검출 방어]** `_history_append`가 직전 대비 발주까지 분(mb_signed)이 **3분+ 뒤로 이동 + 직전이 T-2분 이내**면 오검출로 판정 → **과거의 잘못된 after_close 전부 무효화**하고 올바른(늦은) 발주시각으로 재설정(`deadline_corrected` 표기), 반환 `deadlineCorrected` + 웹 `🛠️ 발주시각 오검출 정정` 배너. **[2번 급락 후 추천 재산출 지속]** `recommendation_history`가 추천 sig 변경 시에만 기록하던 것 → **마지막 추천 이후 새 이상감지 유입(anomaly_history 누적 증가) 시 sig 동일해도 이력 갱신**(`anomCount`/`sigChanged` 기록) → 13:38 멈춤 해결. **[3번 중앙경마 마감 특성]** JRA(`category=japan_central`)는 실제 발주 2분 전 배당판이 닫히므로 → **강제 추천을 T-3분으로 앞당기고(cur_mb≤3.5) T-2분을 최종 확정(실질 마감)으로 처리**(cur_mb≤2.5 locked), `centralClosing`(T-2.5분~) 시 웹·오버레이에 `⚠️ 중앙경마 배당판 T-2분에 닫힘 · 지금이 마지막 신호!` 경고. 반환 `isCentral`/`centralClosing`. 검증: [1] 오검출 7케이스·[2] 이력 4케이스 단위 + 전체 회귀 185건+flow23 통과. app.py `import ast`·app.js `new Function`·overlay.js `node --check` OK + ZIP 재빌드(v2.1.55). ⚠ 확장 새로고침 필수. 기존 마감판정·추천이력·타이밍 로직 **무삭제**(오검출 정정·이력 지속·중앙 앞당김만 가산).
- **⏱ 타이밍 추천 정책: T-2분 강제 추천 · T-1분 최종 확정 · 마감 후 추천 금지**(확장 v2.1.54, `app.py _triple_analyze`(recommend_phase), `static/js/app.js renderBetRecommend`, `chrome-extension/overlay.js renderConclusion`): 발주 시점(cur_mb)·마감 여부로 추천 단계를 명확히 구분(사용자 요청). **[T-2분 강제 추천]** `recommend_phase=forced`(1.5<cur_mb≤2.5) → 신호가 약해(wait/패스형) 평소엔 '⏳ 신호 대기'로 보류되던 것을 **게이트 해제**하고 저배당(시장 유력마) 기준으로 **강제 추천** 편성(웹 `⚡ T-2분 강제 추천` 배너·오버레이 결론박스 `⚡ 지금 사세요! (T-2분 강제)`). **[T-1분 최종 확정]** `recommend_phase=locked`(cur_mb≤1.5) → **🔒 최종 확정** 표시(웹 메인추천 `🔒 확정` 배지+빨강 배너·오버레이 `🔒 최종 확정 · 지금 사세요!`)로 이 조합이 마감까지 확정임을 명시. **[마감 후 추천 금지]** `recommend_phase=closed`(after_close) → 웹·오버레이에서 **추천 조합 숨김**(`🔒 마감 — 추천 종료 · 참고만`), 발주 후 추천 완전 차단. ⚠ **서버 `betRecommend` 데이터는 결과 학습(`_apply_result_learning`)이 재사용하므로 비우지 않고 유지** — 표시 차단은 프론트 플래그(`recommendPhase/Forced/Locked/Closed`)로만 처리해 학습 무영향. 반환에 4개 플래그 추가. 검증: phase 로직 9케이스 단위 + 전체 회귀 185건(formula24·strong45·reversal29·report69·prerace18) + flow23 통과. app.py `import ast`·app.js `new Function`·overlay.js `node --check` OK + ZIP 재빌드(v2.1.54). ⚠ 확장 새로고침 필수. 기존 추천·게이팅·학습 로직 **무삭제**(타이밍 단계·표시 차단만 가산).
- **🎯 TOP5 ↔ 분석기 유력마 정렬 기준 통일(복승배당 기준) + 이상감지 🚨 구분 + 카드 겹침 수정**(`static/js/app.js _marketReprOdds`·`_marketEffOdds`·`_marketOrderNos`·`renderTopHorses`): 유력마 **TOP5(확신도 기반 정렬)**와 **분석기 ⭐유력마 라인(복승배당 기반)**이 서로 다른 순서로 나와 혼란스럽던 문제 해소(사용자 요청). **[정렬 통일]** TOP5 정렬을 **복승 대표배당 낮은 순(시장 기준)** + **이상감지 상위 노출**(급락/집중 신호 말은 배당 할인 ×0.5~0.7로 상위 부상)로 변경 → ⭐유력마 라인(3곳)도 동일 기준(`_marketOrderNos`)으로 정렬 표시 → 두 화면 순서 일치. **[표시]** TOP5 헤더 `⭐ 시장 유력마 TOP 5 (복승배당 기준·이상감지 상위)`, 이상감지 말은 **🚨 이상감지** 배지로 별도 구분(TOP5 포함하되 구분). **[겹침 수정]** TOP5 카드 `position:sticky`(스크롤 시 아래 내용이 노란 테두리 밑으로 겹쳐 들어감) → `position:relative`로 변경해 자연 스크롤(겹침 제거). 확신도 칩(`확신 N`)은 표시용으로 보존(정렬에서만 제외). app.js `new Function` OK. 기존 렌더·확신도·복병 로직 **무삭제**(정렬 기준·🚨 구분·겹침 수정만 가산).
- **📊 배당 우선 로직 전환(전적 40%→30% · 배당 60%→70%) + 이중수렴 강력추천 + 고배당 전적마 보험 하향(후쿠시마 5R 복기 반영)**(확장 v2.1.53, `app.py _prob_ev`·`_learned_integrated_weights`·`_integrated_grades`·`_elimination`·`_triple_analyze`, `static/js/app.js renderTopHorses`, `chrome-extension/overlay.js updatePanel`): 후쿠시마 5경주(추천 1+10 / 서브 7+10) 복기 결과 — **4번(대표배당 9.3배·C등급·전적37.5)**은 시장이 7번(24배·A등급)보다 유력하게 봤으나, 시스템이 **A등급 전적** 때문에 7번을 서브픽으로 넣고 4번을 밀어낸 **전적 과가중** 재확인(4번 배당은 1+4 조합 기준 10.0→9.3으로 완만 하락, 뚜렷한 급락 신호는 없었고 3분 관측창·단승 미수집). 사용자 요청으로 **배당 우선 로직** 전면 전환: **[1]** `_prob_ev` 통합확률 = 시장0.6+전적0.4 → **시장0.7+전적0.3**(배당 우선) + 배당 기반 전적 가감(**10배+ → 전적×0.7 감가**, **5배↓ → 전적×1.1 보정**), `_learned_integrated_weights`/`_integrated_grades` 기본값도 0.3/0.7로. **[2·이중수렴]** 저배당(시장 유력 ≤5배)+전적 우수(≥61) = `dualConverge` → 통합 랭킹 +50 가점 → **💥 이중수렴 강력추천**(오버레이·웹 표시). **[3·보험 하향]** 전적 우수(≥61)하나 배당 높음(15배+) = `insuranceDemote` → 메인 유력마에서 **후순위(보험용)로 재배치**(삭제 아님) + TOP5에 **⚠️ 전적 우수하나 시장 비인기 · 고배당 보험으로만 활용** 경고 + 저배당 시장유력마 상향 정렬. 후쿠시마 5R 재현 검증: 1번(4.1배·전적70) → 💥이중수렴 1위·전적 77(×1.1), 7번(24배·전적88) → ⚠️보험 하향·전적 61.6(×0.7), 4번(9.3배)이 7번보다 상위 편입. 검증 `run_formula` 24건(배당 우선 공식 갱신·고배당×0.7 추가)·전체 회귀(reversal 29·report 69·prerace 18·strong 45) 통과. app.py `import ast`·app.js `new Function` OK.
- **🐎 오버레이 복병 급락 확장 + 정보 복원(적정 수준)**(확장 v2.1.53, `chrome-extension/overlay.js updatePanel`): 오버레이 **[3번 복병]**이 역배열 실질유력마만 표시하던 것 → **급락 이상감지말(anomalyHorse·최대 급락 조합)까지 확장**(유력마 아닌 말·최대 2두, `🐎 복병 5번 (급락)`). 결론 박스(🎯지금 사세요!·복승/삼복승)·⭐유력마·핵심 신호 3줄(🔴급락·🔄역배열·💡저배당 압축)·마감 임박 3단계(T-2분 주황·T-1분 빨강·T-30초 깜빡임)는 기존 유지. overlay.js `node --check` OK + ZIP 재빌드(v2.1.53). ⚠ 확장 새로고침 필수. 기존 렌더·신호 산출 함수 **무삭제**(급락 복병·배당 우선만 가산).
- **🎯 저배당 압축 축 판정 + 실시간 유력마 추가 + 오버레이 적정 균형(후쿠시마 2R 복기 반영)**(확장 v2.1.51, `app.py _compression_pattern`·`_triple_analyze`(realtime_added), `static/js/app.js renderRealtimeAdded`·`renderTopHorses`·`renderKoreaIntegrated`, `chrome-extension/overlay.js updatePanel`·`renderConclusion`): 후쿠시마 2경주(추천 1-7-3) 로그 복기 결과 — **유력마가 전적점수 순(1=101.7·3=76.7·7=73.9)으로만 뽑혀** 7번(전적 우수·복승 82배 아웃사이더)이 들어가고, 시장 최강(복승 **4.9배**)+급락 -29.9%인 6번은 전적 0 → 등급 C로 제외됐고, 그 6번 급락 신호가 와도 유력마에 편입 안 되던 문제를 3방향 개선(사용자 요청). **[2번 저배당 압축 축 판정·경마전용]** `_compression_pattern`이 저배당 밀집만으로 추천하던 것 → **명확한 1순위 축(최저배당이 2순위보다 30%+ 낮음)일 때만** 추천, 둘 다 저배당인데 배당 비슷(차이<30%)이면 **축 불명확 → 추천 보류**(`noAxis`). 경륜/경정/바이크는 **저배당 압축 공식 비활성화**(`sport` 파라미터, 배당 신호 기반만) — 반환에 `axis`·`axisGap` 추가. **[3번 유력마 실시간 추가]** `_triple_analyze`가 초반 유력마 고정 후 **실시간 급락(마감 T-3분내 -25%+·평상 -30%+)·역배열 감지 말**(대표배당 30배 이하 실경쟁마)을 유력마에 **추가**(기존 유지·최대 2두) → `keyHorses`에 편입되어 추천 조합에도 포함, 반환 `realtimeAdded`. 오버레이 `⚡ N번 실시간 추가!`(급락%)·분석기 웹 유력마 카드/한국 통합 뷰에 `⚡ 실시간 유력마 추가` 배너(`renderRealtimeAdded`). **[오버레이 적정 균형]** 직전 과단순화(핵심신호 2줄만)를 되돌려 — 결론 박스(🎯지금 사세요!/💡참고/⏳대기/⛔패스·삼복승 보험 라벨) + 마감 카운트다운 + **⭐유력마 3 + 🐎복병(역배열 실질유력마)** + ⚡실시간 추가 + **핵심 신호 3~4줄(🔄역배열·🔴급락·💡저배당 압축 축)** + **T-30초 마감 임박 시 최종 조합만 표시**(상세 생략·집중). 검증: 압축 축 명확(34% 축 검출)·불명확(6% 보류)·경륜 비활성 단위 + `run_strong_signals` 45건(축 규칙 반영 갱신)·전체 회귀 222건 통과. app.py `import ast`·app.js `new Function`·overlay.js `node --check` OK + ZIP 재빌드(v2.1.51). ⚠ 확장 새로고침 필수. 기존 압축 학습·유력마 산출·오버레이 렌더 함수 **무삭제**(축 판정·실시간 추가·복원만 가산).
- **🎯 오버레이 최종 결론 박스 + 구조 단순화(경마/경륜/경정 공통) — 신호 개수별 결론·핵심 신호 2줄**(확장 v2.1.50, `chrome-extension/overlay.js renderConclusion`·`signalCount`·`updatePanel`): 오버레이가 정보 나열식이라 "지금 뭘 사야 하는지" 한눈에 안 들어오던 것을 경륜 스타일 **최상단 결론 박스 + 단순 구조**로 개편(사용자 요청). **[1번 최종 결론 박스]** 패널 최상단·가장 크게 — **신호 2개+ → `🎯 지금 사세요!`**(복승·삼복승·역배열 추가 조합) / **신호 1개 → `💡 참고 추천`**(복승 소액) / **신호 0 → `⏳ 신호 대기 중`** / **패스형(raceJudgment.type) → `⛔ 이번 경주 패스`**. 신호 개수는 `strongSignals.count`(없으면 급락+역배열 감지 수) 기준, 조합은 기존 `betRecommend`(복승 메인)·`trioRecommend`·역배열 실질유력마(`inverse.invLead`)+유력마 상위2두에서 산출. 마감 후는 회색 `⏱ 마감 후·참고만`. **[2번 정보 순서 단순화]** 1.결론 박스 → 2.마감 카운트다운 → 3.유력마 3마리 → 4.핵심 신호 2줄. **환수율 세부 수치·단승 인기순위 목록·통합등급·전적우수·BMED 상세·역배열 감지 상세 박스 등 색깔 박스를 오버레이에서 제거**(축약). **[3번 핵심 신호 2줄]** `🔴 {말} 급락 -N%`(anomalyHorse·최대 급락) + `🔄 {말}번 역배열 N%`(invLead·배당차) — 이 2줄만 표시. **[전 종목 동일]** 결론 박스·핵심 신호는 종목 무관 공통 필드(betRecommend/trioRecommend/keyHorses/drops/inverse)를 쓰므로 **경마·경륜·경정 모두 같은 구조**로 표시. **무삭제 원칙**: 제거한 상세는 오버레이 **표시만** 축약한 것이고, 렌더 함수(`renderStrongSignals`·`renderCompression`·`topGrades`·`bmedRows`)는 보존(경주 종료 후 보존 신호는 `renderStrongSignals` 유지)하며 **전체 상세는 분석기 웹에 그대로** 있음. 중앙 팝업(`renderAlertPopup`)·드래그 이동·읽기전용 원칙 유지. `node --check` OK + ZIP 재빌드(v2.1.50). ⚠ 확장 새로고침 필수. 기존 분석 데이터·수집·웹 상세 뷰 **무삭제**(오버레이 결론 박스·단순 레이아웃만 추가·표시 축약).
- **🎖️ 등급 체계 + 뒷힘(상3F) 로직 + 고배당(단승) 자동 제거 — 전적 채점·제거를 종합 판단으로 강화(제주 케이스 방지)**(`app.py grade_bonus`·`dist_exp_bonus`·`back_power_bonus`·`_apply_back_power`·`_high_odds_verdict`·`_grade_rank`·`_grade_exp_bonus_of`·`_elimination`·`score_horses_raw`·`_keiba_build_form`·`_jra_build_form`·`_keiba_parse_horse_past`·`_jra_past_cell`·`RACE_SHEET_SCHEMA`, `static/js/app.js renderKeibaStarters`): 착순만 보던 전적 채점에 **등급·당거리·뒷힘·단승 고배당**을 종합 반영(사용자 요청 5개 항목). **[1·5번 고배당 자동 제거]** `_high_odds_verdict`가 **단승 배당** 기준으로 **999배=무조건 제거(전적 착순 좋아도)·100배+=확실 제거·50~99배=제거 권장·30~49배=관찰(이변신호 없으면 제거)** 판정 → `_elimination`이 완화 게이트보다 **우선 적용**(999/100+는 급락·쌍승상위 신호도 무시). 제주 케이스(단승 50배+ 말이 하위등급 성적만으로 착순 좋아 보이는 것) 방지 — 검증: 단승 120배+전적 최고점(placings [1,1,2]) 말도 확실 제거, 999배는 급락신호 있어도 제거. **[2번 등급 체계]** `grade_bonus`가 등급 경험(한국 **G1~G3 +20·오픈 +10**·하위군 보통 / 지방 **A1 +20·A2 +15·B +10·C1 +5**)의 최상위 하나 + **등급 하락마(상위→하위·강자) +15**·**상승마(하위→상위·적응중) -5** 보정. 한국 PDF 스키마에 `grade`(현재/과거 경주 등급) 추출 필드 + 프롬프트 추가, 지방(NAR) 전적표 레이스명·중앙(JRA) 馬柱 Icon_GradeType에서 등급 자동 파싱. **[3번 당거리 경험 + 뒷힘]** `dist_exp_bonus`(당거리 정확 출전 경험 **+10**·미경험 0) + `back_power_bonus`(상3F 경주 내 상대: **상위30%(빠름) +15**·하위30%(느림) -5) — `_apply_back_power`가 경주 전체 상3F로 상대 평가. **거리 미경험 + 뒷힘 강함 → `⚡ 거리 미경험이나 뒷힘 강점`** 플래그(`backPowerInsurance`) → 삼복승 보험 후보(라이브 검증: 후쿠시마 11R ゾンニッヒ 1600→1200 미경험+상3F 34.1 상위30% → 보험 True). **[4번 BMED 통합]** 등급·당거리·뒷힘 보너스를 **전적 총점(totalScore)에 가산** → 기존 확신도(`_confidence`·`_fav_score`·통합등급)에 자동 반영(전적 점수 경유). 프론트: 지방/중앙 전적 표에 **💪 뒷힘 강점 말** 요약 박스(상3F·거리 미경험 여부)·행 표시(💪/⚡·등급보정 배지) 추가. 검증: 신규 헬퍼 8종 단위(등급 경험/하락강자/상승·당거리·뒷힘 상위30%/하위·고배당 999/100/50±신호/30/20)·`_elimination` 고배당 제거 통합(120배 전적최고 제거·999 신호무시)·라이브 JRA(등급 오픈+10·당거리+10·뒷힘±15·보험 플래그)·기존 회귀 7종(formula23·reversal29·report69·prerace18·flow23·strong44·stats15=221건) 전건 통과. app.py `import ast`·app.js `new Function` OK. **서버+프론트만**(확장 무변경·ZIP 불필요). 기존 전적 채점(각질·거리변화·기수·부담)·제거 로직·통합등급 **무삭제**(등급·당거리·뒷힘·단승 고배당 보정만 가산·기존 데이터 없으면 0=기존 동작). ⚠ 한국 PDF 등급은 **재업로드 시 추출**(기존 세션은 grade 없어 등급보정 0), 단승 고배당 제거는 단승 수집되는 **일본 우선**(한국=복승 150배+ 기존 하드컷 유지).
- **🏇 중앙경마(JRA) 전적 신규 수집 — netkeiba 馬柱(shutuba_past) 서버 파싱: 각질·통과순위·거리변화·상3F·부담중량 → 유력마·통합등급 연동**(`app.py _JRA_TRACK`·`_jra_venue_code`·`_jra_race_list`·`_jra_resolve_raceid`·`_jra_past_cell`·`_jra_parse_shutuba_past`·`_jra_style`·`_jra_build_form`·`/api/jra/starters`, `static/index.html`·`static/js/app.js fetchJraStarters`): **중앙경마는 그동안 "전적표 없음 → 배당 이상감지 전용"**이었는데(keiba.go.jp는 지방 전용·JRA 공식은 POST세션 스크래핑 난해), **netkeiba 馬柱 페이지가 서버렌더 HTML로 한 화면에 마번·기수·부담중량 + 과거5주(착순·거리·마장·통과순위·상3F·마체중)를 전부 제공**함을 확인해 전적 소스로 신설(사용자 요청·소스 조사 결과). **[race_id 자동 매핑]** race_id 12자리에 場코드(4:6)·경주번호(10:12)가 내장 → `_jra_race_list`가 `race_list_sub`(서버렌더 fragment)에서 그날 race_id 집합만 받고 `_jra_resolve_raceid`가 경마장(場코드)+경주번호로 매핑(개최회차·개최일 추정 불필요·6시간 캐시). **[馬柱 파싱]** `_jra_parse_shutuba_past`가 HorseList 행에서 마번(Waku)·마명(Horse02 링크)·기수(Jockey 링크)·현재 부담중량 + Past 셀 5개(Data01=날짜·착순 / Data05=코스·거리·마장 / Data03=두수·인기·기수·부담 / **Data06=통과순위·상3F·마체중**)를 추출, 범례(placeholder '馬名') 행 자동 제외. **[각질 직접 취득]** netkeiba가 각질을 표기(`kyakusitu` 逃/先→선행+3·差/追→추격+5·自→평지)해 **통과순위 추정보다 정확** → `_jra_style`이 netkeiba 표기 우선·없으면 통과순위(`_keiba_corner_style`) 폴백. **[통합 채점]** `_jra_build_form`이 지방경마와 **동일 채점식**(base_form_score 최근5착순 가중평균 + 각질/거리변화(단축+5)/부담중량(감소+5) 보정 + 상3F) + 사분위 상대등급 A/B/C/D. `/api/jra/starters`(body: raceKey|venue+raceNb+raceDy 또는 raceId 직접)가 fetch·파싱·채점, raceKey 연동 시 `STARTERS_STORE`(**source=jra**) 저장 → 기존 `_form_from_starters`→`_triple_analyze` 통합등급(전적+배당역배열)에 자동 반영(지방경마와 동일 경로). 프론트: 일본경마 탭에 `🏇 중앙경마(JRA) 전적 분석` 패널(경마장·개최일·경주·raceKey) 추가, 표시는 구조 동일한 `renderKeibaStarters` 재사용. 검증: 라이브 3경주 파싱(福島11R 9두·小倉11R 11두·函館11R 13두·두자릿수 마번 정상)·엔드포인트 200(race_id 202603020511 자동매핑·각질·상3F·등급·raceKey 연동 source=jra)·전 회귀 7종(formula23·reversal29·report69·prerace18·flow23·strong44·stats15) 통과. app.py `import ast`·app.js `new Function` OK. **서버+프론트만**(확장 무변경·ZIP 불필요). 기존 중앙경마 배당 이상감지(복승+쌍승)·지방경마 oddspark 전적·경륜 파이프라인 **무삭제**(JRA 馬柱 파서·엔드포인트·프론트 패널만 가산).
- **🏇 oddspark 전적 메인 소스 전환 — 지방경마(NAR) 출주표·전5경주(각질·거리변화·상3F) 수집 + 경륜 등급 스펙/joCode 교정**(`app.py _keiba_parse_shutsuba`·`_keiba_parse_horse_past`·`_keiba_corner_style`·`_keiba_last3f_note`·`_keiba_build_form`·`_keiba_fetch_details`·`/api/keiba/starters`·`_keirin_grade`·`KEIRIN_JO`·`/api/keirin/starters`, `static/index.html`·`static/js/app.js fetchKeibaStarters`·`renderKeibaStarters`): keiba.go.jp 대신 **oddspark를 메인 전적 소스로 전환**(사용자 요청). **[지방경마 출주표 전적]** `/api/keiba/starters`가 oddspark `RaceList.do`(出走表: 마번·마명·기수·부담중량·거리·마장상태) + 각 말의 `HorseDetail.do`(전5경주)를 **ThreadPool 병렬(동시 4)** fetch·파싱 → 착순·경마장·거리·마장·**통과순위(각질)**·**상3F(막판 스피드)**·기수·부담중량 추출. **각질 자동 판단**(`_keiba_corner_style`: 코너통과 앞→선행형+3·뒤→추격형+5), **거리변화**(직전 vs 현재: 단축+5), **부담중량 변화**(감소+5·기존 함수 재사용), **상3F 평균**(≤38.5초 막판스피드 우수) → 전적점수(최근5착순 가중평균+보정)·**사분위 상대등급 A/B/C/D**. raceKey 연동 시 `STARTERS_STORE`(source=oddspark) 저장 → 기존 `_form_from_starters`→`_triple_analyze`로 **유력마·통합등급(전적+배당역배열) 자동 반영**. opTrackCd/sponsorCd는 기존 `_keiba_schedule`가 자동 탐색(소노다 51/26·카사마츠 42/20·**카와사키 등 자동**). 일본경마 탭에 `🏇 지방경마 전적 분석` 패널(경마장·경주·raceKey 입력 → 표) 추가. **[경륜 스펙 반영]** `_keirin_grade` 등급 임계 사용자 스펙으로 교정(**95+ A / 85~94 B / 75~84 C / <75 D** — 오다와라 5R 5번 99.85점=A 실검증), `KEIRIN_JO` **joCode 교정**(live fetch 검증: **36=오다와라·62=히로시마**, 기존 62=나고야 오류 수정) + title 미검출 시 joCode 폴백(기타 경륜장 자동 탐색), `/api/keirin/starters` 별칭 추가(기존 `/api/keirin/card` 보존). 검증: 소노다 1R 실데이터 12두 파싱(각질 선행/추격/평지·거리단축·상3F)·오다와라 경륜 등급·엔드포인트 2종 200·사분위 등급 분포·기존 4종 회귀(flow23·reversal29·report69·formula23) 전건 통과. app.py `import ast`·app.js `new Function` OK. **서버+프론트만**(확장 무변경·ZIP 불필요). 기존 경륜 카드·keiba 배당·전적 파이프라인 **무삭제**(NAR 출주표 파서·엔드포인트·프론트·경륜 스펙 교정만 가산).
- **🎯 아쉬운 미적중 복기·성장 로직 — 역배열 실질유력마 축 '받치기 복승' + 복승조합엇갈림 near-miss 학습**(`app.py _reversal_backing_bets`·`_pairing_miss_review`·`_triple_analyze`·`_apply_result_learning`·`_recompute_learning_stats`, `static/js/app.js renderReversalBacking`·`renderJapanReviewReport`): 히로시마 2경주(결과 7-4-1) 케이스 학습 — 역배열이 **7번을 실질 유력마**(고배당 3.8배인데 시장이 실질 승자로 미는 말)로 정확히 지목하고 유력마도 1·4·7로 다 맞췄으나, **복승 메인이 1+7이라 실제 복승 7+4(6.6배)를 놓친** 아쉬운 미적중을 근본 보완(사용자 요청). **[받치기 복승·재발 방지]** `_reversal_backing_bets`가 역배열 실질유력마(`inverse.invLead`)를 **복승 축**으로, 다른 모든 유력마와 **받치기 복승**을 소액(8%) 자동 편성(예: 7+1·**7+4**) → 실질유력마가 어느 유력마와 붙어 입상하든 복승 커버. `_triple_analyze` 반환 `reversalBacking`, 분석기 3개 뷰에 `🎯 복승 받치기 (역배열 실질유력)` 카드. 검증: 히로시마 재현 시 받치기 7+4 편성 → **복승 적중 판정 구 False → 신 True**. **[복승조합엇갈림 복기·성장]** `_pairing_miss_review`가 결과 입력 시 **유력마는 맞았으나 복승 상대 페어링만 어긋난** near-miss(복승 미적중+실제 1·2착 둘 다 유력마)를 판정 → 레코드 `pairing_miss`/`pairing_miss_detail`·복기 doc·통계(`pairing_miss.n`·받치기로 커버 가능했던 `would_cover`) 누적, 복기 UI에 `🎯 아쉬운 복승조합 엇갈림` 노트(실제 복승·받치기 커버 가능 여부). 검증: 받치기 없을 때 감지(wouldBackingCover=True)·받치기 편성 후 커버되어 near-miss 해소 단위 테스트 + 기존 5종 회귀(formula23·flow23·reversal29·report69·prerace18) 전건 통과. app.py `import ast`·app.js `new Function` OK. **서버+프론트만**(확장 무변경·ZIP 불필요). 기존 복승 메인/보조·삼복승·역배열·4착 near-miss·실패복기 **무삭제**(받치기 복승·복승조합엇갈림 복기·통계만 가산).
- **🏇 한국경마 PDF 분석 강화 — Vision 스키마 확장(pastRaces)으로 거리·각질·기수교체·부담중량 결정론 활성화 + 병렬화·해시캐싱·토큰비용 추적**(`app.py RACE_SHEET_SCHEMA`·`_do_extract_race`·`_running_style`·`distance_change_bonus`·`jockey_change_bonus`·`weight_change_bonus`·`_leader_jockeys`·`score_horses_raw`·`_korea_deterministic_overlay`·`_det_horse_note`·`_force_trifecta_from_must`·`_do_analyze`·`call_claude`·`_log_api_usage`·`_pdf_md5`·`_pdf_cache_*`·`_korea_run_job`·`/api/usage`, `static/js/app.js renderFormScores`·`renderFormScorePanel`·`renderKoreaReport`): 한국경마 PDF가 **최근 5경주 착순 가중평균 + 기수보너스만** 실제 작동하고 거리·부담중량·기수교체·2착패턴 코드는 데이터 미추출로 **사문화(dead)**돼 있던 것을 근본 해결(사용자 요청·조사 결과). **[1번 스키마 확장]** `RACE_SHEET_SCHEMA` 각 말에 **`pastRaces`**(직전~과거 5경주: `distance`·`condition`(양호/무거움/불량)·`jockey`·`weight`(부담중량)·`placing`(착순)·`position`(선행/중단/후방)) 배열 추가 + `_do_extract_race` Vision 프롬프트에 전적 상세 추출 지침 + max_tokens 8192→12288. **[2번 각질 추정]** `_running_style`이 직전 3경주 `position`으로 선행형/추격형/평지형 판정(포지션 미추출이면 '' 미상—레이팅·착순만으론 초반위치 추정 불가라 정직하게 보류). **[3번 거리 변화]** `distance_change_bonus`—단축=스피드 유리 **+5**, 연장=추격형 +3·선행형 -3·미상 적성체크. **[4번 기수 교체]** `jockey_change_bonus`—리딩기수(복승률 상위20% `_leader_jockeys`)로 교체 **+10**, 리딩→무명 강등 **-5**. **[5번 부담중량 변화]** `weight_change_bonus`—직전 대비 증가 **-5**·감소 **+5**(부담중량=핸디캡, 기존 마체중 special_flags와 별개). 4종 보너스를 `score_horses_raw`에 **가산만**(pastRaces 없으면 전부 0 → 기존 동작 그대로) + `distanceBonus`/`jockeyChangeBonus`/`weightBonus`/`runningStyle` 필드. **[6번 2착패턴 결정론 강제]** 기존 사문화된 `special_flags` 삼복승필수(당거리 2착 2회↑)가 pastRaces로 **자동 활성**되고, `_korea_deterministic_overlay`+`_force_trifecta_from_must`가 AI 추천에 없으면 그 말+결정론 총점 상위 2두로 **삼복승 조합을 결정론적으로 강제 편성**(`report.deterministic`·pattern2_horses 병합)—AI 프롬프트(`_det_horse_note`로 힌트 주입)뿐 아니라 코드로도 이중 보장. **[7번 API 비용 추적]** `call_claude`가 매 호출 `usage`(input/output tokens)를 `_log_api_usage`로 `data/api_usage.json`에 **일별 누적**(calls·input_tokens·output_tokens·모델단가 예상비용 USD·by_model), `GET /api/usage`(?days=N) 조회. **[추가 최적화]** ⓐ**병렬화**: `_korea_run_job` 페이지감지·경주분석 루프를 순차→`ThreadPoolExecutor`(동시 4)—경주 워커는 PyMuPDF 스레드 비안전 회피 위해 **워커별 독립 doc open**, 세션저장·prerace 파일저장(index.json R-M-W)은 메인 스레드 전용. ⓑ**PDF 해시 캐싱**: `korea_start`가 업로드 PDF **MD5**로 완료 세션(`data/korea_pdf_cache/<md5>.json`)을 대조—동일 PDF 재업로드 시 재분석 없이 **즉시 복원**(사전분석 개별파일도 복원). 프론트: 전적 점수표에 거리/기수교체/부담/각질 컬럼 + 한국 리포트에 `🔒 결정론 강제 편성` 박스 + `renderFormScores`가 pastRaces·부담중량 서버 전달(자동 활성화). 검증: 결정론 보너스 8종 단위(각질·거리단축+5·연장 선행/추격·리딩+10/강등-5·부담±5·2착패턴 삼복승필수·오버레이 강제[2,1,3]·토큰 $0.09) + 토큰로깅 격리 테스트 + 기존 7종 회귀(formula23·prerace18·report69·flow23·reversal29) 전건 통과. app.py `import ast`·app.js `new Function` OK. **서버+프론트만**(확장 무변경·ZIP 불필요). 기존 형태점수·기수보너스·코스적성·AI분석·사전분석·백업 **무삭제**(스키마 필드·결정론 보너스·오버레이·병렬화·캐싱·비용추적만 가산).
- **📊 누적 데이터 → 기준치(임계값) 자동 도출 시스템(수동 승인·자동 적용 금지)**(`app.py _threshold_candidates`·`_threshold_config_load/save`·`_ai_rows_for_thresholds`·`/api/thresholds/optimize·config·apply`, `_build_ai_training` `signal_hit`·`thresholds_used`, `static/js/app.js renderThresholdOptimize`): 데이터가 쌓이면 신호 기준치별 적중률을 계산해 최적값을 추천(사용자 요청). **[1번 완전 데이터]** `_build_ai_training`(기존 ai_training)에 **`signal_hit`**(초과급락·역배열·연속하락 대표말의 실제 입상 여부) + **`thresholds_used`**(경주 판정에 쓴 현재 기준치) 추가. **[2번 기준치 후보 자동계산]** `_threshold_candidates`가 누적 ai_training을 읽어 **초과급락(5/10/12/15/20%+)·역배열(0.95~0.70 미만)** 각 기준치의 발화수·입상 적중률 계산(기존 파일은 excess_drops+result로 폴백 역산). **[3번 검토 화면]** 통계 탭 `📊 기준치 최적화 분석` 카드 — 현재값 vs 데이터 추천값 + 근거(기준치별 적중률) + 상태(표본 50회 게이팅). **[4번 수동 승인]** 자동 적용 금지 — `[✅ 적용]` 버튼 클릭 시에만 `POST /api/thresholds/apply`로 `data/thresholds_config.json` 반영, **이전 기준치는 history에 자동 백업**(롤백 가능). **[5번 전후 비교]** 추천 카드에 `추천 기준치 적중률 vs 현재 적중률 (개선 +N%p)` 표시. **[6번 AI 포맷]** `tools/export_ai_data.py`(기존)가 feature(reversal_ratio·max_excess_drop 등)+target(quinella_hit) CSV 생성. 검증: 126경주 폴백 계산(초과급락 발화 13/6/4/2/1·표본 50 미만이라 "표본부족" 보류)+엔드포인트 3종+전체 7종 통과. **⚠ 자동 적용 금지·표본 50회+ 게이팅**(메시지 3 원칙 준수). 기존 ai_training·export 무삭제(signal_hit/thresholds_used·최적화 계층만 추가).
- **🧠 복기 학습 재설계 — 1회 실패로 공식 수정 금지·통계 유의성(50회+) 게이팅 + 패턴 신뢰도 대시보드**(`app.py _pattern_confidence`·`_pattern_status`·`PATTERN_SAMPLE_MIN/SIG`·`FAIL_RULE_THRESHOLD`·`/api/learning/pattern-confidence`, `static/js/app.js renderPatternConfidence`): 소수 실패로 기준을 바꾸던 위험 제거(사용자 요청 "통계적 유의성 확보 후에만 조정"). **[1번 복기는 기록만]** 결과 입력 시 `_failure_record`(기존)가 입상말 역추적·5유형 분류·메모를 기록하되 **공식/가중치는 수정 안 함**. **[2번 패턴 카운터]** `_pattern_confidence`가 강신호 8유형+저배당압축+고배당동반의 발생/적중/적중률을 통합, `_pattern_status`로 신뢰도 산출 — **표본 50회 미만 → 판단 보류(⚠️) / 50회+ 적중률 40%+ → 신뢰(✅) / 40% 미만 → 경고(🔴·수정 수동) / 100회+ → 통계 유의(소폭 자동조정 검토)**. **[3번 조정 기준 엄격화]** 실패 규칙 자동생성 임계 **3회 → 50회**(`FAIL_RULE_THRESHOLD=PATTERN_SAMPLE_MIN`)로 상향, 생성돼도 `manual_only`·`status`(관찰누적/검토후보)로 **수동 검토만**(자동 공식 변경 없음). **[4번 대시보드]** 통계 탭 `🧠 학습 현황` 카드 — 패턴별 적중률·표본·신뢰도 상태 + "표본 50회 미만은 참고만·공식 수정은 수동" 경고. 검증: 게이팅 단위 테스트(8회→표본부족·60회37%→경고·120회→유의)+엔드포인트+전체 7종 통과. **서버+프론트만**. 기존 실패복기·신호학습·가중치 **무삭제**(게이팅·신뢰도 뷰만 추가).
- **🔧 제거 로직 완화(과다 제거 방지) + 유력마 1마리 처리 + 패스 경주 손익 제외**(`app.py _elimination`·`_is_pass_race`·`_forced`, `static/js/app.js renderSingleFavorite`): 소노다 1경주처럼 1마리 빼고 전부 제거되던 문제 수정(사용자 요청). **[1번 유력마 1마리]** 제거 후 유력 후보가 1마리뿐이면 축+배당 낮은 2마리로 **최소 복승 자동 구성**(소액 10%) + `⚠️ 유력마 1마리 — 패스 또는 소액만 권장` 배너(`singleFavorite`). **[2번 제거 완화]** 확실 제거 = **전적 D등급(또는 전적없음)+배당 150배+·이변신호 없음**인 말만 → **전적 C등급 이상은 신호 없어도 후보 유지**(기존 `_elim_score`/verdict 점수는 참고로 보존, 최종 keep에만 완화 게이트 덧씌움·`softKept`/`softCut`/`formGrade`). **[3번 패스 처리]** `_is_pass_race`(추천 게이트=신호대기/패스형 또는 전적없음+신호없음) → 레코드 `passed`/`pass_correct`, **손익(profit_summary)·집계에서 제외**(추천 안 했으므로 미적중 아님). 검증: 8두 중 D+150배+ 2두만 제거·C등급 40배 유지·패스 판정 단위 테스트 + 전체 7종 통과. **서버+프론트만**. 기존 제거 점수·verdict·손익 로직 **무삭제**(완화 게이트·1마리 처리·패스 플래그만 추가).
- **💡 고배당 동반 패턴 참고 추천 + 추천 말 수 유연화(신호 강도별) + 동반 적중률 학습**(`app.py _recommend_flex`·`_high_odds_companion`·`_repr_odds_from_rec`·`_learn_high_odds_companion`·`_high_odds_companion_hitrate`·`/api/high-odds-companion/stats`, `static/js/app.js renderRecommendFlex`·`renderHighOddsCompanion`): 메인 추천(신호 기반)과 **별도의 참고 추천**으로 고배당 동반 패턴 제시(사용자 요청). **[1번 추천 말 수 유연화]** `_recommend_flex`가 강신호 유형 수(`strong_signals.count`)에 따라 추천 말 수 가이드 산출 — 신호 3개+ → 3~4두 / 2개 → 2~3두 / 1개 → 1~2두 / **0개 → 추천 보류**. `recommendFlex` 반환 + 분석기 상단 🎯 가이드 배지. **[2번 고배당 동반 참고]** `_high_odds_companion`이 고배당(7배+) 신호말(급락 ≤-30%·역배열) 감지 시 그 말과 **복승 배당 낮게 묶인 다른 고배당(7배+) 말**을 탐색(묶임배당 오름차순 상위 2두) → 참고 삼복승(유력마1+고배당신호말+동반고배당) 제시. 예: `6번(44배) 급락 → 5번(18배)·9번(32배) 동반 → 1+5+6 / 1+6+9`. **[3번 메인/참고 분리]** 메인 추천 헤더 `🎯 메인 추천 (신호 기반)`, 참고는 별도 `💡 참고 추천 (고배당 동반 패턴)` 점선 카드(강제 아님·배분 미포함). **[4번 적중률 학습]** `_learn_high_odds_companion`이 **고배당(7배+) 1착 경주만 분모**로 2·3착에 다른 고배당 포함 여부를 `data/high_odds_companion_stats.json`(gitignore·경주별 멱등)에 누적 → `_high_odds_companion_hitrate`(표본 10+ 신뢰), 참고 카드에 `고배당 1착 시 3착 내 고배당 포함 N%` 자동 표시, `GET /api/high-odds-companion/stats`. 검증: recommend_flex 4단계·companion(6번→5·9번, 1+5+6/1+6+9)·학습(고배당1착 2경주 중 동반 1건=50%·저배당1착 제외) 단위 테스트 + `_triple_analyze` 반환 통합 + 전체 테스트 7종 통과(reversal29·report69·formula23·flow23·strong44·prerace18·stats15). app.py `import ast`·app.js `new Function` OK. **서버+프론트만**(확장 무변경·ZIP 불필요). 기존 메인 추천·삼복승 편성·강신호·third_place_hunt **무삭제**(참고 추천·유연화 가이드·동반 학습만 추가).
- **🚨 카와사키 11경주 학습 — 막판 급락+역배열 동시 감지 말 삼복승 강제 편성(새 규칙) + 케이스 복기 상세화**(확장 v2.1.46, `app.py _forced_trifecta_insurance`·`_high_odds_case_save`·`_high_odds_cases_load`·`/api/high-odds-review?cases=1`, `static/js/app.js renderForcedTrifecta`·`renderHighOddsCases`, `chrome-extension/overlay.js`): 카와사키 11경주(2026-07-09·결과 6-1-5)에서 6번이 T-1분 `4+6 -33%` 급락+역배열 감지됐으나 **유력마 TOP3(1·8·5) 밖이라 미편성 → 1착 놓침** 케이스를 학습·규칙화(사용자 요청). **[1번 케이스 저장]** `data/high_odds_review/2026-07-09_카와사키_11경주_case.json`에 지정 필드(race·result·missed_horse·signal_type·why_missed·lesson·new_rule) + 복기 narration(추천근거·왜 5번 삼복승·왜 놓쳤나) 저장, `?cases=1` 조회·`POST body{case}` 저장 엔드포인트. **[2번 복기 상세화]** 통계 탭에 `🎓 놓친 케이스 학습 복기` 카드 — 추천 근거 상세·왜 5번을 삼복승에·왜 놓쳤나·✅새 규칙(코드 반영)·교훈을 블록별 표시. **[3번 새 규칙 자동 적용]** `_forced_trifecta_insurance`가 **T-2분 이내(마감 임박)·마감 전**에 강급락(≤-30%)과 역배열(쌍승역전 challenger·역배열 감지말)을 **동시**에 보인 말을 찾아 유력마 상위 2두와 묶은 삼복승 조합을 **순위 무관 강제 편성**(라벨 `삼복승 강제보험(막판급락+역배열)`·`forcedPick` 플래그·alloc 5%), `_triple_analyze` 반환 `forcedTrifecta`. 분석기 웹 3개 뷰 상단 `🚨 삼복승 강제보험` 배너(`renderForcedTrifecta`) + 오버레이 `renderStrongSignals`에 빨강 강조 박스. 검증: 카와사키 재현(6번 [1,6,8] 강제 편성)·마감후/T-5분/역배열X 미적용 단위 테스트 + 전체 테스트 통과(reversal29·report69·formula23·flow23·strong44·prerace18·stats15). app.py `import ast`·app.js `new Function`·overlay.js `node --check` OK + ZIP 재빌드(v2.1.46). ⚠ 확장 새로고침 필수. 기존 삼복승 편성·third_place_hunt·strong_signals·고배당복기 **무삭제**(강제보험 블록·케이스 저장/복기만 추가).
- **🧹 배당판 상단 카운트다운 바 숨김(불필요 UI 제거)**(확장 v2.1.45, `chrome-extension/timer.js`·`manifest.json`): 사설 배당판 페이지 최상단에 주입되던 카운트다운 바(⏱ 경주·발주시각·00:00·마감·📊분석기 열기·✕)를 사용자 요청으로 화면에서 제거("필요없는 기능"). **[방식]** 바 DOM은 남기되 `display:none` 처리 — `timer.js`가 겸하는 **핵심 헤드리스 기능은 유지**해야 하기 때문(분석기↔확장 메시지 중계=경주 새로고침·일괄 결과 등록 `FETCH_RESULT_HTML`·`FORCE_COLLECT`, 마감 임박 능동수집, 발주 카운트다운 내부계산·storage 동기화). 요소 참조·tick()·알림 비프는 정상 동작하고 화면 표시만 사라짐. 분석기 웹(127.0.0.1:8011)의 자체 마감 3단계 알림(`#closingAlert`)이 있어 배당판 배너는 중복. `node --check` OK + ZIP 재빌드(v2.1.45). ⚠ 확장 새로고침 필수. 중계·수집 로직 무삭제(바 표시만 숨김)
- **🎯 배당 3착 자동 발굴 — 저배당 압축 축 2두 + 고배당 3착 후보 → 삼복승 보험 자동 편성 + 유형별 3착 적중률 학습**(확장 v2.1.44, `app.py _third_place_hunt`·`_learn_third_place`·`_third_place_hitrate`·`/api/third-place/stats`, `chrome-extension/overlay.js renderCompression`): 저배당 압축(복승 축 2두)이 확정되면 삼복승 3착 자리를 **고배당 말에서 자동 발굴**(사용자 요청·카와사키 11경주 6번 1착 놓침 케이스 대응). **[1번 후보 선정 우선순위]** 축 2두 밖 말 중 — **①급락 감지 고배당(유형3·5)** ②역배열 감지 고배당(유형2·6+challenger) ③전적 우수(60+)+고배당 ④연속하락 고배당(유형1) + 이상감지말 폴백. **배당 7~50배(고배당) 우선**, 저배당(5배 미만)은 축 후보라 제외, 상위 2~3두 선정. **[2번 삼복승 자동 조합]** 저배당 축 2두 + 각 후보 → `1+3+7`·`1+3+9` 편성(추정배당 `_trio_est` 기하평균×2 채움). **[3번 후보 신뢰도]** 급락=🔴 높음 / 역배열=🔄 중 / 전적=📊 중 / 연속하락=📉 중(`conf`·`icon`·근거 문자열). **[4번 오버레이]** `🎯 삼복승 3착 후보` 순위별 표시(N번·배당·근거·신뢰) + `삼복승: 1+3+7 추정 25배`. **[5번 유형별 3착 적중률 학습]** 결과 입력 시 `_learn_third_place`가 후보별 신호 버킷(급락/역배열/전적우수/연속하락)의 입상(1~3착)·정확3착 여부를 `data/third_place_stats.json`(gitignore·경주별 멱등)에 누적 → `_third_place_hitrate`가 버킷별 적중률 집계, `GET /api/third-place/stats` 제공. `_triple_analyze` 반환 `thirdPlaceHunt{active,axis,mainCombo,candidates[],trios[]}`(마감 후 미적용), 분석 로그에 `third_place_hunt` 저장. 검증: `tests/run_strong_signals.py` 44/44(우선순위·저배당 제외·압축 미감지 비활성·삼복승 편성·유형별 3착 적중률 멱등) + 전체 회귀 통과. app.py `import ast`·overlay.js `node --check` OK + ZIP 재빌드(v2.1.44). ⚠ 확장 새로고침 필수. 기존 추천·삼복승 배분(≤18% 캡) **무간섭**(3착 발굴은 별도 보험 블록으로 표시·기존 alloc 불변). 무삭제(발굴·학습만 추가).
- **🎯 저배당 압축 패턴(축 패턴) 자동 감지 + 신호 결합 + 복승 적중률 학습**(확장 v2.1.43, `app.py _compression_pattern`·`_horse_repr_odds`·`_learn_compression`·`_compression_hitrate`·`/api/compression/stats`, `chrome-extension/overlay.js renderCompression`): 유력마 TOP3에 저배당 말이 몰릴 때(축 패턴)를 자동 감지해 복승 메인을 자신있게 추천(사용자 요청). **[1·2번 압축 감지·구간 기준]** 최종 유력마 TOP3의 대표배당(단승 우선·없으면 최저 복승)으로 **4배 이하 2두+ = 강력 압축**(예: 1번 3.2배·3번 3.8배 → 복승 1+3 자신있게) / **5배 이하 3두+ = 중간 압축** 판정. **[3번 신호 결합]** 강한 신호 8유형과 결합 — **압축 + 급락 계열(연속하락/마감급락/재급락/동시급락/이중수렴) → 최강**("복승 메인 강력 추천") / **압축 + 역배열 → 고배당 보험 추가**("복승 메인 + 삼복승 보험"). `_triple_analyze` 반환 `compressionPattern{detected,level,band,horses[],combo[2],reprs[],withDrop,withReversal,note}`, 오버레이 `🎯 저배당 압축 패턴` 박스(저배당 마킹·복승 메인·결합 note). **[4번 적중률 누적]** 결과 입력 시 `_learn_compression`이 압축 2두 복승 적중(둘 다 1·2착)·부분 적중(1두 입상)을 `data/compression_stats.json`(gitignore·경주별 멱등)에 누적 → `_compression_hitrate`가 강력/중간·급락결합별 적중률 집계, `GET /api/compression/stats` 제공. 분석 로그에 `compression_pattern` 저장. 검증: `tests/run_strong_signals.py` 34/34(압축 강력/중간·단승없을때 복승대표·급락/역배열 결합·복승 적중률 멱등 학습 포함) + 전체 회귀 통과. app.py `import ast`·overlay.js `node --check` OK + ZIP 재빌드(v2.1.43). ⚠ 확장 새로고침 필수. 기존 유력마·추천·오버레이 로직 **무삭제**(압축 파생 뷰·결합·학습만 추가).
- **🔴 강한 신호 8유형 자동 감지 + 오버레이 강조·막판 신호 보존 + 유형별 적중률 학습**(확장 v2.1.42, `app.py _strong_signals`·`_smart_money_combos`·`_learn_strong_signals`·`_signal_type_hitrates`·`/api/signal-types/stats`, `chrome-extension/overlay.js`): T-1분30초~마감 직전 강신호(예: 몬베츠 9경주 막판 3+9 -50% 급락 → 결과 3-9 적중)가 오버레이에서 **사라져 놓치던 문제** 해결(사용자 보고·로그 확인). **[1번 8유형 자동 감지]** 서버 `_strong_signals`가 기존 계산 신호(advanced·inverse·drops·history)만 재사용해 8유형 산출 — **유형1** 연속하락3회+ · **유형2** 역배열10%+ · **유형3** 마감직전 대급락(T-2분 30%+) · **유형4** 재급락(급락→반등→재급락) · **유형5** 복수조합 동시급락2개+ · **유형6** 역배열+급락 동시(이중수렴) · **유형7** 환수율90%+집중 · **유형8** 상승후급락(스마트머니, `_smart_money_combos`가 배당 상승 후 마감 급락 궤적 감지). `_triple_analyze` 반환 `strongSignals{signals[],count,recommendLevel,dualConverge,types[]}`. **[2번 오버레이 강조]** 오버레이 `renderStrongSignals`/`appendStrongBox`가 라이브 강신호를 `🔴 강한 신호 감지!` 박스(유형·말·상세·강도)로 강조 표시. **[3번 강도별 추천 자동화]** 1개=보조·2개=복승·3개+=강력, **유형6 이중수렴은 단독으로도 강력**(`recommendLevel`). **[4번 막판 신호 보존]** T-2분 이내 감지 강신호를 오버레이 모듈 변수(`preservedSig`/`preservedRk`)에 보존 → **경주 종료·데이터 소멸 후에도 `📌 마감 전 주요 신호 (보존)` 박스로 유지**, **새 경주(raceKey 변경) 시에만 초기화**. **[5번 유형별 적중률 학습]** 결과 입력 시 `_learn_strong_signals`가 유형별 신호말 입상(1~3착) 적중률을 `data/signal_type_stats.json`(gitignore·경주별 멱등)에 누적 → `_signal_type_hitrates`가 유형별 발생/적중/적중률 집계, `_triple_analyze`가 각 신호에 학습 적중률(`hitRate`) 부착(가중치 상향 근거), `GET /api/signal-types/stats` 제공. 분석 로그에 `strong_signals` 저장. 검증: `tests/run_strong_signals.py` 24/24(8유형 감지·강도별 추천·이중수렴·멱등 학습) + 전체 회귀 6종 통과(flow23·formula23·reversal29·report69·prerace18·strong24·stats15). app.py `import ast`·overlay.js `node --check` OK + ZIP 재빌드(v2.1.42). ⚠ 확장 새로고침 필수. **오버레이 읽기 전용 원칙 유지**(div/span만·storage 읽기만). 기존 신호·오버레이·학습 로직 **무삭제**(파생 8유형 뷰·보존·유형별 학습만 추가).
- **🚨 이상감지 히스토리 경주별 분리 + 경주 로그 자동 저장**(`app.py _anomaly_events_from_doc`·`_build_analysis_log`, `static/js/app.js`): **[1]** 좌하단 이상감지 누적 패널이 **새 경주 전환 시 즉시 초기화**(`setAnomalyPanelRace`가 이전 이벤트 비움·`resetAnomalyPanel`을 `hardResetRaceState`에 연결) → 현재 경주 이상감지만 표시(헤더 "🚨 이상감지 (소노다 11경주)"). **[2]** anomaly-feed 파생 로직을 공용 헬퍼(`_anomaly_events_from_doc`·`_anomaly_combo`)로 추출 → `_build_analysis_log`가 경주별 `anomaly_history`(시간·조합·급락% 시계열)를 analysis_log에 자동 저장(중복제거·가짜급락 -88%↓ 제외·시간순). **[3]** 결과기록 복기(`renderJapanReview`)에 "🚨 이상감지 내역 (경주 전체)" 시계열 블록 추가(이전 경주 이상감지 확인). 기존 signals_detected·패널·anomaly-feed 무삭제. 단위+통합 테스트 통과.
- **🔁 배당 반등 패턴 분석 — 회복비율 분류 + 재급락 강신호**(`app.py _rebound_analyze`·`_advanced_anomaly`, `static/js/app.js`): 급락 후 반등을 정밀 분류 — **원배당 대비 회복비율** = (현재-최저)/(고점-최저). **20% 이내 반등 → 신호 유효(valid, 자금 유지)** / **80%+ 회복 → 페이크(자금 이탈)** / **급락→반등→재급락(recrash) → 더 강한 신호**. 신호 반영: recrash +20·valid +8·fake −12(기존 streaks/fakes 와 별개 보정). `signalQuality.advanced.rebounds` 반환 + 신호품질 카드에 회복%·궤적(고점→최저→현재) 표시. 기존 단발후반등·페이크·연속하락 로직 무삭제. 단위 6/6 통과(유효·페이크·재급락·부분·급락없음).

### 보완점
- **🛡️ 병렬 Vision 호출 rate-limit 재시도 백오프 + `/api/score` 기수교체 보너스 완성**(`app.py _messages_create_retry`·`_api_should_retry`·`call_claude`·`score`, `static/js/analysis.js scoreHorses`·`app.js renderFormScores`): 직전 배포한 두 기능의 보완(`#보완`). **[1번 재시도]** 한국 PDF 4워커 동시 Vision 호출이 429(rate limit)·529(과부하)·5xx·연결오류로 **재시도 없이 경주 분석이 조용히 실패**하던 위험 제거 — `call_claude`가 `_messages_create_retry`로 **지수 백오프(2·4·8·16초 + 지터·최대 4회)** 재시도(재시도 대상만: RateLimitError·429/500/502/503/529·APIConnectionError, 4xx 등 영구오류는 즉시 raise). 모든 Vision·텍스트 호출(추출·분석·감지·기수)이 자동 보호. **[2번 기수교체 배선]** 전적 자동점수 패널(`/api/score`)이 `jockeyStats` 미전달로 [4] 기수교체 보너스가 **0으로 죽어있던** 것 → 엔드포인트가 `jockeyStats`로 `_leader_jockeys`(리딩기수) 계산해 `compute_horse_scores`에 전달, 프론트 `renderFormScores`가 `state.jockeyStats` 전송 → 리딩기수 교체 +10·강등 -5 활성화. 검증: 재시도 판별(429→재시도·400→즉시)·2회 429 후 성공·`/api/score` 리딩기수 교체 +10 단위 테스트 + 기존 5종 회귀 전건 통과. app.py `import ast`·app.js `new Function`·analysis.js `node --check` OK. **서버+프론트만**(확장 무변경·ZIP 불필요). 기존 호출·점수 로직 **무삭제**(재시도 래퍼·jockeyStats 파라미터만 가산).

### 수정된 버그
- **🐛 전적 과가중 근본 해결 — 저배당(5배↓) 시장유력마는 전적 미수집이어도 유력마 편입**(확장 v2.1.52, `app.py _elimination`·`_triple_analyze`(market_favorites), `static/js/app.js renderMarketFavorites`, `chrome-extension/overlay.js updatePanel`): 후쿠시마 2R 복기에서 드러난 **전적 과가중** 근본 수정 — 6번(복승 **4.9배** 시장 최강·전적 0)이 통합정렬(`combinedProb`=시장0.6+전적0.4)에서 전적 우수 아웃사이더(7번·복승 82배·전적 73.9)에 밀려 **유력마 제외·등급 C**로 떨어지던 문제. **[전적 하한]** `_elimination`이 **단승/복승 대표배당 5배 이하 말**은 전적 미수집/저조라도 **전적 점수 최소 30점 보장**(`formFloored`·`marketFavorite` 플래그) → 제거 방지·등급 하락 방어("배당이 낮으면 시장이 유력하다고 봄"). **[유력마 보장]** `_triple_analyze`가 배당 5배 이하 시장유력마를 배당 낮은 순으로 **유력마에 편입**(기존 유력마 유지·총 5두 이내)·조합 편성 풀도 시장유력 우선 → 전적 0인 시장 최강마가 유력마·추천에서 절대 빠지지 않음. 반환 `marketFavorites`(no·odds·formMissing). 분석기 웹 유력마 카드/한국 통합 뷰 + 오버레이에 **`📊 시장 유력 (전적 미수집) · 배당 4.9배 → 유력마 편입`** 표시. 검증: 6번(복승 4.9·전적0) → formScore 30·marketFavorite·유지 단위 + 전체 회귀 222건 통과. app.py `import ast`·app.js `new Function`·overlay.js `node --check` OK + ZIP 재빌드(v2.1.52). ⚠ 확장 새로고침 필수. 기존 통합정렬·제거·유력마 산출 **무삭제**(저배당 전적 하한·시장유력마 편입·표시만 가산·5배 초과는 기존 동작 그대로).
- **🐛 제주 경마가 "기타"로 표시되고 실시간 배당 분석이 일본경마 탭에만 뜨던 문제 — 제주 경마장 인식 추가 + 한국 탭 실시간 배당 뷰 강화**(`app.py` venue enum·프롬프트, `static/js/app.js pollJapanOdds`·`renderKoreaIntegrated`): 제주(KRA 3경마장 중 하나)가 리스트에 **"기타경주"로 표시**되고, 제주 경주의 **실시간 배당 분석이 한국경마 탭이 아닌 일본경마 탭에서만** 보이던 문제 근본 수정(사용자 보고). **[근본 원인]** 페이지 감지 스키마의 venue enum이 `["서울","부산","기타",""]`로 **제주가 빠져 있어** Vision이 제주 경주를 '기타'로 밖에 못 냈고, 그 결과 한국 탭의 `triple_match`가 배당 raceKey(예:'제주 1경주')와 **경마장 불일치('기타'≠'제주')로 매칭 실패** → 한국 탭엔 안 뜨고, "가장 최근 수집 경주"를 종목 무관 표시하는 일본 탭 폴러(`pollJapanOdds`)가 대신 렌더. **[수정]** ①venue enum·추출 프롬프트 2곳에 **'제주' 추가**(서울경마→'서울'·부산경마→'부산'·**제주경마→'제주'**·그 외 '기타') → 리스트에 제주로 표시 + `triple_match`가 제주 배당을 한국 탭에 정상 매칭(`_area_num`·`_rk_venue_num`은 이미 제주 인식). ②`pollJapanOdds`가 **한국경마(서울/부산/부경/제주/과천 = `jpIsKoreaName`)를 일본 탭에서 렌더하지 않도록** 제외 → 같은 경주가 양 탭에 중복 표시되던 문제 해소(한국 경주는 한국 탭에서만). ③`renderKoreaIntegrated`(한국 탭 통합 뷰)에 일본 탭에만 있던 **실시간 표시(마감 N분전 배지·급락 경고 배너 `renderAlertSignal`·추천 근거 `renderRecommendBasis`)**를 추가해 한국경마 실시간 배당 분석을 **한 화면에서 편하게** 확인(기존 통합등급표·유력마·제거분석·신호·베팅추천은 그대로). app.py `import ast`·app.js `new Function` OK. **서버+프론트만**(확장 무변경·ZIP 불필요). 기존 한국 PDF 분석·통합분석·배당매칭·일본 탭 로직 **무삭제**(제주 인식·한국 탭 실시간 표시·일본 탭 한국 제외만 가산).
- **🐛 결과 자동수집 실패/성공 자동 알림 완전 제거(전역 배너·토스트·확장 Chrome 알림) — 결과기록 탭 입력 기능은 유지**(확장 v2.1.49, `static/js/app.js initResultAutoWatch`·`chrome-extension/background.js resultFail`): 위 `checkResultNotify` 제거에 이어 **남아있던 결과 자동수집 관련 자동 알림 3종**을 마저 제거(사용자 요청 "결과 입력하라는 팝업 불필요·불편"). **[1번 전역 실패 배너]** `initResultAutoWatch` tick()이 자동수집 실패(`state:'manual'`) 시 화면 상단에 표시하던 **`⚠️ N경주 자동수집 실패 → 수동입력` 전역 배너**(`#resultAutoFailBar`)를 항상 `display:none` 처리(표시 안 함). **[2번 성공 토스트]** 새 결과 자동수집 성공(`lastDone.seq` 증가) 시 뜨던 **`✅ … 결과 자동수집 반영됨` `toast`(=`alert()` 블로킹 모달)** 제거 → 결과기록 탭만 **조용히 자동 갱신**(`_refreshResultTab()` 유지). **[3번 확장 실패 Chrome 알림]** `background.js`가 T+10분까지 자동수집 실패 시 띄우던 **`❌ 결과 자동수집 실패 — 캡쳐로 입력하세요` Chrome 데스크톱 알림**(`_notify('resultFail', …)`) 제거(서버 상태 전송 `_postResultAutoStatus`는 결과기록 탭 대기 목록 정확성 위해 유지). **[유지]** 미입력·실패 경주 안내는 **결과기록 탭 `📋 결과 입력 대기` 목록에서만** 조용히 확인, **결과 입력 기능(대기 목록·클릭 팝업·캡쳐→판독·수동 입력폼)은 전부 보존**(안내용 자동 팝업/배너/알림만 제거). app.js `new Function`·background.js `node --check` OK + ZIP 재빌드(v2.1.49). ⚠ 확장 새로고침 필수. 기존 결과 자동수집·수동 입력·대기 목록·캡쳐 판독 **무삭제**(자동 표시만 제거).
- **🐛 경주 종료 후 '결과 입력하세요' 자동 팝업 제거(불편) — 결과기록 탭 입력 기능은 유지**(`static/js/app.js checkResultNotify`): 경기 끝나고 ~30분 경과하면 미입력 경주마다 **블로킹 `alert()` 팝업 + 브라우저 Notification**("🔔 ○○ 결과를 입력하세요")이 자동으로 떠 불편하던 문제 제거(사용자 요청). **[원인]** `checkResultNotify`가 마지막 수집 30분 경과(경주 종료 추정) 미입력 경주에 대해 `toast()`(=`alert()` 블로킹 모달)와 `Notification`을 자동 발생. **[수정]** `checkResultNotify`를 no-op으로 변경(호출부 `loadPendingResults` 호환 유지) → **자동 팝업/알림 미표시**. 미입력 경주 안내는 **결과기록 탭의 `📋 결과 입력 대기` 목록**(`loadPendingResults`·`#pendingResultsList`)에서만 표시하고, **결과 입력 기능(대기 목록·클릭 시 `openQuickResult` 팝업·결과기록 탭 입력폼)은 그대로 유지**. app.js `new Function` OK. **프론트 전용**(서버·확장 무변경·ZIP 불필요). 기존 결과 입력·대기 목록·수동 입력 팝업 **무삭제**(자동 표시만 제거).
- **🐛 한국경마 PDF 분석 후 배당 연동 통합영역이 빈칸 — 전적 기준 통합 초안 즉시 표시로 복구**(`static/js/app.js renderKoreaFormDraft`·`wireKoreaOdds`·`pollKoreaOdds`): PDF 분석 후 배당판 연동(유력마 TOP5·이상감지·조합)이 "없어진" 것처럼 보이던 문제 복구(사용자 보고). **[현황 점검]** 서버 라우트(`/api/korea/form`·`/api/odds/triple/match`·`/api/odds/triple/analyze`·`/api/korea/start·status·session·prerace·history/*`)·프론트 흐름(`renderKoreaRaceUI`→`renderReport`+`renderFormScores`+`wireKoreaOdds`→`pollKoreaOdds`→`renderKoreaIntegrated`)·컨테이너(`#koreaReport`·`#koreaOddsStatus`·`#koreaIntegrated`) 모두 **정상 존재**(삭제된 기능 없음). **[근본 원인]** 통합영역 `#koreaIntegrated`가 **배당이 수집·매칭되기 전까지 빈칸**이라(waiting/no_match 시 `innerHTML=''`), 배당판 미수집 상태에서 PDF 분석만 하면 유력마 TOP5·조합·이상감지 영역이 아무것도 안 보여 '기능이 사라진' 것으로 보임. **[복구]** `renderKoreaFormDraft`가 PDF 분석 직후 **전적 기준 통합 초안**(⭐유력마 TOP5 전적점수순·💰추천 조합·🎯2착패턴 + "배당 연동 시 자동 고도화" 안내)을 `#koreaIntegrated`에 즉시 렌더 → `wireKoreaOdds`가 빈칸 대신 초안 표시, `pollKoreaOdds`가 no_match 시 빈칸으로 지우는 대신 초안으로 복귀(외국 경주 잔존 방어는 유지 — 초안은 이 경주 PDF 전적만 사용). 배당 매칭되면 기존 `renderKoreaIntegrated`(전적40%+배당60% 통합등급·이상감지·역배열·조합)로 자동 고도화 → **PDF 분석 결과 + 배당 연동이 항상 한 화면에서 통합 표시**. app.js `new Function` OK. **프론트 전용**(서버·확장 무변경·ZIP 불필요). 기존 `renderKoreaIntegrated`·배당 매칭·이상감지·히스토리 **무삭제**(배당 전 초안 렌더만 추가).
- **🐛 경기 끝난 후 다음 경주로 자동 전환 안 됨 — 발주시각 미검출 사설배당판에서 전환 체인 미가동 근본 해결 + 자동전환 상태 배너**(확장 v2.1.48, `chrome-extension/background.js _armNextRaceChain`·`_onRaceClosed`·`_nextRaceAnalyze`·RACE_FINISHED 핸들러·`content.js _finishWatchdog`·`watchRaceChange`, `static/js/app.js initAutoStatusBar`): 경주가 끝나도 다음 경주 배당을 자동으로 못 끌어오던 문제 근본 수정(사용자 보고). **[근본 원인]** 기존 자동전환 3단계(T+3분 배당판 새로고침 → T+3분30초 재수집 → T+4분 분석)가 **전부 `cfg.timerDeadline`(발주시각) 의존**(`syncAutoEngine`의 `if (cfg.timerDeadline && ...)` 게이트) → **사설 모의배당판은 표시 시계가 가짜라 발주시각이 "남은시간" 카운트다운으로만 잡히는데, 경주가 끝나 카운트다운이 사라지면 발주시각 미검출 → 전환 체인이 아예 안 걸림**(끝난 경주에 고착). `autoTick`의 마감 감지(`left`)도 deadline 없으면 null이라 마감을 못 잡음. **[수정·발주시각 독립 전환]** ①`content.js _finishWatchdog`(10초 주기)가 **카운트다운(남은시간)이 있었다가 소멸=경주 마감**을 감지해 `RACE_FINISHED`를 background에 통지(경주당 1회). ②background `_armNextRaceChain`이 발주시각 없이도 **지금 시각 +12초 새로고침 → +42초 재수집 → +72초 분석** 체인을 가동(같은 raceKey 1회 `nextChainRk` 가드·이미 T+3 체인 예약 시 스킵). ③`_onRaceClosed` 무변동(추정) 소프트마감에서도 동일 체인 가동(무변동=마감 신호). 진짜 진행중 경주면 새로고침해도 같은 경주라 무해(수집 계속). ④`watchRaceChange` 감지 주기 15→10초 + 새 경주 감지 시 마감상태 초기화. **[4번 자동전환 상태 배너]** `initAutoStatusBar`가 `/api/auto/status`의 `nextRace`를 소비 → 상단 중앙에 **`🔄 다음 경주 대기 중 — {경주} · 예상 시작 HH:MM`**(waiting/refreshing/collecting)·**`✅ {새 경주} 자동 전환됨`**(done·12초 노출)·**`⚠ 배당판 탭 없음`**(no-tab) 배너 표시. `_nextRaceAnalyze`가 전환 완료 시 `nextRace:'done'`+`newRaceKey`를 서버 상태로 전송. `node --check`(background·content)·app.js `new Function` OK + ZIP 재빌드(v2.1.48). ⚠ 확장 새로고침 필수. 기존 발주시각 기반 T+3 체인·`watchRaceChange`·소프트마감 재개·결과 자동수집(resFetch) **무삭제**(발주시각 독립 전환 경로·마감 감지·상태 배너만 추가).
- **🐛 경기 초반 저배당 무조건 추천 근본 해결 — 신호 게이트 서버단 소스 차단 + 90초 관찰 의무화**(`app.py _triple_analyze`·`_bet_judgment`, `static/js/app.js renderBetRecommend`): 배당 수집 시작하자마자 신호가 없어도 **저배당(인기) 순으로 추천이 자동 생성**되던 근본 문제 수정(사용자 보고). **[원인]** `_bet_judgment`에 이미 2-of-4 시장신호 게이트(초과급락10%+·쌍승역전·연속하락2회+·환수율이상)와 `wait` 상태가 있었으나, ①실제 추천 리스트(`betRecommend`/`trioRecommend`/`mainBet`/`bmed`/`massDropStrategy`)는 `key_horses`(배당 인기순)에서 **무조건 생성**돼 비워지지 않음 → 프론트가 wait에서 숨겨도 오버레이·다른 뷰가 계속 소비 ②`signal_ready`가 스냅샷 3건만 쌓이면 True(마감임박 5~15초 간격에선 **90초 미만**에도 조기 추천). **[수정]** ①`signal_ready`에 **90초 관찰 하한** 추가 — `observed_sec = now - hist[0].t` ≥ 90(타임스탬프 없으면 `len(hist)≥4` 폴백) → 첫 수집 후 90초간은 관찰만·추천 보류 ②게이트 상태(`race_judgment.type ∈ {wait, 패스형}` AND 마감 전)일 때 반환 직전 **소스에서 `bet_rec`/`trio_rec`/`main_bet`/`bmed`/`mass_drop_strategy`를 전부 비움** + `recommendGated` 플래그 반환 → 신호 없으면 어떤 뷰에서도 저배당 추천이 안 나옴 ③프론트 `renderBetRecommend`가 `recommendGated`(또는 wait)면 **"⏳ 신호 대기 — 추천 보류"** 안내만 표시. **무삭제 원칙**: 마감 후·신호충족 경주는 기존 추천 로직 그대로(게이트는 마감 전·신호 미충족에만). 검증: 신호 없는 안정배당 3틱 경주 → `wait`·gated·betRecommend 0건·mainBet/bmed None 확인, 신호충족(3스냅샷+연속하락) 경주 → `신중형`·삼복승 편성 정상(run_report 삼복승 편성 검증 통과). 테스트 6종 통과(flow 23·formula 23·reversal 29·report 69·prerace 18·stats 15). app.py `import ast`·app.js `new Function` OK. **서버+프론트만**(확장 무변경·ZIP 불필요). 기존 게이트·판정·전적통합 정렬 무삭제(소스 비움·90초 하한·프론트 안내만 추가)
- **🐛 역배열 오탐(0.1배 차이도 감지) — 배당 차이 10% 최소 + 4단계 강도**(`app.py _reversal_level`·`_inversion_tier`·`_rank_inversion_detail`·`_reversal_strength_score`, `static/js/app.js`·`overlay.js`): 미세 배당차(0.1배)도 역배열로 잡아 오탐이 많던 문제 수정(사용자 보고). **[수정]** ①`_rank_inversion_detail`이 순위 역전(2단계+)에 더해 **배당 차이 10%+ 요구**(인기 낮은데 배당 싼 말이 인기 높은데 배당 비싼 말보다 얼마나 싼지 %) → 10% 미만은 역배열 아님(오탐 제거) ②`_reversal_level` 4단계 강도로 강화: **10~20% 🟡 약한 / 20~35% 🟠 / 35~50% 🔴 강한 / 50%+ 🔴🔴 압도적**(기존 5%=0.95 컷 → 10%=0.90 컷) ③`_reversal_strength_score` 0점 기준 0.95→0.90. 프론트/오버레이 역배열 카드에 등급·배당차% 배지 표시. 검증: `_reversal_level`/`_inversion_tier` 티어 테스트 + run_reversal 29개(테스트도 새 스펙 반영) + 전체 177개 통과. 기존 역전 판정·복승 폴백 무삭제(강도 티어·차이 필터만 추가).
- **🐛 종목 오탐(카와사키=경마인데 경정 표시) + 오버레이 베팅 정보 강화**(확장 v2.1.40, `chrome-extension/content.js detectSport`·`overlay.js`): ①사설 배당판(asyukk) 네비 메뉴의 '경정/경륜' 링크가 `body.innerText`에 잡혀 **경마 경주(카와사키·소노다 등)를 경정으로 오판정**하던 문제 + ②오버레이 베팅 정보 부족(사용자 요청). **[1번 종목 감지]** 일본 경마장명(지방 NAR+중앙 JRA·한글/한자 병기) `HORSE_TRACKS` 도입 → `detectSport(raceKey)`가 **raceKey에 경마장명 있으면 무조건 경마(null)**, 경정/경륜은 **URL·탭 제목에 명시적일 때만** 확정(네비 메뉴 단어 오탐 차단), 본문은 명확한 종목어(競艇/競輪 등)만 보조 판정하되 경마장명이 본문에 있으면 경마. 검증 7/7(카와사키·소노다·모리오카+경정링크→경마 / 실제 경정·경륜 명시→정상 감지). **[2번 마번 필터]** 오버레이 `topGrades`·유력마·단승이 서버 `validHorses`(배당 등장 마번)로 잔존마 제외(6두면 1~6번만). **[3번 오버레이 강화]** ⓐ**단승 인기순위 top3**("1위 4번 (3.2배)"·`singleRanking`+`single`) ⓑ**환수율+자금집중도**(`advanced.overround` 역수합→환수율%·상위3조합 90%+ "특정 조합 집중!") ⓒ**쌍승 역배열 요약**("🔄 역배열: N번 주목"·`inverse.invLead`) ⓓ**마감 임박 강화**(T-2분 주황·T-1분 빨강·T-30초 깜빡임 `startCdBlink`). 오버레이 **읽기 전용 원칙 유지**(storage 읽기만·div/span만). `node --check` OK + ZIP 재빌드(v2.1.40). ⚠ 확장 새로고침 필수. 기존 종목 감지·오버레이 표시 로직 **무삭제**(경마장명 우선판정·명시조건·정보 카드만 추가).
- **🐛 전적 잔존마 표시(6두 경주인데 7·10번 이전 경주 말) — 배당 마번 필터 + starters 정리**(`app.py _form_from_starters`·`_triple_analyze`·`_starters_prune`·`/api/starters/reset`, `static/js/app.js renderTopHorses`·`keibaDetectCurrentRace`): 카와사키 6경주(6마리)인데 유력마 TOP5에 **7번·10번 이전 경주 말**이 표시되던 문제(사용자 보고). **[원인]** `starters_store.json`이 raceKey별로 무한 누적(라이브 정리 시 64건 잔존 확인)되고, 잘못/이전 경주 전적이 같은 raceKey에 들어가면 배당엔 없는 마번(7·10)이 form→TOP5로 새어나옴(drops·ranked는 배당 기반이라 이미 정상, 잔존은 오직 전적 경로). **[수정·3중 방어]** **[1]** `_triple_analyze`가 현재 수집된 배당(복승 curQ·쌍승 curD·단승 curWin)에 실제 등장하는 마번 집합 `valid_nos`를 산출 → `_form_from_starters(valid_nos)`가 그 밖 마번을 전적에서 자동 제외(매칭 전무 시 원본 보존·배당 미수집 시 필터 보류 안전가드), `key_horses`도 배당 마번만. **[2]** 프론트 `renderTopHorses`가 서버 반환 `validHorses`로 TOP5에서 배당 없는 잔존마 재차 제외(이중 방어). **[3]** `_starters_prune(keep_rk)` + `POST /api/starters/reset`(triple_store 불간섭·전적만) → 경주 전환(oddspark 자동추종)·새 경주 시작 시 이전 경주 전적 삭제(현재 경주 + 한국 PDF 사전분석분은 보존). `/api/odds/triple/reset`도 `pruneStarters`(기본 on)로 함께 정리. **기존 전적/등급/오매칭차단 로직 무삭제**(필터·정리만 추가). 검증: 단위테스트(7·10 제외·미수집시 보류·전무시 보존·prune 유지범위) + 통합테스트(validHorses=1..6·form/keyHorses에서 7·10 제거) + 테스트 170개 통과 + `/api/starters/reset` 라이브(잔존 64건 정리). 서버+프론트만(확장 무변경·ZIP 불필요).
- **🐛 oddspark 배당이 현재 경주보다 늦게 나오고 이전 경주 잔존 — 현재 발매중 경주번호 자동감지 신설**(`app.py _keiba_current_raceno`·`/api/keiba/current`·`static/js/app.js keibaDetectCurrentRace`): 위 stale-루프 방어(끝난 경주 폴링 중단)에 더해, **oddspark에서 현재 발매중 경주번호를 직접 감지**하는 누락 조각을 보완(사용자 재보고 "배당이 현재 경주보다 늦게 나옴·이전 경주 데이터 잔존"). **[원인]** `/api/keiba/odds`가 raceKey의 경주번호(`_area_num`)만 사용 → 현재 발매중 경주를 알 방법이 없어 raceKey가 이전 번호에 고착되면 계속 이전 경주 배당 수집(①경주 전환 감지 없음 ②raceKey 자동 갱신 지연 ③이전 경주 캐시 잔존). **[수정]** ①`_keiba_current_raceno`가 oddspark `RaceList.do`(raceNb 미지정) 응답 `<title>`이 **현재 발매중 경주로 자동 포커스**되는 특성으로 현재 경주번호를 단일 요청 감지(라이브 검증 園田=11R·笠松=10R·15초 캐시) + `GET/POST /api/keiba/current`(경마장→현재번호·재구성 raceKey, 한국·JRA는 422 안전 제외) ②프론트 `keibaDetectCurrentRace`가 `closingTick`에서 감지→**전진 전환(번호↑) 시 raceKey 즉시 갱신 + `hardResetRaceState`로 이전 경주 타임라인·캐시 초기화 + 새 경주 배당 즉시 1회 수집**(경주 지정·역주행·블립 무시) ③수집 간격 마감 1분전 **2초**·3분전 3초·평상 15초, 감지주기 마감 3분내/대기중 8초·평상 25초. 기존 `_keiba_odds`·`_do_triple_ingest`·pin·stale 방어 **무삭제**. 테스트 170개 통과 + 엔드포인트 라이브 검증(전환 true/동일 false/한국 422). 서버+프론트만(확장 무변경·ZIP 불필요).
- **🐛 oddspark 수집이 끝난 경주에 고착돼 다음 경주로 안 넘어감·정보를 늦게 끌어옴(카사마츠 3R↔4R stale 루프)**(`static/js/app.js keibaOddsAutoPoll`·`_rrTimer`·`app.py current_race`): 배당판은 카사마츠 4경주인데 oddspark 수집은 **카사마츠 3경주에 멈춰** 정보를 늦게 끌어오던 문제 수정(사용자 보고 "너무 늦게 정보를 끌어온다"). **[원인]** oddspark가 **이미 끝난 경주(카사마츠 3R)의 확정배당을 계속 재수집** → 서버 triple_store에서 그 경주의 `t`(타임스탬프)가 계속 갱신돼 `current_race`의 `max-t`가 **영원히 끝난 경주를 '현재'로 반환** → 폴링 대상이 고착되는 **자기강화 stale 루프**(확장이 4R을 수집해도 oddspark의 3R 타임스탬프가 더 최신이라 밀림). **[수정]** ①클라 `keibaOddsAutoPoll`: 마감 90초+ 지난(끝난) 경주는 폴링 중단(`left < -90000`, pin 지정 시 예외) → 끝난 경주 재수집 정지 → 그 경주 `t` 동결 → 확장의 다음 경주가 최신이 됨. ②서버 `current_race` backstop: `max-t` 경주와 **같은 경마장**에서 최근(10분내) 갱신된 **더 높은 경주번호**가 있으면 그쪽을 현재 경주로 우선(경주는 번호순 진행·다른 경마장 동시개최는 불간섭, `_area_num` 토큰 일치 시만). ③반응성: 경주 자동전환 감지 30초→**12초**, 평상 oddspark 폴링 30초→**15초**(마감 3분전 3초 유지). 검증: 3R이 최신 t여도 4R 선택·소노다 동시개최 불간섭(합성 테스트) + 테스트 6종 통과. **서버/프론트만**(확장 무변경·ZIP 불필요). 기존 pin(경주 지정)·수집 파이프라인 무삭제(중단 가드·backstop·간격 단축만 추가)
- **🐛 역배열 패널 '쌍승 배당' 방향 오독 + 복승값을 '쌍승'이라 오표기**(`app.py _rank_inversion_detail`·`static/js/app.js renderInverse`): 분석기 역배열 설명의 쌍승 배당이 실제 배당판과 틀리게 나오던 문제 수정(사용자 보고 "분석기와 실제 쌍승에 대한 설명이 틀리게 나온다"). **[원인1·방향]** `_rank_inversion_detail`이 각 말의 대표 쌍승을 **방향 무관 최저**로 뽑아, 예컨대 `4→3=5.7`(3번이 4번 뒤 **2着**)을 "3번 쌍승 5.7배 낮음 → 3번 실질 1착 유력"으로 오독(배당판은 4번이 1着 최강인데 "3번 유력"이라 설명이 어긋남). **[원인2·라벨]** 쌍승 데이터가 없을 때는 복승값을 쓰면서도 프론트는 무조건 "쌍승"이라 표기. **[수정]** ①쌍승 대표배당 = **1着 방향만**(`_best_first_place`: curD 키 `(1着,2着)`에서 `k[0]==말`인 조합의 최저) → 시장이 그 말을 **승자로 미는 정도**를 정확히 반영(3번은 이제 3→4=13.6배로 표기, 4번이 1着 최강 5.7로 잡혀 오탐 사라짐). ②프론트가 실제 산출원(`invSource.oddsSrc`=쌍승/복승)을 그대로 라벨링 + 쌍승일 때 "(1着)" 표식. 검증: 소노다 4R 실데이터로 4번=1着 최강 확인·합성 데이터로 3번 13.6(1着) 정정 + 테스트 6종 통과. **서버/프론트만**(확장 무변경). 기존 역배열 판정·복승 폴백 무삭제(1着 방향 산출·라벨 정확화만)
- **🐛 부진마 학습 오류 `'int' object has no attribute 'get'` — keyHorses·anomalyHorse 정수 타입 미대응**(`app.py _learn_upset`): 결과 입력 시 `[부진마학습] 실패: 'int' object has no attribute 'get'` 로그로 부진마 역전 학습이 스킵되던 문제 수정(사용자 보고). **[원인]** `_learn_upset`이 `an.get("keyHorses")`를 딕셔너리 리스트로 가정해 `h.get("no")` 호출했으나, 실제 keyHorses는 **정수 리스트**(`[2,9,1]`)·anomalyHorse도 **정수** → `.get()` 호출에서 AttributeError. **[수정]** 말번호를 int/str/dict 어느 형태든 안전 추출하는 `_hno` 타입 가드 추가(dict면 `.get("no")`·int/str이면 그 값·그 외 None), `key_nos`/`anom_no`/`single_nos` 모두 적용. 검증: 소노다 1경주 재입력 시 "부진마 입상 3건" 정상 처리(이전 오류)·int keyHorses/anomalyHorse 단위 테스트 OK + 테스트 6종 통과. try/except 방어는 유지(다른 예외 대비). **기존 부진마 학습 로직 무삭제**(타입 가드만 추가)
- **🐛 경마 복승 조합 수 오류(9두인데 20개만·정상 36개) + 쌍승 →N열 누락·방향 반전 — 경마 전용 매트릭스 파서 신설**(`app.py _keiba_matrix_rows`·`_keiba_horse_count`·`_keiba_parse_quinella`·`_keiba_parse_exacta`·`keiba_odds`): 경마 배당이 경륜 파서 재사용으로 잘못 읽혀 유력마·역배열·추천이 전부 어긋나던 근본 원인 수정(사용자 보고 "추천 말과 아래 내용이 틀림"·"9두인데 복승 20개"). **[원인]** oddspark 경마는 경륜과 표 구조가 **완전히 다름** — ①복승(馬連): 행이 `[axis, 배당, 상대차번호, 배당, 상대차번호…]`로 **상대차를 명시적 라벨**(경륜은 배당·순위 쌍) → 경륜 파서(위치 기반·other<axis)가 대부분 조합을 버려 20개(2+3+4+5+6)만 수집. ②쌍승(馬単): 그리드 **행=2着·열 위치=1着**이고 9두+는 8열 초과분이 **꼬리 블록**으로 이어짐 → 경륜 파서가 꼬리행을 (k→1)로 오독하고 →9열 통째 누락(64/72), 게다가 방향(1着/2着)이 반전. **[수정]** 경마 전용 파서 신설 — 복승은 상대차 라벨을 따라가는 **walking 파싱**(implicit 최소차번+라벨)으로 상삼각 전체 N(N-1)/2 복원, 쌍승은 **위치=1着·행=2着** + block 오프셋으로 꼬리 블록(8열 초과) 1着 복원, combo=[1着,2着]. 방향은 단승 압도적 인기마(2번 단승1.0)의 1着 조합이 최저(2→1=3.9 < 1→2=26.5)임을 **라이브 검증**해 확정. **[검증 시스템]** N두 → 예상 복승 N(N-1)/2·쌍승 N(N-1) 계산해 실제 파싱 수와 비교, 불일치 시 응답 `warning` + 로그 경고(파싱 누락 조기 발견). 라이브 검증(소노다 R1·9두): **복승 36/36·쌍승 72/72·경고 없음·방향 정확·→9열 포함**. `/api/keiba/odds`가 경마 전용 파서 사용하도록 전환. ⚠ **경륜 파서(`_keirin_parse_*`)는 그대로 보존**(경륜은 다른 구조·경륜 전용). 테스트 6종 통과(reversal 22·report 69·flow 23·formula 23·prerace 18·stats 15). app.py `import ast` OK. **기존 경륜 파서·파이프라인 무삭제**(경마 전용 파서·검증만 추가)
- **🐛 GET /api/keiba/odds → 404(405) — POST만 허용하던 라우트에 GET 추가**(`app.py keiba_odds`): 브라우저 주소창·간편 테스트로 `GET /api/keiba/odds` 호출 시 메서드 미허용으로 실패하던 문제 수정(사용자 보고). **[원인]** 라우트가 `methods=["POST"]`만 등록 → GET 요청 거부(실행 중 서버가 신규 라우트 미로드 시 404, 로드됐어도 GET은 405). **[수정]** `methods=["GET","POST"]`로 확장 + 핸들러가 **GET=쿼리스트링·POST=JSON body** 양쪽에서 파라미터를 읽도록(`request.args`/`request.json` 공용) → `GET /api/keiba/odds?raceKey=소노다%2011경주&raceDy=20260709` 동작. raceKey 없으면 usage 안내 문구 반환. 검증: GET 200(복승 34·쌍승 61 수집)·POST 200·무param 400(usage) + 발매전·마감후 가드 유지. app.py `import ast` OK. **프론트/확장 무변경**(서버 라우트 메서드만 확장). ⚠ 실행 중 서버는 `debug=True` 자동 리로드로 반영되나, 안 되면 서버 재시작 필요. **기존 POST 폴링 경로·수집 로직 무삭제**(GET 허용·쿼리 파싱만 추가)
- **🐛 일본 NAR 출마표 전적 수집 실패 — DebaTable 실제 호스트(www2) 미대응 + onclick URL 추출 취약**(확장 v2.1.38, `chrome-extension/manifest.json`·`content.js`): 지방경마(NAR) 출마표2 전적이 수집되지 않던 근본 원인 3가지 교정(사용자 확인 URL `https://www2.keiba.go.jp/KeibaWeb/TodayRaceInfo/DebaTable?k_raceDate=..&k_raceNo=..&k_babaCode=..&odds_flg=4`). **[원인1·핵심]** `host_permissions`에 `www.keiba.go.jp`만 있고 **`www2`가 없어** background `FETCH_URL`(CORS 우회 fetch)이 www2 DebaTable을 **권한 부족으로 차단**. **[원인2]** `buildDebaUrl`이 onclick에서 뽑은 원본 URL의 호스트를 버리고 상수 `DEBA_PATH`(www)로 **재조립 → www2였다면 잘못된 호스트로 fetch**. **[원인3]** onclick URL 추출 정규식이 `https?://`(절대 URL)만 매칭 → 상대경로·따옴표 내부 URL을 놓침. **[수정]** ①manifest `host_permissions`+content_scripts matches에 `https://www2.keiba.go.jp/*` 추가 ②`DEBA_PATH` 기본값 www→www2 ③`debaParamsFromUrl`이 keiba 원본 URL의 `base`(origin+path) 보존→`buildDebaUrl`이 실제 호스트로 fetch(www/www2 자동) ④출마표 버튼 스캔을 `input[value*="출마표"]` 우선 + 따옴표 안 URL(상대·절대·`&amp;`) 추출로 강화 ⑤`fetchDebaStarters` 동일출처 판정을 host **정확 일치**로 좁혀 www→www2 CORS 차단 방지(불일치 시 background 경유). 기존 `parseDebaTable`(馬番·마명·騎手·최근5착순)·인페이지 탭 폴백·한국/중앙 생략 로직 **무삭제**. 독립 검증 5/5 통과(www2/www/상대/이스케이프/제외) + `node --check` OK + ZIP 재빌드(v2.1.38). ⚠ 확장 새로고침 필수.
- **🐛 쌍승(마단) 방향 표기 혼동 해소 — 화살표를 명시적 "1착·2착"으로**(`static/js/app.js` `renderInverse`·쌍승 매트릭스): 사용자가 "쌍승 순서가 바뀌었다"고 느낀 문제 규명·표기 개선. **[검증]** 사용자 실측(5번1착·2번2착=5.1 / 2번1착·5번2착=4.8)을 배당표 위치에 대입하니 5.1=(행2,열5)·4.8=(행5,열2) → **열(위쪽 헤더)=1착, 행(왼쪽)=2착** 규칙이 확정되고, 이는 현재 코드(`content.js` 긴급수정·`combo[0]`=열=1착)와 **정확히 일치** → 계산은 맞음(역전비율 0.704도 옳음). 혼동 원인은 배당표를 "행→열"로 읽는 직관 vs 실제 "열=1착" 규칙의 표기 차이. **[표기 수정]** ①역배열 카드의 `6→2 (8.1) < 2→6 (11.5)`를 **`6번 1착·2번 2착 8.1배 < 2번 1착·5번 2착 11.5배 → 6번을 1착으로 더 밀어줌`**으로 명시화 ②쌍승 매트릭스 헤더 `행→열 순서`→**`행=1착·열=2착`**, 코너 `쌍승 ↓→`→**`1착↓ / 2착→`**, 셀 툴팁 `2→6`→**`2번 1착·6번 2착 = 8.1배`**. **계산·방향 로직은 일절 변경 없음**(사용자 선택: 표기만 수정). app.js `new Function` OK. 확장 변경 없음(ZIP 불필요)
- **🐛 베팅 추천 조합이 유력마 TOP5(전적+배당 통합)와 딴판이던 문제 — 배당 인기만 따르던 근간 정렬 교정**(`app.py _triple_analyze`): 화면 상단 **유력마 TOP5는 10·2·4·9·6**(전적+배당 통합)로 실제 입상마(10·4·6)를 잘 잡았는데, **맨 아래 베팅 추천은 2+3+10·1+7 등 TOP5에 없는 저배당 인기마**를 추천하던 불일치 수정(사용자 보고). **[원인]** 베팅 추천의 근간 `key_horses`가 **복승 조합 등장 빈도 + 단승 배당 순위 = 순수 배당 인기**로만 정해져, 전적 좋은 말(TOP5)이 배당이 높으면 추천에서 빠지고 전적 나쁜 저배당 인기마가 추천에 들어감. 반면 TOP5(`renderTopHorses`)는 `elimination`의 **통합점수(`combinedProb*1000+total+formScore/100`)** 로 정렬 → 두 기준이 갈림. **[수정]** 전적이 수집된 경우 `key_horses`를 **TOP5와 동일한 통합점수 정렬식**으로 재정렬(상위 3두) → 복승 메인/보조·삼복승 메인이 유력마 TOP5 상위 3두와 일치. 전적 미수집(사설·전적 없음)이면 기존 배당 인기 유지(회귀 없음). 검증: 전적 우수·배당 고배당 4번이 추천에 포함되고 전적 불량·배당 인기 3번이 빠짐(key_horses 10·2·4 = TOP5 top3) + 테스트 6종 통과. 서버 전용(확장 변경 없음·ZIP 불필요). 기존 배당 기반 로직은 전적 없을 때 폴백으로 보존(통합 정렬만 추가)
- **🐛 신호 대기인데 하단 베팅 추천표가 여전히 저배당 조합을 추천하던 문제 + 일본 통합뷰에 근거카드·역배열 미표시**(`static/js/app.js`): 상단은 `⏳ 신호 대기`인데 **맨 아래 🎯 베팅 추천표·혼전 조합은 기존처럼 저배당 조합을 그대로 표시**하던 불일치 수정(사용자 보고). **[원인]** 추천 게이트를 `_bet_judgment`(wait) 상태로만 만들고, 프론트에서 신호 대기 시 추천 조합 렌더를 숨기지 않음(요청 [1] "조건 미충족 시 추천 조합 표시 안 함" 누락). **[수정]** `renderBetRecommend`가 `a.raceJudgment.type==='wait'`이면 조합표 대신 **"⏳ 신호 대기 — 추천 보류"** 안내 표시, `renderChaotic`(혼전 고배당 조합)·`renderBMED` 호출부도 wait 시 숨김(복기·리포트 등 `raceJudgment` 없는 뷰는 영향 없음). **[보완]** 일본경마 통합뷰(`renderJapanIntegrated`)에 누락돼 있던 **역배열/전적우수·시장비인기(`renderInverse`)·추천 근거 카드(`renderRecommendBasis`)** 추가(직전 커밋의 두 기능이 이 뷰에 안 보이던 것 보완). app.js `new Function` OK. 확장 변경 없음(ZIP 불필요). 기존 렌더 로직 무삭제(신호 대기 시 조합 숨김 게이트 + 누락 카드 배선만 추가)
- **🐛 경주 변경이 자동 반영 안 되던 문제 — 자동수집이 이전 경주에 굳음(경주 자동추종)**(확장 v2.1.34, `chrome-extension/content.js`): 배당판이 다음 경주로 넘어가도 분석기에 자동 반영이 안 되고 **확장 "전체 자동 수집" 버튼을 눌러야만** 바뀌던 문제 수정(사용자 보고). **[원인]** 수집 경로(`collectTripleKeiba`·`collectTripleByTabs`)가 raceKey를 **저장된 값(override) 우선 → `extractRaceKey()` 폴백** 순으로 정해, `watchRaceChange`가 storage.raceKey 를 한번 채우면 그 뒤 자동 엔진이 **배당판보다 저장값을 우선**해 이전 경주를 계속 수집(수동 버튼은 그 타이밍에 fresh 값을 읽어 됐음). **[수정]** `_resolveRaceKey(reason, override)` 헬퍼 추가 — 자동 수집(`auto`·`race-change`·`bg`)은 **지금 배당판에 표시된 경주(`extractRaceKey`)를 우선**(저장값은 폴백), 수동(`manual`)·그 외는 기존대로 저장값 우선(명시 입력 존중·회귀 없음). 두 경우 모두 한쪽이 비면 다른 쪽으로 폴백(자동 감지 실패 시 수동값 사용). **[보강]** `watchRaceChange` 직접 수집 실패 시 `_lastWatchedRace` 를 비워 다음 tick 재시도 + 감지 주기 30초→15초 단축(자동수집 OFF 시 추종 반응성). `node --check` OK + ZIP 재빌드(v2.1.34). ⚠ 확장 새로고침 필수. 수동 수집 경로·전적/한국모드/종목감지 로직 무삭제(자동수집 raceKey 우선순위만 배당판 기준으로 교정)

### 수정된 버그
- **🐛 경륜에 한국경마 전적이 오매칭돼 '이상한 정보'(엉뚱한 전적110·A등급) 표시 → 종목 가드로 차단**(`app.py _form_from_starters`·`_triple_analyze`): 경륜(히로시마 2경주) 분석에 **한국 경주마 전적**(source=korea·마명 크루엘라/트리플제·formScore 110)이 raceKey 충돌로 들어가 유력마·통합등급이 엉뚱하게 나오던 문제(사용자 보고). **[원인]** `_form_from_starters`가 종목을 보지 않고 STARTERS_STORE의 전적을 그대로 사용 → 6명 종목(경륜)에 한국경마 전적이 오적용. **[수정]** `_form_from_starters(rk, drops, sport)`로 종목 인자 추가 → **sport가 cycle/boat/bike이고 저장 전적 source=korea면 무시**(전적없음이 정상). 경륜 전적은 oddspark 출마표 분석(source=keirin)으로만 채운다. 손상된 한국 전적(히로시마 2·3경주) 런타임 정리. 검증: 경륜 오매칭 전적 None 반환 확인·기존 한국경주 하위호환 유지 + 테스트 6종 통과. **프론트/확장 무변경**(서버 가드만). ⚠ 삼복승 실시간 배당: oddspark keirin 3連複은 단순 betType 미제공(축 선택 필요)·asyukk 삼복승 수집 불안정 → 현재 추정배당(`_trio_est`) 보험 유지(별도 과제). 복승(2車複=betType5)·쌍승(2車単=betType6)은 oddspark 직접조회 파서 확인됨(연동은 후속)
- **🐛 경륜/경정/바이크 뷰에 '왜 추천했는지' 근거 카드 누락 → 추가**(`static/js/app.js sportAnalysisHTML`): 경륜 분석기에서 유력마 TOP5·복승·삼복승 추천은 나오는데 **추천 이유가 안 보이던 문제**(사용자 보고). **[원인]** 일본경마 뷰(`renderJapanIntegrated`)에는 `📋 추천 근거` 카드(`renderRecommendBasis`)·패턴매칭(`renderPatternMatch`)이 있었으나, 6명 종목(경륜·경정·바이크)·중앙경마를 그리는 `sportAnalysisHTML`에는 **빠져 있었음**. 백엔드는 이미 모든 종목에 `recommendBasis`를 생성(`_triple_analyze` 반환 4335). **[수정]** `sportAnalysisHTML`에 `renderPatternMatch(a.patternMatch)` + `renderRecommendBasis(a.recommendBasis)` 추가 → 상위 추천마별 **말별 근거 카드**(배당 급락·쌍승역전·연속하락·확신도 사유) 표시. 경륜은 전적이 없어 `_recommend_basis`가 **배당·이상감지 기반 근거**를 채운다(예: 5번=급락 ▼22%·연속하락 2회·확신도 71·"초과급락 40점", 신호 없는 말=`뚜렷한 배당 신호 없음`). 검증: 경륜 대표 payload로 근거 카드 렌더(요소 8종 확인)·빈입력 방어 + 테스트 통과. app.js `new Function` OK. **프론트 전용**(백엔드·확장 변경 없음·ZIP 불필요) — 브라우저 새로고침만. **기존 렌더 무삭제**(근거 카드·패턴매칭 호출만 추가)

### 추가된 기능
- **🎯 oddspark 수집 경주 직접 지정(pin) — 경주 고착(다음 경주로 안 넘어감) 해소**(`static/index.html`·`static/js/app.js _keibaTargetRk`·keibaOddsAutoPoll): 서버 수집이 stale한 현재 경주(예: 소노다 1경주)를 계속 타깃해 다음 경주로 안 넘어가던 문제 대응(사용자 보고 "소노다 2경주인데 1경주로 불러옴"). oddspark 카드에 **🎯 경주 지정** 입력 추가 — 경주가 안 넘어가면 현재 경주(예: `소노다 2경주`)를 직접 입력·[지정]하면 그 경주를 폴링, [자동]으로 자동추종 복귀. `_keibaTargetRk()`가 pin>panelRk>activeRaceKey 순으로 타깃 결정. **프론트 전용**(확장 변경 없음·ZIP 불필요). **기존 자동추종 로직 무삭제**(pin 우선 분기만 추가)
- **🎯 마감 임박 동시급락말 복승 메인 승격 + 혼전 복승 박스 확대 + 결과 수정(멱등) — 카사마츠 1R 복기 반영**(`app.py _triple_analyze`·`_apply_result_learning`): 카사마츠 1경주 복기에서 발견한 "복승 놓침"을 3가지로 보완(사용자 요청). **[1 동시급락말 승격]** 마감 전, 같은 말이 **2개+ 복승 조합에서 동시 30%+ 급락**(자금 집중)하면 유력마 TOP3 최상위로 조기 승격 → 복승 메인에 자동 편성(`surgePromote` 반환). 롱샷 노이즈 방지 위해 **대표배당 20배 이하·최대 2두**만(급락 조합 수·급락폭 순). 카사마츠 3번(3+6 ▼61%·3+4 ▼41%·2+3 ▼48%)처럼 마감 직전 자금 폭주 말을 놓치지 않음. **[2 혼전 복승 박스]** `_chaotic_race` 감지 시 복승 메인 2두 고정 → **상위 3두 박스**(h2+h3 조합 추가 = `복승 박스(혼전)`)로 확대해 3두 3조합 전부 커버. **[3 결과 수정 멱등]** `_apply_result_learning`이 같은 경주 기존 레코드를 제거 후 추가 → **결과 재입력(정정) 시 이중집계 방지**(record-result 재호출로 착순 정정 가능). ⚠ 재입력은 현재 코드로 재분석하므로 코드 변경 후엔 추천 판정이 달라질 수 있음(착순만 바뀐 정정은 삼복승 세트 불변 → 직접 보정). 카사마츠 1경주 착순 **3-4-6→3-6-4 정정**(복승 정답 {3,6}·삼복승 {3,4,6} 적중·+890,000원 유지). 테스트 6종 통과(reversal 22·report 69·flow 23·formula 23·prerace 18·stats 15). app.py `import ast` OK. 확장 변경 없음. **기존 pre_reversal·chaotic·결과학습 로직 무삭제**(승격·박스·멱등 훅만 추가)
- **🔀 일본경마 탭 레이아웃 재배치 — 실시간 배당 분석(이상감지·유력마 TOP5) 상단·전적표 업로드 하단(보조)**(`static/index.html`): 배당 우선 워크플로에 맞게 탭 구성 재배치(사용자 요청). **[상단]** oddspark 수집 상태 → ⚡ 실시간 배당 분석(이상감지·역배열·유력마 TOP5·추천, `#jpIntegrated`)·베팅 예산 — 전적표 없이 배당만으로 동작. **[하단]** 📋 전적표 업로드(보조·선택, `#jpFormSlot`·`#jpAnalyzeBtn`·`#jpReport`) — 넣으면 통합등급(전적40+배당60)에 반영. 모든 요소 ID·JS 참조 보존(DOM 순서만 변경). **프론트 전용**(확장 변경 없음·ZIP 불필요). **기존 전적표 업로드·분석 기능 무삭제**(위치만 하단 이동)
- **🏇 oddspark 서버 수집 토글을 일본경마 탭 최상단으로 이동 + 3줄 상태 표시**(`static/index.html`·`static/js/app.js _renderKeibaStatus`): 직전 커밋에서 추가한 서버 수집 토글이 일본경마 탭 **중간(베팅 예산 아래)** 에 있어 잘 안 보이던 문제 수정(사용자 보고 "토글이 일본경마 탭에 없음"). **[위치]** 탭 최상단에 초록 강조 카드로 이동 — `🏇 oddspark 서버 수집 ON/OFF` 체크박스(18px) + [지금 1회 조회] 버튼 + 안내. **[상태 표시]** 요청 형식 3줄로 개선 — `✅ oddspark 수집 중` / `복승 N조합 · 쌍승 N조합` / `마지막 수집: HH:MM:SS`(24h). 발매 전·마감 후엔 사유(⏳), 꺼짐 시 안내 표시. `_renderKeibaStatus`가 상태(enabled·lastCounts·lastTime·lastRk)를 중앙 렌더. **[동작]** 토글 ON 시 즉시 1회 수집 + 이후 적응형 폴링(마감 3분전 3초·평상 30초), 설정 localStorage 유지, 한국 경주 제외. 백엔드(`/api/keiba/odds`)·수집 로직 무변경. app.js `new Function` OK. **프론트 전용**(확장 변경 없음·ZIP 불필요) — 브라우저 새로고침만. **기존 토글·수집 로직 무삭제**(위치·상태 렌더만 개선)
- **🏇 경마(지방) oddspark 서버 직접 수집 전환 + 역배열 감지 강화(Chrome 확장 없이·마감 3분전 3초 폴링)**(`app.py _keiba_odds_url`·`_keiba_schedule`·`_keiba_resolve_track`·`_keiba_odds_live`·`/api/keiba/odds`·`/api/keiba/schedule`·`_triple_analyze` recentFlip, `static/js/app.js keibaOddsAutoPoll`·`fetchKeibaOdds`·closingTick, `static/index.html`): 지방경마(NAR) 배당을 **oddspark에서 서버가 직접 조회**하도록 전환(사용자 요청 "oddspark 서버 직접 수집으로 전환·향상·수집 간격 단축"). 기존 확장 수집은 **폴백으로 보존**(무삭제). **[핵심 실측 정정]** oddspark 경마는 경륜과 URL 체계가 달라, 지정 스펙(joCode·복승=betType5·쌍승=6)을 그대로 쓰면 예전 "이상한 정보"가 재발함 → 실측으로 확정: URL은 `Odds.do?sponsorCd=&opTrackCd=&raceDy=&raceNb=&viewType=0&betType=`, **betType은 복승(馬連)=6·쌍승(馬単)=5(경륜과 반대)**. **[경마장 코드 런타임 매핑]** 하드코딩(오류 위험) 대신 `_keiba_schedule(ymd)`가 그날 oddspark 개최 스케줄에서 (경마장 한자명→opTrackCd,sponsorCd)를 실시간 추출·당일 캐시 → `_keiba_resolve_track`이 raceKey 경마장명(`_area_num`+`_track_norm` 한/일/영 별칭)으로 매칭(라이브 검증: 소노다→51/26·카사마츠→42/20 자동, 미개최는 None). **[파이프라인 재사용]** 검증된 매트릭스 파서(`_keirin_parse_quinella/exacta`)로 복승6·쌍승5를 파싱→`_do_triple_ingest(sport=horse,category=japan_local,source=oddspark)` 주입→`_triple_analyze`가 역배열·배당변화·이상감지 자동 계산. **[발매전·마감후 방어]** `_keiba_odds_live`가 소수부 배당 비율(30%+)로 라이브 여부 판정 → 마감 후 oddspark가 주는 마방리스트·환급표(정수 0.0/착순)를 파이프라인에 주입하지 않음(가짜값 오염 차단·검증 완료). **[프론트 폴링]** 일본경마 탭 `🏇 경마 oddspark 서버 수집` 토글 + [지금 1회 조회] → `closingTick`(1초)에서 마감까지 남은시간 기반 적응형 폴링(**마감 3분전 3초·평상 30초**)으로 `/api/keiba/odds` 호출, 확장 없이 동작·페이지 닫으면 정지·한국 경주 제외·설정 localStorage 유지. **[역배열 감지 강화]** ①방향 정확성 재검증 — oddspark 쌍승 [1착,2착]이 `_odds_map_dir`→`curD`→`_win_exacta_reversal`로 정확히 흐름(2→1 급락 시 favored=[2,1]·flipped 확정, end-to-end 검증). ②`recentFlip` 추가 — 기존 `flipped`(직전 3초 대비 단발 전환)에 더해 **보관 창의 가장 이른 스냅샷과도 방향 비교** → 마감 임박에 여러 틱에 걸쳐 서서히 뒤집힌 역전을 포착(단발 flip으로는 놓치던 것). 정렬을 flipped OR recentFlip 우선으로 → 마감 직전 역전이 [:10] 컷에 밀리지 않음. 프론트 이상감지 칩에 🔴(단발)·🟠누적(서서히) 구분 표시. **[경마장 코드 주의]** 지정하신 joCode(카와사키=21/54·후나바시=32·모리오카=22 등)는 oddspark keiba 체계가 아니고 하코다테·삿포로는 JRA(중앙)라 지방경마 미포함 → 런타임 매핑이 그날 실제 코드를 자동 사용(수동 지정 불필요). ⚠ **라이브 배당 매트릭스 최종 검증은 다음 개최일 발매 중 경주에서** 자동 완료(엔드포인트가 counts 반환·`_keiba_odds_live` 가드로 안전). 검증: 스케줄/해석/URL/가드/파이프라인/recentFlip end-to-end + 테스트 6종 통과(reversal 22·report 69·flow 23·formula 23·prerace 18·stats 15). app.py `import ast`·app.js `new Function` OK. **확장 변경 없음(ZIP 불필요)**. **기존 확장 수집·triple_ingest·역배열 로직 무삭제**(서버 수집 폴백 계층·recentFlip 필드만 추가)
- **💰 경륜 배당(복승·쌍승) oddspark 서버 직접조회 → 역배열·배당변화·이상감지 자동 반영(확장 불필요)**(`app.py _keirin_odds_url`·`_keirin_parse_quinella`·`_keirin_parse_exacta`·`_do_triple_ingest`·`/api/keirin/odds`, `static/js/app.js fetchKeirinOdds`, `static/index.html`): 경륜 분석기가 배당을 잘못 읽어 "이상한 정보"를 주고 삼복승도 못 읽던 문제를 근본 해소 — **oddspark 배당판(Odds.do)을 서버가 직접 fetch·파싱**해 깨끗한 배당을 파이프라인에 주입(사용자 요청 "경마에도 적용하면 편해질까 진행해보자"). **[파서]** `betType=5(2車複=복승)`·`betType=6(2車単=쌍승)` 매트릭스를 파싱 — 복승은 상삼각(행 축차 k·쌍 index j→조합{j+1,k}), 쌍승은 방향성(행=1착·쌍 index+1=2착), `0.0`·대각선(빈칸) 제외·중복 제거. oddspark 라이브 검증(joCode11 20260709 1R): **복승 20조합·쌍승 37조합·역배열 방향성 확인(1→2=134.2 vs 2→1=44.7)**. **[파이프라인 재사용]** 기존 `triple_ingest`의 코어를 `_do_triple_ingest(rk,q,x,tr,win,sport,category,source,deadline)`로 추출(확장 수집과 공용) → `/api/keirin/odds`가 파싱한 복승·쌍승을 `sport=cycle`로 주입 → `_triple_analyze`가 **역배열(쌍승 B→A/A→B)·배당변화(반복 호출 시 히스토리 누적)·이상감지**를 자동 계산. Flask 테스트클라이언트 end-to-end 검증(복승20·쌍승37 주입→역배열 10건 산출·source=oddspark 저장·정리). **[UI]** 출마표 분석 패널에 **💰 배당 조회** 버튼 + **30초 자동갱신(배당변화)** 토글 추가 — raceKey(비우면 현재 경주 자동)로 조회 후 `refreshCurrentRace`로 즉시 반영. **[경마 적용 검토]** oddspark 지방경마(NAR, `/keiba/`)도 서버 조회 가능(200·RaceList.do/Odds.do 동일 구조·로그인 불필요) → **경마도 확장 없이 배당 직접조회 가능함을 확인**(패턴 재사용 즉시 가능). 단 기존 keiba.go.jp 확장 수집이 이미 안정적이고 전적까지 제공하므로, 경마는 "확장 없이 서버만으로 운영" 필요 시 후속 배선 권장(현재는 검토·검증만). ⚠ **삼복승(3連複)은 oddspark가 단순 betType 미제공(축 선택 필요)** → 현재 추정배당(`_trio_est`) 보험 유지(별도 과제). 테스트 6종 통과(report 69·flow 23·formula 23·reversal 22·prerace 18·stats 15). app.py `import ast`·app.js `new Function`·content.js `node --check` OK. **확장 변경 없음(ZIP 불필요)**. **기존 triple_ingest·keirin_card·확장 수집 파이프라인 무삭제**(코어 추출은 동작 동일·oddspark 조회 계층만 추가)
- **🚴 경륜 출마표(競走得点) → live 배당 분석 통합 (전적 + 삼복승·역배열·배당변화)**(`app.py keirin_card`, `static/js/app.js fetchKeirinCard·renderKeirinCard`, `static/index.html`): 경륜 분석기가 배당만 보고 "전적없음"으로 나오던 것을 → **oddspark 출마표 전적을 live 분석에 통합**(사용자 요청). **[통합]** 기존 `keirin_card`(oddspark RaceList.do fetch·競走得点/각질/착순 파싱)에 `raceKey` 파라미터 추가 → 파싱된 전적(competitor 得点)을 `STARTERS_STORE[raceKey]`(source=keirin)에 저장. 이후 같은 raceKey로 배당(복승·쌍승·삼복승)이 수집되면 `_triple_analyze`가 `_form_from_starters`로 전적을 읽어 **유력마 등급·📋추천 근거(전적점수)·통합등급(전적40+배당역배열60)** 에 반영. 삼복승·쌍승역배열·배당변화 감지는 기존 확장 수집 파이프라인이 이미 제공 → 이제 **전적과 통합**됨. **[UI]** 출마표 분석 패널에 `live 배당 연동 raceKey` 입력(비우면 현재 경주 자동) + 연동 성공 배너 + 즉시 `refreshCurrentRace`로 전적 반영된 유력마 재표시. 검증: 경륜 전적 저장→`_triple_analyze` 반영 end-to-end(7번 A·110점, 근거카드 전적점수 110.0, 통합등급 생성) + 테스트 6종 통과. app.py `import ast`·app.js `new Function` OK. **프론트+서버**(확장 변경 없음·ZIP 불필요). **기존 keirin_card·전적 파이프라인 무삭제**(raceKey 연동 저장만 추가). ⚠ 남은 여지: oddspark 배당(Odds.do) 서버 직접 조회는 미구현(현재 배당은 확장 asyukk 수집) — betType 코드 확정 시 추가 가능
- **🏷️ 연속하락 패턴 태깅 추가 + 🚤 경정 삼복승 수집 차단**(확장 v2.1.39, `app.py _extract_patterns`·`chrome-extension/content.js`): 사용자 우선순위 4종 중 실제 미구현 갭 2건 구현. **[항목1 강화·연속하락 태그]** 결과 입력 시 자동 패턴 태깅(`_extract_patterns` → `record.patterns` → `pattern_stats` 적중률 집계)은 이미 급락30+/급락50+/쌍승역전/배당압축/복승불일치를 붙였으나 **연속하락 누락** → `advanced.horseStreaks` 재사용해 말별 배당 2회+ 연속하락(count≥2·level 🟡/🔴) 시 `연속하락` 태그 추가(단위검증: 태깅/미태깅/하위호환 3종). 이후 `_recompute_learning_stats`가 연속하락 적중률을 자동 집계(추가 배선 불필요). **[항목4·경정 삼복승 차단]** 사설 배당판 탭 수집(`collectTripleByTabs`)이 경정(boat)도 삼복승을 눌러 수집하던 것을 → `effSport==='boat'` 분기 추가로 **복승+쌍승만 수집**(삼복승 불안정 노이즈 방지). 한국·중앙경마 생략 로직과 동일 패턴. ⚠ **확장 코드 변경 → manifest v2.1.39 bump + ZIP 재빌드 완료, 브라우저 확장 새로고침 필수**(안 하면 구코드 실행). **[항목2·3 확인]** 패턴별 적중률 즉시 집계(`pattern_stats`)·keiba 전적 fetch(`fetchDebaStarters` 2회 재시도)는 **이미 구현됨** → 중복 작업 없이 현황 보고. 테스트 6종 통과(report 69·flow 23·formula 23·reversal 22·prerace 18·stats 15). app.py `import ast`·content.js `node --check` OK. **기존 태깅·수집 로직 무삭제**(연속하락 태그·경정 분기만 추가)
- **💎 고배당 미적중 심층 분석 + A/B/C 유형 + 개선 시뮬레이션**(`app.py` `_high_odds_review_one`·`_rr_classify`·`_high_odds_simulate`·`_high_odds_scan`·`_high_odds_stats`, `GET/POST /api/high-odds-review`, `static/js/app.js renderHighOddsReview`): 복승30배+/삼복승100배+ 고배당 경주를 골라 "왜 놓쳤나"를 심층 복기(사용자 요청). **[핵심 설계]** 실데이터가 `race_results/`(tracked)에 있고 `odds_history/`는 gitignore라 사무실 동기화분에 비어 있으므로, 기존 실패복기(`_timeline_signal_label`·5유형 로직)를 **race_results 기반으로 이식**(`_rr_horse_timeline`·`_rr_winning_odds`·`_rr_classify`) → odds_history 없이도 동작. **[1번]** `_high_odds_scan`이 결과 입력 경주를 저비용 프리필터(정답 복승 배당≥30)로 골라 분석·`data/high_odds_review/` 저장. **[2번]** 정답말(1·2·3착) 배당 타임라인 역추적 + 마감 전 신호 유무. **[3번]** 놓친 말 배당 변화 vs 우리 추천 대조. **[4번]** 5유형→A(구조불가:타이밍·전적오판)/B(개선가능:노이즈)/C(로직오류:신호미반영·페이크) 그룹핑. **[5번]** `_high_odds_simulate` 반사실("v2.1.37 절대급락폭 적용 시…" 등 catchable 판정). **[6번]** 유형별 내일 규칙 자동 생성. **[7번]** 누적 통계(총/적중/미적중·A/B/C 분포·개선 후 예상 추가 적중=B+C catchable). 통계 탭 `💎 고배당 미적중 심층 분석` 카드. 실검증(2026-07-08): **고배당 7경주 전 미적중, A 14%·C 86%, 개선 후 예상 추가 6경주(예상 적중률 85.7%)**; 카와사키 3경주(7-9-3, 복승 72.3배)=우리추천 3+7 미적중 → **"9번 마감 전 -44% 급락 신호 보유했으나 추천 제외"(유형C·catchable)** 정확 진단. Flask 테스트클라이언트 scan/stats/get/list 200 + 프론트 렌더 검증 + 테스트 6종 통과(report 69·flow 23·formula 23·reversal 22·prerace 18·stats 15). app.py `import ast`·app.js `new Function` OK. 확장 변경 없음. **기존 실패복기(`_classify_failure`·`failure_review.json`)·명예의전당 무삭제**(고배당 필터·A/B/C·시뮬 계층만 추가)
- **📔 일별 학습 일지 + 빅데이터 집계 + 22:00 자동 생성**(`app.py` `_daily_learning_generate`·`_daily_bigdata`·`_dl_reliability`·`_start_daily_learning_scheduler`, `GET/POST /api/daily-learning`, `static/js/app.js renderDailyLearning`): 하루 경마 학습을 `data/daily_learning/<날짜>.json` 에 일지로 축적(사용자 요청). **[1번 학습일지]** `results_summary`(총경주·적중·적중률·손익)는 기존 `_build_daily_summary` + 결과파일 `investment.profit` 재사용, `key_learnings`/`missed_opportunities`/`pattern_discoveries`/`system_improvements`/`tomorrow_focus`(정성)는 API `extra`/기존 파일 **병합 보존**(재생성해도 수동 기록 삭제 없음 — 삭제금지 원칙). `patterns_tagged`는 오늘 `analysis_log/` 의 `pattern_tags` 를 데이터 기반 유니크 수집. **[2번 빅데이터]** `bigdata` 에 경마장별·출전두수별·마감시간대별·경주등급별·주로/날씨별 `{n,hits,hit_rate}` 집계(`ai_training/` 스캔) — 현재 수집되는 track·horse_count·시간대는 실집계, 미수집 필드(grade·condition·weather)는 '미수집' 슬롯 유지(수집 배선 시 자동 집계). **[3번 대시보드]** 통계 탭 상단에 `📔 오늘 배운 것` 카드(성공/실패/새패턴/개선 카운트 + 내일 집중 리스트 + 누적 패턴 신뢰도: 마감급락·쌍승역전·전적유력마·추천종합 적중 N/N). **[4번 자동생성]** 매일 22:00(`DAILY_LEARNING_HOUR` 조정) 데몬이 오늘 집계→일지 생성→GitHub 백업(기존 `_start_periodic_backup` 데몬 패턴·`_run_data_git_backup` 재사용, `data/daily_learning` 백업 경로 추가). 실검증(2026-07-08): **31경주·16적중·51.6%·손익 +120,900원** 실집계 + 경마장별(소노다 66.7%·카사마츠 50%…) + 재생성 시 정성 5종 전건 보존 + Flask 테스트클라이언트 GET/POST/list 200 + 프론트 렌더 런타임 검증 + 테스트 6종 통과(report 69·flow 23·formula 23·reversal 22·prerace 18·stats 15). app.py `import ast`·app.js `new Function` OK. 확장 변경 없음(ZIP 불필요). **기존 daily_summary(수치 요약)·learning.json(통계) 무삭제**(그 위에 학습 일지 계층만 추가)
- **⏰ 한국 PDF 발주시각 자동 추출 → 마감 전 3단계 알림 자동 활성(수동 입력 불필요)**(`app.py _extract_korea_post_time`·`_korea_extract_race`·`_prerace_save_race`, `static/js/app.js openPreraceRace`): 배당판 없이 PDF 사전분석만 볼 때 그동안 `⏰ 발주시각(수동)` 을 직접 입력해야 마감 전 알림이 떴는데(v2.3.0 보완점 "남은 여지"), **PDF 텍스트 레이어에서 발주시각을 자동 추출**해 저장·자동 채움. **[추출]** 요약 페이지 텍스트에서 ①제목줄 `…경마 N경주 … (HH:MM)` 괄호 ②`발주`/`시각` 라벨 뒤 첫 `HH:MM` 을 24h·2자리로 정규화(Vision 추가 호출 없음·텍스트만). `_korea_extract_race`가 추출 즉시 `race['postTime']` 에 채우고 `_prerace_save_race` payload에 영구 저장 → `GET /api/korea/prerace/<key>` 로 노출. **[자동 활성]** 프론트 `openPreraceRace`가 로드 시 `d.postTime` 을 `#koreaDeadline` 에 자동 입력하고, **오늘 기준 아직 미래면** 기존 검증 흐름(`setKoreaManualDeadline`)으로 마감 전 3단계 알림을 자동 무장(이미 지난 경주는 값만 채우고 알림 생략). 실검증(`data/korea_last.pdf` 2026-07-05 서울·부산): **17경주 전건 발주시각 정확 추출**(10:35→11:00→11:25… 순차 증가 정합성 확인) + 빈/무관 텍스트 None 방어 + 테스트 6종 통과(report 69·flow 23·formula 23·reversal 22·prerace 18·stats 15). app.py `import ast`·app.js `new Function` OK. 확장 변경 없음(ZIP 불필요). **기존 수동 발주시각 입력·마감 알림 로직 무삭제**(자동 추출·채움만 추가). ⚠ 기존에 저장된 사전분석 파일은 postTime 없음 — 다음 PDF 분석부터 자동 부여
- **📸 결과 사진 첨부 → 자동 반영(웹 분석기)**(`static/index.html`·`static/js/app.js`): 경주결과 화면 **사진(스크린샷) 파일을 웹 분석기에 첨부하면 자동으로 판독·매칭·등록·학습**되게 함(사용자 요청). 결과기록 탭 `📋 일괄 결과 등록` 아래 **📸 결과 사진 첨부** 블록 추가 — 파일 선택(미리보기) + 정액 가정 + [사진 판독·자동 반영] 버튼. `FileReader`로 dataURL 변환 → **기존 `/api/result/ocr`(Vision 다중경주 판독 + `_register_result_rows` 자동 매칭·적중판정·학습) 재사용** → `renderBulkSummary`(타겟 셀렉터 인자 추가)로 요약 표시 + 통계/히스토리/대기목록 자동 갱신. **확장·로그인 불필요**(웹에서 직접 업로드). 실검증(사용자 7-8 결과표 사진): **38경주 판독 → 30경주 등록·적중 16·손익 +121,900원**(카와사키 7경주 7-5-6 복승·삼복승 적중, 소노다 10경주 삼복승 100배 포함), 미분석 초반 8경주는 unmatched 정상 분류. app.js `new Function` OK. 확장 변경 없음(ZIP 불필요). **기존 일괄등록(URL/HTML)·확장 캡처 OCR 경로 무삭제**(웹 파일첨부 경로만 추가)
- **⚡ raw 쌍승역전 → 마감 전 예비 유력마 조기 반영(카와사키 7 근본 해결)**(`app.py _triple_analyze`, `static/js/app.js renderPreReversal`): 카와사키 7경주에서 raw 쌍승역전(11↔7)은 T-3분에 감지됐으나 **정식 win-exacta 공식(단승 대비)은 단승 배당이 늦게 들어와 마감 후에야 7번 확정** → 정답(복승 5+7)을 마감 전에 못 잡던 문제 해결(사용자 요청). **[수정]** `wx_reversals`(복승 인기 기반이라 단승 불필요)의 **강한 역전(ratio<0.80) challenger(실질 1착 후보)** 를 **마감 전에 한해 `key_horses` 상위로 조기 승격**(`pre_reversal`) → 베팅 추천에 즉시 편성. **[아웃사이더 보호]** challenger 대표배당(단승 없으면 최저 복승)이 **15배 초과(아웃사이더)면 승격하지 않고** 기존 `reversalPick`(소액 삼복승 보험) 유지 → 삼복승 소액 배분(≤18%) 설계 보존. 마감 후(`after_close`)엔 미적용(기존 정책). 반환 `preReversal` + 프론트 보라색 배너("⚡ 쌍승역전 조기 감지 → 예비 유력마"). 격리 검증: 단승 5번 유력·쌍승 7→5 역전(0.795)·7=contender → **preReversal [7]·keyHorses 7 1순위 승격**(아웃사이더 11번은 미승격·reversalPick 유지) + 테스트 6종 통과(report 69·reversal 22·formula 23·flow 23·prerace 18·stats 15). 확장 변경 없음(ZIP 불필요). **기존 정식 역전공식·reversalPick·key_horses 로직 무삭제**(마감 전 contender 조기 승격만 추가)
- **🎯 소노다 케이스 근본 해결 2종: timer.js 능동 수집(SW 절전 무관) + 대규모 급락 시 절대급락폭 병행**(확장 v2.1.37, `chrome-extension/timer.js`·`background.js`·`app.py _excess_drop_analysis`): 소노다 11경주에서 ①실측 수집 간격이 40~67초로 늘고 ②대규모 급락 시 3번(-70%)이 누락된 근본 원인 2가지를 해결(사용자 요청). **[1번 timer.js 능동 수집]** MV3 서비스워커가 절전되면 background 알람(최소 30초)만으로 부활해 fine setInterval(5초)이 억제되던 문제 → **배당판 탭의 `timer.js`(페이지 1초 틱, 스로틀 없음)** 가 마감 임박(T-5분 이내)에 5단계 스케줄(T-30초3초·T-1·2분5초·T-3분10초·T-5분15초)대로 **직접 `FORCE_COLLECT` 발사**(SW를 깨워 수집) → 실측 간격 실제 보장. 배당판 호스트에서만 발동(분석기 탭 제외)·`autoSend` ON일 때만·`_lastForceCollect` 간격 준수. background `FORCE_COLLECT`에 `_collecting` 가드 추가(autoTick과 중복 수집 방지). **[2번 절대급락폭 병행]** `_excess_drop_analysis`가 대규모 급락 시 개별 조합 -70%가 시장평균(-58%)에 희석돼 **excess가 양수(+0.9)→노이즈로 누락**되던 3번을 방어 → **절대 단일급락폭(`maxDrop<=-50`, `absBig`)** 을 초과급락(상대)과 **병행**해 둘 중 하나라도 걸리면 🔴 집중신호로 채택 + 신호강도에 maxDrop 반영. 격리 검증(소노다 11R [4] 재현): 3번 excess +0.9·maxDrop -70 → **absBig 승격·signal_horse 1순위 채택**(기존엔 12번, 이제 3번). `absConcentrated`(절대급락폭으로만 잡힌 말) 필드 추가. 테스트 6종 통과(report 69·flow 23·formula 23·reversal 22·prerace 18·stats 15). timer.js·background.js `node --check` OK + ZIP 재빌드(v2.1.37). ⚠ 확장 새로고침 필수. **기존 수집·초과급락·ABS_STRONG 로직 무삭제**(능동 수집 트리거·절대급락폭 병행만 추가)
- **⚡ 마감 후 대급락 신호 처리 시스템 — 별도 감지·입상률 학습·다음경주 참고**(확장 v2.1.36, `app.py`·`static/js/app.js`): 발주(T-0) 후에도 배당을 수집·분석하는데, **마감 후 대급락(50%+)이 실제 정답인 경우**가 있어(카와사키 3경주: 15:44 마감 후 15:54 `3+7 -99.3%` 대급락 → 실제 3·7 입상) 이를 **추천엔 반영하지 않되 학습·다음경주 참고로 활용**(사용자 요청). **[1번 별도 감지]** `_history_append`가 마감 후 스냅샷에서 50%+ 급락 조합을 모아 대급락 말(`surge_horses`)·조합(`surge_combos`)을 `after_close_cases.json`에 별도 저장(`_record_after_close_case` 확장). `_triple_analyze`가 `afterCloseSurge`(대급락 말·조합·학습 입상률·신뢰여부) 반환 → 프론트 `renderAfterCloseSurge`가 **"⚡ 마감 후 대급락 감지!"** 주황 배너(추천 미반영·참고만 명시). **[3번 입상률 학습]** 결과 입력 시 `_after_close_learn_result`가 대급락 말의 실제 입상(1~3착) 판정 → `surge_hit` 갱신. `_after_close_stats`가 **"마감 후 대급락 → 실제 입상률 N%"**(표본·최근 케이스) 산출 → `GET /api/after-close/stats` + 통계 탭 `renderAfterCloseStats` 카드. **[4번 다음경주 활용]** 표본 5건+·입상률 50%+면 `reliable` 플래그 → 마감 후 대급락 배너에 "다음 경주 같은 패턴 발생 시 참고" 표시. 격리 검증(surge 저장→7-9-3 결과→3·7 입상 판정→입상률 50%) + 테스트 6종 통과(report 69·flow 23·formula 23·reversal 22·stats 15). **기존 after-close 케이스 저장·마감후 제외 로직 무삭제**(대급락 구조·입상률 학습·배너·통계만 추가). ⚠ 추천 판정(복승 top2·삼복승 top3) 불변 — 마감 후 신호는 여전히 추천 미반영
- **⏱️ 마감 임박 수집 간격 5단계 세분화(T-5분 15초·T-3분 10초·T-1·2분 5초·T-30초 3초)**(확장 v2.1.36, `chrome-extension/background.js`): 마감 직전 급락 신호를 놓치지 않게 수집 간격을 사용자 [2번] 스케줄로 세분화(기존 T-3분 10초·T-1분 5초 2단계 → 5단계). `autoTick` interval 로직: `left≤30s→3초 · ≤120s→5초 · ≤180s→10초 · ≤300s→15초 · 평상시 30초`. ⚠ **관찰된 한계**: 소노다 11경주에서 실측 수집 간격이 T-2분 구간 **67초**로 설정(10초)보다 훨씬 길었음 — **MV3 서비스워커 절전**(fine setInterval 억제, 알람 최소 주기 30초) 영향. `_ensureFineLoop`(autoTick 시작부에서 재무장·20초 keepalive)은 이미 존재하나 SW 하드 스로틀은 완전 극복 불가 → 간격 설정은 SW 활성 시 유효. background.js `node --check` OK + ZIP 재빌드(v2.1.36). ⚠ 확장 새로고침 필수. 기존 수집·단계알림 로직 무삭제(간격 임계값만 세분화)
- **🕓 추천 변경 이력 보존 — 덮어쓰지 않고 누적 저장(추천 변경 경위 추적)**(`app.py _build_analysis_log`, `static/js/app.js _recHistoryBlock`): 분석 로그가 매 분석(30초)마다 `final_recommendation`을 **통째로 덮어써** 이전 추천(예: 6+9→9+10→3+7)이 사라지던 문제 해결(사용자 요청·카와사키 3경주 복기에서 발견). **[원인]** `_build_analysis_log`가 기존 `doc`을 읽어도 `analyzed_at`·`profit`·`review`만 보존하고 **추천 조합·유력마는 항상 최신으로 교체** → odds_history 스냅샷(배당·signal_horse)은 누적되지만 **추천 자체는 이력이 없어** 변경 경위 추적 불가. **[수정]** 로그에 **`recommendation_history`** 배열 추가 — 조합/유력마 시그니처(`정렬 keyHorses|복승메인|복승보조|삼복승메인`)가 **바뀔 때만** `{time, minutes_before, keyHorses, summary, quinella_main·sub, trifecta_main, top_signals}` append(같은 추천 반복은 무시, 빈 추천/워밍업 제외, 최근 50회 상한). 기존 `doc.recommendation_history`를 읽어 누적하므로 **덮어쓰지 않음**. **[표시]** 복기 화면(`renderJapanReview`·`renderAnalysisDetail`) 경기 전 패널에 **🕓 추천 변경 이력** 블록(시각·T-분·복승/삼복승 조합·유력마·유발 신호, 최종 강조). 2회 이상 변경 시에만 노출. app.py `ast`+app.js `new Function` OK + `run_report` 69/0. 확장 변경 없음(ZIP 불필요). **기존 final_recommendation·로그 저장 로직 무삭제**(이력 배열·표시 블록만 추가). ⚠ 과거 경주는 이력이 비어 있고, 신규/재분석부터 누적됨
- **📑 복기 시각화: 경기 전 분석 vs 경기 후 분석 2단 분리 + 예측↔실제 대조 (전 종목·한국 전면 적용)**(`static/js/app.js` `renderJapanReview`·`renderAnalysisDetail`·`renderRaceReport`·신규 `_reviewCompareBlock`·`_reportCompareStrip`): 모든 복기 화면을 **복기 자료용으로 전후 분리 시각화**(사용자 요청 "모든 경기 한국에도"). **[적용 범위]** ①**일본경마 복기**(`renderJapanReview`) ②**전 종목 통합 + 한국 복기**(`renderAnalysisDetail` — 결과기록 탭 `🔎 분석기록` 서브탭의 경마 지방/중앙·경륜·경정·바이크·**한국** 전부 + 경정/경륜/바이크/중앙 스포츠 탭 기록) ③**고배당 재현 리포트**(`renderRaceReport`). **[2단 패널]** 세로로 쌓여 있던 유력마/이상감지/추천조합과 결과/복기를 **🔮 경기 전 분석(파랑·배당·전적 기반 예측)** 과 **🏁 경기 후 분석(초록·실제 결과·복기)** 두 패널로 좌우 분리(반응형 flex·모바일 세로 스택). 경기 후 패널은 결과 미입력 시 안내 문구, 입력 시 결과·적중·손익·복기 표시. **[예측↔실제 대조 블록]** 상단에 **🔍 예측 vs 실제** 대조 카드 — ①경기 전 유력마/추천마별 실제 입상 여부(✅입상/✗) + 적중률(N/M두, %) ②실제 1·2·3착마를 예측 분류(유력마 예측✅ / 제거마였음⚠️ / 미분류 놓침) ③📌 총평. 순수 파생(`d.elimination`·`d.result` 또는 리포트 `why_recommended`·`result`만 소비·재계산 없음). `_reviewCompareBlock`(elimination 기반, 일본·통합·한국 공통)과 `_reportCompareStrip`(리포트 why_recommended 기반) 두 파생 함수로 구현. **[저장 후 자동 갱신]** `saveJapanResult`가 결과 저장 후 `openJapanReview` 재조회로 2단·대조 즉시 반영, `renderAnalysisDetail`도 기존 `saveRecResult`→`openAnalysisRecord` 재조회로 반영. `#jpReport` 등 저장 참조 요소는 항상 존재하도록 유지(저장 로직 호환). `_reviewCompareBlock`은 `function` 선언(호이스팅)이라 앞쪽 `renderAnalysisDetail`에서도 호출 가능. app.js `new Function` OK. 확장 변경 없음(ZIP 불필요). **기존 복기·결과입력·리포트 로직 무삭제**(전후 2단 레이아웃·대조 블록만 추가)
- **추천 근거 상세 설명 시스템 — 말별 근거 카드 + 기수 근거 3번째 카테고리(복기·통계·가중치)**(`app.py _recommend_basis`·`_compare_recommend`·`_recompute_learning_stats`, `static/js/app.js renderRecommendBasis`·`renderCompareStats`): 추천 이유를 한눈에 볼 수 있게 근거를 조립·표시(사용자 요청). **[1] 말별 근거 카드**(`recommendBasis`, 상위 3두) — ①전적(전적점수·최근5착순·평균착) ②배당(급락%·쌍승역전·연속하락 횟수·배당 변화 시퀀스) ③기수(복승률) ④종합 확신도(점수·등급·왜 이 점수인지 근거 나열). 실시간 이상감지 화면에 `📋 추천 근거` 카드로 표시. **[2] 근거별 복기** — `_compare_recommend`에 **기수 근거 조합(기수 복승률 상위)** 추가 → 결과 입력 시 `cmp_jockey_hit` 판정 → `compare_stats.jockey` 적중률(기존 전적/이상감지에 기수 카테고리 추가). 통계 탭 `🆚 근거별 추천 적중률 비교`에 기수 기반 셀 추가. **[3] 근거별 가중치 자동조정** — `basis_weights`(전적/배당/기수 적중률 → 신뢰 비례 정규화 + `top`=가장 신뢰할 근거) 산출·표시(통계 탭 `🧠 근거별 신뢰 가중치`). ⚠ **데이터 한계 명시**: 당거리·주로상태·날씨별 성적, 기수-말 조합성적은 현재 **미수집**(KRA 거리·코스 미제공) → 카드에 `미수집`으로 표기(수집 연동 시 자동 반영). 단위·`_triple_analyze` 통합 검증(3두 카드·기수 조합·compare_stats.jockey 33.3%·basis_weights) + 테스트 6종 통과. 확장 변경 없음(ZIP 불필요). **기존 compareRecommend·compare_stats·통합가중치 무삭제**(기수 카테고리·근거 카드·근거별 가중치만 추가)
- **추천 신중화 — 시장 신호 2개+ 확인 시에만 추천(신호 게이트) + 90초 관찰창**(확장 v2.1.35, `app.py _bet_judgment`·`signal_ready`): 기존 확신도 엔진에 사용자 요청 규칙을 정밀 반영. **[1] 시장 신호 4종 게이트** — ①초과급락 절대10%+(`excess.absStrong`) ②쌍승역전(`wx_reversals`) ③연속하락 2회+(`horseStreaks.count≥2`) ④환수율 이상(`overround.concentrated`) 중 **2개+ 확인 시에만 추천**, 미충족이면 `⏳ 신호 대기`(alloc 0). **전적은 시장 신호가 아니므로 카운트 제외** — 전적만 좋고 시장신호 0이면 확신도 높아도 추천 보류(사용자 핵심 요구). **[5] 90초 관찰창** — `signal_ready` 스냅샷 기준 **2→3회(≈90초)** 상향(첫 수집 직후 추천 금지). 판정 결과는 기존 `raceJudgment`(wait/확실형/신중형/애매형/패스형)로 그대로 표시(신규 프론트 불필요). `metrics.signalCount` 추가. 단위·`_triple_analyze` 통합 검증(신호1개→wait·2개→추천·전적만→wait) + 테스트 6종 통과. **기존 _bet_judgment·확신도·단계추천 로직 무삭제**(2-of-4 게이트·관찰창 상향만 추가)
- **역배열 감지 기준 수정 + '전적 우수하나 시장 비인기' 분류 신설**(확장 v2.1.35, `app.py _inverse_arrangement`·`static/js/app.js renderInverse`·`chrome-extension/overlay.js`): "전적 좋은 말이 배당만 높아도 역배열로 오탐"되던 문제 해결(사용자 요청). **진짜 역배열 = 쌍승 배당순위 역전(`wx_reversals`)일 때만** `detected=True`로 한정(기존의 "보조 유형 존재/단승1위가 복승최저에 빠짐"만으로 True 하던 조건 제거). **전적 상위(총점 60+)인데 배당은 비인기(대표 복승배당 15배+/인기 하위 절반 밖)인 말은 역배열이 아니라 `📈 전적 우수하나 시장 비인기`로 별도 표시**(`inverse.strongUnpopular`, `form`을 역배열 호출 앞으로 이동해 전달). 원격의 `invDetail`/`invLead` 팝업·보조 유형(`types`)·invCombos는 그대로 보존(삭제 아님). 단위(역배열 vs 비인기)·`_triple_analyze` 통합 검증 + `run_reversal` 22/0. **기존 역배열 배너·유형·팝업 무삭제**(판정 기준만 정밀화 + 신규 분류 추가)
- **⚠️ 혼전 경주 감지 + 고배당 포함 삼복승 전략**(`app.py`·`static/js/app.js`): 상위 배당이 근접해 이변 가능성이 큰 '혼전 경주'를 자동 감지하고 전용 전략 배너를 표시(사용자 요청). **[감지]** `_chaotic_race(curQ, curWin, key_horses, anomaly_horse, drops)` — 말별 대표배당(단승 우선·없으면 그 말 포함 최저복승 근사)으로 ①**상위 3두 배당 차이 20% 미만**(`top3Close`) 또는 ②**저배당 3두 이상 비슷**(최저 대비 25% 이내, `lowCluster`) 중 하나면 혼전 판정(근거 문자열 첨부). **[전략]** 복승 **저배당 메인(30%)** + 삼복승 **저배당 3두(10%)** + **저배당 2두+이상감지말(30%)** + **저배당 2두+고배당 복병(30%)** = 합 100%(고배당 복병=급락 있는 말 중 최고배당·없으면 최고배당 아웃사이더, 이상감지말과 중복 방지). 삼복승 실배당 미수집 시 구성 복승 3쌍 기하평균×2 추정. **[표시]** 프론트 `renderChaotic(a, bsel)`이 판정카드 바로 아래에 "⚠️ 혼전 경주 감지 / 이변 가능성 있음 / 고배당 포함 삼복승 권장" 주황 배너 + 조합별 배분(예산 입력 시 금액)·고배당 칩 렌더(`renderJapanIntegrated`·`sportAnalysisHTML` 양쪽 배선). **[안전]** `signal_ready`(기준값 설정/재설정/스냅샷 2건 미만 아님)일 때만 감지 → 초반 불안정 배당 오탐 방지. 격리 검증: 상위3두 3.0·3.3·3.5(17%) 혼전 감지·배분 100%·1강 독주(1.5배) None 정상. app.py `ast`+app.js `new Function` OK. 확장 변경 없음(ZIP 불필요). 기존 betRecommend·판정·이상감지 로직 무삭제(별도 `chaotic` 필드·배너만 추가)
- **🔄 마감 후 다음 경주 자동 전환(배당판 새로고침 → 재수집 → 분석)**(확장 v2.1.33, `chrome-extension/background.js`): 경주가 끝나면(T-0) 수집 엔진이 멈춰, 사람이 배당판을 새로고침하지 않으면 다음 경주 배당이 안 들어오던 것을 **발주 후 3단계 알람으로 자동 전환**(사용자 요청). **[타이밍]** `syncAutoEngine`이 발주시각(`timerDeadline`) 기준 3개 알람 예약 — **T+3분 `nextRefresh`**(끝난 경주 결과 1회 확보 시도 후 `chrome.tabs.reload`로 배당판 탭 새로고침) → **T+3분30초 `nextCollect`**(`_collectOnce`+`_forceAnalyze` 강제 1회 수집, 콘텐츠 스크립트가 새 raceKey/발주시각 자동 감지) → **T+4분 `nextAnalyze`**(분석 + "🔄 다음 경주로 전환 / 새 경주: … / 자동 수집 시작" 알림 + `syncAutoEngine` 재가동). **[엔진 정지 후에도 동작]** `stopAutoEngine`(T-0 정지)이 이 알람들을 지우지 않아 자연 종료 후에도 전환이 이어짐. **[안전]** `autoNextRace`(기본 true) 토글로 제어 + autoSend OFF 시 `syncAutoEngine`이 전환 알람도 함께 취소(임의 새로고침 방지) + `nextRaceDone`(발주시각) 표식으로 발주시각당 1회만 실행(예약된 알람 중복 삭제 방지). **[결과 자동수집 보존]** 다음 경주로 넘어가기 전 `nextRefresh`가 끝난 경주 결과를 먼저 1회 수집 시도하고, 실패해도 기존 T+5/7/10분(`resFetch`) 재시도가 백업으로 유지됨. `node --check` OK + ZIP 재빌드(v2.1.33). ⚠ 확장 새로고침 필수. 기존 수집 엔진·결과 자동수집·마감 알림 로직 무삭제(전환 알람 3개·토글만 추가)
- **📊 급락 신호 저/고배당 분리 표시(%기반) + 🎯 핵심 추천 3~4개 정리**(`app.py`·`static/js/app.js`): 사용자 최신 지시 반영. **[급락 저/고배당 분리]** "30배 이상 제외"는 **적용 안 함**(모든 급락 신호 유지) — 대신 고배당(직전 50배+) 급락은 **절대값이 아닌 %로만 판단**. 서버가 급락 신호(마감급락·급락·단승급락)에 `oddsBefore`/`oddsAfter`/`dropPct`/`highOdds`(직전 50배+) 구조화 필드 추가 → 프론트 `renderSignalsSimple`이 **저배당 급락은 상단**(기존), **고배당 급락은 하단 별도 '📊 고배당 급락 신호 (참고)' 블록**으로 분리(급락 큰 순·-40%+ '주목'/그외 '약한' 하위 노출). 라이브 검증(히로시마 1경주): 2+5(8→4.8 -40%)·5+7(11.5→7.3) 저배당 상단, 2+3(75→55 **-26.7% 약한**) 고배당 하단 참고. **[핵심 추천 3~4개]** 추천 조합이 14개로 너무 많던 것 → 판정카드 배분 섹션에 **🎯 핵심 추천(복승 메인1·복승 보조1·삼복승 보험1(소액)·쌍승 강신호시1)** 헤더로 3~4개 핵심만 명시(전체 추천표는 아래 유지·무삭제). app.py `ast`+app.js `new Function` OK. 확장 변경 없음(ZIP 불필요). 기존 신호·추천 로직 무삭제(밴드 필드·분리 표시·핵심 헤더만 추가)
- **🔄 역배열 로직 정확화: 인기순위(단승/복승) vs 쌍승 배당순위 2단계+ 역전만 판정**(확장 v2.1.31, `app.py`·`chrome-extension/overlay.js`·`static/js/app.js`): "단순히 배당 낮은 말 표시"에서 **두 순위 비교 기반**으로 정확화(사용자 정의: 인기순위보다 쌍승 배당순위가 2단계 이상 높은 말=역배열). **[서버]** `_rank_inversion_detail(curWin,curQ,curD)` — ①인기순위=단승(있으면)·없으면 복승 ②쌍승 배당순위=쌍승(exacta)·없으면 복승(단, 산출원이 같으면 비교 불가 → 기존 유지) ③각 말 `인기순위−배당순위≥2 & 30배미만`만 역배열 판정(역전폭 gap·최저=lowest) ④lead에 대조마 `vs`(인기 더 높은데 배당 더 비싼 말) 첨부. `_triple_analyze`가 inverse의 `invDetail`/`invLead`를 이 rank비교 결과로 교체(비교 가능 시)·`invSource` 표기(기존 `_inverse_arrangement`·invHorses·types·banner 무삭제). **[표시]** overlay 팝업/사이드패널·분석기 `renderInverse`가 "인기4위 3번·쌍승4.2배 ←낮음 / 인기1위 2번·쌍승12.7배 / → 인기4위가 1위보다 배당낮음 / → 3번 실질 유력! / 강도 🔴압도적(gap≥3)·🟠강한(gap2)" 형식으로 렌더. **라이브 검증**(히로시마 1경주·단승 미수집→인기=복승·배당=쌍승): 3번 인기4위·쌍승순위1위(gap3)·4.2배 = 역배열, 대조마 2번(인기1위·12.7배) 정확 산출·팝업 형식 사용자 요청과 일치. app.py `ast`+app.js `new Function`+overlay.js `node --check` OK + ZIP 재빌드(v2.1.31). ⚠ 확장 새로고침 필수. 기존 역배열 감지·유형·배너 무삭제(rank비교 판정·표시형식으로 정확화)
- **💰 배팅 배분 실반영: 경주유형 배분비율 → 실제 추천 조합별 금액**(`static/js/app.js`): 판정카드의 유형별 배분(확실 60·30·10/신중 40·20·40/애매 20·0·80/패스·대기 0)이 %만 보이던 것을 **실제 추천 조합(`betRecommend`)에 매핑해 조합별 금액**으로 표시(사용자 [2번] 실반영). `renderRaceJudgment`가 betRecommend에서 복승 메인/보조·삼복승을 골라(⚠ **읽기만·betRecommend 로직 무수정**) 예산×비율을 조합별로 분배(보조·보험은 조합 수로 분할). 검증(예산 2만): 확실형 → 복승메인 2+5(5.7배) 12,000원·복승보조 2조합 각 3,000원·삼복승보험 3조합 각 667원(합계 100%), 애매형 → 복승메인 4,000원·삼복승 3조합 각 5,333원(고배당 소액 분산). app.js `new Function` OK. 확장 변경 없음(ZIP 불필요). 기존 betRecommend·판정카드 무삭제(금액 매핑만 추가)
- **🔄 역배열 감지 팝업 정확화: 인기순위(단승) 대비 쌍승 저배당 말 상세 표시(<30배만)**(확장 v2.1.30, `app.py`·`chrome-extension/overlay.js`·`static/js/app.js`): 역배열 팝업이 "실질 유력마 X번" 한 줄만 나오던 것을 **인기순위별 배당 상세**로 정확화(사용자 요청·정의: 인기 순위보다 쌍승 배당이 낮게 형성=이상). **[서버]** `_inverse_arrangement`가 `invDetail`(역배열 말별 {마번·인기순위(단승 배당 기준)·최저 조합배당}, **30배 미만 조합만**·배당 낮은 순·최저=`lowest` 표식) + `invLead`(인기 낮은데(순위 3위+) 배당 최저 = 실질 유력 후보) 추가 반환(기존 invHorses·invCombos·banner 무삭제). **[확장 팝업]** `computeCritical` 역배열이 `lines` 배열(예: "8번 인기3위 · 쌍승 11.6배 ← 낮음" / "→ 인기 낮은 8번이 쌍승 더 낮음" / "→ 8번 실질 유력 가능성" / "강도: 🟠 강한 역배열")을 구성 → `renderAlertPopup`이 여러 줄로 렌더(div/textContent 안전 방식 유지·강도는 유형 레벨 🔴압도적/🟠강한/🟡). 오버레이 사이드패널 역배열 라인도 인기순위·쌍승배당 상세로 강화. **[분석기 웹]** `renderInverse`에 동일 상세 블록 추가. 라이브 검증: `/api/odds/triple/analyze`에서 invDetail `[8번 인기3위 11.6배(최저), 6번 인기5위 12.8배, 7번 인기4위 13.8배]`·invLead 8번 정확 산출. app.py `ast` + app.js `new Function` + overlay.js `node --check` OK + ZIP 재빌드(v2.1.30). ⚠ 확장 코드 변경 → **브라우저에서 확장 새로고침 필수**. 기존 역배열 감지·유형·배너 로직 무삭제(상세 필드·팝업 형식만 추가)
- **🎯 추천 로직 전면 개편: BMED 확신도 엔진 + 실전 경주유형 판정 + 단계별 추천 + 강화 제거마**(`app.py`·`static/js/app.js`): "저배당 무조건 추천"에서 **신호 기반 확신도 추천**으로 전환(사용자 요청). ⚠ 기존 `_signal_confidence`·`_elimination`·`_bmed_strategy`·`betRecommend` 무삭제 — 이들을 **소비해 새 지표만 파생**(반환 dict에 4필드 추가). **[2번 확신도]** `_confidence_engine` = 이상감지강도(40%) + 전적점수(30%) + 시세급락지속성(30%) → 말별 확신도·밴드(강력≥65/주목≥45/관찰≥25/약함) + 확신도순 랭킹. 급락지속성은 `_drop_persistence`(연속하락 확정=높음·페이크 반등=15). **[1·4번 경주유형]** `_bet_judgment` = 확실형(초과급락15%+·연속3회+·쌍승역전<0.80·전적60+ 또는 확신도80+)/신중형/애매형/패스형/대기 자동 판정 + 근거 + **예산 배분비율**(확실 60·30·10 / 신중 40·20·40 / 애매 20·0·80 / 패스·대기 0) → 프론트 `renderRaceJudgment`가 화면 최상단에 **크게** 표시(유형·근거·확신도 + 예산×비율 실금액). **[3번 쌍승강화]** 역전<0.70 + 초과급락15%+ + 연속3회+ + 확신도80+ **모두 충족 시에만** `exactaSignal`(⚡ 쌍승 강신호 조합). **[3번 단계추천]** `_stage_guide` = T-3(1차)/T-2(급락지속·페이크)/T-1(환수율·확정)/T-30초(강력/일반/보험/패스) 단계별. **[5번 저배당 보류]** 첫 수집(기준값 설정)·재설정·스냅샷 2건 미만이면 `wait`(⏳ 신호 대기)로 **저배당 무조건 추천 방지**. **[6번 제거마]** `_elimination_strong` = 제거판정(🔴🟠) 말을 근거(전적 하위30%·급락없음)와 함께 TOP5 카드에 목록화. 라이브 검증: `/api/odds/triple/analyze`에서 4필드 정상 산출(확신도 랭킹 7·6·8·5·2·쌍승역전0.651 감지·wait 판정·배분 0). app.py `ast` + app.js `new Function` OK. 확장 변경 없음(ZIP 불필요·페이지 새로고침). 데이터 보호: 작업 전 `data/` 전체(33MB·analysis_log133·race_results15·alerts11 등) `backups/data_20260707_224935/` 물리 백업. 기존 통합등급·제거분석·BMED·베팅 패널 무삭제(상단 판정카드·확신도·제거마 목록만 추가)
- **⭐ 유력마 TOP5 + 복병·이상감지 2두 상단 고정 카드(실시간·클릭 상세)**(`static/js/app.js`): "유력마가 전산 분석 결과에 묻혀 실시간 확인이 불편하다"는 요청 해결 — **실시간 배당 이상감지 화면(`#tripleAnalyzeReport`) 최상단에 고정 카드**(`renderTopHorses`, `position:sticky`)를 추가해 스크롤해도 항상 보이게 함. **[TOP5]** 제거분석(`elimination.horses`)의 통합확률(`combinedProb`)→제거점수→전적점수 순으로 상위 5두를 **순위·마번·마명·전적점수·배당·주요신호(급락%·집중급락·🚨이상감지) 한 줄**로 표시(제거분석 없으면 전적+배당유력마 폴백). **[복병·이상감지 2두]** TOP5 밖에서 이상감지(+100)·급락폭·집중급락(초과%p) 가중 신호강도 상위 2두를 별도 섹션(🐎)으로 표시. **[30초 자동갱신·순위변동 강조]** 기존 30초 폴링이 `renderTripleAnalyze`를 재호출할 때마다 재계산 + 직전 순위와 비교해 **"↑ 2위→1위 상승"(초록)·"↓"(주황)·"🆕 신규"** 배지(같은 경주 내에서만 비교, `raceKey` 변경 시 리셋). **[클릭 상세]** 각 행 클릭 → 전적점수·등급·최근착순·기수·대표배당·통합확률·유력점수·기대값·플래그 + **배당 타임라인**(`loadOddsTimeline` 스냅샷 재사용해 그 말 포함 최저 복승을 시각별 ▼▲로 표시) 펼침. app.js syntax OK(`new Function`). 확장 변경 없음(ZIP 불필요·페이지 새로고침만). 기존 통합등급/제거분석/베팅 패널 무삭제(상단 요약 카드만 추가)
- **중복 raceKey 근본 방지: 같은 경주면 한 로그 파일로 병합(`_canonical_log_key`)**(`app.py`): 같은 경주가 asyukk(한글 '모리오카 1경주')·keiba(한자 '2026-07-07 盛岡 1R') **두 컨텍스트에서 각각 수집돼 로그가 2개** 생기던 근본 원인 차단. `_build_analysis_log` 저장 시 **자기 파일이 없으면** 오늘·(트랙+라운드) 일치하는 **기존 로그를 파일명으로 경량 스캔**(트랙명은 `_track_norm`으로 한/일 별칭 통일: 盛岡=모리오카·金沢=카나자와)해 **그 기존 key로 병합** → 새 경주는 먼저 수집한 컨텍스트의 한 파일로 수렴(중복 로그·미입력 중복 미발생). 라이브 폴링(triple_store)·기존 파일은 불변. 검증: `金沢 1R→카나자와 1경주` 병합·자기파일 있으면 유지·신규 경주는 그대로 확인. ⚠ 이미 만들어진 중복(양쪽 파일 다 존재)은 결과 입력 시 다중 동기화(_match_row_to_all_keys)로 해소. 기존 로그 저장 로직 무삭제(병합 리다이렉트만 추가)
- **결과 등록이 중복 raceKey로 한쪽만 등록되던 문제 수정(캡쳐했는데 미입력으로 남음)**(`app.py`): 같은 경주가 **한글('모리오카 1경주', asyukk 수집)·한자('2026-07-07 盛岡 1R', keiba 경로)** 등 **중복 raceKey로 저장**돼 있어, 캡쳐 OCR/일괄등록이 트랙 별칭(모리오카=盛岡)으로 **한자 key에만 결과를 등록** → 한글 key는 그대로 미입력 목록에 남던 문제. 수정: `_match_row_to_all_keys`가 (경마장+경주번호) 일치하는 **모든 중복 로그**를 반환(명시적 과거 날짜 key는 제외해 다른 날 오염 방지) → `_register_result_rows`가 **추천 있는 로그를 primary로 전체 적중판정·학습**하고, 나머지 중복 로그엔 **결과만 동기화(`_mark_result_in_log`, `result_via=duplicate_sync`)** 해 모두 미입력에서 제거(학습 중복은 방지). 라이브 검증: 모리오카 1·2경주 등록 시 primary=盛岡에 학습 + `모리오카 N경주` 한글 로그에 결과 동기화 확인(미입력 해제). 서버 전용(확장 변경 없음). ⚠ 분석기 미입력 목록은 60초 폴링으로 자동 갱신. 기존 일괄등록·캡쳐 경로 무삭제(다중 key 매칭·동기화만 추가). ⚠ 보완점: 중복 key 생성 자체를 막는 수집 시점 정규화는 별도 과제
- **asyukk 지방경마 출마표(DebaTable) 전적 자동 수집 연결**(확장 v2.1.29, `chrome-extension/content.js`): asyukk34 지방경마에서 **[출마표] 버튼의 keiba.go.jp DebaTable URL로 일본 전적을 가져오도록** 연결(사용자 요청). 원인: 지방경마(NAR) 수집 경로는 이미 `fetchDebaStarters()`를 호출하지만, `getDebaParams()`가 `<a href>` 링크만 스캔해서 **asyukk의 [출마표]가 `<input onclick="window.open('...DebaTable?k_raceDate=..&k_raceNo=..&k_babaCode=..')">` 버튼**인 걸 못 읽어 파라미터 확보 실패 → 빈 전적. 수정: `getDebaParams()`가 `[onclick]` 속성에서 keiba DebaTable URL(`k_raceDate` 포함)을 정규식으로 직접 추출(출마표2=rakuten은 `k_raceDate` 없어 자연 제외). 이후 기존 `fetchDebaStarters`→background `FETCH_URL` 릴레이(www.keiba.go.jp)→`parseDebaTable`(馬番+競走馬 헤더·競走成績 최근5착순)→`POST_JAPAN` 통합분석 흐름이 그대로 동작. 검증: 실제 onclick에서 파라미터 추출(k_raceDate 2026/07/07·k_raceNo 1·k_babaCode 10)·www 호스트 DebaTable 서빙(馬名·前走 포함)·구조가 `parseDebaTable` 기대와 일치(착순+날짜 셀·~10두) 확인 + content.js syntax OK + ZIP 재빌드(v2.1.29). ⚠ 확장 새로고침 필수 + asyukk 지방경마 라이브 최종 확인 권장. 기존 keiba·인페이지 폴백·전적 로직 무삭제(파라미터 소스 1개만 추가)
- **결과 캡쳐 OCR을 다중경주 결과표 대응으로 재설계(캡쳐 1번=여러 경주 자동등록)**(확장 v2.1.28, `app.py`·`chrome-extension/popup.js`): 실제 asyukk '경주결과' 화면이 **단일 경주 보드가 아니라 여러 경주가 한 표(경주지역·라운드·1착·2착·3착·단승·복승·쌍승…)** 인 것을 확인 → 단일착순 가정이던 OCR을 **결과표 전체 판독 → 각 경주 자동 매칭·등록**으로 재설계. `/api/result/ocr`이 Vision으로 표의 모든 확정 경주 행을 읽어(`RESULT_OCR_SCHEMA` = results[{venue,raceNo,placing,quinellaOdds,trioOdds}]) rows로 변환 → **기존 일괄등록 로직을 공용 헬퍼 `_register_result_rows`로 추출**(일괄등록·캡쳐OCR 공용)해 분석경주 자동매칭(`_match_row_to_key`)·적중판정·학습까지 처리하고 **{registered,hits,profit,matched,unmatched} 요약** 반환. 매칭 후보를 `_all_analyzed_keys`(triple_store + analysis_log, 프룬된 과거경주 포함)로 확장해 매칭율↑. 팝업은 [📸 캡쳐→판독] 1번에 **"✅ N경주 등록 · 적중 M · 손익 X" + 경주별 착순** 표시(개별 저장 불필요). 라이브 검증: 실제 결과표 형태 이미지(사세보 1~7·호후 1 = 8경주)에서 **8경주 전부 정확 판독(착순+복승배당) + registered 8·unmatched 0**. app.py/popup.js syntax OK + ZIP 재빌드(v2.1.28). ⚠ 확장 새로고침 필수. 기존 일괄등록(HTML/URL) 경로 무삭제(공용 헬퍼로 리팩터·캡쳐 경로 추가)
- **캡쳐 시 오버레이 자동 숨김(결과 가림 방지) + OCR 판독 검증**(확장 v2.1.27, `chrome-extension/background.js`·`overlay.js`): "경주결과가 분석기(오버레이)에 가려져서 캡쳐가 잘 안 읽힌다"는 문제 해결 — `captureVisibleTab`은 화면 그대로 찍으므로 오버레이 패널(`kbOvPanel`)·칩(`kbOvToggle`)·강조팝업(`kbOvAlert`)이 결과 보드를 덮으면 판독 실패. `CAPTURE_TAB`이 **캡쳐 직전 오버레이를 잠깐 숨기고(visibility·상태 보존) 180ms 리페인트 후 캡쳐, 즉시 복원**(background→overlay `KB_CAPTURE_PREP{hide}` 메시지). 오버레이 읽기전용 원칙 유지(자체 요소 visibility 토글만, 새 DOM·금지태그 없음). **[OCR 검증]** 실제 결과보드 형태 이미지로 `/api/result/ocr` Vision 판독 정확도 확인: 경륜(일본어 1着 1-7-6)·경정 컴팩트(3-4-1)·한국식(5-2-7) 모두 placing·venue·raceNo 정확, **배당 숫자가 가득한 전체화면에서도 결과 스트립만 정확 추출([2,5,3])**, 순수 배당판은 빈 배열(오탐 없음). overlay.js/background.js syntax OK + 안전감사 통과 + ZIP 재빌드(v2.1.27). ⚠ 확장 새로고침 필수. 기존 캡쳐·오버레이 로직 무삭제(캡쳐 중 숨김만 추가)
- **경주결과 캡쳐+OCR 자동입력 (탭 이동해도 결과 등록)**(확장 v2.1.26, `chrome-extension/background.js`·`popup.html`·`popup.js`·`manifest.json`, `app.py`): 경륜/경정/바이크는 탭을 옮겨 다녀 자동수집이 놓치는 경우가 많은데, **경주결과 화면을 캡쳐→Vision 판독→착순 자동입력**으로 보완. **[서버]** `POST /api/result/ocr` — 캡쳐 이미지(dataURL)를 Claude Vision으로 판독해 **1·2·3착(placing)·raceNo·venue** 반환(경륜/경정/바이크/경마 공통, 기존 `image_block`/`call_claude` 패턴 재사용). **[확장]** 팝업에 **📸 경주결과 캡쳐→판독** 버튼 — `chrome.tabs.captureVisibleTab`으로 현재 보이는 결과 화면 캡쳐(`CAPTURE_TAB`) → 서버 판독(`POST_RESULT_OCR`) → **1착·2착·3착 표시** → [✅ 판독 착순 저장] 시 현재 raceKey로 **기존 `/api/history/record-result` 재사용**(`POST_RECORD_RESULT`, 적중판정·학습·손익 동일). **[미등록 알림]** 결과 자동수집 T+10분 실패 시 알림 문구를 **"경주결과 화면을 띄운 뒤 [📸 캡쳐→판독]을 누르세요"** 로 강화(캡쳐 경로 안내). 라이브: OCR 엔드포인트 등록 확인(빈요청 400)·서버 정상 리로드 + 확장 4파일 syntax OK + ZIP 재빌드(v2.1.26). ⚠ 확장 코드 변경 → **브라우저에서 확장 새로고침 필수**. ⚠ 실제 결과 화면 Vision 판독은 라이브 최종 확인 권장(판독 후 착순을 사용자가 확인·저장). 기존 결과 등록(자동수집·일괄·세션폼)·record-result 무삭제(캡쳐 입력 경로만 추가)
- **전송 실패 원인 명확화: "Failed to fetch" → "분석 서버 꺼짐" 안내**(확장 v2.1.25, `chrome-extension/background.js`): 확장이 수집한 배당을 분석 서버(`127.0.0.1:8011`)로 전송(`POST_TRIPLE`·`POST_RESULTS`·`POST_JAPAN`·`ANALYZE_TRIPLE`)할 때 **서버가 꺼져 있으면 `fetch()`가 `TypeError: Failed to fetch`(연결 거부)** 로 실패 → 팝업·오버레이·"3종 수집" 상태에 원인 불명 "Failed to fetch"로만 표시되던 문제. `svrErr(err)` 헬퍼가 연결거부류 오류(`Failed to fetch`/`NetworkError`/`ERR_CONNECTION`/`load failed`)를 **"분석 서버 꺼짐(127.0.0.1:8011 응답 없음) — 서버(경마분석기)를 실행하세요"** 로 변환해 상태에 저장 → 오버레이/팝업이 그대로 노출(원인 즉시 파악). ⚠ 근본 조치는 서버 실행(수집은 정상, 전송만 실패한 것). 외부(keiba) fetch 릴레이 메시지는 의미가 달라 미변경. background.js syntax OK + ZIP 재빌드(v2.1.25). ⚠ 확장 코드 변경 → **브라우저에서 확장 새로고침 필수**. 기존 전송/수집 로직 무삭제(에러 메시지 변환만 추가)
- **경륜 출마표 자동 수집·분석 (oddspark 선수 전적)**(`app.py`, `static/index.html`·`static/js/app.js`): 경륜 배당판에 없는 **선수 전적(競走得点·각질·결정타·전장소 성적)**을 oddspark 출마표에서 자동 수집·분석. **[수집]** `_keirin_parse_card`가 oddspark `RaceList.do?joCode=&kaisaiBi=&raceNo=` HTML을 서버가 직접 fetch(공개 페이지·**확장 불필요**)해 선수별 선수명/선수번호·競走得点·직근4개월 착순(1-2-3-외)·결정타 비율(逃-捲-差-マ)·각질(逃/追/両/差)·기어배수·今·前·前々장소 성적 파싱 + 경륜장 결정타 경향(직근1년 逃/捲/差%)·라인(隊列) 예상·예상 코멘트. **[분석]** `_keirin_analyze`가 **전적 등급**(競走得点 85+ A·75~84 B·65~74 C·<65 D) + **결정수 보정**(차입/마크 우세→추입형 +5·도주 우세→선행형 +3·젖히기 +4) → 보정점수(adjScore)로 랭킹 + 2연대율·결정수 비율(%)·경륜장 유리 각질 팁. 엔드포인트 `POST /api/keirin/card`({joCode,kaisaiBi,raceNo}|{url}|{html}). **[프론트]** 경륜 탭에 **🚴 출마표 분석** 카드 — 경륜장코드+개최일+경주 또는 oddspark URL 입력 → 등급 배지·득점(보정)·각질·착순·결정수 랭킹 표 + 경향·라인·팁·예상 표시. 실제 사세보 6R 걸즈경륜 라이브 파싱 검증(7명·競走得点 47~56·라인 4→3→2→1→5→7→6·결정타 경향 差50.5%·득점 랭킹·보정 정상) + 서버 함수 격리 테스트 + app.py/app.js syntax OK. ⚠ 걸즈(L급)는 得点 스케일이 낮아 대부분 D등급(상대 랭킹·보정점수로 비교). 확장 변경 없음(서버 직접 fetch). 기존 경륜 수집/분석 무삭제(출마표 전적 수집·분석·탭 카드만 추가)
- **분석기록 보완 4종: 종목 재태깅 마이그레이션 + 상세 결과입력 + 정렬/기간 + 복기 표식·학습**(`tools/migrate_log_category.py`(신규), `app.py`, `static/js/app.js`·`static/index.html`): 검색 가능한 분석기록 페이지의 보완점 순차 처리. **[보완1] 과거 로그 종목 재태깅** — `tools/migrate_log_category.py`(멱등·`--dry-run`): `category` 없는 구버전 분석로그에 raceKey 트랙명으로 종목 추론 백필(KRA 서울/부산/제주/과천→korea·JRA 도쿄/나카야마/후쿠시마 등→japan_central·경륜장 광명/창원→cycle·경정장 미사리/사세보→boat·오토레이스→bike·그 외 japan_local). 실행 완료(113건 중 110건 태그: 한국17·중앙19·지방70·경륜1·경정3, 재실행 시 전부 건너뜀). ⚠ 구버전 로그엔 종목 마커가 없어 트랙명으로만 추론(경륜/경정 일부는 지방으로 남을 수 있음, 신규 로그는 확장이 정확 저장). **[보완2] 상세에 결과 입력** — 분석기록 상세(`renderAnalysisDetail`)에 접이식 결과 입력/수정 폼(1~4착·투자·실수령·확정배당·메모) 추가 → 기존 `/api/history/record-result` 재사용(적중·손익·학습 동일), 저장 후 상세 자동 재조회. 패널별 클래스 스코프로 ID 충돌 방지(전체 기록·스포츠 탭 동시 안전). **[보완3] 정렬/기간 옵션** — 목록 API에 `pnl` 추가, 프론트에 **정렬(최신순/적중순/손익순)** + **기간(전체/최근7일/최근30일)** 셀렉트(적중·손익순은 날짜 그룹 없이 평면 정렬·손익 배지 표시). **[복기 표식·학습]** — 복기 메모 저장 시 서버가 `reviewed`/`reviewed_at` 마킹(목록·상세에 **🧠 복기완료 배지**) + 메모를 종목·적중·손익 태그와 함께 학습 코퍼스(`data/review_notes.json`)에 축적(같은 경주는 최신 갱신), `GET /api/review-notes/list`로 검색·기억. 마이그레이션 dry-run→실행 검증 + 복기 코퍼스 격리 테스트(중복 갱신·태그) + app.js/app.py syntax OK. 확장 변경 없음(ZIP 불필요). 기존 읽기전용 복기·결과입력·로그 무삭제(입력폼·정렬·기간·복기표식·코퍼스만 추가)
- **검색 가능한 통합 분석 기록 페이지(전 종목 복기)**(`app.py`, `static/js/app.js`·`static/index.html`·`static/css/style.css`): 경주기록·경륜 등 어느 페이지에서도 **과거에 어떻게 분석·배팅했는지 확인할 곳이 없던 문제**를 해결. **[서버]** 분석 로그(`_build_analysis_log`)에 **`category`/`sport` 저장**(기존 값 보존) + 목록 API(`/api/analysis-log/list`)가 category·sport·summary·keyHorses·top3·적중여부(`won`) 반환 → 매 fetch 없이 목록에서 검색·필터·배지 표시. **[프론트 컴포넌트]** `renderRecList`/`openAnalysisRecord`/`renderAnalysisDetail`(읽기 전용 복기: 유력마·제거마·이상감지 내역·추천 조합·결과/적중/손익·복기 메모, 입력폼 없어 ID 충돌 없음) + `loadRecCache`(1회 캐시). **[결과기록 탭 🔎 분석기록 서브탭]** 전 종목 통합 — **검색창**(경주명·날짜·유력마·요약) + **종목 필터 칩**(전체/경마/지방/중앙/경륜/경정/바이크/한국) + 날짜별 목록(적중 ✅/미적중 ❌/대기 ⬜ 배지·유력마·착순) + 좌목록/우상세 2단. **[각 스포츠 탭 📋 분석 기록 섹션]** 경정/경륜/바이크/중앙 탭에 해당 종목 과거 분석만 필터해 표시(클릭 복기). 브라우저 라이브 검증(111건·경륜 필터 1건·"사세보" 검색 4건·상세 복기 렌더·경륜 탭 종목별 기록·콘솔 무오류). 확장 변경 없음(ZIP 불필요). 기존 복기(일본경마·결과기록)·로그 무삭제(category 저장·검색 뷰만 추가)
- **삼복승 탭 정확 클릭 + 오버레이 삼복승 추천 + 강조 팝업 알림음(보완 3종)**(확장 v2.1.24, `chrome-extension/content.js`·`overlay.js`·`manifest.json`): 실제 asyukk34 삼복승 DOM(DevTools 확인) 기반 3종 보완. **[보완1] 삼복승 탭 정확 클릭** — 삼복승 탭 = `span.bet_type_btn`(bet_mode=11, 텍스트 "삼복승")임을 확인. `삼복승조합`(bet_mode=12)이 "삼복승"을 포함해 혼동될 수 있으므로 `clickAsyukkBetTab`이 `.bet_type_btn`을 **정확 텍스트 일치**로 클릭(폴백은 기존 `clickTabAndWait`). 미수집 시 `.odds_content`·"a-b-c" 텍스트 raw 덤프 진단 로그 추가(실보드 배당 위치 파악용). **[보완2] 오버레이 삼복승 추천** — 배당판 오버레이 패널에 **🎲 삼복승** 추천(trioRecommend 최상위, 실배당/추정 표기) 한 줄 추가(실데이터 "3+5+7 추정 12.9배" 검증). **[보완3] 강조 팝업 알림음** — 역배열/강한급락 등 중요 신호 강조 팝업에 **알림음 옵션**(기본 off, `chrome.storage.overlaySound`) 추가: 새 신호 발생 시 짧은 2단 삑(Web Audio), 팝업 헤더 🔔/🔕 토글로 즉시 on/off(같은 신호 반복 알림 방지·신호 해제 시 리셋). 안전 원칙 유지(div/span/button만·kbOv 네임스페이스·금지 문자열 0·try-catch·읽기 전용). content.js/overlay.js syntax OK + 안전 감사 + ZIP 재빌드. ⚠ 확장 코드 변경 → **브라우저에서 확장 새로고침 필수**. ⚠ 삼복승 실배당 추출은 실보드 라이브 검증 필요(미수집 시 `[삼복승진단]` 로그 공유). 기존 수집/오버레이 로직 무삭제(정확클릭·삼복승줄·알림음만 추가)
- **경륜·경정·바이크 삼복승 수집 추가(6명 종목 삼복승 탭 클릭)**(확장 v2.1.23, `chrome-extension/content.js`): 경륜/경정/바이크에서 삼복승 탭을 **아예 클릭하지 않던 문제**를 해결 — 6명 종목도 삼복승 탭을 눌러 배당을 조회. **[강화 수집]** 삼복승 탭 클릭 후 ①**직접 목록**(`currentTrios`, "a-b-c 배당" 나열형 소규모 보드) + ②**유력마 3두 축 클릭**(`collectTrioByAxis`, 매트릭스형 보드)을 **함께 시도해 병합**(같은 3두 조합은 최저 배당 유지) → 보드 형태와 무관하게 최대한 수집. 한국·중앙경마만 삼복승 생략 유지. 독립 try/catch 격리(삼복승 실패해도 복승·쌍승 유지). 서버는 실배당 삼복승을 그대로 분석(추정 아님) → 경륜 삼복승 추천이 실배당 기반으로 표시(격리 테스트: 광명 경륜 trio 4개·추천 18.5·22·25배 실배당·estimated=None). content.js syntax OK + ZIP 재빌드. ⚠ 확장 코드 변경 → **브라우저에서 확장 새로고침 필수**. ⚠ 실제 asyukk34 경륜 삼복승 보드 DOM은 라이브 검증 필요(미수집 시 콘솔 `[삼복승수집]` 로그로 직접/축 추출 수 확인). 기존 복승·쌍승 수집 무삭제(삼복승 조회만 추가)
- **배당판 오버레이 강화 + 중요 신호 강조 팝업**(확장 v2.1.22, `chrome-extension/overlay.js`·`manifest.json`): 배당판 위 오버레이(📊 실시간 분석)에 정보를 대폭 추가하고, 중요 신호를 팝업으로 강조. **[강화] 오버레이 표시 확장** — 헤더에 **종목 배지**(🚴 경륜/🚤 경정/🏍 바이크/🏇 중앙/🇯🇵 지방/🇰🇷 한국, `d.category`), **🎖️ 통합 등급 상위 3두**(전적등급+배당유력 병합, `topGrades`), **🔄 역배열 감지 라인**(실질 유력마), **🛡️ BMED 저배당 배분**(보험형 combos→조합·배당·비율·원금보전 ✅/❌, 없으면 plan 폴백, 6명 배지, `bmedRows`) 추가(기존 마감카운트·유력마·급락·추천·collectAlert 유지). **[강조 팝업]** `computeCritical`이 우선순위(역배열 > 강한급락 −50%+ > 경고신호 > 마감 1분 이내)로 가장 중요한 신호 1건을 산출 → 화면 **상단 중앙에 큰 팝업**(빨강/주황 그라데이션·흰 테두리·**깜빡임 강조**·`kbOvAlert`)으로 표시 + 패널 상단 배너로도 재강조. ✕로 그 신호를 닫으면 **다른 신호가 올 때까지** 숨김(같은 신호 반복 알림 방지). **[안전 원칙 유지]** div/span/button만 주입·`kbOv*` 네임스페이스·금지 문자열(odds/result/payout 등) 회피·전 구간 try-catch·chrome.storage 읽기만(수집/베팅 무영향). 실데이터 검증(사세보 경륜: 통합등급 5A·7A·3D·BMED 역배열형 배분·역배열 강조 "실질 유력마 5·1번") + 안전 감사(생성 태그 div/span/button만·금지 문자열 0) + overlay.js syntax OK + ZIP 재빌드. ⚠ 확장 코드 변경 → **브라우저에서 확장 새로고침 필수**. 기존 오버레이(토글·드래그·위치기억·2단 롤백)·수집 로직 무삭제(표시·강조 팝업만 추가)
- **asyukk34 종목별 분석기 탭 분리 (경정·경륜·바이크·중앙경마)**(확장 v2.1.21, `chrome-extension/content.js`·`popup.html`·`popup.js`·`manifest.json`, `app.py`, `static/index.html`·`static/js/app.js`): 사설(asyukk34) 배당판의 종목을 자동 감지해 분석기 탭을 종목별로 분리. **[1번] 종목 자동 감지** — `detectSport`에 바이크(오토레이스/オートレース), `detectCentralHint`(중앙경마/JRA) 추가. 수집 시 `category`(korea|japan_local|japan_central|boat|cycle|bike)를 산출해 `chrome.storage.detectedCategory`에 기록 → **팝업에 "현재: 일본 경륜 감지됨"** 실시간 표시(팝업 종목 셀렉트에 🏍 바이크 추가). **[2번] 분석기 탭 4개 추가** — nav에 🚤 경정·🚴 경륜·🏍 바이크·🏇 중앙경마 탭 신설(기존 한국/일본/결과기록/기수DB/통계 무손상, 13개 ID 보존). 라이브 폴링(`pollJapanOdds`)이 분석 결과의 `category`로 해당 탭에 미러링(`mirrorSportAnalysis`/`sportAnalysisHTML`, 순수 렌더 헬퍼만 조합) → 이상감지·역배열·통합등급·베팅추천·BMED를 탭별 예산으로 표시. **[3번] 종목별 수집 방식** — 지방경마=단승+복승+쌍승+삼복승+전적 / **중앙경마=복승+쌍승만**(삼복승·단승·전적 제외, keiba·사설 양 경로) / **경정·경륜·바이크=복승+쌍승만(6명)**. **[4번] 종목별 분석 방식** — `_bmed_strategy`/`_bmed_insurance`에 `sport` 인자 → 6명 종목(cycle·boat·bike)은 `sixRacer` 플래그 + **BMED 저배당(원금보전) 집중을 기본 근거·안내**로 표기. 서버 `triple_ingest`가 category 저장·`_triple_analyze`가 반환. **브라우저 라이브 검증**(사세보 경륜 경주가 경륜 탭에 6명 배지·역배열·통합등급·BMED "6명 출전 저배당 집중"·예산배분 정상 렌더, 타 종목 탭은 플레이스홀더 유지, 콘솔 무오류) + 경륜 category/sixRacer/저배당 격리 테스트 + 4파일 syntax OK. ⚠ 확장 코드 변경 → **브라우저에서 확장 새로고침 필수**. 기존 수집/분석/탭 로직 무삭제(종목 감지·탭·라우팅·category만 추가)
- **BMED 보험형 매트릭스 배당구간별 자동 배분 + 시나리오/원금보전 표시**(`app.py _bmed_insurance`, `static/js/app.js renderBMED`): **[1번] 배당 구간별 자동 배분**(1+2위/1+3위/1+4위 고정 비율, 합 100%) — **저배당(<3배) 70/20/10**(기존 60/25/15)·**중배당(3~7배) 50/30/20**(기존 원금보전 역산 → 고정 비율로 변경)·**고배당(≥7배) 40/35/25**(유지). 유력마 3두(2조합)면 앞 2개 비율을 조합 수만큼 재정규화(합 100% 유지). **[2번] 시나리오 자동 표시** — 예산 입력 시 각 조합에 **배당별 손익률(%)**을 함께 표시(`1+2(2배): 70,000원 → 적중 시 140,000원 (+40%)`, % = 총원금 대비 회수율−1) + 하단에 **최선**(가장 좋은 조합 적중)·**평균**(커버 조합 균등 적중 가정, `avgRatio`)·전부 미적중(전액 손실)·기대환수. **[3번] 원금보전 가능 여부** — 각 조합마다 그 조합만 적중해도 총원금 이상 회수되면 **원금보전 ✅**, 아니면 **원금보전 ❌(손실 N원)** 표기(`combo.preserved`, 예산 입력 시 손실 금액까지). 격리 테스트(저배당 70/20/10=+40/−40/−65%·원금보전 ✅/❌/❌·중배당 50/30/20·고배당 40/35/25·재정규화) — 사용자 예시와 정확히 일치. 확장 변경 없음(ZIP 불필요). 기존 전략 자동선택·정상 추천 로직 무삭제(보험형 배분 비율·시나리오·원금보전 표기만 조정)
- **수집 3종 보완 + 경륜/경정 종목 추가**(확장 v2.1.20, `chrome-extension/content.js`·`popup.html`·`popup.js`·`manifest.json`, `app.py`, `static/js/app.js`): **[1번] 삼복승 수집 복구** — v2.1.9에서 제거했던 삼복승(三連複)을 일본경마 양 수집 경로에서 복구(무삭제로 보존돼 있던 함수 재배선). 사설(asyukk) `collectTripleByTabs`: 유력마 3두를 축으로 삼복승 탭 자동 클릭 + 축마 버튼 자동 클릭(`collectTrioByAxis`·`localKeyHorses` 재사용) → 복승·쌍승·삼복승 3종 수집. keiba.go.jp `collectTripleKeiba`: `Odds3LenFuku` fetch로 삼복승 안정 수집(TRIPLE_STEPS 필터 복구). **독립 try/catch로 격리**해 삼복승 실패해도 복승·쌍승은 유지. 한국경마는 기존대로 복승만. **[2번] BMED 매트릭스 조건 완화**(`_bmed_insurance`) — 보험형 매트릭스가 안 뜨던 문제를, 활성조건을 **유력마 4두→3두 이상**으로 완화하고 **1착 신뢰도 70% 게이트 제거**(참고 지표로만 노출)해 해결. 3두(2조합)일 때 고정비율 합이 100%가 되도록 재정규화. 정상 추천 + BMED 보험형 2가지가 나란히 표시됨(기존 렌더 유지). 격리 테스트(3두 active·비율합 1.0·고배당 정규화·2두 비활성). **[3번] 경륜/경정 종목 추가** — 팝업에 **종목 선택(🏇경마/🚴경륜/🚤경정)** 신설(`sport`, 경마 기본=기존 동작 불변). 경륜·경정은 **6명 출전·복승+쌍승만 수집**(삼복승·전적 생략), 경마장명 오탐 방지 위해 한국모드 판정 우회. `detectSport`가 asyukk34 배당판 본문 텍스트로 종목 자동 감지(수동 선택 우선). 서버 `triple_ingest`가 `sport` 저장·`_triple_analyze`가 반환 → 분석기 이상감지 헤더에 🚴/🚤 배지. 경정 6명 ingest+분석 통합 테스트 통과. **[4번] 전적등급 + 배당유력마 통합 표시**(`renderIntegratedGrades`) — 따로 나오던 배당유력마·전적등급을 **말별 한 줄**로 통합(예 "1번 A등급(전적65·배당유력)"·"7번 A등급(전적80·배당신호없음)"). `a.form`+`a.keyHorses`+`a.anomalyHorse` 병합, 배당유력 우선·전적점수 내림차순 정렬, 이상감지말 🚨 표기. 기존 '⭐ 유력마' 줄·'🏇 전적 등급' 표는 보존(통합 카드만 추가). 4파일 syntax OK. ⚠ 확장 코드 변경 → **브라우저에서 확장 새로고침 필수**. 기존 수집/전략/렌더 로직 무삭제(복구·완화·종목·통합 카드만 추가)
- **결과기록 탭 UI 전면 정리 4종: 구조 재편 + 직접입력 접기 + 명예의전당 중복제거 + 빈상태**(`static/index.html`, `static/css/style.css`, `static/js/app.js`): **[1번] 화면 구조 재편** — ①최상단 **최근 결과 요약 카드**(recentSummary) ②그 아래 **결과 입력 대기**를 파란 테두리로 강조(가장 눈에 띄게, [결과 입력]→팝업 즉시 입력) ③나머지를 **서브탭 4개**로 정리: `📋 복기`(날짜별 그룹 클릭 복기)·`📥 일괄등록`(일괄 결과 등록+순서대로 빠른입력)·`🏆 명예의전당`·`📄 리포트`. 서브탭 CSS(`.subtab-bar/.subtab-btn`)·전환 JS(`initResultSubtabs`, 명예의전당/리포트는 클릭 시 지연 로드). **모든 기존 ID·기능 보존**(13개 ID 무손실, 섹션 이동만). **[2번] 경주별 직접 입력 숨김** — "경주 결과 입력"(resultForm, 한국경마가 쭉 나오던 목록)을 **기본 접힘**(resultFormWrap display:none)으로 두고 결과 입력 대기 카드의 [✏️ 경주별 직접 입력 펼치기] 버튼으로 토글(펼칠 때 최신 렌더). **[3번] 고배당 명예의 전당 중복 제거** — 같은 경주가 여러 번 표시되던 버그를 `renderHallOfFame`·`loadHighlights` 양쪽에서 **경주별 1개(가장 높은 배당) 유지**로 수정(`raceKey`/`race`/top3 키 기준 dedup). **[4번] 빈 상태 개선** — 빈 섹션(명예의전당/리포트)은 클릭 전 숨김 서브탭에 배치돼 화면을 낭비하지 않고, 각 빈 상태(대기 완료·기록 없음·리포트 없음)에 깔끔한 안내. 라이브 검증(서브탭 전환·직접입력 토글 숨김↔펼침·중복 3→2·요약 최상단·복기 목록 review탭·무오류). 기존 기능 무삭제(구조 재편·접기·dedup만)
- **결과기록 최근 뷰 보완 3종: 기간 선택 + raceKey 매칭 + 경마장 필터**(`static/js/app.js`): 직전 추가한 최근 결과·복기 뷰를 3가지로 강화. **[1번] 기간 선택** — 7일 고정을 **7일/30일/전체 토글**로(`_recentPeriod`, `localStorage` 유지). 상단에 현재 기준 표시. **[2번] raceKey 매칭율 향상** — 결과 저장 시 `History.addResult`에 **`raceKey` 필드 저장**(saveResult=분석 title, saveBatch=매칭 key). 최근 뷰 복기 클릭이 `raceKey` 우선으로 서버 배당 히스토리를 조회해 매칭 실패(raceTitle↔raceKey 불일치)를 줄임. `showFailureReport`도 데이터 없을 때 사유 안내. **[3번] 경마장별 필터** — 기간 내 등장 경마장을 **칩 필터**(전체/경마장별 건수)로 제공, 클릭 시 해당 경마장만 표시하고 요약·그룹도 필터 반영. 라이브 검증(7일 3건 66.7%·전체 4건 50%·모리오카 필터 2건 100%·트랙칩 건수·무오류). 기존 뷰 로직 무삭제(컨트롤·필터·필드만 추가)
- **결과기록 탭 UI 개선 4종: 최근 7일 뷰 + 복기 상세 강화 + 빈화면 + 요약카드**(`static/index.html`, `static/js/app.js`, `app.py`): 경주 없는 날도 리스트가 쭉 나와 화면을 낭비하던 문제를, **최근 7일만 날짜별로 모은 요약 뷰**를 결과기록 탭 최상단에 추가해 해결(기존 섹션 전부 보존). **[1번] 최근 경주 위주 표시** — `renderRecentResults`: `History`(로컬 결과기록)에서 **최근 7일(오늘 포함)** 레코드만 필터, **날짜별 그룹**(오늘/어제/N일 전 라벨·데이터 없는 날 숨김·최신순). **[2번] 복기 상세 강화** — 각 경주 클릭 시 복기 토글(적중=✅"왜 맞았는지"/미적중=❌"왜 놓쳤는지"). 복기 리포트에 **전적 점수**(`_failure_report` `scores` 반환)·**당시 이상감지말/유력마**(`anomaly_horse`/`key_horses`)를 추가해 신호·전적·배당 타임라인·이상감지 시점을 한눈에. **[3번] 빈 화면 처리** — 데이터 없으면 "최근 분석한 경주가 없습니다 / 배당판에서 분석을 시작하세요" 안내 카드. **[4번] 요약 카드** — 상단에 최근 7일 **기록·적중·적중률·손익** 4칸 스탯 그리드. 라이브 검증(요약 3기록·2적중·66.7%·+22,000원 정확·오늘/어제 그룹·8일전 제외·빈화면·무오류). 새로고침 버튼 + 결과 저장/자동수집 시 자동 갱신. 기존 대기목록·빠른입력·재현리포트·명예의전당 무삭제(최상단 요약 뷰만 추가)
- **추가 보완 3종: legacy AI 마이그레이션 + 백업주기 설정화 + 복기 UI 통합**(`tools/migrate_ai_schema.py`(신규), `app.py`, `static/js/app.js`, `.env`): **[1번] legacy AI 파일 마이그레이션** — `tools/migrate_ai_schema.py` 생성(멱등·`--dry-run` 지원): `data/ai_training/*.json` 중 `schema_version` 없는 구파일에 현재 버전(app.py의 `AI_SCHEMA_VERSION` 자동 파싱)을 맨 앞 필드로 백필. 실행 완료(legacy 1건 → 1.0 통일, `schema_counts`에서 legacy 제거됨). **[2번] 백업 주기 설정화** — 6시간 하드코딩을 `.env`의 `BACKUP_INTERVAL_HOURS`로 이관(`_backup_interval_seconds`, 기본 6h·최소 0.5h 클램프·잘못된 값 폴백). `.env`에 설명 주석과 함께 `BACKUP_INTERVAL_HOURS=6` 추가(`.env`는 gitignore 확인됨). 격리 테스트(2h→7200·0→1800클램프·bad→21600폴백). **[3번] 복기 UI 통합** — 적중/미적중 경주를 **한 복기 화면**에서: ✅적중 → "왜 맞았는지"(정답말 신호 근거·최대 급락·1·2·3착 타임라인·적중 조합) 녹색 카드(`_success_report_text` 신규), ❌미적중 → 기존 "왜 놓쳤는지" 빨강 카드. `_failure_report`가 `was_hit`에 따라 분기하고 `hit_combos` 반환. 프론트 `renderFailureReport` 적중 분기 추가 + `showFailureReport` 조기종료(was_hit skip) 제거 + 호출부 3곳(한국/일본 복기·재조회)을 적중/미적중 공통 호출로 변경. 격리 테스트(적중 리포트 헤더·왜맞았나 섹션·적중추천 7+4) + app.js syntax OK. 확장 변경 없음(ZIP 불필요). 기존 복기/백업/AI 로직 무삭제(스크립트·설정·분기만 추가)
- **경마 없는 시간 정비 4종: 복기 완성 + 결과입력 UI + AI 데이터 정비 + 데이터 자동백업**(`app.py`, `static/js/app.js`, 신규 `docs/AI_DATA_SCHEMA.md`): **[1번] 복기 시스템 완성** — 복기 텍스트 리포트(`_failure_report_text`)가 기존엔 놓친 말(`focus`) 1마 타임라인만 담아 복사/내보내기 시 정보가 빠졌던 것을, **1·2·3착 정답말 배당 역추적 전체**(각 말 시점별 배당·급락%·신호 라벨)를 포함하도록 완성(프론트 카드와 동일 수준). 미적중 경주만 대상(적중 경주는 기존대로 제외). **[2번] 결과 입력 UI 개선** — 이미지 일괄 결과표(`renderBatch`)에 **확정배당 컬럼 + 행별 자동계산** 추가: 확정배당 입력 시 그 행의 **투자금액×배당=수익**을 100원 단위로 자동 입력(녹색 피드백)하고 합계 즉시 재계산 → 일괄 입력 시 수동 계산 부담 제거(기존 적중 미리보기·합계 유지). **[3번] AI 학습 데이터 구조 정비** — 학습 파일에 **`schema_version`("1.0") 필드 도입**(`AI_SCHEMA_VERSION`, `_build_ai_training`), 현황 API(`_ai_data_status`)에 `schema_version`·`schema_counts`(버전별 개수·구파일=`legacy`) 추가로 마이그레이션 현황 파악 가능. **`docs/AI_DATA_SCHEMA.md` 데이터 사전** 신규 생성(최상위/race_info/horses/odds_features/prediction/result/labels/quality 전 필드 표 + 정비·마이그레이션 원칙 + 변경 이력). 필드 삭제 없이 버전만 추가(하위호환). **[5번] 데이터 자동 백업 완성** — 기존 백업은 결과 입력 시에만 트리거돼 장시간 분석만 하고 결과 미입력이면 분석로그가 백업 안 되던 공백을, **6시간 주기 안전 백업 데몬 스레드**(`_start_periodic_backup`, 리로더 메인 프로세스 1회 기동, 변경 없으면 no-op)로 보완. 격리 테스트(복기 텍스트 1·2·3착 포함·AI schema_counts·주기백업 함수·batch 자동계산 10000×3.5=35000) + 브라우저 무오류 로드 통과. ⚠ 4번(ngrok)은 사용자 요청으로 이번 제외. 기존 복기/결과입력/AI/백업 로직 무삭제(필드·컬럼·섹션·스레드만 추가)
- **오버레이 보완 3종: 팝업 토글 + 드래그·위치기억 + 실측 체크리스트**(확장 `popup.html`/`popup.js`/`overlay.js`, 신규 `OVERLAY_TEST.md`): **[1번] 팝업 오버레이 토글** — 팝업에 `📊 배당판 오버레이` ON/OFF 스위치 추가(기존 자동전송 스위치와 동일 `.switch` 패턴). `chrome.storage.overlayEnabled` 저장 → overlay.js가 `onChanged`로 즉시 반영. **배당판을 열지 않아도** 미리 설정 가능(수집 로직 무관). popup.js `els`·초기 로드·change 리스너에 3줄 추가(기존 코드 무수정). **[2번] 오버레이 드래그 + 위치 기억** — 패널 **헤더를 잡고 드래그**하면 이동(닫기 ✕ 버튼은 드래그 제외, 뷰포트 경계 클램프). 이동 종료 시 `chrome.storage.overlayPos({left,top})` 저장, 다음 열 때 `applyPos`로 **같은 위치 복원**(다른 탭도 `onChanged`로 동기화). 드래그는 document 레벨 mousemove/mouseup만 사용하고 새 요소를 만들지 않아 격리 원칙 유지(생성 태그 여전히 div/span/button만). **[3번] 실제 배당판 실측 체크리스트** — `chrome-extension/OVERLAY_TEST.md` 생성: 확장 재설치→배당판 열기→오버레이 ON→수집 정상 확인→OFF→수집 정상 확인 6단계 + 롤백표(✕/overlayKill/버전 재설치) + 판정 기준. 재감사(overlay.js 생성 태그 div/span/button만·금지 태그 0), overlay.js/popup.js syntax OK, popup.html 스위치 쌍 확인, zip 재빌드(12항목). ⚠ 확장 새로고침 필요. 기존 수집/팝업 기능 무삭제(스위치·드래그·문서만 추가)
- **배당판 실시간 분석 오버레이(완전 격리·읽기전용·2단 롤백)**(확장 v2.1.19, 신규 `overlay.js`): 배당판/분석기 페이지에 유력마·급락·추천·마감카운트를 띄우는 오버레이를 **기존 수집 엔진에 무영향으로** 추가. **[안전 분석]** content.js는 DOM에 아무것도 주입하지 않고 **읽기만** 하며(create/append/innerHTML= 전무), 수집은 범용 `querySelectorAll('table')`·`tr`·`[class*=result/payout/chaku]` 선택자에 의존 → 유일 리스크는 "오버레이가 table/tr나 금지 class를 주입해 스캔에 잡히는 것". **[격리 구현]** ①**content.js를 건드리지 않고 별도 파일 `overlay.js`로 분리**(수집 엔진 바이트 단위 무변경, git diff 0) — manifest content_scripts에 독립 등록. ②**`<div>`/`<span>`/`<button>`만 주입**(table/tr/td/th/li/dl 금지) → 수집 선택자에 완전 비가시. ③**`kbOv*` 네임스페이스**(odds/odd_/result/payout/chaku/haraimodoshi 문자열 회피, timer.js `kbTimer*`와도 무충돌). ④**전 구간 try-catch 다중 보호막** → 오버레이 예외가 수집/페이지로 전파 불가. ⑤**데이터는 `chrome.storage`(analyzeStatus/collectAlert/timerDeadline) 읽기만** → 신규 수집·탭클릭·서버호출 0(2초 경량 갱신). **[2단 롤백]** `overlayEnabled`(패널 ON/OFF·기본 OFF·런처 칩 📊 클릭 토글) + `overlayKill`(전체 즉시 비활성, `chrome.storage.local.set({overlayKill:true})`)을 storage에 저장, `onChanged`로 즉시 반영. **[테스트]** 오버레이 주입 전/후 수집 선택자 카운트 완전 동일 실증(`table` 3→3·`odd_ranking_table` 1→1·`odds_table` 1→1·`tr` 0→0·result류 0→0) + 정적 안전감사(생성 태그 div/span/button만·금지 문자열 주석에만) + overlay.js/manifest syntax OK + content.js 무변경 확인 + zip 재빌드(11항목). ⚠ 확장 코드 변경 → **확장 새로고침 필수**. 오버레이 기본 OFF라 켜기 전엔 작은 📊 칩만 표시. 기존 수집/탭클릭/결과수집/발주감지 전부 무영향(무삭제)
- **결과 자동수집 3종 보완: 삼복승 배당 알림 + 실패 분석기 배너 + 성공 시 결과탭 자동갱신**(확장 `background.js`/`content.js`, `app.py`, `static/js/app.js`): **[1번] 삼복승 배당값 알림** — 결과 수집 성공 Chrome 알림에 확정 삼복승 배당줄을 **조건부** 추가(`fo.trio.odds` 있을 때만): `삼복승 7+4+9: 88.5배 ✅ 적중!`(삼복승 적중 시 ✅). 기존 복승 배당줄·추천 적중줄 유지. **[2번] 자동수집 실패 → 분석기 상단 배너** — T+10분까지 실패(state='manual') 시 background가 현재 raceKey와 함께 신규 서버 브리지 `POST /api/results/auto-status`로 전송. 분석기가 `initResultAutoWatch`(5초 폴링)로 GET → **상단 고정 배너** `⚠️ N경주 자동수집 실패 → 수동입력 필요: <경주명> (클릭)` 표시(클릭 시 결과기록 탭 이동). 이미 결과가 저장된 경주는 서버가 실패 목록에서 자동 제외, 1시간 지난 실패도 제외. **[3번] 성공 시 결과기록 탭 자동 갱신** — 수집 성공 시 background가 `state='done'`(raceKey·top3·hit·finalOdds) 전송, 서버가 `_RESULT_AUTO_SEQ` 증가·`lastDone` 기록. 분석기 폴러가 `lastDone.seq` 증가를 감지하면 **새로고침 없이** 결과탭 갱신(`loadPendingResults`·`renderStats`·`loadReportList`·`loadHighlights`·`renderResultForm`) + `✅ <경주명> 결과 자동수집 반영됨` 토스트. 입력칸 타이핑 중이면 폼 재렌더 생략(입력 유실 방지). 신규 엔드포인트 `GET/POST /api/results/auto-status`(인메모리, `/api/auto/status`와 동일 브리지 패턴). 라이브 검증(POST manual/done→GET failures·lastDone·seq 정상, 실패 배너 화면 렌더 확인) + 확장/서버/JS syntax OK + `chrome-extension.zip` 재빌드. ⚠ 확장 코드 변경 → **확장 새로고침 필수**. 기존 알림/수집/폴링 로직 무삭제(배당줄·서버전송·폴러만 추가)
- **일본경마 결과 자동 수집 타이머 스펙 정합화**(확장 v2.1.18, `background.js`/`content.js`): 기존에 구축돼 있던 발주 후 결과 자동수집(fetch·iframe `/bet/result` 감지·raceKey 매칭·적중판정·학습·알림) 시스템을 요청 스펙에 맞춰 **타이밍·알림 2가지 보완**(기능 신설 아님, 무삭제). **[1번] 재시도 타이밍 5/7/10분** — `doResultFetch` 예약을 **발주 후 7/9/11분 → 5/7/10분**으로 조정(`resFetch0/1/2` 알람: T+5 최초 시도·실패 시 T+7·T+10 재시도, 최대 3회). 보조 경로 `RESULT_CHECK_OFFSETS`도 `[10,12,14]→[5,7,10]` 정렬. **[2번] 결과 페이지 fetch(기존 유지)** — `findResultUrl`(video_iframe→모든 iframe/frame src→링크→HTML 폴백)로 `/bet/result?id=N` 자동 감지 후 로그인 세션(`credentials:'include'`)으로 fetch → `_parseResultDoc`가 지역/라운드/1~3착/복승·삼복승 확정배당 파싱. **[3번] raceKey 매칭(기존 유지)** — `_matchResultRow`(지역 한글 + 라운드 숫자)로 분석 로그와 연결 → `/api/results/auto` → `_apply_result_learning`(적중판정·이상감지·전적·제거 학습, learning.json). **[4번] 미수신 처리(기존 유지+문구 정정)** — T+10분까지 실패 시 `resultAutoStatus.state='manual'` + **"❌ 결과 수집 실패 · 수동 입력하세요(결과기록 탭에서 착순 입력)"** 알림. 해당 경주는 결과 미입력으로 남아 기존 미입력 추적(`/api/race-results/missing`) 배너·목록에 표시. **[5번] 결과 수집 알림 배당값 추가** — 성공 알림에 **확정 복승 배당줄**을 추가해 스펙 형식 완성: `✅ 모리오카 3경주 결과 수집 / 1착 7번 / 2착 4번 / 3착 9번 / 복승 7+4: 12.3배 / 추천 7+4 ✅ 적중!`. `content.js collectResultsByFetch`가 `finalOdds`를 함께 반환하고 `background doResultFetch`가 `복승 {조합}: {배당}배` 줄을 조립(배당 미수집 시 해당 줄 생략=하위호환). 알림 메시지 빌더 격리 검증(적중/미적중/배당미수집 3케이스) + 확장 3파일 syntax OK + `chrome-extension.zip` 재빌드(v2.1.18). ⚠ 확장 코드 변경 → **브라우저에서 확장 새로고침 필수**. 기존 수집/파싱/매칭/학습 로직 무삭제(타이밍 상수·알림 문자열만 변경)
- **보완 3종 (2차): 급락 칩 색상 통일 + 일괄표 적중 미리보기 + 수익금액 자동계산**(`static/js/app.js`): **[1번] 급락 칩 색상 적용** — `renderAlertSignal(al, roleMap)`에 `roleMap` 인자 추가(호출부 `_horseRoleMap(a)` 전달). 경고 배너의 급변 말·급락 조합·추천 픽을 베팅표와 **동일하게 유력(녹색)/제거권장(주황)/확실제거(빨강 취소선)** 로 채색 통일(`_colorCombo` 재사용). roleMap 미전달 시 기존 노랑 표기 유지(하위호환). **[2번] 일괄 결과 등록표 적중 미리보기** — `renderBatch` 표에 **적중 컬럼** 추가. 이미지에서 추출된 착순으로 추천 베팅 적중 여부(✅ 적중/❌ 미적중)를 **저장 전에** 표시(마우스오버 시 베팅별 상세), 착순없음/매칭없음/추천없음 구분 안내 → 일괄 입력 실수 방지. **[3번] 수익금액 자동 계산** — 경주별 결과 입력 폼에 **확정배당 입력 필드 + 🧮 자동계산 버튼** 추가. 클릭 또는 확정배당 확정(change) 시 **투자금액 × 확정배당 = 수익금액**(100원 단위 반올림, 시각 피드백) 자동 입력 → 수동 계산 부담 제거. 브라우저 라이브 검증(자동계산 10000×5=50000·착순 미리보기 칩/판정·저장버튼 활성·17블록 무오류 렌더). ⚠ 기존 렌더/저장/합계 로직 무삭제(색상·컬럼·필드·버튼만 추가)
- **보완 3종: 복승 배당판 유력/제거 색상 + BMED 매트릭스 표기 버그 수정 + 경주별 결과입력 UI 개선**(`static/js/app.js`, `app.py _bmed_insurance`): **[1번] 복승 배당판 유력/제거 색상 표시** — 베팅 추천표 조합 셀의 말 번호를 역할별로 채색(`_horseRoleMap`/`_colorCombo`): **유력마(keyHorses ∪ 제거패널 후보)=녹색 굵게** · **제거권장=주황** · **확실제거(verdict 🔴)=빨강 취소선**. 표 상단에 색상 범례 추가 → 조합만 봐도 유력/제거 말을 즉시 구분. **[2번] BMED 매트릭스 표기 버그 2건 수정** — ①**중간값 중복 버그**: 보험 조합이 2개(짝수)일 때 `sorted[n//2]`가 최댓값과 같아져 "최선==중간"으로 중복 표기되던 것을 **정확한 중앙값**(짝수는 가운데 두 값 평균, `_median`)으로 수정. ②**'최악' 오인 표기**: `worstRatio`(적중 조합 중 최소 회수)를 "최악"으로 표기해 **양수(+수익)로 보이던** 위험한 오해 → "적중 시 최선/중간/최소"로 재라벨링하고 **진짜 최악(커버 조합 전부 미적중=전액 손실 -100%, `allMissRatio`)을 별도 명시**. 보험 헤더의 "1+2/1+3/1+4" 고정 문구도 실제 존재 조합 라벨로 동적 표기, null 비율 NaN 가드. **[3번] 경주별 결과 입력 UI 개선** — 착순 입력 시 **라이브 미리보기**: 착순 칩(추천 포함 말=⭐녹색 강조)·**추천 베팅별 ✅적중/❌미적중 즉시 판정**·중복 마번 경고·적중 시 "🎯 수익금액 입력" 안내. 착순 미입력 시 저장 버튼 비활성. 착순 파싱을 콤마·공백 모두 허용(미리보기·저장 동일). 격리 테스트(2조합 중앙값 분리·allMissRatio·3조합 중앙값) + 브라우저 무오류 로드 검증. ⚠ 기존 렌더/저장 로직 무삭제(표기·색상·미리보기만 추가)
- **배당 신호 로직 전면 점검·개선 5종**(확장 v2.1.17 + `_advanced_anomaly`/`_triple_analyze`): **[1번] 수집 속도** — 마감 임박 수집 간격을 **T-3분 10초·T-1분 5초**로 단축(이전 15초/10초, `background.js autoTick`). **[2번] 단승 강한 신호** — 확장 keiba 경로가 단승(単勝複勝 표 fetch, oper 후보 다중 시도+현재 DOM 폴백)을 수집해 `payload.win` 전송(사설 경로는 기존부터 전송). 서버는 단승 급락을 **추천 우선순위 최상단(`_sh_order` 최우선)**으로 반영(기존엔 6번째). `extractHorses(doc)` 인자화. **[3번] 환수율 실시간** — `_advanced_anomaly.overround`에 `refundRate`(=Σ 1/각복승조합배당) 명시 + 상위3조합 점유율 90%+ 시 `🟠 자금집중` 경고 신호(기존 로직 명확화). **[4번] 연속 하락 강화** — 말별 연속 하락 횟수 추적(`horseStreaks`, 단승 우선·없으면 최저 복승): **1회 후보(⚪)/2회 약한신호(🟡)/3회+ 확정신호(🔴)/급락후 반등 페이크의심(🟠)**. 확정신호(3회+) 말은 추천 우선순위에 반영, 페이크는 제외. 신호 리스트에 등급별 노출. **[5번] 경주 전환 완전 초기화** — raceKey별 히스토리 파일 분리 + 첫 수집 `baselineSet`(기준값) + 활성 캐시 30분 정리(`_triple_prune_stale`)로 이미 보장됨(직전 경주 스냅샷 미혼입·변동 재시작). 격리 테스트(3회 연속→확정🔴·반등→페이크🟠·환급률 집중경고·단승급락 최우선 추천) + 회귀(run_flow 23/0). ⚠ 확장 코드 변경 → **브라우저에서 확장 새로고침 필수**. 기존 복승/쌍승 수집·기존 신호 로직 무삭제
- **데이터 보호 시스템(결과 입력마다 GitHub 자동 백업 + reset 방지)**(`_data_git_backup`/`_run_data_git_backup`, `/api/data/backup`·`/api/data/status`, `scripts/backup_data.bat`·`safe_reset.bat`): **[1번] 코퍼스 추적 확정** — `race_results`·`analysis_log`·`ai_training`·패턴·`daily_summary`·`race_report`·korea 데이터를 GitHub 추적 대상으로 백업(`DATA_BACKUP_PATHS`). `.gitignore`가 3대 코퍼스를 제외하지 않음을 재확인하고 미커밋 상태였던 `race_results`·`ai_training`을 실제 추적에 편입. **[2번] 결과 입력 자동 백업** — `_apply_result_learning` 종료 시 `_data_git_backup` 호출 → **5초 디바운스**(연속/일괄 입력을 1회 커밋으로 묶어 `index.lock` 충돌 방지)로 데몬 스레드에서 `git add`+`git commit -- <경로>`(pathspec: 지정 코퍼스만 커밋, 스테이징된 코드 미포함)+`git push`. API 응답을 블로킹하지 않음. `/api/data/backup`(수동 즉시 백업·통계 탭 `🛡️ 데이터 보호` 카드 버튼)·`/api/data/status`(경로별 파일 수·추적 현황). **[3번] reset 방지** — `scripts/safe_reset.bat`가 `git reset --hard` 등 위험 명령 실행 전 `backup_data.bat`(data\ 전체를 `backups\data_<타임스탬프>\`로 물리 스냅샷, 최근 20개 유지, `backups/` gitignore)을 자동 호출 → 데이터 유실 원천 차단(복구: 스냅샷 또는 `git reflog`). 라이브 검증(물리 스냅샷 250파일 생성·`/api/data/status` 경로 현황·디바운스 자동 커밋). ⚠ 기존 백업 함수(`_analysis_log_git_backup`·`_korea_git_backup`)·`backup_checkpoint.bat` 보존(무삭제)
- **실패 복기 학습 시스템 + 일괄등록 유연화 + 명예의 전당**(`_classify_failure`/`_failure_record`/`_failure_report`/`_failure_stats`, `data/failure_review.json`): **[1번] 실패 자동 분류** — 결과 입력 시 미적중이면 실제 입상마(추천에서 빠진 최상위 말)의 배당 타임라인을 역추적해 5개 유형으로 자동 판정(유형1 신호미반영 / 유형2 페이크베팅 / 유형3 노이즈 / 유형4 전적오판 / 유형5 타이밍[마감후]). 단승 미수집(일본 NAR) 시 최저 복승 조합 배당을 대리지표로 사용. **[2번] 복기 리포트** — `GET /api/failure/report?raceKey=`가 "왜 N번을 놓쳤나"(타임라인 T-10/5/3/1분 + 신호 라벨) + 원인 + 개선점 텍스트/구조 반환, 미적중 저장 즉시 자동 표시(일본경마 복기·순서대로 빠른입력). **[3번] 정답말 역추적** — 1·2·3착 각 말의 배당 타임라인 + 시점별 신호 강도(강한/약한/반등/없음) 자동 표시. **[4번] 놓친 신호 패턴 누적** — 연속하락 미감지·약한신호 무시·페이크 후 진짜신호 등 패턴별 카운트. **[5번] 개선 전/후 적중률** — 규칙 생성 시점의 추천 적중률(before) vs 규칙 적용 이후 결과 적중률(after) 자동 비교. **[6번] 복기 대시보드**(통계 탭 `📚 복기 학습 현황`) — 유형별 분포·비율 바 + 가장 많은 실패 원인 + **실제 1착말 신호 보유율**(신호는 감지됐으나 추천 미반영 규모) + 놓친 패턴 TOP + 자동 규칙 + 최근 사례. **[7번] 규칙 자동 생성** — 같은 놓친 패턴 3회+ 반복 시 개선 규칙 자동 추가(`🔔 새 규칙 학습`). **[일괄등록 유연화]** — 경마장명 한/일/영 별칭 통일(오비히로=帯広=obihiro=OBI, 모리오카=盛岡=morioka 등 25개 경마장, `_TRACK_GROUPS`/`_track_norm`) + 영문 토큰 인식(`_area_num`) → "obihiro 5"·"OBI 5R"·"2026-07-07 오비히로 5경주" 전부 매칭. **[순서대로 빠른입력]**(결과기록 탭 `⚡ 순서대로 빠른 입력`) — 분석 경주를 시간순 나열(`GET /api/races/list`), 1~3착만 순서대로 입력하면 경주명 자동매칭·즉시 적중판정·복기. **[고배당 명예의 전당]**(통계 탭 `🏆`, `GET /api/hall-of-fame`) — 복승 30배+/삼복승 100배+ 적중 자동 등록 + "왜 맞았는지" 스토리(`_highlight_story`)·정답말 배당 타임라인 스냅샷. 격리 테스트(5유형 분류·규칙 자동생성 3회 임계·1착 신호율·한/일/영 별칭 매칭) + 실경주 라이브 리포트(佐賀/高知 미적중) 통과. ⚠ 기존 학습(learning.json 등)과 독립 저장소 → 기존 기능 영향 없음
- **시스템 완전 문서화 + 홈서버 운영 인프라**(안정 체크포인트 `v2.3.0-stable`): **[문서]** CLAUDE.md `📘 완전 가이드`(시스템 개요·핵심 공식 6종·데이터 구조·알려진 버그 전부 수정·AI 로드맵 4단계·홈서버 운영) + `RECOVERY.md`(서버 안켜질때/확장/데이터복구/코드되돌리기/전체백업/테스트 6절). **[자동시작]** `경마서버_자동시작.bat`(git pull→서버 기동→분석기·배당판 열기, `/auto` 무인 모드) + `scripts/register_startup.bat`(부팅 시 자동 실행 시작프로그램 등록). **[자동백업]** `scripts/backup_checkpoint.bat`(data/+문서 커밋·푸시·백업로그) + `scripts/register_daily_backup.bat`(작업 스케줄러 매일 00:00 자동 커밋 등록). **[품질기준]** `_ai_quality_score` 5필수×20점 재편(배당타임라인2회+·이상감지·전적·결과1~3착·확정배당, 80+ AI학습용/60~79 참고용/오류나 60미만 제외). **[일별요약]** `_build_daily_summary`→`data/daily_summary/YYYY-MM-DD.json`(경주수·품질·이상감지·적중·AI준비수·누적) 결과 입력마다 자동 갱신, `GET/POST /api/daily-summary`. **[AI 준비도]** `_ai_data_status`에 `high_quality`·마일스톤(패턴발견 100·모델학습 500)+`_eta_days` 예상 완료일, 통계 탭 `🤖 AI 학습 준비 현황` 진행바. 79 회귀 테스트 전부 통과
- **AI 분석 시스템 Phase 1: 데이터 완전 수집**(`data/ai_training/`, `_build_ai_training`/`_ai_quality_score`/`_ai_data_status`, `tools/export_ai_data.py`): **[1번] AI 학습용 완전 데이터** — 결과 입력마다 ML-ready 완전 파일 저장(race_info·horses[no·name·jockey·jockey_winrate·recent_results·record_score·distance_score·interval_days·weight_change]·**odds_features**[timeline·drop_rates·excess_drops·drop_speed·exacta_reversal·reversal_ratio·quinella_mismatch·refund_rate·consecutive_drops·fake_betting·large_scale_drop]·prediction·result[hit·hit_pattern]·labels[quinella_hit·winner·second·odds_range]). 기존 신호(초과급락·역전·급락속도·연속하락·페이크·환급률)를 피처로 재사용. **[2번] 품질 자동 검증·점수** — 필수 누락(-12)·선택 누락(-4)·범위 오류(마번1~16·배당1.0~9999·날짜) 감점, 완전 데이터 100점·90점+ & 오류無 = complete. **[3번] 현황 대시보드** — 통계 탭 `🤖 AI 학습 데이터 현황`(수집/완전/평균품질/목표 500·진행률 바·일평균·예상 완료 개월). `GET /api/ai-training/status`. **[4번] 누락 알림** — `_missing_results` 재사용(복기 상단 배너). **[5번] 내보내기** — `tools/export_ai_data.py`(평탄화 CSV 37컬럼 + 원본 JSON 배열, `--min-quality`·`--format` 옵션). 엔드포인트 `/api/ai-training/status·list·get`. `ai_training/` git 추적(코퍼스)·`dist/` 무시. 격리 테스트(완전조립·피처 재사용·품질점수·현황 진행률·CSV 평탄화) 통과
- **결과 데이터 완벽 저장 시스템**(`data/race_results/`, `_build_race_result`/`_save_race_result`/`_validate_race_result`/`_missing_results`): **[1번] 경주별 완전 저장** — 결과 입력 시 경주마다 완전 파일 1개 저장(race_id·date·track·round·distance·track_condition·weather·horse_count·result 1~4착·odds_at_start·odds_timeline·anomalies[급락·초과급락]·prediction[candidates·eliminated·recommend_main/sub·strategy·signal_quality·confidence·inverse]·result_analysis[main_hit·sub_hit·hit_reason·miss_reason·pattern_tags]·investment[budget·actual_return·profit]·memo). **[2번] 결과 입력 UI 완성** — 1~4착 + 투자금액 + 실수령배당 + **확정 복승/삼복승 배당 + 메모** 필드 추가, 저장 시 자동 매칭·적중판정·패턴태깅·수익계산·완전저장. **[3번] 누락 방지** — 당일 분석했으나 결과 미입력 경주 추적(`GET /api/race-results/missing`), 일본경마 복기 목록 상단 `⚠️ 오늘 결과 미입력 N경주` 배너. **[4번] 데이터 품질 검증** — 저장 전 마번 1~16·배당 1.0~9999·날짜 형식·착순 중복·필수값 검증, 오류 시 경고 표시(저장은 진행하되 validation 기록). **[5번] 통계 자동 집계** — 결과 입력마다 경마장별·월별 적중률/수익 자동 집계(`by_track`/`by_month`), 통계 탭 `📊 경마장별·월별 성과` 표. 엔드포인트 `GET /api/race-results/list`·`/get`·`/missing`. `race_results/`는 git 추적(AI 학습 코퍼스). 격리 테스트(완전 저장 구조·검증·누락추적·경마장/월별 집계) 통과
- **BMED 매트릭스 베팅 전략 시스템**(`_bmed_strategy`/`_bmed_insurance`/`_capital_preservation`/`_expected_return`, `_triple_analyze` 반환 `bmed`): **[1번] 전략 자동 선택** — 상황 자동 판별 후 5전략 중 최적 선택(보험형=이상감지 없음+유력마 명확 / 압축형=2두 강한 신호 / 역배열형=쌍승 역전 / 분산형=대규모 급락 노이즈 / 고배당도전형=강한 신호+고배당). **[2번] 원금 보전 자동 계산** — 배당 역산으로 각 조합 적중 시 총원금 이상 회수되도록 배분(Σ1/o≤1이면 원금보전+잔여 균등, >1이면 등환수 더치). **[3번] 기대 환수율** — 시장 복승 역수(1/o) 정규화 확률 가중 기대 환수율% + 최선/최악 시나리오. **[4번] 전략명 표시** — `📊 현재 적용 전략: BMED OO형` + 근거. **[+정상/보험 2종 선택]** — `_bmed_insurance` 보험용 매트릭스(1착축 1+2 최다·1+3 중간·1+4 최소, 배당별 자동 비율: 저배당<3 60/25/15 · 중배당3~7 원금보전 역산 · 고배당≥7 40/35/25)를 **정상 추천과 함께** 제시(조건 충족 시만) → 사용자가 보고 선택. 활성조건(유력마 4두 압축·1착 신뢰도 70%+·역배열 미감지, A+B 3배+는 원금보전 지표) 자동 판단, 미충족 시 대안(역배열형/정상) 안내. 예산 입력 시 조합별 베팅액·수령액·최선/중간/최악 자동 계산. 프론트 🛡️ 보험용 카드(정상 추천 아래 나란히). 격리 테스트(저/중/고배당 비율·원금보전·역배열 비활성·정상+보험 2종 동시) 통과
- **분석 고도화 2종: 적중 근거 표시 + 실시간 이상감지 고도화**: **[1번] 적중 근거**(`_apply_result_learning` `hit_basis`) — 결과/복기 리포트에 경주별 `🎯 적중 근거` 블록: 전적점수·급락점수·**급락폭**·역배열(감지·입상)·**최종신뢰도** 칩 + `📌` 한줄 근거 요약("전적유력마 1번 적중 · 1+2 복승 급락(-62.5%) → 1·2번 입상 · 역배열 감지말 3번 입상"). record·review 저장(재조회 유지). **[2번] 실시간 이상감지 고도화**(`_advanced_anomaly`, `signalQuality.advanced`): ①**급락 속도**(급락폭/수집간격 분) — 분당 10%+ 🔴 강한 신호·5%+ 🟡. ②**연속 하락 vs 단발 반등** — 3회 연속 하락(총 8%+)이면 종합 신뢰도 **+20**, 단발 후 반등이면 **−15**(신뢰도 재클램프 0~100). ③**페이크 베팅 감지** — 급락 후 반등(15%↓후 15%↑) `⚠️ 페이크 베팅 의심`. ④**복승 환급률 이상** — 전체 복승 역수합 + 상위 3조합 점유율 90%+ 시 `🟠 자금집중` 경고(이변 시 고배당). 신호 리스트·🎯 신호품질 카드에 표시. 격리 테스트(급락속도 분당 21%·연속하락/반등·페이크·환급률·신뢰도 보정 + 적중근거 생성) 통과
- **역배열 감지 특별 표시**(`_inverse_arrangement`, 추천 상단 `🔄 역배열 감지!` 배너): 단승 유력마 순서 ≠ 복승/쌍승 배당 순서(=실질 유력마 변경 가능성)를 **4유형 통합** 감지. ①**배너 상단부** — 단승 1위(+배당)·복승 최저 조합(단승1위 포함=정상/빠짐=불일치)·쌍승 역전 방향(비정상)·"→ 실질 유력마가 바뀌었을 가능성". ②**4유형별 표시** — `🔄 쌍승역전`(단승 N번 유력이나 쌍승 M번 1착이 더 낮음)·`⚠️ 복승불일치`(단승 예상 A+B, 실제 최저 A+C → C번 주목)·`📊 배당압축`(상위 말 배당 비정상 근접)·`🔴 집중급락`(N번 전체 평균보다 10%+ 더 급락). ③**추천 반영** — `⭐ 역배열 감지말`을 추천 상단에 별도 블록(빨강 강조)으로 표시(배당 높아도 우선 노출) + `복승 역배열` 조합(감지말 낀 저배당순)을 일반 추천과 구분 표시. 앞서 만든 `_win_exacta_reversal`/`_quinella_mismatch`/`_excess_drop_analysis` 결과를 재사용(중복 계산 없음)해 신호조합 추천(`_signal_combo_bets`)에도 이미 반영됨. 격리 테스트(단승 9번→실질 2번, 복승 2+9/2+5) 통과
- **핵심 이상감지 수학 공식 5종**(`_win_exacta_reversal`/`_quinella_mismatch`/`_signal_confidence`): ①**쌍승 역전 감지 공식** — 단승(미수집 시 복승인기) 유력마 A vs 다른 말 B 방향 비교. 역전비율 = 쌍승(B→A)/쌍승(A→B). <0.95 🟡 역전 · <0.80 🔴 강한역전 · <0.60 🔴🔴 압도적 역전("🔄 역전감지: 단승 9번 유력이나 쌍승에서 2번 1착이 더 낮음 → 실질 1착 2번 가능성"). ②**복승 불일치 감지 공식** — 불일치점수 = 단승 예상 최저복승(1+2위 조합)/실제 최저복승. 1.2+ 🟡 · 1.5+ 🔴 · 2.0+ 🔴🔴("⚠️ 복승 불일치: 예상 A+B / 실제 A+C → C번 집중 자금 유입"). ③**초과급락 기준 수정** — 전체 평균 대비 10%+ 초과(또는 절대 10%+)는 무조건 집중신호(노이즈 아님, 앞 커밋 ABS_STRONG 연계). ④**종합 신뢰도 점수** — 초과급락40% + 쌍승역전35% + 복승불일치25% 합산(말별 0~100). 70+ 🔴 강력 · 40~69 🟡 참고 · 40 미만 노이즈. ⑤**추천 조합 자동 반영** — 신뢰도 강력(70+)·참고(40~69) 말을 신호조합 추천 최우선 순위로 반영(배당 높아도 신호 있으면 표시). 프론트 🎯 신호 품질 분석 카드에 종합신뢰도표·쌍승역전·복승불일치 추가. 격리 테스트(역전 0.5/불일치 3.0/신뢰도 합산/70+ 강력 트리거) 통과
- **일본경마 삼복승 수집 제거 + 추정 보험 유지(절충)**(확장 v2.1.9): ①**수집 제거**(안정성) — `collectTripleKeiba`(keiba fetch)·`collectTripleByTabs`(사설 탭클릭) 양 경로에서 삼복승(3連複) fetch/탭클릭 완전 제거(일본=복승+쌍승, 한국=복승). 불안정하던 축 클릭 수집을 없앰. 기존 삼복승 수집 함수는 코드로 보존(호출만 안 함). ②**추천은 추정배당으로 유지**(절충) — 삼복승 배당 미수집 시 `_triple_analyze`가 삼복승 추천을 `_trio_est`(구성 복승 3쌍 기하평균×2) 추정배당으로 유지하되 복승 중심으로 '보험(추정)' 소액(총 ≤18%)만 배분하고 남는 몫은 복승 메인으로(`estimated` 플래그, 프론트 '추정보험' 배지). 이변 고배당 보험·4착 near-miss·대규모급락 보험 학습 유지. 삼복승 실배당이 수집되면 기존 로직 그대로(추정 아님). 쌍승 역전은 참고로 계속 표시. ③**UI 정리** — 팝업 라벨·수집 상태·index 안내 문구를 복승·쌍승 중심으로 정리
- **신호말 전체 복승 조합 추천(고배당 놓침 방지)**(`_signal_combo_bets`): 이상감지 신호가 있는 말(집중급락·복승/단승 급락·쌍승역전·이상감지말)들의 **모든 복승 조합을 추천에 추가**(이미 추천된 조합 제외, 배당 있는 조합만, 고배당 우선). 배당대별 tier 표기(고배당신호 50배+·신호기반·낮은배당신호). `복승 신호` 라벨로 betRecommend 포함 → 결과 입력 시 적중 판정·학습에 반영(예: 코치 3경주 결과 2-6-7에서 2/6 신호 있던 **2+6 복승 147배**를 추천에 포함해 적중 인정). 마감 후에는 미추가. 프론트 베팅표 예상배당 셀에 tier 배지
- **결과 입력 4착 확장 + 삼복승 near-miss 학습**: ①**4착 입력** — 일본경마 복기·결과기록·history 결과 입력 폼에 4착 필드 추가(적중 판정은 복승 1+2·삼복승 1+2+3 기존 유지). ②**아깝게 4착(거의 적중)** — 추천 삼복승 2두가 top3 + 나머지 1두가 정확히 4착이면 `near_miss`/`near_miss_horse`/`trio_near_miss` 기록(`_apply_result_learning`), 복기 리포트에 "🟡 아깝게 4착 - 거의 적중" 표시. ③**4착 빈번 말 삼복승 보험픽 우선** — `data/near_miss.json` 누적, 2회+ 4착 말이 이번 출전마에 있으면 `_triple_analyze`가 `삼복승 보험(4착빈번)` 픽 자동 추가(마감 전만). 통계 탭 `🟡 삼복승 아깝게 4착` 카드 + `GET /api/learning/near-miss`
- **이상감지 vs 추천 자동 비교 학습**(`_compare_recommend`/`_learned_integrated_weights`): ①**3종 추천 저장** — 매 분석 시 이상감지 기반(집중급락→급락조합→배당인기)·전적 기반(전적 총점 상위)·최종(betRecommend 블렌드) 추천 조합을 각각 산출·저장(`compareRecommend`, 분석 로그 `compare_recommendation`). 결과 입력 시 각 조합 적중 자동 판정(복승 top2 정확 또는 삼복승 top3 정확 → `cmp_anomaly_hit`/`cmp_form_hit`/`cmp_final_hit`). ②**통계 누적** — `compare_stats`(이상감지/전적/최종 적중률·표본) 자동 집계, 통계 탭 `🆚 이상감지 vs 전적 추천 적중률 비교` 카드. ③**가중치 자동 조정** — 이상감지·전적 각 50경주+ 누적 시 적중률 우세 쪽으로 통합 가중치 자동 조정(`_integrated_grades`, 기본 전적40/이상감지60 · ±15%p · 이상감지 0.45~0.75). 데이터 부족 시 기본값 유지
- **마감 후 신호 처리 + 수집 간격 단축**(확장 v2.1.8): ①**마감 후 신호 무효 처리** — 발주(T-0) 이후 수집 스냅샷(`mb_signed<0`·`after_close`)의 급락은 "⚠️ 마감 후 신호 - 참고만"으로 표시하되 **추천 조합·삼복승 보험·대규모급락 전략에 미반영**(마감 전 기준 유지). ②**수집 간격 단계 단축** — T-3분 15초 · T-1분 10초 · T-30초 5초(기존 T-3분 일괄 10초 → 세분화). ③**유효 신호 시간 표시** — 각 이상감지 신호에 `phase`("마감 N분전"), 마감 후 신호는 회색·소리/플래시 생략. ④**마감 후 케이스 학습** — 베팅 반영 불가했던 케이스를 `data/after_close_cases.json`에 경주별 저장(`_record_after_close_case`, `GET /api/after-close/cases`) → 수집 간격 단축 필요성 근거 데이터
- **이상감지 신호 품질 필터링**(`_excess_drop_analysis`/`_signal_situation`/`_integrated_adaptive`/`_combo_signal_quality`): ①**초과 급락률** — 시장 전체 평균 급락 대비 각 말의 집중도(초과=말평균-전체평균). 초과 5%p+ 🔴 진짜신호 · 0~5%p 🟡 약한신호 · 그 외 노이즈 제거. ②**대규모 급락 시 개별 신호 신뢰도 하향**(`lowConfidence`) + 초과급락(집중) 말을 `🔴 집중급락` 신호로 승격. ③**상황별 가중치 자동 조정** — 일반 전적50:신호50 / 이상감지다수 40:60 / 대규모급락 30:70(집중신호) / 대규모+집중 20:80(집중신호). 기존 `_integrated_grades`(40/60)는 보존하고 `integratedAdaptive` 별도 추가. ④**추천 조합 품질 점수**(상/중/하) + 근거("N번 초과급락 -11.4%p") — `betRecommend`에 `signalQuality`/`signalReason` 부착. 프론트: 🎯 신호 품질 분석 카드(상황·가중치·초과급락표·집중신호 통합등급) + 베팅추천표 신호품질 컬럼
- **일본경마 분석 내역 · 결과 복기**(일본경마 탭 신설 섹션 `📒 일본경마 분석 내역 · 결과 복기`): ①**목록** — `data/analysis_log/`에서 일본경마 경주(서울/부산/부경/제주/과천·TEST 제외)를 자동 필터·날짜별 표시(기본 오늘, [지난 날짜도 보기] 토글). 경주별 신호 수·결과입력 여부 배지. ②**클릭 상세** — 당시 유력마/제거마·이상감지 내역·추천 조합(복승/삼복승) 표시 + 실제 결과 입력 폼(1·2·3착, 투자금액, 실수령 배당금[선택]). ③**저장 시 자동 복기** — `/api/history/record-result`로 적중 판정(복승/삼복승)·이상감지 정확도(급락말 입상 여부)·전적유력마/제거 판정·손익 자동 계산 → 🧾 복기 리포트 즉시 표시. ④**통계 자동 갱신**(적중률·이상감지 패턴·손익). 백엔드: 분석 로그에 `raceKey` 저장 + `review_doc`에 `pnl`/`stake` 추가로 재조회 시 손익 유지
- **이상감지 결과 영구 누적**: 경주별 스냅샷(삭제 안 됨)에서 감지된 이상을 시간순·중복제거로 누적 표시 → 새 수집/마감 후에도 유지. `GET/POST /api/odds/anomaly-feed`(단승 급락·복승 급락·쌍승 역전, 심각도 🔴/🟡). 화면 좌하단 `🚨 이상감지 누적` 패널(3초 갱신)
- **쌍승 역전 영구 감지**: `_history_append`에 최저 쌍승 조합의 순서 반전(a,b)→(b,a) 감지 추가 → 스냅샷에 영구 기록
- **마감 전 3단계 알림**(`initClosingWatch`, `/api/auto/status` deadline 기반): T-1분30초 🔔소리2회+주황강조(현재 이상감지+추천) / T-1분 🚨소리3회+빨강강조(최종 메인 베팅+"지금 베팅하세요") / T-30초 ⏰소리4회+강한빨강("마지막 기회")
- **최종 알림에 누적 이상감지 요약 포함**: 각 단계 알림에 그동안 감지된 이상 전체 요약 + 메인 복승/삼복승 조합(`betRecommend`) 표기
- **대규모 급락 패턴 학습**(3종): ①**감지** `_mass_drop_detect` — 전체 복승 조합의 50%+ 또는 30개+ 가 동시 30%+ 급락 시 `massDrop` + `🌊 대규모 자금 분산 패턴` 신호(최상위 레벨, 특정 유력마 없음·이변 가능성↑). ②**전략** `_apply_mass_drop_strategy` — 삼복승 보험 8%→15% 확대 + 중배당(7~40배) 급락 복승 보험 추가 + 최저배당 복승 신뢰도 하락 표기(기존 조합 삭제 없이 alloc 조정, 합계≈100 유지). ③**학습** `_learn_mass_drop` — 결과 입력 시 `pattern_learning.json`에 사례(total_combos_dropped·drop_ratio·result·result_odds·signal·lesson) + 조건통계(고배당율·적중률) 누적. 프론트 🌊 레벨 색상/우선순위/한국·일본 알림 배선. 사용자 시나리오(87조합·5+6 -58.4% 고배당) 격리+라이브 검증.
  - **히스토리 분석 결과**: `odds_history`/`analysis_log` 스캔 → 초기정착·전량아티팩트 제외 후에도 **11경주에서 66~84% 동시급락** 관측 = **반복 패턴**(이번만 아님). 단 결과(착순) 0건 저장 → 적중률 검증은 학습 축적 필요.
- **이상감지 패널 경주별 분리 + 한국 포함 + 히스토리**(3종 개선): ①한국경마도 좌하단 누적 패널에 영구 표시(`pollKoreaOdds`→`setAnomalyPanelRace(m.raceKey)`, 기존 팝업 oddsToast 유지) ②현재 경주만 `[서울 5R]` 헤더 블록으로 표시(다른 경주와 안 섞임, `_rkLabel`) ③`📜 히스토리` 토글 → 이상감지 있는 과거 경주를 raceKey별 블록으로 분리 표시(`/api/history/list` `anomalyCount`·`lastT` 추가, `◀ 현재`로 복귀). 경주 전환 시 이전 경주는 서버 스냅샷에 영구 보존
- 격리 테스트(누적·중복제거·심각도·쌍승역전) + 라이브 수집→피드 파이프라인 + history/list anomalyCount 검증

### 수정된 버그
- **🔴 쌍승 역전 오판 근본원인 수정: asyukk 쌍승 매트릭스 선착/후착 방향 반대**(확장 v2.1.32, `chrome-extension/content.js`): 역전 감지가 반대로 나오던 **진짜 원인**은 서버 로직이 아니라 **확장의 쌍승 배당판 파싱 방향**이었음(사용자 배당판 이미지로 확인). asyukk 쌍승 매트릭스는 **열(헤더)=1착(선착)·행=2착(후착)** 규칙(예: 행3·열5 셀=5.2="5번 1착, 3번 2착"=5→3)인데, `collectTripleByTabs`가 `parseMatrixTable`의 `{a:행, b:열}`을 `combo=[행,열]`(행=선착)로 조립 → **모든 쌍승 방향이 반대로 저장**(5→3 저배당을 3→5로)돼 역전 감지가 정반대로 오판(5번 유력 정상인데 "3번 역전"). 수정: 쌍승 조합 키를 `${p.b}>${p.a}`(선착=열·후착=행) → `combo=[열,행]`. 검증: 라이브(히로시마 1경주) 서버 curD가 `3→5=5.1·5→3=22.8`(반대)였던 것이 배당판(5→3=5.2·3→5=23.7)과 일치하도록 교정 → `_win_exacta_reversal`이 정상(5번 유력·역전 아님) 판정. keiba(fetch) 경로는 HTML "1着-2着" 텍스트를 순서대로 읽어 이미 정상(무영향). 복승은 무순(a<b) 정렬이라 무영향. content.js `node --check` OK + ZIP 재빌드(v2.1.32). ⚠ **확장 새로고침 + 재수집 필수**(현재 서버 데이터는 이전 방향이라, 재수집 후 교정 반영). ⚠ 서버 `_win_exacta_reversal` 로직은 원래 올바랐음(입력 방향만 교정). 기존 파싱·수집 로직 무삭제(쌍승 방향만 수정)
- **[보완] 확신도/확실형이 전적 미수집 경주에서 무력화 + 역배열 invLead 오표기 방지**(`app.py`): 추천 개편의 실전 보완 2건. **①확신도 재분배** — 확신도=이상감지40+전적30+급락지속30인데 **지방(NAR) 등 전적 미수집 경주는 전적=0이라 확신도가 최대 70에 갇혀** 확실형(80+)·전적60+ 조건이 무력화되던 문제. `_confidence_engine`이 전적 데이터가 **아예 없으면**(`formAvailable=False`) 전적 30%를 이상감지·급락지속에 비례 재분배(0.40:0.30→**0.57:0.43**)해 신호만으로도 확신도 산출 + `_bet_judgment` 확실형 전적조건을 전적 미수집 시 완화(`max_form>=60 or not has_form`). 검증: 강신호(이상감지92·연속3회·초과급락18·역전0.72)+전적미수집 → 확신도 **91.1(강력)·확실형** 도달(기존 ~64에 갇혀 불가), 전적 있음(65) 동일신호 83.3 확실형과 일관. **②invLead 가드** — 역배열 팝업 '실질 유력' 후보(`invLead`)가 인기 낮은(3위+) 말이 없으면 `inv_detail[0]`로 폴백해 **인기1~2위(정상 최유력)를 '실질 유력'으로 오표기**할 수 있던 문제 → 폴백 제거(인기3위+ 없으면 None, 상세만 표시). 라이브 검증: 모리오카 6경주 formAvailable=False·가중치 0.57/0/0.43·확신도 12→17 개선·invLead 8번(인기3위=진짜 언더독) 정상. app.py `ast` OK. 확장/프론트 변경 없음(서버 파생값만 개선, 기존 필드 무영향). 기존 확신도·판정·역배열 로직 무삭제(가중치 조건만 보완)
- **경마 무관 페이지에 타이머 바 노출 제거**(확장 v2.1.16, `manifest.json` `timer.js` content_scripts.matches 한정): 상단 고정 카운트다운 바(`kbTimerBar`)가 `<all_urls>` 주입이라 **claude.ai·네이버 등 경마와 무관한 모든 페이지에도 떠서** 거슬리던 문제(✕로 닫아도 새로고침하면 재출현). timer.js 주입 대상을 **배당판(keiba.go.jp·사설 qwqwd25)·분석기(127.0.0.1:8011·localhost)로 한정** → 무관 페이지에는 바가 아예 주입되지 않음. 배당판 카운트다운·`📊 분석기 열기`·분석기↔확장 릴레이(FORCE_COLLECT/OPEN_ANALYZER/FETCH_RESULT_HTML)는 그대로 유지(기능 삭제 없음). ⚠ 확장 코드 변경 → **브라우저에서 확장 새로고침 필수**(chrome://extensions → 새로고침). 되돌리려면 matches를 `<all_urls>`로 복원.
- **경주 전환 시 직전 경주 배당 잔존 수정**(`_triple_prune_stale`, `current_race`/`triple_latest` `stale` 플래그): `triple_store.json`이 모든 경주를 무한 누적하고 `max-t` 폴백(triple_latest·current_race·analyze)이 '끝난 직전 경주'를 계속 끌어오던 문제. **[1번 완전초기화]** `triple_ingest`가 매 수집 시 30분+ 미갱신 경주를 활성 캐시에서 자동 정리(`STALE_ACTIVE_SEC=1800` · 방금 수집·최근 30분내 경주는 유지 → 한국·일본 동시 수집 안전 · 히스토리 파일은 영구 보존). **[2번 유효성 검증]** 프론트 `pollJapanOdds`가 분석 응답 `raceKey`≠요청 `raceKey`면 무시(직전 경주 데이터 차단) + `stale` 응답 시 표시 억제. **[3번 강제 초기화 버튼]** 상단 바 `🆕 새 경주 시작`(`newRaceStart`) — 활성 3종 초기화(`/api/odds/triple/reset`)+프론트 상태 완전 초기화(`hardResetRaceState`: 타임라인·이상감지·경고·현재 rk)+확장 즉시 수집 요청. **[4번 자동 감지 강화]** 30초 자동 감지(`refreshCurrentRace`)가 경주 변경 시 알림만 하던 것 → **자동 초기화 + 새 경주 화면 전환**(수동 새로고침도 변경 시 완전 초기화). 라이브 검증: 활성 저장소 74경주→3경주(전부 30분내) 자동 정리 + `🆕 새 경주 시작` 버튼 렌더·콘솔 무오류 확인. 기존 baseline 확립 가드·raceKey 전환 로직과 상호 보완(기존 기능 무삭제)
- **확장 팝업 즉시분석 결과 되돌이 수정**(확장 v2.1.15, `popup.js` `applyAnalyzeStatus`): 즉시분석으로 최신 분석을 봐도 **시간이 지나면 초반 분석으로 되돌아가던** 문제 — 앞선 수정은 분석기 웹(app.js)만 고쳤고 **확장 팝업은 별도 경로**라 미반영이었음. 원인: 백그라운드(background.js·content.js)가 주기적으로 analyze를 재실행해 `analyzeStatus` 저장소를 덮어쓰면 팝업 `onChanged` 리스너가 그 값으로 **자동 재렌더** → 초반(기준값 설정/재설정) 결과로 되돌림. 수정: 즉시분석(수동) 결과를 `manual` 플래그로 저장하고 **현재 경주(raceKey)에 고정** → 백그라운드의 '같은 경주 초반 결과'로는 덮어쓰지 않음(더 풍부한 갱신·경주 전환은 반영, 전환 시 고정 해제). ⚠ 확장 코드 변경 → **브라우저에서 확장 새로고침 필수**(v2.1.15 확인)
- **실시간 분석 결과 유지 — 초반 되돌이·중복 경고 제거**(`triple_ingest`/`_triple_analyze` baseline 확립 가드 + `onJapanOddsUpdate`): 즉시분석 후 시간이 지나면 분석이 **초반(기준값 재설정)으로 되돌아가고 불필요한 경고가 반복**되던 문제. 원인 — `_baseline_reset_needed`(직전 대비 60%+ 조합 90%+ 급락)가 변동성 큰 배당의 **단발 블립에도 오발동**해 history를 통째로 비움 → 분석이 첫 수집 상태로 회귀. **[1·3번] 확립 가드** — history가 **확립(4+스냅샷)되면 배당 휴리스틱으로 초기화하지 않음**(초반 미확립·다른 경주 잔존 배당만 즉시 초기화). 확립 후 경주 전환은 **raceKey 변경**(확장이 새 경주 감지 시 새 rk→새 레코드)으로 자연 처리. `_triple_analyze`도 `len(hist)>4`면 '재설정' 표시 안 함(개별 95%+ 급락은 기존 필터가 제외). **[2번] 프론트 중복 경고 제거** — `onJapanOddsUpdate`가 ①raceKey 변경 시에만 경고/타임라인 초기화 ②기준값 설정/재설정 상태에선 경고·`jpOddsPrev` 갱신 생략(신호 유지→복귀 시 재경고 안 함). 격리 테스트 `tests/run_report.py`(+4=38) + 라이브(확립5+블립 무초기화 / 미확립+블립 초기화) 검증
- **발주시각 오탐 수정 — 배당판 카운트다운 최우선**(확장 v2.1.14, `content.js` `autoDetectPostTime`/신규 `detectRemainingMs`/`POST_TIME_RES` 재작성): 사설 배당판에서 발주시각이 **엉뚱한 값(실제 30분 남았는데 "18~19시간 후")**으로 잡히던 문제. **라이브 페이지 진단으로 3중 원인 규명** — ①`detectPostTime()`이 페이지 첫 `발주 HH:MM`을 잡아 **헤더 현재시각 시계**(`발주 17:31:46`)의 HH:MM(17:31)을 발주시각으로 오인, ②**상단 네비의 다른 경주**(大井12R `발주 06:21`)를 현재 경주(모리오카 2경주) 것으로 오인, ③배당판 `남은시간`/`발주시간` **라벨이 깨끗한 텍스트로 안 잡히고 값만**(`6분 48초`/`12:25`) 존재 → v2.1.13의 라벨필수 방식(`labeledRemainingMs`)도 실패. 게다가 이 모의배당판은 `발주시간 12:25`가 실제 시계(17:31)와 불일치 → **카운트다운(`N분 M초`)이 유일 신뢰신호**. 수정 — ①`detectRemainingMs`가 `남은시간` 라벨 우선 + **라벨 소실 시 라벨 없는 "N분 M초"도 채택**(0<ms≤99분 임박범위)해 `deadline=now+남은시간`, ②`POST_TIME_RES` 재작성: 명시 라벨(`발주시간/発走/締切`)을 바레 `발주`보다 우선 + **시계(HH:MM:SS) 거부**(`(?![:：]?\d)`), ③HH:MM 폴백이 **6시간 넘게 미래면 자동설정 안 함**("18시간 후" 증상 원천 차단). raceKey는 정상, 발주시각만 교정. 라이브 검증: 카운트다운 3분15초 → 발주 12:26 정확 산출. ⚠ 확장 코드 변경 → **브라우저에서 확장 새로고침 필수**
- **급락 속도 마감임박 과대평가 수정**(`_advanced_anomaly`): 마감 임박 짧은 수집간격(5초 등)에서 -5% 블립이 분당 60%로 계산돼 `🔴 강한 신호`가 오탐되던 문제. **간격 하한(0.25분) + 절대 급락폭 동시 요구**(🔴=분당10%+ **그리고** abs≥15% / 🟡=분당5%+ **그리고** abs≥8%)로 과대 신호 제거. 진짜 강한 급락(5초 -20% 등)은 그대로 🔴 유지. 격리 테스트(블립 무신호·진짜급락 🔴 유지·-12% 🟡) 통과

### 추가된 기능(보완)
- **타임라인 데이터 정제 + 이상감지 신호 안정화**(`_next_race_surge`/`_as_qmap`/`_signal_timeline`/`_signal_timeline_from_doc`, `_history_append` 스냅샷 확장, `_triple_analyze` 반환 `signalTimeline`): 마감 후·다음 경주 배당이 타임라인에 섞이고, 이상감지 말이 흔들려 신뢰가 어렵던 문제. **[1번] 마감 후 데이터 자동 제외** — `_combo_timeline`이 마감(T-0) 이후 스냅샷을 `excluded`+`"마감 후 데이터 - 제외됨"`로 표기(데이터 보존, 변동 계산·분석에서 배제). 재현 리포트 배당 타임라인 탭에 회색 🗑️ 행. **[2번] 다음 경주 데이터 혼입 방지** — 직전 대비 공통 복승 조합 다수(60%+)가 **200%+ 급등**하면(경주 종료 후 다음 경주 초기 고배당 유입) `_next_race_surge`가 감지 → `_history_append`가 해당 스냅샷을 `next_race_blocked` 표기하고 이상감지·신호말 계산 생략 → 타임라인·분석에서 제외. **[3번] 이상감지 말 변경 이력** — 매 스냅샷에 대표 이상감지 말(`signal_horse`=집중급락 1순위, `_excess_drop_analysis` 재사용)을 영구 기록 → `_signal_timeline`이 `{time, previous_signal, new_signal, reason, minutes_before}` 변경 이력 도출("4번→3번 / 4번 배당 반등·3번 추가 급락"). **[4번] 신호 안정화(2연속 확정)** — 1회 감지=후보, **2회 연속=확정 신호**(`confirmed`/`candidates` 분리, 변경 시 직전 말이 1회 후 소멸이면 `prev_was_candidate`). **[5번] 최종 유효 신호 시점** — 말별 최초 감지·확정·소멸 시점(`events{first,confirmed,vanished,count}`) + 최종 유효 신호(`finalSignal`/`finalConfirmed`). 프론트: 분석 상단 `🎯 이상감지 신호 안정화 · 변경 이력` 카드(파랑) + 재현 리포트 `신호 이력` 탭(5번째) + 배당 타임라인 제외 행. 엔드포인트 `GET /api/odds/signal-timeline`. 리포트에 `signal_change_history` 저장. 격리 테스트 `tests/run_report.py`(+17=69): 급등 감지·변경이력·후보/확정·유효시점·제외표기 + `_history_append` 실기록 경로 E2E(신호말 기록·급등 차단·타임라인 도출). **삭제 없이 확장만**(기존 after_close·baseline_reset·drops 로직 보존)
- **고배당 경고 신호 완전 기록 시스템**(`_record_alert`/`_match_alerts_to_result`/`_alert_meta`, `data/alerts/`, `_triple_analyze` 반환 `alertSignal`): 배당 급변(복승 30%+ 급락) 경고가 화면에만 뜨고 사라져 **경고가 정답이었는지 사후 검증·학습이 안 되던** 문제(예: 카와사키 2경주 6+8 -49.5%·1+8 -46.3% 경고 → 실제 8-6-5로 경고가 정답 신호였음). **[1번] 경고 완전 저장** — 급락 30%+ 발생 시 `data/alerts/<경주>.json`에 `odds_snapshot`(조합별 before→after)·경고말·**당시 복승 메인 추천**·마감전분 기록(같은 조합쌍 1회만, 마감후·기준값 상태 제외). **[2번] 경고→결과 자동 매칭** — 결과 입력 시 `_match_alerts_to_result`가 경고말 입상 여부(`alert_correct`)·**경고 무시 후 놓침**(`ignored_miss`=경고말 입상했으나 당시 추천에 없었음) 판정. **[3번] 경고 신호 추천 반영** — `alertSignal`(경고말·최대급락·before→after·추천픽)을 분석 결과 상단 `⚠️ 경고 신호 감지!` 배너로 노출, 해당 말 조합을 추천에 포함(기존 `_signal_combo_bets` 재사용). **[4번] 고배당 경고 패턴 학습** — 학습 레코드에 `alert_fired`/`alert_hit`/`alert_ignored` + `highlight_wins.json`에 `alert_triggered`/`alert_horses`/`alert_ignored`/`lesson`. **[5번] 경고 적중률 통계** — `_recompute_learning_stats` `alert_stats`(발생 N·경고말 입상 N%·무시후미적중 N·조언), 통계 탭 `⚠️ 경고 신호 분석` 카드(40%+ 적중 시 "경고 발생 시 해당 말 추천 포함 권장"). 엔드포인트 `GET /api/alerts/list`·`/get`. 격리 테스트 `tests/run_report.py`(+11=52): 경고 저장·중복방지·마감후 제외·결과 매칭·무시판정·통계집계 + 카와사키 케이스 재현
- **삼복승 추정배당 부분추정 + 역배열 픽 표시**(`_trio_est`/`_triple_analyze` `reversalPick`, 베팅표 배지): 항상 편성한 삼복승 중 아웃사이더(역배열) 조합이 **구성 복승 1쌍 미수집 시 배당 None**으로 떠 판단이 어렵던 문제. ①**부분추정** — 3쌍 중 2쌍만 있으면 미수집 쌍을 `max(known)`(비인기 고배당 가능성↑)로 보수적 근사해 거친 추정배당 표시(`estRough` 플래그, 프론트 '(거친추정)'). 2쌍+ 미수집만 None. ②**역배열 픽 표시** — 쌍승역전 challenger를 낀 삼복승 픽에 `reversalPick` 플래그 → 베팅표 `🔄역배열` 배지(보험 라벨이어도 가시화). 격리 테스트 `tests/run_report.py`(+3=41): 아웃사이더 배당표시·estRough·reversalPick
- **삼복승 무조건 편성 + 역배열 조합**(`_triple_analyze` 베팅 추천, 배당판 수집 무관): 실배당(삼복승 배당판) 유무·유력마 3두 미만과 무관하게 **삼복승을 항상 추천에 포함**(배당 역배열 이변 대비). ①**항상 생성** — 선호순 풀(`ranked`→단승순→복승조합 등장마)로 3두를 채워 `삼복승 메인`을 항상 편성(유력마 3두 미만이어도). ②**역배열 대비** — 쌍승역전 challenger(실질 상위 지목마) 중 **메인 미포함 아웃사이더**를 낀 조합(`p1+p2+challenger`, `p1+p3+challenger`, 상위 2명·최대 3세트) 추가. challenger가 이미 메인/보험(anomaly)에 포함이면 중복 스킵. ③**소액 유지** — 배분은 기존 삼복승 ≤18% 캡(추정 미수집 시)으로 소액 유지 → 복승 중심 불변, 삼복승 배당은 `_trio_est`(구성 복승 3쌍 기하평균×2) 추정(미수집 쌍 있으면 배당 None이나 조합은 편성). **기존 삼복승 메인/보험 로직 보존**(중복은 `_addbet` 자동 dedup). 격리 테스트 `tests/run_report.py`(+5=34항목): 항상 생성·아웃사이더 역전 커버·≤18% 배분·메인 추정배당 검증
- **📋 경주별 결과 입력 시스템**(`_missing_results` 보강 + `/api/race-results/missing` + 결과기록 탭 대기목록·간단팝업, 저장은 기존 `/api/history/record-result` 재사용): 일괄 등록만 가능하고 경주별 개별 입력이 없던 문제. **[1번] 분석 경주 자동 대기목록** — 분석·배당 수집된(analysis_log 있고 결과 없는) 경주를 결과기록 탭 상단 `📋 결과 입력 대기 (N경주)`에 자동 표시(`_missing_results`에 **추천 요약**[삼복승 우선→복승, `final_recommendation`서 파생]·**이상감지 여부**[🔴]·**갱신시각** 추가 + 최근순 정렬). **[2번] 경주별 간단 결과 입력 팝업** — [결과 입력] → 모달(1~4착·복승/삼복승 확정배당·투자/실수령) → 저장 시 **기존 record-result가 적중판정·수익·학습을 그대로 처리** → 판정요약(복승/삼복승 적중·손익) 표시 + 목록·명예의전당·리포트·통계 즉시 갱신 + 목록에서 제거. **[3번] 경주 종료 후 자동 알림** — 마지막 배당 갱신(발주 근접) 30분 경과 미입력 경주에 `🔔 X 결과 입력하세요` 토스트+브라우저 알림(1회, `localStorage` 중복 방지) — 탭 무관 60초 전역 폴링. **[4번] 미입력 추적** — 대기 카드 헤더 `⚠️ 결과 미입력 N경주 — 지금 입력하면 AI 학습에 즉시 반영` 배너. **기존 결과 입력(일괄·세션 폼·일본 복기) 전부 보존**(신규 UI만 추가). 격리 테스트 `tests/run_report.py`(+5=29항목) + 라이브(실 10경주 대기목록·팝업 렌더) 확인
- **🏆 고배당 적중 상세 분석 리포트 시스템**(신규 `_build_race_report`/`_signal_win_tags`/`_combo_timeline` + `data/race_report/` + `win_tag_stats`, 결과기록 탭 UI): 고배당 적중이 **왜 추천됐는지 완전 재현**. **[1번] 경주별 재현 리포트** — 결과 입력마다 `data/race_report/<날짜>_<경마장>_<경주>.json` 자동 생성(`_apply_result_learning` 후킹): `why_recommended`(입상마+유력마별 초과급락%p·대표조합 배당 타임라인·쌍승역전 비율·전적점수·신뢰도), `recommendation_process`(전적→수집→초과급락→쌍승역전→신뢰도→BMED전략→최종추천 스토리 단계), `confidence_breakdown`(초과40%+역전35%+불일치25% 가중 기여+종합+상/중/하), `hit_type`. **기존 데이터만 소비**(`_triple_analyze` 반환·스냅샷)해 재계산 없음. **[2번] 명예의 전당** — 기존 `_highlight_save`(복승30배+/삼복승100배+) 엔트리에 `win_tags`·`report_slug`·경마장/날짜 리치 필드 추가, `GET /api/highlights`. **[3번] 추천 근거 재현 화면** — 결과기록 탭에 `🏆 명예의 전당` 카드형 + `📄 경주 재현 리포트`(4탭: 추천근거 스토리·배당 타임라인·전적 분석·이상감지 내역), `GET /api/race-report/list·get`(경로조작 방어). **[4번] 신호조합 학습 태깅** — 결과가 실제 입상마를 맞힌 신호(초과급락_적중/쌍승역전_적중/복승불일치_적중/전적보조_적중)를 레코드 `win_tags`로 태깅 + `_recompute_learning_stats`의 `win_tag_stats`(신호별·**동시조합별 적중률·고배당 적중률** = "초과급락+쌍승역전 동시 → 고배당 N%"). **기존 기능 무삭제**(highlight/race_results/learning 모두 확장만). 격리 테스트 `tests/run_report.py` 24항목 + 라이브 E2E(실 `_triple_analyze`→리포트 생성, 백업/복원으로 데이터 무오염) 통과. `race_report/` 추적(코퍼스)·`highlight_wins.json` gitignore
- **회귀 테스트 편입: `tests/run_reversal.py`**(22항목): 이번 세션 신규 핵심 로직을 영구 회귀 테스트로 저장소에 편입(향후 변경 시 자동 보호). `_reversal_level`(등급 컷)·`_win_exacta_reversal`(primary 보존·다중순위 감지·노이즈 게이트·wx[0] 우선순위·방어입력)·`_exa_fav_dirs`·`_history_append` 쌍승 flip 다중조합 기록(`ODDS_HISTORY_DIR` 임시경로 monkeypatch)까지 `app.py` 실제 함수를 직접 로드해 검증. CLAUDE.md 테스트 명령 목록도 갱신(run_formula·run_reversal·run_prerace 추가). 전체 Python 스위트 통과(formula 23·reversal 22·prerace 18·flow 23)
- **한국경마 수동 발주시각 → 마감 전 3단계 알림 발동**(한국 탭 `⏰ 발주시각(수동)` + `closingTick` 폴백, `_closing.manualDeadlineMs`/`manualRk`): 라이브 배당판(확장 수집) 없이 **PDF 사전분석만** 보는 한국 경주는 `/api/auto/status`에 발주시각(deadline)이 없어 마감 전 알림이 안 뜨던 문제. ①**수동 입력 UI** — 한국 탭에 `<input type=time>` + `설정/해제` 버튼(`#koreaDeadline`), 입력 시 현재 경주(`panelRk`)에 발주시각 바인딩. ②**폴백 로직** — `closingTick`이 서버 deadline 없으면 현재 경주에 설정된 수동 발주시각으로 폴백(서버 deadline 있으면 항상 서버 우선). 경주 불일치·과거 시각은 미적용. ③**지각 진입 스팸 방지** — 이미 지난 단계는 조용히 소진, 남은 단계만 발동(기존 무장 로직 재사용). ④**Enter 키 지원**. ⑤**새로고침 유지**(`localStorage` `bmed_korea_deadline`) — 설정한 발주시각을 페이지 새로고침에도 복원(`restoreKoreaManualDeadline`), 지난 시각은 자동 정리. 격리 로직 테스트(시각 파싱 엣지·폴백 게이트 6종·단계 발동 순서·지각 진입 12항목 + 저장/복원 왕복·만료게이트 9항목) 통과 + 서버 서빙 확인
- **쌍승 flip 다중조합 영구 기록**(`_history_append` + `_exa_fav_dirs`, 상수 `EXA_REVERSAL_TOPN=3`): 이상감지 누적(anomaly-feed)에 영구 기록하던 쌍승 방향 반전이 **최저(최유력) 1조합**만 감지하던 것을, **상위 3개 저배당(유력) 조합 각각의 방향 반전**까지 기록하도록 확장. ①**유력방향 계산 분리**(`_exa_fav_dirs`) — 무순쌍별 양방향 중 저배당 방향을 '유력(1→2착)'으로 판정하는 순수 함수(테스트 용이). ②**상위 N개만** — 저배당 순 상위 3조합으로 한정해 하위(비유력) 조합 flip 노이즈 배제. ③**포맷 호환** — 기존 `쌍승역전: a↔b` 문자열 그대로라 `_anomaly_pretty`·피드 중복제거(text 기준)가 조합별 **별도 라인**으로 자동 표시. 최저 1조합 flip은 종전과 동일하게 기록(하위호환). 격리 테스트(유력방향 계산·다중조합 flip 2건 기록·최저조합 보존·topN 밖 미기록·미반전 미기록·포맷 호환) 15항목 통과 + 회귀(run_formula 23·run_flow 23) 통과
- **쌍승 다중순위 역전 감지**(`_win_exacta_reversal` 확장 + `_reversal_level` 분리): 기존엔 **단승 1위 유력마 vs 나머지** 방향 역전만 감지했으나, 이제 **상위권(최대 4위) 다른 순위쌍(2·3·4위 간)의 역전**도 함께 감지(예: 단승 2위가 실은 3위보다 아래로 시장이 판단하는 경우). ①**primary(1위 기준) 먼저·multiRank 뒤** — 항상 1위 기준 역전을 강한 순으로 앞에 두고 순위쌍 역전을 덧붙여, `wx[0]`(AI 피처 `reversal_ratio`)·`[:3]`(역배열 배너)·`[:5]`(신호 리스트) 를 쓰는 기존 소비처 동작을 **완전 보존**. ②**노이즈 억제** — 하위 순위쌍은 노이즈가 크므로 **강한 역전(비율<0.80)만** 채택(1위 기준은 기존대로 <0.95). ③**신호 반영** — 각 역전의 challenger(실질 상위 지목마)가 `_signal_confidence` reversalScore·신호조합 추천(`_push_sig`)에 자동 반영. ④**프론트 구분 표시** — `🎯 신호 품질 분석` 카드의 쌍승 역전 목록에 `N·M위간` 보라 배지 + 헤더에 "상위권 순위쌍 역전 포함" 안내. `favRank`/`chalRank`/`multiRank` 필드 추가(기존 필드 불변). 격리 테스트(기존 primary 동작·문구 보존 · 2·3위 다중역전 감지 · 약한 하위쌍 제외 · wx[0] primary 우선순위 보존 · 방어 입력) 24항목 통과 + 기존 회귀(run_formula 23·run_flow 23) 통과
- **BMED 전략별 성과 학습**(`_recompute_learning_stats` `by_strategy`, record `bmed_strategy`/`inverse_detected`): 결과 입력마다 적용된 BMED 전략(보험형·압축형·역배열형·분산형·고배당도전형)별 적중률·수익 자동 집계 → 통계 탭 `📊 경마장별·월별·전략별 성과` 표에 `🎯 BMED 전략별` 추가. "어느 전략이 실제로 잘 맞는지" 데이터 축적(향후 전략 자동 가중의 근거). 격리 테스트(전략별 n/hit/rate/profit 집계) 통과

### 수정된 버그(계속)
- **자정 자동백업 무인 실행 hang 수정**(`scripts/backup_checkpoint.bat`·`register_daily_backup.bat`): 작업 스케줄러가 백업 배치를 무인 실행할 때 마지막 `pause`(키 입력 대기)로 **작업이 영원히 멈춰 자정 백업이 완료되지 않던** 문제. ①배치에 `/auto` 인자 모드 추가 → 무인 실행 시 `pause` 생략(파라미터 `%~1`로 판정, `endlocal` 후에도 유효). ②스케줄러 등록 시 `/TR "... /auto"`로 인자 전달. ③추가 방어 — `.git\index.lock` 감지 시(서버가 커밋 중) 이번 회차 스킵·로그 기록 후 다음 자정 재시도(git 잠금 충돌 방지).
- **자동전송 OFF 시 탭 클릭 방지**(확장 v2.1.12, `content.js` `AUTO_COLLECT` 핸들러): 사용자가 자동수집을 끈 직후 이미 발사된 in-flight 알람(stageT/fine)이 도착하면 복승/쌍승 탭을 강제 클릭해 **수동 베팅 화면을 방해**하던 문제. background 엔진에 autoSend 게이트가 있지만 끄는 순간 발사된 틱은 막지 못해, content.js `AUTO_COLLECT` 최종 진입부에 `autoSend` 재확인 가드 추가 — OFF면 `skipped:true`로 수집·탭클릭 모두 생략. 수동 수집(MANUAL_SEND)·결과 수집은 영향 없음.
- **한국경마 결과 매칭 버그 수정(형식 불일치)**(`_resolve_race_key`): 분석 로그 raceKey(`2026-07-05 서울 5경주`)와 결과 입력 raceKey(`서울 5`) 형식이 달라 "분석 경주 없음"이 뜨던 문제. ①**유연 매칭 리졸버** — 우선순위 ①완전 일치 ②경마장+경주번호 일치(오늘 날짜 후보 우선) ③경주번호만 일치(후보 유일할 때만). 날짜·"경주" 접미사 없어도 매칭. ②**`_area_num` 보강** — 날짜 먼저 제거(번호 오검출 방지) + `서울 5`(접미사 없음)도 번호 추출 + 한글·**일본어(가나·한자) 경마장명** 지원 + **부경=부산 별칭 정규화**(`_track_norm`). ③**결과학습 재구성** — triple_store(활성)가 churn으로 비어도 `_rec_from_history`가 odds_history 스냅샷에서 rec를 재구성해 분석 재현(결과 입력 정상 처리). ④**후보 소스 통합**(`_analyzed_race_keys`) — 활성 배당+odds_history+analysis_log 전체에서 매칭(한국·일본 중앙/지방 모든 경기). ⑤**일괄 등록도 유연화** — `_match_row_to_key`가 리졸버 사용(일본 포함). `record-result` 응답에 실제 매칭 key·`matchedFrom` 반환. 테스트: 서울 1~10 + 부산(부경) 1~7 + 오늘우선 + 완전일치 + 일본중앙 전부 매칭 + 재구성 결과학습 확인
- **새 경주 배당판 초반 미수집 → 가짜 급락·추천 불일치 수정**(긴급, 사가 6R·코치 7R): 새 경주 배당판을 초반에 못 끌어와 **opening/placeholder 배당**(복승 100배+, 예: 4+6=1585.1)이 캡처되면 ①실배당 정착 시 -80~-100% 가짜 급락이 이상감지 누적에 범람(사가 6R -94%/-98%/-100% 다수) ②추천 배당이 현재 배당판과 불일치(코치 7R "복승 2+9=7.1배"인데 실제 63.2배)하던 문제. **①opening 배당 정착 제외**(`_is_opening_settle`: 직전 100배+ & -80%↓ 급락은 기계적 정착 → 신호 아님) — `_history_append`(기록)·`_triple_analyze`(단승·복승 drops) 전 경로 적용. **②baseline 재설정 보강**(`_baseline_reset_needed`: 공통 조합 60%+가 opening 100배+에서 70%+ 정착 시에도 기준값 재설정 → 기록·표시 생략). **③이미 기록된 가짜 급락 숨김**(anomaly-feed에서 복승/단승 -88%↓ 필터). **④배당판 일치 검증**(`marketCheck`) — 추천 복승 메인이 배당판 최저 인기 조합보다 2.5배+ 비싸면(전적 편중/초반 미수집) `⚠️ 배당불일치` 경고 + **배당판 인기 조합을 `복승 인기(배당최저)`로 추천에 자동 추가**, 최저 복승도 80배+면 `⚠️ 배당불안정`(재수집 권장) 경고. 프론트 배너 표시. 격리 테스트(opening 정착 reset·개별 판정·정상경주 미오작동·불일치 감지+인기조합 추가) 통과
- **초과급락 신호말 노이즈 오판 → 추천 누락 수정**(긴급, 카나자와 10R 5-3-6): 시장 전체가 대규모 급락하면 3번(-17%)이 시장평균(예: -18%)에 묻혀 `excess≥0` → 노이즈 판정으로 추천에서 빠지던 문제(동일 패턴 3회째, 실제 2착 입상). **절대 급락 10%+(`avg≤-10`)는 시장평균과 무관하게 🔴 집중신호로 승격**(`_excess_drop_analysis` `ABS_STRONG=-10`) → `concentrated`에 포함되어 신호 조합 추천에 **반드시 반영**. 신호강도(`strength`=초과급락·절대급락 중 강한 쪽)로 점수(`_concentration_score`)·정렬·조합품질(`_combo_signal_quality`) 일관 계산. 집중신호 오판 사례는 `data/signal_lessons.json`에 학습 축적(`GET/POST /api/learning/signal-lessons`). 약한 급락(10% 미만)은 기존대로 노이즈 유지.
- **마감 오판 후 자동수집 영구 정지 수정**(확장 v2.1.11): 무변동 추정 마감에도 `autoSend:false`로 엔진을 완전히 끄고 `closed` 배너를 남겨, 한 번 오검출되면 수집이 꺼진 채 배너가 매 수집마다 재생성되던 문제. `_onRaceClosed`를 분기 — **확정 마감(DOM 발매마감)만 완전 정지**, **무변동(추정)은 `autoSend` 유지 소프트 일시중지 + 60초 뒤 재점검(`resumeCheck`) 자동 재개**(알림/배너 미표시). 배당 변동·새 경주 시 스스로 복구.
- **마감 오판(자동수집 조기 중단) 수정**(확장 v2.1.10): 발주 2분 40초 남았는데 "배당 5회 무변동"만으로 자동수집을 멈추던 문제. 확장이 발주시각을 못 잡으면(`deadline` 미설정) `!deadline`→무조건 무변동 마감을 적용하던 것을 제거. 배당판의 **실제 "남은시간"을 직접 파싱**(`pageRemainingMs`)해 **진짜 마감 임박(≤90초)일 때만** 무변동 마감 적용(발주시각 미검출 대비). 시간 정보 전무 시 DOM 마감 문구에만 의존.
- **첫 수집 가짜 급락 수정**: 배당판을 처음에 못 가져오거나(부분 로드) 시장 형성 초기 고배당일 때, 2번째 수집과 비교해 -90%대 가짜 급락이 이상감지 누적에 뜨던 문제. **첫 비교(수집 2건뿐)는 1틱 워밍업으로 급락 계산 보류**(`_triple_analyze` `market_forming`·`_history_append` 스냅샷 2건 이상부터 기록) → 2번째 수집을 기준으로 3번째부터 변동 계산.
- **경주 전환 시 배당 데이터 잔존 오검출 수정**: 새 경주로 넘어가도 이전 경주 배당이 남아 변동폭이 비정상적으로 크게 계산되던 문제(예: 이전 5+6=147.4 → 새 경주 5+6=5.7 → -96.1% 오검출) 해결. ①**경주 전환 감지**(`_baseline_reset_needed`) — 직전 대비 공통 복승 조합의 60%+가 90%+ 급락하면 다른 경주 잔존으로 판단, `triple_ingest`가 이전 history 완전 초기화 + 이번 수집을 새 기준값으로(`baselineReset`). ②**첫 수집 기준값** — 새 경주 첫 수집은 변동 계산 없이 기준값으로만 저장·"🎯 기준값 설정됨" 표시(`baselineSet`), 둘째 수집부터 변동 계산. ③**비정상 변동폭 필터** — 95%+ 급락 개별 조합은 복승/단승 drops에서 제외, 다수 발생 시 자동 재설정 + "⚠️ 비정상 변동폭 - 기준값 재설정" 신호. `_history_append`도 재설정 시 이상감지 생략·스냅샷 `baseline_reset` 표기. 서버단 방어라 확장 raceKey 갱신 지연/재사용에도 안전
- **현재 경주 자동 감지 오류 수정(확장 v2.1.7)**: 사설 보드에서 화면은 `고쿠라 4경주`인데 raceKey가 `모리오카 2경주`로 잘못 잡히던 문제. `extractRaceKey`가 경마장 목록(고쿠라 미포함)에서 본문의 첫 매칭(모리오카)을 집던 것을, **하단 네비 "이전 \<경마장\> \<N\>경주 … 다음"에 앵커링해 현재 경주를 직접 추출**하도록 수정(목록에 없는 경마장도 잡힘). 폴백 목록에 JRA 중앙(고쿠라·삿포로·니가타·도쿄·나카야마·주쿄·교토·한신) 추가. 고쿠라/부산/니가타 시나리오 검증.
- **한국경마 쌍승/삼복승 수집 완전 차단(확장 v2.1.6)**: 부산 등 KRA 경주인데도 `market`=auto + raceKey 추출 실패(사설 보드 표기 다양) 시 삼복승 탭을 클릭하던 문제. `pageLooksKorean()` 추가 — raceKey가 비어도 **페이지 본문/URL에 KRA 경마장명(서울/부산/제주/과천) + 경마 맥락**이 있으면 한국 확정. `isKoreaMode(raceKey, market)`로 판정 통일(종목=한국 OR raceKey OR 페이지). `collectTripleKeiba`·`collectTripleByTabs` 양 경로 적용 → 복승만 수집, 쌍승/삼복승 탭 클릭 완전 스킵. 로그 `[한국모드] 복승만 수집 - 쌍승/삼복승 생략`. 부산 화면 시나리오 포함 판정 검증.
- **(v2.1.5)** raceKey에 KRA 경마장명이 있으면 무조건 한국모드(`isKoreaByRaceKey`) — v2.1.6에서 페이지 감지까지 확장.

### 보완점
- ✅ **쌍승 역전 다중화 완료(공식·flip 양쪽)** — ①공식(`_win_exacta_reversal`)은 1위 기준 외 상위권 순위쌍(2·3·4위 간) 역전까지 감지. ②영구 flip 기록(`_history_append`)은 최저 1조합 → **상위 3개 저배당 조합** 각각의 방향 반전까지 누적(`EXA_REVERSAL_TOPN`). 남은 여지: 하위 조합/다단계(3개 초과) flip 누적은 노이즈 우려로 미채택(상수로 조정 가능).
- ✅ **한국 PDF 전용 경주 마감 알림 해결** — 라이브 배당판 없이 사전분석만 볼 때도 한국 탭 `⏰ 발주시각(수동)` 입력으로 마감 전 3단계 알림 발동(서버 deadline 있으면 자동 우선). ✅ **해결(자동화 완료)**: `_extract_korea_post_time`가 PDF 텍스트에서 발주시각을 자동 추출 → 사전분석 로드 시 자동 채움·알림 무장(17경주 전건 검증). 남은 여지: PDF에 발주시각 표기가 없는 다른 양식은 여전히 수동 입력.

---

## v2.2.0 (2026-07-05) — 유력마/제거마 공식 고도화 + PDF 전경주 사전분석
태그 커밋 `ab772d7`

### 추가된 기능
- **유력마/제거마 선정 공식 전면 개선 [1~5]**: 유력마 점수(전적40+배당신뢰도30+기수20+적성10), 배당신뢰도(전적우수+배당낮음 +30 / 전적불량+배당낮음 -10 / 단독 0), 제거 점수(150배+ -40·80~149 -20·평균5착+ -30·기수<10% -10·급락 +30·쌍승 +20), verdict 컷 30/50/70, 수학적 확률(시장 1/배당×0.75·통합 0.6/0.4·기대값)
- **제거 공식 거리경험 -15 훅** 완결(`_elim_score(no_dist_exp)`, 데이터 확보 시 자동 활성)
- **PDF 전경주 사전분석**: 아침 1회 업로드 → 전경주 백그라운드 순차분석 → `data/prerace/` 경주별 영구저장 → `GET /api/korea/prerace`(목록)·`/api/korea/prerace/<key>`(즉시 로드)
- **사전분석 프론트 UI 배선**: 한국경마 탭에 `📅 저장된 사전분석` 패널 — 날짜별 그룹·경주 칩 클릭 시 세션과 무관하게 과거 분석 즉시 로드(`loadPreraceList`·`openPreraceRace`, 기존 렌더 흐름 재사용으로 푸터·결과입력까지 완전 동작)
- **실제 투자금액 손익 정확화**: 결과 입력에 `실수령 배당금(원)` 입력란 추가 — 입력 시 서버가 확정배당 추정 대신 `실수령 − 투자금`으로 정확 계산(`payout` 파라미터, 공란이면 기존 추정 유지). `record.payout_actual` 저장 + 격리 테스트 4케이스 통과
- **일괄 등록 경주별 손익 조정**: 요약 표를 경주별 `투자금·실수령` 편집 가능 표로 개편(라이브 손익 합계) + `POST /api/results/adjust`(가장 최근 학습 레코드 in-place 갱신, 중복 방지). 배당배수는 결과페이지 실제 확정배당 사용. `_recompute_pnl` 격리 6케이스 통과
- **사전분석 패널 자동 갱신**: `🔄 갱신` 버튼 + 아침 분석 진행 중 폴링에 맞춰 목록 실시간 갱신(`refreshPreraceIfOpen`)
- **거리 이력 수집 준비**(`fetch_kra.py`): 응답에 거리가 있으면 `rcDist`를 race+byHorse에 담도록 추가(다중 필드명 방어). 거리 보유 엔드포인트 확정이 활성 선행조건(현 `racedetailresult` 미반환·`API299_1` 500)
- 스펙 정합성 테스트 신설: `tests/run_formula.py`(23) · `tests/run_prerace.py`(18)

### 수정된 버그
- 중앙 JRA 결과 파싱 보강(전각숫자 ０-９·전각콜론·1着/2着/3着·複勝·三連複 컬럼)
- 지방 NAR 전적수집 안정화(`parseDebaTable` try/catch + `fetchDebaStarters` 2회 재시도)
- 합성 기수 제거 → KRA 실데이터 104명만 유지

### 보완점
- ✅ 프론트 `prerace` 목록 UI 배선 완료(한국탭 `📅 저장된 사전분석` 패널 + 🔄 자동 갱신)
- ✅ 실제 투자금액 입력 필드 완료(단건 실수령 + 일괄 경주별 조정 → 정확 손익)
- ✅ 데이터 churn 운영 규칙 문서화(CLAUDE.md): 워킹트리 churn 정상 · 커밋=명시적 백업/`#백업`만
- `.gitignore` 정리: `.claude/` 제외 · `data/analysis_log/`는 학습 코퍼스로 추적 유지(주석 명시)
- 거리·코스 이력: `rcDist` 수집 준비 완료 · **거리 보유 KRA 엔드포인트 확정이 활성 선행조건**(3계층 파이프라인 잔여)

---

## v2.1.0 (2026-07-04 ~ 07-05) — KRA 실데이터 연동 + 학습 고도화
태그 커밋 `e5f9028`

### 추가된 기능
- **KRA 공공데이터 연동**(data.go.kr, `tools/fetch_kra.py`): 현직기수 104명 실제 복승률(`jockeys.json`), 경주성적 647경주(전적 3건+ 1,120두, `kra_history.json`), 기수통산성적비교 API
- **부진마 역전 학습**(`pattern_learning.json`) + **패턴 자동 발견**(`discovered_patterns.json`)
- **분석 로그 완전 저장**(`data/analysis_log/`) — 배당 타임라인·전적점수·이상감지·결과 원시 보존
- 발주시간 자동 감지(배당판→타이머 자동 시작), 마감 후 일괄 결과 등록, 일본 중앙(JRA)/지방(NAR) 분리
- 실제 투자금액 입력 필드 + 누적 손익 집계, 모바일 반응형 CSS

### 수정된 버그
- 한국 PDF 분석 405 근본 해결(`fitz` import try/except 방어)
- 전적 파서 오탐(334행 오즈표) 방어 + 전적 0두가 기존 전적 덮어쓰기 방지
- 팝업 자동전송 토글 클릭 불가 버그

### 보완점
- 거리·코스·기수이력 세부 미수집(부진마 "거리 변경/기수 교체" 조건 보류)
- 이상감지 UI 3단계(🔴/🟡/🟢) 종합으로 단순화

---

## v2.0.0 (2026-07-02 ~ 07-03) — Chrome 확장 + 백그라운드 자동수집
태그 커밋 `bab0cbf`

### 추가된 기능
- **Chrome MV3 확장**(`chrome-extension/`): 배당판 자동 추출, 전체 자동수집(복승·쌍승·삼복승 원버튼)
- 사설(asyukk) 배당판 지원 + 범용 매트릭스 파서, 3종 시계열 그래프
- 출마표2/DebaTable(keiba.go.jp) 전적 자동수집, 배당 히스토리 + 결과기반 자동학습
- 전적+배당 복합 제거(elimination) 엔진, 경주 결과 자동수집→즉시 학습(v1.9.0/v2.0.1)
- 경주 타이머·마감 임박 알림, 한국경마 PDF전적+배당판 통합분석(전적40%+배당60%)
- **백그라운드 자동수집**(팝업/탭 무관, `chrome.alarms` 하트비트) — v2.0.0

### 수정된 버그
- 배당판 분석 JSON 잘림(Unterminated string), 복승 매트릭스 배당 오매핑(삼각형 구조)
- 삼복승 축마 버튼 클릭, 타임라인 UI, 전적/축마 수집 등 다수 안정화

### 보완점
- 마감 임박 타이밍 정밀도(→ v2.1.0/v2.2.0에서 T-3/T-2/T-1 단계로 고도화)

---

## v1.0.0 (2026-06-29 ~ 07-01) — 기본 분석 시스템
태그 커밋 `743e509`

### 추가된 기능
- **Flask + Claude Vision BMED 분석 엔진**: A/B/C/D 등급(투자비중 45:28:17:10), 복승+삼복승만(단승 없음)
- 배당 이상감지(시계열 급락·괴리), 주로상태/날씨/마체중/삼복승 데이터
- 거리별 적중률 동적 처리, 이상감지 통계·기수DB, 다중 캡처 + 실시간 분석

### 수정된 버그
- 이상감지 속도 개선(1차 캐시 / 빠른 입력 / 2차 백그라운드)

### 보완점
- Chrome 확장 자동화 필요(→ v2.0.0에서 해결)

---

## 관리 규칙
- **새 기능 추가·버그 수정 시 이 파일 최상단 최신 버전 섹션에 즉시 반영**(추가된 기능 / 수정된 버그 / 보완점). 자세한 규칙은 `CLAUDE.md`의 "CHANGELOG 자동 관리" 참조.
- 주요 마일스톤은 `git tag -a vX.Y.Z`로 고정하고 `git push origin --tags`로 백업.
- 되돌리기 방법은 `README.md`의 "🔄 버전 관리 & 되돌리기" 참조.

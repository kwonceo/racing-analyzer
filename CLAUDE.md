# 📌 오늘 상태  (⚠ `tools/archive_claudemd.py` 가 갱신한다 · 손으로 고치지 않는다)

- **날짜** 2026-09-05
- **CLAUDE.md** 67,464자 / 한도 100,000자 (67%) · 측정 2026-09-05 12:58
- **⏳ 열린 항목** 31건  (150줄 넘으면 그날 정리한다)
- **⏳ 승인 대기** 5건
- **▶ 다음 할 것** 운영 pull(00:10 이후) · 오버레이 재진단 · 급락 축 재측정(≈09-15)

⚠ **일지는 최근 1일만 둔다.** 그 이전은 `docs/archive/CLAUDE_일지_YYYY-MM.md` 에 원문 그대로 있다.
⚠ `docs/archive/` 는 **읽기 전용 보관소**다. 살아 있는 규칙을 절대 두지 않는다.


---

# ═══ 상시 · 원칙 ═══

---

# 📇 원칙 색인 1~30 — **측정·수정 전에 여기부터 본다** (2026-08-22 작성)

⚠ 원칙이 서른 개인데 목록이 없어 원칙 28(새 원칙으로 기존 것을 훑는다)을 실행할 수가 없었다.
🔴 **「잡은 사례」가 비어 있는 원칙은 아직 검증되지 않은 것**이다. 근거로 쓸 때 주의한다.

| # | 원칙 | 실제로 잡은 사례 |
|---|---|---|
| 1 | n<30 은 판정 불가 | 여러 안(💎 적중 33건 · 삼복승 적중 10건 등) |
| 2 | 상위 1·3건 제외를 병기한다(극단값) | 8/11 회수 156% → 대박 뺀 76%(149.9배 1건이 32%) |
| 3 | 리플레이 전 실제 코드 경로를 재현했는지 본다 | 경계 시뮬 v1 137.9% → v2 85.6%(`_lowodds_exempt` 미재현) |
| 4 | 판정식은 발동률 먼저, 발동 사례 3건을 눈으로 | 급락 미반영·D 중복·B라인 — 셋 다 다른 것을 재고 있었다 |
| 5 | 함수 통과 ≠ 실데이터 반영. 저장 건수로 확인 | `raw_profile` 0% · `surface` 0% · 4필드 0% |
| 6 | 감으로 박힌 상수는 실측으로 재검증 | `_EST_CAL=2.0`(실측 2.35) · paceBonus 방향 반대 |
| 7 | 「이미 고쳐졌다」를 전제하지 않는다 | `fix_trio_coherence` 뒤에도 위반 11.6% |
| 8 | 비교 전에 **비교 가능한 값인지** 본다 | 아오모리 5R 6배 차이(저장소가 달랐다) · 비율 1.000(자기비교) |
| 8-B | 한 목록 안의 백분율은 분모를 통일 | 45.0%(/282) ↔ 38.6%(/207) |
| 8-C | 모든 비율에 분모를 명시 | 하루에 다섯 번 — 81.6↔69.4 등 |
| 8-D | **검증 코드도 검증한다** | 오탐 83.3% · `trifecta_main` 문자열 오독(100% → 37%) |
| 8-E | 「없다」고 하기 전에 원자료를 연다 | payouts 최상위 · confTop1Conf 95.6% 저장돼 있었다 |
| 9 | except 안 빈 기본값 재저장은 데이터를 지운다 | 와카야마 8R 20틱 + archive 23건 소실 |
| 10 | 외부가 느리면 네트워크가 아니라 헤더를 의심 | oddspark `Accept` 누락 → 9초 tarpit |
| 11 | (결번) | — |
| 12 | 회귀 테스트는 **실제 함수를 호출**해야 한다 | `run_freeze_regression` 이 Fixture 만 읽어 영원히 빨간불 |
| 13 | 필드가 존재하는지 먼저 확인(`.get(X) or 0`) | 경륜에 `totalScore` 없음 → 정렬이 통째로 무의미 |
| 14 | **포함률 최대화 = 시장 복사**. 목표가 아니다 | `ranked` 전체 55.6% = 시장 내재확률 55.6% |
| 15 | 회수율은 **확정배당** 기준으로 낸다 | 마감배당 근사는 27%가 30% 넘게 벗어난다 |
| 16 | 파일 매칭은 **날짜까지** 포함한다 | `regrade_market` · 회수율 측정 · 스냅샷 8건 오염 |
| 17 | 테스트를 만들면 **실패도** 확인한다 | `run_glob_safety` 가 주석 한 줄로 통과됐다 |
| 18 | 감시 항목은 실제 위험한 것만(발동률 5~30%) | I5 파일중복 · I3 데몬스탬프 — 영원히 빨간불이었다 |
| 19 | **고치면 대상에서 빠지는 정의**는 잘못된 정의 | I3 를 고칠수록 분모가 0 이 됐다 |
| 20 | 가드는 **오탐률을 재기 전에** 켜지 않는다 | 출주명단 게이트 오탐 83.3% · netkeiba guard |
| 21 | 방어가 다른 실패를 가리지 않는지 본다 | `io` 미import 977건이 무동작으로 은폐 |
| 22 | 명단은 출마표 단독으로 판정하지 않는다 | 모리오카 7R — 출마표 7두인데 실제 10두 |
| 23 | 판정 경로를 건드리면 **계수기**를 함께 넣는다 | 발주시각 배선을 두 번 틀렸고 두 번 다 계수기가 잡았다 |
| 24 | 폴백을 넣으면 **몇 번 돌았는지 센다** | mb 결측 2,215행 전부 None 인데 17일간 몰랐다 |
| 25 | 경기장 키는 **두 축**으로 본다(정식명·저장 토큰) | 토야마 ↔ 도야마 — 회원 화면에 다른 종목 배당 |
| 26 | 보고에 **표본·정제·구좌·명단**을 함께 적는다 | 같은 이름 수치가 두 배(100.3↔72.5 · 66.2↔25.2 · 1.59↔0.10) |
| 27 | **그 시점에 존재한 값만** 쓴다(시간 축) | 되살린 조합 T-5 가용 16.3% · 발주 후 알림 · 실패 복기 |
| 28 | 원칙을 만들면 **그 자리에서 기존 것을 훑는다** | 원칙 30 훑기 — 다섯 축 중 셋이 성립 안 함 |
| 29 | 정제 필터는 **내부 판정용**. 화면에 안 쓴다 | 켜면 8/11 이 156% → 284%(미적중만 걸러진다) |
| 30 | **결손은 신호처럼 보인다**(보유율을 함께 낸다) | 판형 U자 · 세 신호 +19%p · 급락 편향 62.0% |

⚠ **11번은 결번**이다(과거 번호 재사용 금지 — 대조가 깨진다).
🔴 **27과 30은 짝이다** — 27은 「그때 있었나」(시간), 30은 「없어서 모였나」(결손).
🔴 **26·8-C 도 짝이다** — 둘 다 「무엇을 세는지 적어라」이고, 오늘 두 배 차이가 전부 여기서 났다.

---

# 🔴🔴🔴 [2026-08-21] 원칙 30 — **결손은 신호처럼 보인다**

어떤 값이 없으면 그 행은 **0 · False · 낮은 범주**로 떨어진다.
그 무리가 다른 무리와 성적이 다르면 **「신호를 찾았다」로 착각한다.**

## 하루에 세 번 걸렸다
```
세 신호 우위 +19%p   수집 얕은 경주가 False 로 몰림       → 매칭 고치니 +9.4%p
급락 62.0%           마감 후 살아남은 것만 채워져 있었다   → 소급하니 54.8%
🔴 판형 U자           각질 43.7% 인 달이 「느린 판」이 됐다 → 8월만 보니 소멸
```

## 실물 — 판형 U자
```
2026-07  경주  369   느린 60.7% · 정석 30.9% · 난전  8.4%   🔴 각질 보유 43.7%
2026-08  경주 1458   느린 11.4% · 정석 67.5% · 난전 21.1%   🟢 각질 보유 99.0%
```
🔴 7월의 「느린 판 60.7%」는 **판형이 아니라 각질 미수집**이다.
  각질을 못 읽으면 선행으로 세어지는 말이 줄어 `leadCount` 가 낮게 나오고,
  그 경주가 통째로 「느린 판」으로 분류된다.
⇒ **양극단이 좋다(U자)** 는 데이터 결손이 만든 무늬였다. **철회한다.**
🟢 남는 것은 **난전(선행 4+) 하나**다 — 8월 정제 1,346경주에서
  회수 82.7% · 대박 뺀 71.8% · 배당중앙 2.95배 · 전·후반 유지(82.9 ↔ 82.6).

## 규칙
```
🔴 무리를 나눠 성적을 비교하기 전에, 그 무리가 「값이 없어서」 모인 것은 아닌지 본다.
🔴 각 무리의 **원자료 보유율**을 함께 낸다. 보유율이 다르면 그 비교는 성립하지 않는다.
🔴 **기간 반 분할도 같다.** 그 축의 보유율이 두 기간에서 같은지 먼저 본다.
   다르면 그 분할은 「앞뒤 비교」가 아니라 **「결손 비교」**다.
   실례: T-5 이력이 **7월 0% · 8월 44.6%** — 이 축으로 7↔8월을 가르면 전반은 무의미하다.
```

## 🔴 실제로 한 안이 잘못 기각됐다 — **T-5 고정**
```
7↔8월 분할(내 첫 측정)   전반 B(현행) 71.9 > A(T-5) 70.2  ·  후반 A 72.3 > B 66.6   → 「앞뒤 갈림」 기각
🟢 8월 안에서만 다시     전반 A 70.0 > B 67.5   ·  후반 A 73.6 > B 68.4   → **둘 다 A 승**
   전체 8월 1,868경주    A 회수 72.0% · 대박 뺀 69.9% · 경주당 1.74
                        B 회수 68.1% · 대박 뺀 65.6% · 경주당 2.44
```
🔴 **기각은 철회한다.** 7월에 T-5 이력이 0%라 전반이 통째로 결손 구간이었다.
⚠ 다만 판정 넷 중 둘은 여전히 미달이다 — **적중률 43.5 → 39.9%**(-3.6%p) · **배당중앙 2.50 → 2.30**.
  🟢 대표는 이 둘을 **감수하기로 이미 정했다**(「취소 66%가 더 나쁘다」).
⚠ 취소율은 내 재생에서 **25.2%**(8월 1,868경주)로 나온다 — 66.2% 와 다르다. 재생 방식 차이다.
⏸ **지금 켜지 않는다** — 사흘 관찰 중인 셋과 섞인다. 관찰이 끝나면 1순위 후보다.
  소급 기준값: **회수 72.0% · 대박 뺀 69.9% · 경주당 1.74 · 적중 745/1,868 · 표본 시작 2026-08-01**
⚠ 원칙 27(그 시점에 있었는가)의 짝이다. **27은 시간을, 30은 결손을 본다.**
⚠ 원칙 26(표본·정제를 함께 적는다)에 **「보유율」을 한 줄 더 적는다.**

# 🔴🔴🔴 [2026-08-21] 원칙 29 — **정제 필터는 내부 판정용이다. 회원 화면에 쓰지 않는다**

내부 측정에서 괴리 정제(확정배당 ÷ 마감배당 0.5~2.0)는 **오염 제거**다.
🔴 **그런데 화면에서는 반대로 작동한다** — 정제로 빠지는 경주가 **미적중 쪽에 치우친다.**
```
정제 전후 적중률 차이   8/07 +19%p · 8/11 +34%p · 8/19 +31%p · 8/20 +52%p
8/11 실물              정제 끔 156%  →  정제 켬 **284%**
```
**이유**: 괴리는 **고배당 정답에서 커지고**(마감 직전 배당이 급변한다),
고배당 정답은 대개 **우리가 놓친 경주**다.
⇒ 🔴 화면에 정제를 걸면 **못 맞힌 경주를 빼서 성적을 올리는** 셈이 된다.

🟢 **화면은 「대박 뺀 회수율」로 극단값을 다스린다** — 원본 일별 범위 417%p → **48%p**.
⚠ 정제는 **안·규칙을 비교할 때만** 쓴다. 그때는 여러 안이 **같은 경주 집합**을 쓰므로 편향이 상쇄된다.
🔴 **같은 규칙이라도 「비교용」과 「단일 수치용」은 다르다.** 원칙 26(표본·정제를 함께 적는다)의 짝이다.

# 🔴🔴🔴 [2026-08-21] 측정 보고에는 **표본·정제·용어를 함께 적는다** (원칙 26 신설)

```
같은 것을 쟀는데 두 배 달랐다 — 회수 100.3 ↔ 72.5 · 취소율 66.2 ↔ 28.5
🔴 버그가 아니었다. **무엇을 세는지가 달랐다.**
```
⇒ 앞으로 회수율·적중률을 적을 때는 **반드시 아래 넷을 함께** 적는다. 하나라도 빠지면 그 수치는 인용 금지.

| 항목 | 적을 것 |
|---|---|
| **표본** | 어느 파일에서 · 몇 경주 · 무엇이 있어야 포함되나(결과? 확정배당? 카톡 발송?) |
| **정제** | `measure_recovery` 의 CLEAN_LO/HI(확정÷마감 0.5~2.0) 적용했나 · 안 했으면 명시 |
| **구좌** | 조합 1개 = 1구좌인가 · 경주 단위인가 |
| **명단** | 회원 수신(카톡) ↔ 판정 명단(displayedCombos) 중 어느 쪽인가 · T+1 포함 여부 |

🔴 **정제를 안 걸면 같은 데이터에서 회수율이 30%p 넘게 부풀 수 있다.**
🔴 **「회원 기준」과 「판정 기준」은 다른 수치다** — 실측 격차 회수 +2.75%p · 대박 뺀 +3.47%p.

---

# 🔴🔴🔴 원칙 25 — **경기장 키는 두 축으로 본다: 외부 정식명 · 우리 저장 토큰** (2026-08-10 신설)

## 실사고
2026-08-10 아침에 joCode 전수 실측으로 오매핑 3 · 미등록 4를 잡았다. **그런데 같은 날 오후에
`토야마` 가 또 나왔고, 회원 오버레이에 다른 종목 배당이 나갔다.**
```
아침 방법  oddspark RaceKekka.do 의 <title> = **외부 정식명**(富山競輪)만 봤다
놓친 것    우리가 저장한 raceKey 토큰 `토야마` — 확장이 만든 **한글 표기 변형**
⇒ 외부 소스에는 애초에 없는 문자열이라 그 방법으로는 **구조적으로 못 잡는다**
```

## 규칙
```
🔴 경기장 키를 점검할 때는 **반드시 두 축을 다 본다**:
   ⓐ 외부 소스의 정식명(oddspark title · keiba.go.jp 등) — 오매핑·미등록을 잡는다
   ⓑ 🔴 **우리 저장 파일의 실제 토큰 전수** — 표기 변형·키 갈림을 잡는다
      `data/{odds_history,analysis_log,race_results}/<날짜>_<경기장>_<경주>.json` 의 3번째 토큰
🔴 별칭을 추가하기 전에 **토큰 전·후 대조로 오탐 범위를 낸다**(2026-08-01 설계 2단계).
   「달라지는 토큰」과 「의도 밖 변경 0건」을 숫자로 적는다.
🔴 **종목이 섞이는 지명은 합치지 않는다.** 합치면 두 종목이 한 키가 되어 지금보다 나쁘다.
🔴 별칭만 고치면 **종목 오판은 남는다** — `_KEIRIN_ONLY_RE` 같은 종목 판정 목록에도
   같은 변형을 넣어야 한다. 판정 순서에 따라 정규화 전에 종목이 정해질 수 있다.
```
⚠ 원칙 22(명단은 출마표 단독으로 판정하지 않는다)와 같은 계열이다 — **소스 하나로 판정하지 않는다.**

---


# 🔴🔴🔴 원칙 24 — **폴백을 넣으면 폴백이 몇 번 돌았는지 센다** (2026-08-09 신설)

> **폴백은 결함을 고치지 않는다. 결함을 안 보이게 한다.**

## 실사고
`recommendation_history[].minutes_before` 가 **키 이름 어긋남으로 2,215행 전부 None** 이었다.
그런데 `review_engine` 이 **결측이면 시각 차이로 mb 를 추정**하는 폴백을 갖고 있어
**한 번도 오류가 뜨지 않았다.** 값이 늘 그럴듯하게 나왔기 때문이다.
```
2026-07-23  「mb 결측 항목이 많아 T-5/T-2/T-1 이 같은 항목으로 수렴」 → **폴백을 넣었다**
2026-08-09  진짜 원인(카멜↔스네이크)을 찾았다 — **17일 걸렸다**
```
🔴 **증상을 보고도 원인을 찾지 않았다.** 폴백이 증상을 지웠기 때문에 찾을 이유가 사라졌다.
⚠ 그때 「결측 항목이 많다」를 **숫자로 적었다면**(0.0% = 전부) 즉시 배선 오류로 갈렸을 것이다.
  **「많다」는 관측이 아니다.**

## 규칙
```
🔴 폴백을 넣을 때 **발동 횟수를 센다**(_gate_hit). 도달과 발동을 따로.
   발동률 100% = 폴백이 아니라 **본 경로가 죽은 것**이다.
   발동률 0%   = 그 폴백은 필요 없다(원칙 18 — 안 쓰는 것을 감시하면 노이즈다).
🔴 폴백이 만든 값에는 **추정 표식**을 남긴다(`mbEstimated: True`).
   나중에 「이 결론이 실값 기반인가 추정 기반인가」를 가를 수 있어야 한다.
🔴 폴백을 넣기 전에 **왜 결측인지 한 번은 원자료를 연다**(원칙 8-E).
   결측률이 **100% 면 데이터 문제가 아니라 배선 문제**다 — 폴백을 넣을 자리가 아니다.
```
⚠ 원칙 21(방어가 다른 실패를 가린다)의 짝이다.
  **21 은 방어가 「무동작」을 만드는 것**을, **24 는 폴백이 「그럴듯한 값」을 만드는 것**을 본다.
  🔴 **그럴듯한 값이 더 위험하다** — 무동작은 언젠가 눈에 띄지만, 그럴듯한 값은 결론까지 간다.
⚠ 원칙 23(판정 경로엔 계수기)의 확장이다. 23 은 **조건**을, 24 는 **폴백**을 센다.

---


# 🔴🔴🔴 원칙 23 — **판정 경로를 건드리면 계수기를 함께 넣는다** (2026-08-09 신설)

> **「됐다」와 「탔다」는 다르다.** 문법 통과와 서버 200 은 **「탔다」의 증거가 아니다.**

## 이번 주에 계수기가 세 번 값을 했다
```
① 경륜 wide     도달 1727 · 발동 801 인데 finalQuinellas 1.53개 그대로
                ⇒ 「컷이 병목이 아니다」를 **하루에** 갈랐다(없었으면 며칠 짐작했다)
② 발주시각 1차   계수기 0 → **종목 정정 블록 안**이라 확장이 sport 를 정확히 보내면 건너뛴다
③ 발주시각 2차   옮긴 뒤에도 계수기 0 → **들여쓰기 8칸**이라 여전히 try 안이었다
                ⇒ 4칸으로 내리니 발동 1. **같은 배선을 두 번 틀렸고 두 번 다 계수기가 잡았다**
```

## 규칙
```
🔴 판정·수집·추천 경로에 조건을 넣으면 **도달과 발동을 따로** 센다(_gate_hit).
   도달 0 = 그 줄에 안 온다(배선·위치 문제)
   도달 O · 발동 0 = 조건이 안 맞는다(임계·데이터 문제)
   ⇒ 이 둘을 구분 못 하면 원인 추적이 짐작이 된다.
🔴 실발동을 **실데이터로** 확인하기 전에는 "고쳤다"고 적지 않는다.
⚠ 계수기가 0 인 채로 며칠 지나면 **그 자체가 이상 신호**다(원칙 21과 짝).
```
⚠ 2026-08-04 「도달 계수기가 죽어 있었다(`io` 미import·977건)」의 연장이다.
  그때는 계수기가 고장났고, 이번엔 계수기가 **배선 오류를 잡았다.**


## ⇒ 원칙 8-E — **「없다」고 보고하기 전에 원자료를 한 번 연다**
```
자기 코드가 못 찾은 것을 "데이터가 없다"로 옮겨 적지 않는다.
🔴 판정 순서: ⓐ 원자료 1건을 열어 키 목록을 직접 본다 → ⓑ 그다음에 없다고 말한다
⚠ 특히 **CLAUDE.md 에 "채웠다·저장한다"는 기록이 있는데 0 이 나오면 구조적으로 불가능하다.**
  그 모순이 곧 계측 오류의 신호다(①이 그렇게 잡혔다).
```
⚠ 원칙 8-D(검증 코드도 검증한다)의 확장이다. 8-D 는 테스트를, 8-E 는 **조회식**을 의심한다.


# ✅ [2026-08-05] 원칙 18 정제 — **감시 항목과 교정 규칙에 같은 구간을 쓰지 않는다**

```
감시 항목 : 발동률 5~30% 가 적정. 0% 에 가까우면 표본이 안 쌓이고
            100% 에 가까우면 변별력이 없다(Gemini WARNING 99.5% 전례)
🔴 교정 규칙 : **정밀도가 우선**이다. 낮은 발동률이 오히려 안전하다
            (틀린 교정은 곧 데이터 훼손이므로 적게·정확하게 발동해야 한다)
```
실례 — 축소 판정: 발동 3.1% 로 구간 밖이지만 **되살린 마번의 25% 가 실제 3착**에 들었다.
발동률을 6.6%(D안)로 올리면 정확도가 25% → 그대로인데 오교정만 는다.
⚠ 「옳음 %」는 **하한**이다 — 되살린 말이 3착 밖이면 확인할 수단이 없다(오탐이 아니라 판별 불가).

---


# 🔴🔴🔴 원칙 22 — **명단 판정은 출마표 단독으로 하지 않는다**(2026-08-02 규칙 · 재확인)

```
roster = 출마표(form/starters_store) ∪ 서버 직접수집(oddspark · netkeiba)
```
🔴 **출마표는 부분수집이 실존한다.** 출마표만 명단으로 쓰면 **정상 배당을 오염으로 판정**한다.
2026-08-02 에 이것으로 **오탐 83.3%** 를 냈고 그때 고쳤는데, **2026-08-04 에 같은 실수를 두 곳에서 반복했다.**
```
실물 근거 — 모리오카 7경주: 출마표 **7두**인데 oddspark 가 45조합·최대마번 **10** 을 44틱 일관 송신.
            🔴 **결과 3착이 10번.** 진짜 10두 경주이고 출마표가 덜 수집된 것이다.
```

# 🔴🔴🔴 원칙 21 — **방어를 넣을 때 그 방어가 다른 종류의 실패를 가리지 않는지 본다**

어제 원칙 9(빈 기본값 금지)를 지키려고 `_gate_hit` 에 **「읽기 실패면 저장을 건너뛴다」** 를 넣었다.
그 방어가 **`import io` 누락(NameError)을 조용한 무동작으로 바꿔 977건을 은폐**했다.
```
방어 없을 때  : NameError → disk={} → 빈 값으로 덮어씀 → 값이 지워진다(눈에 보인다)
방어 넣은 뒤  : NameError → except 가 삼킴 → 3회 재시도 → return → 🔴 **아무 일도 안 일어난다**
```
🔴 **방어는 「잘못된 동작」을 「무동작」으로 바꾼다. 무동작은 정상과 구분되지 않는다.**
⇒ 규칙 셋:
① **방어가 발동한 횟수를 센다.** 977번 발동하는 방어는 방어가 아니라 고장이다.
② `except Exception` 이 무엇까지 삼키는지 본다 — **NameError·AttributeError·ImportError**는
  「일시적 실패」가 아니라 **배선 오류**다. 재시도로 낫지 않는다.
③ 방어를 넣을 때 **「이 방어가 계속 발동하면 어떻게 알아채나」** 를 함께 답한다.
  답이 없으면 그 방어는 침묵 장치다.
⚠ 원칙 9와 충돌하지 않는다 — 빈 값으로 덮어쓰지 않는 것은 그대로 옳다.
  빠진 것은 **「건너뛴 횟수를 세는 것」** 이었다.

---


## ⇒ 절차 (원칙 8 프로세스판)
🔴 **프로세스를 판정할 때는 ⓐ부모-자식 관계와 ⓑ명령줄을 먼저 확인한다.**
  · `netstat` 스냅샷 하나로 "고아"·"2벌"·"우회 실행" 을 규정하지 않는다.
  · PID 가 둘이라고 프로세스 쌍이 둘이 아니다(리로더는 항상 부모+자식).
  · 포트를 잡은 것처럼 보여도 **그 프로세스가 우리 서버인지**를 명령줄로 확인한다.
⚠ 오늘 실측 결과 **아무것도 종료하지 않았고 종료할 필요도 없었다.**

## 🔴 원칙 20 — 오탐률을 재서 **범위 과다를 켜기 전에 잡았다**
```
1차 설계: `_track_norm` 결과를 **무조건** 적용
회귀 테스트 [5] 신설 후 실측 → 🔴 **오탐률 83.3%**
  저장된 경기장 토큰 66개 중 **18종이 한꺼번에 바뀐다**:
  고치→코치 · 오오이→오이 · 고쿠라→코쿠라 · 타마노→다마노 · 한자 12종 …
⇒ 방향은 옳지만 **한 번에 18종을 바꾸면 기존 파일과의 분리가 대량으로 새로 생긴다.**
  그리고 승인 범위는 "中京/중경/추쿄/주쿄" 였다.
```
✅ **`_INGEST_NORM_ALLOW`(승인된 원문 + 사유)로 좁혔다 → 오탐률 0.0%**
🔴 **이 항목이 없었으면 18종을 그대로 켰을 것이다.** 원칙 20의 첫 사전 차단 사례다.

## 적용
```
① _TRACK_GROUPS["추쿄"] 에 중경·주쿄·나카교 추가
   ⚠ 넣기 전 **영향 범위 실측**(2026-08-01 설계 2단계): 토큰 66개 전·후 대조 →
     달라지는 키 **中京 계열 3개뿐 · 의도 밖 0건**
② triple_ingest 진입부 → _ingest_normalize_rk(rk, body)
   안전장치 4개: try/except 격리 · 🔴 실패·범위밖이면 **원문 그대로(막지 않는다)** ·
                 변환 로그(조용히 안 바꾼다) · 회귀 테스트 오탐률 항목
   원문 보존: body["venueRaw"]
③ _JRA_TRACKS 에 추쿄·나카교 / _JP_TRACKS 에 나카교 추가
   🔴 **내 변경이 만든 구멍을 막는 것이다** — 이제 `추쿄` 로 저장되므로 그 목록에 없으면
     중앙 판정과 한국 전적 오매칭 가드가 **새 키를 통째로 놓친다.**
```

# 🔴 [2026-08-02] **원칙 20 — 가드는 오탐률을 재기 전에는 켜지 않는다**

> 하루에 **세 번** 같은 실수를 했다. 안전장치가 정상 동작을 막았다.

- **netkeiba guard**: 간격 미달을 **거부**해 JRA 발주시각 36건이 전부 실패 → **중앙경마 0경주 수집**
- **출주명단 게이트(1판)**: 출마표만 진실로 써서 **오탐 83.3%**(자를 90개 중 75개가 정상)
- **카카오 발송 차단(1판)**: 경주 **전체를 보류**해 오염 11개 때문에 **정상 17개가 함께 막힘**
  🔴 호후 4경주는 **적중한 `[4,6]`(16.8배)이 안 나갈 뻔했다.**

⇒ 규칙
- 🔴 **새 가드를 켜기 전에 "막은 것 중 정상이 몇 %인가"를 소급으로 잰다.**
- 🔴 **오탐률 항목을 회귀 테스트에 넣는다**(`run_roster_gate` ⑦ — 0%가 아니면 rc=1).
- 🔴 **가드는 "확실히 없는 것"만 막는다.** 애매하면 통과시킨다 — **오탐이 더 위험하다.**
- ⚠ **막은 대가**도 함께 잰다(막는 것은 공짜가 아니다).

## ✏️ 정정 — "출마표가 넓다"는 확인 없는 추정이었다
```
(내 판단) oddspark 는 취소마로 좁다 → 출마표(form)를 진실로 쓴다
(실측)    🔴 **출마표가 더 좁았다** — 소노다 3R 출마표 최대 6 ↔ 실제 배당 최대 12
```
🟢 **"넓은 쪽이 안전하다"는 방향은 맞았다.** 🔴 **어느 쪽이 넓은지를 확인 없이 추정한 것이 틀렸다.**
⇒ 현행: `roster = 출마표(form) ∪ 서버 직접수집(oddspark·netkeiba)`.
  ⚠ `curQ` 는 쓰지 않는다 — **오염된 private 배당이 그 안에 있다**(호후 3R 의 55조합).

## 🧪 자체 검증 원칙 (2026-07-30 정리 · 세션 리셋 대비)
> 하루 동안 같은 유형의 실수가 반복돼 원칙으로 고정한다. **측정 전에 이 목록을 먼저 읽을 것.**

1. **`n<30` 은 판정 불가** — 명시하고 **결론에 쓰지 않는다.** 방향 참고까지만.
2. **상위 1건·3건 제외 수치를 항상 병기** — 극단값 착시가 오늘만 4번(급등 20~50% · 155.6배 1건 ·
   도야마 33.5배 · EV 강등분 108.6%→3건제외 51.7%).
3. **리플레이 전 '실제 코드 경로를 재현했는지' 먼저 확인** — 면제·예외 로직을 빠뜨리면 숫자가 정교해도 무의미.
   실제 사고: 경계 시뮬 **v1 137.9% → v2 85.6%**(`_lowodds_exempt` 미재현).
4. **판정식은 발동률 먼저, 성적 나중.** ①발동률(적정 **5~30%**) → ②**발동 사례 3건을 직접 열어**
   의도한 상황인지 육안 확인 → ③성적(적중률·회수율·z). 순서를 바꾸면 엉뚱한 지표를 채택한다.
5. **함수 단위 통과 ≠ 실데이터 반영** — 반드시 **저장 건수로 확인 후** 보고한다.
   오늘 3회 반복(`raw_profile` 0% · `surface/trackCond` 0% · 4필드 0%). 전부 "배선 시각이 경주 시간대 밖"이었다.
6. **감으로 박힌 상수는 실측으로 재검증** — 오늘 3건 발견:
   `_EST_CAL=2.0`(실측 2.35·배당대별 2.70↔1.40) · `paceBonus` 매핑 방향 반대 · EV 곡선 1%↔실측 3.2%.
7. **"이미 고쳐졌다"를 전제하지 말 것** — `fix_trio_coherence` 가 강제 정합을 하는데도 위반이 11.6% 남았다.
8. 🔴 **두 값을 비교하기 전에 '비교 가능한 값인지' 먼저 확인한다** — **같은 시각·같은 저장소·같은 정의**인가.
   서로 다른 저장소(`triple_store` ↔ `odds_history`)나 서로 다른 시점의 값을 비교하면 **없는 차이가 만들어진다.**
   · **실패 사례 A (2026-07-30)**: 아오모리 5R "oddspark 105.0 ↔ private 17.9 **= 6배 차이**"로 보고했으나,
     105.0은 `triple_store`(활성 캐시), 17.9는 `odds_history`(private 스냅샷)였다. 실제로 그 경주의
     **oddspark 유효 스냅샷은 0개**여서 **두 소스가 같은 조합을 동시에 들고 있던 적이 없다.**
     → 그 위에 "열 밀림(복연승 오독)" 가설과 "회원 실수령이 더 낮을 것"이라는 사업 우려까지 쌓였다.
     13경주 재대조 결과 **비율 중앙 1.000 · private가 체계적으로 낮은 경주 0건**으로 전부 기각됐다.
   · **실패 사례 B (2026-07-30)**: "oddspark 마감 직전 None **4틱 연속**"으로 보고했으나,
     그 스냅샷들은 `minutes_before=None`(**발주시각 미확정**)이지 마감 직전이 아니었다.
     전 기간 재측정: 마감 10분 내 oddspark 스냅샷 **9,609개 중 빈 값 0건(0.0%)**.
   · **두 건 모두 원칙 4(발동 사례를 직접 열어 확인)를 건너뛴 결과다.** 이상해 보이는 수치일수록
     **먼저 원자료를 열어야 한다** — 놀라운 발견일수록 측정 오류일 확률이 높다.
   · ✅ **차단 성공 사례 C (2026-07-30)**: 사설↔공식 확정배당 대조에서 첫 대조쌍이
     `finalOdds` ↔ 같은 레코드 `payouts` 였고 **비율 23/23 전부 1.000**이었다. 그런데 `payouts` 는
     **`finalOdds` 에서 파생된다**(app.py:17796 — finalOdds 1순위, 공식은 폴백) → **자기비교**였다.
     **1.000 이라는 "완벽한 일치"도 의심 신호다** — 값이 너무 안 맞을 때만 의심하지 말고,
     **너무 잘 맞을 때도 파생 관계를 의심**할 것. 독립 대조쌍(작성 시각·작성 경로가 다른 저장소)을
     따로 찾아야 비교가 성립한다. 상세는 「사설 ↔ 공식 확정배당 대조」 절.
   · 🔴 **배당을 비교할 때는 '같은 조합인지' 게이트를 먼저 통과시킬 것** — 정합 스윕은 **착순도 정정**하므로
     착순이 다르면 두 배당은 애초에 **다른 조합의 값**이다(위 대조에서는 착순 동일 52·불일치 0 확인 후 측정).
8-B. 🔴 **한 목록 안의 백분율은 분모를 통일한다(2026-07-30 신설).**
   같은 표·같은 목록에서 항목마다 분모가 다르면 **합이 100%가 안 되고 독자가 비율을 오독한다.**
   · **실패 사례**: 막판 편입 측정 보고에서 "복승 표시에 그 말 포함 127(45.0%)"은 **/282**,
     "그 말조차 없음 80(38.6%)"은 **/207** 이었다. 두 값이 같은 목록에 나란히 있었지만 분모가 달랐다.
     207 로 통일하면 **61.4% / 38.6%** (합 100%)가 맞다.
   · **규칙**: 목록 머리에 `⚠ 분모 통일: 아래 %는 전부 /N` 을 명시하고, 분모가 바뀌면 **표를 분리**한다.

8-C. 🔴 **모든 비율에 분모를 명시한다(2026-07-31 신설).**
   두 숫자가 안 맞으면 **"틀렸다"고 판정하기 전에 분모부터 확인한다.** 같은 목록 안의 백분율은 분모를 통일한다.
   · 2026-07-30~31 하루에 **다섯 번** 이 문제가 났다: 0틱 경주 제외(81.6%↔69.4%) ·
     45.0%(/282) ↔ 38.6%(/207) · 마 단위 ↔ 조합 단위 · **조합 단위(n=722) ↔ 경주 단위(n=35)** ·
     `displayedCombos` 없는 610경주가 B안 분모에서 이탈.
   · **"엣지 0.507 vs 적중 0" 은 모순이 아니었다** — 분모가 달랐을 뿐이다.

8-D. 🔴 **검증 코드도 검증한다(2026-07-31 신설).**
   **"테스트 통과"가 곧 "정상"이 아니다.** 테스트가 **의도한 것을 재는지** 발동 사례를 눈으로 확인한다.
   · 실패 사례: Phase A 단위 검증 22건 중 2건 실패 → 코드 결함이 아니라 **검사식이
     `_excluded` 필드의 문자열을 오탐**한 것이었다. 원자료를 직접 열어 실제 누출 0을 확인했다.
   · 회귀 테스트에도 같이 적용한다 — **통과·실패 둘 다** 사례를 열어본다(원칙 4와 같은 취지).

9. 🔴 **`except` 안에서 빈 기본값을 세우고 그것을 다시 저장하는 경로는 데이터를 지운다(2026-07-30 신설).**
   `except: pass` 는 '못 보게' 할 뿐이지만 `except: doc = {"snapshots": []}` 뒤의 저장은 **지운다** — 질이 다르다.
   · **실사고**: 7/30 14:09 와카야마 8경주 — 스냅샷 **20틱 + `archive_snapshots` 23건이 1틱/10건으로 초기화**.
     `archive_snapshots` 는 설계상 **영구 보존**인데도 잘렸다(복구 불가).
   · **연쇄 구조**: 두 스레드가 같은 tmp 를 공유 → 섞인 내용이 rename → **원본이 손상 JSON** →
     다음 reader 가 파싱 실패 → **빈 문서 폴백 → 전량 덮어쓰기**. 저장 실패 하나가 전량 소실이 된다.
   · **원칙**: 손상을 만나면 **덮어쓰지 말고 격리하고 그 사이클 저장을 건너뛴다**(`_json_load_guard`).
     격리는 **이동이 아니라 사본**(옮기면 그것도 소실). 앞부분 유효 JSON 복구를 먼저 시도한다
     (7/28 전수 복구에서 19건 중 13건이 'Extra data' 유형이었다).
   · **점검법**: `json.load` 의 `except` 에서 빈 dict/list 를 세우는 지점을 AST 로 훑고,
     그중 **로드 → 수정 → 같은 경로 저장**인 것만 고른다(읽기 전용 접근자는 위험하지 않다).
     7/30 기준 app.py 안에 **62곳**이 있고 그중 핫 경로 **5곳**을 처리했다.
10. 🔴 **외부 사이트가 느리면 네트워크가 아니라 헤더를 의심한다(2026-07-30 신설).**
   · **DNS·TCP·TLS 를 분리 측정하면 즉시 갈린다.** 실측: DNS 0.009초 · TCP 0.005초 · TLS 0.031초인데
     `urlopen` 은 **9.32초**였다 → 네트워크가 아니다.
   · 헤더를 하나씩 넣어 분리: UA만 **10.70초** / +`Accept-Encoding` 8.23초 / **+`Accept: */*` 0.23초**.
     oddspark(Akamai)는 `Accept` 가 없으면 **약 9초 tarpit**을 건다(브라우저는 항상 보낸다 = 봇 판별).
   · ⚠ 이런 상황에서 **동시성을 올리는 것은 오답**이다 — 요청당 비용이 문제인데 요청 수를 늘리면
     WAF 위험만 커진다. 실제로 7/29 "수집 경합" 진단이 이 함정이었다(실측으로 기각).
   · ⚠ **사이트마다 다르다.** 같은 조치를 일괄 적용하지 말 것 — `keiba.go.jp` 는 0.16~0.20초로 문제가 없어
     `_nar_fetch` 는 건드리지 않았다.

## 🧪 [2026-07-31] 원칙 12 — 회귀 테스트는 **실제 함수를 호출해야 한다**

> 🔴 **오늘 실제로 걸린 사례다.** "오늘 문제된 경주를 스냅샷으로 떠서 고정"이라는 지시를
> `run_freeze_regression.py` 가 **고정 Fixture 만 읽는 형태**로 구현했다.
> 그 파일은 `_build_analysis_log` 를 **한 번도 호출하지 않는다**.
> ⇒ Fixture 는 *이미 오염된 과거를 찍어둔 사진*이라 **코드를 고쳐도 영원히 초록이 되지 않는다.**
> 그런데도 "동결하면 4개 전부 초록이 된다"고 **두 번 단언**했다. 확인 없이 한 말이었다.

- **테스트가 무엇을 읽는지 먼저 본다.** 고정 파일만 읽으면 그것은 *역사 기록*이지 *동작 검증*이 아니다.
- **동작 검증은 실제 함수를 통과시켜야 한다.** 입력을 넣고 **출력이 바뀌는지**를 봐야 한다.
- **Fixture 를 지우지 않는다.** 외부 앵커(회원이 받은 카톡 원문 = 파이프라인 밖의 사실)로서
  독립적 가치가 있다. 내부 저장값끼리 비교하면 둘 다 오염됐을 때 "일치"가 나와 통과해 버린다.
- 🔴 **역할을 분리한다**:

| 파일 | 역할 | 등급 |
|---|---|---|
| `tests/run_freeze_regression.py` | 외부 앵커 · 과거 사실 기록 | 🟡 `EXPECTED_FAIL` **영구 유지**(초록 불가) |
| `tests/run_freeze_behavior.py` | 실제 동결 함수 호출 · 동작 검증 | 🔴 **차단 등급**(실패 시 커밋 불가) |

- ⚠ "고치면 초록이 된다"는 **테스트를 돌려본 뒤에만** 말한다. 원칙 7("이미 고쳐졌다" 전제 금지)의 확장이다.

## 🔴 [2026-07-31] 원칙 14 — **포함률 최대화는 목표가 아니다**

> `ranked` 전체 조합 55.6% = 시장 내재확률 55.6% 는 **우연이 아니라 수학적으로 같은 값**이다.
> **컷을 없애는 것은 "시장을 더 정확히 복사하는 것"** 이다.
> **포함률이 높다 = 시장과 같아진다 = 공제율(약 25%)만큼 진다.**
> 🔴 **목표는 시장이 틀린 곳에서 맞는 것이다.**

- ⚠ **"새 데이터 없이 3.7%p"라는 표현을 쓰지 않는다** — 그건 **포함률**이지 회수율이 아니다.
- ⇒ **"상위10 컷 확대"를 우선순위에서 내린다.**

### 🔴 원칙 15 — 회수율은 확정배당 기준으로 낸다
- **모든 리플레이는 확정배당 기준**으로 하고, 마감배당을 쓸 때는 **"근사"를 명시**한다.
- ⚠ **"현행 106.2%"·"67.6%" 는 단독 인용 금지**(혼용·경마 전패 셈).
  🔴 **신뢰 가능한 유일한 값은 95.1%(499구좌·07-22~31)** 이다.

### 🔴 원칙 16 — **파일 매칭은 날짜까지 포함한다**
`glob` 으로 경주를 찾을 때 **경기장+경주번호만으로는 안 된다.** 같은 경기장이
여러 날 개최하므로 **다른 날 데이터가 섞인다.** 반드시 `YYYY_MM_DD_` 접두사를 붙인다.
⚠ 오늘 이 유형이 **두 번** 나왔다(`regrade_market.py` · 회수율 측정).

## 🔴 [2026-08-01] 원칙 17 — **테스트를 만들면 그 테스트가 실패하는 것도 확인한다**

> 통과만 확인하면 **아무것도 안 재는 테스트가 통과한다.**

### 오늘 그 증명이 나왔다
`tests/run_glob_safety.py` 가 어제 위반 1건을 잡았는데, **내가 단 주석 한 줄로 통과**됐다.
```
검사 정규식: _DATE_HINT = r"...|date|day"
검사 대상  : _pat = "*_%s.json" % slug   # date-filtered below
🔴 매칭된 것: 주석의 "date"
```
그리고 **한 줄 단위 검사**라 `_pat` 선언과 `glob()` 호출을 **변수로 분리하면 그대로 빠져나갔다.**

### ⇒ 자기검증 의무
테스트를 만들거나 고치면 **반드시 둘 다** 확인한다:
1. 정상 코드에서 **통과(rc=0)**
2. 🔴 **위험 코드를 일부러 주입했을 때 실패(rc=1)**

`run_glob_safety.py` 검증 실측: 주입 시 `rc=1` 검출 · 제거 시 `rc=0`. **오탐 0건.**

## 🔴 [2026-08-01] 원칙 18 — **감시 항목은 "실제 위험한 것"만 센다**

오늘 같은 판단이 **두 번** 나왔다.
| | 항목 | 문제 | 조치 |
|---|---|---|---|
| ① | **I5** 파일 중복 | "중복 자체는 정상"이라 **영원히 빨간불** | → 회귀 테스트로 이관 |
| ② | **I3** 데몬 스탬프 | 60초 데몬까지 목표에 넣어 **영원히 2/5** | → 주기 ≥5분만 대상 |

- 🔴 **구조적으로 못 채우거나 항상 정상인 항목은 감시가 아니다.**
  회귀 테스트나 목록으로 옮긴다.
- 🔴 **판정: 발동률이 0%에 가깝거나 100%에 가까우면 감시 항목으로 부적합**(적정 5~30%).
- ⚠ 항상 빨간 항목은 **무시하게 된다** — Gemini WARNING 99.5% 가 무의미했던 것과 같다.

### ⚠ 그리고 함정이 하나 더 있었다
I3 를 "긴 주기 sleep-first 데몬"으로 정의하니 **고칠수록 분모가 사라졌다**
(수정하면 sleep-first 가 아니게 되므로). ⇒ **이미 스탬프를 붙인 데몬도 분모에 포함**한다.
🔴 **"고치면 측정 대상에서 빠지는" 정의는 잘못된 정의다.**

## 🔴 [2026-08-01] 원칙 19 — **고치면 측정 대상에서 빠지는 정의는 잘못된 정의다**

(분모 원칙 계열 — 8-B·8-C·16 과 함께 본다)

### 오늘 사례
I3 를 **"긴 주기 sleep-first 데몬"** 으로 정의했더니, **고칠수록 sleep-first 가 아니게 되어
분모가 0 이 됐다**(`0/0`). 두 곳을 다 고쳤는데 측정 대상이 사라진 것이다.
⇒ **이미 스탬프를 붙인 데몬도 분모에 포함**하도록 고쳤다 → `2/2`.

### 전수 확인 — 같은 함정이 있는 항목
| 항목 | 분모 | 고치면 줄어드나 |
|---|---|---|
| D2a·D2b 저장 실패 | 당일 로그의 WinError 건수 | 🟢 **0 이 목표**라 정상(줄어드는 게 성공) |
| D6 동결 성공률 | 당일 `freeze_log` 적재분 | 🟢 분모는 시도 수, 분자가 성공 |
| D7 모듈 로드 실패 | .py 자동 탐색 전체 | 🟢 분모 고정 |
| I1·I4 | 당일 경주 | 🟢 분모 고정 |
| 🔴 **I3** | ~~긴 주기 sleep-first~~ | **수정 완료**(스탬프 보유분 포함) |

⇒ **분모가 "아직 안 고친 것"으로 정의되면 위험**하다. 분모는 **모집단**이어야 하고,
   **분자**가 "고친 것"이어야 한다.

## 🔴 [2026-08-01] 원칙 17 두 번째 실증 — **테스트가 `glob` 만 봤다**

`run_glob_safety.py` 는 어제 "위반 0건", 오늘 자기검증 4/4 통과였다.
그런데 **실제 위반이 화면에 나왔다.**
```
_snapshot_index 가 쓰는 방식:  glob 사용 ❌ · os.listdir 사용 ✅
⇒ 검사 대상 밖이었다.
```
- 🔴 **`app.py` 안에 `os.listdir` + 경주 매칭이 52곳** 있다. 전부 검사 밖이었다.
- ⇒ **검사 범위를 `glob` 에서 `os.listdir`·파일명 파싱까지 넓혀야 한다.**
- ⚠ **원칙 17 의 두 번째 실증**: 통과는 "그 방식으로는 안 걸린다"는 뜻일 뿐이다.


---

# ═══ 상시 · 절차와 규칙 ═══

---

# 🔴🔴🔴 [필수·무조건] 보고 규칙 — **모든 보고는 `docs/REPORT.md` 에 남긴다** (2026-08-12)

```
🔴 이것은 선택이 아니다. **답변을 화면에 내기 전에 파일에 먼저 적재한다.**
   적재하지 않은 보고는 **보고가 아니다.**
```

## 🔴 지킬 것 (예외 없음)
```
① 3단 보고(1부·2부·3부)를 낼 때마다 **매번** _report_append 를 호출한다
② 조사만 했어도 · 측정만 했어도 · 코드 무변경이어도 **전부 적재한다**
③ 적재 → 화면 답변 → 커밋 순서. 🔴 적재를 뒤로 미루면 빠뜨린다
④ 커밋 메시지를 쓰는 작업이면 **적재부터 하고 커밋에 함께 넣는다**
```
🔴 **두 번 어겼다**(2026-08-12): 부담중량 조사 · 8/11 배선분 조사.
  둘 다 화면에는 답했는데 파일에는 없었다. **규칙을 만든 직후에도 또 빠뜨렸다.**
  ⇒ 그래서 「필수·무조건」으로 못 박는다. **잊었으면 그 자리에서 즉시 등록한다.**
⚠ 대표가 밖에서 `/api/report` 로 읽는 것이 이 파일이다 — **비면 아무것도 못 본다.**

```
3단 보고(1부 쉬운 설명 · 2부 예상 · 3부 결과)를 낼 때마다
같은 내용을 docs/REPORT.md 에 **반드시 적재한다.**
화면에만 답하고 끝내지 않는다.
```
🔴 **왜**: 2026-08-12 부담중량 조사 답변이 화면에는 나왔는데 파일에는 안 쌓였다.
  **적재는 자동이 아니라 매번 호출해야 하는 구조**라 한 번 빠뜨리면 그대로 사라진다.
  대표가 밖에 있을 때 `/api/report` 로 읽는 것이 이 파일이다 — **비면 못 본다.**

## 적재 방법
```python
# app.py 를 import 해 _report_append 를 호출한다(서버 재기동 불필요 · 읽기 전용 계층)
m._report_append(제목, 1부, 2부, 3부, meta={...})
```
⚠ **보고 직후에 한다.** 커밋 전에 하면 그 보고가 그 커밋에 함께 들어간다.
⚠ 맨 위가 최신이다. 400KB 를 넘으면 `docs/report_archive/` 로 회전한다(지우지 않는다).

## 세 창구를 함께 본다
```
GET  /api/next?n=1     세션 시작 때 **먼저** 읽는다(&ack=1 로 처리 표시)
POST /api/next         대표가 명령어를 써두는 곳
GET  /api/report?n=1   보고를 읽는다(&format=md 로 읽기 편하게)
```
🔴 셋 다 **로컬 전용**이다. 외부에 열지 않는다.

---

# 🔴🔴 수정 전 규칙 — **위험목록을 먼저 조회한다** (2026-08-12 신설)

```
코드를 고치기 전에 docs/codemap/위험목록.md 에서 그 함수를 찾는다.
있으면 그 자리에 적힌 **사고 사례**부터 읽는다.
```
🔴 **왜**: 같은 유형의 사고가 반복된다. 빈값 폴백·tmp 충돌·키 탈락·감으로 박힌 상수·
  sleep-first 데몬·가드 범위 — 여섯 다 **이미 한 번 이상 실제로 터진 것**이다.
⚠ 위험목록은 「무엇이 어디에」만 담는다. **「왜 결함인가」는 안 담는다** — 판단은 사람이 한다.

## 조회 순서
```
① docs/codemap/위험목록.md 에서 함수명 검색       ← 있으면 사고 사례부터
② docs/codemap/00_INDEX.md 에서 소속 섹션 확인    ← 맨 위 신선도 배너도 함께 본다
③ 그 섹션 파일에서 Caller·Callee 확인            ← 누가 부르는지 모르고 고치지 않는다
④ 낡았으면 python tools/build_codemap.py 재생성
```

## 위험목록 여섯 (2026-08-12 실측)
```
① 빈값 폴백 재저장     69곳(핫경로 18)  except 안에서 빈 dict 를 세우고 그대로 저장하는 곳
② os.replace tmp·락    29곳(락없음 27)  같은 tmp 를 두 스레드가 공유하면 원본이 깨진다
③ 파서 → 저장행 키 차집합              🔴 전용 도구가 정확하다 · tools/audit_key_drift.py
④ 전역 상수 참조 전수  153개           감으로 박힌 값이 여러 곳에서 읽힌다
⑤ while True + sleep   21곳           ⚠ 실측 6곳과 기준이 다르다(주기 5분 이상만 셀 때)
⑥ 데이터 가드 조건     23곳           가드가 무엇을 감싸는지 · 언제 걸리는지
```
⚠ ⑤의 21곳과 실측 6곳은 **세는 기준이 다르다.** 코드맵은 `while True`+`sleep` 이면 전부 세고,
  실측은 **주기 5분 이상**만 센다. 어느 쪽이 틀린 게 아니다 — 기준을 확인하고 쓴다.

## 미분류 510개를 고칠 때
```
🔴 지도의 목적은 「고칠 때 찾는 것」이다. 전부 분류하지 않는다.
99_미분류.md 맨 위 두 목록을 본다:
  · 최근 30일 손댄 횟수 상위 50
  · 호출되는 횟수 상위 50
🔴 두 목록이 겹치는 14개가 최우선이다(_area_num · _canon_rk · _race_result_id …).
⚠ 나머지 460개는 **미분류로 둔다.** 한 번도 안 건드린 함수는 지도에 없어도 된다.
```

---

# 🔴🔴 세션 시작 규칙 — **`/api/next?n=1` 을 먼저 읽는다** (2026-08-12 신설)

```
세션을 시작하면 GET http://127.0.0.1:8011/api/next?n=1 을 **먼저 읽는다.**
읽고 착수했으면 GET /api/next?n=1&ack=1 로 처리됨을 표시한다.
```
🔴 **왜**: 종전에는 대표가 명령어를 매번 복사해 붙여넣었다. 길면 잘리고 외부에서는 아예 못 한다.
  이제 대표가 서버에 써두면 되고, **대표는 짧게만 말해도 된다.**
⚠ 대기 중인 명령어가 없으면 `pending: 0` 이 온다 — 그때는 대화의 지시를 따른다.

## 창구 두 개 (둘 다 로컬 전용 · 외부 노출 금지)
```
POST /api/next        본문 텍스트를 docs/NEXT.md 에 쌓는다(최신이 위 · 시각 자동)
GET  /api/next?n=1    대기 중인 명령어를 읽는다(&ack=1 이면 처리됨 표시 · &format=md)
GET  /api/report?n=1  3단 보고를 읽는다(docs/REPORT.md · &format=md)
```
🔴 `_next_local_only()` 가 127.0.0.1·::1 외 요청을 **403** 으로 막는다.
🔴 CORS 는 `http://localhost` · `http://127.0.0.1` 출처만 허용한다(정규식 대조).
⚠ 400KB 를 넘으면 `docs/next_archive/` · `docs/report_archive/` 로 회전한다. **지우지 않는다.**

## ✏️ 배선 중 실측으로 잡은 것
`ack` 표식이 `[처리됨 07:21:24]` 처럼 **시각이 붙는데** 판정을 `"[처리됨]"` 으로 해서
영영 안 걸렸다. ack=1 을 눌러도 `pending` 이 그대로 1 이었다.
⇒ `"[처리됨"` 으로 고쳤다. **문자열 표식과 판정식을 따로 쓰면 이렇게 어긋난다.**

---

# 🔴🔴🔴 [2026-08-03] 절차 2개 — 오늘 둘 다 실사고로 배웠다

## ① 함수 안에 게이트를 넣기 전에 **호출 경로를 먼저 확인한다**
```
ⓒ 를 `_do_triple_ingest` **안**에 넣었다.
그런데 `_multi_collect_one` 의 `raise _SkipOddsparkBridge()` 가
**그 함수 호출 자체를 막아** 영영 도달하지 못했다.
실측: 경마 배당보유 48경주 중 두 소스 공존 **30경주**인데 발동 **0건**.
```
🔴 **상위에서 raise·return 으로 빠져나가면 아래 코드는 영영 안 돈다.**
✅ 고침: `raise` **앞**으로 옮겼다(거기서 `sport`·`category` 가 이미 확정돼 있다).
⚠ `_do_triple_ingest` 안의 같은 판정은 **지우지 않았다** — 확장 `triple_ingest` 등 다른 경로에서는 유효하다.
🔴 **파생 규칙**: 발동 0건인 게이트는 *"조용한 날"* 과 *"도달 못 함"* 이 **구분되지 않는다.**
  ⇒ **자기검증으로 오염 데이터를 주입해 발동을 증명**해야 한다. 발동 기록이 있으면 도달이 증명된 것이다.
  (오늘 카카오 발송 검사는 **5건 발동**으로 도달이 증명됐고, ⓑ·출주명단은 **미증명**이다.)

## ② 판정선은 **실제 실전에 나가는 분모**로 잰다
```
경륜 삼복승 해제(2026-08-01)의 근거 76.6% 는 `shadowWouldBet`(**가상 집합**) 기준이었다.
🔴 실제 판정 명단(`displayedCombos.trifectas`)으로 재니:
    전체      구좌 200 · 회수율 **42.3%** · **3제외 24.3%**
    해제 후만 구좌  86 · 회수율 **17.4%** · **3제외  1.3%**
    날짜별 3제외 0.0 / 0.0 / 19.9 / -0.0% — **한 번도 74.5% 근처에 간 적이 없다**
```
🔴 **가상 집합으로 재고 실전을 결정하지 않는다.**
✏️ 보고 오기 정정: *"해제 후 3제외 17.4%"* 는 틀렸다 — **17.4% 는 회수율**이고 **3제외는 1.3%** 다.
⏳ **다른 판정도 가상 집합 기준인지 전수 확인이 필요하다**(다음 세션 목록).

---


# 🔴 [2026-08-03] app.py 편집 절차 — **Edit 도구로만**
오늘 편집 중 서버가 **1회 사망**했다. 원인은 `python - <<EOF` 셸 힌트로 문자열 치환하며
`"
"` 을 넣었는데 **셸이 실제 줄바꿈으로 바꿔** 문법이 깨진 것이다.
🔴 **app.py 는 Edit 도구로만 고친다. 셸 문자열 치환 금지.**
⚠ 그러면 "한 번의 편집이 그 자체로 문법이 성립한다"가 자동으로 지켜진다.

---



# 🔴🔴 [2026-08-03] **오늘 프로세스 판정에서 두 번 틀렸다** — 절차 신설

## 실측 정정 (대표 확인)
```
(내 판정 ①) "서버 2벌 · 7148 은 부모가 사라진 고아 · 즉시 정리 필요"
(실제)      🟢 7148(부모)+24384(자식) = **Flask 리로더 정상 구조**. 1벌이었다.

(내 판정 ②) "24660 이 11:21 에 LISTEN 을 잡았다 · 배치 가드를 우회한 직접 실행"
(실제)      🟢 **가드는 처음부터 잘 막고 있었다.**
            24660 은 서버가 아니라 **대표가 켠 CMD 창의 파이썬**이었다.
```
🔴 **두 번 다 "확인 없이 판단"했고, 두 번째는 첫 오판 위에 쌓았다.**
  ①은 자식 프로세스를 안 봤고, ②는 그 프로세스의 **명령줄**을 안 봤다.

## 🟢 과거 2벌 구간 — **7/30 13:30~13:59 (29분) 단 한 번**
- 와카야마 8R **19틱 소실은 7/30 09:44** 로 **그 구간 밖**이다.
  ⇒ 🔴 **2벌이 원인이 아니다.** 원인은 이미 규명된 **스레드 간 tmp 충돌**이다(같은 프로세스 안에서 난다).
- ⚠ 잔존 tmp PID 8종을 근거로 "여러 번 2벌이었다"고 보면 안 된다 — **리로더 부모·자식이 각각 PID 를 쓴다.**

---


## 🔴 [2026-08-01] 서버 종료 절차 — **스크립트로 만들었다**(사람 절차는 잊는다)

### 오늘 서버 사망 3회 · 원인 · 결손
| | 구간 | 길이 | 원인 | 데이터 결손 |
|---|---|---|---|---|
| ① | 07:03:20 → 07:49:31 | **46분** | `app.py` 를 나눠 고치는 사이 리로더가 **중간 상태**를 읽음 | **0** (첫 수집 08:20:05) |
| ② | 07:49:52 → 07:51:14 | 1분 22초 | 동상(`return jsonify({` 와 `})` 를 다른 편집으로 분리) | **0** |
| ③ | 11:39:48 → 11:40:28 | 40초 | 🔴 **내가 `Stop-Process` 로 죽였다** — 재기동으로 **PID 가 바뀌어** 있었는데 "아까 확인한 PID"를 제외했다고 착각 | **0** (그 시각 발주 없음) |

### `tools/kill_safe.py`
```
① netstat 으로 **지금 이 순간** 8011 LISTEN PID 를 구한다 (⚠ 캐시하지 않는다)
② 그 PID 는 무조건 종료 대상에서 제외
③ `--match` 로 지정한 것만 종료 · `--apply` 없으면 아무것도 안 함
④ 종료 직전 **한 번 더** 서버 PID 를 확인(그 사이 재기동 대비)
```
🔴 **절차: `Stop-Process`/`taskkill` 을 직접 쓰지 말고 이 스크립트를 쓴다.**
  직접 써야 한다면 **그 직전에 `netstat -ano | findstr :8011` 을 다시 본다.**
  "아까 확인한 PID"는 재기동이 있었으면 **이미 낡은 값이다.**

---

## ⚠ [2026-08-01] 서버 사망 2회 — **데이터 결손 0건** · 편집 절차 확정

| | 구간 | 길이 |
|---|---|---|
| 1차 | 마지막 요청 **07:03:20** → 재기동 **07:49:31** | 약 **46분** |
| 2차 | **07:49:52** → **07:51:14** | 약 **1분 22초** |

### 🟢 데이터 결손 = **0건**
오늘 첫 배당 수집 틱이 **08:20:05**(첫 발주 08:30·아오모리 1R)이다. **다운타임 구간에 수집 대상 경주가 없었다.**
08:20:05~09:09:44 사이 **5분 초과 공백 0건**(틱 213개) — 재기동 이후 수집도 정상이다.
⚠ **미해결**: 07:03~07:49 구간에 내가 성공적으로 호출한 API 기록이 **어느 로그에도 없다.**
  서버가 두 벌 떠 있었을 가능성(중복 기동)이 남는다. **로그로 완전히 재구성하지 못했다 — 그대로 남긴다.**

### 🔴 원인과 절차
원인은 문법 실수가 아니다. **`app.py` 를 두 번에 나눠 고치는 사이**, Flask 디버그 리로더가
**중간 상태 파일**을 읽어 `SyntaxError` 로 종료했고 **스스로 다시 올라오지 않았다.**

**절차(이제부터 지킨다)**
1. `app.py` 편집은 **한 번의 편집이 그 자체로 문법이 성립하도록** 쪼갠다.
   (❌ `return jsonify({` 와 `})` 를 서로 다른 편집으로 나누는 것 — 이번 2차 사망의 원인)
2. 편집 직후 **반드시** `python -c "import ast;ast.parse(open('app.py',encoding='utf-8').read())"` +
   **HTTP 생존 확인**(`/api/snapshot/health` 등)을 한다. 죽었으면 즉시 재기동한다.
3. ⚠ **"문법이 지금 맞다"와 "리로더가 중간에 본 것"은 다른 문제다.** 문법 확인만으로는 부족하다.
⚠ **자동 재기동 감시**(죽으면 다시 띄우는 워치독)가 가장 확실하나 **승인 사항**이라 넣지 않았다.

---

## CHANGELOG 자동 관리
**새 기능 추가 / 버그 수정 / 보완 작업을 할 때마다 `CHANGELOG.md`를 함께 갱신한다(잊지 말 것).**
- **위치**: 파일 최상단의 **현재 최신 버전 섹션**(`## vX.Y.Z (날짜) — 제목 · 현재 최신`) 안의 해당 소제목에 한 줄 추가.
  - 새 기능 → `### 추가된 기능` / 버그 수정 → `### 수정된 버그` / 남은 과제 → `### 보완점`
- **새 버전 승격 기준**: 큰 기능 묶음/마일스톤이면 새 `## vX.Y.Z` 섹션을 최상단에 추가(이전 "현재 최신" 표기는 제거)하고, 그 커밋에 `git tag -a vX.Y.Z -m "..."` + `git push origin --tags`.
  - 버전 규칙: 호환 깨짐=MAJOR / 기능 추가=MINOR / 버그·문서=PATCH.
- **커밋 관례**: 코드 변경 커밋에 `CHANGELOG.md` 갱신을 **같은 커밋**에 포함. 문구는 커밋 메시지와 일치시킨다.
- 되돌리기 안내는 `README.md` "🔄 버전 관리 & 되돌리기" 섹션 유지.

## 세션 시작 자동 실행
1. `git pull`로 최신 코드 확인
2. 현재 보완 필요 항목 파악
3. 파악 후 대기

---

## 🧭 [2026-07-30] 위임 범위 · 세션 규칙 (권대표 지시)

> **배경**: 대표가 매 단계 라우터가 되는 상태를 줄인다.
> CLAUDE.md **자체 검증 원칙 10개**가 이미 작동하고 있으므로(2026-07-30 하루에만
> **자기 정정·중단 5회 실증**: 회수율 계산식 오류 자체 발견 → `payouts`→`payouts_raw` 재계산 /
> 나고야 3R 전제 3회 정정 / `raceGrade` 성적 "측정 불가" 자진 철회 /
> 잘린 지시를 추측으로 확정하지 않고 확인 요청 / 중복 지시 재실행 거부),
> 아래 범위는 **승인 없이 진행**한다.

### ✅ 승인 없이 진행
| 범위 | 조건 |
|---|---|
| **리플레이 · 측정** | **읽기 전용** — 저장·수정 없음 |
| **관측 배선** | **추천 경로 무개입** |
| **문서 갱신 · 커밋 · 푸시** | — |
| **발견 사항 기록** | **보류 목록에만** 적는다. **변경은 하지 않는다** |

### 🔴 승인 필요 (변경 없음 = 손대지 않는다)
- **추천 경로**: `_final_picks` · **EV필터** · `_apply_pace_analysis` · `_scenario_plan` · `_profit_tier_of`
- **파일 삭제** · **프로세스 종료** · **서버 바인딩**
- **완료선 변경** (⚠ **사후 하향 방지** — 바꾸려면 근거와 함께 체크리스트 표를 먼저 고친다)

### 📏 세션 규칙
1. 🔴 **한 세션 = 한 목표. 새 발견은 목록에만 넣고 쫓지 않는다.**
   (2026-07-30 하루에 발견이 계속 새 작업을 낳아 크리티컬 패스가 계속 밀린 것에 대한 규칙)
2. 🔴 **"이게 더 급하다"는 판단을 세션 중에 하지 않는다.**
   **세션 끝에 우선순위만 제안하고, 다음 세션에서 정한다.**
3. **세션 종료 보고 형식** — 이 3가지만 낸다:
   ① **목표 달성 여부** ② **발견 목록** ③ **다음 목표 제안 3줄**

### ✅ 완료 정의
> **체크리스트가 전부 초록이면 이 단계는 끝이다.**
- 보고 시 **현재 `N/19` 를 함께 보고**한다(고정값이 아니라 그 시점 실측값).
- 완료선은 **사후에 낮추지 않는다** — 바꿔야 할 근거가 생기면 **근거와 함께 기록하고** 바꾼다.
- ⚠ **미구현은 통과가 아니다**(D3). 미구현 항목도 목록에서 빼지 않는다 —
  **19개 중 몇 개가 미구현인지도 진행 상황**이다.

### 🎯 다음 세션 목표 — **하나만**
🔴 **마감 시점 단일 동결** — 설계안 제출 → 승인 → 구현
> **완료되면 처음으로 "회원이 실제로 본 등급"으로 성적을 잴 수 있다.
> 그 전의 모든 등급 측정은 무의미하다.**
- 핵심은 `readonly` **타이밍** — 마감 후가 아니라 **마감 시점**에 걸어야 한다.
- `timeline_snapshot` 이 이미 T-5/T-7 값을 담고 있으므로 **새로 만드는 게 아니라 정식 판정으로 승격**.
- 대상: `raceGrade` · `strong_signals` · `betGrade` · 화면 API 응답 · `odds_history.analysis`
- 부수 결함: `_GRADE_LOCK` **메모리 의존 → 파일 동결**로 전환.

## 🔮 [2026-07-31] Gemini 독립 예측 Phase A/B — 판정선 고정

> **역할 전환**: Gemini 를 **코드 리뷰가 아니라 경주 예측 엔진**으로 쓴다.
> 통계 테이블(`flow_table`)은 셀 단위로만 봐서 *"같은 라인에 선행형이 둘이라 내부 경쟁"* 같은 건 표현되지 않는다.
> ⚠ `GEMINI_REVIEW_ENABLED`(코드 리뷰)는 **끈 채로 둔다 — 재개하지 않는다.**

### 구성
| 단계 | 위치 | 시점 |
|---|---|---|
| **Phase A** 예측 기록 | `_multi_collect_one` 의 전적 수집 **직후**(`_forecast_after_form`) | 수집 창(발주 10분전~2분후) · 경주당 1회 |
| **Phase B** 기계 채점 | `_apply_result_learning` 진입 직후(`_grade_forecast_on_result`) | 결과 확정 이벤트 |
- 🔴 **분석 경로(`_triple_analyze`)에 넣지 않은 이유**: 마감 후 폴링마다 재호출된다.
  2026-07-30 Gemini 하루 **670건**(경주당 7회) 사고가 정확히 그 경로였다.
- 🔴 **완전 격리**: 예측·채점이 실패해도 배당 수집·추천 생성·결과 학습에 **일절 영향 없다**(각 호출부 자체 try).
- 저장 `logs/forecast/<YYYYMMDD>_<경주>.json` · 플래그 `GEMINI_FORECAST_ENABLED`.

### 🔴 3대 금지 입력 (Overfitting 방지 · 실측 검증 완료)
① 배당·인기순위·시장 최저 조합 ② 우리 추천(`finalQuinellas`·`keyHorses`·`axis`)
③ paceBonus **가산된** 점수(`record_score`·`totalScore`) → **가산 전 `paceBonusBase` 만**
- 실측(平塚 2경주): `horses[0]` 키 = `no·name·gait·grade·paceBonusBase·recentPlacings·weeksInARow` — **금지 항목 없음** ✅
- 넣으면 시장·우리 답안을 베껴 **잘 맞아도 새 정보가 아니다**(7/30 "마감 후 유력마" 함정과 동형).

### 🔴 판정선 — **사후에 낮추지 않는다**
| 도달 | 판정 |
|---|---|
| **30경주** | 예측 JSON **형식 점검**. **폐기율 20% 초과 시 프롬프트 수정** |
| **100경주** | **Gemini 평균 `hit_count` ↔ 시장 평균 `market_hit_count`** 대조<br>· **낮으면 종결** · **비슷하면 이변 경주만 재판정** · **높으면 추천 입력 편입 검토** |
- **시장 대조군**: 마감 전(T-8~T-0) 스냅샷의 **단승 최저 3두**. ⚠ **마감 후 배당 사용 금지**(시장이 유리해져 비교 불성립).
- 체크리스트에 **폐기율·실패율** 2항목 추가 → **19 → 21항목**.

### 검증 (2026-07-31 · 실저장 건수로 확인)
`平塚 2경주` 1건 실제 저장 — `top3=[2,4,1] conf=4 · 폐기 0 · 실패 0 · 폐기율 0.0%`
예측 근거: *"선행형이 셋이지만 코멘트에서 2번·5번 대결을 예고 → 특정 라인에 힘이 실림"*
→ **통계 셀로는 표현되지 않는 관찰**이다(이 시스템을 만든 목적 그대로).


---

# ═══ 상시 · 기본·사업 원칙 ═══

---

# 🔴🔴🔴 사업 원칙 — **고배당·중배당이 기본이다** (2026-08-01 권대표 확정)

> **"고배당 중배당을 기본으로 가져가는 건 공식이고 회원들이 원하는 거다."**

## 왜 (근거)
회수율도 중요하지만, **지고 있던 경주가 고배당 한방에 역전되는 쾌감**에 회원이 들어온다.
**전개와 전략, 배당의 흐름으로 고배당을 맞춰줘야만 열광하는 회원이 생긴다.**
저배당만 맞추면 회원이 떠난다.

## 🔴 경마는 경륜과 다르다
| | 구조 | 함의 |
|---|---|---|
| 경륜 | 7~9명 · **라인 · 각질**로 구조가 있다 | 예측 가능성이 높다 → 저배당 수렴이 합리적일 수 있다 |
| 경마 | **11~18두** · 변수가 많다 | **이변이 흔하다** → 같은 로직을 쓰면 안 된다 |
🔴 **경륜 기준 로직을 경마에 그대로 쓰지 않는다.**

## 🔴 이것은 측정 결과와 무관하게 유지되는 **사업 방향**이다
- **회수율 최적화가 저배당으로 수렴하더라도 고배당·중배당 추천을 기본으로 가져간다.**
- 측정은 **"어떻게 고배당을 맞출 것인가"** 를 위한 것이지,
  🔴 **"고배당을 포기할 근거"로 쓰지 않는다.**

## ⚠ 다만 순서는 지킨다
경마 확정배당 보유율 **6.4%** 로는 **어떤 안도 검증 불가**다.
**분모를 채우는 것이 선행 조건**이고, **검증 없이 실전 반영하지 않는다.**
(현황: 확정배당만 없는 경주 **650건** — 채우면 분모 24 → 674건. 아래 절 참조)

## ⚠ 이 원칙의 지위
**앞으로 모든 세션에서 우선순위 판단의 기준이 된다.**
🔴 **회수율만으로 안을 기각할 때, 이 원칙과 충돌하는지 먼저 확인할 것.**
> 예: *"조합 확대는 회수율이 떨어지므로 기각"* → ⚠ 그 안이 **고배당 적중을 늘리는 것**이었다면
> 회수율 하락만으로 기각하면 안 된다. **고배당 적중 건수·배당 분포를 함께 봐야 한다.**

---

## 기본 원칙
1. **기존 기능 절대 삭제 금지** (추가/수정만)
2. **작업 전 현재 파일 구조 확인** 후 진행
3. **한국어로 답변**
4. **각 단계 완료 후 보고**
5. **GitHub 백업 필수** (`git push origin master`)
6. **작업 완료 후 보완점 자동 파악해서 함께 보고**
7. **CHANGELOG 자동 갱신** — 새 기능 추가·버그 수정 시 `CHANGELOG.md` 최신 버전 섹션에 반영(아래 규칙)
8. **CLAUDE.md 자동 갱신 (권대표 지시 2026-07-29)** — **모든 작업 후 반드시 이 파일을 갱신·저장**한다.
   코드 수정뿐 아니라 **파악·조사만 한 경우도 포함** — 문서와 실제 코드가 다르면 그 자리에서 "정정"으로 명시해 남긴다.
   (이 파일이 다음 세션의 유일한 인수인계 문서다. 갱신이 밀리면 다음 세션이 stale한 전제 위에서 잘못 출발한다.)

> 커밋 메시지 끝에 `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`.

## 분석 원칙
- **통합 점수 = 이상감지(배당) 60% + 전적 40%** (`_integrated_grades` 기본값). **50경주+ 누적 시 비교학습으로 자동 조정**(`_learned_integrated_weights`: 이상감지·전적 적중률 우세 쪽으로 ±15%p, 이상감지 0.45~0.75).
- **한국경마**: 복승만 수집 / **일본경마**: 복승+쌍승+전적 (삼복승 **수집만** 제거 v2.1.9 — 추천은 추정배당 보험으로 유지, 삼복승 로직 코드 보존)
- **이상감지**: T-2분 강제 실행 (중앙 JRA)
- **배당 급락**: 30%↑ 경고(🟠) / 50%↑ 강력(🔴)
- **A/B/C/D 등급**: 상위 비율 45:28:17:10
- **배당 급락말 → 삼복승 보험픽 자동 추가**


---

# ═══ 상시 · 참조 ═══

---

## 🗺 주요 파일 지도 (전문은 `docs/ARCHITECTURE.md`)

| 파일 | 역할 |
|---|---|
| `app.py` | Flask 서버 · 분석·판정·수집·학습 전부 (44,004줄 · 함수 1,206개) |
| `review_engine.py` | 리플레이 엔진 — 규칙 변경 전 회수율 사전검증 |
| `static/js/app.js` | 분석기 웹 프론트(5개 탭) |
| `static/index.html` | 분석기 웹 화면 |
| `chrome-extension/content.js` | 배당판 수집(복승·쌍승·삼복승·전적) |
| `chrome-extension/overlay.js` | 배당판 위 실시간 분석 패널 |
| `chrome-extension/background.js` | 서비스워커 · 자동수집 · 결과 수집 |
| `chrome-extension/timer.js` | 카운트다운 + 페이지↔확장 릴레이 |
| `tools/` | 측정 도구 143개 (대부분 읽기 전용) |
| `tests/` | 회귀 테스트 30개 |

🔴 고치기 전에 `docs/codemap/위험목록.md` 에서 그 함수를 먼저 찾는다.

# 경마 BMED 분석기 - 프로젝트 지침

## 정보
- **경로**: `C:\Users\USER\Desktop\경마분석서버`
- **GitHub**: https://github.com/kwonceo/racing-analyzer.git (`origin/master`)
- **서버**: Flask, port 8011 (`python app.py`, `debug=True` 자동 리로드)
- **Chrome 확장**: v2.1.11 (`chrome-extension/`, MV3)
- **안정 체크포인트**: 태그 `v2.3.0-stable` (AI 준비 인프라 완성). 복구는 `RECOVERY.md`.

## 📘 완전 가이드 (한눈 요약)
### 시스템 개요
- Flask 서버 port 8011 + Chrome 확장 v2.1.x + 분석기 웹(5개 탭).
- 일본(복승·쌍승·전적)·한국(복승·PDF전적) 경마 이상감지 분석.
- **AI 학습 데이터 수집 중**(`data/ai_training/`, 목표 500경주 → Phase 2/3).

### 핵심 분석 공식
1. **초과급락** = 말N 평균급락 − 전체평균급락. 절대 **10%+ 급락 → 집중신호(노이즈 아님)** (`_excess_drop_analysis` `ABS_STRONG=-10`).
2. **역전비율** = 쌍승(B→A) / 쌍승(A→B). **<0.95 역전신호** / <0.80 강한 / <0.60 압도적 (`_win_exacta_reversal`).
3. **불일치점수** = 예상최저복승 / 실제최저복승. **1.2+ 주의** / 1.5+ 강한 / 2.0+ 압도적 (`_quinella_mismatch`).
4. **종합 신뢰도** = 초과급락40% + 쌍승역전35% + 복승불일치25% (70+ 🔴 / 40~69 🟡) (`_signal_confidence`).
5. **BMED 전략 5가지**(`_bmed_strategy`): 보험형(이상감지 없음+유력마 명확)·압축형(2두 강한신호)·역배열형(쌍승역전)·분산형(대규모급락)·고배당도전형(강한신호+고배당) + 원금보전 배분·기대환수율 + 보험용 매트릭스(정상/보험 2종).
6. **실시간 고도화**(`_advanced_anomaly`): 급락속도(분당%·간격하한0.25분+절대폭)·연속하락(+20)/단발반등(−15)·페이크베팅·복승 환급률(역수합=`refundRate`, 상위3조합 90%+ `🟠 자금집중`)·**말별 연속하락 `horseStreaks`(1회 후보⚪/2회 약한🟡/3회+ 확정🔴/반등 페이크🟠)**. 단승 급락 = 최우선 신호(`_sh_order` 최상단). 수집 간격 마감임박 T-3분 10초·T-1분 5초.

### 데이터 구조 (`data/`)
```
data/
├── ai_training/     ← AI 학습 핵심(완전 데이터 + 품질점수)  [추적]
├── analysis_log/    ← 분석 로그(패턴학습 코퍼스)        [**미추적**·gitignore]
├── race_results/    ← 경주 결과 완전 저장                   [추적]
├── race_report/     ← 고배당 적중 재현 리포트(추천근거·타임라인) [추적]
├── daily_summary/   ← 일별 자동 요약(YYYY-MM-DD.json)       [추적]
├── pattern_learning.json / discovered_patterns.json ← 패턴 통계 [추적]
├── prerace/ · korea_history/ · korea_session.json ← 한국 PDF/결과 [추적]
└── odds_history/ · triple_store.json · learning.json ← 임시·고빈도 [gitignore]
```

### 알려진 버그 (전부 수정 완료 ✅)
- **노이즈 판정 오류**(초과급락 미반영) → ✅ 절대 10%+ 집중신호 승격(`ABS_STRONG`).
- **한국경마 raceKey 매칭 불일치**(`서울 5`↔`2026-.. 서울 5경주`) → ✅ `_resolve_race_key` 유연 매칭.
- **자동전송 OFF 시 탭 클릭 버그** → ✅ 수집 게이트에서 autoSend/한국모드 우선 체크.
- **신호말 전체 조합 미표시**(147배 놓침) → ✅ `_signal_combo_bets` 신호말 전 조합 추천.
- **마감 오판/첫수집 가짜급락/opening 배당 정착** → ✅ `pageRemainingMs`·워밍업·`_is_opening_settle`.

### AI 개발 로드맵
- **Phase 1 (지금)**: 데이터 수집 — `ai_training/` 완전 구조 + 품질점수(80+ AI학습용).
- **Phase 2 (100경주)**: 데이터 정제 — 패턴 자동 발견 강화.
- **Phase 3 (500경주)**: 모델 학습 — `tools/export_ai_data.py`로 CSV/JSON 내보내 학습.
- **Phase 4 (검증 후)**: AI 통합 — 예측 모델을 분석 파이프라인에 병합.
- 현황·마일스톤은 통계 탭 `🤖 AI 학습 준비 현황` + `GET /api/ai-training/status`.

### 홈서버 운영
- **자동 시작**: `경마서버_자동시작.bat`(서버+배당판 열기) · 시작프로그램 등록 `scripts/register_startup.bat`.
- **자동 백업**: `scripts/backup_checkpoint.bat`(수동) · 매일 자정 자동 `scripts/register_daily_backup.bat`(작업 스케줄러 등록).

## 단축 명령어
- `#보완` → 현재 보완점 파악 후 우선순위 작업
- `#백업` → `git add . && git commit && git push`
- `#상태` → 파일 구조 + 현재 상태 보고
- `#기능` → 완성 기능 목록 보고
- `#변경` → 최근 작업을 `CHANGELOG.md` 최신 섹션에 반영(+ 필요 시 새 버전 태그)

## 명령어 (Commands)
```bash
python app.py                             # 서버 실행 (보통 이미 떠 있음)
pip install -r requirements.txt           # flask, anthropic, PyMuPDF(fitz)

# 문법 검증 (커밋 전 필수)
python -c "import ast; ast.parse(open('app.py',encoding='utf-8').read())"
node --check chrome-extension/content.js  # 확장 JS 각각
node -e "new Function(require('fs').readFileSync('static/js/app.js','utf8'))"  # app.js

# 테스트
node tests/run_stats.js
python tests/run_flow.py
python tests/run_formula.py          # 유력마/제거마 공식 정합성
python tests/run_reversal.py         # 쌍승 역전 다중순위·flip 다중조합
python tests/run_report.py           # 고배당 적중 재현 리포트·신호조합 태깅
python tests/run_prerace.py          # 한국 PDF 전경주 사전분석

# 확장 ZIP 재빌드 (확장 코드 변경 시에만, manifest 버전 bump 후)
cd chrome-extension && python -c "import zipfile,os; zf=zipfile.ZipFile('../chrome-extension.zip','w',zipfile.ZIP_DEFLATED); [zf.write(os.path.join(r,f),os.path.relpath(os.path.join(r,f),'.')) for r,_,fs in os.walk('.') for f in fs]; zf.close()"
```
- **단일 함수 검증**: `importlib.util`로 `app.py` 로드 → 저장소 상수(`UPSET_FILE` 등)를 임시 경로로 monkeypatch → 함수 직접 호출(프로덕션 데이터 오염 방지).
- 한글 본문 POST는 shell curl 대신 python urllib로 테스트(인코딩).

## 시장별 수집 규칙
- **한국**: 복승만. 전적=PDF Vision. **일본**: 단승+복승+쌍승 수집(삼복승 수집 제거 v2.1.9 · **단승 수집 재도입 v2.1.17** — 단승 급락=최강 신호. keiba는 単勝複勝 표 fetch, 사설은 탭 수집). 삼복승 배당 미수집 시 `_triple_analyze`가 삼복승을 `_trio_est` 추정배당 '보험(추정)' 소액(≤18%)으로 유지(`estimated`)·복승 메인에 잔여 배분. 실배당 수집 시 기존 로직.
  - **한국 판정 강화(확장 v2.1.6)**: `isKoreaMode(raceKey, market)` = 종목=한국(팝업) OR raceKey에 KRA 경마장명(`isKoreaByRaceKey`) OR **페이지 본문/URL에 KRA 경마장명+경마맥락**(`pageLooksKorean`, raceKey 추출 실패 대비). true면 복승만·쌍승/삼복승 탭 클릭 완전 스킵(`collectTripleKeiba`·`collectTripleByTabs` 양 경로). 로그 `[한국모드] 복승만 수집 - 쌍승/삼복승 생략`. ⚠ 확장 코드 변경이므로 **브라우저에서 확장 새로고침 필수**(안 하면 구코드가 계속 실행).
  - **지방(NAR, keiba.go.jp)**: 전적표(출마표2/DebaTable) 있음. 마감 T-1분·T-30초.
  - **중앙(JRA)**: 전적표 없음→배당만. 마감 T-1분30초 수집중지, 이상감지 T-2분 강제. 팝업 `japanType` 토글.
- **마감 감지**: "발매마감/締切" DOM 또는 배당 무변동 5틱 → 자동수집 중단.

## 학습 시스템 (결과 입력 시 `_apply_result_learning`에서 갱신)
- **배당 패턴**(`data/learning.json`): 급락50+/급락30+/쌍승역전/배당압축/복승불일치 + 시점. 표본 5회+ 신뢰도로 베팅 비중 조정.
- **부진마 역전**(`data/pattern_learning.json`, `_learn_upset`): 부진마=최근5평균착순≥4.0. 입상 시 급락30%+·복승이상감지 동반 태깅 → condition_stats. 전적 있는 경주만(한국 PDF 즉시). `GET /api/learning/upset`.
- **대규모 급락**(`data/pattern_learning.json`의 `patterns`, `_learn_mass_drop`): 전체 복승 조합 50%+ 또는 30개+ 동시 30%급락(`_mass_drop_detect`, `an.massDrop`) 감지 시 결과 입력마다 사례 축적(고배당율·적중률 `condition_stats["대규모급락"]`). 전략(`_apply_mass_drop_strategy`)=삼복승 보험 8→15%·중배당 복승 보험·최저배당 신뢰도 하락. 히스토리상 반복 패턴(66~84% 동시급락 11경주).
- **패턴 자동 발견**(`data/discovered_patterns.json`, `_discover_patterns`): `data/analysis_log/` 스캔 → 적중 경주 공통점(기준 +12%p·표본 5↑만). 충분도 목표 50경주. `GET/POST /api/patterns/discovered`.
- **원시 데이터**(`data/analysis_log/`): `_analysis_log_save`가 매 분석(30초)마다 배당 타임라인·전적점수·이상감지·결과 완전 저장. 별도 raw 저장소 불필요(재사용).

## 결과 자동수집 (2경로)
- **자동**(발주 후 7/9/11분): `doResultFetch`→`collectResultsByFetch`(video_iframe→/bet/result?id=N, 로그인 세션)→`POST /api/results/auto`. 성공/실패 Chrome 알림.
- **일괄**(마감 후): [일괄 등록]→확장 `FETCH_RESULT_HTML`→`POST /api/results/bulk`(`_parse_result_rows`→`_match_row_to_key` 지역+라운드 매칭→학습). URL 실패 시 HTML 붙여넣기 폴백.

## 데이터 저장소
- 루트: `triple_store.json`(3종 배당+히스토리)·`starters_store.json`(전적)·`results_store.json`(착순).
- `data/`: `learning.json`·`pattern_learning.json`·`discovered_patterns.json`·`analysis_log/`·`odds_history/`·`korea_history/`·`korea_session.json`(PDF 사전분석 세션)·`prerace/`.

### 데이터 커밋 정책 (churn 운영 규칙)
- **워킹트리 churn은 정상**: 라이브 분석 중 서버가 데이터 파일(`analysis_log/`·`korea_session.json`·`discovered_patterns.json`·`prerace/` 등)을 30초 주기로 갱신 → `git status`가 상시 dirty. 이는 **의도된 동작**이며 매 변경마다 커밋하지 않는다.
- **커밋 시점 = 명시적 백업 + 결과 입력 자동 백업**: 서버 백업 함수(`_analysis_log_git_backup`·`_korea_git_backup`, 버튼/엔드포인트) 또는 `#백업`/마일스톤 커밋 외에, **결과 입력마다 `_data_git_backup`(5초 디바운스·pathspec 커밋)이 코퍼스만 자동 add+commit+push**(데몬 스레드·비블로킹). 30초 churn은 여전히 자동 커밋 안 함(결과 입력이라는 명시적 이벤트에만 트리거). 수동 즉시: `POST /api/data/backup` / 통계 탭 `🛡️ 데이터 보호`. ⚠ 위험한 `git reset --hard`는 `scripts/safe_reset.bat`로 실행(실행 전 `backups/data_<ts>/`에 data\ 물리 스냅샷 자동 생성, `backups/`는 gitignore).
- **추적 유지(백업 대상)**: `race_results/`(경주별 완전 저장)·`race_report/`(고배당 적중 재현 리포트)·`ai_training/`(AI 학습 완전 데이터·품질점수)·`korea_session.json`·`korea_history/`·`prerace/`·`discovered_patterns.json`·`pattern_learning.json`·`simulation_db/`(적중왕전개 프로파일). **`dist/`(내보내기 출력)·`highlight_wins.json`은 gitignore.**
- ⚠️ **`data/analysis_log/` 는 추적 제외다(2026-07-20 [C안] 권대표 승인 · 2026-07-29 문서 정정).**
  하루 수십~수백 파일이라 **양쪽 PC 자동커밋 충돌의 주범**이었다(당일 3회 실측). 로컬 파일은 그대로 유지되고
  보존은 **6시간 주기 백업 zip(+구글 드라이브)** 이 담당한다.
  · 종전 이 문서와 `.gitignore` 5행 주석에 "analysis_log 추적 유지"가 남아 실제 상태와 상충했다 → 정정 완료.
  · 🔴 **운영 주의 — 분석 로그를 고치는 작업은 PC마다 따로 돌려야 한다.**
    `analysis_log/`가 git으로 오가지 않으므로, 이 디렉터리를 수정하는 스크립트의 결과는 **그 PC에만 남는다**.
    예: 경륜장 `sport` 오분류 정정 213건(`tools/fix_keirin_sport_tag.py --apply`)은 **서버 PC에서만 적용**됐다.
    → **다른 PC(랩탑 등)에서도 같은 스크립트를 각각 재실행**해야 종목별 통계가 일치한다.
    (스크립트는 멱등이다 — 이미 정정된 파일은 `sport=cycle`이라 대상에서 자동 제외된다.)
- **gitignore(고빈도 임시)**: `triple_store.json`·`starters_store.json`·`results_store.json`·`odds_store.json`·`learning.json`·`odds_history/`·`kra_history.json`·`.claude/`.

## PDF 전경주 사전분석 (한국)
- **아침 1회 업로드 → 전경주 백그라운드 순차 분석 → 경주별 즉시 사용.** `_korea_run_job`(데몬스레드)이 PDF 전 페이지 감지→기수표→경주 그룹핑→경주별 추출+`_do_analyze`. 진행상황 `"분석 중... N/M 경주 완료"`를 `korea_session.json`에 실시간 저장 → 탭 전환/새로고침/서버 재시작에도 지속·재개.
- **경주별 영구 저장**: 완료 즉시 `_prerace_save_race` → `data/prerace/<날짜>_<경마장>_<라운드>.json` + `index.json`. `GET /api/korea/prerace`(목록·경량) / `GET /api/korea/prerace/<key>`(1건 전체·즉시 로드). `/api/korea/reset` 시 `_prerace_clear`로 초기화. 경로조작 방어·index 유실 시 디렉터리 스캔 복구. 검증: `tests/run_prerace.py`.

## ⚠️ 알려진 데이터 제약
- **KRA 실데이터 연동됨**(data.go.kr, `tools/fetch_kra.py`): 현직기수 104명(실 복승률, `static/data/jockeys.json`) + 경주성적 647경주(20260403~0704, `data/kra_history.json`)로 **전적 3건+ 보유마 1,120두** 확보. `kra_horse_summary`로 한국 분석 프롬프트에 실제 전적 주입됨.
- **한국 PDF 전적 정상 작동**(formScore·recentPlacings). 출마표2 파서가 오즈표를 긁어 334행 쓰레기로 한국 전적을 덮어쓰던 버그 수정(`_sanitize_starters` 마번 1~18 중복제거 + 전적 0두가 기존 전적 덮어쓰기 방지).
- **일본 NAR DebaTable recent 파싱**: 코드 안정화 완료(`parseDebaTable` try/catch + `fetchDebaStarters` 2회 재시도로 [] 폴백). 실제 keiba 라이브 경주 최종 검증만 잔여.
- **중앙 JRA 결과 파싱**: 전각숫자(０-９)·전각콜론·1着/2着/3着·複勝·三連複 컬럼 + 완화 헤더 매칭 대응 완료(`_parseResultDoc`·`_parse_result_rows`). 착순 컬럼 부재 시 [] 조기 반환.
- **거리·코스·기수이력 세부는 미수집** → 부진마 학습의 "거리 변경/기수 교체" 조건은 이력 수집 선행 필요(현재 배당 급락·이상감지 동반만 계산). KRA전적의 착순은 확보됨.
  - **제거 공식 거리경험 -15 훅은 배선 완료**(`_elim_score(no_dist_exp)` ← `_elimination`이 `fh.noDistExp` 전달). 거리 이력 수집 시 전적표에 `noDistExp` 플래그만 채우면 자동 활성(현재는 데이터 미수집→감점 미적용). 공식 정합성은 `tests/run_formula.py`가 검증.
  - **거리 수집 준비 완료(`fetch_kra.py`)**: 응답에 거리가 있으면 `rcDist`를 race 레코드+`byHorse`에 담도록 추가(다중 필드명 방어). **단, 현 구독 엔드포인트 `racedetailresult`는 거리 미반환**(필드에 dist 없음), `API299_1`은 500 → **거리 보유 엔드포인트 확정이 선행 조건**. 활성화 잔여: ①거리 엔드포인트 배선(`--dist-url` 패턴) ②현재 경주 거리를 triple/starters 레코드에 저장 ③`byHorse.rcDist`↔현재거리 매칭으로 `noDistExp` 계산·주입. 3계층 모두 갖춰지면 자동 활성.
- KRA 기수통산성적비교 API는 EndPoint 미확정(500) — `--comp-url`로 정확 주소 지정 필요. 통산 핵심 지표는 현직기수정보에 포함.

## 작업 관례
- 확장 변경 시: `manifest.json` 버전 bump + ZIP 재빌드. 서버/프론트만 변경 시: ZIP 불필요(자동 리로드 + 브라우저 새로고침).
- 커밋 전 검증: `node --check`·`import ast`·app.js `new Function(...)` + 가능하면 라이브/합성 단위 테스트.
- 페이지↔서버 통신은 확장(timer.js 릴레이) 경유 — 분석기 웹은 `chrome.runtime` 직접 접근 불가.

---


## 핵심 함수 위치 (`app.py`)
> 라인 번호는 수정 시 밀림. **정확한 위치는 항상 `grep -n "^def <함수명>" app.py`로 재확인**할 것.

| 함수 | 라인(2026-07-28 기준) | 범위 | 역할 |
|---|---|---|---|
| `_confidence_picks` | **6955** | 6955~7075 | 신뢰도 기반 축/후보마 선정 (왕축·strongAxis 판정) |
| `_final_picks` | **7195** | 7195~8279 | **최종 복승/삼복승 확정**. 왕축 강제 로직 ~7870, B라인 그물망 ~7872 |
| `_triple_analyze` | **9230** | 9230~11813 | 분석 총괄 진입점. Gemini 검수 호출 11406/11423 |
| `_history_save_analysis` | **13064** | 13064~13105 | 분석 스냅샷 히스토리 저장 |
| `_build_analysis_log` | **13194** | 13194~13488 | 분석 로그(패턴학습 코퍼스) 문서 생성 |
| `_apply_result_learning` | **16212** | — | 결과 입력 시 학습 연쇄 진입점 |
| `_ev_band_p` / `_ev_bands_update` | 8415 / 8436 | — | EV 추정적중률(학습 표본 50+ 자동 교체) · ⚠ 학습 적용 시 선형보간 우회 |
| `_EV_RESCUE_*` + 복원 블록 | 8395 / EV필터 직후 | — | **12~30배 EV강등분 1개 메인 복원**(2026-07-29, OOS 검증) |

### 🔁 리플레이(회수율 사전검증) — **규칙 변경 전 필수**
- 엔진: **`review_engine.py`**(48KB) · `replay_day(date, stake, keirin_re)`(478행) · 정책 20여종을 나란히 측정.
- 엔드포인트 `GET/POST /api/review/replay` · 리포트 `/review-report?date=YYYY-MM-DD` · 원장 `data/review_replay/<날짜>.json`.
- 데이터 소스: `data/analysis_log/`(추천·후보·타임라인) + `data/race_results/`(착순·확정배당) **동일 파일명 조인**.
- 회수율 비교는 **총투자 동일**(경주당 stake 고정 → 조합 수로 분할) 가정으로 계산해야 원금 노출이 공정하게 비교된다.
  조합당 정액으로 계산하면 조합을 늘리는 안이 자동으로 유리해 보이는 착시가 생긴다.
- ⚠ **in-sample 금지** — 같은 데이터로 곡선을 뽑아 같은 데이터로 검증하면 부풀려진다(이번 실측: in-sample +5.5%p ↔ OOS +0.9%p).
  **날짜로 전·후반 분할**해 후반부(OOS)에서 개선되는지, **종목별로도 모두 개선**되는지까지 확인할 것.
- 🔴 **판정식은 발동률을 먼저 재고, 발동 사례 3건을 눈으로 확인한다(2026-07-30 · 3회 반복 후 신설).**
  판정식이 **의도한 것과 다른 것을 재는** 실패가 하루에 세 번 났다. 발동률이 적정해도 다른 걸 재고 있을 수 있다.
  | 항목 | 의도 | 실제로 잰 것 |
  |---|---|---|
  | ④ 급락 미반영 | "급락말을 놓쳤다" | **"급락말이 많았다"**(발동 77% · z=+0.18) |
  | D 중복·상한 | "2+5 중복 버그" | **"같은 리스트 내 중복"**(0.2%) — 실제 버그는 **리스트 간** 중복(54.8%) |
  | ② B라인 누락 | "2번째 라인이 빠졌다" | ⚠ **정정** — 삼복승 4개+ 조건을 걸어도 54.7%라 **"2번째 라인을 실제로 안 담는 것"이 맞았다**(가설이 틀린 게 아니라 기준이 과했다) |
  **절차**: ①발동률 측정(적정 **5~30%**) → ②발동 사례 **3건을 직접 열어** 의도한 상황인지 확인 →
  ③그 다음에 성적(적중률·회수율·z) 측정. 순서를 바꾸면 엉뚱한 지표를 채택하게 된다.
- 🔴 **리플레이는 '실제 코드 경로를 재현했는지'를 먼저 검증한다.**
  면제·예외 로직(`_lowodds_exempt`·`_mkt2`·EV 면제 토큰 등)을 빠뜨리면 **숫자가 아무리 정교해도 무의미하다.**
  · **실제 사례(2026-07-30 v1→v2 정정)**: 경계 1.8→1.5 시뮬에서 정액 컷에 면제가 없다고 가정해
    "1순위가 mid 로 가면 잘린다"고 계산 → **137.9%** 를 보고했다. 그러나 `_lowodds_exempt` ⓐ가
    **시장 최저배당 조합을 면제**하므로 1순위는 그대로 남고, EV 필터까지 반영하면 실제는 **85.6%**였다.
    전제 하나로 결론이 뒤집혔다(그 사이 "적중 중앙값 1.6→6.6배 개선"이라는 부수 근거도 함께 무효화됐다).
  · **체크리스트**: 시뮬 대상 함수의 ⓐ면제/예외 분기 ⓑ전멸 시 복원(관망 금지) ⓒ상한 캡(`_quinella_target`)
    ⓓ정렬 기준을 코드에서 **직접 읽어** 재현했는지 확인하고, 재현하지 못한 항목은 **결과에 명시**한다.
  · 재현 불가 항목이 있으면 그 시뮬은 **낙관/비관 어느 쪽 상한인지**를 함께 적는다.

## 데이터 경로
| 경로 | 추적 | 용도 |
|---|---|---|
| `data/analysis_log/` | ✅ git | 분석 로그 = 패턴학습 코퍼스 (30초 주기 갱신) |
| `data/ai_training/` | ✅ git | AI 학습 완전 데이터 + 품질점수(80+ 학습용) |
| `data/race_results/` · `data/race_report/` | ✅ git | 경주 결과 / 고배당 재현 리포트 |
| `data/pattern_learning.json` · `data/discovered_patterns.json` | ✅ git | 부진마 이변·자동 발견 패턴 통계 |
| `data/korea_history/` · `data/prerace/` · `data/korea_session.json` | ✅ git | 한국 PDF 사전분석 |
| `logs/gemini_review/` | ❌ 미추적 | **Gemini 자동진단 결과 JSON** (`gemini_reviewer._LOG_DIR`) |
| `triple_store.json` · `starters_store.json` · `results_store.json` | ❌ gitignore | 배당/전적/착순 고빈도 캐시 |
| `data/odds_history/` · `data/learning.json` | ❌ gitignore | 스냅샷·학습 원장 (임시·고빈도) |
| `backups/` | ❌ gitignore | 위험 작업 전 `data/` 물리 스냅샷 |

## Git 워크플로우 (서버PC ↔ 랩탑 2대 운영)
```bash

# 1) 항상 pull 먼저 (서버가 결과 입력마다 자동 커밋+푸시 → 원격이 수시로 앞서감)
git pull --no-rebase origin master


# 2) 코드 수정


# 3) 문법 검증 (커밋 전 필수)
python -c "import ast; ast.parse(open('app.py',encoding='utf-8').read())"
node -e "new Function(require('fs').readFileSync('static/js/app.js','utf8'))"
node --check chrome-extension/content.js


# 4) 커밋 + 푸시
git add -A && git commit -m "..." && git push origin master


# 5) 마일스톤이면 태그
git tag -a vX.Y.Z -m "..." && git push origin --tags
```

### ⚠️ 2대 동시 운영 충돌 대응
- 서버 PC의 `_data_git_backup`이 **결과 입력마다 자동 커밋+푸시** → 랩탑 작업 중 원격이 계속 전진.
- `git pull` 충돌 시: **`data/` 는 최신(=경주 더 진행된) 쪽 채택**이 원칙.
  ```bash
  cp -r data "backups/data_premerge_$(date +%Y%m%d_%H%M%S)/"   # 먼저 물리 백업
  git checkout --theirs -- data/ && git add data/              # 원격(랩탑) 채택 예시
  git commit && git push origin master
  ```
- 채택 전 **반드시 양쪽 내용 비교**(`git show :2:<파일>` = ours / `:3:<파일>` = theirs).
  - 판단 기준: `discovered_patterns.json`의 `races_with_result`가 큰 쪽 = 더 많이 학습된 쪽.
- `app.py`는 보통 자동머지됨 — 충돌 시에만 수동 병합(**절대 한쪽 통째 채택 금지**).

## ✅ [2026-07-30] 완료 조건 체크리스트 — 19항목 (외부 자동 확인)

> **설계 의도**: "감시"가 아니라 **완료 조건 체크리스트**다. 각 항목에 숫자 완료선이 있고 충족되면 목록에서 빠진다.
> **조용해지는 것이 곧 진행 상황이다.** 지금까지 발견이 대부분 육안이었고, 외부에 있으면 0건이 됐다.
> 구현: `tools/health_check.py` · `GET /api/health/checklist`(읽기 전용·UTF-8) · `python tools/health_check.py`

### 🔴 이 체크리스트의 제1 규약 — `denominator`(분모 정의)는 필수 필드다
**분모를 좁히면 가장 실패한 것이 통계에서 사라진다.** 실사고(2026-07-30):
"스냅샷 3틱+"를 **스냅샷 보유 파일만** 분모로 잡으면 **81.6%**, **0틱 경주 123개를 포함**하면 **69.4%**.
애매하면 **넓은 쪽(실패가 포함되는 쪽)** 을 고른다. 좁히는 것은 **대상 자체가 아닐 때만** 허용한다.

### 완료선 (⚠️ 사후 하향 방지 — 바꾸려면 근거와 함께 이 표를 먼저 고친다)
| id | 항목 | 분모 | 완료선 |
|---|---|---|---|
| **D1** | 스냅샷 3틱+ 경주 비율 | `odds_history` 전체(**0틱 포함**)·당일 rolling·**distinct** 틱 | **≥90%** |
| **D2a** | 저장 실패 — **수정 경로**(`_json_atomic`) | 당일 로그 전체(**회전본 포함**) | **0건** |
| **D2b** | 저장 실패 — **미수정 17곳**(`path+".tmp"`) | 동상 | **0건** ← ⓐ 완료 판정 |
| **D3** | schema contract test | `tools/schema_contract.py` 계약 대상 | **통과**(미구현=실패) |
| **D4** | 스키마 드리프트 🔴 필드 보유율 | 당일 `source=="oddspark"` 경주 행 | **≥90%** |
| **D5** | 점수 분해 `rank`·`baseScore` 보유율 | 당일 `analysis_log` horses 전체 행 | **≥90%** |
| A1~A5 | ① 적중왕전개 준비 5항목 | (설계 완료·구현 대기) | — |
| B1~B4 | ② 배당판 오류 4항목 | (설계 완료·구현 대기) | — |
| C1~C4 | ③ 예상·복기 4항목 | (설계 완료·구현 대기) | — |

- **`n<10` 이면 `ok=null`** — 하루 경주가 적으면 n=2 로 100% 가 나와 **rolling 이 스스로를 속인다.**
- **당일 rolling + 누적 병기** — 당일분만 보면 "과거가 오염돼 있다"는 사실이 화면에서 사라진다.
  **리플레이·시뮬레이션은 그 과거 데이터로 돌아가므로** 누적값을 반드시 함께 표시한다.
- **미구현은 통과가 아니다**(D3). 미구현 항목도 목록에서 빼지 않는다 — **19개 중 몇 개가 미구현인지도 진행 상황**이다.

### 📌 rolling 전환이 드러낸 것 — 누적값은 배선 성공을 가린다
| 필드 | 누적(262행) | **당일(22행)** |
|---|---|---|
| `winOdds` | 7.3% | **86.4%** |
| `pop` | 7.3% | **86.4%** |
| `weight` | 7.6% | **90.9%** |
| `surface`·`trackCond` | 0.0% | **0.0%** |
- **배선은 이미 작동하고 있었다** — 누적 7.3% 는 배선 전 과거가 섞인 값이다. D1 과 정확히 같은 구조.
- `surface`·`trackCond` 는 rolling 으로도 0% 이고 **그게 정확한 판정**이다(1계층 재수집이 선행 조건).
  **낮게 나온다고 기준을 낮추지 않는다.**

### 🔒 외부 접근 — 현재 **불가**(설계상 안전, 변경은 승인 필요)
- 바인딩 `127.0.0.1:8011`(`app.py`: `_host = "0.0.0.0" if os.environ.get("PORT") else "127.0.0.1"`).
- 실검증: Tailscale `100.80.114.84:8011` → **연결 실패** · `127.0.0.1:8011` → 200. 8011 전용 방화벽 규칙 **없음**.
- ⚠ 이 서버에는 `.env`(카카오 토큰·API 키 3종)와 `/admin` 패널이 있다. `0.0.0.0` 으로 열면
  Tailscale 뿐 아니라 **공유기 내부망 전체**에, 포트포워딩이 있으면 **인터넷**에도 열린다.
- 권고안(⏸ 승인 대기): `0.0.0.0` 바인딩 + **Windows 방화벽에서 8011 을 Tailscale 대역(100.64.0.0/10)만 허용**.

## 📮 [2026-07-30] 체크리스트 카카오 푸시 — 서버 개방 대신 Push
> 외부 확인을 위해 8011 을 열면 **방화벽이 유일한 방어선**이 되고 리셋 리스크가 있다
> (`.env` 키 3종·`/admin` 존재). → **공격 표면을 늘리지 않고 서버가 밖으로 밀어낸다.**
- 바인딩은 **`127.0.0.1` 유지**(변경 금지).
- `GET /api/health/send_kakao`(미리보기) · `POST`(발송) · **매일 22:00 자동**(`HEALTH_KAKAO_HOUR`).
- 본문 = `summary` 1줄 + **미충족 항목만**. 충족 항목은 넣지 않는다(짧아지는 것이 진행 신호).
- ⚠ **미충족 0개여도 `✅ 전부 충족 N/N` 한 줄은 반드시 보낸다** —
  비어서 안 보내면 "문제 없음"과 "발송 실패"가 구분되지 않는다. **침묵이 이상 신호가 되게 한다.**
- 🔒 기존 추천 픽 카카오(`_kakao_notify_race`·`_kakao_build_message`·`_kakao_rich_message`·
  `_KAKAO_SENT_FILE`)는 **일절 건드리지 않았다.** `_kakao_send_to_me` 만 재사용하고
  이력은 별도 `data/kakao_sent/<날짜>.json`(성공·실패 모두 기록).
- ✅ 실발송 검증 완료(2026-07-30 15:49 · 폰 도착 확인 · 359자).

## 🔴 [2026-07-31] 측정 주기 정정 — **무결성과 성능을 분리한다**
| | 주기 | 이유 |
|---|---|---|
| ① **무결성 감시** | 🔴 **매일·자동** | 사람이 볼 필요 없다. **절대 줄이지 않는다** |
| ② 성능 측정 | 주 1회 | 표본이 필요해 매일 재도 판정 불가 |
⚠ 앞서 "주 1회"로 묶어 제안한 것은 **둘을 섞은 잘못**이었다.

## 🔴 [2026-07-31] 무결성 감시 5항목 신설 (체크리스트 23 → **28**)

> 오늘 큰 결함 셋이 전부 **"우연히 조사하다가"** 잡혔다. 매일 측정이 잡은 게 아니다.

| ID | 항목 | 목표 | 분모 | 첫 측정 |
|---|---|---|---|---|
| **I1** | 확정배당 보유율(종목별) | 90% | 당일 결과확정 경주 | 🔴 **cycle 100% · horse 19%** |
| **I2** | 백업 마지막 실행 경과(시간) | ≤12 | `_periodic_backup_last.txt` | 🟢 0.4시간 |
| **I3** | 데몬 실행시각 스탬프 보유 | 5 | sleep-first 5곳 | 🔴 **1/5** |
| **I4** | 🔴 **오늘분 측정 가능 비율** | 80% | 당일 로그 · 결과+확정배당+마감시각 3종 | 🔴 **70.8%** |
| **I5** | 동명 경주 최대 중복 일수 | 기록만 | `analysis_log` 전체 | ⚠ **최대 10일 · 중복 386종** |

- ⚠ 각 항목에 **"2일 연속 미달이면 조사, 3일이면 위험"** 을 명시했다.
- 🔴 **I4 가 핵심이다** — 축적 중에도 매일 확인되면 **일주일치가 통째로 날아갈 수 없다.**
  첫 측정에서 결손 내역이 바로 나왔다: `확정배당없음 21 · 결과없음 7 · 마감시각없음 0`.


---

# ═══ ⏳ 열린 항목  (닫히면 그 줄을 지운다) ═══

---

## ⏳ ⑧ 남은 것
```
🔴 운영 반영은 **00:10 이후**(마지막 발주 23:30+40분). 리로더가 켜져 있어 편집=재기동이다
🔴 실제 차단률은 계수기 `ml_dup_skip`/`ml_append` 로 **다음 개최일에 실측**(원칙 23)
⚠ 기존 6,463행은 **지우지 않았다**(원칙: 삭제 금지). 정리는 별건·승인 사항
⚠ rk 에 날짜를 넣는 것(47.1% 해결)은 `_canon_rk` 변경이라 소비처가 많다 — 별건
🔴 다변량 461셀 재측정 — 중복 없는 데이터가 쌓인 뒤에
```

---
## ⚠ 별건으로 남기는 것
```
🔴 카톡 「어떻게 봤나」 근거줄 — 전적X 경주 발송 40건 중 **33건이 근거줄 없이** 나갔다.
   ⚠ 이건 **T-5 시점**이라 위 기록 오염과 다른 원인일 수 있다. 미확인.
🔴 우츠노미야(경륜장)가 `sport=horse` 로 태깅돼 2경주가 이 분모에 섞였다.
```

---
## 🔁 재검토 트리거
```
경마 급락 신호의 **구간별 적중 30건**(④ 기준 현재 17~28) — 표본 약 1.3배.
현재 기간이 2026-07-21 ~ 09-01(6주)이므로 **약 2주** 뒤 도달 예상.
그때 `python tools/measure_late_drop_sport.py 2026_0* --vary` 를 **그대로** 다시 돌린다.
🔴 세 구간 모두 3제외 100% + CI 하한 1.0 을 넘으면 그때 대표 승인 사항이 된다.
⚠ 문턱을 더 훑지 않는다 — 오늘 여섯 안이 전부 통과한 것으로 충분하다.
```

## 🔁 재검토 트리거
```
경마 집중도 0.65+ 표본 **1,200경주**(현재 738) 도달 시 기간 3분할을 다시 본다.
🔴 그때도 세 구간 모두 3제외 74.5% 를 넘지 못하면 **이 축은 종결**한다.
⚠ 문턱(0.65)을 옮겨 가며 찾지 않는다 — 2026-08-27 에 8개 축 중 최고를 골라 기각된 이력이 있다.
```

## ⚠ 별건으로 남기는 것
```
🔴 중앙(JRA) D「절반만」 34.3% — 지방의 7배다. 후보를 반만 맞힌다.
   172경주(정제 52)라 판정 불가 · 원인 미확인. 표본이 쌓이면 따로 본다.
🔴 E「제거에 걸림」 47경주 · 정답배당 중앙 **117.1배** — 제거가 초고배당을 자른다.
   ⚠ 8월 전 종목 측정에서 그 말들의 시장순위 중앙이 8위였다(시장도 몰랐다). n=47 판정 불가.
```

---

## 🔁 재검토 트리거
```
대상군(평균 5착↑) **입상 30건 이상**(현재 13) — 경륜 하루 60~70경주라 3~5일.
그때 `python tools/measure_keirin_placings.py` 를 **그대로** 다시 돌린다.
🔴 그때도 엣지가 1.0 아래로 내려가지 않으면 **이 축은 종결**한다.
⚠ 문턱(avg_place >= 5)을 낮춰 가며 찾지 않는다 — 그건 소급 최적화다.
```

## ⏳ 다음 (②③은 승인 사항)
```
② 며칠 쌓아 `measure_recovery` 로 리플레이 — 현행 ↔ keirinPlacings 반영안
🔴 ③ 판정 4단계(적중 30건 · 대박3뺀 · 기간 3분할 · 종목별)를 넘어야 반영한다.
   판정선 74.5% 는 낮추지 않는다.
```

---

# ⏸ [2026-08-31] 경륜 착순 복원 — **측정 완료 · 승인 대기**

## ⚠ 별건으로 남기는 것
```
🔴 `GET /api/odds/triple/analyze` 는 **raceKey 를 안 읽는다**(POST 전용 · 16236행).
   독스트링은 「raceKey를 명시하면 그 경주만」이라 적혀 있는데 GET 으로는 명시할 방법이 없다.
   ⚠ 오늘 내 검증이 여기 걸려 네 경주가 **전부 같은 값**으로 나왔다.
🔴 경륜 `recentPlacings` **44.6%(328/736)가 날짜 문자열**이다(착순 배열이 아니다).
   ⚠ 소비 측(`fact_short`)은 2026-08-28 에 「리스트가 아니면 버린다」로 방어됐다 — 환각은 없다.
   그러나 그 44.6% 에서는 「직전 N착」을 **못 쓴다**. 원인은 파싱 단계이고 미확인이다.
⚠ 완료조건 **7/24** (미충족 1 = D2a · 미측정 16). 미측정 16종은 측정 함수 자체가 미구현이다.
```

---

# ⚠ [2026-08-31] 별건으로 남기는 것 — `[컷 등급·실시간]` 반복 예외
```
[컷 등급·실시간] 스킵(무시): 'NoneType' object does not support item assignment
app.py 16252 (except 안 print)
```
⚠ **오늘 로그에 반복 등장**한다. 표시 계층(`cutTiers`)이라 판정 피해는 없어 보이나
  **조용히 스킵되므로 배당 컷 등급이 화면에 안 붙는 경주가 있다.** 원인 미확인 — 목록에만 남긴다.

---

## 🔁 재검토 트리거
```
🔴 경륜 마지막 구간이 **83.5%**(3제외 62.4%)로 올라오고 있다 — 추세가 개선 중일 수 있다.
⇒ 9월 경륜 표본이 700경주 이상 쌓이면 `tools/measure_kakao_cut.py` 를 그대로 다시 돌린다.
  ⚠ 그때도 **판정선 74.5% 와 3제외를 낮추지 않는다.**
```

---

## ⏳ 다음 개최일에 볼 것
```
🔴 한국 경주의 배당판 틱 보유율이 **41% 에서 오르는가**(8/29 = 7/17)
🔴 `korea_private_first` 실발동 · 중간점검 「한국 배당정지」가 내려가는가
```

---

## ⏳ 다음 개최일에 볼 것
```
🔴 `korea_private_first` **실발동**(오늘 5건은 검증 주입분이다 — 원칙 23)
🔴 중간점검 「한국 배당정지」가 아침에 걸리는가 · 고친 뒤 0 으로 내려가는가
🔴 한국 성적 재측정 — 적중 0/88 이 배당 오차 탓이었는지는 **정확한 배당으로 다시 쌓아야** 안다
⚠ private 은 대표가 배당판 탭을 **열어둔 경주에서만** 들어온다(8월 46경주 중 20 = 43%)
  ⇒ 운영: 한국 개최일에 배당판 탭을 열어두면 그 경주만 정확해진다
```

---

## ⏳ 다음 개최일에 볼 것
```
🔴 계수기 `kakao_trio_official` — 실제로 정식으로 나가는가(원칙 23)
🔴 삼복승 적중 시 회원 반응 · 성적은 **복승과 따로 잰다**(회수율 도구는 복승만 센다)
```

---

## ⏳ 다음 개최일에 볼 것
```
🔴 계수기 `late_drop` reach·fire — 실제로 발송되는가(원칙 23 · 배선했다고 도는 게 아니다)
🔴 `logs/late_drop/<날짜>.jsonl` 적재 · 결과가 나오면 **따로 성적을 잰다**
⚠ 회원이 살 시간이 **2분**이다 — 실효성은 실전 반응으로 본다
⚠ 경륜은 아직 안 켰다(하한 0.874). 경마 실적을 본 뒤 판단한다
```

---

## ⏳ 다음 — 대표 판단 사항
```
ⓐ T-2 알림을 여는가 — ⚠ 회원이 살 시간이 **2분**이다. 실효성은 대표 판단이다
ⓑ 연다면 **경주당 0.70개**(진성+급락25%+10~80배)로 좁혀서 낸다 — 기준 3개를 크게 밑돈다
⚠ 경륜은 하한 0.874 로 미달 — 켠다면 **경마부터**가 근거에 맞는다
```

---

## ⏳ 남은 것
```
🔴 **한국 점수 반영은 아직 안 했다** — 배당이 기수를 이미 반영하므로
   **시장 위에 정보를 더하는지 먼저 재야 한다**. 안 재고 넣으면 record_score 와 같은 결과가 난다.
   ⇒ 오늘 저장분이 쌓이면(적중 30건 · 3~4주) `measure_score_edge` 와 같은 설계로 잰다.
⚠ 일본도 소급 불가 — 오늘부터 쌓인다.
```

---

## ⏳ 남은 것 — **저장은 됐고 점수에는 아직 안 들어간다**
```
🔴 한국 `jockeyRate` 0.0% 는 그대로다. 보존은 됐으나 **점수 반영은 별개 배선**이다.
   ⚠ 일본 경마는 `jkbonus = _jockey_rate_bonus(...)` 로 **이미 들어간다**(경륜은 기수 개념이 없다).
   ⇒ 한국만 빠져 있다.
⚠ 반영하기 전에 **시장 위에 정보를 더하는지 먼저 재야 한다** — 오늘 확인했듯
  배당은 기수를 이미 상당히 반영한다. 안 재고 넣으면 `record_score` 와 같은 결과가 난다.
🔴 소급 불가 — 오늘부터 쌓인다(한국 주 3일 · 적중 30건까지 3~4주)
```

---

## ⏳ 남은 것
```
🔴 조교사 마크·통과위치가 시장 위에 정보를 더하는가 — **오늘부터 쌓여야 잰다**
   (표본 기준: 한국은 주 3일 · 경주당 10두 → 적중 30건까지 3~4주)
🔴 한국 경주당 조합 수는 **대표 판단 사항**이다 — 3개로는 원리적으로 안 되고(45조합의 6.7%),
   10개는 회수율 100% 미만인데 원금이 3.3배가 된다
```

---

## ⏳ 남은 것
```
⚠ 기수 교체·마체중은 **오늘 이후 경주에만** 나온다(소급 없음) — 다음 개최일 확인 대상
🔴 회원 화면 경기장 한자 표기(「小松島 8경주」) · 경륜 recentPlacings 63.7% 날짜 오염
```

---

## ⏳ 남은 것
```
🔴 화면(app.js)에 예상문 띄우기 — API(`/api/race/preview`)는 됐고 표시가 없다
🔴 경륜 recentPlacings 날짜 오염 63.7% — 원인 추적(별건)
⚠ 기수 변경·마체중 증감은 **과거값이 로그에 없어** 아직 문장에 못 쓴다
```

---

## ⏳ 남은 것
```
🔴 화면·카톡 배선 — 지금은 커맨드라인 도구다. 회원에게 나가지 않는다
⚠ 「경험 많은 기수로 변경」 — `jockey_change_bonus`(kbonus)가 이미 있는데 문장에 안 썼다
⚠ 마체중 증감·상3F 비교 — 저장돼 있는데 아직 문장으로 안 만들었다
⚠ 문장 끝 마침표 누락 · 경륜은 `recentPlacings` 가 비어 prev1 파싱에 의존한다
```

---

# 🟢🟢 [2026-08-28] 자동 백업 **자동 발동까지 확인** — 확인 목록 M 종결

```
c8105d75a · e825326a3   「결과 자동백업: 이토 5경주 2-5-3」
⇒ 서버가 **결과 입력 시 스스로** 커밋했다. 수동(679파일)에 이어 자동도 실증됐다
```

## ⏳ 다음 개최일에 볼 것
```
🔴 odds_twin_reach / nocand / nomatch 로 오염 가드 침묵 원인 확정
🔴 failure_frozen_learn 이 미적중 경주마다 뜨는지(실전 반영 확인)
⚠ D2b(계측 신뢰성)가 초록일 때만 A 의 숫자를 믿는다
⚠ J 는 n=15 라 판정 불가 — 하루 더 본다
```

---

## ⏳ 다음 (대표 승인 순서 C → A → B)
```
🟢 C 완료 — 데이터가 쌓이는 중(소급 불가 · 오늘부터)
⏳ A 분산 경주(집중도 0.65 미만 · n=311) 측정 — 적중 36%인데 배당이 가장 크다
   집중도별 적중률은 단조: 0.45~0.55 **28.1%** · 0.55~0.65 37.3% · 0.65~0.75 45.3% · 0.75+ **53.3%**
   ⚠ 집중도가 높으면 배당이 싸다 — 「집중 경주만 사자」는 인기마 사기다(원칙 14)
⏳ B 복기 산출을 tools/auto_finding.py(판정 1·2단계)에 태운다. 🔴 3단계는 대표가 정한다
```

---

## ⏳ 남은 것
```
🔴 오염 의심 16경주 사후 처리 — 그 경주들의 성적·회수율을 인용하지 말 것
🔴 odds_twin_drop 실발동 확인(원칙 23 — 배선했다고 도는 것이 아니다)
⏸ 중단한 3건: 복기 명단 고정 · 경마 시장3두 · 경륜 12~20배
```

---

## ⏳ 남은 것
```
🔴 tmp 공유 나머지 14곳 — **효과를 하루 재고**(D2b 가 616 → 몇 건이 되는지) 그다음에 넓힌다
   ⚠ 그 14곳은 로그에 실패가 안 잡히는 곳들이라 「조용히 지워지는」 쪽일 수 있다
🔴 D2b 분모 — 616 을 28 로 세고 있었다. **계수 기준을 다시 봐야 한다**(원칙 8-C)
```

---
## 🔴 남은 것 — **오염 범위를 아직 안 쟀다**
```
언제부터 쌍승이 복승으로 저장됐는지 모른다. `_tabless` 는 2026-08-14 에 들어갔다
⇒ 저장된 복승이 「양방향 min」인지 가려낼 방법을 찾아야 한다(확정배당 대비 비율이 단서다)
⚠ 그 전에는 8/14 이후 경륜·사설 배당판 경주의 회수율을 **확정값으로 인용하지 않는다**
```

---

## 현재 보완 필요 항목
1. ✅ **KRA 실제 기수 데이터 연동 완료** — 현직기수 104명 실데이터(합성기수 0). `static/data/jockeys.json`.
2. **일본 NAR 전적 수집** — `parseDebaTable` try/catch+2회 재시도로 코드 안정화 완료. 실제 keiba 라이브 경주 검증만 잔여.
3. **결과 페이지 파싱** — `_parseResultDoc`(확장)·`_parse_result_rows`(서버) 전각숫자·着 컬럼·완화 헤더 매칭 보강 완료. 실경주 HTML 라이브 검증만 잔여.
4. ✅ **모바일 화면 최적화 완료** (반응형 CSS).
5. ✅ **실제 투자금액 입력 필드 완료(일괄 포함)** — 단건 결과입력에 `실수령 배당금(원)` 란 추가. 입력 시 서버가 `실수령−투자금`으로 정확 손익 계산(`payout` 파라미터·`record.payout_actual`), 공란이면 확정배당 추정 유지. **일괄 등록도 완료** — `renderBulkSummary`가 경주별 `투자/실수령` 편집 표를 렌더하고 `POST /api/results/adjust`(`_recompute_pnl`)로 저장된 학습 레코드를 in-place 갱신(공란이면 확정배당 추정). 정액 가정은 초기값일 뿐, 경주별 실측 조정으로 학습 통계에 반영됨.
6. **패턴 학습 데이터 축적 중** (결과 입력 쌓여야 통계 산출) — 진행 중.
7. **📌 보류(권대표 지시 2026-07-28 — 나중에 다시 논의) · 교체 보조 조합 자금 배분 UI**
   - **무엇**: `_apply_rec_hysteresis`가 복승 메인 교체 시 이전 조합을 `switchBackup=True`로 2순위 보존하도록 적용됨(커밋 `0aaea857`). 그런데 **화면에 "얼마씩 걸어야 하는지"가 안 나온다** — `budget_allocation`에 `switchBackup` 몫이 반영돼 있지 않음.
   - **왜 중요**: 시뮬 수치는 **총투자 동일·조합당 절반(500/500)** 가정에서 나온 것. 지금처럼 각 1,000원씩 걸면 **투자가 2배**가 된다(회수율은 214.1%로 같지만 원금 노출이 커짐).
   - **근거 데이터**: 막판 복승 메인 교체 210건 중 확정배당 보유 119건 시뮬 —
     현행(교체후만) 투자 119,000 / 손익 **+48,700** / 적중 26 / 회수율 **140.9%**
     안A(교체후·전 분산) 투자 119,000 / 손익 **+135,750** / 적중 47 / 회수율 **214.1%**
     · 놓친 적중 배당 합 341.8(최대 155.6배) vs 잡은 적중 167.7(최대 21.6배) → "하나만 남기는 것"이 손해였음.
   - **주의**: 표본 119건(배당 미확보 91건 제외) · 155.6배 1건의 영향이 큼 · 둘 다 미적중 72건(61%)은 개선 없음.
   - **남은 일**: ⓐ`budget_allocation`에 보조 몫 배분 ⓑ프론트에 보조 조합 금액 표시 ⓒ**삼복승에도 동일 적용 검토**(현재 삼복승 히스테리시스는 '보류'만 하고 교체 시 이전 조합 보존은 미적용).

### 🔁 재검토 트리거 (충족 전에는 경계·전략 재변경 금지)
- **1.5~1.8배 구간 표본 120경주 이상**(현재 **56**) **또는 적중 30건 이상**(현재 **24**)
- 도달 시 **out-of-sample 기간분할 검증**을 실행한다.
- ⛔ 지금 하지 말 것: ⓐ병행안(1순위+2.5배+ 1개) — 계산상 기대수익이 낮아진다 ·
  ⓑ기간분할 — 56경주를 반으로 쪼개면 표본이 더 나빠진다.
- 재현 스크립트 기준: A안(1순위 1조합 = 실전 일치) · 총투자 동일 · `_lowodds_exempt`/EV 면제 토큰 반영.

⏸ 모든 조치안은 **권대표 승인 대기**. ⚠ 추천 경로(`_profit_tier_of`·`_apply_profit_strategy`) 수정 금지.

### 🔁 재검토 트리거
- ⓐ`빠른|7두` 경주 **500경주 이상**(현재 160) 또는 ⓑ`declaredStyle` 보유율 **70% 이상**(현재 4.3%) ·
  ⓒ`빠른|7두 추입+추입` 후반기 표본 **1,000조합 이상**(현재 541)에서 CI 상한이 1.0 미만 유지되는지 재확인.
- 재현 시 반드시 `--split` OOS 를 함께 돌리고, **상위 1·3건 제외 수치를 병기**할 것.


---

# ═══ 최근 일지 (2026-09-05) ═══

---

# 🔴🔴🔴 [2026-09-05] **`late_drop` 반등 판정이 미래 틱을 본다** — 원칙 27 위반. 그래도 축은 산다

> 대표: *「좀 더 강력한 분석을 원한다」* ⇒ 시계열 축을 파다가 **실전 함수의 look-ahead** 를 찾았다.

## 🔴 결함 — `tools/late_drop.py:100`
```python
after = [q.get(c) for m, q in ser if m <= cmb and q.get(c)]   # cmb 이후 = **미래 틱**
```
「진성 vs 페이크」는 2026-08-28 발견의 **핵심**인데 그 판정이 급락 시점 **이후** 값을 본다.
🔴 **실전에서는 이 필터가 사실상 안 걸린다** — 마지막 틱쌍은 `after` 가 1개라 검사를 건너뛴다.
  소급 측정(전체 히스토리)에서만 걸린다 ⇒ **측정이 실전보다 낙관적이었다.**
⚠ 2026-08-31 에 *「전체 히스토리로 재현했다 · 건수가 다를 수 있다」* 고 적어 뒀는데,
  **다른 것은 건수가 아니라 판정 자체**였다.

## 🟢 그런데 실전 재현으로도 **경마는 생존한다**
⚠ 실전 호출을 그대로 재현: **매 폴링(`0<=left<=150`) · 최근 12틱 상한 · 첫 발동에서 잠금**
  · 4,677경주 스캔 · 발동 960경주 · 경주당 1.58개 · 발동 시점 중앙 **T-1.0분**
| 방식 | 신호 | 경주 | 적중 | 엣지 | 엣지 95%CI | 3제외 |
|---|---|---|---|---|---|---|
| 🔴 종전(미래 틱 포함) · 경마 | 849 | 511 | 42 | 1.576 | [1.109,2.048] | 89.2% |
| 🟢 **실전 재현 · 경마** | 937 | 575 | 42 | **1.445** | **[1.034,1.864]** | **82.9%** |
| 실전 재현 · 경륜 | 486 | 335 | 19 | 1.257 | [0.747,1.826] | ⚠판정불가 |
🔴 **look-ahead 효과는 엣지 -8%.** 축을 뒤집지는 않는다 — 유일한 생존 축은 실전 기준으로도 유지된다.
⚠ **재현 못 한 것**: `exclude=finalQuinellas`(이미 나간 조합 제외)를 적용하지 않았다 —
  실전보다 신호가 많다. 절대값이 아니라 **같은 구현 안의 상대 비교**로만 읽을 것(원칙 3).

## 🟡 새 축 C(상관 붕괴) — 방향은 일관, **판정 4단계는 미완주**
`cb` = 이 조합의 변화 − 그 두 말의 다른 조합 변화 중앙값. **급락 틱쌍에서 그 시점 값만으로** 계산.
⚠ 분모 = 실전 재현 경마 신호 937 (cb 산출 100%)
| 구간 | 신호 | 적중 | 엣지 | 엣지 95%CI | 회수 | 3제외 |
|---|---|---|---|---|---|---|
| 현행 그대로 | 937 | 42 | 1.445 | [1.034,1.903] | 104.0% | 82.9% |
| + C ≤ -0.20 | 857 | **42** | 1.554 | [1.087,2.025] | 113.8% | 90.6% |
| + C ≤ -0.25 | 818 | 41 | 1.572 | [1.115,2.067] | 117.5% | 93.3% |
| 🔴 C 가 걸러낼 것 | 80 | **0** | 0.000 | — | 0.0% | 0.0% |
🟢 **문턱을 조일수록 엣지·3제외가 단조 상승하는데 적중은 안 잃는다**(42→42→41).
🔴 **그러나 판정 4단계를 못 넘는다**:
  ① 적중 42 ≥ 30 🟢  ② 3제외 **90.6% < 100% 미달**  ③ 기간 3분할 적중 **12/15/15 → 판정 불가**
  ④ 경마만(경륜 적중 19 판정불가)
⚠ **「걸러낼 80신호 적중 0」을 근거로 쓰지 말 것** — 적중 0건은 원칙 1로 **판정 불가**다.
⚠ 기간3(08-20~)이 가장 나쁘다(엣지 1.376 · 3제외 57.1%) — 하락 추세가 이 축에도 있다.
⇒ **켜지 않는다.** 판정선을 낮추지 않는다.

## ✏️ 이번에 내가 두 번 틀렸다
🔴 ① 처음 측정은 **마감 배당·마감 전 마지막 틱** 기준이라 look-ahead 였다(경마 엣지 1.353).
  그 값으로 「기간3분할·종목별」까지 표를 냈다 — **전부 무효**다.
🔴 ② 그것을 고치려 `--cut=2 --ref=5`(T-5→T-2 창)로 다시 쟀더니 엣지가 **0.994로 소멸**해
  「축이 죽었다」고 볼 뻔했다. **그건 실전 함수와 다른 창을 잰 것**이다(실전은 T-2 안쪽 연속 틱쌍).
  ⇒ 🔴 **원칙 3 — 실제 함수를 import 해서 재기 전에는 죽었다고도 살았다고도 말하지 않는다.**

## 🔧 도구
`tools/extract_ts_features.py` 에 **`--cut=N`(그 시점 이후 틱 제거) · `--ref=N`(변화량 기준 틱)** 추가.
⚠ 이 도구의 창은 실전과 다르다 — **탐색용**이고 판정은 `late_drop.picks` 를 import 해서 한다.


---

# 🔴🔴🔴 [2026-09-05] 「학습 데이터 6,069행」은 **596경주였다** — 측정 기반이 깨져 있었다

> 대표: *「진짜 병목을 풀기 위해 해야할 것들 분석해」*
> ⇒ 방향을 다시 논하기 전에 **재료부터 쟀다. 재료가 깨져 있었다.**

## ① 실태 (⚠ 분모 = `data/ml_training_data.jsonl` 전체 **6,463행**)
```
고유 raceKey        596개
🔴 중복 append      5,867행 = **90.8%**
   같은 경주 최다   **89회**(부산 6경주) · 사세보 4경주 54 · 부산 4경주 52
🔴 raceKey 에 날짜  **0.0%** → 경주일 복원 불가
```
🔴 **2026-09-01 에 「학습 데이터가 완제품으로 6,069행」이라 적은 것은 실제로 596경주였다.**
  마감 후 재분석이 돌 때마다 append 된다 — 「히로시마 3경주 8시간 16분 58회 재분석」이 그대로 89표가 된다.

## 🔴 ② 원인 둘 — 코드에서 확정
```
app.py 24621  "date": time.strftime("%Y-%m-%d")   ← 경주일이 아니라 **입력한 날**
app.py 23768  _RESULT_LEARNED = {}                  ← 중복 방지가 **메모리** · 재기동마다 초기화
```
🔴 **복기(`_review_case_append`)에서 2026-08-27 에 똑같은 date 버그를 이미 고쳤는데 ML 학습셋에는 안 고쳤다.**
  그때 적어 둔 문구 그대로다 — *「늦게 들어온 결과는 분석로그와 조인 불가(원칙 16)」*.
⚠ 원칙 5(같은 헬퍼를 다른 호출부가 다르게 쓰는지 본다)의 새 사례다. **한 곳을 고칠 때 같은 유형을 전수로 훑어야 한다.**

## 🔴 ③ 기준선 표류(48.4→70.6%)는 **「원인 미확인」이 아니라 「규명 불가」다**
```
중복 포함     07: 41.6%  08: 64.2%  09: 78.6%
중복 제거 후   07: 44.8%  08: 72.6%  09: 77.0%   ← 표류가 그대로 남는다
중복 횟수별   1회 64.4% · 2~5회 62.5% · 6~20회 76.7% · 21회+ 65.7%  ← 단조 아님·상관 없음
```
🔴 **중복은 원인이 아니다.** 그런데 `date` 가 입력일이고 raceKey 에 날짜가 없어
  **경주월 기준으로 다시 잴 방법 자체가 없다.** 원인 후보(수집 방식·종목 구성·실제 개선)를 가릴 수단이 없다.
⚠ 경륜 09월 88.1% 는 물리적으로 불가능하다(7~9명 중 3착 이내 = 무작위 상한 약 43%).
  ⇒ `hit_place` 는 **말 입상이 아니라 「그 경주에서 우리 추천이 적중했나」**(`was_hit`)다. 이름이 오해를 부른다.

## 🔴 ④ 그래서 「예측 방향이 막혔다」는 결론을 어떻게 보나
| 근거 | 기반 데이터 | 판정 |
|---|---|---|
| 단변량 6축 생존 0 | analysis_log | 🟢 유효 |
| 능력 22% ↔ 시장 52% | 2,415경주 analysis_log | 🟢 유효 |
| 회피 12유형 통과 2(미달) | 3,949경주 | 🟢 유효 |
| 🔴 **다변량 461셀 OOS 생존 16** | **ml_training_data** | 🔴 **596경주·90.8% 중복 위에서 낸 것 — 재검토 대상** |

🟢 **결론 자체는 안 뒤집힌다(3/4 유효).** 다만 *「다변량도 안 된다」* 는 근거는 약해졌다.

## ✅ ⑤ 고친 것 (사본 app.py · 추가만 · 삭제 0줄)
```
_ml_race_date(rk)   rk 에서 경주일을 뽑는다. 못 구하면 오늘 + **dateSrc='today'** 로 표기
_ml_seen()          (raceKey, date) 집합을 **파일에서 1회 로드** → 재기동해도 살아난다
_ml_append_row      중복이면 append 하지 않고 `ml_dup_skip` 계수 · 통과하면 `ml_append` 계수
저장행 신설 필드     dateSrc('rk'|'today') · raceKeyRaw
```
🔴 **`dateSrc` 가 핵심이다** — 나중에 「이 행의 date 를 믿을 수 있나」를 가릴 수 있다(원칙 24).
⚠ `_review_race_date` 를 그냥 쓰지 않았다 — 그 함수는 `_race_result_id(rk)[1]` 을 쓰는데
  **그것도 rk 에 날짜가 없으면 오늘로 폴백**한다(app.py 19630). `_analysis_log_path` 도 같다(17182).
  ⇒ **경주일을 안전하게 주는 함수가 app.py 에 없다.**

## ✏️ ⑥ 정정 — 「90.8% → 0」은 과장이었다
```
raceKey 고유            596개
(raceKey, date) 고유   5,032개   ← 같은 경주가 **여러 날짜**로 흩어져 있다
⇒ 이 패치가 실제로 차단했을 중복은 1,441행 = **22.3%**
```
🔴 **`raceKey` 만으로 dedupe 하면 안 된다** — rk 에 날짜가 없으면 *다른 날의 같은 경주*
  (매주 같은 경기장 7경주)가 하나로 합쳐져 **정상 데이터가 차단**된다. `(raceKey, date)` 가 맞다.
⇒ rk 에 날짜 있는 52.9% 는 완전 차단 · 없는 47.1% 는 자정 넘기면 통과. **부분 해결이다.**

## 🟢 ⑦ 자기검증 6/6 (원칙 17 — 발동·미발동 둘 다 · 파일 무수정)
```
'2026-09-04 나고야 10경주' → 2026-09-04 · src=rk      🟢
'2026_09_04_나고야_10경주' → 2026-09-04 · src=rk      🟢
'서울 7경주 1200M'         → 오늘 · src=today          🟢 미발동
None · '' 방어             → 오늘 · src=today          🟢
기존 키 → 차단 True  ·  새 키 → 통과 False             🟢 둘 다
```
